# Fortran 2023 9.4.1: Substrings

The canonical catalogue is `doc/catalogues/substring_9_4_1.json`. Effective review state comes from
`Registry.catalogue_review_state("9.4.1")`; source accounting does not
approve cases, canonical relationships or an execution inventory.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Eight base units: p1, R908, R909, R910, C908, p2, p3 and the unnumbered note. This source-only packet records substring syntax, character parent strings, endpoint defaults, length and range plans without approving any fixture.

All facets in this packet are pending source plans. No Fortran test program,
compiler invocation, execution evidence, oracle approval, fixture approval or
coverage claim is supplied here.

<!-- BEGIN GENERATED 9.4.1 -->

### R908: A substring is a parent string followed by an explicit parenthesized substring range

**Source:** 9.4.1 R908, J3/24-007, 18 December 2023, physical PDF151. **Class:** Syntax.

**Definition:** The syntactic form of a substring is exactly parent-string followed by parentheses
containing a substring-range. The substring-range is not optional: a whole scalar
character variable, scalar character component, array element, coindexed named object,
or character constant without the following range is not a substring under R908.

**Diagnostic obligation:** required.

**Facets:** `parent-string-then-range`, `explicit-range-required`, `parentheses-delimit-range`, `whole-parent-not-substring`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.1.3R401;4.2;6.2.2;7.4.4.1;7.4.4.2;7.4.4.3;9.1R901;9.2;9.3;9.4.1R909-R910/C908/p2-p3;9.4.2;9.4.3;9.5.3.1;10.1.12.

### R909: Parent strings are the five scalar character-capable designator or constant forms

**Source:** 9.4.1 R909, J3/24-007, 18 December 2023, physical PDF151. **Class:** Syntax.

**Definition:** A parent-string is one of five alternatives: scalar-variable-name, array-element,
coindexed-named-object, scalar-structure-component, or scalar-constant. Array sections,
whole array names, non-scalar structure components, arbitrary expressions, function
references not otherwise a scalar constant, and parenthesized character expressions are
not additional R909 alternatives.

**Diagnostic obligation:** required.

**Facets:** `scalar-variable-parent`, `array-element-parent`, `coindexed-named-object-parent`, `scalar-structure-component-parent`, `scalar-constant-parent`, `array-section-not-parent`, `expression-not-parent`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;6.2.2;7.4.4.1-.3;8.5.6.1;9.1R901;9.2R903/C903;9.3;9.4.2R911-R913/C909-C920;9.4.3R914/C921;9.5.3.1R917-R925/C924-C931;9.6;10.1.12;19.6.5.

### R910: A substring range is a colon with optional scalar integer endpoints

**Source:** 9.4.1 R910, J3/24-007, 18 December 2023, physical PDF151. **Class:** Syntax.

**Definition:** A substring-range consists of an optional first scalar-int-expr, a required colon, and
an optional second scalar-int-expr. Both endpoints may appear, either endpoint may be
omitted, or both may be omitted, but the colon is always present and any endpoint
expression is scalar integer.

**Diagnostic obligation:** required.

**Facets:** `both-endpoints`, `omitted-start`, `omitted-end`, `both-omitted`, `colon-required`, `scalar-integer-endpoints`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.1.3R401;4.2;7.4.1;7.4.4.1;9.4.1p2-p3;10.1.1;10.1.11;10.1.12;10.1.13.

### C908: The parent string of a substring is character typed

**Source:** 9.4.1 C908, J3/24-007, 18 December 2023, physical PDF151. **Class:** Restriction.

**Definition:** For every R909 parent-string alternative used in a substring, the parent-string shall be
of type character. The constraint is on the parent string, not on the endpoint
expressions or on the value selected by the substring.

**Diagnostic obligation:** required.

**Facets:** `character-variable-parent`, `character-array-element-parent`, `character-component-parent`, `character-constant-parent`, `noncharacter-variable-rejected`, `noncharacter-component-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;7.4.4.1-.3;7.5.1;7.5.2.1;7.5.4.1-.5;9.4.1R909;9.4.2;9.4.3;9.5.3.1;10.1.12.

### S9.4.1-001: Substring value, defaults and length follow the effective starting and ending points

**Source:** 9.4.1 p1-p3, J3/24-007, 18 December 2023, physical PDF151. **Class:** Effect.

**Definition:** A substring is a contiguous portion of a character string. The first scalar-int-expr
supplies the starting point f and the second supplies the ending point l; omitted f
defaults to 1 and omitted l defaults to the parent string length n. Parent characters
are numbered from 1 through n, and the substring contains the characters from f through
l inclusive in sequence. Its length is MAX(l - f + 1, 0), so f greater than l yields a
zero-length substring.

**Diagnostic obligation:** not-required.

**Facets:** `contiguous-portion`, `first-expression-start`, `second-expression-end`, `inclusive-character-selection`, `default-start-one`, `default-end-parent-length`, `length-formula`, `zero-length-when-start-exceeds-end`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 7.4.4.1p1;7.4.4.2p4-p5;7.4.4.3p4-p6;9.4.1R908-R910/C908;10.1.5.5;10.1.12;16.9.122;19.6.5.

### S9.4.1-002: Nonzero substrings require effective endpoints within the parent string numbering

**Source:** 9.4.1 p3, J3/24-007, 18 December 2023, physical PDF151. **Class:** Restriction.

**Definition:** When the effective starting point is not greater than the effective ending point, both
endpoints shall be within the range 1 through n, where n is the parent string length.
The stated range condition is not applied by this sentence to the zero-length case where
the starting point is greater than the ending point. Endpoint values can be compile-time
constants or execution-time expression values, so any future diagnostic plan must
distinguish static numbered constraints from this unnumbered prose restriction.

**Diagnostic obligation:** not-required.

**Facets:** `in-range-nonzero`, `start-before-one`, `end-after-parent-length`, `zero-length-out-of-order`, `runtime-computed-endpoints`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 7.4.4.1p1;9.4.1R910/p2;10.1.11;10.1.12;16.9.122;19.6.5.

<!-- END GENERATED 9.4.1 -->
