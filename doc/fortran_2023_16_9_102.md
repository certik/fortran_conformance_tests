# Fortran 2023 16.9.102: IBCLR

The canonical catalogue is `doc/catalogues/ibclr_16_9_102.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where compound or mixed source units are split.

<!-- BEGIN GENERATED 16.9.102 -->

### S16.9.102-001: IBCLR argument restrictions

**Source:** S16.9.102, J3/24-007, 18 December 2023, physical PDF418. **Class:** Restriction.

**Definition:** I shall be integer and POS shall be an integer value that is nonnegative and less than
BIT_SIZE(I).

**Diagnostic obligation:** not-required.

**Facets:** `IBCLR-I-integer`, `IBCLR-POS-integer`, `IBCLR-POS-nonnegative`, `IBCLR-POS-less-than-bit-size`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations. Bit-producing intrinsic results are observed in bit
context by BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric
interpretation of a result whose leftmost bit may be set. Each positive plan establishes
a non-default state first, uses a non-vacuous companion for zero, .FALSE., blank, or
identity results, and includes a feature mutation such as changing the intrinsic name,
argument order, DIM, MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Exact bit plans use real BOZ actual arguments for BOZ rules and observe bit results only
in bit context. Grep of tests/ and tests/fixtures/*/fixture.json for IALL, IAND, IANY,
IBCLR, IBITS, IBSET, and ICHAR found no existing fixture.json binding in this worktree;
IAND appears only in a Clause 11 DO CONCURRENT reduction fixture source and related test
assertions, so this packet records that coverage without rebinding it.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

### S16.9.102-002: IBCLR result characteristics match I

**Source:** S16.9.102, J3/24-007, 18 December 2023, physical PDF418. **Class:** Effect.

**Definition:** The IBCLR result has the same characteristics as I.

**Diagnostic obligation:** not-required.

**Facets:** `IBCLR-result-same-kind-as-I`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations. Bit-producing intrinsic results are observed in bit
context by BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric
interpretation of a result whose leftmost bit may be set. Each positive plan establishes
a non-default state first, uses a non-vacuous companion for zero, .FALSE., blank, or
identity results, and includes a feature mutation such as changing the intrinsic name,
argument order, DIM, MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Exact bit plans use real BOZ actual arguments for BOZ rules and observe bit results only
in bit context. Grep of tests/ and tests/fixtures/*/fixture.json for IALL, IAND, IANY,
IBCLR, IBITS, IBSET, and ICHAR found no existing fixture.json binding in this worktree;
IAND appears only in a Clause 11 DO CONCURRENT reduction fixture source and related test
assertions, so this packet records that coverage without rebinding it.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

### S16.9.102-003: IBCLR clears exactly bit POS

**Source:** S16.9.102, J3/24-007, 18 December 2023, physical PDF418. **Class:** Effect.

**Definition:** The IBCLR result has I's bit sequence except bit POS is zero, using the integer bit
model of 16.3.

**Diagnostic obligation:** not-required.

**Facets:** `IBCLR-clears-selected-bit`, `IBCLR-preserves-other-observed-bits`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations. Bit-producing intrinsic results are observed in bit
context by BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric
interpretation of a result whose leftmost bit may be set. Each positive plan establishes
a non-default state first, uses a non-vacuous companion for zero, .FALSE., blank, or
identity results, and includes a feature mutation such as changing the intrinsic name,
argument order, DIM, MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Exact bit plans use real BOZ actual arguments for BOZ rules and observe bit results only
in bit context. Grep of tests/ and tests/fixtures/*/fixture.json for IALL, IAND, IANY,
IBCLR, IBITS, IBSET, and ICHAR found no existing fixture.json binding in this worktree;
IAND appears only in a Clause 11 DO CONCURRENT reduction fixture source and related test
assertions, so this packet records that coverage without rebinding it.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

<!-- END GENERATED 16.9.102 -->
