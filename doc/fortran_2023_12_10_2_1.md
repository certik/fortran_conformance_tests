# Fortran 2023 12.10.2.1: Inquiry specifier list

The canonical catalogue is `doc/catalogues/inquiry_specifier_list_12_10_2_1.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, R1231, C1247, C1248, C1249, C1250, C1251, p2, p3, p4, p5. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.10.2.1 -->

### S12.10.2.1-001: Inquiry specifiers may be used by file or by unit unless constrained

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Effect.

**Definition:** Unless otherwise constrained, the inquiry specifiers listed for INQUIRE may be used in
either inquire-by-file or inquire-by-unit forms.

**Diagnostic obligation:** not-required.

**Facets:** `specifier-usable-by-file-form`, `specifier-usable-by-unit-form`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### R1231: An inquire-spec is one of the listed INQUIRE specifiers

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Syntax.

**Definition:** An inquire-spec is one of UNIT/file-unit-number, FILE=, ACCESS=, ACTION=, ASYNCHRONOUS=,
BLANK=, DECIMAL=, DELIM=, DIRECT=, ENCODING=, ERR=, EXIST=, FORM=, FORMATTED=, ID=,
IOMSG=, IOSTAT=, LEADING_ZERO=, NAME=, NAMED=, NEXTREC=, NUMBER=, OPENED=, PAD=,
PENDING=, POS=, POSITION=, READ=, READWRITE=, RECL=, ROUND=, SEQUENTIAL=, SIGN=, SIZE=,
STREAM=, UNFORMATTED=, or WRITE= with the required variable or expression form.

**Diagnostic obligation:** required.

**Facets:** `inquire-spec-unit`, `inquire-spec-file`, `inquire-spec-access`, `inquire-spec-action`, `inquire-spec-asynchronous`, `inquire-spec-blank`, `inquire-spec-decimal`, `inquire-spec-delim`, `inquire-spec-direct`, `inquire-spec-encoding`, `inquire-spec-err`, `inquire-spec-exist`, `inquire-spec-form`, `inquire-spec-formatted`, `inquire-spec-id`, `inquire-spec-iomsg`, `inquire-spec-iostat`, `inquire-spec-leading-zero`, `inquire-spec-name`, `inquire-spec-named`, `inquire-spec-nextrec`, `inquire-spec-number`, `inquire-spec-opened`, `inquire-spec-pad`, `inquire-spec-pending`, `inquire-spec-pos`, `inquire-spec-position`, `inquire-spec-read`, `inquire-spec-readwrite`, `inquire-spec-recl`, `inquire-spec-round`, `inquire-spec-sequential`, `inquire-spec-sign`, `inquire-spec-size`, `inquire-spec-stream`, `inquire-spec-unformatted`, `inquire-spec-write`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### C1247: No INQUIRE specifier appears more than once

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Restriction.

**Definition:** No specifier shall appear more than once in a given inquire-spec-list.

**Diagnostic obligation:** required.

**Facets:** `duplicate-inquire-specifier-rejected`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### C1248: An inquire-spec-list identifies exactly one file or unit

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Restriction.

**Definition:** An inquire-spec-list shall contain one FILE= specifier or one file-unit-number, but not
both.

**Diagnostic obligation:** required.

**Facets:** `inquire-file-or-unit-required`, `inquire-file-and-unit-mutually-exclusive`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### C1249: A positional UNIT in INQUIRE by unit appears first

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Restriction.

**Definition:** In inquiry by unit, if UNIT= is omitted, the file-unit-number shall be the first item in
the inquire-spec-list.

**Diagnostic obligation:** required.

**Facets:** `positional-inquire-unit-first`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### C1250: ID= requires PENDING= in an INQUIRE specifier list

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Restriction.

**Definition:** If an ID= specifier appears in an inquire-spec-list, a PENDING= specifier shall also
appear.

**Diagnostic obligation:** required.

**Facets:** `id-requires-pending`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### C1251: ERR labels in INQUIRE identify same-scope branch targets

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Restriction.

**Definition:** The label in the ERR= specifier shall be the statement label of a branch target
statement that appears in the same inclusive scope as the INQUIRE statement.

**Diagnostic obligation:** required.

**Facets:** `inquire-err-label-branch-target`, `inquire-err-label-same-inclusive-scope`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.2.1-002: INQUIRE on an internal unit causes an error condition

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Effect.

**Definition:** If file-unit-number identifies an internal unit, an error condition occurs.

**Diagnostic obligation:** not-required.

**Facets:** `internal-unit-inquire-error-condition`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.2.1-003: Character inquiry results other than NAME are returned in upper case

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Effect.

**Definition:** When a returned value of a specifier other than NAME= is character, the value returned
is in upper case.

**Diagnostic obligation:** not-required.

**Facets:** `character-inquiry-results-uppercase-except-name`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.2.1-004: INQUIRE error conditions undefine inquiry variables except IOSTAT and IOMSG

**Source:** S12.10.2.1, J3/24-007, 18 December 2023, physical PDF279. **Class:** Effect.

**Definition:** If an error condition occurs during INQUIRE execution, all inquiry specifier variables
become undefined except variables in IOSTAT= and IOMSG=.

**Diagnostic obligation:** not-required.

**Facets:** `inquire-error-iostat-remains-defined`, `inquire-error-iomsg-remains-defined`, `inquire-error-other-specifiers-undefined`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

<!-- END GENERATED 12.10.2.1 -->
