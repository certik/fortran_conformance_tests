# Fortran 2023: 7.5.8 Derived-type values

**Source independently reviewed; all thirteen implementation facets remain pending.**
Effective content-bound status is
`Registry.catalogue_review_state("7.5.8")`. Batch052 records the explicit
source and execution-inventory context adjudications; case and connection
approval are separate.

Source authority is checksum-pinned J3/24-007, 18 December 2023, physical PDF106 (printed92), SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

This registration adds definitions and finite plans only. It creates no
Fortran input, fixture, compiler observation, case/link approval or baseline
entry. Existing named witnesses are reuse candidates, not newly registered
links. Their complete sources, roles, fingerprints and qualifications still
need independent connection review. Existing finite canonical-case and
source-use mechanisms do not automatically approve those connections or
establish broader family/documentary coverage.

The p2 value-set definition is explicitly classified as a normative **definition**, including each fine unit. Its model of possible component-value sequences is not omitted, but it does not justify intrinsic derived-type equality, arbitrary storage encodings, enumeration of all values or a passing empty program. Finite operational witnesses retain the p1 and canonical-operation premises.

<!-- BEGIN GENERATED 7.5.8 -->

### S7.5.8-001: A pointer component contributes its association rather than a copied target value

**Source:** 7.5.8, p1.pointer-component, p1.pointer-association-value; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Effect.

**Definition:** For a pointer component, its component value is its pointer association. A data target's
contents are not a replacement for that association, and a procedure pointer denotes its
procedure association rather than an evaluated function result. Disassociated and
associated states must be distinguished without querying an undefined association. The
operation used to observe or transfer this value retains its own pointer-assignment and
interface conditions.

**Diagnostic obligation:** not-required.

**Facets:** `data-pointer-association`, `array-pointer-association`, `procedure-pointer-association`, `disassociated-pointer-value`.

**Oracle:** The existing canonical S10.2.1.3-024 program was inspected as a reuse candidate: it
establishes named live targets and initialized data, checks association and shared
mutation, then explicitly nullifies before checking disassociation. Any new connection
still needs its own source and current fixture review.

**Oracle limitation:** Read only defined values and independently established allocation/association states.
Keep pointer targets alive, use exact eligible procedure interfaces, guard allocation
before dependent inquiries, and do not dereference null or undefined pointers. A
component value is not an ABI layout or a byte encoding. This paragraph does not
independently prescribe how every operation copies a pointer; the canonical assignment
rules supply that operation. No allocation-address, target-value equality or opaque C
descriptor oracle is authorized.

**Dependencies:** 7.5.4.4/.5;7.5.4.7;9.2.2;10.2.1.3p15/p16 and10.2.2;15.5.2;16.9 ASSOCIATED;19.5.2/19.6.

### S7.5.8-002: An allocatable component's value includes conditional allocation characteristics and data

**Source:** 7.5.8, p1.allocatable-status, p1.allocated-condition, p1.allocated-dynamic-type, p1.allocated-type-parameters, p1.allocated-bounds, p1.allocated-value; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Effect.

**Definition:** An allocatable component contributes its allocation status. When it is allocated, its
component value additionally includes dynamic type, type parameter values, bounds and
data value. The allocated-only qualification does not authorize reading absent data or
inferring an allocation's physical address. Allocation, assignment, inquiry and
defined-assignment operations retain their separately specified semantics.

**Diagnostic obligation:** not-required.

**Facets:** `unallocated-status`, `allocated-status`, `allocated-dynamic-type`, `allocated-type-parameters`, `allocated-bounds`, `allocated-data-value`.

**Oracle:** Use independently established allocation state and operation-specific canonical
evidence. The named existing cases are candidates, not new executions or automatic
approval of this semantic connection.

**Oracle limitation:** Read only defined values and independently established allocation/association states.
Keep pointer targets alive, use exact eligible procedure interfaces, guard allocation
before dependent inquiries, and do not dereference null or undefined pointers. A
component value is not an ABI layout or a byte encoding. This definition is not an
independent deep-copy or deallocation requirement, and it does not promise an allocation
can never fail. Unallocated components do not acquire observable payloads by this
paragraph.

**Dependencies:** 7.2;7.3.2.3p5;7.5.4.2/.3;9.4.5p2, PDF153;9.7.1/.3;10.2.1.3p15/p16;16.9
ALLOCATED/LBOUND/UBOUND;19.6.

### S7.5.8-003: An ordinary component contributes its data value

**Source:** 7.5.8, p1.nonpointer-nonallocatable, p1.ordinary-component-value; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Effect.

**Definition:** For a component with neither POINTER nor ALLOCATABLE, the component value is its data
value. This includes intrinsic data, a nested derived value and array-valued data under
their own type, parameter, shape and definedness rules. It does not make general
derived-type equality intrinsic or override a separately applicable defined assignment.

**Diagnostic obligation:** not-required.

**Facets:** `ordinary-intrinsic-components`, `ordinary-nested-derived-component`, `ordinary-array-components`.

**Oracle:** The existing S10.2.1.3-026 source was inspected: all read members are assigned by name,
and the finite expectations are independently stated. Its actual source qualifications
and observation/review binding must be retained.

**Oracle limitation:** Read only defined values and independently established allocation/association states.
Keep pointer targets alive, use exact eligible procedure interfaces, guard allocation
before dependent inquiries, and do not dereference null or undefined pointers. A
component value is not an ABI layout or a byte encoding. Defined assignment can
legitimately change the destination value; S10.2.1.3-025 is not interchangeable with the
no-matching-binding case. The p2 value-set definition remains fully accounted without a
no-op execution.

**Dependencies:** 7.2;7.3.1;7.5.4.7;10.2.1.3p15/p16;10.2.1.4;19.6.

<!-- END GENERATED 7.5.8 -->

## Complete finite pending plans

### S7.5.8-001

* **`data-pointer-association`** - PENDING - Assess reuse of S10_2_1_3_024_valid's defined scalar TARGET, ASSOCIATED(copy%scalar,scalar) and later shared-target mutation. Keep the actual primary S10.2.1.3-024 and source/phase/review binding; do not copy its program under this owner.
* **`array-pointer-association`** - PENDING - Assess the same canonical case's values(2:6:2) target section, eligible ASSOCIATED comparison and independently observed values(4) mutation. Do not replace association/view identity by equal element values or raw addresses.
* **`procedure-pointer-association`** - PENDING - Assess its explicit transform interface, live module procedure double_value and association comparison plus separately evaluated call result10. A matching integer return alone is insufficient procedure-identity evidence.
* **`disassociated-pointer-value`** - PENDING - Assess its explicitly NULLIFY-established scalar/array/procedure disassociation before the second derived assignment. Do not infer association states from uninitialized storage or require a trap for undefined association.

### S7.5.8-002

* **`unallocated-status`** - PENDING - Review the allocated-to-unallocated and both-unallocated branches of existing S10_2_1_3_029_valid for this connection. Retain actual status observations and no inquiry of absent payloads; its finalization variant is a different operation context, not a substitute.
* **`allocated-status`** - PENDING - Review the successful allocation and guarded status observations in S10_2_1_3_030_valid before linking the allocated branch. No failed allocation or optional-facility absence is a successful component-value observation.
* **`allocated-dynamic-type`** - PENDING - Assess the actual polymorphic component/dynamic-type witnesses in S10_2_1_3_030_valid and S10_2_1_3_032_valid. Verify their exact eligible type guards and allocation state, rather than assuming a declared type proves the allocated dynamic type.
* **`allocated-type-parameters`** - PENDING - Assess the canonical kind/length-parameter observations in S10_2_1_3_030_valid. Bind their real parameter kinds and source characteristics; no arbitrary numeric intrinsic kind or unallocated deferred-parameter inquiry is allowed.
* **`allocated-bounds`** - PENDING - Assess its nonunit-bound witness against allocation/assignment and LBOUND/UBOUND rules. Keep zero-size inquiry conventions separate; do not infer a stored empty-array lower bound from a nonempty example.
* **`allocated-data-value`** - PENDING - Assess independent initialized data observations in S10_2_1_3_030_valid and S10_2_1_3_032_valid. Defined assignment under S10.2.1.3-031 can transform values and must not be silently treated as intrinsic copying.

### S7.5.8-003

* **`ordinary-intrinsic-components`** - PENDING - Assess S10_2_1_3_026_valid's explicitly initialized INTEGER, CHARACTER and LOGICAL members and independent literal checks. Preserve the actual canonical program instead of duplicating its scalar copy example.
* **`ordinary-nested-derived-component`** - PENDING - Assess its named initialization and separate nested scalar member checks with no applicable type-bound defined assignment. No construction-order or shared-oracle cancellation is introduced.
* **`ordinary-array-components`** - PENDING - Assess its two explicitly initialized nested array elements, independent expected member values and later mutation that leaves the source element unchanged. This is finite data-value evidence, not a general ABI representation claim.

## Source accounting

Every original base unit and fine subdivision is classified below. Fine IDs are anchors, not new standard R/C numbers or new executions.

| Source unit | Disposition | Requirement or rationale |
| --- | --- | --- |
| `p1` | structural | PDF106 lines2-6 divide the semantic component-value definition into pointer, allocatable and ordinary branches, retaining the allocated-only condition. |
| `p2` | definition | PDF106 lines7-8 define the value set by possible sequences of the type's component values. This is a normative semantic model, not permission to enumerate arbitrary bit patterns or a new requirement for intrinsic derived-type equality, storage layout or an empty executable. The p1 branches and canonical operations provide the finite observable witnesses; 7.2 and7.5.4.7 retain parameter dependence and component ordering. |
| `p1.pointer-component` | requirements | S7.5.8-001 |
| `p1.pointer-association-value` | requirements | S7.5.8-001 |
| `p1.allocatable-status` | requirements | S7.5.8-002 |
| `p1.allocated-condition` | requirements | S7.5.8-002 |
| `p1.allocated-dynamic-type` | requirements | S7.5.8-002 |
| `p1.allocated-type-parameters` | requirements | S7.5.8-002 |
| `p1.allocated-bounds` | requirements | S7.5.8-002 |
| `p1.allocated-value` | requirements | S7.5.8-002 |
| `p1.nonpointer-nonallocatable` | requirements | S7.5.8-003 |
| `p1.ordinary-component-value` | requirements | S7.5.8-003 |
| `p2.particular-type` | definition | PDF106 lines7-8 define the value set by possible sequences of the type's component values. This is a normative semantic model, not permission to enumerate arbitrary bit patterns or a new requirement for intrinsic derived-type equality, storage layout or an empty executable. The p1 branches and canonical operations provide the finite observable witnesses; 7.2 and7.5.4.7 retain parameter dependence and component ordering. |
| `p2.all-possible-sequences` | definition | PDF106 lines7-8 define the value set by possible sequences of the type's component values. This is a normative semantic model, not permission to enumerate arbitrary bit patterns or a new requirement for intrinsic derived-type equality, storage layout or an empty executable. The p1 branches and canonical operations provide the finite observable witnesses; 7.2 and7.5.4.7 retain parameter dependence and component ordering. |
| `p2.component-values` | definition | PDF106 lines7-8 define the value set by possible sequences of the type's component values. This is a normative semantic model, not permission to enumerate arbitrary bit patterns or a new requirement for intrinsic derived-type equality, storage layout or an empty executable. The p1 branches and canonical operations provide the finite observable witnesses; 7.2 and7.5.4.7 retain parameter dependence and component ordering. |
| `p2.components-of-type` | definition | PDF106 lines7-8 define the value set by possible sequences of the type's component values. This is a normative semantic model, not permission to enumerate arbitrary bit patterns or a new requirement for intrinsic derived-type equality, storage layout or an empty executable. The p1 branches and canonical operations provide the finite observable witnesses; 7.2 and7.5.4.7 retain parameter dependence and component ordering. |

The private-type-identity and real-BOZ interpretation questions remain outside this source-only packet. Source eligibility, implemented coverage and qualified processor execution remain separate gates.
