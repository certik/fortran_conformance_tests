# Fortran 2023 18.5.5.4: CFI_deallocate

The canonical catalogue is `doc/catalogues/cfi_deallocate_18_5_5_4.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted are listed in the catalogue. Fine census units are recorded there for mixed or compound source units.

<!-- BEGIN GENERATED 18.5.5.4 -->

### S18.5.5.4-001: CFI_deallocate has the ISO_Fortran_binding.h deallocation interface

**Source:** S18.5.5.4, J3/24-007, 18 December 2023, physical PDF537. **Class:** Effect.

**Definition:** CFI_deallocate has the interface int CFI_deallocate(CFI_cdesc_t *dv) and deallocates
memory for a descriptor-described object.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_deallocate-interface`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.4-002: CFI_deallocate actual argument must describe an object allocated by Fortran allocation mechanisms

**Source:** S18.5.5.4, J3/24-007, 18 December 2023, physical PDF537. **Class:** Restriction.

**Definition:** The descriptor address shall describe the object, the object shall have been allocated
using the same mechanism as Fortran ALLOCATE, and a pointer object shall be associated
with a target satisfying Fortran DEALLOCATE success conditions.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_deallocate-descriptor-address`, `CFI_deallocate-fortran-allocation-mechanism`, `CFI_deallocate-pointer-target-deallocatable`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.4-003: Successful CFI_deallocate releases storage and nulls base_addr

**Source:** S18.5.5.4, J3/24-007, 18 December 2023, physical PDF537. **Class:** Effect.

**Definition:** Successful CFI_deallocate uses the Fortran DEALLOCATE mechanism and makes the descriptor
base_addr member a null pointer.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_deallocate-success-base-null`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.4-004: Detected CFI_deallocate errors preserve the descriptor

**Source:** S18.5.5.4, J3/24-007, 18 December 2023, physical PDF537. **Class:** Effect.

**Definition:** After a detected CFI_deallocate error, the C descriptor is not modified.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_deallocate-detected-error-preserves-descriptor`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.4-005: CFI_deallocate returns an error indicator

**Source:** S18.5.5.4, J3/24-007, 18 December 2023, physical PDF537. **Class:** Effect.

**Definition:** The result of CFI_deallocate is an error indicator.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_deallocate-result-error-indicator`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

<!-- END GENERATED 18.5.5.4 -->
