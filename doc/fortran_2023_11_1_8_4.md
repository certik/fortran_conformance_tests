# Fortran 2023: 11.1.8.4 IF statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.1.8.4 -->

### S11.1.8.4-001: The IF statement controls one action statement based on one logical expression

**Source:** 11.1.8.4 p1; J3/24-007 PDF p219. **Class:** Effect.

**Definition:** An IF statement controls execution of a single action statement based on a single
logical expression. Grammar and nested-IF restrictions are R1141 and C1148.

**Diagnostic obligation:** not-required.

**Facets:** `single-action-control`, `single-logical-expression-control`.

**Oracle:** Observe only whether the action statement ran.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** 10.1 expressions registered;R1141/C1148 in this section.

### R1141: IF statement syntax is IF, a scalar logical expression, and an action statement

**Source:** 11.1.8.4 R1141; J3/24-007 PDF p219. **Class:** Syntax.

**Definition:** An if-stmt is IF, a parenthesized scalar logical expression, and an action-stmt.

**Diagnostic obligation:** required.

**Facets:** `scalar-logical-if-stmt-expression`, `if-stmt-action-stmt`.

**Oracle:** Syntax only; execution is p2.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or agreement with another compiler.

**Dependencies:** 10.1 expressions registered;10.2 assignment registered.

### C1148: The action statement in an IF statement shall not be another IF statement

**Source:** 11.1.8.4 C1148; J3/24-007 PDF p219. **Class:** Restriction.

**Definition:** The action-stmt in the if-stmt shall not be an if-stmt.

**Diagnostic obligation:** required.

**Facets:** `nested-if-stmt-action-rejected`, `non-if-action-control`.

**Oracle:** Negative is attributed to the explicit shall not.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or agreement with another compiler.

**Dependencies:** R1141 in this section;11.1.8.1 IF construct form in this packet.

### S11.1.8.4-002: Executing an IF statement evaluates its expression and executes the action only when true

**Source:** 11.1.8.4 p2; J3/24-007 PDF p219. **Class:** Effect.

**Definition:** Executing an IF statement evaluates the scalar logical expression. If true, the action
statement executes; if false, it does not.

**Diagnostic obligation:** not-required.

**Facets:** `true-condition-executes-action`, `false-condition-skips-action`.

**Oracle:** Use final action effects, not function-call counts.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** 10.1 expressions registered;10.2 assignment registered.

### S11.1.8.4-003: A function reference in an IF statement condition may affect entities in the action statement

**Source:** 11.1.8.4 p3; J3/24-007 PDF p219. **Class:** Effect.

**Definition:** The execution of a function reference in the scalar logical expression may affect
entities in the action statement. This is local permission text and the broader
same-statement side-effect rule is 10.1.4.

**Diagnostic obligation:** not-required.

**Facets:** `if-condition-function-action-entity-permission`.

**Oracle:** Permission text cannot support a diagnostic by itself and does not require side effects.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** 10.1.4 registered;15 procedure side effects NOT YET REGISTERED;19 definedness NOT YET
REGISTERED.

<!-- END GENERATED 11.1.8.4 -->
