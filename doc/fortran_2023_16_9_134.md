# Fortran 2023 16.9.134: MATMUL

The canonical catalogue is `doc/catalogues/matmul_intrinsic_16_9_134.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.134")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.134 -->

### S16.9.134-001: MATMUL argument restrictions

**Source:** S16.9.134, J3/24-007, 18 December 2023, physical PDF430-431. **Class:** Restriction.

**Definition:** MATRIX_A shall be rank one or rank two and numeric or logical; MATRIX_B shall have the
corresponding numeric or logical type, rank one or two, not form a rank-one/rank-one
pair with MATRIX_A, and have its first or only dimension size equal to the last or only
dimension size of MATRIX_A.

**Diagnostic obligation:** not-required.

**Facets:** `MATMUL-MATRIX_A-rank-one-or-two`, `MATMUL-MATRIX_A-numeric-or-logical`, `MATMUL-MATRIX_B-type-corresponds-to-MATRIX_A`, `MATMUL-MATRIX_B-rank-one-or-two`, `MATMUL-not-both-rank-one`, `MATMUL-inner-dimensions-conform`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for
p5-p6 matrix/formula glyph extraction; catalogue source units retain the inventory
hashes.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

### S16.9.134-002: MATMUL result type and kind follow intrinsic operator result rules

**Source:** S16.9.134, J3/24-007, 18 December 2023, physical PDF430-431. **Class:** Effect.

**Definition:** For numeric arguments, MATMUL result type and kind are determined as for the * operator;
for logical arguments, the result is logical with the kind determined as for the .AND.
operator.

**Diagnostic obligation:** not-required.

**Facets:** `MATMUL-numeric-result-type-kind`, `MATMUL-logical-result-type-kind`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for
p5-p6 matrix/formula glyph extraction; catalogue source units retain the inventory
hashes.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

### S16.9.134-003: MATMUL result shape follows the three rank cases

**Source:** S16.9.134, J3/24-007, 18 December 2023, physical PDF430-431. **Class:** Effect.

**Definition:** MATMUL returns shape [n,k] for matrix-matrix arguments, shape [k] for vector-matrix
arguments, and shape [n] for matrix-vector arguments.

**Diagnostic obligation:** not-required.

**Facets:** `MATMUL-matrix-matrix-shape`, `MATMUL-vector-matrix-shape`, `MATMUL-matrix-vector-shape`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for
p5-p6 matrix/formula glyph extraction; catalogue source units retain the inventory
hashes.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

### S16.9.134-004: MATMUL result values are SUM of products or ANY of conjunctions

**Source:** S16.9.134, J3/24-007, 18 December 2023, physical PDF430-431. **Class:** Effect.

**Definition:** MATMUL elements are SUM of elementwise products for numeric arguments and ANY of
elementwise logical conjunctions for logical arguments in each matrix-matrix,
vector-matrix, and matrix-vector rank case.

**Diagnostic obligation:** not-required.

**Facets:** `MATMUL-matrix-matrix-numeric-values`, `MATMUL-matrix-matrix-logical-values`, `MATMUL-vector-matrix-numeric-values`, `MATMUL-vector-matrix-logical-values`, `MATMUL-matrix-vector-numeric-values`, `MATMUL-matrix-vector-logical-values`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for
p5-p6 matrix/formula glyph extraction; catalogue source units retain the inventory
hashes.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.134 -->
