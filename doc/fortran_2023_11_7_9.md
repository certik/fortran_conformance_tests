# Fortran 2023: 11.7.9 FORM TEAM statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.7.9 -->

### S11.7.9-001: FORM TEAM creates sibling teams under the current team

**Source:** 11.7.9 p1; J3/24-007 PDF p236. **Class:** Effect.

**Definition:** The FORM TEAM statement creates a set of sibling teams whose parent team is the current
team.

**Diagnostic obligation:** not-required.

**Facets:** `form-team-purpose-source-control`.

**Oracle:** This purpose sentence is a team-formation effect, not a single-image proof of team
creation.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.1.5 CHANGE TEAM registered;16 TEAM_TYPE NOT YET REGISTERED.

### R1179: FORM TEAM statement syntax

**Source:** 11.7.9 R1179; J3/24-007 PDF p236. **Class:** Syntax.

**Definition:** A form-team-stmt is FORM TEAM with a team-number, team-variable, and optional
form-team-spec-list.

**Diagnostic obligation:** required.

**Facets:** `form-team-minimal-form`, `form-team-spec-list-form`.

**Oracle:** This grammar rule owns FORM TEAM source form only; team creation and synchronization are
p2/p5.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1180;R1181;R1182;C1186;C1187.

### R1180: Team number syntax

**Source:** 11.7.9 R1180; J3/24-007 PDF p237. **Class:** Syntax.

**Definition:** A team-number is a scalar integer expression.

**Diagnostic obligation:** required.

**Facets:** `team-number-scalar-int-expr-form`.

**Oracle:** This grammar rule owns expression form; the positive value obligation is p2.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 10 expressions registered;7 integer/scalar concepts registered.

### R1181: Team variable syntax

**Source:** 11.7.9 R1181; J3/24-007 PDF p237. **Class:** Syntax.

**Definition:** A team-variable is a scalar variable.

**Diagnostic obligation:** required.

**Facets:** `team-variable-scalar-form`.

**Oracle:** This grammar rule owns scalar variable form; intrinsic module type identity is C1186.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 9 variables registered;16 TEAM_TYPE NOT YET REGISTERED.

### C1186: Team variables have TEAM_TYPE

**Source:** 11.7.9 C1186; J3/24-007 PDF p237. **Class:** Restriction.

**Definition:** A team-variable shall be of type TEAM_TYPE from the intrinsic module ISO_FORTRAN_ENV.

**Diagnostic obligation:** required.

**Facets:** `team-type-control`, `non-team-type-team-variable-rejected`.

**Oracle:** The negative/control plan changes only the declared type of the team-variable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 16 ISO_FORTRAN_ENV TEAM_TYPE NOT YET REGISTERED;R1181 in this packet.

### R1182: FORM TEAM specifiers are NEW_INDEX= or sync-stat

**Source:** 11.7.9 R1182; J3/24-007 PDF p237. **Class:** Syntax.

**Definition:** A form-team-spec is NEW_INDEX = scalar-int-expr or sync-stat.

**Diagnostic obligation:** required.

**Facets:** `form-team-new-index-spec`, `form-team-sync-stat-spec`.

**Oracle:** This grammar rule admits specifier alternatives; p3 owns NEW_INDEX= value restrictions.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 sync-stat in this packet.

### C1187: FORM TEAM specifiers are not repeated

**Source:** 11.7.9 C1187; J3/24-007 PDF p237. **Class:** Restriction.

**Definition:** No specifier shall appear more than once in a form-team-spec-list.

**Diagnostic obligation:** required.

**Facets:** `unique-form-team-spec-list-control`, `duplicate-new-index-rejected`, `duplicate-stat-rejected`, `duplicate-errmsg-rejected`.

**Oracle:** Each negative/control plan changes one duplicated specifier named by the constraint.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1182;11.7.11 sync-stat in this packet.

### S11.7.9-002: Successful FORM TEAM creates teams, requires positive team numbers, and defines team variables

**Source:** 11.7.9 p2; J3/24-007 PDF p237. **Class:** Effect.

**Definition:** Successful FORM TEAM creates a new team for each unique team-number value of active
images; team-number shall be positive; each executing image belongs to the matching
team; and the team-variable is defined with a value identifying that team.

**Diagnostic obligation:** context-dependent.

**Facets:** `unique-team-number-formation-source-control`, `positive-team-number-source-control`, `image-belongs-to-team-source-control`, `team-variable-defined-source-control`.

**Oracle:** Team creation and membership are dynamic team effects. The positive team-number
obligation is recorded without manufacturing a processor-dependent error.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.1.5 CHANGE TEAM registered;11.7.11 STAT= in this packet;16 TEAM_TYPE NOT YET
REGISTERED.

### S11.7.9-003: NEW_INDEX= specifies a positive unique valid image index in the new team

**Source:** 11.7.9 p3; J3/24-007 PDF p237. **Class:** Effect.

**Definition:** The NEW_INDEX= value specifies the executing image index in the new team and shall be
positive, not greater than the team size, and different from every other image in that
team.

**Diagnostic obligation:** context-dependent.

**Facets:** `new-index-image-index-source-control`, `new-index-positive-source-control`, `new-index-within-team-size-source-control`, `new-index-unique-source-control`.

**Oracle:** These are team-formation obligations. Cross-image uniqueness and team size cannot be
portably observed by this single-image suite.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1182 in this packet;16 TEAM_TYPE/team inquiries NOT YET REGISTERED.

### S11.7.9-004: Absent NEW_INDEX= leaves image index processor dependent but valid and unique

**Source:** 11.7.9 p4; J3/24-007 PDF p237. **Class:** Effect.

**Definition:** If NEW_INDEX= is absent, the image index in the new team is processor dependent; it is
positive, within the team size, and different from every other image in the team.

**Diagnostic obligation:** not-required.

**Facets:** `default-new-index-processor-dependent`, `default-new-index-positive-source-control`, `default-new-index-within-team-size-source-control`, `default-new-index-unique-source-control`.

**Oracle:** The processor-dependent choice is permission/latitude; the bounds are normative but
unobservable here without team inquiry and multi-image support.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 16 TEAM_TYPE/team inquiries NOT YET REGISTERED.

### S11.7.9-005: FORM TEAM synchronizes active images and orders segments

**Source:** 11.7.9 p5; J3/24-007 PDF p237. **Class:** Effect.

**Definition:** If FORM TEAM is executed on one image, all active images of the current team execute the
same statement; execution implicitly synchronizes them, delays following segments until
the same statement count is reached, orders preceding and following segments across
images, and undefines the team variable on nonfailed-image errors.

**Diagnostic obligation:** context-dependent.

**Facets:** `form-team-same-statement-source-control`, `form-team-implicit-sync-source-control`, `form-team-segment-delay-source-control`, `form-team-segment-precedence-source-control`, `form-team-variable-undefined-other-error-source-control`.

**Oracle:** These are dynamic multi-image synchronization and error-state effects. They are not
required diagnostics and cannot be portably observed in the current suite regime.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.2 segments UNREGISTERED (parallel);11.7.11 STAT= in this packet;16 TEAM_TYPE NOT
YET REGISTERED.

### S11.7.9-006: STAT_FAILED_IMAGE FORM TEAM otherwise has successful FORM TEAM effects

**Source:** 11.7.9 p6; J3/24-007 PDF p237. **Class:** Effect.

**Definition:** If FORM TEAM assigns STAT_FAILED_IMAGE to the stat-variable, the effect is the same as
successful FORM TEAM except for the STAT= value.

**Diagnostic obligation:** not-required.

**Facets:** `form-team-stat-failed-image-source-control`.

**Oracle:** This paragraph delegates named failed-image status handling to 11.7.11 while recording
FORM TEAM effects under the failed-image status case.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 p5 in this packet;16 STAT_FAILED_IMAGE and TEAM_TYPE NOT YET REGISTERED.

<!-- END GENERATED 11.7.9 -->
