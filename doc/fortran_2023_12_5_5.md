# Fortran 2023 12.5.5: Preconnection - OPEN fixtures

The canonical catalogue is `doc/catalogues/preconnection_12_5_5.json`. This author packet adds finite runtime and diagnostic fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 2 facets in 2 program(s); 2 of 2 facets are represented and 0 remain pending.

<!-- BEGIN GENERATED 12.5.5 -->

### S12.5.5-001: Preconnection connects a unit to a file at program start

**Source:** S12.5.5, J3/24-007, 18 December 2023, physical PDF251. **Class:** Effect.

**Definition:** Preconnection means that the unit is connected to a file at the beginning of execution
of the program.

**Diagnostic obligation:** not-required.

**Facets:** `preconnection-at-program-beginning`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 0 diagnostic
program(s) cover `preconnection-at-program-beginning`. Runtime oracles use controlled
stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.5-002: A preconnected unit may be used in I/O statements without a prior OPEN

**Source:** S12.5.5, J3/24-007, 18 December 2023, physical PDF251. **Class:** Effect.

**Definition:** A preconnected unit may be specified in input/output statements without prior execution
of an OPEN statement.

**Diagnostic obligation:** not-required.

**Facets:** `preconnected-unit-io-without-open`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 0 diagnostic
program(s) cover `preconnected-unit-io-without-open`. Runtime oracles use controlled
stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.5.5 -->

## I/O OPEN fixture derivations

### `preconnected_output_beginning` / `S12.5.5-001`

**Facets:** `preconnection-at-program-beginning`.

12.5.5 p1 says preconnection exists at program beginning; OUTPUT_UNIT writes before any OPEN.

Feature mutations: `output-unit-routed-to-error`.

### `preconnected_without_open` / `S12.5.5-002`

**Facets:** `preconnected-unit-io-without-open`.

12.5.5 p1 permits a preconnected unit in I/O without prior OPEN; WRITE * emits an exact record.

Feature mutations: `star-output-routed-to-error`.

