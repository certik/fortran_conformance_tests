# Fortran 2023: 11.7.10 LOCK and UNLOCK statements

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.7.10 -->

### R1183: LOCK statement syntax

**Source:** 11.7.10 R1183; J3/24-007 PDF p237. **Class:** Syntax.

**Definition:** A lock-stmt is LOCK with a lock-variable and optional lock-stat-list.

**Diagnostic obligation:** required.

**Facets:** `lock-minimal-form`, `lock-stat-list-form`.

**Oracle:** This grammar rule owns LOCK source form only; acquisition effects are p3/p4.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1184;R1186;C1188;C1189.

### R1184: LOCK status specifiers are ACQUIRED_LOCK= or sync-stat

**Source:** 11.7.10 R1184; J3/24-007 PDF p238. **Class:** Syntax.

**Definition:** A lock-stat is ACQUIRED_LOCK = scalar-logical-variable or sync-stat.

**Diagnostic obligation:** required.

**Facets:** `acquired-lock-spec-form`, `lock-sync-stat-spec-form`.

**Oracle:** This grammar rule admits lock status specifiers; C1188 owns duplicates.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 sync-stat in this packet;7 logical/scalar concepts registered.

### C1188: LOCK specifiers are not repeated

**Source:** 11.7.10 C1188; J3/24-007 PDF p238. **Class:** Restriction.

**Definition:** No specifier shall appear more than once in a lock-stat-list.

**Diagnostic obligation:** required.

**Facets:** `unique-lock-stat-list-control`, `duplicate-acquired-lock-rejected`, `duplicate-lock-stat-rejected`, `duplicate-lock-errmsg-rejected`.

**Oracle:** Each negative/control plan changes one duplicated specifier named by the constraint.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1184;11.7.11 sync-stat in this packet.

### R1185: UNLOCK statement syntax

**Source:** 11.7.10 R1185; J3/24-007 PDF p238. **Class:** Syntax.

**Definition:** An unlock-stmt is UNLOCK with a lock-variable and optional sync-stat-list.

**Diagnostic obligation:** required.

**Facets:** `unlock-minimal-form`, `unlock-sync-stat-list-form`.

**Oracle:** This grammar rule owns UNLOCK source form only; unlocking effects are p5.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1186;C1189;11.7.11 sync-stat in this packet.

### R1186: Lock variables are scalar variables

**Source:** 11.7.10 R1186; J3/24-007 PDF p238. **Class:** Syntax.

**Definition:** A lock-variable is a scalar variable.

**Diagnostic obligation:** required.

**Facets:** `lock-variable-scalar-form`.

**Oracle:** This grammar rule owns scalar variable form; intrinsic module type identity is C1189.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 9 variables registered;16 LOCK_TYPE NOT YET REGISTERED.

### C1189: Lock variables have LOCK_TYPE

**Source:** 11.7.10 C1189; J3/24-007 PDF p238. **Class:** Restriction.

**Definition:** A lock-variable shall be of type LOCK_TYPE from the intrinsic module ISO_FORTRAN_ENV.

**Diagnostic obligation:** required.

**Facets:** `lock-type-control`, `non-lock-type-lock-variable-rejected`.

**Oracle:** The negative/control plan changes only the declared type of the lock-variable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 16 ISO_FORTRAN_ENV LOCK_TYPE NOT YET REGISTERED;R1186 in this packet.

### S11.7.10-001: LOCK variables and ACQUIRED_LOCK= variables are independent of status variables and each other

**Source:** 11.7.10 p1; J3/24-007 PDF p238. **Class:** Restriction.

**Definition:** The lock-variable shall not depend on STAT=, ERRMSG=, or ACQUIRED_LOCK= variables; the
ACQUIRED_LOCK= variable shall not depend on the lock-variable, STAT=, or ERRMSG=
variables.

**Diagnostic obligation:** not-required.

**Facets:** `lock-variable-status-acquired-independence-source-control`, `lock-variable-stat-dependence-source-control`, `lock-variable-errmsg-dependence-source-control`, `lock-variable-acquired-lock-dependence-source-control`, `acquired-lock-variable-lock-status-independence-source-control`, `acquired-lock-lock-variable-dependence-source-control`, `acquired-lock-stat-dependence-source-control`, `acquired-lock-errmsg-dependence-source-control`.

**Oracle:** The p1 compound shall sentences decompose into six dependency prohibitions, all recorded
as source-control/program-obligation facets only. No conforming processor diagnostic is
required, and no rejection facet is claimed.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 sync-stat in this packet;19 association/dependence terminology NOT YET
REGISTERED.

### S11.7.10-002: Lock variable state and ownership are defined by default component values and LOCK/UNLOCK/failure history

**Source:** 11.7.10 p2; J3/24-007 PDF p238. **Class:** Effect.

**Definition:** A lock variable is unlocked iff each component has its default value, locked otherwise,
and locked by an image if that image locked it, has not unlocked it, and has not failed.

**Diagnostic obligation:** not-required.

**Facets:** `unlocked-default-components-source-control`, `locked-nondefault-components-source-control`, `locked-by-image-source-control`.

**Oracle:** This paragraph defines lock state and ownership; it supplies context for p3-p7 but no
direct lock-component oracle.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 16 LOCK_TYPE NOT YET REGISTERED;5.3.6 image failure NOT YET REGISTERED.

### S11.7.10-003: LOCK without ACQUIRED_LOCK= locks or waits for the lock variable

**Source:** 11.7.10 p3; J3/24-007 PDF p238. **Class:** Effect.

**Definition:** Successful LOCK without ACQUIRED_LOCK= locks the variable by the executing image, or if
it is locked by another image, locks it after that other image unlocks it.

**Diagnostic obligation:** not-required.

**Facets:** `lock-without-acquired-source-control`, `lock-waits-for-other-image-source-control`.

**Oracle:** Lock acquisition and waiting/contention are dynamic synchronization effects and are not
portable executable oracles in this suite.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.2 segments UNREGISTERED (parallel);16 LOCK_TYPE NOT YET REGISTERED.

### S11.7.10-004: LOCK with ACQUIRED_LOCK= reports true for an unlocked lock and false for another image holding it

**Source:** 11.7.10 p4; J3/24-007 PDF p238. **Class:** Effect.

**Definition:** Successful LOCK with ACQUIRED_LOCK= locks an unlocked lock and defines the logical
variable true, or if the lock is locked by a different image leaves it unchanged and
defines the logical variable false.

**Diagnostic obligation:** not-required.

**Facets:** `acquired-lock-locks-unlocked-source-control`, `acquired-lock-true-local-executable`, `acquired-lock-false-other-image-source-control`.

**Oracle:** The ACQUIRED_LOCK=.true. result for an initially unlocked local lock is implementable in
a single-image coarray fixture by observing the scalar logical variable after LOCK and
contrasting it with a pre-LOCK false control. The lock-state change itself remains
source-control here because this source packet binds no LOCK_TYPE state inquiry, and the
.false. case requires another image.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 16 LOCK_TYPE NOT YET REGISTERED;7 logical variables registered;single-image coarray
fixture support required (for gfortran, -fcoarray=single).

### S11.7.10-005: UNLOCK unlocks successful locks and failed images release locks

**Source:** 11.7.10 p5; J3/24-007 PDF p238. **Class:** Effect.

**Definition:** Successful UNLOCK unlocks the lock variable, and failure of an image unlocks all lock
variables locked by that image.

**Diagnostic obligation:** not-required.

**Facets:** `successful-unlock-source-control`, `failed-image-unlocks-source-control`.

**Oracle:** Unlocking is a lock-state synchronization effect; failed-image release cannot be
portably induced.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 5.3.6 image failure NOT YET REGISTERED;16 LOCK_TYPE NOT YET REGISTERED.

### S11.7.10-006: LOCK/UNLOCK state changes order segments only when locking succeeds

**Source:** 11.7.10 p6; J3/24-007 PDF p238. **Class:** Effect.

**Definition:** A lock variable changes through locked/unlocked states by LOCK, UNLOCK, and image
failure; if UNLOCK on image M is followed by LOCK on image T, segments before the UNLOCK
precede segments after the LOCK; a LOCK that does not lock does not affect segment
ordering.

**Diagnostic obligation:** not-required.

**Facets:** `lock-state-sequence-source-control`, `unlock-next-lock-segment-order-source-control`, `unsuccessful-lock-no-order-source-control`.

**Oracle:** Segment ordering and contention are dynamic multi-image synchronization relations.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.2 segments UNREGISTERED (parallel);16 LOCK_TYPE NOT YET REGISTERED.

### S11.7.10-007: LOCK/UNLOCK local error conditions leave lock and ACQUIRED_LOCK= values unchanged

**Source:** 11.7.10 p7; J3/24-007 PDF p238. **Class:** Effect.

**Definition:** LOCK errors if the lock is already locked by the executing image; UNLOCK errors if the
lock is not already locked by the executing image; on LOCK/UNLOCK error the lock
variable and any ACQUIRED_LOCK= variable are not changed.

**Diagnostic obligation:** context-dependent.

**Facets:** `lock-already-locked-local-error`, `unlock-not-locked-local-error`, `lock-error-state-unchanged-source-control`, `acquired-lock-unchanged-on-error-source-control`.

**Oracle:** The two local error conditions are deterministic and named by 11.7.11; lock-state
preservation remains source-control because LOCK_TYPE state has no independent inquiry
here.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 p7-p8 in this packet;16 STAT_LOCKED, STAT_UNLOCKED, LOCK_TYPE NOT YET
REGISTERED.

<!-- END GENERATED 11.7.10 -->
