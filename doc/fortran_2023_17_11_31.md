# Fortran 2023 17.11.31: IEEE_QUIET_LE

The canonical catalogue is `doc/catalogues/ieee_quiet_le_17_11_31.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 17.11.31 -->

### S17.11.31-001: IEEE_QUIET_LE arguments are real with matching kind

**Source:** 17.11.31 p3; J3/24-007, 18 December 2023, physical PDF499. **Class:** Restriction.

**Definition:** The actual argument A to IEEE_QUIET_LE shall be of type real, and B shall have the same
type and kind type parameter as A.

**Diagnostic obligation:** not-required.

**Facets:** `ieee_quiet_le-argument-a-real`, `ieee_quiet_le-argument-b-same-type-kind`.

**Oracle:** Plans are pending and source-derived. Guarded branch plans name the IEEE inquiry probe,
execute assertions only on supported branches, and record unsupported branches as skips
rather than passes. Exact finite controls use representable values such as -1.0, 0.0,
1.0, 1.5, and 2.0. IEEE_COPY_SIGN, IEEE_IS_NEGATIVE, IEEE_VALUE, IEEE_IS_NAN, and
IEEE_CLASS observer calls carry their own IEEE_SUPPORT_DATATYPE restriction guard before
constructing or observing signed zeros, infinities, or NaNs; NaN rows additionally probe
IEEE_SUPPORT_NAN(1.0), and infinity rows additionally probe IEEE_SUPPORT_INF(1.0). Named
preamble FLAG-HALTING-OFF(flag, x), referenced by each flag row: after
IEEE_SUPPORT_FLAG(flag, x) succeeds, execute the explicit branch "if
IEEE_SUPPORT_HALTING(flag, x): IEEE_SET_HALTING_MODE(flag,.FALSE.); else
IEEE_GET_HALTING_MODE(flag,h), proceed only if h is .FALSE., else skip". Exception-flag
controls clear all supported flags before each exception row and observe each flag in
its own guarded branch.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IEEE comparison execution is guarded by the relevant IEEE_SUPPORT_DATATYPE and, for
IEEE_VALUE/IEEE_IS_NAN/IEEE_CLASS NaN observers, IEEE_SUPPORT_NAN; infinity rows also
require IEEE_SUPPORT_INF. Flag observations are keyed to IEEE_SUPPORT_FLAG(flag, 1.0),
with halting disabled or verified quiet by the named FLAG-HALTING-OFF preamble before
signaling-NaN operations. These sections contain no set/restore round-trip facet;
IEEE_SET_HALTING_MODE(flag,.FALSE.) appears only in the FLAG-HALTING-OFF precondition,
so calibration rule 10 changed-value and restored-original observations are not
applicable here. Argument restrictions and invocation restrictions in module procedure
descriptions are not numbered constraints and therefore carry no local diagnostic facet.
Halting .TRUE. behavior is not exercised.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, logical
observation, and intrinsic inquiry support. Unregistered dependencies explicitly
retained: neighbouring clause 17 IEEE_ARITHMETIC and IEEE_EXCEPTIONS procedures, IEEE
named constants, and ISO/IEC 60559 operation semantics referenced by these sections.

### S17.11.31-002: IEEE_QUIET_LE is invoked only for supported IEEE datatypes

**Source:** 17.11.31 p4; J3/24-007, 18 December 2023, physical PDF499. **Class:** Restriction.

**Definition:** IEEE_QUIET_LE(A, B) shall not be invoked with IEEE_SUPPORT_DATATYPE(A) false.

**Diagnostic obligation:** not-required.

**Facets:** `ieee_quiet_le-requires-supported-datatype`.

**Oracle:** Plans are pending and source-derived. Guarded branch plans name the IEEE inquiry probe,
execute assertions only on supported branches, and record unsupported branches as skips
rather than passes. Exact finite controls use representable values such as -1.0, 0.0,
1.0, 1.5, and 2.0. IEEE_COPY_SIGN, IEEE_IS_NEGATIVE, IEEE_VALUE, IEEE_IS_NAN, and
IEEE_CLASS observer calls carry their own IEEE_SUPPORT_DATATYPE restriction guard before
constructing or observing signed zeros, infinities, or NaNs; NaN rows additionally probe
IEEE_SUPPORT_NAN(1.0), and infinity rows additionally probe IEEE_SUPPORT_INF(1.0). Named
preamble FLAG-HALTING-OFF(flag, x), referenced by each flag row: after
IEEE_SUPPORT_FLAG(flag, x) succeeds, execute the explicit branch "if
IEEE_SUPPORT_HALTING(flag, x): IEEE_SET_HALTING_MODE(flag,.FALSE.); else
IEEE_GET_HALTING_MODE(flag,h), proceed only if h is .FALSE., else skip". Exception-flag
controls clear all supported flags before each exception row and observe each flag in
its own guarded branch.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IEEE comparison execution is guarded by the relevant IEEE_SUPPORT_DATATYPE and, for
IEEE_VALUE/IEEE_IS_NAN/IEEE_CLASS NaN observers, IEEE_SUPPORT_NAN; infinity rows also
require IEEE_SUPPORT_INF. Flag observations are keyed to IEEE_SUPPORT_FLAG(flag, 1.0),
with halting disabled or verified quiet by the named FLAG-HALTING-OFF preamble before
signaling-NaN operations. These sections contain no set/restore round-trip facet;
IEEE_SET_HALTING_MODE(flag,.FALSE.) appears only in the FLAG-HALTING-OFF precondition,
so calibration rule 10 changed-value and restored-original observations are not
applicable here. Argument restrictions and invocation restrictions in module procedure
descriptions are not numbered constraints and therefore carry no local diagnostic facet.
Halting .TRUE. behavior is not exercised.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, logical
observation, and intrinsic inquiry support. Unregistered dependencies explicitly
retained: neighbouring clause 17 IEEE_ARITHMETIC and IEEE_EXCEPTIONS procedures, IEEE
named constants, and ISO/IEC 60559 operation semantics referenced by these sections.

### S17.11.31-003: IEEE_QUIET_LE returns default logical

**Source:** 17.11.31 p5; J3/24-007, 18 December 2023, physical PDF499. **Class:** Effect.

**Definition:** The result of IEEE_QUIET_LE has type default logical.

**Diagnostic obligation:** not-required.

**Facets:** `ieee_quiet_le-result-default-logical`.

**Oracle:** Plans are pending and source-derived. Guarded branch plans name the IEEE inquiry probe,
execute assertions only on supported branches, and record unsupported branches as skips
rather than passes. Exact finite controls use representable values such as -1.0, 0.0,
1.0, 1.5, and 2.0. IEEE_COPY_SIGN, IEEE_IS_NEGATIVE, IEEE_VALUE, IEEE_IS_NAN, and
IEEE_CLASS observer calls carry their own IEEE_SUPPORT_DATATYPE restriction guard before
constructing or observing signed zeros, infinities, or NaNs; NaN rows additionally probe
IEEE_SUPPORT_NAN(1.0), and infinity rows additionally probe IEEE_SUPPORT_INF(1.0). Named
preamble FLAG-HALTING-OFF(flag, x), referenced by each flag row: after
IEEE_SUPPORT_FLAG(flag, x) succeeds, execute the explicit branch "if
IEEE_SUPPORT_HALTING(flag, x): IEEE_SET_HALTING_MODE(flag,.FALSE.); else
IEEE_GET_HALTING_MODE(flag,h), proceed only if h is .FALSE., else skip". Exception-flag
controls clear all supported flags before each exception row and observe each flag in
its own guarded branch.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IEEE comparison execution is guarded by the relevant IEEE_SUPPORT_DATATYPE and, for
IEEE_VALUE/IEEE_IS_NAN/IEEE_CLASS NaN observers, IEEE_SUPPORT_NAN; infinity rows also
require IEEE_SUPPORT_INF. Flag observations are keyed to IEEE_SUPPORT_FLAG(flag, 1.0),
with halting disabled or verified quiet by the named FLAG-HALTING-OFF preamble before
signaling-NaN operations. These sections contain no set/restore round-trip facet;
IEEE_SET_HALTING_MODE(flag,.FALSE.) appears only in the FLAG-HALTING-OFF precondition,
so calibration rule 10 changed-value and restored-original observations are not
applicable here. Argument restrictions and invocation restrictions in module procedure
descriptions are not numbered constraints and therefore carry no local diagnostic facet.
Halting .TRUE. behavior is not exercised.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, logical
observation, and intrinsic inquiry support. Unregistered dependencies explicitly
retained: neighbouring clause 17 IEEE_ARITHMETIC and IEEE_EXCEPTIONS procedures, IEEE
named constants, and ISO/IEC 60559 operation semantics referenced by these sections.

### S17.11.31-004: IEEE_QUIET_LE implements quiet less-or-equal comparison for finite operands

**Source:** 17.11.31 p6 finite comparison; J3/24-007, 18 December 2023, physical PDF499. **Class:** Effect.

**Definition:** For non-NaN real operands, the IEEE_QUIET_LE result is true exactly for operands where A
compares less than or equal to B according to the ISO/IEC 60559:2020
compareQuietLessEqual operation.

**Diagnostic obligation:** not-required.

**Facets:** `ieee_quiet_le-finite-true-case`, `ieee_quiet_le-finite-false-case`, `ieee_quiet_le-signed-zero-row-true`, `ieee_quiet_le-infinity-truth-table`.

**Oracle:** Plans are pending and source-derived. Guarded branch plans name the IEEE inquiry probe,
execute assertions only on supported branches, and record unsupported branches as skips
rather than passes. Exact finite controls use representable values such as -1.0, 0.0,
1.0, 1.5, and 2.0. IEEE_COPY_SIGN, IEEE_IS_NEGATIVE, IEEE_VALUE, IEEE_IS_NAN, and
IEEE_CLASS observer calls carry their own IEEE_SUPPORT_DATATYPE restriction guard before
constructing or observing signed zeros, infinities, or NaNs; NaN rows additionally probe
IEEE_SUPPORT_NAN(1.0), and infinity rows additionally probe IEEE_SUPPORT_INF(1.0). Named
preamble FLAG-HALTING-OFF(flag, x), referenced by each flag row: after
IEEE_SUPPORT_FLAG(flag, x) succeeds, execute the explicit branch "if
IEEE_SUPPORT_HALTING(flag, x): IEEE_SET_HALTING_MODE(flag,.FALSE.); else
IEEE_GET_HALTING_MODE(flag,h), proceed only if h is .FALSE., else skip". Exception-flag
controls clear all supported flags before each exception row and observe each flag in
its own guarded branch.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IEEE comparison execution is guarded by the relevant IEEE_SUPPORT_DATATYPE and, for
IEEE_VALUE/IEEE_IS_NAN/IEEE_CLASS NaN observers, IEEE_SUPPORT_NAN; infinity rows also
require IEEE_SUPPORT_INF. Flag observations are keyed to IEEE_SUPPORT_FLAG(flag, 1.0),
with halting disabled or verified quiet by the named FLAG-HALTING-OFF preamble before
signaling-NaN operations. These sections contain no set/restore round-trip facet;
IEEE_SET_HALTING_MODE(flag,.FALSE.) appears only in the FLAG-HALTING-OFF precondition,
so calibration rule 10 changed-value and restored-original observations are not
applicable here. Argument restrictions and invocation restrictions in module procedure
descriptions are not numbered constraints and therefore carry no local diagnostic facet.
Halting .TRUE. behavior is not exercised.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, logical
observation, and intrinsic inquiry support. Unregistered dependencies explicitly
retained: neighbouring clause 17 IEEE_ARITHMETIC and IEEE_EXCEPTIONS procedures, IEEE
named constants, and ISO/IEC 60559 operation semantics referenced by these sections.

### S17.11.31-005: IEEE_QUIET_LE gives false for NaN operands

**Source:** 17.11.31 p6 NaN result; J3/24-007, 18 December 2023, physical PDF499. **Class:** Effect.

**Definition:** For IEEE_QUIET_LE, the result is false with a NaN operand.

**Diagnostic obligation:** not-required.

**Facets:** `ieee_quiet_le-quiet-nan-result-false`, `ieee_quiet_le-signaling-nan-result-false`.

**Oracle:** Plans are pending and source-derived. Guarded branch plans name the IEEE inquiry probe,
execute assertions only on supported branches, and record unsupported branches as skips
rather than passes. Exact finite controls use representable values such as -1.0, 0.0,
1.0, 1.5, and 2.0. IEEE_COPY_SIGN, IEEE_IS_NEGATIVE, IEEE_VALUE, IEEE_IS_NAN, and
IEEE_CLASS observer calls carry their own IEEE_SUPPORT_DATATYPE restriction guard before
constructing or observing signed zeros, infinities, or NaNs; NaN rows additionally probe
IEEE_SUPPORT_NAN(1.0), and infinity rows additionally probe IEEE_SUPPORT_INF(1.0). Named
preamble FLAG-HALTING-OFF(flag, x), referenced by each flag row: after
IEEE_SUPPORT_FLAG(flag, x) succeeds, execute the explicit branch "if
IEEE_SUPPORT_HALTING(flag, x): IEEE_SET_HALTING_MODE(flag,.FALSE.); else
IEEE_GET_HALTING_MODE(flag,h), proceed only if h is .FALSE., else skip". Exception-flag
controls clear all supported flags before each exception row and observe each flag in
its own guarded branch.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IEEE comparison execution is guarded by the relevant IEEE_SUPPORT_DATATYPE and, for
IEEE_VALUE/IEEE_IS_NAN/IEEE_CLASS NaN observers, IEEE_SUPPORT_NAN; infinity rows also
require IEEE_SUPPORT_INF. Flag observations are keyed to IEEE_SUPPORT_FLAG(flag, 1.0),
with halting disabled or verified quiet by the named FLAG-HALTING-OFF preamble before
signaling-NaN operations. These sections contain no set/restore round-trip facet;
IEEE_SET_HALTING_MODE(flag,.FALSE.) appears only in the FLAG-HALTING-OFF precondition,
so calibration rule 10 changed-value and restored-original observations are not
applicable here. Argument restrictions and invocation restrictions in module procedure
descriptions are not numbered constraints and therefore carry no local diagnostic facet.
Halting .TRUE. behavior is not exercised.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, logical
observation, and intrinsic inquiry support. Unregistered dependencies explicitly
retained: neighbouring clause 17 IEEE_ARITHMETIC and IEEE_EXCEPTIONS procedures, IEEE
named constants, and ISO/IEC 60559 operation semantics referenced by these sections.

### S17.11.31-006: IEEE_QUIET_LE signals IEEE_INVALID for signaling NaN operands

**Source:** 17.11.31 p6 signaling NaN; J3/24-007, 18 December 2023, physical PDF499. **Class:** Effect.

**Definition:** For IEEE_QUIET_LE, IEEE_INVALID signals with a signaling NaN operand.

**Diagnostic obligation:** not-required.

**Facets:** `ieee_quiet_le-signaling-nan-invalid-flag`, `ieee_quiet_le-signaling-nan-other-flags-quiet`.

**Oracle:** Plans are pending and source-derived. Guarded branch plans name the IEEE inquiry probe,
execute assertions only on supported branches, and record unsupported branches as skips
rather than passes. Exact finite controls use representable values such as -1.0, 0.0,
1.0, 1.5, and 2.0. IEEE_COPY_SIGN, IEEE_IS_NEGATIVE, IEEE_VALUE, IEEE_IS_NAN, and
IEEE_CLASS observer calls carry their own IEEE_SUPPORT_DATATYPE restriction guard before
constructing or observing signed zeros, infinities, or NaNs; NaN rows additionally probe
IEEE_SUPPORT_NAN(1.0), and infinity rows additionally probe IEEE_SUPPORT_INF(1.0). Named
preamble FLAG-HALTING-OFF(flag, x), referenced by each flag row: after
IEEE_SUPPORT_FLAG(flag, x) succeeds, execute the explicit branch "if
IEEE_SUPPORT_HALTING(flag, x): IEEE_SET_HALTING_MODE(flag,.FALSE.); else
IEEE_GET_HALTING_MODE(flag,h), proceed only if h is .FALSE., else skip". Exception-flag
controls clear all supported flags before each exception row and observe each flag in
its own guarded branch.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IEEE comparison execution is guarded by the relevant IEEE_SUPPORT_DATATYPE and, for
IEEE_VALUE/IEEE_IS_NAN/IEEE_CLASS NaN observers, IEEE_SUPPORT_NAN; infinity rows also
require IEEE_SUPPORT_INF. Flag observations are keyed to IEEE_SUPPORT_FLAG(flag, 1.0),
with halting disabled or verified quiet by the named FLAG-HALTING-OFF preamble before
signaling-NaN operations. These sections contain no set/restore round-trip facet;
IEEE_SET_HALTING_MODE(flag,.FALSE.) appears only in the FLAG-HALTING-OFF precondition,
so calibration rule 10 changed-value and restored-original observations are not
applicable here. Argument restrictions and invocation restrictions in module procedure
descriptions are not numbered constraints and therefore carry no local diagnostic facet.
Halting .TRUE. behavior is not exercised.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, logical
observation, and intrinsic inquiry support. Unregistered dependencies explicitly
retained: neighbouring clause 17 IEEE_ARITHMETIC and IEEE_EXCEPTIONS procedures, IEEE
named constants, and ISO/IEC 60559 operation semantics referenced by these sections.

### S17.11.31-007: IEEE_QUIET_LE signals no exception for finite and quiet-NaN operands

**Source:** 17.11.31 p6 quiet operands; J3/24-007, 18 December 2023, physical PDF499. **Class:** Effect.

**Definition:** For IEEE_QUIET_LE, no exception is signaled except for signaling NaN operands.

**Diagnostic obligation:** not-required.

**Facets:** `ieee_quiet_le-finite-no-exception-flags`, `ieee_quiet_le-quiet-nan-no-exception-flags`.

**Oracle:** Plans are pending and source-derived. Guarded branch plans name the IEEE inquiry probe,
execute assertions only on supported branches, and record unsupported branches as skips
rather than passes. Exact finite controls use representable values such as -1.0, 0.0,
1.0, 1.5, and 2.0. IEEE_COPY_SIGN, IEEE_IS_NEGATIVE, IEEE_VALUE, IEEE_IS_NAN, and
IEEE_CLASS observer calls carry their own IEEE_SUPPORT_DATATYPE restriction guard before
constructing or observing signed zeros, infinities, or NaNs; NaN rows additionally probe
IEEE_SUPPORT_NAN(1.0), and infinity rows additionally probe IEEE_SUPPORT_INF(1.0). Named
preamble FLAG-HALTING-OFF(flag, x), referenced by each flag row: after
IEEE_SUPPORT_FLAG(flag, x) succeeds, execute the explicit branch "if
IEEE_SUPPORT_HALTING(flag, x): IEEE_SET_HALTING_MODE(flag,.FALSE.); else
IEEE_GET_HALTING_MODE(flag,h), proceed only if h is .FALSE., else skip". Exception-flag
controls clear all supported flags before each exception row and observe each flag in
its own guarded branch.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IEEE comparison execution is guarded by the relevant IEEE_SUPPORT_DATATYPE and, for
IEEE_VALUE/IEEE_IS_NAN/IEEE_CLASS NaN observers, IEEE_SUPPORT_NAN; infinity rows also
require IEEE_SUPPORT_INF. Flag observations are keyed to IEEE_SUPPORT_FLAG(flag, 1.0),
with halting disabled or verified quiet by the named FLAG-HALTING-OFF preamble before
signaling-NaN operations. These sections contain no set/restore round-trip facet;
IEEE_SET_HALTING_MODE(flag,.FALSE.) appears only in the FLAG-HALTING-OFF precondition,
so calibration rule 10 changed-value and restored-original observations are not
applicable here. Argument restrictions and invocation restrictions in module procedure
descriptions are not numbered constraints and therefore carry no local diagnostic facet.
Halting .TRUE. behavior is not exercised.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, logical
observation, and intrinsic inquiry support. Unregistered dependencies explicitly
retained: neighbouring clause 17 IEEE_ARITHMETIC and IEEE_EXCEPTIONS procedures, IEEE
named constants, and ISO/IEC 60559 operation semantics referenced by these sections.

<!-- END GENERATED 17.11.31 -->
