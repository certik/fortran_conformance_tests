# Fortran 2023 8.6.7: DATA statement

The original source review is recorded in batch073. Current facet states
remain in the catalogue, and source, fixture and inventory reviews stay
separate. This generator grants or renews none. The source of truth is
`doc/catalogues/data_statement_8_6_7.json`; effective status comes from
`Registry.catalogue_review_state("8.6.7")`.

The authority is the original J3/24-007, dated December 18, 2023, with688
physical pages and SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The actual DATA heading begins on physical PDF136. The entire section,
including the note and its explanation, occupies136-138 and ends at the
actual8.6.8 DIMENSION statement heading on138. Inline references do not
define boundaries. The pinned section hash is
`7e454dde575025edaa6dee03100e251cd9f913557b35b703bf2fba1553ee5a37`.

| Original scope | Treatment |
| --- | --- |
| `R840`-`R845`, `R846`-`R850` | Eleven syntax definitions, including every alternative and optional item |
| `C878`-`C891` | Fourteen constraints with exact object/value/repeat/constructor roles |
| `p1`-`p4` | Initialization definition, default-initialization restriction, two ordering conditions, canonical SAVE implication and confirmation permission |
| `p5`-`p8` | Pointer/scalar expansion, array/implied-DO order, repeats, zero contributions, position correspondence and equal lengths |
| `p9`-`p11` | Pointer iff/compatibility, ordinary initial-value compatibility/conversion and INTEGER BOZ domain |
| Complete note | Informative example with its actual PERSON/NODE definitions and finite counts |

The catalogue accounts for **37 original units and184 fine units**, with221
accounting rows and reciprocal requirement mappings. Fine units inherit
their original parent's pinned source material; they do not have fictional
independent PDF hashes. There are25 numbered requirements and eight
append-only `S8.6.7-001` through `S8.6.7-008` requirements. The25 numbered
units were already counted globally; the genuine new base accounting is
the eleven paragraphs plus the note, **12 units**, not37.

## Bounded contexts, phases and reporting

**D0** is the default context for the finite plans: a complete free-form
program unit with `IMPLICIT NONE`, prior explicit declarations of ordinary
local fixed-shape INTEGER, LOGICAL or default CHARACTER objects, constant
in-range selectors and matching DATA sequence counts. Selected storage has
no other explicit/default initialization and no overlapping DATA occurrence.
COMMON, coarrays, pointers, derived types, other kinds, dummy/result roles
and implicit typing are introduced only when a plan explicitly supplies
their complete prerequisites. Every valid control preserves all unaffected
predicates. The typing/order plans deliberately use a nonnull implicit
mapping rather than creating unrelated IMPLICIT NONE errors.

Syntax/constraint admissions have **positive-control / compile** roles.
Prospective numbered negatives have **diagnostic / compile** roles only
after source-minimal repair and cause qualification. Source-use and
canonical-operation plans have no invented compiler phase or passing
classifier. S8.6.7-004's observations are **effect / run** plans requiring
the complete future parent program and actual qualified compile/link/run
evidence, not just syntax admission.
The bounded implemented subset below does not complete the other plans.

4.2 requires a capability to detect and report numbered syntax/constraint
violations. It does not prescribe fatal rejection, nonzero status, exact
English, a stream, or printed R/C identifiers. Rejection-only, warning-as-
error and exact-message policies are additional contracts, not the standard
obligation. Nonnumbered prose does not acquire mandatory diagnosis merely
because it contains *shall*. If a contract cannot represent a qualifying
nonfatal report, the case stays gated rather than silently narrowing the
standard's permission.

DATA amongst executable statements and COMMON/BLOCK DATA are obsolescent
under B.3.5/B.3.11; their reporting is separate from an invalid DATA rule.
Generic unsupported facilities, unimplemented F2023 syntax, crashes,
resource failures, bad source form or unrelated type/scope/count errors do
not corroborate a chosen DATA predicate. No invalid source is run.

## DATA is initialization, not executable assignment

P1 retains8.4 as the explicit-initialization owner. DATA can appear through
R507 or R510 without being an executable action. The bounded observation
with an explicitly declared local initialized by DATA text after RETURN
tests initial state, not whether an unreachable marker executes. Explicit
prior locality is essential:19.5.1.4p3 forbids an earlier reference when
DATA alone created a local entity that hides a host entity.

Occurrence uniqueness remains S8.4-002. Equal values do not make two
initializers for the same storage legal; disjoint parts are not duplicates.
DATA's prior-array-property rule retains a relationship to S8.4-003,
without changing that requirement or its pending plans.

The p4 SAVE implication remains the existing S8.4-005 operation and
8.5.16's lifetime/retention semantics. Initialization of any part can give
the named variable SAVE without initially defining its other parts.
Explicit SAVE may confirm the implication. Named-COMMON members are
excepted; blank-COMMON DATA is independently forbidden, while a named block
can have SAVE from another route, including BIND. No duplicate retention
program or new local SAVE S ID is needed.

DATA does not gain intrinsic assignment's scalar broadcast across an
unmatched expanded list, allocatable reallocation, arbitrary defined-
assignment/finalization trace or universal derived-value copy semantics.
P10 imports compatibility and conversion for the actual correspondence.
Constructor meanings, default initialization and opaque/private components
retain their own owners.

## Constants, repeats and precise object roles

R847 is **constant or constant subobject**, not arbitrary integer constant
expression. R848 likewise lists nine specific alternatives. A legal
constant expression such as `1+2` does not automatically become a DATA
value; a prior PARAMETER can supply a named constant with that value.
Signed integer/real literals are explicit alternatives. A general
`initial-proc-target` is not an R848 alternative.

Constant-expression classification follows10.1.12. A defined ordinary
variable value is not constant, but a qualifying inquiry on previously
specified constant properties may mention a variable. A DATA index is
expressly permitted as a constant-expression primary within its own
implied DO; C885 retains current/enclosing scope conditions. Index scope
does not declare an enclosing variable or produce an ordinary final-index
value. Typed indices use supported kinds, without ordering kind numbers or
assuming two selected kinds are distinct.

Repeat/value roles matter for causal isolation. The clean C890 negative
uses a mutable array element in the **repeat** slot, where it cannot be
mistaken for an R848 initial data target. Constructor nonconstancy cases
keep the type already defined and the value inside the enum/enumeration
domain. Moving a constructor type later can independently invalidate the
receiver declaration; such a proposed C887 negative remains gated.

Scalar structure components in implied DOs must be variables and contain
a subscript list in some part-ref. The final part has no subscript under
R913/C920; a final subscript instead denotes an array element/section.
One-iteration controls avoid silently adding repeated initialization.
The p5 unqualified-array wording does not waive the actual R844 grammar;
the selected plans do not invent a bare-whole-array implied-DO control.

## State, lifetime, count and numerical oracles

The finite initial-value plans use independently written coordinate/literal
checks rather than recreating the same DATA/implied-DO/RESHAPE
transformation as their expected result. For example, the two-by-three
array plan checks the six coordinates against11,21,12,22,13,23 in column
element order. Selected sections are nonoverlapping and every inspected
element is defined.

| Source condition | Preserved distinction and planned observable |
| --- | --- |
| Nonpointer whole array or section | Expand scalar elements in the prescribed order |
| Whole array pointer | One pointer item; never inspect unavailable shape to expand it |
| Zero-sized array / zero-trip implied DO | No variable contribution; observe a separately initialized nonzero tail |
| Zero-length scalar CHARACTER | Still one variable; its consumed value is distinguished by the following scalar |
| Repeat0 | No constant contribution, but applicable syntax/definition requirements remain |
| Partial DATA initialization | No read of untouched elements/characters before a separate valid definition |
| Initial pointer association | Not allocation or target-value initialization; status is known before inquiry |

Pointer controls use saved, live, noncoindexed, nonallocatable TARGET
objects with constant nonvector selectors and appropriate type, kind, rank
and nondeferred parameters. Final entire pointer parts differ from earlier
pointer hops, subscripted pointers and substrings of pointer targets.
`ASSOCIATED` is never queried on undefined status; target-form checks
exclude zero-sized/zero-storage ambiguity and guard payload reads in
separate control flow, not assumed short-circuit expressions. Pointer
targets are not automatically subobjects of their pointer's base object.

Default initialization is not DATA initialization. The whole nonpointer
default-initialized object is excluded; a pointer of that type is not a
constructed target and can have a valid NULL DATA initializer. Actual
subobject default status must be established before a component-specific
case is admitted. Direct procedure pointers retain15.4.3.6 initialization,
not a guessed DATA-variable interpretation; constructor procedure-pointer
components require separate constant-target/interface qualification.

REAL and COMPLEX compile admissions do not establish an exact runtime
value or arbitrary tolerance, even for small-looking literals. Integer
BOZ effect plans bind actual kind/capacity and integer bit/radix conditions;
they assume neither32/64-bit width nor a universal high-bit interpretation.
The independent small integer expectation is a decimal literal, not INT
of the same BOZ as a self-oracle. DATA's contributing BOZ receiver is
INTEGER; permissions for REAL BOZ elsewhere do not enlarge that domain.
The unchanged `S10_2_1_3_017_valid__real` case remains **NEEDS_ORACLE**.

## Explicit pending cause and mechanism gates

| Gate | Required evidence before materializing or crediting a case |
| --- | --- |
| C880 host association | DATA can itself declare a local homonym; prove actual host identity rather than assuming it |
| C880 automatic object | DATA implies SAVE, also activating C814; an automatic/SAVE report is not independent C880 corroboration |
| C880 function name | Separate a true data/result object from a bare procedure/designator grammar failure |
| C885 foreign index | Resolve the exact statement entity; do not hide ordinary nonconstancy or a nested-name conflict |
| C887 forward constructor type | Avoid a competing receiver type-declaration/resolution violation |
| C890/C891 value-role diagnostic | Disambiguate constant subobject versus initial target before attributing the cause |
| Nonempty/empty sequences | Keep syntactic list presence, valid expansion and per-set equal lengths separate |
| Representation-dependent values | Independent documented/derived oracle, not compiler consensus or a successful skipped inquiry |

These are evidence-isolation/qualification gates, not unclassified source
text. Every original and fine unit is accounted, while no facet is
completed. The original p9 association operations and p4 SAVE attribution
have explicit pending canonical relationships instead of duplicate S
effects or no-op classifiers.

The pending wrapper graph is
`R840 -> R841 -> R842/R843/R844/R845` and
`R841 -> R846 -> R847/R848 -> R849/R850`, with each defining R/C or
paragraph attached at its real scope. Shared operations retain8.4,
7.5.4.6/7.5.10,10.2,11.1.7.4 and19 owners. Current canonical links do not
support arbitrary numbered-wrapper or positive-only reuse. No duplicated
program, fictional source inquiry or automatic link fills that gap.

## Complete note and preserved population

The original PERSON definition has INTEGER AGE and CHARACTER(LEN=50)
NAME, without component defaults. NODE, from7.5.4.6 NOTE4, has VALUE=0
and NEXT_NODE=>NULL(); DATA on HEAD_OF_LIST initializes the pointer,
not a NODE payload. NAME's eight characters are padded to length10;
MILES(0:9) consumes ten integer zeros. The SKEW index sets have5050 and4950
members, cover the lower and strict upper triangles without overlap, and
total10000. Those REAL example constants do not waive approximation gates.
The note is informative, not an additional requirement or generated fixture.

The assigned immutable author base is
`789867f5da9978d5cc4ad4d77771de1432e08c17`:2,049 cases,118 catalogues,
696 requirements,2,013 raw reviews in2,011 groups, and22 current links.
There are no primary R840-R850/C878-C891 or S8.6.7 cases at that base.
The two existing S8.5.16-001 effect programs explicitly SAVE their local
state and initialize it executably; they are not DATA-implication witnesses.
BIND-common effect/explicit-SAVE-control roles, all legacy cases, R402,
profiles, raw reviews and the baseline retain their exact bindings.
No old-case metadata migration is performed.

Only this catalogue/view are committed; a one-entry local index overlay
is solely for loading/rendering and remains uncommitted. The handoff
records measured raw/effective evidence transitions instead of renewing
source, case, link or inventory reviews. Source accounting is not
execution coverage, newer main populations are not imported, and no
earlier full run is relabelled. Independent source review is the next gate,
not whole-standard closure.

<!-- BEGIN DATA POSITION EFFECTS -->
## Finite DATA position and initial-state effects

Three complete run/effect/f2023 programs initialize actual default INTEGER
variables once. One checks the distinct scalar values11/22/33 before any
executable assignment to them, with three observations. Another initializes
a(2,3) from11,21,12,22,13,23 and checks every coordinate against independent
literals in Table9.1 order, with six observations and no reconstructed oracle.

The third calls an internal INTEGER function whose explicitly declared
local kept is directly checked and returned as7 before the physical
DATA kept/7/ statement. Main checks entry, before-return, normal-return,
local-check and result events plus zero execution of the ordinary action
after DATA. Six caller checks precede exact completion. This tests initial
definition, not executable DATA, a saved-lifetime effect or an undefined
DATA-only host-shadowing case. B.3.5 obsolescent placement stays allowed.

Every guard/completion has a complete-parent wrong-oracle plan. Separate
omissions target observations with their counters, the actual call, a
defined-result no-op function and the explicit RETURN. All DATA initializers
survive every mutation; no undefined subject or function result is created.
Current passing parents and intended runtime failures are necessary for
sensitivity. Synthetic transports and f2018 supplementary observations do
not become f2023 native qualification.

Only three S8.6.7-004 facets and their bounded oracle text are generator-owned.
The other18 owner facets, all foreign requirements and independently managed
pending/control/raw-review state remain unchanged. No repeat/implied-DO,
pointer/component/character/BOZ/profile, source-use or canonical-link credit
is added. Generation supplies neither approval nor baseline changes.
<!-- END DATA POSITION EFFECTS -->

<!-- BEGIN DATA STATEMENT FIXTURES -->
## Finite DATA statement fixture packet

Thirteen complete fixtures under `data_statement_*` represent 24 selected facets. R840/R841 controls exercise set and matched-list syntax while observing nonzero initialized values. R846/R847/C886 controls isolate default, literal, named, positive and zero repeat contributions. R848 covers portable scalar constants, signed literals, NULL() and initial data targets. S8.6.7-004 observes repeat/default-one correspondence, zero-length CHARACTER consumption, same-kind CHARACTER padding/truncation with LEN guards, a qualified positive BOZ INTEGER value, and triplet, vector, component-section and negative-step implied-DO order. S8.6.7-006 and S8.6.7-008 add pointer-association and INTEGER-domain controls.

Every checked value is nonzero or otherwise nondefault, and CHARACTER comparisons are preceded by LEN guards. The BOZ cases first check radix/capacity and then compare against an independently written decimal literal. The pointer cases query only defined DATA-initialized pointer status. Mutations replace DATA features or constants and are calibrated separately; unsupported implied-DO ordering, sections, derived constructors, constant subobject repeats and DATA-implied SAVE retention stay pending here.

This block and the bounded oracle paragraphs are generator-owned. The existing DATA position effects block, its fixtures, all foreign pending facets, reviews, links, baselines and rollout accounting remain unchanged.
<!-- END DATA STATEMENT FIXTURES -->

<!-- BEGIN GENERATED 8.6.7 -->

### R840: DATA contains one or more initialization sets, optionally separated by commas

**Source:** 8.6.7 R840, physical PDF136 **Class:** Syntax.

**Definition:** DATA is followed by a required data-stmt-set and zero or more further sets. The comma
before each additional set is optional. DATA specifies initialization, not an executable
assignment, even when an otherwise permitted statement-order context places it in an
execution part.

**Diagnostic obligation:** required.

**Facets:** `single-set`, `sets-with-comma`, `sets-without-comma`, `missing-first-set`, `statement-kind-owner`.

**Oracle:** Compile admission or a source-minimal syntax diagnostic/control pair in complete D0
units; source-kind relationships need bound statement locations and their original
owners.

DATA statement fixture family: one complete positive-control program reaches one set,
comma-separated sets and adjacent sets without the optional comma. Five distinct nonzero
INTEGER objects are checked after normal execution, so syntax admission is tied to the
actual DATA statements and not to declaration initialization or default zero storage.

**Oracle limitation:** Required capability to report is not mandatory fatal rejection, fixed English, nonzero
status or printed R840. Unsupported facilities, crashes, resource errors and an
obsolescence warning alone do not corroborate the local syntax cause. All facets are
pending.

DATA statement fixture family boundaries: only `single-set`, `sets-with-comma`,
`sets-without-comma` are represented by this generator. All other facets, diagnostics,
source-use links, reviews and the existing DATA position effect fixtures remain
independently managed. Generation grants no review or baseline update.

**Dependencies:** 4.1.1-.4/4.2;5.1 R507/R510;5.3.1-.2;8.4;R841;p1;B.3.5.

### R841: Each DATA set has nonempty object and value lists between two slashes

**Source:** 8.6.7 R841, physical PDF136 **Class:** Syntax.

**Definition:** A set consists of a data-stmt-object list, a slash, a data-stmt-value list and a closing
slash. R401 defines nonempty comma lists. A syntactically present list can expand to an
empty sequence under p7; syntactic nonemptiness and expanded cardinality are different
predicates.

**Diagnostic obligation:** required.

**Facets:** `matched-lists`, `opening-slash`, `closing-slash`, `required-object-list`, `required-value-list`, `list-expansion-graph`.

**Oracle:** Matched, otherwise conforming compile controls and exact slash/list omissions;
distinguish syntax-list length from expanded-sequence length.

DATA statement fixture family: one complete positive-control program reaches a
two-object and two-value DATA set between slashes and observes the two nonzero initial
values. This establishes only the selected matched-list control; empty-list and
slash-omission diagnostics remain separate.

**Oracle limitation:** No runtime query of a zero-sized object proves this grammar. Empty-list repairs must not
leave a p8 mismatch. Diagnostics need a local cause, not a mandatory rule code or fatal
status.

DATA statement fixture family boundaries: only `matched-lists` are represented by this
generator. All other facets, diagnostics, source-use links, reviews and the existing
DATA position effect fixtures remain independently managed. Generation grants no review
or baseline update.

**Dependencies:** R401;4.2;R842/R846;p5-p8;19.6.2.

### R842: A DATA object is a variable or a DATA implied DO

**Source:** 8.6.7 R842, physical PDF136 **Class:** Syntax.

**Definition:** The alternatives are variable and data-implied-do. C879 narrows the variable alternative
to a designator with constant addressing expressions; C878/C880/C881 further restrict
eligible entities and pointer paths. A variable is a data object, not a direct
procedure-pointer entity.

**Diagnostic obligation:** required.

**Facets:** `variable-designators`, `implied-do-alternative`, `pointer-versus-procedure-role`, `object-wrapper-graph`.

**Oracle:** Bounded source/role relationships and complete admitted declarations, retaining every
inherited constraint and canonical grammar owner.

**Oracle limitation:** Do not use a function with a nonpointer result, direct procedure pointer, undefined
dereference or invalid entity role as a supposedly conforming control. No automatic
source-use or wrapper-link credit.

**Dependencies:** 3.41/3.42/3.104.1-.2/3.151;4.1.2;8.2 R804/C810;9.1-.2;R843;C878-C881;15.4.3.6.

### R843: A DATA implied DO has an object list and constant integer loop control

**Source:** 8.6.7 R843, physical PDF136 **Class:** Syntax.

**Definition:** Parentheses enclose a nonempty data-i-do-object list followed by a comma, optional
integer-type-spec with double colon, the data-i-do-variable and equals, two scalar
integer constant expressions, and an optional third such expression introduced by a
comma. Constant-expression rules permit an appropriate current or enclosing DATA index;
the nonzero step and iteration interpretation come from the referenced DO semantics, not
a new local numbered constraint.

**Diagnostic obligation:** required.

**Facets:** `inherited-index-type`, `explicit-integer-type`, `explicit-step`, `nested-dependent-limit`, `control-comma`, `type-double-colon`, `limit-and-step-owner`, `step-zero-owner-gate`.

**Oracle:** Complete typed integer contexts with exact finite index sequences and matching value
counts. Local punctuation diagnostics are separate from canonical expression/type/DO
restrictions.

**Oracle limitation:** No integer overflow in loop-parameter conversion/count arithmetic, zero step,
out-of-bounds reference, wrong scope or unsupported F2023 typed-index syntax can
corroborate a different predicate. Admission is not iteration-effect coverage.

**Dependencies:** R401/R403/C401;10.1.12;11.1.7.2 R1124/C1121;11.1.7.4.1/.3;19.4p1-p5;C885;p5-p8.

### R844: A DATA implied-DO object is an array element, scalar component or nested implied DO

**Source:** 8.6.7 R844, physical PDF136 **Class:** Syntax.

**Definition:** The alternatives are array-element, scalar-structure-component and data-implied-do. They
are narrower than R842's general variable alternative. C882/C883 require variables, C884
requires a subscript list in the scalar-component path, and C885 restricts the
subscripts and index scope.

**Diagnostic obligation:** required.

**Facets:** `array-element`, `indexed-parent-component`, `nested-implied-do`, `bare-scalar-exclusion`, `whole-array-wording-gate`.

**Oracle:** Admit each actual alternative with all role predicates satisfied; use a one-iteration
bare-scalar contrast to avoid hidden cardinality changes.

**Oracle limitation:** A scalar structure component without any subscript path fails C884 even though its
surface alternative exists. A named constant element fails C882/C883. Neither failure
can stand in for R844 admission.

**Dependencies:** R843;C882-C885;R403/C401;9.4.2 R913/C920;9.5.3.1 R917/C924;p5.

### R845: The DATA index uses the canonical DO-variable grammar

**Source:** 8.6.7 R845, physical PDF136 **Class:** Syntax.

**Definition:** A data-i-do-variable is a do-variable, inheriting R1124/C1121 and scalar/name
conventions. Its DATA statement-entity scope and attributes come from19.4, including
explicit integer-type-spec or inherited integer typing and the absence of other
attributes.

**Diagnostic obligation:** required.

**Facets:** `integer-index-admission`, `noninteger-index-owner`, `index-entity-identity`.

**Oracle:** Canonical syntax/type and scope relationships bound to concrete DATA control tokens,
with independent integer-context repairs where feasible.

**Oracle limitation:** The alias does not create another integer type restriction or scope runtime classifier.
Current source-use and supplementary-target link mechanisms do not automatically cover
this numbered wrapper.

**Dependencies:** 4.1.3;11.1.7.2 R1124/C1121;19.4p1-p5;R843;10.1.12p1(13).

### C878: DATA objects are not coindexed variables

**Source:** 8.6.7 C878, physical PDF136 **Class:** Restriction.

**Definition:** Neither a data-stmt-object nor a data-i-do-object may be a coindexed variable. A local
coarray object without an image selector is not excluded merely because it is a coarray.

**Diagnostic obligation:** required.

**Facets:** `direct-coindexed-object`, `implied-do-coindexed-element`, `local-coarray-control`.

**Oracle:** Single image-selector deletions in otherwise valid, saved, nonallocatable intrinsic
coarray declarations.

**Oracle limitation:** No native coarray execution is part of this source packet or these grammar controls. An
unsupported-facility diagnostic is not the required role-specific report.

**Dependencies:** 8.5.6.1 C823-C826;8.5.6.3;9.4.2/9.6;R842-R844;C879-C885;4.2.

### C879: The variable branch uses a designator with constant addressing expressions

**Source:** 8.6.7 C879, physical PDF136 **Class:** Restriction.

**Definition:** A variable used as a data-stmt-object must be a designator. Every subscript, section
subscript and substring endpoint in it is a constant expression. This is the R842
variable branch; implied-DO subscripts have their separate C885 context, including
permitted DATA-index primaries.

**Diagnostic obligation:** required.

**Facets:** `function-reference-excluded`, `variable-subscript`, `section-bound`, `section-stride`, `substring-start`, `substring-end`, `constant-inquiry-and-vector-admission`.

**Oracle:** Complete local declarations, constant-expression contrasts that preserve
role/range/cardinality, and a separately qualified non-designator function-reference
pair.

**Oracle limitation:** Ordinary defined values are not constant expressions, but valid specification inquiries
may mention variables. Generic unsupported expressions, out-of-bounds errors and
DATA-index scope failures are not substitute reports. No invalid program is run.

**Dependencies:** R842;9.1 R901;9.2 R902/C902;9.4.1;9.5.3;10.1.12;C885;8.4;4.2.

### C880: DATA designators exclude specific entity roles and retain the named-COMMON exception

**Source:** 8.6.7 C880, physical PDF136 **Class:** Restriction.

**Definition:** A variable whose designator is a direct or implied-DO DATA object is not a dummy
argument, USE- or HOST-associated entity, a named-COMMON member unless the DATA is in
BLOCK DATA, a blank-COMMON member, a function name or result name, an automatic data
object, or an allocatable variable. Declarations, entity identity, implicit SAVE and
initialization count must be resolved before attributing a diagnostic to one role.

**Diagnostic obligation:** required.

**Facets:** `dummy-scalar`, `dummy-array-element`, `use-associated-identity`, `host-shadowing-gate`, `named-common-outside-block-data`, `block-data-admission`, `blank-common`, `function-result`, `separate-result`, `function-name-category-gate`, `automatic-save-cause-gate`, `allocatable-scalar`.

**Oracle:** Role-specific complete units and minimal repairs, with explicit source/cause gates where
DATA changes identity or necessarily activates a second restriction.

**Oracle limitation:** No generic wrong-context/unsupported-facility/error-count result is corroboration. Host,
automatic and bare-function branches do not become isolated cases merely because a
compiler rejects them. COMMON/ordering obsolescence reporting is separate; required C880
reporting is not forced fatal rejection or a printed code.

**Dependencies:** 8.3 C814;8.4;8.5.16
C862;p3-p4;8.10.2;8.7;14.2.2;14.3;19.4p3;19.5.1.4p2-p3;9.1-.2;B.3.11;4.2.

### C881: A pointer in a DATA object designator must be its entire rightmost part

**Source:** 8.6.7 C881, physical PDF137 **Class:** Restriction.

**Definition:** For a direct variable DATA object or implied-DO object, a pointer may occur in the
object designator only as the entire rightmost part-ref. Earlier pointer hops and
subscripted or substring-selected pointer targets are different from initializing a
whole final pointer's association.

**Diagnostic obligation:** required.

**Facets:** `whole-pointer-admission`, `rightmost-pointer-component`, `pointer-element-exclusion`, `earlier-pointer-hop`, `pointer-substring-exclusion`.

**Oracle:** Known-live-target contrasts that change only the forbidden pointer path, with valid
final-pointer association controls and exact scalar/value counts.

**Oracle limitation:** 9.4.2 distinguishes a pointer component considered as a pointer from its dereferenced
target; the target is not automatically a base-object subobject. Do not query undefined
association status, infer target definition from association, or treat array-pointer
initialization as element initialization.

**Dependencies:** 9.4.1-.2;9.5.3.1 note2;7.5.4.6 C770;p9;19.5.2;8.4;16.9.20;4.2.

### C882: An implied-DO array element is a variable

**Source:** 8.6.7 C882, physical PDF137 **Class:** Restriction.

**Definition:** An array-element used in the R844 role must be a variable, not an element of a named
constant.

**Diagnostic obligation:** required.

**Facets:** `variable-element`, `constant-element-exclusion`.

**Oracle:** Resolve variable versus constant-array identity in the actual R844 array-element role
and admit the base-name-only repair.

**Oracle limitation:** Canonical variable meaning remains9.2/C901. A generic unsupported array constructor or
bad index is not this role-specific diagnostic.

**Dependencies:** R844;9.2 C901;8.5.13;C885;8.4;4.2.

### C883: An implied-DO scalar structure component is a variable

**Source:** 8.6.7 C883, physical PDF137 **Class:** Restriction.

**Definition:** The scalar-structure-component alternative in R844 must designate a variable rather than
a subobject of a constant.

**Diagnostic obligation:** required.

**Facets:** `variable-component`, `constant-component-exclusion`.

**Oracle:** Exact constant-versus-variable component identity with the required indexed path already
present.

**Oracle limitation:** Do not omit the indexed parent and accidentally test C884, or leave default/repeated
initialization in the control. No private representation is inspected.

**Dependencies:** R844;C884-C885;9.2/9.4.2;7.5.10;10.1.12;8.4;4.2.

### C884: An implied-DO scalar component path contains a subscript list

**Source:** 8.6.7 C884, physical PDF137 **Class:** Restriction.

**Definition:** A scalar-structure-component in the R844 role contains at least one part-ref with a
subscript list. The subscript need not be on the final part; indeed the
structure-component grammar has no final section-subscript list.

**Diagnostic obligation:** required.

**Facets:** `indexed-ancestor`, `unsubscripted-path-exclusion`.

**Oracle:** One-iteration scalar-component path contrast with exactly one value and no prior default
or explicit initialization.

**Oracle limitation:** A final subscript on a component instead changes the canonical designator to an array
element/section under9.4.2 note3. It is not a shortcut to this
scalar-structure-component control.

**Dependencies:** R844;C883/C885;9.4.2 R913/C920/note3;19.4;8.4;4.2.

### C885: Implied-DO subscripts are constant expressions using only current or enclosing indices

**Source:** 8.6.7 C885, physical PDF137 **Class:** Restriction.

**Definition:** In a DATA implied-DO array element or scalar structure component, each subscript is a
constant expression. A data-i-do-variable primary within it belongs to that implied DO
or a containing DATA implied DO.10.1.12 expressly admits the appropriately scoped index
primary; this is not a blanket ban on variables in subscripts.

**Diagnostic obligation:** required.

**Facets:** `current-index`, `containing-index`, `ordinary-value-exclusion`, `component-subscript`, `foreign-index-scope-gate`.

**Oracle:** Scoped index/constant-expression proof and one-iteration value-variable contrasts whose
count and range are fixed.

**Oracle limitation:** No ordinary executable loop, out-of-scope final-index value, generic undeclared-name
report or repeated-element initialization substitutes for the exact predicate.

**Dependencies:** R843-R845;10.1.12p1(13)/p2;19.4p1-p5;9.5.3;8.4;4.2.

### R846: A DATA value has an optional repeat-and-asterisk prefix and a required constant

**Source:** 8.6.7 R846, physical PDF137 **Class:** Syntax.

**Definition:** Each data-stmt-value has a required data-stmt-constant, optionally preceded by a
data-stmt-repeat and an asterisk. A zero repeat suppresses sequence contribution, not
the syntactic constant or its independently applicable source requirements.

**Diagnostic obligation:** required.

**Facets:** `unrepeated-value`, `repeated-value`, `repeat-asterisk`, `zero-still-needs-constant`, `repeat-value-owner`.

**Oracle:** Required-constant and asterisk syntax contrasts with repaired counts fixed, plus finite
repeated/unrepeated admissions.

DATA statement fixture family: one complete positive-control program initializes a
three-element INTEGER array from one unprefixed value and one explicit repeat. The
checks distinguish a default repeat of one from a repeated contribution without claiming
the missing-asterisk or zero-repeat syntax negatives.

**Oracle limitation:** Zero repetition is not a waiver for malformed syntax or an undefined named constant. Do
not infer a runtime effect from a zero-only declaration.

DATA statement fixture family boundaries: only `unrepeated-value`, `repeated-value` are
represented by this generator. All other facets, diagnostics, source-use links, reviews
and the existing DATA position effect fixtures remain independently managed. Generation
grants no review or baseline update.

**Dependencies:** R847/R848;C886-C891;p6-p8;4.2.

### R847: A repeat is a scalar integer constant or scalar integer constant subobject

**Source:** 8.6.7 R847, physical PDF137 **Class:** Syntax.

**Definition:** The repeat alternatives are scalar-int-constant and scalar-int-constant-subobject. These
are narrower than scalar-int-constant-expr. R607/C602, R849/C889, R403/C401 and C886
supply their distinct type, scalarity, subobject and value/order conditions.

**Diagnostic obligation:** required.

**Facets:** `literal-and-named-repeat`, `constant-element-repeat`, `constant-component-repeat`, `expression-not-repeat`, `inquiry-through-parameter`, `repeat-type-rank-graph`.

**Oracle:** Bounded literal/named/subobject admissions and a constant-expression-versus-constant
grammar contrast with the same repaired cardinality.

DATA statement fixture family: one complete positive-control program uses both a literal
repeat and a previously defined named INTEGER PARAMETER repeat on separate arrays, with
all elements checked. No signed, noninteger, forward or expression repeat form is
credited.

**Oracle limitation:** Do not treat any integer-valued constant expression as admitted, any initialized
variable as a named constant, or all DATA subobjects as integer-only. No numbered
wrapper reuse is implemented.

DATA statement fixture family boundaries: only `literal-and-named-repeat` are
represented by this generator. All other facets, diagnostics, source-use links, reviews
and the existing DATA position effect fixtures remain independently managed. Generation
grants no review or baseline update.

**Dependencies:** 6.2.3 R604/R607/C602;R403/C401;R849/C889/R850/C890/C891;C886;10.1.12.

### C886: Repeats are nonnegative and named repeat constants are defined previously

**Source:** 8.6.7 C886, physical PDF137 **Class:** Restriction.

**Definition:** A data-stmt-repeat is positive or zero. If the repeat itself is a named constant, that
constant has been defined previously. Constant-subobject parent references also retain
their separate8.5.13 requirements; the explicit named-constant condition is not silently
broadened to every expression.

**Diagnostic obligation:** required.

**Facets:** `positive-repeat`, `zero-repeat`, `negative-named-repeat`, `forward-named-repeat`, `subobject-definition-owner`.

**Oracle:** Nonnegative repeat values and source-order comparisons, with direct cause qualification
rather than relying on an uninterpretable negative expansion's length.

DATA statement fixture family: one complete positive-control program uses a positive
named repeat and a zero repeat in the same matched DATA list. The zero repeat consumes
no constant before a nonzero tail value, so the oracle distinguishes a zero contribution
from an omitted DATA statement.

**Oracle limitation:** No invalid repeat is executed or used as a resource test. Standard-required reporting
need not be fatal or print C886; an unsupported repetition feature is not the cause.

DATA statement fixture family boundaries: only `positive-repeat`, `zero-repeat` are
represented by this generator. All other facets, diagnostics, source-use links, reviews
and the existing DATA position effect fixtures remain independently managed. Generation
grants no review or baseline update.

**Dependencies:** R847;8.5.13p2;8.6.11;C889-C891;p6-p8;4.2.

### R848: DATA constants retain all nine grammar alternatives and their role conditions

**Source:** 8.6.7 R848, physical PDF137 **Class:** Syntax.

**Definition:** The alternatives are scalar constant, scalar constant subobject, signed integer literal,
signed real literal, null-init, initial-data-target, structure constructor, enum
constructor and enumeration constructor. The first inherits the literal/named forms,
including BOZ subject to its consumer restriction. This is not arbitrary
constant-expression grammar, and it has no direct initial-proc-target alternative.

**Diagnostic obligation:** required.

**Facets:** `scalar-intrinsic-constants`, `scalar-constant-subobjects`, `signed-integer`, `signed-real`, `null-init`, `initial-data-target`, `structure-constructor`, `enum-constructor`, `enumeration-constructor`, `arithmetic-expression-excluded`, `constant-versus-target-disambiguation`.

**Oracle:** Complete finite admissions for every alternative, with canonical
literal/constructor/target definitions and receiver roles resolved first.

DATA statement fixture family: one complete positive-control program reaches scalar
intrinsic constants (INTEGER, LOGICAL, CHARACTER, COMPLEX and a named constant), signed
integer literals of both signs, a signed REAL literal, NULL() pointer initialization and
an INTEGER initial data target. It observes values or defined association status only
for these portable default-kind contexts.

**Oracle limitation:** Scalar syntax is not general constant-expression syntax. Zero-repeat tokens still obey
applicable grammar/order rules. Initial association is not allocation or target
definition; REAL/COMPLEX/BOZ interpretation needs its actual qualified oracle.

DATA statement fixture family boundaries: only `scalar-intrinsic-constants`,
`signed-integer`, `signed-real`, `null-init`, `initial-data-target` are represented by
this generator. All other facets, diagnostics, source-use links, reviews and the
existing DATA position effect fixtures remain independently managed. Generation grants
no review or baseline update.

**Dependencies:** 6.2.3;7.4.3-.5;7.5.4.6 R744/C770;7.5.10;7.6.1-.2;7.7 C7119;8.2
R806/C813;9.3;10.1.12;C887-C891;p9-p11;15.4.3.6.

### C887: Named DATA constants and constructor types are previously defined

**Source:** 8.6.7 C887, physical PDF137 **Class:** Restriction.

**Definition:** When the DATA constant value is a named constant, structure constructor, enum
constructor or enumeration constructor, the named constant or corresponding type has
been defined previously. This is distinct from constructor constant-expression
eligibility and from the order requirements on parameters referenced inside subobjects.

**Diagnostic obligation:** required.

**Facets:** `prior-named-constant`, `forward-named-constant`, `prior-structure-type`, `prior-enum-type`, `prior-enumeration-type`, `forward-type-cause-gate`.

**Oracle:** Named-constant source-order pairs and explicit prior-type relationship checks;
unresolved isolation remains a gate, not a fabricated diagnostic family.

**Oracle limitation:** C888 constant-expression failure, nonexistent type, wrong constructor domain or an
incompatible receiver is not evidence for this ordering predicate.

**Dependencies:** 8.5.13p2;8.6.11;7.5.10p4;7.6.1 C7112;7.6.2 C7116;R848/C888;4.2.

### C888: DATA structure, enum and enumeration constructors are constant expressions

**Source:** 8.6.7 C888, physical PDF137 **Class:** Restriction.

**Definition:** Each constructor alternative named here is a constant expression under10.1.12. A valid
constructor spelling or an initialized variable argument is not sufficient;
pointer/allocatable component sources and enum/enumeration domains retain their defining
restrictions.

**Diagnostic obligation:** required.

**Facets:** `structure-argument`, `enum-argument`, `enumeration-argument`, `component-constant-expression-graph`.

**Oracle:** Previously defined types and scalar receivers with one nonconstant constructor argument
changed to a legal literal, plus canonical component-source relationships.

**Oracle limitation:** No invalid constructed value is executed. Default-initialized DATA receivers, private
components, duplicate component specifications, wrong ordinals and nonmatching types
must not contaminate a C888 pair.

**Dependencies:** R848/C887;10.1.12p1(3)-(5);7.5.10;7.6.1-.2;p2/p10;15.4.3.6;4.2.

### R849: An integer constant subobject uses the constant-subobject production

**Source:** 8.6.7 R849, physical PDF137 **Class:** Syntax.

**Definition:** int-constant-subobject is a constant-subobject; C889 supplies its integer type
restriction. Scalarity in the repeat role comes from the scalar prefix, not an
additional property of every constant subobject.

**Diagnostic obligation:** required.

**Facets:** `integer-element-alias`, `integer-component-alias`, `type-and-scalar-owner-graph`.

**Oracle:** Finite, role-bound canonical grammar relationships, not a new no-op execution.

**Oracle limitation:** A whole named constant belongs to a different R847 alternative; a subobject relationship
and the integer/scalar predicates must each be established.

**Dependencies:** R847/R850;C889-C891;R403/C401;8.5.13.

### C889: An int-constant-subobject has integer type

**Source:** 8.6.7 C889, physical PDF137 **Class:** Restriction.

**Definition:** The R849 subobject must have type integer. This condition does not make every
scalar-constant-subobject in the DATA value role integer-only.

**Diagnostic obligation:** required.

**Facets:** `integer-subobject`, `noninteger-repeat-subobject`, `value-role-distinction`.

**Oracle:** A constant, scalar, in-range subobject whose type alone fails the integer-repeat role,
paired with a one-name repair.

**Oracle limitation:** Do not use an unsupported intrinsic kind, a rank-one whole array or a mutable base and
then call the resulting error C889.

**Dependencies:** R847/R849/R850;C890/C891;R848;p10;4.2.

### R850: A constant subobject is expressed by a designator

**Source:** 8.6.7 R850, physical PDF137 **Class:** Syntax.

**Definition:** constant-subobject uses designator syntax. C890 requires an actual subobject of a
constant; C891 restricts addressing expressions. An arbitrary expression or function
reference is not this production, and a whole scalar named constant need not be
reclassified as a subobject.

**Diagnostic obligation:** required.

**Facets:** `constant-element`, `constant-substring`, `constant-component`, `designator-owner-graph`.

**Oracle:** Concrete constant-element/substring/component source paths and their canonical grammar,
type and identity conditions.

**Oracle limitation:** No automatic numbered-wrapper evidence mechanism is supplied. A constant expression is
not necessarily a constant-subobject designator.

**Dependencies:** R901;9.3/9.4.1-.2/9.5.3;C890-C891;8.5.13;R847-R849.

### C890: A constant-subobject designates a subobject of a constant

**Source:** 8.6.7 C890, physical PDF137 **Class:** Restriction.

**Definition:** The selected entity is a subobject of a constant, not merely a subobject of a variable
with a known initial value. Repeat and value contexts must be disambiguated before
choosing a diagnostic owner.

**Diagnostic obligation:** required.

**Facets:** `constant-parent-admission`, `mutable-repeat-parent`, `value-target-ambiguity-gate`.

**Oracle:** Constant-parent identity in an unambiguous repeat role with all other predicates
satisfied.

**Oracle limitation:** Do not repair by dropping PARAMETER while retaining an initializer on the eventual DATA
target, or conflate constancy with runtime value stability.

**Dependencies:** 3.34;6.2.3;9.3;8.5.13;R847-R850;C889/C891;p9;4.2.

### C891: Constant-subobject subscripts and substring endpoints are constant expressions

**Source:** 8.6.7 C891, physical PDF137 **Class:** Restriction.

**Definition:** Each subscript and each substring starting/ending point in a constant-subobject is a
constant expression. A constant parent does not make an ordinary variable selector
constant.

**Diagnostic obligation:** required.

**Facets:** `constant-selectors`, `variable-element-selector`, `variable-substring-start`, `variable-substring-end`, `inquiry-selector-owner`.

**Oracle:** Only the selected addressing expression changes; constant parent, type, role, scalarity
and repaired cardinality remain fixed.

**Oracle limitation:** Value-role ambiguous diagnoses remain gated. No undefined selector, out-of-range
substring, whole-array rank error or unsupported constant folding substitutes for C891.

**Dependencies:** R850/C890;10.1.12;9.4.1/9.5.3;8.5.13;R847/R848;4.2.

### S8.6.7-001: A default-initialized nonpointer variable is not a DATA object

**Source:** 8.6.7 p2, physical PDF136 **Class:** Restriction.

**Definition:** A nonpointer variable that has default initialization must not appear in the DATA object
list. The nonpointer condition matters: a data pointer of a default-initialized derived
type is not the same as a constructed target object. Default initialization is not
another explicit initialization occurrence or a general SAVE implication.

**Diagnostic obligation:** not-required.

**Facets:** `whole-derived-object`, `array-of-derived-objects`, `pointer-exception`, `subobject-default-status-gate`.

**Oracle:** Complete semantic source contrasts and admitted repairs with the
nonpointer/default-initialization conjunction explicit; source/policy qualification
precedes any negative fixture.

**Oracle limitation:** This is nonnumbered prose. Neither fatal rejection nor a specific report is
standard-required solely by p2. Declaration initialization can override default
initialization under7.5.4.6; that is not permission for DATA to do so.

**Dependencies:** 8.4p1;7.5.4.6p1/p6-p8;p9;R848/C888;19.6.3;4.1.2/4.2.

### S8.6.7-002: A later declaration of a previously untyped DATA variable confirms implicit typing

**Source:** 8.6.7 p3 first sentence, physical PDF136 **Class:** Restriction.

**Definition:** For a variable that appears in DATA without having been typed previously, a subsequent
type declaration is allowed only if it confirms the implicit typing. This does not
demand an explicit type declaration before every DATA statement and is separate from the
array-property ordering rule.

**Diagnostic obligation:** not-required.

**Facets:** `implicit-integer-confirmation`, `later-type-change`, `prior-explicit-type`, `type-parameter-qualification`.

**Oracle:** Trace type establishment at the DATA occurrence and the exact later declaration, in
complete scopes with a deliberately nonnull implicit mapping.

**Oracle limitation:** Do not accidentally use IMPLICIT NONE and turn the contrast into an undeclared-entity
error. Nonnumbered prose alone supplies no mandatory rejection requirement.

**Dependencies:** 8.7;8.2;5.1/5.3.2;p10;4.1.2/4.2.

### S8.6.7-003: DATA arrays have their array properties established by a previous specification

**Source:** 8.6.7 p3 second sentence, physical PDF136 **Class:** Restriction.

**Definition:** An array name, array section or array element appearing in DATA has its array properties
established by a previous specification statement. This DATA-specific ordering condition
is stricter than merely finding a matching array declaration somewhere later in the
unit.

**Diagnostic obligation:** not-required.

**Facets:** `prior-array-specification`, `late-whole-array`, `late-array-element`, `late-array-section`, `initialization-owner-graph`.

**Oracle:** Prior-versus-later declaration order with fixed one-element expansions and unchanged
implicit type; source-only violations require an explicit diagnostic policy before
negative execution contracts.

**Oracle limitation:** No shape inference from a DATA value list, later attribute repair, scalar-array
assignment broadcast or shape declaration for another scoped entity is a conforming
substitute.

**Dependencies:** 8.4 S8.4-003;8.2;8.5.8;8.7;9.5.2-.3;p5-p8;4.1.2/4.2.

### S8.6.7-004: DATA expansion establishes positionwise initial values and pointer items

**Source:** 8.6.7 p5-p8, p10-p11, physical PDF137-138 **Class:** Effect.

**Definition:** Expand the object list into pointer items and scalar variables, in the specified
array/section/implied-DO order, and expand the value list using its repeats. Zero-size
arrays and zero-iteration loops contribute no variables; zero-length scalar characters
still contribute one; a zero repeat contributes no constants. In a conforming
equal-length set, corresponding positions specify initial values, initial data targets
or null initialization. Ordinary value compatibility/conversion, pointer association and
BOZ-to-INTEGER conversion retain their canonical meanings; DATA does not execute an
assignment, broadcast one value across an unexpanded array or define untouched storage.

**Diagnostic obligation:** not-required.

**Facets:** `scalar-position-order`, `whole-array-element-order`, `triplet-section-order`, `vector-section-order`, `component-section-order`, `nested-implied-do-order`, `negative-step-order`, `outer-dependent-limit`, `repeat-and-default-one`, `zero-repeat-and-zero-array`, `zero-iteration`, `zero-length-scalar-position`, `pointer-position-order`, `array-pointer-single-item`, `character-length-conversion`, `derived-initial-value`, `enum-and-enumeration-values`, `integer-kind-conversion`, `integer-boz-qualified-value`, `real-complex-oracle-gate`, `initialization-not-execution`.

**Oracle:** Real initial-state observations in complete future programs, using independent small
INTEGER/default CHARACTER/LOGICAL literals and guarded known pointer states. Every
observed element/component is initialized once, every selected index/count is derived
from original source, and every check is reached. Canonical conversion/association/scope
meanings require explicit source relationships; the original source-only packet authored
no fixture or connection.

S8.6.7-004 finite DATA position effects: three complete run/effect/f2023 programs
initialize actual ordinary default INTEGER variables exactly once using DATA. The scalar
program observes i,j,k directly against independent11/22/33 before any executable
definition of those subjects, with three counted observations. The array program
declares a(2,3), initializes its whole name from the flat sequence11,21,12,22,13,23, and
checks six separate coordinates(1,1),(2,1),(1,2),(2,2),(1,3),(2,3) against those
independent literals. The rank-two Table9.1 order is1+(s1-1)+2*(s2-1); no RESHAPE, loop
or re-expanded DATA is the oracle. The nonexecution program calls one internal INTEGER
function whose explicitly declared ordinary local kept is directly checked as7 and
returned before the physical DATA kept/7/ statement. Prior explicit locality excludes
the DATA-only host-hiding condition of19.5.1.4p3. Main checks one entry, one
before-RETURN event, one normal caller return, one direct local check, result7 and zero
execution of the ordinary statement physically after DATA. Six caller checks precede
completion. All parents require exact stdout, empty runtime stderr and exit0. Every
guard and completion has a one-span full-source wrong-oracle plan. Omission plans remove
reached checks with their increments, omit the function call, replace its executable
prefix with a defined-result no-op, or remove RETURN so the after-DATA event is reached.
DATA initialization is preserved in every mutation. Only current passing complete
parents and intended runtime failures qualify.

DATA statement fixture family: five complete run/effect programs observe
repeat/default-one correspondence, zero-length CHARACTER still consuming one value,
same-kind CHARACTER padding/truncation with LEN guards, a qualified six-bit positive BOZ
INTEGER value, triplet and vector array-section order, scalar component section order,
and negative-step DATA implied-DO order. Each expected value is a nondefault literal
independent of the DATA spelling, and the four order cases deliberately distinguish
reversed or transposed expansion. The vector-subscript case uses a rank-one constant
array constructor section-subscript permitted by R921/R925/C929 and constant under C879.

**Oracle limitation:** All facets were pending at the original source-only registration. Compile success is not
runtime credit. A DATA statement is not an executable assignment: no automatic
allocation, scalar broadcast across unmatched lists, mandatory finalizer trace,
arbitrary deep-copy rule or storage-layout oracle follows. Undefined parts, dead
targets, zero-sized association ambiguities, unsupported kinds and unqualified REAL/BOZ
representations are excluded or remain explicit gates.

S8.6.7-004 finite DATA position boundaries: only scalar-position-order,
whole-array-element-order and initialization-not-execution are represented. The other18
owner facets and all foreign requirements/pending/control/raw-review states remain
independent. The DATA statements are initialization, not executable assignments or
runtime scalar broadcasting; the array has exactly six object and value contributions.
No subject has declaration/default/overlapping initialization,
dummy/result/COMMON/host-data role, pointer, allocation, optional kind, REAL, BOZ,
character conversion, component or implied-DO mechanism. The local kept is not the
function result. No mutation deletes DATA or reads an uninitialized subject or result.
Obsolescent placement under B.3.5 remains a conforming construct whose warning/report is
not a wrong-value failure; no warning-as-error allowance is added. No SAVE retention,
physical static storage, finalization, ABI or canonical graph is claimed.
Crashes/internal/resource/time failures, missing artifacts/traces, failed/stale parents
and preempting guards cannot establish sensitivity. Guard markers are program output,
not required ERROR STOP English or a universal error status. F2018 observations remain
supplementary to f2023 qualification. Generation grants no review and changes no
baseline, index or evidence inventory.

DATA statement fixture family boundaries: only `repeat-and-default-one`,
`zero-length-scalar-position`, `character-length-conversion`,
`integer-boz-qualified-value`, `triplet-section-order`, `vector-section-order`,
`component-section-order`, `negative-step-order` are represented by this generator. All
other facets, diagnostics, source-use links, reviews and the existing DATA position
effect fixtures remain independently managed. Generation grants no review or baseline
update.

**Dependencies:** p1/p5-p11;8.4;9.4.1-.2/9.5.2-.3;10.1.12;10.2.1.2-.3;11.1.7.4.1/.3;19.4/19.5.1.4/19.5.2/19.6.2-.4;7.4/7.5.10/7.6/7.7;16.3/16.4/16.9.20/16.9.47/16.9.110/16.9.118/16.9.166;B.3.5.

### S8.6.7-005: The two expanded DATA sequences have equal lengths

**Source:** 8.6.7 p8 final sentence, physical PDF138 **Class:** Restriction.

**Definition:** The expanded sequence of variables/pointers and expanded sequence of constants have the
same length. The count is after array/implied-DO/repeat expansion; zero-size arrays,
zero-iteration loops, zero repeats and zero-length scalar characters have their distinct
p7 contributions. Each DATA set has its own correspondence.

**Diagnostic obligation:** not-required.

**Facets:** `too-few-values`, `too-many-values`, `per-set-count`, `zero-contribution-counts`, `pointer-count-boundary`.

**Oracle:** Compute both finite sequence lengths from legal syntax and roles before any optional
diagnostic-policy test; verify repaired controls independently.

**Oracle limitation:** Nonnumbered length equality is not automatically a standard-mandated compile rejection.
Invalid negative repeats, out-of-range indices, repeated storage initialization or
malformed lists cannot masquerade as pure length failures.

**Dependencies:** R840-R848;C886;p5-p9;8.4;4.1.2/4.2.

### S8.6.7-006: Pointer DATA items use NULL or a compatible initial data target, and only pointer items do

**Source:** 8.6.7 p9, physical PDF138 **Class:** Restriction.

**Definition:** For each actual correspondence, null-init or initial-data-target is required if and only
if the DATA object has POINTER. A supplied initial data target must be
data-pointer-initialization compatible: type compatibility, equal rank, equal
nondeferred parameters, and a contiguous target when the pointer is CONTIGUOUS. Initial
disassociation/association themselves retain8.4/19.5.2 meanings; no target value is
initialized by pointer association.

**Diagnostic obligation:** not-required.

**Facets:** `pointer-with-ordinary-value`, `nonpointer-with-null`, `nonpointer-with-target`, `type-compatibility`, `rank-compatibility`, `nondeferred-parameters`, `contiguous-target`, `target-eligibility-owner`, `initial-association-owner`.

**Oracle:** Explicit two-direction receiver/form and target-compatibility source contrasts with
valid controls; canonical target/association conditions are checked independently.

DATA statement fixture family: one complete positive-control program observes DATA
NULL() producing a defined disassociated pointer and DATA initial-data-target producing
association with a live saved TARGET. It does not claim allocation, target definition
from association, procedure-pointer targets or incompatible parameter cases.

**Oracle limitation:** These p9 restrictions are nonnumbered, although C770 and other canonical constraints
still have their own reporting obligations. No mandatory fatal rejection, universal
pointer representation, zero-sized-target association inference or expired-target query
is permitted.

DATA statement fixture family boundaries: only `initial-association-owner` are
represented by this generator. All other facets, diagnostics, source-use links, reviews
and the existing DATA position effect fixtures remain independently managed. Generation
grants no review or baseline update.

**Dependencies:** R848;7.5.4.6p2/R744/C770;7.3.3;8.4p2;8.5.7/8.5.18;9.4.2/9.5.3-.4;19.5.2;16.9.20;15.4.3.6;4.1.2/4.2.

### S8.6.7-007: Ordinary DATA constants obey intrinsic-assignment compatibility at each correspondence

**Source:** 8.6.7 p10 first sentence, physical PDF138 **Class:** Restriction.

**Definition:** A DATA constant other than BOZ, null-init or initial-data-target must be compatible with
its corresponding variable according to10.2.1.2. The exceptions have p9/p11 rules. This
imports the compatibility relationship, not every executable-assignment action or a
scalar broadcast that could waive DATA's expanded-count rule.

**Diagnostic obligation:** not-required.

**Facets:** `intrinsic-type-pair`, `logical-type-pair`, `derived-type-identity`, `enum-identity`, `enumeration-identity`, `character-parameter-permissions`, `type-parameter-and-shape-owner`, `special-value-exclusions`.

**Oracle:** DATA-specific pair compatibility with all grammar/role/count premises already valid,
followed by canonical conversion relationships only on the permitted ordinary branch.

**Oracle limitation:** Nonnumbered prose does not require fatal compile rejection.10.2 supplies shared
compatibility/conversion meanings, not a universal DATA allocation, defined-assignment
invocation, finalization trace or deep-copy oracle.

**Dependencies:** p5-p11;10.2.1.2 Table10.8;10.2.1.3;7.3.3/7.5.10/7.6.1-.2;8.4;C880/C887/C888;4.1.2/4.2.

### S8.6.7-008: A contributing BOZ DATA constant corresponds to an INTEGER variable

**Source:** 8.6.7 p11 first sentence, physical PDF138 **Class:** Restriction.

**Definition:** When a BOZ constant contributes a value to a DATA correspondence, its corresponding
variable is INTEGER. The value interpretation is as INT with the receiver's kind. C7119
permits the DATA occurrence but does not waive this receiver domain; permissions for
REAL BOZ declaration initialization or executable assignment are different contexts.

**Diagnostic obligation:** not-required.

**Facets:** `integer-domain`, `real-data-exclusion`, `logical-character-exclusion`, `integer-constant-not-boz`, `zero-repeat-correspondence-gate`.

**Oracle:** Resolve the actual expanded correspondence and literal versus typed named-constant role,
using a minimal receiver-type repair for contributing invalid BOZ pairs.

DATA statement fixture family: one complete positive-control program uses a BOZ literal
with an INTEGER receiver and checks RADIX, BIT_SIZE and the independent decimal value
53. Noninteger receivers, high-bit interpretation and kind-width variation remain
pending.

**Oracle limitation:** This nonnumbered restriction is not mandatory fatal diagnosis. INT padding/truncation,
integer model/radix, kind capacity and high-bit interpretation remain separately
qualified. The existing REAL BOZ assignment NEEDS_ORACLE case and profile are preserved
unchanged.

DATA statement fixture family boundaries: only `integer-domain` are represented by this
generator. All other facets, diagnostics, source-use links, reviews and the existing
DATA position effect fixtures remain independently managed. Generation grants no review
or baseline update.

**Dependencies:** 7.7 C7119/p1-p2;6.2.3;16.3/16.4/16.9.110;p6-p8/p10;S10.2.1.3-017;4.1.2/4.2.

<!-- END GENERATED 8.6.7 -->
