# Fortran 2023 16.9.28: ATOMIC_ADD (ATOM, VALUE [, STAT])

The canonical catalogue is `doc/catalogues/atomic_add_16_9_28.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.28 -->

### S16.9.28-001: ATOMIC_ADD description identifies atomic addition

**Source:** S16.9.28, J3/24-007, 18 December 2023, physical PDF385. **Class:** Effect.

**Definition:** ATOMIC_ADD performs atomic addition.

**Diagnostic obligation:** not-required.

**Facets:** `atomic-add-operation-description`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming calls with sentinel variables where a value is observed, and
assert only portable properties fixed by this intrinsic section: exact type/kind
characteristics, exact logical or integer effects, source-stated equality between
intrinsic results, sign, zero, and bounded-range properties. Processor-dependent
approximations, multi-image scheduling effects, specific STAT numeric values beyond
zero/nonzero or named 16.5 constants, and IOMSG text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions in Clause 16 do not create diagnostic obligations.
Negative plans are source-control plans unless a future numbered rule requires rejection
and the rejection is portably observable. ATOMIC_ADD runtime facets are OUT OF SCOPE for
this suite's fixtures because 16.9.28#p3 requires ATOM to be a scalar coarray or
coindexed object, and coarray fixtures are outside this suite by user decision;
non-coarray ATOM plans are non-conforming and are not retained.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, and 10 for source form, declarations, coarray
designators, expressions, calls, and integer arithmetic; clause 16.5 for STAT values and
ISO_FORTRAN_ENV for ATOMIC_INT_KIND. Coarray designators and coarray execution are
outside this suite's fixture scope by user decision; these facets remain
source-accounting only unless a future coarray fixture profile is authorized.

### S16.9.28-002: ATOMIC_ADD is classified as an atomic subroutine

**Source:** S16.9.28, J3/24-007, 18 December 2023, physical PDF385. **Class:** Effect.

**Definition:** ATOMIC_ADD is an atomic subroutine.

**Diagnostic obligation:** not-required.

**Facets:** `atomic-add-atomic-subroutine-class`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming calls with sentinel variables where a value is observed, and
assert only portable properties fixed by this intrinsic section: exact type/kind
characteristics, exact logical or integer effects, source-stated equality between
intrinsic results, sign, zero, and bounded-range properties. Processor-dependent
approximations, multi-image scheduling effects, specific STAT numeric values beyond
zero/nonzero or named 16.5 constants, and IOMSG text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions in Clause 16 do not create diagnostic obligations.
Negative plans are source-control plans unless a future numbered rule requires rejection
and the rejection is portably observable. ATOMIC_ADD runtime facets are OUT OF SCOPE for
this suite's fixtures because 16.9.28#p3 requires ATOM to be a scalar coarray or
coindexed object, and coarray fixtures are outside this suite by user decision;
non-coarray ATOM plans are non-conforming and are not retained.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, and 10 for source form, declarations, coarray
designators, expressions, calls, and integer arithmetic; clause 16.5 for STAT values and
ISO_FORTRAN_ENV for ATOMIC_INT_KIND. Coarray designators and coarray execution are
outside this suite's fixture scope by user decision; these facets remain
source-accounting only unless a future coarray fixture profile is authorized.

### S16.9.28-003: ATOMIC_ADD ATOM argument satisfies coarray, type, kind, and definability restrictions

**Source:** S16.9.28, J3/24-007, 18 December 2023, physical PDF385. **Class:** Restriction.

**Definition:** ATOM shall be a scalar coarray or coindexed object of type integer with kind
ATOMIC_INT_KIND from ISO_FORTRAN_ENV and is an INTENT(INOUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `atomic-add-atom-scalar-coarray-or-coindexed`, `atomic-add-atom-integer-atomic-int-kind`, `atomic-add-atom-intent-inout`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming calls with sentinel variables where a value is observed, and
assert only portable properties fixed by this intrinsic section: exact type/kind
characteristics, exact logical or integer effects, source-stated equality between
intrinsic results, sign, zero, and bounded-range properties. Processor-dependent
approximations, multi-image scheduling effects, specific STAT numeric values beyond
zero/nonzero or named 16.5 constants, and IOMSG text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions in Clause 16 do not create diagnostic obligations.
Negative plans are source-control plans unless a future numbered rule requires rejection
and the rejection is portably observable. ATOMIC_ADD runtime facets are OUT OF SCOPE for
this suite's fixtures because 16.9.28#p3 requires ATOM to be a scalar coarray or
coindexed object, and coarray fixtures are outside this suite by user decision;
non-coarray ATOM plans are non-conforming and are not retained.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, and 10 for source form, declarations, coarray
designators, expressions, calls, and integer arithmetic; clause 16.5 for STAT values and
ISO_FORTRAN_ENV for ATOMIC_INT_KIND. Coarray designators and coarray execution are
outside this suite's fixture scope by user decision; these facets remain
source-accounting only unless a future coarray fixture profile is authorized.

### S16.9.28-004: ATOMIC_ADD VALUE argument is a representable integer scalar

**Source:** S16.9.28, J3/24-007, 18 December 2023, physical PDF385. **Class:** Restriction.

**Definition:** VALUE shall be an integer scalar INTENT(IN) argument, and both VALUE and ATOM + VALUE
shall be representable in kind ATOMIC_INT_KIND.

**Diagnostic obligation:** not-required.

**Facets:** `atomic-add-value-integer-scalar`, `atomic-add-value-and-sum-representable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming calls with sentinel variables where a value is observed, and
assert only portable properties fixed by this intrinsic section: exact type/kind
characteristics, exact logical or integer effects, source-stated equality between
intrinsic results, sign, zero, and bounded-range properties. Processor-dependent
approximations, multi-image scheduling effects, specific STAT numeric values beyond
zero/nonzero or named 16.5 constants, and IOMSG text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions in Clause 16 do not create diagnostic obligations.
Negative plans are source-control plans unless a future numbered rule requires rejection
and the rejection is portably observable. ATOMIC_ADD runtime facets are OUT OF SCOPE for
this suite's fixtures because 16.9.28#p3 requires ATOM to be a scalar coarray or
coindexed object, and coarray fixtures are outside this suite by user decision;
non-coarray ATOM plans are non-conforming and are not retained.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, and 10 for source form, declarations, coarray
designators, expressions, calls, and integer arithmetic; clause 16.5 for STAT values and
ISO_FORTRAN_ENV for ATOMIC_INT_KIND. Coarray designators and coarray execution are
outside this suite's fixture scope by user decision; these facets remain
source-accounting only unless a future coarray fixture profile is authorized.

### S16.9.28-005: ATOMIC_ADD no-error execution defines ATOM with the sum

**Source:** S16.9.28, J3/24-007, 18 December 2023, physical PDF385. **Class:** Effect.

**Definition:** If no error condition occurs, ATOM becomes defined with the value ATOM + VALUE; if an
error condition occurs, ATOM becomes undefined.

**Diagnostic obligation:** not-required.

**Facets:** `atomic-add-no-error-atom-sum`, `atomic-add-error-atom-undefined`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming calls with sentinel variables where a value is observed, and
assert only portable properties fixed by this intrinsic section: exact type/kind
characteristics, exact logical or integer effects, source-stated equality between
intrinsic results, sign, zero, and bounded-range properties. Processor-dependent
approximations, multi-image scheduling effects, specific STAT numeric values beyond
zero/nonzero or named 16.5 constants, and IOMSG text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions in Clause 16 do not create diagnostic obligations.
Negative plans are source-control plans unless a future numbered rule requires rejection
and the rejection is portably observable. ATOMIC_ADD runtime facets are OUT OF SCOPE for
this suite's fixtures because 16.9.28#p3 requires ATOM to be a scalar coarray or
coindexed object, and coarray fixtures are outside this suite by user decision;
non-coarray ATOM plans are non-conforming and are not retained.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, and 10 for source form, declarations, coarray
designators, expressions, calls, and integer arithmetic; clause 16.5 for STAT values and
ISO_FORTRAN_ENV for ATOMIC_INT_KIND. Coarray designators and coarray execution are
outside this suite's fixture scope by user decision; these facets remain
source-accounting only unless a future coarray fixture profile is authorized.

### S16.9.28-006: ATOMIC_ADD STAT argument and absent-STAT error behavior follow 16.5

**Source:** S16.9.28, J3/24-007, 18 December 2023, physical PDF385. **Class:** Effect.

**Definition:** STAT, if present, shall be a noncoindexed integer scalar with decimal exponent range at
least four and is INTENT(OUT); it is assigned a value as specified in 16.5. If an error
condition occurs and STAT is absent, error termination is initiated.

**Diagnostic obligation:** not-required.

**Facets:** `atomic-add-stat-noncoindexed-integer-scalar-range`, `atomic-add-stat-assigned-16-5`, `atomic-add-error-without-stat-terminates`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming calls with sentinel variables where a value is observed, and
assert only portable properties fixed by this intrinsic section: exact type/kind
characteristics, exact logical or integer effects, source-stated equality between
intrinsic results, sign, zero, and bounded-range properties. Processor-dependent
approximations, multi-image scheduling effects, specific STAT numeric values beyond
zero/nonzero or named 16.5 constants, and IOMSG text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions in Clause 16 do not create diagnostic obligations.
Negative plans are source-control plans unless a future numbered rule requires rejection
and the rejection is portably observable. ATOMIC_ADD runtime facets are OUT OF SCOPE for
this suite's fixtures because 16.9.28#p3 requires ATOM to be a scalar coarray or
coindexed object, and coarray fixtures are outside this suite by user decision;
non-coarray ATOM plans are non-conforming and are not retained.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, and 10 for source form, declarations, coarray
designators, expressions, calls, and integer arithmetic; clause 16.5 for STAT values and
ISO_FORTRAN_ENV for ATOMIC_INT_KIND. Coarray designators and coarray execution are
outside this suite's fixture scope by user decision; these facets remain
source-accounting only unless a future coarray fixture profile is authorized.

<!-- END GENERATED 16.9.28 -->
