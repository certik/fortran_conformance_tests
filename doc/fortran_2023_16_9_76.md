# Fortran 2023 16.9.76: DSHIFTR

The canonical catalogue is `doc/catalogues/dshiftr_16_9_76.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.76")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN INTRINSICS 16.9 H FIXTURES -->
## Intrinsics 16.9 H runtime observations

The `intrinsics_16_9_h` generator supplies 4 requirement bindings covering 7 portable facets in this section. Oracles use direct result inquiries, same-model DIGITS relationships, exact integer/logical/character/small-real results, and DSHIFT BTEST observations. DPROD processor-dependent approximation latitude and SHOULD guidance remain pending.
<!-- END INTRINSICS 16.9 H FIXTURES -->

<!-- BEGIN GENERATED 16.9.76 -->

### S16.9.76-001: DSHIFTR argument restrictions

**Source:** S16.9.76, J3/24-007, 18 December 2023, physical PDF405. **Class:** Restriction.

**Definition:** I and J shall be integer or BOZ literal constants with matching integer kinds when both
are integer, shall not both be BOZ literals, and SHIFT shall be an integer in the
permitted nonnegative BIT_SIZE range for DSHIFTR.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTR-I-integer-or-boz`, `DSHIFTR-J-integer-or-boz`, `DSHIFTR-integer-kinds-match`, `DSHIFTR-not-both-boz`, `DSHIFTR-SHIFT-integer-nonnegative-in-range`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.76-001 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.76-001 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.76-002: DSHIFTR result characteristics follow the integer operand

**Source:** S16.9.76, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** The result of DSHIFTR has the same type and kind as I when I is integer, otherwise the
same type and kind as J.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTR-result-kind-from-I-or-J`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.76-002 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.76-002 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.76-003: DSHIFTR converts a BOZ operand as INT to the other integer kind

**Source:** S16.9.76, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** If either I or J is a BOZ literal constant, DSHIFTR first converts it as if by INT to
integer with the kind type parameter of the other operand.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTR-boz-converted-as-int-to-other-kind`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.76-003 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.76-003 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.76-004: DSHIFTR result bit composition and intrinsic equivalence

**Source:** S16.9.76, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** The DSHIFTR result is formed from shifted bits of I and J and is equal to
IOR(SHIFTL(I,BIT_SIZE(I)-SHIFT), SHIFTR(J,SHIFT)); the integer-as-bits model is the one
in 16.3.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTR-shifted-bits-from-operands`, `DSHIFTR-ior-shift-equivalence`, `DSHIFTR-shift-zero-and-full-width-edges`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.76-004 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.76-004 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

<!-- END GENERATED 16.9.76 -->
