# Fortran 2023 8.5.8.2: Explicit-shape array

**Source review: reviewed.** Original source review is recorded in batch039;
current source, fixture and inventory adjudications are separate content-bound records.
Two run/effect programs represent six named-local entry-capture facets of S8.5.8.2-005.
1 S8.5.8.2-005 facet and 39 other local facets remain PENDING.

The catalogue is `doc/catalogues/explicit_shape_8_5_8_2.json`.
The original is J3/24-007, December 18, 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
All thirteen base units are on physical PDF page 125 and stop at the
actual 8.5.8.3 Assumed-shape array heading. The section hash is
`61897dae3dcb7ec46e19c38baffe3e9a057a0b44e1345e3d29d9a6c330ab7ffd`.
Original excerpts remain session evidence, not copied normative body text.

Scalar bounds use R815-R817 and the specification-expression contract.
R818/R819 instead supply rank-one INTEGER restricted vectors of constant
size, with both scalar/vector broadcasting combinations. Constant vector
size does not require constant values. A zero-size vector establishes a
scalar; a vector containing a zero bound can instead establish a zero-extent
array. Equal vector sizes do not mean equal values or equal index origins.

C831's four contexts are necessary, not sufficient. Component declarations
still use R740/C754/C755's scalar-list grammar and component expressions.
Ordinary POINTER/ALLOCATABLE arrays have their own C834 forms; assumed-rank
and scalar contexts must not be indiscriminately excluded. Function results
and dummy arguments retain their canonical declaration/association rules.

Main/module nonconstant-bound diagnostic candidates have a genuine
implicit-SAVE/C814 overlap. They remain source-gated rather than being
presented as isolated C831 negatives or copies of the existing C814 tests.
Every future report needs the exact source relation and a case-specific
cause; broad constant, kind, SAVE or expected-token text is insufficient.

Runtime plans use independent literal bounds, shape order, sizes and
defined data. For whole `a(5:3,-2:1)`, the qualified inquiry expectations
are shape `[0,4]`, size zero, lower bounds `[1,-2]` and upper bounds `[0,1]`.
The nonzero-extent dimension is not normalized merely because total size
is zero. No empty-array element access or unallocated inquiry is proposed.

Entry-capture plans observe actual procedure/BLOCK locals and retain their
bounds after bound-source redefinition or controlled undefinition. Expected
values never come from the changed source or another affected descriptor.
The existing S8.3 CHARACTER capture cases are not array-bound proxies.
No C814, S8.3, coarray, C819, CONTIGUOUS or guard work is changed.

The original batch039 source packet created no fixtures, compiler observations, profiles,
SourceUses entries, canonical links or approvals. The handoff records exact
case/review/evidence preservation and any source-context staleness without
renewing receipts or claiming future main counts.

<!-- BEGIN GENERATED 8.5.8.2 -->

### R815: A scalar explicit-shape specification has an upper bound and optional lower-bound colon

**Source:** 8.5.8.2 R815, physical PDF125, printed111, line2 **Class:** Syntax.

**Definition:** Each scalar explicit-shape-spec supplies an upper-bound. Its lower-bound and following
colon can be omitted together. The constituent bounds are specification expressions, not
declaration strides or unrestricted vector expressions; R818 supplies the separate
vector-bounds alternatives.

**Diagnostic obligation:** required.

**Facets:** `upper-only`, `explicit-lower`, `multidimensional-list`, `declaration-stride-excluded`.

**Oracle:** Grammar admissions and one minimally repaired, unambiguously malformed declaration
candidate. A form that can instead be assumed-shape or assumed-size is not an R815
negative merely because an upper bound is absent.

Batch314 scalar explicit-shape syntax controls: one run/positive-control/f2023 program
declares ordinary local INTEGER arrays with `a(4)`, `b(-2:1)` and `c(2,0:2)`. Direct
LBOUND/UBOUND/RANK assertions distinguish the omitted lower bound, explicit scalar lower
bound and two-entry scalar explicit-shape-spec list. Feature mutations replace the
upper-only form by a same-extent explicit lower, shift the explicit lower range, or
collapse the two-entry list to a rank-one declaration; all remain conforming and fail
before the completion line.

**Oracle limitation:** No prescribed fatal exit, diagnostic code or English wording. Unsupported vector/rank
features, type errors elsewhere, source echoes, crashes, verifier failures and
wrong-source recovery cannot establish the intended grammar condition.

Batch314 scalar explicit-shape syntax controls boundaries: Only scalar-list admissions
with small default INTEGER bounds are covered; declaration strides, vector bounds and
malformed alternatives remain pending.

**Dependencies:** R814;R816;R817;R818;10.1.11 R1029/C1011;4.2p2.

### R816: A scalar lower bound is a specification expression

**Source:** 8.5.8.2 R816, physical PDF125, line3 **Class:** Syntax.

**Definition:** The lower-bound nonterminal is a specification-expr. Its scalar integer and
restricted-expression requirements, context-dependent constancy, prior
typing/specification and function restrictions come from10.1.11 and10.1.12. An
explicit-bounds-expr in the vector form is a different nonterminal.

**Diagnostic obligation:** required.

**Facets:** `scalar-integer-lower`, `restricted-lower`, `prior-typing-inquiry`, `nonconstant-context-source-use`.

**Oracle:** Complete scalar-bound contexts and source-specific canonical expression gates, with
focused repaired controls for any proposed negative. The constancy of an inquiry is
decided from the particular property inquired about.

Batch314 lower-bound specification control: one run/positive-control/f2023 program uses
the scalar INTEGER lower bound in `a(-2:1)` and directly observes LBOUND(a)=[-2]. Its
feature mutation changes only that lower bound to -1 while preserving extent and
conformance, making the lower-bound assertion fail.

**Oracle limitation:** No blanket optional prose diagnostic or generic constant/expected-string gate.
Expression-category and prior-specification requirements keep their canonical owners; no
source-use credit is asserted.

Batch314 lower-bound specification control boundaries: Only the scalar INTEGER
lower-bound admission is covered; restricted-function, prior-typing and context-source
diagnostics remain pending.

**Dependencies:** 10.1.11p1-p9/R1029/C1011;10.1.12p1-p3;C831;16.9.119/16.9.194;8.3 C814.

### R817: A scalar upper bound is a specification expression

**Source:** 8.5.8.2 R817, physical PDF125, line4 **Class:** Syntax.

**Definition:** The upper-bound nonterminal is a specification-expr, with the scalar integer,
restricted-expression and applicable constancy/typing prerequisites of10.1.11
and10.1.12. This production neither requires positivity nor makes every bound expression
constant.

**Diagnostic obligation:** required.

**Facets:** `scalar-integer-upper`, `restricted-upper`, `prior-typing-inquiry`, `nonconstant-context-source-use`.

**Oracle:** Scalar integer specification-expression admission and exact type/expression-source
relations; negative candidates have minimally repaired valid controls and case-specific
causes.

Batch314 upper-bound specification control: one run/positive-control/f2023 program
admits positive, zero and negative scalar INTEGER upper bounds with `positive(3)`,
`zero(0:0)` and `negative(-3:-1)` and directly observes their upper bounds. Feature
mutations change upper-bound values while preserving valid ranges, so the upper-bound
assertions are load-bearing.

**Oracle limitation:** No arbitrary kind IDs, constant-value assumption from initialization, implicit-type
mismatch, unavailable inquiry, or unrelated generic kind/constant message. Ordinary
scalar and vector upper-bound productions stay distinct.

Batch314 upper-bound specification control boundaries: Only scalar INTEGER upper-bound
admissions are covered; restricted-function, prior-typing and context-source diagnostics
remain pending.

**Dependencies:** 10.1.11p1-p9/R1029/C1011;10.1.12;C831;16.9.194;8.3 C814.

### R818: Explicit vector bounds have four concrete scalar/vector combinations

**Source:** 8.5.8.2 R818, physical PDF125, lines5-7 **Class:** Syntax.

**Definition:** The first production permits an upper-bound vector alone or a lower-bound vector
followed by an upper-bound vector. The other productions pair a scalar lower-bound with
an upper-bound vector, or a lower-bound vector with a scalar upper-bound. R819/C833
govern vector expressions; R816/R817 govern scalar bounds. Component-array-spec has no
corresponding vector-bounds alternative.

**Diagnostic obligation:** required.

**Facets:** `upper-vector-only`, `vector-pair`, `scalar-lower-vector-upper`, `vector-lower-scalar-upper`, `zero-size-vector-boundary`.

**Oracle:** Complete conforming grammar admissions for the four combinations and zero-size boundary,
with meaningful runtime property plans under p1/p3 rather than a no-op classification
program.

**Oracle limitation:** Alternative parses must be resolved from the original grammar. Neither a vector placed
where a scalar was expected nor a scalar paired with a vector is automatically invalid.
No new compiler feature/profile assumption or duplicated canonical component case.

**Dependencies:** R814;R816;R817;R819;C831-C833;7.5.4.1 R740/C754/C755;8.5.8.2p1/p3.

### R819: An explicit bound vector starts from an integer expression

**Source:** 8.5.8.2 R819, physical PDF125, line8 **Class:** Syntax.

**Definition:** An explicit-bounds-expr is an int-expr. Its integer type is distinct from C833's
rank-one, restricted-expression and constant-size requirements. Integer element values
are not required to be constant or positive by this production.

**Diagnostic obligation:** required.

**Facets:** `integer-expression-admission`, `noninteger-type-source`, `c833-premises-source-use`.

**Oracle:** Type-specific integer-expression admission/causal relation; C833 supplies the additional
vector contract and keeps its own source ownership.

**Oracle limitation:** No fixed kind codes or compiler consensus. Do not credit an unsupported vector-bound
facility, unrelated shape error, source echo or broad expected-integer recovery at
another source point.

**Dependencies:** R818;C833;4.1.3;4.2p2;10.1.11.

### C831: Nonconstant explicit bounds are restricted to four contexts

**Source:** 8.5.8.2 C831, physical PDF125, lines9-10 **Class:** Restriction.

**Definition:** An explicit-shape-spec or explicit-shape-bounds-spec with bounds that are not constant
expressions occurs only in a subprogram, derived type definition, BLOCK construct or
interface body. These contexts are necessary, not sufficient: the actual grammar,
restricted/component expression, prior specification, initialization and SAVE conditions
still apply.

**Diagnostic obligation:** required.

**Facets:** `constant-context-admissions`, `subprogram-nonconstant`, `derived-type-nonconstant`, `block-nonconstant`, `interface-nonconstant`, `main-context-reporting-gate`, `module-context-reporting-gate`.

**Oracle:** Four complete context admissions plus honest source-gated forbidden-context candidates
with minimal constant-bound repairs. Source-defined constancy is tested by the
particular expression/property, not by the presence of a variable token.

Batch314 explicit-shape context controls: one run/positive-control/f2023 program covers
constant main/module bounds and nonconstant subprogram and BLOCK contexts. Module and
main arrays use named constants; a subroutine declares `local(n)` from an INTENT(IN)
dummy; a BLOCK declares `b(n)` from a host scalar defined before the BLOCK statement.
SIZE assertions use independent literals. Feature mutations change the named constant,
the subprogram actual, or the host scalar before BLOCK, all as conforming programs that
fail the corresponding assertion.

**Oracle limitation:** C831 reporting capability is required, but no particular report format or fatal exit is
mandated. The main/module candidates have a genuine C814 overlap requiring independent
causal adjudication; unsupported facilities, unrelated type errors or general
constant/SAVE nouns cannot stand in for that work.

Batch314 explicit-shape context controls boundaries: Only constant main/module,
subprogram and BLOCK positive controls are covered. Derived-type, interface-body and
forbidden main/module nonconstant diagnostics remain pending.

**Dependencies:** 4.2p2;10.1.11p1-p9;10.1.12p1-p3;7.5.4.1 R740/C754/C755;8.2 C808;8.3
C814;8.5.16p4/C862;15.6.2.2.

### C832: Two explicit bound vectors have equal sizes

**Source:** 8.5.8.2 C832, physical PDF125, line11 **Class:** Restriction.

**Definition:** When an explicit-shape-bounds-spec contains two explicit-bounds-exprs, their sizes
agree. This does not require equal element values or equal declared lower bounds of the
vector objects, and it does not apply a two-vector size test to a scalar/vector mixed
production.

**Diagnostic obligation:** required.

**Facets:** `equal-vector-sizes`, `unequal-vector-sizes`, `empty-vector-pair`, `scalar-vector-boundary`.

**Oracle:** Exact constant sizes in a focused two-vector mismatch/control pair, plus zero-size and
scalar/vector boundary admissions.

**Oracle limitation:** No forced fatal status, message wording, arbitrary extent proxy or guessed kind. The
source location and actual two-vector size relation must both be established;
declaration recovery and unsupported facilities do not count.

**Dependencies:** R818;R819;C833;8.5.8.2p1/p3;4.2p2.

### C833: An explicit bound vector is restricted, rank one, integer and constant-size

**Source:** 8.5.8.2 C833, physical PDF125, lines12-13 **Class:** Restriction.

**Definition:** An explicit-bounds-expr is a restricted expression that has rank one, integer type and
constant size. Its element values need not be constant, and its constant size need not
be positive. It is not the scalar specification-expr nonterminal; scalar/vector
alternative parsing and the particular restricted-expression premises remain
significant.

**Diagnostic obligation:** required.

**Facets:** `rank-one-integer`, `restricted-admission`, `constant-size-not-values`, `zero-constant-size`, `rank-two-excluded`, `noninteger-owner-source`, `nonrestricted-function-excluded`, `nonconstant-size-excluded`.

**Oracle:** Independent checks of the four conjuncts, with focused minimal repairs for rank,
nonrestricted function and nonconstant-size candidates, and one source-preserving
canonical type relation.

**Oracle limitation:** No blanket demand for constant bound values, no rank-zero pseudo-negative that parses
legally, and no ignored context/typing defect. Future causal contracts exclude broad
kind/constant/expected strings, source echoes, unsupported language features, crashes,
verifier failures and wrong-source recovery.

**Dependencies:** R818/R819;C831/C832;10.1.11p2-p8;10.1.12;16.9.185/16.9.194;4.2p2.

### S8.5.8.2-001: The list length or bound-vector size determines rank

**Source:** 8.5.8.2 p1 rank clauses, physical PDF125, lines14-16 **Class:** Effect.

**Definition:** The rank established by an explicit-shape-spec-list is its number of specifications.
With explicit-shape-bounds-spec it is the size of a bound vector. A zero-size bound
vector therefore establishes rank zero; a zero extent among an array's dimensions does
not reduce that array's rank.

**Diagnostic obligation:** not-required.

**Facets:** `list-rank`, `vector-rank`, `zero-vector-scalar`.

**Oracle:** Direct declared-object rank and legal shape/value checks with independent small literal
expectations. The scalar/array classifications are not separate no-op programs.

Batch314 explicit-shape list-rank effect: one run/effect/f2023 program declares
constant-bound explicit-shape arrays in a module and main program, including a
zero-extent first dimension, and directly requires RANK=2 on the declared objects. The
feature mutation changes the main two-specification list to one specification, so the
direct rank assertion fails while the program remains conforming.

**Oracle limitation:** No fixed-destination RANK, constructor proxy, corank substitution or compiler-derived
expected value. Zero rank and zero total array size remain distinct; the argument
contracts of every inquiry apply.

Batch314 explicit-shape list-rank effect boundaries: Only scalar
explicit-shape-spec-list rank is covered. Vector-bounds rank and zero-size vector scalar
behavior remain pending because the reference compiler rejects the F2023 vector-bound
forms on this host.

**Dependencies:** R814/R815/R818;C832/C833;5.4.6;16.9.171;16.9.185;16.9.194.

### S8.5.8.2-002: Scalar bounds establish each dimension and an omitted lower bound is one

**Source:** 8.5.8.2 p2, physical PDF125, lines17-18 **Class:** Effect.

**Definition:** The scalar lower/upper values establish the bounds and resulting extent of their
corresponding dimension. An omitted lower bound is one. Bound inquiry results can
normalize empty dimensions and expressions, so they are not unconditional echoes of the
written bounds.

**Diagnostic obligation:** not-required.

**Facets:** `upper-only-default`, `per-dimension-bounds`, `mixed-omitted-lowers`.

**Oracle:** Actual whole-array inquiries and defined element checks with literal bounds/extents in
dimension order, after every allocation/association or argument premise applicable to
the chosen object is satisfied.

Scalar explicit-shape bounds runtime fixtures: one complete run/effect/f2023 program
declares three ordinary local INTEGER arrays with scalar explicit-shape-spec lists.
`upper_only(4)` observes omitted lower bound one through LBOUND=[1], UBOUND=[4],
SHAPE=[4], SIZE=4 and a nonzero whole-array payload. `explicit2d(-2:0,4:5)` observes
dimension-order lower bounds [-2,4], upper bounds [0,5], shape [3,2], total size six and
payload 52. `mixed(2,-1:1)` observes an omitted lower bound in only the first dimension,
lower [1,-1], upper [2,1], shape [2,3], total size six and payload 63. Feature
substitutions replace the upper-only declaration with a same-extent nonunit lower bound
and remove explicit lower bounds from the other declarations, so default-lower and
per-dimension lower-bound behavior are load-bearing rather than descriptor aliases.

**Oracle limitation:** Use small representable INTEGER values, no kind-code folklore or descriptor-derived
oracle. No illegal subscript or unallocated/disassociated bound inquiry is a negative
twin for this effect.

Scalar explicit-shape bounds fixture boundaries: only scalar explicit-shape-spec-list
effects on ordinary local INTEGER arrays are covered. This packet does not claim
vector-bound syntax, rank-zero scalar vector-boundary behavior, dummy sequence
association, function results, diagnostics, type parameters, storage layout, contiguity,
coarrays, COMMON/EQUIVALENCE, allocation, pointer association or named-local nonconstant
entry-capture semantics.

**Dependencies:** R815-R817;8.5.8.2p4;9.5.2;9.5.3.3;16.9.119/16.9.185/16.9.194/16.9.215.

### S8.5.8.2-003: Vector position and element order determine bounds, with scalar broadcasting

**Source:** 8.5.8.2 p3, physical PDF125, lines19-22 **Class:** Effect.

**Definition:** A vector immediately before the colon provides lower bounds; otherwise it provides upper
bounds. Its elements correspond to dimensions in order. A scalar lower-bound or
upper-bound in the vector production is used in every dimension. If lower bounds are
absent, they are all one.

**Diagnostic obligation:** not-required.

**Facets:** `vector-pair-order`, `scalar-lower-broadcast`, `scalar-upper-broadcast`, `omitted-lower-vector-default`, `vector-index-origin-independence`.

**Oracle:** Independent literal rank, bounds, shape and size for all four R818 combinations, with
defined data when inspected. Constant vector size and C832 agreement are established
before the declarations.

**Oracle limitation:** No array-component vector syntax under R740, no rank/field/stride proxy, and no
compile-only replacement for these runtime effects. Empty-dimension inquiry conventions
are separate from bound-vector indexing.

**Dependencies:** R818/R819;C831-C833;10.1.11/10.1.12;9.5.2/9.5.3.3;16.9.119/16.9.185/16.9.194/16.9.215.

### S8.5.8.2-004: Bound order determines inclusive or empty ranges and zero extents

**Source:** 8.5.8.2 p4, physical PDF125, lines23-26 **Class:** Effect.

**Definition:** Either bound can be positive, negative or zero. When the upper bound is at least the
lower bound, the dimension's valid subscripts run inclusively between them. Otherwise
the range is empty, that dimension's extent is zero, and the array has zero total size.

**Diagnostic obligation:** not-required.

**Facets:** `bound-signs`, `inclusive-nonempty-range`, `singleton-zero-bound`, `empty-one-dimensional`, `mixed-zero-extent`, `inquiry-normalization-source`.

**Oracle:** Finite valid range/extent effects using independently known integer bounds and direct
inquiries on eligible objects. Empty arrays have no element-read or invalid-program
twin.

Scalar explicit-shape range runtime fixtures: two complete run/effect/f2023 programs
exercise scalar bound signs, inclusive nonempty ranges, singleton zero bounds and empty
ranges. The range program declares positive_range(2:4), negative_range(-2:0) and
zero_singleton(0:0), assigns nonzero whole-array payloads, observes bounds/shape/size
and reads the actual lower and upper endpoint elements through LBOUND/UBOUND-derived
subscripts. The empty program declares empty1(5:3) and empty2(5:3,-2:1), observes rank,
normalized whole-array bounds, shapes [0] and [0,4], SIZE=0 for both, and a nonzero
scalar path sentinel without reading any nonexistent element. Feature substitutions turn
ranges into same-rank nonempty or default-lower declarations, so zero extent, inclusive
range and bound-sign effects are distinguished while every mutant remains a conforming
program.

**Oracle limitation:** No negative-size allocation surrogate, unsigned arithmetic or guessed byte layout. Pure
effects are not mandatory rejection tests, and empty extent does not authorize
unallocated or disassociated inquiries.

Scalar explicit-shape range fixture boundaries: only valid scalar-list declarations with
small default INTEGER bounds are covered. Empty arrays are observed only through
permitted whole-array inquiries and a separate path sentinel; no element access, section
inquiry boundary, assumed-size, optional dummy, allocation or processor diagnostic
behavior is asserted. The separate inquiry-normalization-source facet remains pending.

**Dependencies:** 5.4.6p1;9.5.2;9.5.3.3;16.9.119p3-p5;16.9.215p3-p5;16.9.185;16.9.194.

### S8.5.8.2-005: Named local explicit bounds are captured on entry and remain stable during that activation

**Source:** 8.5.8.2 p5 establishment and stability clauses, physical PDF125, lines27-31 **Class:** Effect.

**Definition:** For a named local explicit-shape array of a subprogram or BLOCK construct with
nonconstant bounds, evaluating the bound expressions on procedure entry or execution of
BLOCK establishes the bounds and shape. Redefining or undefining variables during that
procedure or construct does not alter those established bounds. This is not a global
rule for every dummy, pointer, allocatable, result or coarray descriptor.

**Diagnostic obligation:** not-required.

**Facets:** `procedure-entry`, `post-redefinition`, `post-undefinition`, `block-entry`, `fresh-entries`, `nested-blocks`, `vector-bound-capture`.

**Oracle:** Two complete run/effect/f2023 programs implement only the first six facets. The
procedure case declares an ordinary unsaved local INTEGER a(-2:n) after its nonoptional
INTEGER INTENT(INOUT) n. Calls with incoming n=3 and n=1 supply independent scalar
literal upper/extent/value expectations3/6/101 and1/4/102. The payload is
assigned100+visit before inquiry. Each activation checks the actual whole array with
LBOUND, UBOUND, SIZE, SHAPE and all-element value comparisons, first on entry, then
after n=0, then after a separate plain INTEGER INTENT(OUT) helper leaves only n
undefined and increments its distinct initialized event counter. No read of n occurs in
that last window; n is restored from the independent upper expectation before return and
checked again by main. Entry, change, undefinition, return and
twenty-procedure-check-per-entry counts are independently consumed. The BLOCK case
executes the same outer BLOCK twice with n=3/1, then takes nested inner bounds1:5/1:2
after redefining the host source. Distinct defined payloads201/202 and301/302, both
arrays' actual bounds/shape/size, redefinition to9, inner exit and outer lifetime are
checked with independent literal branch expectations. Main declares no array.
Entry/exit/event/check counters and exact completion stdout, empty stderr and normal
exit0 reject early-success/no-op behavior. Full-program one-span wrong-oracle probes
address every guard, each repeated activation separately, and both completion literals,
only on processors that execute the current complete parent.

**Oracle limitation:** Vector-bound-capture stays pending, and all twelve other explicit-shape requirements
retain their source, plans and reviews. These are scalar R815-R817 bounds, not
vector-bound syntax or copied S8.3 CHARACTER/type-parameter cases. The arrays are real
named procedure/BLOCK locals, never array dummies, constructor RANK arguments, second
expected descriptors, fixed destinations, allocatables, pointers, coarrays,
COMMON/EQUIVALENCE objects or C interop storage. No SAVE, declaration/DATA/default
initialization or escaped object masks their lifetime. Every payload is defined before
inspection; only scalar independent observer inputs or literal branch expectations are
used. The OUT helper neither reads nor defines the bound-source integer; all other
actuals are distinct and defined, and restoration precedes the caller's next read. Inner
arrays are never referenced after END BLOCK. Nonzero extents6/4 and5/2 make the
whole-array inquiry bounds literal rather than zero-extent normalization cases. Runtime
sensitivity requires completed builds, actual runtime and the intended unique
guard/output failure; a compile rejection, unsupported facility, resource failure or
crash is not a successful probe. ERROR STOP status/token behavior is empirically
qualified, not a universal printed-code or numeric-status requirement. Source, fixture,
native mode qualification and inventory adjudication remain separate; no approval,
baseline, source-use or canonical-link renewal is added.

**Dependencies:** 8.5.8.2 C831/C833;10.1.11/10.1.12;8.2
C808;8.3p1/C814;8.5.16;15.5.2.5p20;19.6.6p1(15);16.9.119/16.9.185/16.9.194/16.9.215;existing
S8.3-001 and C814 witnesses.

<!-- END GENERATED 8.5.8.2 -->

<!-- BEGIN BATCH314 ARRAY SHAPES 8.5.8.2 -->

## Batch314 scalar explicit-shape controls

This packet adds positive controls for scalar explicit-shape syntax and C831 contexts, plus one list-rank runtime effect. It intentionally leaves vector-bound and source-gated diagnostic facets pending because the reference compiler rejects the F2023 vector-bound forms or the catalogue calls for separate causal adjudication. Existing scalar-bound and entry-capture generator regions are preserved.

<!-- END BATCH314 ARRAY SHAPES 8.5.8.2 -->

<!-- BEGIN EXPLICIT SHAPE SCALAR EFFECTS -->

## Scalar explicit-shape runtime effects

Three complete run/effect/f2023 fixtures cover eight selected scalar-list facets without changing the existing explicit-bound capture fixtures. The scalar-bounds case observes `upper_only(4)`, `explicit2d(-2:0,4:5)` and `mixed(2,-1:1)` with independent LBOUND/UBOUND/SHAPE/SIZE and nonzero whole-array payload checks. The range case observes positive, negative and zero bounds, including actual endpoint reads through LBOUND/UBOUND-derived subscripts. The empty-range case observes normalized whole-array inquiries for `empty1(5:3)` and `empty2(5:3,-2:1)` plus a nonzero path sentinel, with no element access.

Feature substitutions are same-rank, conforming declarations: they supply a nonunit lower bound for the upper-only case, remove explicit lower bounds from same-shape nonempty arrays, and turn empty ranges into nonempty ranges. Guard, omission and input-sentinel mutations cover every observation and completion literal. S8.5.8.2-002 is fully represented here; S8.5.8.2-004 still leaves `inquiry-normalization-source` pending. Vector bounds, C831 diagnostics or contexts, dummy sequence association, function results and p5 entry-capture semantics remain with their existing pending plans or owner packets.

<!-- END EXPLICIT SHAPE SCALAR EFFECTS -->

<!-- BEGIN EXPLICIT BOUND CAPTURE -->

## Bounded ordinary-array entry capture

The procedure owns a real `integer :: a(-2:n)`. Its previously typed nonoptional
INOUT dummy arrives as3 then1; independent literal observer inputs specify
upper/extent/value3/6/101 then1/4/102. Defined payload precedes every inquiry.
The same named array is checked on entry, after n becomes0, and while n is
undefined following a separate plain INTEGER INTENT(OUT) helper. That helper
only increments its distinct event counter. Array inquiries and independent
expectations do not read n; restoration precedes return and the caller's read.

The BLOCK program has no main-program array. Two activations capture outer
`outer(-2:n)` at3/1 and inner `inner(1:n)` at5/2. A later source value9 changes
neither live array. Both have distinct defined payloads and literal branch
expectations. Inner exit is followed only by outer observations; outer exit
is followed only by live scalar counters, never expired objects.

Whole-array LBOUND/UBOUND/SIZE/SHAPE and all-element data checks use no second
descriptor oracle or constructor RANK. Positive entry/change/undefinition/
exit/check counts and exact completion lines reject no-op and early success.
Full-program one-span probes perturb every guard, separately on first and
second activations where it repeats, plus each completion literal. A failure
before runtime is not successful sensitivity evidence.

Vector-bound syntax remains pending. No SAVE, initializer, allocation, pointer,
coarray, COMMON/EQUIVALENCE, C interoperability, S8.3 proxy, fixture approval,
baseline change, canonical-link or SourceUse credit is supplied.

<!-- END EXPLICIT BOUND CAPTURE -->
