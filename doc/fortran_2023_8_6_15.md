# Fortran 2023 8.6.15: TARGET statement

The canonical catalogue is `doc/catalogues/target_statement_8_6_15.json`.
Effective source review comes from
`Registry.catalogue_review_state("8.6.15")`; current evidence-facet states
remain in the catalogue. A source definition or declaration admission is
not processor conformance, runtime target behavior or an approval.
Batch078 records independent source review and separate inventory-context
renewal, not fixture approval. All 16 local facets remain pending.

The complete original section is on physical PDF140, printed126, of
J3/24-007, December18,2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
Its section hash is
`7af9f0989f681ec6392cf431f079583e7802e8338615914bf3debd8c6531ea7b`.
All four base units are included: R862, R863, p1 and the unnumbered note.
The next heading is 8.6.16 on the same page. No continuation or neighboring
body belongs to the section.

R862 has optional double colons but a mandatory nonempty target-decl list.
It has no omitted-list all-objects form. R863 is not CONTIGUOUS's name-only
production: after an object-name it permits a parenthesized array-spec,
a bracketed coarray-spec, or both in that order. Omitting either group is
independent of omitting the other. Explicit R804/C810 governs the name and
its data-object role; the global 502-rule head scan confirms that this is
not an assumed R402 or array-name shortcut.

R814 and R809 supply the actual array/coarray grammars. Their appearance
inside R863 does not waive entity-category, bound, rank/corank, allocation
or scope constraints. Constant explicit shape gives a simple bounded
control. Assumed-shape/size/rank forms need their genuine dummy contexts;
deferred shape requires its ALLOCATABLE or POINTER owner, and POINTER is
independently excluded for TARGET. Implied-shape named constants are not
TARGET variables. A coarray needs the appropriate explicit/deferred
coshape, C822-C829 conditions and an actual configured capability/mode.
An empty parent heading for 8.5.6 does not replace its three subclauses.

Initial controls avoid duplicate specifications: a previously typed
ordinary variable can receive its sole shape/coshape in TARGET, while
an already shaped variable can be named without repeating those groups.
An apparent suffix such as `a(2)` in TARGET can be a declaration, not an
array-element reference; the CONTIGUOUS `p(:)` exclusion cannot be copied.
The note's rank-two declaration and name-only second item illustrate this
distinction. Its large bounds are not a required test allocation, and the
note supplies no complete context or observation.

C865/C866 retain variable and non-POINTER eligibility. TARGET is not
initialization, allocation or association. Nonpointer-subobject
propagation is owned by 8.5.18p2, and a pointee reached through a pointer
has its own target and lifetime rules. Pointer allocation, rather than
a statement declaring the pointer itself TARGET, creates the implicit
target described by 9.7.1.4.

SAVE and TARGET do not merge lifetimes. TARGET of a local object does not
preserve it after exit; saving a pointer does not preserve a dead target.
An actual without TARGET can have a present TARGET dummy during a call,
but escaped association, VALUE, noncontiguous actuals and post-return
status follow their precise 15.5.2.4/.5 and 19.5.2.5 conditions. Optional
absence and unallocated/disassociated/undefined states also remain
separate. No post-exit inquiry, trap, retained-value, copying or address
oracle is invented.

Scope is not borrowed from another attribute statement. Object names in
TARGET are an explicit local-identifier route in 19.5.1.4p2, so a host
homonym does not by itself supply a local type or inherited target role.
USE-associated attribute changes are restricted by 14.2.2p9. TARGET is a
nonexceptional BLOCK specification under 11.1.4p2/19.4, and complete local
construct declarations must be supplied rather than assuming the outer
object was modified.

Finite canonical source/occurrence relationships remain pending.
Assignments to a variable or function result named `target` are not
TARGET statements, and type-declaration attributes do not automatically
become R862/R863 witnesses. Existing attribute, shape, bound, COMMON,
pointer and runtime owners retain their exact programs, roles,
fingerprints and reviews. The source classification introduces no local
S effect, fixture, compiler run, canonical link, source use or baseline
entry.

<!-- BEGIN GENERATED 8.6.15 -->

### R862: A TARGET statement has optional colons and a required target-declaration list

**Source:** 8.6.15 R862, J3/24-007, 18 December 2023, physical PDF140, printed126. **Class:** Syntax.

**Definition:** TARGET is followed by an optional double colon and a nonempty target-decl list. R401
supplies list expansion and R863 supplies the mandatory object name plus independently
optional array/coarray declarator groups. There is no listless all-objects TARGET form.
The statement specifies the canonical TARGET attribute, subject to each entity's
eligibility and scope.

**Diagnostic obligation:** required.

**Facets:** `double-colon-form`, `no-double-colon-form`, `multiple-target-declarations`, `missing-target-list`, `list-separator-repair`, `variable-and-single-route-source`, `scope-state-and-canonical-source`.

**Oracle:** Complete statement-form admissions and minimally repaired nonempty-list/separator
contrasts, using independently eligible variables and actual R863 declarators. Exact
source/name/role mappings precede any future canonical reuse or diagnostic predicate.

**Oracle limitation:** Numbered reporting capability does not mandate fatal exit, printed R862 or standardized
English. A condition-bound complete diagnostic can qualify only with its true input,
mode, phase, control and live-origin evidence; source echoes, unrelated context,
unsupported facilities or native/internal/resource failures do not. Declaration
admission does not establish allocation, pointer association, defined data, persistent
target lifetime or physical storage layout. No source/case/link/inventory approval
follows from classification.

**Dependencies:** 4.1.1;4.1.3R401;4.2;8.2R801-R804/C810;8.5.1C815;8.5.14C854;8.5.18C865/C866/p1/p2;R863;11.1.4p2;14.2.2p9;19.3.1;19.4;19.5.1.4p2;9.7.1.4;10.2.2.1-.3;15.5.2.4/.5/.13;19.5.2.2/.5.

### R863: A target declaration can specify array shape and coarray coshape after an object name

**Source:** 8.6.15 R863, physical PDF140. **Class:** Syntax.

**Definition:** A target-decl has a required object-name, optionally followed by a parenthesized
array-spec and then independently by a bracketed coarray-spec. Either or both groups can
be absent; when both appear the array group comes first. Explicit R804/C810 governs the
data-object name. R814 and R809 supply the actual array/coarray alternatives with their
separate category, rank, corank, bound and state restrictions. This production is not
CONTIGUOUS's name-only list.

**Diagnostic obligation:** required.

**Facets:** `bare-scalar-name`, `inline-explicit-array-shape`, `array-category-source`, `inline-explicit-coarray-spec`, `array-and-coarray-groups`, `deferred-coarray-source`, `declarator-delimiters-and-order`, `whole-name-not-designator`, `attribute-shape-and-lifetime-source`.

**Oracle:** Classify the actual required name and independently optional declaration groups before
choosing complete finite controls. Keep array/coarray syntax, eligible variable
categories, declaration order, scope and compiler capability separate. Future
source/interface/reuse plans must bind exact full programs and roles; no generic
declaration classifier or no-op runtime stands in for these relationships.

**Oracle limitation:** R863 supplies neither initialization nor POINTER or ALLOCATABLE by itself. A permitted
array-spec form is not valid for every entity category, and a coarray-spec requires its
actual constraints and configured support. Declaration-level success cannot prove
association, payload, target lifetime, image execution or storage representation.
Existing TARGET/SAVE/COMMON, bound, pointer and argument owners remain unchanged; every
new finite evidence plan is pending until independently implemented and adjudicated.

**Dependencies:** 4.1.1;4.1.3p2;6.2.2R603/C601;8.2R803/R804/C810;8.3;8.5.1C815;8.5.3;8.5.6.1R809/C822-C826;8.5.6.2R810/C827;8.5.6.3R811-R813/C828-C829;8.5.8.1R814/8.5.8.2-.7;8.5.16p4;8.5.18;11.1.4p2;14.2.2p9;19.4;19.5.1.4p2;9.7.1.4;10.2.2.1-.3;15.5.2.4/.5/.13;19.5.2.2/.5;19.6.6.

<!-- END GENERATED 8.6.15 -->
