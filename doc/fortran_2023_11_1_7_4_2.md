# Fortran 2023: 11.1.7.4.2 DO CONCURRENT loop control

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.1.7.4.2 -->

### S11.1.7.4.2-001: DO CONCURRENT limit and step expressions may be evaluated in any order

**Source:** 11.1.7.4.2 p1; J3/24-007 PDF p213. **Class:** Effect.

**Definition:** The concurrent-limit and concurrent-step expressions in the concurrent-control-list are
evaluated, and these expressions may be evaluated in any order.

**Diagnostic obligation:** not-required.

**Facets:** `concurrent-control-evaluation-order-unobservable`.

**Oracle:** This records processor or source latitude, not a required observable behavior. It
supports rejecting order-sensitive or latitude-sensitive oracles and cannot support a
test that requires the processor to exercise any particular order, grouping, temporary,
or implementation strategy.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 10.1.4 evaluation latitude registered;15 pure procedure side-effect rules NOT YET
REGISTERED.

### S11.1.7.4.2-002: Each DO CONCURRENT index-name value set is established from converted integer bounds and a nonzero step

**Source:** 11.1.7.4.2 p1; J3/24-007 PDF p213. **Class:** Effect.

**Definition:** For each index-name, the lower bound, upper bound, and step are integers with the same
kind type parameter as the index-name and are established by evaluating the first
concurrent-limit, second concurrent-limit, and concurrent-step with numeric conversion
as needed; an absent concurrent-step has value one; m3 shall not be zero; if max = (m2 -
m1 + m3) / m3 is nonpositive for some index-name, execution of the construct is
complete; otherwise the index-name value set is m1 + (k - 1) * m3 for k = 1, 2, ...,
max.

**Diagnostic obligation:** context-dependent.

**Facets:** `concurrent-bounds-converted-to-index-kind`, `concurrent-default-step-one`, `concurrent-zero-step-source-control`, `empty-index-set-completes-construct`, `positive-index-set-values`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 10.1.5.2.1 integer operations registered;10.1.5.2.2 integer division registered;10.2.1.3
assignment effects registered;11.1.7.2 concurrent-header syntax owned by parallel
packet.

### S11.1.7.4.2-003: DO CONCURRENT active combinations are the Cartesian product filtered by the scalar mask

**Source:** 11.1.7.4.2 p2-p4; J3/24-007 PDF p213. **Class:** Effect.

**Definition:** The set of combinations of index-name values is the Cartesian product of the value sets
from each triplet specification; an index-name becomes defined when this set is
evaluated. The scalar mask expression, if any, is evaluated for each combination, absent
mask is as if true, index-name variables may be primaries in the mask, and active
combinations are exactly those possible combinations for which the mask is true.

**Diagnostic obligation:** not-required.

**Facets:** `cartesian-product-combinations`, `index-name-defined-for-mask`, `mask-evaluated-for-each-combination`, `absent-mask-all-true`, `index-names-usable-in-mask`, `active-combinations-true-mask-only`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 10.1 expression semantics registered;11.1.7.4.3 DO CONCURRENT block execution in this
packet.

<!-- END GENERATED 11.1.7.4.2 -->
