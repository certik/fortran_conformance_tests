# Fortran 2023 16.9.62: COSD

The canonical catalogue is `doc/catalogues/cosd_16_9_62.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN INTRINSICS 16.9.F FIXTURES -->

## Intrinsics 16.9.F runtime observations

This batch adds 1 generated f2023 runtime fixtures for 16.9.62, covering 1 portable pending facets with exact integer, logical, kind, shape/rank, or exactly representable zero-value oracles. Feature mutations are conforming source substitutions that must compile and fail at run time on both toolchains. Approximate COS/COSD/COSH/COSPI values and nonzero command-argument support remain pending where the standard provides no portable oracle.
<!-- END INTRINSICS 16.9.F FIXTURES -->

<!-- BEGIN GENERATED 16.9.62 -->

### S16.9.62-001: COSD argument has the required type

**Source:** S16.9.62, J3/24-007, 18 December 2023, physical PDF399. **Class:** Restriction.

**Definition:** X shall be of type real.

**Diagnostic obligation:** not-required.

**Facets:** `cosd-real-argument`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found no tests/ or tests/fixtures/*/fixture.json binding
for the COSD intrinsic.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.62-002: COSD result has the same characteristics as its argument

**Source:** S16.9.62, J3/24-007, 18 December 2023, physical PDF399. **Class:** Effect.

**Definition:** The result characteristics are the same as X.

**Diagnostic obligation:** not-required.

**Facets:** `cosd-result-same-characteristics`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

S16.9.62-002 intrinsics_16_9_f fixture: COSD result characteristics are checked directly
with KIND(COSD(X)) and SHAPE(COSD(X)) inquiries; no exact COSD value is asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found no tests/ or tests/fixtures/*/fixture.json binding
for the COSD intrinsic.

S16.9.62-002 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.62-003: COSD result is an approximation to the specified mathematical function

**Source:** S16.9.62, J3/24-007, 18 December 2023, physical PDF399. **Class:** Effect.

**Definition:** The result is a processor-dependent approximation to the cosine of X regarded as
degrees.

**Diagnostic obligation:** not-required.

**Facets:** `cosd-mathematical-function-approximation`, `cosd-argument-degrees`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found no tests/ or tests/fixtures/*/fixture.json binding
for the COSD intrinsic.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.62-004: COSD approximation value is processor dependent

**Source:** S16.9.62, J3/24-007, 18 December 2023, physical PDF399. **Class:** Effect.

**Definition:** The processor has latitude in the approximation used for the mathematical result.

**Diagnostic obligation:** not-required.

**Facets:** `cosd-approximation-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found no tests/ or tests/fixtures/*/fixture.json binding
for the COSD intrinsic.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

<!-- END GENERATED 16.9.62 -->
