# Fortran 2023 16.9.58: CO_SUM

The canonical catalogue is `doc/catalogues/co_sum_16_9_58.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.58 -->

### S16.9.58-002: CO_SUM argument A has numeric, correspondence, and noncoindexed restrictions

**Source:** S16.9.58, J3/24-007, 18 December 2023, physical PDF397. **Class:** Restriction.

**Definition:** A shall be numeric, have the same shape, type, and type parameter values in
corresponding references, shall not be coindexed, and is INTENT(INOUT).

**Diagnostic obligation:** not-required.

**Facets:** `co_sum-a-numeric`, `co_sum-a-corresponding-shape-type-parameters`, `co_sum-a-not-coindexed`, `co_sum-a-intent-inout`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Coarray and image-dependent facets are marked OUT OF SCOPE for this suite's
single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.58-003: CO_SUM assigns the computed value to A on the result image when no error occurs

**Source:** S16.9.58, J3/24-007, 18 December 2023, physical PDF397. **Class:** Effect.

**Definition:** If no error condition occurs and RESULT_IMAGE is absent or identifies the executing
image, the computed value is assigned to A; otherwise A becomes undefined.

**Diagnostic obligation:** not-required.

**Facets:** `co_sum-single-image-absent-result-image-assigns-a`, `co_sum-nonresult-image-a-undefined`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Coarray and image-dependent facets are marked OUT OF SCOPE for this suite's
single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.58-004: CO_SUM cross-image computed values are sums of corresponding A values

**Source:** S16.9.58, J3/24-007, 18 December 2023, physical PDF397. **Class:** Effect.

**Definition:** For scalar A the computed value is the sum of A in corresponding references; for array A
each element is the sum of corresponding elements.

**Diagnostic obligation:** not-required.

**Facets:** `co_sum-scalar-corresponding-sum`, `co_sum-array-element-corresponding-sums`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Coarray and image-dependent facets are marked OUT OF SCOPE for this suite's
single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.58-005: CO_SUM numeric sum approximation is processor dependent

**Source:** S16.9.58, J3/24-007, 18 December 2023, physical PDF397. **Class:** Effect.

**Definition:** The sum assigned by CO_SUM is a processor-dependent approximation to the mathematical
sum.

**Diagnostic obligation:** not-required.

**Facets:** `co_sum-sum-approximation-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Coarray and image-dependent facets are marked OUT OF SCOPE for this suite's
single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.58-006: CO_SUM RESULT_IMAGE is a same-valued integer scalar image index when present

**Source:** S16.9.58, J3/24-007, 18 December 2023, physical PDF397. **Class:** Restriction.

**Definition:** RESULT_IMAGE, if present, shall be an integer scalar, present with the same value in all
corresponding references, and have the value of an image index in the current team.

**Diagnostic obligation:** not-required.

**Facets:** `co_sum-result-image-integer-scalar`, `co_sum-result-image-same-presence-and-value`, `co_sum-result-image-current-team-index`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Coarray and image-dependent facets are marked OUT OF SCOPE for this suite's
single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.58-007: CO_SUM STAT and ERRMSG arguments have the declared noncoindexed scalar forms

**Source:** S16.9.58, J3/24-007, 18 December 2023, physical PDF397. **Class:** Restriction.

**Definition:** STAT, if present, shall be a noncoindexed integer scalar with decimal exponent range at
least four; ERRMSG, if present, shall be a noncoindexed default character scalar.

**Diagnostic obligation:** not-required.

**Facets:** `co_sum-stat-noncoindexed-integer-scalar-range`, `co_sum-errmsg-noncoindexed-default-character-scalar`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Coarray and image-dependent facets are marked OUT OF SCOPE for this suite's
single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

<!-- END GENERATED 16.9.58 -->
