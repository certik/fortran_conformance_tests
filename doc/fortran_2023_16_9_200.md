# Fortran 2023 16.9.200: STORAGE_SIZE

The canonical catalogue is `doc/catalogues/storage_size_intrinsic_16_9_200.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.200")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, note1, note2, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.200 -->

### S16.9.200-001: STORAGE_SIZE argument restrictions

**Source:** S16.9.200, J3/24-007, 18 December 2023, physical PDF462. **Class:** Restriction.

**Definition:** A shall be a data object of any type; polymorphic, unlimited polymorphic,
deferred-parameter, allocatable, and pointer actual arguments have the stated defined,
allocated, and associated status restrictions; KIND shall be a scalar integer constant
expression.

**Diagnostic obligation:** not-required.

**Facets:** `STORAGE_SIZE-A-data-object-any-type`, `STORAGE_SIZE-polymorphic-not-undefined-pointer`, `STORAGE_SIZE-unlimited-or-deferred-not-unallocated-or-disassociated`, `STORAGE_SIZE-KIND-scalar-integer-constant`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. STORAGE_SIZE values for arbitrary entities are processor
storage-model facts; portable plans use result characteristics, dynamic-type equality,
and ISO_FORTRAN_ENV named-constant consistency rather than absolute storage sizes.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.200-002: STORAGE_SIZE result is an integer scalar with selected kind

**Source:** S16.9.200, J3/24-007, 18 December 2023, physical PDF462. **Class:** Effect.

**Definition:** The result is an integer scalar; KIND present selects the kind type parameter and absent
KIND uses default integer.

**Diagnostic obligation:** not-required.

**Facets:** `STORAGE_SIZE-result-integer-scalar`, `STORAGE_SIZE-result-default-kind`, `STORAGE_SIZE-result-KIND-selected`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. STORAGE_SIZE values for arbitrary entities are processor
storage-model facts; portable plans use result characteristics, dynamic-type equality,
and ISO_FORTRAN_ENV named-constant consistency rather than absolute storage sizes.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.200-003: STORAGE_SIZE uses dynamic type and type parameters

**Source:** S16.9.200, J3/24-007, 18 December 2023, physical PDF462. **Class:** Effect.

**Definition:** The result is the storage size in bits for an array element having the dynamic type and
type parameters of A.

**Diagnostic obligation:** not-required.

**Facets:** `STORAGE_SIZE-dynamic-type-used`, `STORAGE_SIZE-type-parameters-used`, `STORAGE_SIZE-array-element-size`, `STORAGE_SIZE-size-expressed-in-bits`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. STORAGE_SIZE values for arbitrary entities are processor
storage-model facts; portable plans use result characteristics, dynamic-type equality,
and ISO_FORTRAN_ENV named-constant consistency rather than absolute storage sizes.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.200-004: STORAGE_SIZE consistency with ISO_FORTRAN_ENV named constants

**Source:** S16.9.200, J3/24-007, 18 December 2023, physical PDF462. **Class:** Effect.

**Definition:** For types and type parameters covered by storage association or ISO_FORTRAN_ENV sized
kind constants, STORAGE_SIZE is consistent with the named bit-size constants from
ISO_FORTRAN_ENV.

**Diagnostic obligation:** not-required.

**Facets:** `STORAGE_SIZE-int8-storage-size`, `STORAGE_SIZE-int16-storage-size`, `STORAGE_SIZE-int32-storage-size`, `STORAGE_SIZE-int64-storage-size`, `STORAGE_SIZE-real32-storage-size`, `STORAGE_SIZE-real64-storage-size`, `STORAGE_SIZE-default-character-length-storage-units`, `STORAGE_SIZE-KIND-result-kind`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. STORAGE_SIZE values for arbitrary entities are processor
storage-model facts; portable plans use result characteristics, dynamic-type equality,
and ISO_FORTRAN_ENV named-constant consistency rather than absolute storage sizes.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.200-005: STORAGE_SIZE absolute values are processor storage-model facts

**Source:** S16.9.200, J3/24-007, 18 December 2023, physical PDF462. **Class:** Effect.

**Definition:** The standard specifies the returned quantity but not a portable absolute bit count for
arbitrary dynamic types, padding, alignment, or expression storage models.

**Diagnostic obligation:** not-required.

**Facets:** `STORAGE_SIZE-arbitrary-absolute-size-no-portable-oracle`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. STORAGE_SIZE values for arbitrary entities are processor
storage-model facts; portable plans use result characteristics, dynamic-type equality,
and ISO_FORTRAN_ENV named-constant consistency rather than absolute storage sizes.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

<!-- END GENERATED 16.9.200 -->
