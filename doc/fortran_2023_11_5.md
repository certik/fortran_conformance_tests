# Fortran 2023: 11.5 FAIL IMAGE statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.5 -->

### R1165: FAIL IMAGE statement syntax is FAIL IMAGE

**Source:** 11.5 R1165; J3/24-007 PDF p229. **Class:** Syntax.

**Definition:** A fail-image-stmt is FAIL IMAGE.

**Diagnostic obligation:** required.

**Facets:** `fail-image-stmt-form`.

**Oracle:** Syntax admission only; image failure effects are owned by p1 and image execution
semantics.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 11.7.x image execution control parallel/unregistered;5.3 image execution and 16
ISO_FORTRAN_ENV image inquiry dependencies;this suite has no multi-image execution
capability.

### S11.5-001: Executing FAIL IMAGE makes the image cease participation and execute no further statements

**Source:** 11.5 p1; J3/24-007 PDF p229. **Class:** Effect.

**Definition:** Execution of a FAIL IMAGE statement causes the executing image to cease participating in
program execution without initiating termination. No further statements are executed by
that image.

**Diagnostic obligation:** not-required.

**Facets:** `image-ceases-participation`, `no-termination-initiated-by-fail-image`, `no-further-statements-on-failed-image`.

**Oracle:** Effects are image-lifecycle effects, not process-exit-value effects. A single-image run
cannot portably observe failed-image participation without the unregistered multi-image
owners.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 5.3.6 failed images and 5.3.7 termination;11.7.x image control parallel/unregistered;16
ISO_FORTRAN_ENV image status constants/procedures;multi-image execution capability
absent.

<!-- END GENERATED 11.5 -->
