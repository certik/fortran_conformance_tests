# Fortran 2023 8.5.10: INTENT attribute

**Source independently reviewed in batch053.** Every implementation facet
remains pending. Effective source status is
`Registry.catalogue_review_state("8.5.10")`. No case, source-use,
canonical-link or compiler approval follows from source accounting.

The canonical catalogue is `doc/catalogues/intent_attribute_8_5_10.json`.
The seventeen original base units span physical PDF pages 128-130 of
J3/24-007, December 18, 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The replayed section hash is
`f7a24f1767ca25bcc37000a8ac8f8ebcfc3191813fa44f2a40b0bf49bdf1c4c5`.
The scope ends before the actual 8.5.11 INTRINSIC heading. Both `note4`
and its separately inventoried continuation `note4.2` are accounted for.

The declaration syntax, dummy categories, syntactic context prohibitions
and invocation/execution semantics have separate owners. The complete
variable-definition and pointer-association context lists remain finite
canonical coverage tasks, not claims proved by one assignment example.
Coarray and opaque intrinsic-type negatives require valid surrounding
declarations and exact causes; unsupported features are not that evidence.

Pointer association is distinct from definition of the target. An IN
pointer component cannot be reassociated merely because its enclosing
object is nonpointer, but its live definable target can be modified under
the actual association and aliasing rules. Other-route target deallocation
can leave an IN pointer undefined; that does not permit querying the
undefined association.

OUT entry undefinition has no portable value or trap oracle. The two
undefined-state S requirements therefore use `undefined-result` and plan
only context evidence and source relations. Default initialization,
finalization and deallocation retain their existing effect owners and
their precise ordering, containing-object, presence and state conditions.
No existing effect is cloned or retagged to force a context-only link.

The global 19.6.2 qualification still applies: zero-sized arrays and
zero-length strings are always defined. This does not establish presence,
allocation or pointer association, and supplies no undefinedness oracle.

An actual for a nonpointer OUT/INOUT dummy must be definable, not
necessarily already defined. A data-pointer function reference can designate
a definable target in that context, while an OUT/INOUT pointer dummy has
the distinct pointer-variable/non-result restriction. The statement-wide
OUT association interference prohibition is retained without assuming
an argument-evaluation order or a required outcome of an invalid program.

Omitted INTENT is not INOUT. Meaningful read-only expression-actual
controls and conditional-write controls must preserve the effective
argument's limitations. The notes' possible checking and optimization
do not create new compulsory warnings, traps or generated-code contracts.

This source-only registration adds no tests, observations, case approvals
or evidence links. Source and inventory contexts are explicitly reviewed;
every proposed executable, source/use relation and diagnostic qualification
remains an explicitly pending facet.

<!-- BEGIN GENERATED 8.5.10 -->

### R828: An intent specification is IN, OUT or INOUT

**Source:** 8.5.10 R828, physical PDF128, printed114, lines24-26 **Class:** Syntax.

**Definition:** The intent-spec alternatives are IN, OUT and INOUT. Their entity eligibility and
semantics are separate from the spelling. INOUT is one alternative, not a
comma-separated pair of IN and OUT specifications.

**Diagnostic obligation:** required.

**Facets:** `in-admission`, `out-admission`, `inout-admission`, `malformed-specification`.

**Oracle:** Three complete grammar admissions and minimally repaired nonalternatives. Keep syntax,
entity role and runtime meaning separate.

**Oracle limitation:** Required reporting capability does not prescribe fatal status, exact English or a
printed rule number. Case-insensitive spelling retains its lexical owner; unsupported
features and unrelated recovery do not qualify.

**Dependencies:** 8.2;8.6.9 INTENT statement;C845-C849;4.2p2.

### C845: INTENT is restricted to dummy data objects and dummy procedure pointers

**Source:** 8.5.10 C845, physical PDF128, line27 **Class:** Restriction.

**Definition:** An entity with INTENT must be a dummy data object or a dummy procedure pointer. An
ordinary dummy procedure without POINTER is not the second alternative; neither is a
local procedure pointer or ordinary local data variable.

**Diagnostic obligation:** required.

**Facets:** `dummy-data-admission`, `dummy-procedure-pointer-admission`, `nondummy-exclusion`, `nonpointer-dummy-procedure-exclusion`.

**Oracle:** Entity-category negatives and minimal controls with precise role diagnostics and
legitimate source origins.

**Oracle limitation:** No broad 'dummy argument' predicate, fatal-exit promise, unsupported-feature credit or
source-line widening. The two allowed categories do not waive their other restrictions.

**Dependencies:** 8.5.14;15.4.3.6;15.5.2.6-.8/.10;4.2p2.

### C846: A nonpointer INTENT(IN) object cannot appear in a variable definition context

**Source:** 8.5.10 C846, physical PDF128, lines28-29 **Class:** Restriction.

**Definition:** A nonpointer object with IN intent cannot occur in any variable definition context
defined by19.6.7. The context list includes syntactic uses that imply definition or
undefinition; a branch that would not execute does not remove the syntactic restriction.
Pointer target references and inherited subobject intent require their exact separate
interpretation.

**Diagnostic obligation:** required.

**Facets:** `assignment-and-loop-contexts`, `input-and-internal-output`, `io-result-specifiers`, `allocation-output-contexts`, `out-inout-association`, `associate-selector-propagation`, `coarray-control-contexts`, `nonpointer-subobjects`, `read-only-controls`.

**Oracle:** A finite context census plus isolated syntax/relationship diagnoses with intent-only or
receiving-intent repairs. For every case, bind the actual IN object, its context and the
precise cause.

**Oracle limitation:** The required diagnostic is not necessarily fatal or code-labelled. Generic
I/O/type/coarray errors, source echoes, missing-interface recovery and native compiler
failures do not qualify. Canonical source-use coverage is not manufactured from one
program.

**Dependencies:** 19.6.7p1(1)-(15)/p2;8.5.10p2/p6;11.1;12;16.10.2;4.2p2.

### C847: An INTENT(IN) pointer cannot occur in a pointer association context

**Source:** 8.5.10 C847, physical PDF128, line30 **Class:** Restriction.

**Definition:** An IN pointer is excluded from the pointer association contexts in19.6.8. Those contexts
alter the pointer as a pointer; an ordinary definition of its associated definable data
target is a different operation.

**Diagnostic obligation:** required.

**Facets:** `nullify-context`, `pointer-assignment-context`, `allocate-deallocate-context`, `pointer-out-inout-actual`, `target-operation-controls`.

**Oracle:** All four19.6.8 context families, narrowly repaired, plus the actual pointer/target
boundary. Source and complete-program controls distinguish association from target
definition.

**Oracle limitation:** No mandatory trap, status or printed code. ASSOCIATED is used only for defined
association status. The p2 other-route deallocation exception does not authorize an
otherwise forbidden pointer association context.

**Dependencies:** 19.6.8;19.5.2.5;8.5.10p2/p6 and note3;10.2.2;15.5.2.8/.10;4.2p2.

### C848: Nonintrinsic OUT dummies exclude allocatable coarrays and allocatable-coarray subobjects

**Source:** 8.5.10 C848, physical PDF128, lines31-32 **Class:** Restriction.

**Definition:** An OUT dummy of a nonintrinsic procedure cannot itself be an allocatable coarray or
contain a subobject that is an allocatable coarray. Ordinary allocatable objects and
eligible nonallocatable coarrays are not excluded merely by this constraint. The
nonintrinsic-procedure condition is retained.

**Diagnostic obligation:** required.

**Facets:** `allocatable-coarray-dummy`, `allocatable-coarray-subobject`, `nonallocatable-coarray-control`, `ordinary-allocatable-control`, `nonintrinsic-boundary-source`.

**Oracle:** Direct/subobject declaration negatives with intent-only repairs and two category
controls; keep a capability failure distinct from a qualifying report.

**Oracle limitation:** Required reporting has no fatal/text/code mandate. Unsupported coarrays, invalid
containing types, missing deferred coshape, descriptor failures or generic OUT errors
are not causal C848 evidence.

**Dependencies:** 7.5.2/7.5.4 coarray-component restrictions;8.5.6;15.5.2.7/.9;9.7.3.2p6;4.2p2.

### C849: OUT entities exclude EVENT_TYPE, LOCK_TYPE and NOTIFY_TYPE subcomponents

**Source:** 8.5.10 C849, physical PDF128, lines33-35 **Class:** Restriction.

**Definition:** An OUT entity cannot be of EVENT_TYPE, LOCK_TYPE or NOTIFY_TYPE from ISO_FORTRAN_ENV, or
have a subcomponent of one of those types. The type identity is the intrinsic-module
identity, not its local spelling. The rule is not an exemption from the separate
named-variable/coarray or variable-definition restrictions.

**Diagnostic obligation:** required.

**Facets:** `event-type-out`, `lock-type-out`, `notify-type-out`, `nested-subcomponents`, `in-inout-controls`.

**Oracle:** Three exact intrinsic-type exclusions and source-qualified nested routes, with minimal
intent controls. Declaration-only isolation avoids a competing prohibited
actual-argument definition context.

**Oracle limitation:** No fatal/text/code mandate, opaque-value guess or byte/layout assumption. A missing
coarray attribute, unsupported intrinsic module/type, prohibited actual call or
private-component reference is not C849 evidence.

**Dependencies:** 16.10.2.10 C1603-C1606;16.10.2.19 C1607-C1610;16.10.2.22 C1611-C1614;7.5.1;9.4.2;4.2p2.

### S8.5.10-001: A nonpointer IN dummy is neither defined nor made undefined during invocation and execution

**Source:** 8.5.10 p2 first sentence, physical PDF128, lines36-37 **Class:** Restriction.

**Definition:** Throughout invocation and execution, a nonpointer IN dummy must not be defined or become
undefined. This semantic restriction includes invocation and indirect routes, not only
explicit assignment syntax. It does not itself require an initially defined value to be
referenced, and is not permission to use unavailable data.

**Diagnostic obligation:** context-dependent.

**Facets:** `defined-read-controls`, `definition-route-census`, `undefinition-route-census`, `allocation-subobject-relations`.

**Oracle:** Finite valid-use controls plus canonical source/use and constraint relations.
Definedness restrictions are not tested by observing a value after making a program
nonconforming.

**Oracle limitation:** A specific numbered or scope rule can require reporting; the prose prohibition alone
does not. Preserve context-dependent diagnosis and positive-control roles instead of
relabelling a read-only admission as a runtime no-undefinition effect.

**Dependencies:** C846;19.6.5/.6/.7;15.5.2.4/.7/.13/.14;8.5.10p6;4.2p2.

### S8.5.10-002: An IN pointer preserves association except for other-route target deallocation

**Source:** 8.5.10 p2 second sentence, physical PDF128 lines37-38 through PDF129 lines1-2 **Class:** Restriction.

**Definition:** During invocation and execution, an IN pointer dummy's association must not change,
except that deallocation of its target by another route may make its association
undefined. Intent restricts association, not every operation on the target. A nonpointer
actual to an eligible IN pointer dummy follows its own pointer-association route rather
than being assigned a fabricated effective argument.

**Diagnostic obligation:** context-dependent.

**Facets:** `data-association-controls`, `procedure-association-controls`, `target-definition-controls`, `other-route-deallocation-source`, `nonpointer-actual-source`.

**Oracle:** Valid association/target controls and separately accounted exception/source relations.
Any queried pointer association is independently established as defined.

**Oracle limitation:** No undefined-pointer trap/value oracle, no relaxed aliasing rule and no compulsory
S-level rejection. The exception does not permit deallocation through the prohibited
IN-pointer context itself.

**Dependencies:** C847;19.5.2.5p1(3);15.5.2.4p5-p6;15.5.2.8/.10/.14;8.5.10p6 and note3.

### S8.5.10-003: A nonpointer OUT dummy loses definition on invocation except for default-initialized subcomponents

**Source:** 8.5.10 p3 first sentence, physical PDF129, printed115, lines3-4 **Class:** Undefined result.

**Definition:** Invocation makes a nonpointer OUT dummy undefined except for its default-initialized
subcomponents. The exception uses7.5.4.6's actual subcomponent and
containing-initialization rules. It does not preserve arbitrary pre-call values.
Allocation status, pointer association and finalization have distinct canonical entry
rules.

**Diagnostic obligation:** not-required.

**Facets:** `plain-entry-context`, `default-initialized-exception-source`, `allocation-finalization-relations`, `optional-absence-context`, `redefinition-before-use`.

**Oracle:** Context-only execution and canonical source relations, never a prescribed value after
undefinition. Actual default-initialization/finalization/deallocation effects retain
their own qualified runtime owners.

**Oracle limitation:** No negative twin, mandatory warning/trap, zero value, disassociation or physical-storage
erasure follows from undefinedness. Source/role relation mechanisms remain pending
instead of being bypassed through evidence retagging.

**Dependencies:** 7.5.4.6p3-p8;7.5.6.3p7;9.7.3.2p6;15.5.2.7/.13;19.5.2.3/.4/.5;19.6.5p1(24);19.6.6p1(15).

### S8.5.10-004: An actual for a nonpointer OUT dummy must be definable

**Source:** 8.5.10 p3 second sentence, physical PDF129, lines4-5 **Class:** Restriction.

**Definition:** An actual argument corresponding to a nonpointer OUT dummy must be definable. Definable
is not the same as already defined and is not restricted to an ordinary variable-name
spelling; a data-pointer function reference can designate a definable target
under19.6.7p2. Other actual/dummy restrictions still apply.

**Diagnostic obligation:** context-dependent.

**Facets:** `definable-variable-controls`, `nondefinable-source-contrasts`, `data-pointer-result-target`, `state-presence-and-array-limits`.

**Oracle:** Complete admissible actuals and a source-qualified finite definability relation, with
numbered diagnostics retained at their actual owners.

**Oracle limitation:** No mandatory prose rejection, blanket function-reference exclusion or read of initially
undefined data. A successful call to one writable scalar does not prove all definability
contexts.

**Dependencies:** 15.5.2.4-.7/.13/.14;19.6.7p1(10)/p2;C846;8.5.10p3 pointer alternative;4.2p2.

### S8.5.10-005: Invocation gives an OUT pointer dummy undefined association status

**Source:** 8.5.10 p3 pointer-entry sentence, physical PDF129, lines5-7 **Class:** Undefined result.

**Definition:** On invocation, an OUT pointer dummy has undefined association status. For a data pointer
actual,15.5.2.8p3 explicitly gives the actual the same entry consequence. Undefined
association is not the defined state of disassociation and does not by itself deallocate
or undefine the former target.

**Diagnostic obligation:** not-required.

**Facets:** `undefined-entry-source`, `data-pointer-reestablishment`, `procedure-pointer-reestablishment`, `former-target-context`.

**Oracle:** Source-bound undefined-state accounting and context-only executions that reestablish
status before observation.

**Oracle limitation:** No mandatory warning/trap, false ASSOCIATED result, zero address or physical null
representation. A post-reassociation pass is not runtime proof of the intermediate
undefined association.

**Dependencies:** 15.5.2.4p5;15.5.2.8p3;15.5.2.10;19.5.2.3-.5;19.6.6p1(16);10.2.2.

### S8.5.10-006: OUT pointer actuals are pointer variables or non-result procedure pointers

**Source:** 8.5.10 p3 pointer-actual sentence, physical PDF129, lines7-8 **Class:** Restriction.

**Definition:** An actual corresponding to an OUT pointer dummy must be a pointer variable or a
procedure pointer that is not the result of a function reference. Merely being a valid
target for an IN pointer dummy does not satisfy this OUT requirement.

**Diagnostic obligation:** context-dependent.

**Facets:** `data-pointer-variable`, `procedure-pointer-variable`, `function-result-exclusion-source`, `target-and-null-expression-boundaries`.

**Oracle:** Finite admitted pointer-variable contexts and explicit source restrictions on
result/target/expression alternatives.

**Oracle limitation:** No blanket required rejection of prose violations and no association inquiry before
status is established. Nonpointer OUT and pointer OUT actual rules are not merged.

**Dependencies:** 15.5.2.6/.8/.10;19.6.7p2;9.2;8.5.10p2/p3;4.2p2.

### S8.5.10-007: OUT association changes cannot affect another entity in the invoking statement

**Source:** 8.5.10 p3 final sentence, physical PDF129, lines8-10 **Class:** Restriction.

**Definition:** Definition or undefinition implied by associating an actual with an OUT dummy must not
affect another entity within the statement invoking the procedure. Both induced
definition and induced undefinition matter; the scope is the whole invoking statement,
not only the invoked body or one written argument.

**Diagnostic obligation:** not-required.

**Facets:** `invoking-statement-use-census`, `default-initialization-interference`, `deallocation-finalization-relations`, `independent-precomputed-controls`.

**Oracle:** Finite whole-statement source/use accounting and valid independent-entity controls. A
nonconforming interference path has no required runtime value.

**Oracle limitation:** No compulsory trap/rejection and no assumed left-to-right evaluation, short circuit or
copy timing. Required diagnosis from some overlapping numbered rule remains with that
rule.

**Dependencies:** 15.5.2.14;10.1 expression evaluation;7.5.4.6;7.5.6.3p7;9.7.3.2p6;19.6.5/.6.

### S8.5.10-008: A nonpointer INOUT dummy always requires a definable actual

**Source:** 8.5.10 p4 first sentence, physical PDF129, lines11-12 **Class:** Restriction.

**Definition:** An actual corresponding to a nonpointer INOUT dummy must be definable, even if that
invocation never writes the dummy. INOUT does not impose OUT's entry undefinition.
Definability alone does not assert that all data are initially defined or permit reading
an undefined part.

**Diagnostic obligation:** context-dependent.

**Facets:** `definable-actual-controls`, `nonmodifying-inout-source`, `partial-definition-and-retention`, `optional-and-attribute-boundaries`.

**Oracle:** Finite admissible definable-actual controls and source-qualified unconditional
definability, with separate definedness premises for every read.

**Oracle limitation:** No mandatory prose rejection, guarantee of a write, initially defined-value requirement
or blanket ban on data-pointer function references. Controls retain positive-control
roles.

**Dependencies:** 15.5.2.4-.7/.13/.14;19.6.5p1(6);19.6.7;8.5.10p5;4.2p2.

### S8.5.10-009: INOUT pointer actuals have the pointer-variable restriction

**Source:** 8.5.10 p4 second sentence, physical PDF129, lines12-14 **Class:** Restriction.

**Definition:** An INOUT pointer dummy requires a pointer variable or a procedure pointer that is not a
function-reference result as its actual. IN's qualified nonpointer-target alternatives
do not apply. Unlike OUT, INOUT does not itself make a previously defined association
status undefined on entry.

**Diagnostic obligation:** context-dependent.

**Facets:** `data-pointer-variable`, `procedure-pointer-variable`, `function-result-exclusion-source`, `nonpointer-target-boundary`.

**Oracle:** Admitted pointer-variable controls and exact source-bound result/target exclusions;
meaningful observations occur only at defined association states.

**Oracle limitation:** No arbitrary undefined-association query, universal target mutability, compulsory
S-level rejection or duplicate pointer-assignment effect claim.

**Dependencies:** 15.5.2.4p5;15.5.2.6/.8/.10;19.5.2;8.5.10p2/p3;4.2p2.

### S8.5.10-010: Omitted INTENT leaves use constrained by the effective argument

**Source:** 8.5.10 p5, physical PDF129, lines15-16 **Class:** Restriction.

**Definition:** When a dummy has no INTENT attribute, its use remains subject to the limitations of its
effective argument. Omission is not INOUT: an ordinary read-only use can admit an actual
that would fail INOUT's unconditional definability requirement. Pointer, allocatable,
presence and other argument rules are not waived.

**Diagnostic obligation:** not-required.

**Facets:** `read-only-expression-actual`, `conditional-definition-controls`, `omitted-versus-inout-source`, `effective-argument-category-census`.

**Oracle:** Meaningful complete valid-use controls with source-qualified effective-argument and
conditional-definition relations.

**Oracle limitation:** No universal equivalence to INOUT, unconditional permission to mutate expressions or
warning/trap for invalid use. Logical short circuit, argument evaluation order and
unspecified copy behavior are not assumed.

**Dependencies:** 15.5.2.4p4-p7;15.5.2.5-.8/.10/.13/.14;8.5.10p4 and note4.2;19.6.5/.6.

### S8.5.10-011: All subobjects of a nonpointer object inherit its INTENT

**Source:** 8.5.10 p6, physical PDF129, line17 **Class:** Restriction.

**Definition:** Every subobject of a nonpointer object with INTENT has that same INTENT. A pointer
component as a pointer is a subobject; the target it designates is not. Consequently
inherited IN can forbid reassociating that component while allowing definition of an
associated definable target.

**Diagnostic obligation:** context-dependent.

**Facets:** `elements-and-substrings`, `derived-and-allocatable-components`, `pointer-subobject-target-boundary`, `canonical-inherited-intent-census`.

**Oracle:** Source-qualified inherited-attribute relations and meaningful canonical negative/control
contexts. Pointer association and target data definition remain separately judged.

**Oracle limitation:** Required diagnosis arises from an applicable numbered/scope rule, not automatically
every prose relation. No pointer-target-as-subobject shortcut, broader pure-procedure
restriction or evidence-role bypass.

**Dependencies:** 9.4.2;7.5.1 component/subcomponent definitions;C846/C847;8.5.10p2-p4 and
note3;15.5.2.14;19.6.7/.8;4.2p2.

<!-- END GENERATED 8.5.10 -->
