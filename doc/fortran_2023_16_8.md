# Fortran 2023 16.8: Specific names for standard intrinsic functions

The canonical catalogue is `doc/catalogues/specific_names_for_standard_intrinsic_functions_16_8.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, table16.2, table16.3. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.8 -->

### S16.8-001: Specific result types match corresponding generic result types except four names

**Source:** S16.8, J3/24-007, 18 December 2023, physical PDF374. **Class:** Effect.

**Definition:** Except for AMAX0, AMIN0, MAX1, and MIN1, the result type of a specific function is the
same as the corresponding generic function reference invoked with the same arguments.

**Diagnostic obligation:** not-required.

**Facets:** `specific-result-type-matches-generic`, `amax0-amin0-max1-min1-result-type-exceptions`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.8-002: Table 16.3 functions are restricted in procedure contexts

**Source:** S16.8, J3/24-007, 18 December 2023, physical PDF374. **Class:** Restriction.

**Definition:** A function listed in Table 16.3 is not permitted as an actual argument, procedure
pointer assignment target, initial target in a procedure declaration statement, or
interface specification.

**Diagnostic obligation:** not-required.

**Facets:** `restricted-specific-not-actual-argument`, `restricted-specific-not-procedure-pointer-target`, `restricted-specific-not-initial-procedure-target`, `restricted-specific-not-interface-specification`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.8-003: Table 16.2 lists unrestricted specific intrinsic functions

**Source:** S16.8, J3/24-007, 18 December 2023, physical PDF374. **Class:** Effect.

**Definition:** Table 16.2 lists unrestricted specific intrinsic functions, corresponding generic names,
and argument type/kind rows: ABS, ACOS, AIMAG, AINT, ALOG, ALOG10, AMOD, ANINT, ASIN,
ATAN, ATAN2, CABS, CCOS, CEXP, CLOG, CONJG, COS, COSH, CSIN, CSQRT, DABS, DACOS, DASIN,
DATAN, DATAN2, DCOS, DCOSH, DDIM, DEXP, DIM, DINT, DLOG, DLOG10, DMOD, DNINT, DPROD,
DSIGN, DSIN, DSINH, DSQRT, DTAN, DTANH, EXP, IABS, IDIM, IDNINT, INDEX, ISIGN, LEN, MOD,
NINT, SIGN, SIN, SINH, SQRT, TAN, TANH.

**Diagnostic obligation:** not-required.

**Facets:** `unrestricted-specific-function-list`, `unrestricted-specific-generic-mappings`, `unrestricted-specific-argument-type-kind-rows`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.8-004: Table 16.3 lists restricted specific intrinsic functions

**Source:** S16.8, J3/24-007, 18 December 2023, physical PDF375. **Class:** Effect.

**Definition:** Table 16.3 lists restricted specific intrinsic functions, corresponding generic names or
conversion expressions, and argument type/kind rows: AMAX0, AMAX1, AMIN0, AMIN1, CHAR,
DMAX1, DMIN1, FLOAT, ICHAR, IDINT, IFIX, INT, LGE, LGT, LLE, LLT, MAX0, MAX1, MIN0,
MIN1, REAL, SNGL.

**Diagnostic obligation:** not-required.

**Facets:** `restricted-specific-function-list`, `restricted-specific-generic-mappings`, `restricted-specific-argument-type-kind-rows`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

<!-- END GENERATED 16.8 -->
