# Fortran 2023 11.7.4: SYNC IMAGES statement

Source registration packet for J3/24-007 (sha256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`), PDF page start from the source inventory. This packet is source accounting only: it adds no fixtures and approves no executable oracle.

## Source accounting summary

| Section | Title | Base units read | Numbered R/C found | Already-counted base units | Newly-accounted base units | Declared facets |
| --- | --- | ---: | --- | --- | --- | ---: |
| 11.7.4 | SYNC IMAGES statement | 12 | R1170, R1171, C1181, C1182 | R1170, R1171, C1181, C1182 | p1, p2, p3, p4, note1, note2, note2.2, note3 | 21 |

Pre-existing-fixture check: R1170, R1171, C1181, C1182 were checked with `grep -rl <identifier> tests/`; every checked identifier returned no matches.

## Attribution table for diagnostic facets

| Unit | Facet | Exact prohibiting words | Single differing property |
| --- | --- | --- | --- |
| C1181 | `rank-two-image-set-rejected` | An image-set that is an int-expr shall be scalar or of rank one. | The int-expr image-set has rank two. |
| C1182 | `image-set-depends-on-stat-variable-rejected` | The value of image-set shall not depend on the value of stat-variable or errmsg-variable. | image-set value depends on stat-variable. |
| C1182 | `image-set-depends-on-errmsg-variable-rejected` | The value of image-set shall not depend on the value of stat-variable or errmsg-variable. | image-set value depends on errmsg-variable. |

## Implementability table

| Facet | Classification | Reason |
| --- | --- | --- |
| `sync-images-image-set-only` | implementable | Pure syntax form can be parsed. |
| `sync-images-with-sync-stat-list` | implementable | Pure syntax form can be parsed; STAT/ERRMSG effects delegated. |
| `image-set-int-expr-form` | implementable | Pure syntax alternative. |
| `image-set-asterisk-form` | implementable | Pure syntax alternative. |
| `scalar-image-set-control` | implementable | Local rank property for diagnostic control. |
| `rank-one-image-set-control` | implementable | Local rank property for diagnostic control. |
| `rank-two-image-set-rejected` | implementable | Numbered constraint requires rejection of rank-two int-expr image-set. |
| `image-set-independent-of-sync-stat-control` | implementable | Local expression-dependency source property. |
| `image-set-depends-on-stat-variable-rejected` | implementable | Numbered constraint prohibits dependency on stat-variable. |
| `image-set-depends-on-errmsg-variable-rejected` | implementable | Numbered constraint prohibits dependency on errmsg-variable. |
| `array-image-set-positive-in-range-control` | source-control | Value-domain control can be source-reviewed, but synchronization remains unobserved. |
| `array-image-set-nonpositive-source-violation` | source-control | Nonpositive array element value is a dynamic program value defect with no required diagnostic. |
| `array-image-set-too-large-source-violation` | source-control | Array element value above current-team image count is a dynamic program value defect with no required diagnostic. |
| `array-image-set-repeated-value-source-violation` | source-control | Repeated-value obligation remains a distinct source-control facet with no required diagnostic. |
| `scalar-image-set-positive-in-range-control` | source-control | Local value 1 in single-image can be valid, but proves no synchronization. |
| `scalar-image-set-nonpositive-source-violation` | source-control | Nonpositive scalar value is a dynamic program value defect with no required diagnostic. |
| `scalar-image-set-too-large-source-violation` | source-control | Scalar value above current-team image count is a dynamic program value defect with no required diagnostic. |
| `asterisk-image-set-current-team-source-control` | unimplementable-context-dependent | All-current-team effect is vacuous on one image and cross-image otherwise. |
| `sync-images-synchronizes-with-image-set-source-control` | unimplementable-context-dependent | Pairwise synchronization cannot be observed portably. |
| `sync-images-correspondence-count-source-control` | unimplementable-context-dependent | Same-count correspondence would require multi-image progress/deadlock evidence. |
| `sync-images-corresponding-segment-precedence` | unimplementable-context-dependent | Cross-image segment precedence is not observable here. |

## Compound-obligation sweep

| Unit | Obligation count summary |
| --- | --- |
| R1170 | Two syntax forms: image-set only and image-set with sync-stat-list. |
| R1171 | Two syntax alternatives: int-expr and asterisk. |
| C1181 | Two admitted ranks plus one rejected higher-rank diagnostic. |
| C1182 | One control plus two dependency diagnostics: STAT= and ERRMSG=. |
| p1 | Three value obligations carried as distinct facets: positive element values, element values <= current-team image count, and no repeated values. |
| p2 | Two scalar value obligations carried as distinct facets: positive and <= current-team image count. |
| p4 | Three dynamic effects: synchronization with image-set, correspondence count, segment precedence. |

## Permissions recorded

| Unit | Facet | Why this is permission/latitude |
| --- | --- | --- |
| _None_ | _None_ | _No processor/source permission newly classified in this section._ |

## Dependency and ownership notes

This packet cites, and does not duplicate, registered 11.1.5 CHANGE TEAM and 11.1.6 CRITICAL synchronization material, all of Clause 10, and source-complete Clauses 6, 8, and 9. It flags unregistered reaches to 11.2-11.6, 11.7.7-11.7.11, Clauses 12-19, Clause 15 procedures, and Clause 16 ISO_FORTRAN_ENV/team/status facilities where they are prerequisites.

<!-- BEGIN GENERATED 11.7.4 -->

### R1170: A SYNC IMAGES statement names an image set with optional sync status list

**Source:** 11.7.4 R1170; J3/24-007 PDF p233. **Class:** Syntax.

**Definition:** A sync-images-stmt is SYNC IMAGES ( image-set [ , sync-stat-list ] ).

**Diagnostic obligation:** required.

**Facets:** `sync-images-image-set-only`, `sync-images-with-sync-stat-list`.

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** R1171, C1181, C1182 in this packet; R1169/C1179/C1180 in 11.7.3; 11.7.11 effects NOT YET
REGISTERED.

### R1171: An image-set is an integer expression or an asterisk

**Source:** 11.7.4 R1171; J3/24-007 PDF p233. **Class:** Syntax.

**Definition:** An image-set is an int-expr or an asterisk.

**Diagnostic obligation:** required.

**Facets:** `image-set-int-expr-form`, `image-set-asterisk-form`.

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** 10 expressions registered; C1181-C1182 and p1-p3 in this packet.

### C1181: An integer image-set is scalar or rank one

**Source:** 11.7.4 C1181; J3/24-007 PDF p233. **Class:** Restriction.

**Definition:** An image-set that is an int-expr shall be scalar or of rank one.

**Diagnostic obligation:** required.

**Facets:** `scalar-image-set-control`, `rank-one-image-set-control`, `rank-two-image-set-rejected`.

**Oracle:** The exact prohibiting words are: "shall be scalar or of rank one."

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** Clause 9 arrays source-complete; Clause 10 expressions registered.

### C1182: The image-set value does not depend on STAT= or ERRMSG= variables

**Source:** 11.7.4 C1182; J3/24-007 PDF p233. **Class:** Restriction.

**Definition:** The value of image-set shall not depend on the value of stat-variable or
errmsg-variable.

**Diagnostic obligation:** required.

**Facets:** `image-set-independent-of-sync-stat-control`, `image-set-depends-on-stat-variable-rejected`, `image-set-depends-on-errmsg-variable-rejected`.

**Oracle:** The exact prohibiting words are: "shall not depend on the value of stat-variable or
errmsg-variable."

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** R1169/C1179/C1180 in 11.7.3; expression dependency analysis in Clause 10 registered
where applicable; 11.7.11 NOT YET REGISTERED.

### S11.7.4-001: Array image-set element values are positive, in range, and unrepeated

**Source:** 11.7.4 p1; J3/24-007 PDF p233. **Class:** Restriction.

**Definition:** If image-set is an array expression, each element value shall be positive and not
greater than the number of images in the current team, and there shall be no repeated
values.

**Diagnostic obligation:** context-dependent.

**Facets:** `array-image-set-positive-in-range-control`, `array-image-set-nonpositive-source-violation`, `array-image-set-too-large-source-violation`, `array-image-set-repeated-value-source-violation`.

**Oracle:** This is a value restriction on a dynamic image set. The p1 obligations are kept
distinct: positive element values, element values not greater than the number of images
in the current team, and no repeated values. A future deterministic local fixture may
use only portable current-team cardinality facts; it must not infer synchronization from
success.

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** 16 NUM_IMAGES/THIS_IMAGE/ISO_FORTRAN_ENV NOT YET REGISTERED; team formation 11.7.9 NOT
YET REGISTERED.

### S11.7.4-002: Scalar image-set values are positive and in range

**Source:** 11.7.4 p2; J3/24-007 PDF p233. **Class:** Restriction.

**Definition:** If image-set is a scalar expression, its value shall be positive and not greater than
the number of images in the current team.

**Diagnostic obligation:** context-dependent.

**Facets:** `scalar-image-set-positive-in-range-control`, `scalar-image-set-nonpositive-source-violation`, `scalar-image-set-too-large-source-violation`.

**Oracle:** The p2 scalar obligations are kept distinct: the value shall be positive and shall not
be greater than the number of images in the current team. The one-image case can make
value 1 locally valid, but using it to prove synchronization would be vacuous; this
packet records only the value-domain rule.

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** 16 NUM_IMAGES/THIS_IMAGE NOT YET REGISTERED; team formation 11.7.9 NOT YET REGISTERED.

### S11.7.4-003: An asterisk image-set specifies all images in the current team

**Source:** 11.7.4 p3; J3/24-007 PDF p233. **Class:** Effect.

**Definition:** An image-set that is an asterisk specifies all images in the current team.

**Diagnostic obligation:** not-required.

**Facets:** `asterisk-image-set-current-team-source-control`.

**Oracle:** The source form is implementable as syntax via R1171; the all-images effect is not
non-vacuously observable in a single-image run.

**Oracle limitation:** This source-only catalogue records pending plans only. The specified synchronization,
segment delay, segment precedence, or cross-image memory-order effect is not approved as
an executable oracle in the current single-image suite: a one-image run would be vacuous
and multi-image timing, scheduling, interleaving, progress, deadlock, race, or failure
observations are not portable evidence.

**Dependencies:** R1171 in this packet; 16 team/image inquiries NOT YET REGISTERED.

### S11.7.4-004: SYNC IMAGES synchronizes with image-set members and orders corresponding segments

**Source:** 11.7.4 p4; J3/24-007 PDF p233. **Class:** Effect.

**Definition:** Executing SYNC IMAGES synchronizes the image with each other image in image-set.
Executions on images M and T correspond when their mutual image-set execution counts in
the current team match. Segments before the statement on either image precede segments
after the corresponding statement on the other image.

**Diagnostic obligation:** not-required.

**Facets:** `sync-images-synchronizes-with-image-set-source-control`, `sync-images-correspondence-count-source-control`, `sync-images-corresponding-segment-precedence`.

**Oracle:** All three effects are cross-image. This packet does not use vacuous single-image SYNC
IMAGES(1) or SYNC IMAGES(*) as evidence of synchronization.

**Oracle limitation:** This source-only catalogue records pending plans only. The specified synchronization,
segment delay, segment precedence, or cross-image memory-order effect is not approved as
an executable oracle in the current single-image suite: a one-image run would be vacuous
and multi-image timing, scheduling, interleaving, progress, deadlock, race, or failure
observations are not portable evidence.

**Dependencies:** 11.7.2 segments in this packet; 11.7.11 NOT YET REGISTERED; 16 image/team inquiries NOT
YET REGISTERED.

<!-- END GENERATED 11.7.4 -->
