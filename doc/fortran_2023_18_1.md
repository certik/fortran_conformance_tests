# Fortran 2023 18.1: Interoperability with C overview

The canonical catalogue is `doc/catalogues/interoperability_overview_18_1.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 18.1 -->

### S18.1-001: Fortran can reference C procedures and C-prototype-compatible procedures

**Source:** S18.1, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** Fortran provides means to reference procedures defined by C and procedures describable
by C prototypes.

**Diagnostic obligation:** not-required.

**Facets:** `reference-c-defined-procedure`, `reference-c-prototype-described-procedure`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.1-001 C procedure reference fixture: a C function add_one is referenced from Fortran
through an interoperable BIND(C) interface and returns 42 from exact integer(C_INT)
input 41. A separate Fortran BIND(C) function, whose interface is describable by a C
prototype but is not C-defined, is referenced from Fortran and returns 36 from 12.
Feature mutants redirect each call to a different conforming exact computation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.1-001 C procedure reference boundaries: Only the listed exact single-image integer,
logical and character effects are claimed. Processor-dependent numeric kind values,
pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING public
entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.1-002: Fortran subprograms can be made referenceable from C

**Source:** S18.1, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** Fortran provides means to specify that a Fortran subprogram can be referenced from a C
function.

**Diagnostic obligation:** not-required.

**Facets:** `fortran-subprogram-referenceable-from-c`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.1-002 C-to-Fortran reference fixture: a C bridge calls the Fortran subroutine
mark_from_c exported with BIND(C); the Fortran program first sets a nondefault sentinel
and then observes exact value 31 after the C call. The mutation skips the C callback
while preserving a conforming C/Fortran link.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.1-002 C-to-Fortran reference boundaries: Only the listed exact single-image integer,
logical and character effects are claimed. Processor-dependent numeric kind values,
pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING public
entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.1-003: Fortran global variables can be associated with C external-linkage variables

**Source:** S18.1, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** Fortran provides means to declare global variables associated with C variables having
external linkage.

**Diagnostic obligation:** not-required.

**Facets:** `fortran-global-variable-associated-with-c-external`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.1-003 C external global association fixture: a module integer(C_INT),
BIND(C,name='batch310_shared_counter') is initialized to -5 and modified by a C
companion through extern int batch310_shared_counter; Fortran then observes exact 123.
The mutation writes a different value through the same C external linkage.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.1-003 C external global association boundaries: Only the listed exact single-image
integer, logical and character effects are claimed. Processor-dependent numeric kind
values, pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING
public entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.1-004: ISO_C_BINDING constants and Fortran facilities correspond to C types

**Source:** S18.1, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** ISO_C_BINDING provides named constants for C-compatible representations, and Fortran
facilities define derived types and interoperable enumerations corresponding to C types.

**Diagnostic obligation:** not-required.

**Facets:** `iso-c-binding-kind-constants`, `derived-types-correspond-to-c-types`, `interoperable-enumerations-correspond-to-c-types`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.1-004 C-type correspondence fixture: integer(C_INT) and a BIND(C) derived type with
integer(C_INT) components are exchanged with C. The C companion sees exact integer
values and returns exact checksums for the scalar kind constant and the C struct-shaped
derived value. Enumeration correspondence remains pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.1-004 C-type correspondence boundaries: Only the listed exact single-image integer,
logical and character effects are claimed. Processor-dependent numeric kind values,
pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING public
entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.1-005: ISO_Fortran_binding.h enables C interoperation with descriptor-requiring Fortran dummies

**Source:** S18.1, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** ISO_Fortran_binding.h provides definitions and prototypes enabling C functions to
interoperate with Fortran procedures having allocatable, assumed-shape, assumed-rank,
pointer, or assumed-length character dummy data objects.

**Diagnostic obligation:** not-required.

**Facets:** `iso-fortran-binding-header-definitions`, `iso-fortran-binding-header-prototypes`, `descriptor-dummy-interoperation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.1-006: Interoperable Fortran entities have equivalent C entities but need not have an actual C partner

**Source:** S18.1, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** An interoperable Fortran entity could be defined by C and would interoperate with an
equivalent C entity, and no actual interoperating C entity is required.

**Diagnostic obligation:** not-required.

**Facets:** `interoperable-entity-equivalent-c-entity`, `no-actual-c-entity-required`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.1-006 interoperable equivalent/no-partner fixture: C mutates the address of a TARGET
integer(C_INT) obtained by C_LOC, showing an equivalent C entity can interoperate, while
a distinct BIND(C) Fortran function and BIND(C) variable with no C companion are used
only from Fortran and retain exact values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.1-006 interoperable equivalent/no-partner boundaries: Only the listed exact
single-image integer, logical and character effects are claimed. Processor-dependent
numeric kind values, pointer bit patterns, storage alignment, warning text, extra
ISO_C_BINDING public entities, C_F_STRPOINTER/F_C_STRING availability on a reference
that lacks them, ISO_Fortran_binding.h descriptor semantics, interoperable enumeration
transfer, negative optional-kind sentinel branches not manifest on this host,
C_LONG_DOUBLE declarations when the c-long-double-positive profile exits 77, and later
18.2.3 procedures remain pending or belong to sibling catalogues. Every owned facet has
a distinct assertion and conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

<!-- END GENERATED 18.1 -->
