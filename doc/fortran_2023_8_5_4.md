# Fortran 2023 8.5.4: ASYNCHRONOUS attribute

**Independently source-reviewed; fixtures still pending.** All facets in
`doc/catalogues/asynchronous_attribute_8_5_4.json` remain pending.
No fixtures, executable models, compiler probes, profiles, source-use
inventories or links are added. Source adjudication and the separate
observational-inventory renewal are recorded in
`doc/source_audits/batch_031.json`; neither grants fixture approval.

Original J3/24-007, December 18, 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The scope starts on physical PDF120 and ends before8.5.5 on PDF121.
Paragraph4 and the single unnumbered NOTE are on121; neither is renamed as
a continuation of the ALLOCATABLE note. All five original unit hashes and
section hash
`a05d3f04de933c73727508a5b42d095ea2649d30888e5bb31e8b36da4b44d48c`
were verified with bounded typography helpers.

| Original unit | Treatment |
| --- | --- |
| `p1` | Variable category and permitted I/O/communication, not a required operation |
| `p2` | Exact two-conjunct base-object/scoping obligation |
| `p3` | Canonical implicit-attribute route owned by12.6.2.5p4 |
| `p4` | Association permission; nonlocal BLOCK scope effect; subobject propagation |
| Unnumbered NOTE | Possible code-motion use, not a mandatory optimization or hardware barrier |

Five base units have twenty-seven fine subdivisions and thirty-two
accounting rows. Three local S requirements have twenty-two facets, all
pending. The p2 usage condition (dummy or executable/specification
occurrence) is separate from execution of some statement while pending.
The variable need not be referenced by that particular pending-time
statement. Attribute presence is not itself proof of an active operation.

| Operational qualification | Required limit |
| --- | --- |
| Initiation | External file successfully opened with ASYNCHRONOUS=YES; transfer's YES is a constant expression. Internal/preconnected-without-OPEN routes are not substituted. |
| Implicit attribute | I/O-list variables, namelist group objects and SIZE storage have the defined base-object implication in the transfer scope. Deleting an explicit keyword may therefore leave ASYNCHRONOUS in force. |
| Pending state | Separate actual data movement from the language's pending/wait protocol. Synchronous implementation is permitted; a successful ID can be zero. No positive-ID, PENDING=true, overlap, sleep or throughput assertion. |
| Input versus output | Pending input forbids value reference, definition, undefinition, VALUE association and association-status changes. Output references may be permitted, but redefinition, undefinition and association changes are not. |
| Completion and errors | Check initiation and WAIT IOSTAT independently. Never read failed/undefined ID, SIZE or input data, or continue with indeterminate file position. Later I/O/positioning can implicitly wait and must not conceal a missing intended completion step. |
| Arguments and lifetime | Use complete explicit interfaces, correct type/kind/rank and C1548/C1549 contiguity premises. No pending-input VALUE/INTENT(OUT) shortcut, vector-subscript dummy violation, lost temporary result or local buffer deallocation while pending. |
| Foreign communication |18.10.4 requires actual processor-recognized non-Fortran initiation/completion procedures. I/O WAIT, a Fortran stub, MPI-looking names, threads or coarray synchronization are not substitutes. |
| Evidence | I/O-free declarations are not execution oracles. Actual I/O outcomes do not automatically isolate an attribute source, especially when the transfer also implies it. Source-use inventories are separately adjudicated and credit-free. |

The finite operational candidates use defined two-INTEGER records17/23 or
an explicit formatted ABCD record, real successful initiation/completion,
and independent post-WAIT values/counts. Only after completion is buffer
reuse allowed. These are plans for canonical Clause12 evidence, not newly
authored tests or byte/encoding oracles. Processor-dependent I/O errors and
external-device conditions remain explicit qualification gates, not silent
optional-capability skips or automatic attribute-conformance failures.

The prose presence obligation has no invented mandatory diagnostic policy.
Numbered I/O/argument constraints keep their own owners and causal reports.
No body/data access is used to provoke an undefined-value trap.

The following definitions are generated with `Registry.render(write=True)`
for this owned view only. Full finite plans and source limitations remain
in the catalogue.

<!-- BEGIN GENERATED 8.5.4 -->

### S8.5.4-001: Used pending-affector variables require scoped ASYNCHRONOUS base objects

**Source:** 8.5.4 p2, physical PDF120, printed106, lines22-26 **Class:** Restriction.

**Definition:** The base object needs ASYNCHRONOUS in a scoping unit when the variable is a dummy
argument or occurs in an executable statement or specification expression there, and
some statement of that scope executes while the variable is a pending I/O
storage-sequence affector or pending communication affector. The two conditions are
separate: an occurrence only after completion can still satisfy the usage condition.
Pending status and storage overlap come from the defining I/O/communication rules, not
elapsed time, attribute presence or the spelling of a call.

**Diagnostic obligation:** not-required.

**Facets:** `dummy-pending-input`, `dummy-pending-output`, `executable-scope-input`, `executable-scope-output`, `specification-scope-input`, `specification-scope-output`, `overlap-and-base-object`, `io-completion-controls`, `communication-input`, `communication-output`, `absent-conjuncts-source-use`, `missing-attribute-policy-gate`.

**Oracle:** Original source establishes the complete conjunction and affector lifetimes. Proposed
operational controls require successful external connection, initiation and completion,
defined independent post-WAIT values, disjoint unit/ID/status storage and complete
interfaces. They exercise legitimate I/O but do not automatically prove every
attribute-necessity predicate or simultaneous hardware execution. For foreign
communication, processor-recognized procedure roles and interface evidence are a
prerequisite.

**Oracle limitation:** All twelve facets remain pending. This prose obligation has no invented mandatory
diagnostic or optional-capability skip. C1225/C1226/C1227, C1546-C1549 and other
numbered conditions retain their own required-reporting owners and exact subjects.
Actual data movement may be synchronous, a successful ID may be0, and errors may be
reported at initiation or completion. On a failed operation, report the specific
stage/status and do not read an undefined ID, SIZE variable or input buffer, or continue
using indeterminate file position. I/O environment failures are not automatically
attribute violations. No arbitrary timing, storage-layout, VOLATILE, thread, coarray or
generic memory-barrier oracle.

**Dependencies:** 4.2;5.4.5;9.4.2p3/p5;12.5.6.2/.5/.13/.19;12.6.2.1,C1225-C1227,C1231;12.6.2.5p4-p6;12.6.2.9;12.6.4.1p3-p7;12.7.1/.2;12.11;15.4.2.2;15.5.2.5p20/p21,C1546-C1549;18.10.4;19.6.6.

### S8.5.4-002: A BLOCK can give a nonlocal object scoped ASYNCHRONOUS

**Source:** 8.5.4 p4 middle sentence, physical PDF121, printed107, lines2-5 **Class:** Effect.

**Definition:** When an object that is not a local variable of a BLOCK is specified ASYNCHRONOUS in that
construct's specification-part, the object has the attribute within the construct even
if it lacks it outside. This is a scoped property of the same object, not a new local
variable, allocation, SAVE implication or hardware fence. The outer object may already
have the attribute; outside absence is not a mandatory premise.

**Diagnostic obligation:** not-required.

**Facets:** `same-nonlocal-object`, `nested-block-scope`, `use-and-host-object`, `operational-block-io-control`, `argument-constraint-evidence-gate`.

**Oracle:** A finite identity/scope and canonical-constraint plan. The effect category records the
source's declaration semantics. Any future operational check must use actual I/O
initiation/completion and independent defined values, and separately justify attribution
to the scoped attribute rather than an implicit I/O route.

**Oracle limitation:** All five facets remain pending. Source-use inventories, including reviewed ones, cannot
grant facet-completion credit. No direct ASYNCHRONOUS inquiry exists in this plan; no
no-op program, race, thread/barrier model, compiler timing or compile-only substitute is
an effect oracle. The18.10.4 foreign-communication BLOCK example needs actual
processor-recognized procedures, not a fabricated Fortran replacement.

**Dependencies:** 3.151.2;8.6.3;11.1.4p2;8.8;14.2.2p2/p9/p10;19.5.1.4p1/note1;12.6.2.5p4;12.7;15.4.2.2;15.5.2.5
C1548/C1549;18.10.4.

### S8.5.4-003: ASYNCHRONOUS propagates to the object's subobjects

**Source:** 8.5.4 p4 final sentence, physical PDF121, printed107, lines5-6 **Class:** Effect.

**Definition:** An object's ASYNCHRONOUS attribute also belongs to all of its subobjects. Subobject
identity follows the original object/designator rules, not arbitrary storage
reachability: a pointer component in a pointer-association context differs from the
target to which it refers. Storage-overlap affectors and per-scope base-object
obligations remain separately governed by p2 and the I/O/communication rules.

**Diagnostic obligation:** not-required.

**Facets:** `array-elements-sections`, `character-substrings`, `nested-data-components`, `base-object-pointer-boundary`, `argument-and-io-evidence-gate`.

**Oracle:** The original subobject relation and scoped attribute determine the proposed semantic
evidence. Actual I/O controls require successful transfer/completion and independently
expected values/counts; canonical argument diagnostics require their complete
type/rank/attribute/contiguity premises. Neither is silently re-owned as automatic local
effect coverage.

**Oracle limitation:** All five facets remain pending. No unallocated value/bounds/deferred-parameter read,
pointer-target identity guess, ALLOCATED(pointer), VALUE association of pending input,
early SIZE read or mutation of a pending affector. SourceUses has no execution or facet
credit, and no inventory/graph/link is added. Optional processor scheduling is not
permission to skip required language behavior or substitute a race-based test.

**Dependencies:** 5.4.3.2.1/5.4.5;9.4.2p3-p5;8.5.8.4;7.5.4.6;9.7;12.6.2.1
C1214/C1217/C1223;12.6.2.5p4-p6;12.6.2.16;12.6.4.1p5/p6;12.7;15.5.2.5p21,C1548,C1549;18.10.4;19.6.

<!-- END GENERATED 8.5.4 -->
