# Fortran 2023: 11.6 NOTIFY WAIT statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.6 -->

### S11.6-001: NOTIFY WAIT waits until its notify-variable count reaches a threshold

**Source:** 11.6 p1; J3/24-007 PDF p229. **Class:** Effect.

**Definition:** The NOTIFY WAIT statement waits until the value of its notify-variable is greater than
or equal to a threshold value. The threshold computation and decrement sequence are
specified in p3.

**Diagnostic obligation:** not-required.

**Facets:** `notify-wait-threshold-purpose`.

**Oracle:** Observe the notify count relation through registered notification owners, not by
assuming an implementation counter layout or a particular processor error condition.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** R1166/R1167/C1177/C1178 in this section;11.7.x event-wait specifiers
parallel/unregistered;16.10 ISO_FORTRAN_ENV NOTIFY_TYPE unregistered;multi-image
execution capability absent.

### R1166: NOTIFY WAIT syntax names a notify variable and optional event wait specs

**Source:** 11.6 R1166; J3/24-007 PDF p229. **Class:** Syntax.

**Definition:** A notify-wait-stmt is NOTIFY WAIT followed by a parenthesized notify-variable and an
optional comma plus event-wait-spec-list.

**Diagnostic obligation:** required.

**Facets:** `notify-wait-without-event-wait-specs`, `notify-wait-with-event-wait-specs`.

**Oracle:** Syntax admission only; notify-variable type/coindex restrictions and event-wait
specifier forms have separate owners.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** R1167/C1177/C1178 in this section;11.7.x event-wait-spec-list parallel/unregistered;9.2
variables and 16 ISO_FORTRAN_ENV dependencies.

### R1167: A notify-variable is a scalar variable

**Source:** 11.6 R1167; J3/24-007 PDF p229. **Class:** Syntax.

**Definition:** A notify-variable is a scalar variable.

**Diagnostic obligation:** required.

**Facets:** `scalar-notify-variable-control`, `nonscalar-notify-variable-rejected`, `nonvariable-notify-expression-rejected`.

**Oracle:** Each negative is attributed to the scalar-variable production, not to NOTIFY_TYPE or
coindexing.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must isolate the cited source unit, use a conforming positive control
when a diagnostic is expected, and observe control transfer by which statement executes
next rather than by assumed branch implementation, compiler wording, process status, or
agreement with another processor.

**Dependencies:** 9.2 variable registered;10.1.9 scalar characteristics registered;16 ISO_FORTRAN_ENV
NOTIFY_TYPE unregistered.

### C1177: A notify-variable has type NOTIFY_TYPE from ISO_FORTRAN_ENV

**Source:** 11.6 C1177; J3/24-007 PDF p229. **Class:** Restriction.

**Definition:** A notify-variable shall be of type NOTIFY_TYPE from the intrinsic module
ISO_FORTRAN_ENV.

**Diagnostic obligation:** required.

**Facets:** `notify-type-control`, `non-notify-type-variable-rejected`, `same-name-user-type-notify-rejected`.

**Oracle:** The invalid sources remain scalar variables and noncoindexed; only the required
intrinsic-module type relation differs.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must isolate the cited source unit, use a conforming positive control
when a diagnostic is expected, and observe control transfer by which statement executes
next rather than by assumed branch implementation, compiler wording, process status, or
agreement with another processor.

**Dependencies:** 16 ISO_FORTRAN_ENV and NOTIFY_TYPE unregistered;14 USE association unregistered;7.5
derived type identity registered.

### C1178: A notify-variable is not coindexed

**Source:** 11.6 C1178; J3/24-007 PDF p229. **Class:** Restriction.

**Definition:** A notify-variable shall not be a coindexed object.

**Diagnostic obligation:** required.

**Facets:** `local-notify-variable-control`, `coindexed-notify-variable-rejected`.

**Oracle:** The invalid/control pair must not also violate C1177 or R1167; coindexing is the single
differing property.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must isolate the cited source unit, use a conforming positive control
when a diagnostic is expected, and observe control transfer by which statement executes
next rather than by assumed branch implementation, compiler wording, process status, or
agreement with another processor.

**Dependencies:** 9.6 coindexed object registered;16 NOTIFY_TYPE unregistered;multi-image image selectors
remain source-only until execution capability exists.

### S11.6-002: A notify-variable is independent of STAT and ERRMSG variables

**Source:** 11.6 p2; J3/24-007 PDF p229. **Class:** Restriction.

**Definition:** The notify-variable in a NOTIFY WAIT statement shall not depend on the value of its
stat-variable or errmsg-variable. These are unnumbered dependence restrictions; this
catalogue does not create a blanket mandatory static diagnostic.

**Diagnostic obligation:** not-required.

**Facets:** `notify-not-depend-on-stat`, `notify-not-depend-on-errmsg`.

**Oracle:** Future negatives must avoid undefined variables and must attribute invalidity to this
dependence restriction, not to R1166/R1167 or STAT=/ERRMSG= syntax.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 11.7.x event-wait STAT=/ERRMSG= specifier owners parallel/unregistered;9.7.4/9.7.5
allocation STAT/ERRMSG precedents only by analogy;19 definition/association dependencies
unregistered.

### S11.6-003: Executing NOTIFY WAIT computes a threshold, waits for count or error, and decrements on success

**Source:** 11.6 p3; J3/24-007 PDF p229. **Class:** Effect.

**Definition:** Execution of NOTIFY WAIT sets the threshold to UNTIL_COUNT= when present and greater
than one, otherwise to one; waits until the notify count is at least the threshold or an
error condition occurs; and, if no error condition occurs, atomically decrements the
count by the threshold.

**Diagnostic obligation:** not-required.

**Facets:** `until-count-greater-than-one-threshold`, `default-threshold-one`, `wait-until-count-or-error`, `successful-atomic-count-decrement`.

**Oracle:** Only successful, source-reviewed notification interactions can observe count/decrement;
no particular processor-dependent error condition or internal counter representation is
asserted.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 11.7.x event-wait specifiers parallel/unregistered;9.6 R928/C933 NOTIFY= image-selector
owners registered;16 NOTIFY_TYPE unregistered;multi-image execution capability absent.

### S11.6-004: Successful NOTIFY WAIT satisfies the first k matching unsatisfied NOTIFY assignments

**Source:** 11.6 p5; J3/24-007 PDF p230. **Class:** Effect.

**Definition:** Execution of an assignment statement whose variable has a NOTIFY= specifier is initially
unsatisfied. A successful NOTIFY WAIT with threshold value k satisfies the first k
unsatisfied assignment executions whose NOTIFY= specifier specifies the same notify
variable as the NOTIFY WAIT statement.

**Diagnostic obligation:** not-required.

**Facets:** `notify-assignment-initially-unsatisfied`, `first-k-unsatisfied-assignments-satisfied`, `same-notify-variable-matching`.

**Oracle:** Observe satisfaction order through registered notification semantics, not through timing
guesses or internal queues.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 9.6 R928/C933 NOTIFY= image-selector owners registered;11.7.x image execution control
parallel/unregistered;16 NOTIFY_TYPE unregistered;multi-image capability absent.

### S11.6-005: NOTIFY WAIT STAT and ERRMSG variables are independent of notify and each other

**Source:** 11.6 p6; J3/24-007 PDF p230. **Class:** Restriction.

**Definition:** The stat-variable of NOTIFY WAIT shall not depend on the value of the notify variable or
errmsg-variable. The errmsg-variable shall not depend on the value of the notify
variable or stat-variable. These are four distinct unnumbered dependence restrictions
and do not create a blanket mandatory static diagnostic.

**Diagnostic obligation:** not-required.

**Facets:** `stat-not-depend-on-notify`, `stat-not-depend-on-errmsg`, `errmsg-not-depend-on-notify`, `errmsg-not-depend-on-stat`.

**Oracle:** Each future negative must isolate exactly one dependence relation and avoid undefined
denotation or unrelated STAT=/ERRMSG= syntax errors.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 11.7.x event-wait STAT=/ERRMSG= specifier syntax parallel/unregistered;9.7.4/9.7.5
precedents only by analogy;19 association/definition dependencies unregistered.

### S11.6-006: NOTIFY WAIT STAT receives zero on success or a constrained processor-dependent positive value on error

**Source:** 11.6 p7; J3/24-007 PDF p230. **Class:** Effect.

**Definition:** If NOTIFY WAIT has a STAT= specifier, stat-variable is assigned zero on successful
execution. If an error condition occurs, stat-variable is assigned a processor-dependent
positive value different from STAT_FAILED_IMAGE and STAT_STOPPED_IMAGE from
ISO_FORTRAN_ENV.

**Diagnostic obligation:** not-required.

**Facets:** `successful-stat-zero`, `error-stat-positive`, `error-stat-not-failed-or-stopped-image`.

**Oracle:** The exact error STAT value is processor dependent; future fixtures may assert only zero
on success and the sign/distinctness properties for an actually occurring error
condition.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 16.10.2 STAT_FAILED_IMAGE/STAT_STOPPED_IMAGE unregistered;11.7.x event-wait STAT= owner
parallel/unregistered;processor-dependent error conditions in p10;multi-image capability
absent.

### S11.6-007: NOTIFY WAIT error without STAT initiates error termination

**Source:** 11.6 p8; J3/24-007 PDF p230. **Class:** Effect.

**Definition:** If an error condition occurs during execution of a NOTIFY WAIT statement with no STAT=
specifier, error termination is initiated.

**Diagnostic obligation:** not-required.

**Facets:** `notify-wait-error-without-stat-error-termination`.

**Oracle:** An executable oracle requires an actually occurring processor-supported error condition
and independent lifecycle evidence; process exit value alone is not specified.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Image, team, failed-image, stopped-image, notification, and processor-dependent
error-condition behavior is gated on future multi-image/source-owner capability; this
suite currently has no multi-image execution capability.

**Dependencies:** 5.3.7 error termination;11.4 ERROR STOP distinctions;11.7.x event-wait specifiers
parallel/unregistered;p10 processor-dependent error conditions;multi-image capability
absent.

### S11.6-008: NOTIFY WAIT ERRMSG is assigned on error and definition status and value are unchanged on success

**Source:** 11.6 p9; J3/24-007 PDF p230. **Class:** Effect.

**Definition:** If a NOTIFY WAIT statement has an ERRMSG= specifier and an error condition occurs,
errmsg-variable is assigned an explanatory message as if by intrinsic assignment. If no
such condition occurs, the definition status and the value of errmsg-variable are
unchanged. The exact explanatory message content is not specified by this subclause;
this requirement owns assignment on error and unchanged definition status and value on
success, including the initially undefined definition-status branch.

**Diagnostic obligation:** not-required.

**Facets:** `error-errmsg-explanatory-message-assigned`, `error-errmsg-intrinsic-assignment`, `success-errmsg-defined-status-unchanged`, `success-errmsg-value-unchanged`, `success-errmsg-undefined-status-unchanged`.

**Oracle:** Error-message content is not a portable oracle. Error facets record source obligations
only unless a future processor-specific premise is separately reviewed. No-error facets
may use an unchanged defined sentinel, with an explicit LEN assertion before character
equality, and the initially undefined branch remains classification-only.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
ERRMSG text on error is unspecified here: no equality, inequality against a sentinel,
substring, wording, or compiler-agreement oracle is admissible. Character equality in
the no-error value-unchanged branch must be guarded by explicit LEN checks because
shorter character operands blank-pad. The initially undefined definition-status branch
cannot be observed by reading undefined storage.

**Dependencies:** 9.7.5 ERRMSG precedent;10.2.1.2 intrinsic assignment registered;11.7.x event-wait
ERRMSG= owner parallel/unregistered;p10 processor-dependent error conditions;19
definition status unregistered;multi-image NOTIFY execution capability absent.

<!-- END GENERATED 11.6 -->
