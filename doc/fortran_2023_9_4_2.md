# Fortran 2023 9.4.2: Structure components

The canonical catalogue is `doc/catalogues/structure_component_9_4_2.json`. Effective review state comes from
`Registry.catalogue_review_state("9.4.2")`; source accounting does not
approve cases, canonical relationships or an execution inventory.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Twenty-four base units: p1, R911, R912, C909-C918, p2, C919, p3-p5, NOTE 1, R913, C920, p6, NOTE 2 and NOTE 3. This packet records data-ref and structure-component requirements; twelve selected effect facets now have bounded runtime fixtures without approving the new cases.

Selected effect facets now have executable fixtures and mutation plans. Remaining pending facets, reviews and approvals stay integrator-owned.

<!-- BEGIN STRUCTURE COMPONENT FIXTURES -->
## Structure component runtime observations

Twelve complete run/effect/f2023 fixtures now discharge the S9.4.2-001, selected S9.4.2-002 and S9.4.2-003 effect facets. They exercise actual percent component references with scalar parents, scalarized array parents and parent array sections. Array-valued references assert SHAPE, LBOUND and UBOUND as well as coordinate-coded values, using nonunit bounds and unequal extents where the component has rank two.

The rank derivation is from 9.4.2 p2-p3: a bare part-name part-ref has the rank of that part-name; a section-subscript-list contributes triplets, vector subscripts and multiple-subscript array sizes; a data-ref has the rank of the one nonzero-rank part-ref, if any, otherwise zero. C919 permits no more than one nonzero-rank part-ref, so these fixtures never use the invalid general form array_parent%array_component.

Every source has distinct values for every observed component and array element. Planned mutation metadata covers every oracle, every observed input literal, component-reference substitution, parent-subscript substitution, component-value swapping and a reverse input+oracle sentinel control. No observed sentinel is zero, `.FALSE.` or blank.
<!-- END STRUCTURE COMPONENT FIXTURES -->

<!-- BEGIN GENERATED 9.4.2 -->

### S9.4.2-001: Structure components are derived-type object parts referenced by object designators and may be scalar or arrays

**Source:** 9.4.2 p1, J3/24-007, 18 December 2023, physical PDF151. **Class:** Effect.

**Definition:** A structure component is part of an object of derived type. It can be referenced by an
object designator, and the component denoted by the designator may be scalar or
array-valued. Formation of the data-ref, array element, array section, and component
definitions remains with R911-R913, 7.5, 9.5, and 9.6 as applicable.

**Diagnostic obligation:** not-required.

**Facets:** `derived-type-object-part`, `object-designator-reference`, `scalar-component`, `array-component`.

**Oracle:** The original source-only catalogue recorded pending plans. The selected runtime fixtures
below now supply executable standard-oracle observations for the covered effect facets;
facets still listed in pending remain unimplemented.

S9.4.2-001 structure component runtime fixtures: four complete run/effect/f2023 programs
observe structure components through actual percent references, not copied locals or
whole-object comparisons. The derived-object, object-designator, scalar-component and
array-component cases define every observed component before use, give the parallel
component distinct values, and check direct component reads against independent integer
literals. The array-component case uses a scalar parent and an INTEGER component
declared with nonunit bounds -5:-3; it checks SHAPE [3], LBOUND [-5], UBOUND [-3] and
all three element values. Each source also contains a parent-array control x(2)%alpha
with distinct x(3)%alpha, so parent-subscript mutations are load-bearing.

**Oracle limitation:** The original source-only catalogue created no Fortran test program, execution, evidence
link, fixture approval, oracle approval, or coverage claim; this generator supplies
selected runtime fixtures and mutation plans without granting those approvals or claims.
4.2 requires detection and reporting for numbered syntax rules and constraints, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number; unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

S9.4.2-001 structure component fixture boundaries: only the four S9.4.2-001 effect
facets are represented. The fixtures cite but do not re-prove derived-type declaration
validity, component declaration syntax, assignment semantics, object definition rules or
default initialization. They do not assert component storage order, offsets, addresses,
padding, finalization, inheritance, allocatable/pointer components, coarrays, image
selectors, polymorphism or diagnostics.

**Dependencies:** 7.5.1;7.5.2.1;7.5.4.1-.5;9.1R901;9.2;9.4.2R911-R913/C909-C920;9.5.2;9.5.3.1;9.6;19.6.5.

### R911: A data reference is a chain of one or more part references separated by percent signs

**Source:** 9.4.2 R911, J3/24-007, 18 December 2023, physical PDF151. **Class:** Syntax.

**Definition:** A data-ref begins with one part-ref and may be followed by zero or more occurrences of
percent sign and another part-ref. A single part-ref is a data-ref but is not by itself
necessarily a structure-component; C920 supplies the additional structure-component
restrictions.

**Diagnostic obligation:** required.

**Facets:** `single-part-ref-data-ref`, `two-part-ref-chain`, `three-part-ref-chain`, `percent-separator-required`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.1.3R401;4.2;7.5.1;7.5.2.1;9.1R901;9.2;9.4.2R912-R913/C909-C920;9.4.3R914/C921;9.5.3.1R917-R918/C924-C925;9.6.

### R912: A part reference is a part name with optional section subscripts and optional image selector

**Source:** 9.4.2 R912, J3/24-007, 18 December 2023, physical PDF151. **Class:** Syntax.

**Definition:** A part-ref consists of a part-name, optionally followed by a parenthesized
section-subscript-list, and optionally followed by an image-selector. If both optional
forms appear, the section-subscript-list precedes the image-selector. The
section-subscript and image-selector grammars and their detailed semantics are owned by
9.5.3.1 and 9.6.

**Diagnostic obligation:** required.

**Facets:** `bare-part-name`, `part-name-with-section-subscripts`, `part-name-with-image-selector`, `section-before-image-selector`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.1.3R401;4.2;7.5.1;8.5.6.1;9.5.3.1R919-R925/C927-C931;9.6R926-R928/C932-C935/p1-p7.

### C909: Every nonrightmost part name in a data reference is of derived type

**Source:** 9.4.2 C909, J3/24-007, 18 December 2023, physical PDF151. **Class:** Restriction.

**Definition:** In an R911 data-ref chain, each part-name except the rightmost shall be of derived type.
The rightmost part-name may be intrinsic or derived subject to the context and other
constraints.

**Diagnostic obligation:** required.

**Facets:** `derived-nonrightmost`, `intrinsic-rightmost`, `intrinsic-nonrightmost-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;7.4 intrinsic types;7.5.1;7.5.2.1;9.4.2R911-R912/C910-C912;9.2.

### C910: Every nonleftmost part name names a component of the declared type of the preceding part name

**Source:** 9.4.2 C910, J3/24-007, 18 December 2023, physical PDF151. **Class:** Restriction.

**Definition:** Each part-name except the leftmost shall be the name of a component of the declared type
of the preceding part-name. The lookup is against the declared type, not a later dynamic
type extension chosen by execution.

**Diagnostic obligation:** required.

**Facets:** `declared-component`, `nested-declared-component`, `missing-component-rejected`, `declared-not-dynamic-type`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;7.5.1;7.5.2.1;7.5.7;9.2;9.4.2R911-R912/C909/C911-C912;11.1.11.

### C911: A data reference ending in an abstract type part name is polymorphic

**Source:** 9.4.2 C911, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** If the rightmost part-name of an R911 data-ref is of abstract type, the data-ref shall
be polymorphic. Nonabstract rightmost part-names are not constrained by C911, and other
rules own abstract type definition and polymorphic declaration syntax.

**Diagnostic obligation:** required.

**Facets:** `abstract-rightmost-polymorphic`, `nonabstract-rightmost`, `abstract-rightmost-nonpolymorphic-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;7.3.2.3;7.5.2.1C738-C739;7.5.7;7.5.8;9.4.2R911-R912;11.1.11.

### C912: The leftmost part name in a data reference names a data object

**Source:** 9.4.2 C912, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** The leftmost part-name in an R911 data-ref shall be the name of a data object. It is not
a component name relative to an implicit receiver, a type name, a procedure name, or
another nonobject entity.

**Diagnostic obligation:** required.

**Facets:** `leftmost-object`, `leftmost-type-name-rejected`, `leftmost-procedure-name-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;3.42;6.2.2;7.5.1;9.2;9.4.2R911-R912;15.6 procedure names.

### C913: A part reference subscript list accounts exactly for the rank of its part name

**Source:** 9.4.2 C913, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** When a section-subscript-list appears in a part-ref, the sum of the rank of that
part-ref, the sizes of the arrays in each multiple subscript, and the number of scalar
subscripts shall equal the rank of the part-name. The meanings of subscript,
multiple-subscript, section-subscript, vector-subscript, and part-ref rank are supplied
by p2 and 9.5.3.1-.2.

**Diagnostic obligation:** required.

**Facets:** `scalar-subscript-reduces-rank`, `triplet-contributes-part-ref-rank`, `vector-subscript-contributes-part-ref-rank`, `multiple-subscript-size-accounting`, `too-few-or-too-many-subscripts-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;8.5.8;9.4.2R912/p2/C919;9.5.3.1R919-R925/C927-C931/p1-p2;9.5.3.2;9.5.3.3;10.1.11;16.9.122.

### C914: An image selector has as many cosubscripts as the part name has corank

**Source:** 9.4.2 C914, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** When an image-selector appears in a part-ref, the number of cosubscripts in that
image-selector shall equal the corank of the part-name. Image-selector syntax,
cosubscript values, teams, STAT, and NOTIFY semantics remain owned by 9.6.

**Diagnostic obligation:** required.

**Facets:** `scalar-coarray-one-cosubscript`, `higher-corank-cosubscripts`, `cosubscript-count-mismatch-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;8.5.6.1R809/C822-C826;9.4.2R912;9.6R926-R928/C932-C935/p1-p7.

### C915: A data reference with an image selector is not of C_PTR, C_FUNPTR, or TEAM_TYPE

**Source:** 9.4.2 C915, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** If one of a data-ref's part-refs has an image-selector, the data-ref shall not be of
type C_PTR or C_FUNPTR from ISO_C_BINDING, and shall not be of type TEAM_TYPE from
ISO_FORTRAN_ENV. The prohibition is triggered by any image selector in the chain and is
about the resulting data-ref type.

**Diagnostic obligation:** required.

**Facets:** `image-selector-trigger`, `c-ptr-result-rejected`, `c-funptr-result-rejected`, `team-type-result-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;9.4.2R911-R912/C914;9.6;16.10.2 TEAM_TYPE;18.2 ISO_C_BINDING C_PTR/C_FUNPTR;18.3.2.

### C916: An image-selected array part name also has a section-subscript list

**Source:** 9.4.2 C916, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** If an image-selector appears in a part-ref and the part-name is an array, a
section-subscript-list shall also appear in that part-ref. Scalar coarray part-names do
not trigger this requirement.

**Diagnostic obligation:** required.

**Facets:** `array-with-image-and-subscripts`, `scalar-with-image-no-subscripts`, `array-with-image-no-subscripts-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;8.5.6.1;8.5.8;9.4.2R912/C913-C914;9.5.3.1;9.6.

### C917: Most references to coindexed objects with polymorphic allocatable potential subobject components are prohibited

**Source:** 9.4.2 C917, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** Except when the data-ref is an actual argument to an intrinsic inquiry function or is
the designator in a type parameter inquiry, a data-ref shall not be a coindexed object
that has a polymorphic allocatable potential subobject component. Potential subobject
components are defined by 7.5.1, coindexed object formation by 9.4.3 and 9.6, and type
parameter inquiries by 9.4.5.

**Diagnostic obligation:** required.

**Facets:** `intrinsic-inquiry-exception`, `type-parameter-inquiry-exception`, `ordinary-use-rejected`, `no-polymorphic-allocatable-component-control`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;7.5.1p5-p6;7.5.4.2 allocatable components;7.5.7
polymorphism;8.5.3;8.5.6.1;9.4.2R911-R912/C914;9.4.3R914/C921;9.4.5R916/C923;9.6;16
intrinsic inquiries.

### C918: When the rightmost part reference is polymorphic, earlier coindexed part references are generally prohibited

**Source:** 9.4.2 C918, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** Except when the data-ref is an actual argument to an intrinsic inquiry function or is
the designator in a type parameter inquiry, if the rightmost part-ref is polymorphic, no
other part-ref in the data-ref shall be coindexed.

**Diagnostic obligation:** required.

**Facets:** `rightmost-polymorphic-no-earlier-coindex`, `intrinsic-inquiry-exception`, `type-parameter-inquiry-exception`, `nonpolymorphic-rightmost-control`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;7.5.7 polymorphism;8.5.6.1;9.4.2R911-R912/C914;9.4.3;9.4.5;9.6;16 intrinsic
inquiries.

### S9.4.2-002: Part-reference rank is inherited from the part name or computed from section subscripts

**Source:** 9.4.2 p2, J3/24-007, 18 December 2023, physical PDF152. **Class:** Effect.

**Definition:** A part-ref of the form part-name has the rank of that part-name. A part-ref with a
section-subscript-list has rank equal to the number of subscript triplets, plus the
number of vector subscripts, plus the sizes of one of the arrays in each multiple
section subscript. This definition feeds C913, C919, and data-ref rank in p3; it does
not define the detailed validity of the subscript forms.

**Diagnostic obligation:** not-required.

**Facets:** `bare-part-name-rank`, `triplet-rank-contribution`, `vector-subscript-rank-contribution`, `multiple-section-subscript-rank-contribution`.

**Oracle:** The original source-only catalogue recorded pending plans. The selected runtime fixtures
below now supply executable standard-oracle observations for the covered effect facets;
facets still listed in pending remain unimplemented.

S9.4.2-002 part-reference rank runtime fixtures: three complete run/effect/f2023
programs observe p2 rank contribution for a bare array part-name, a subscript-triplet
and a vector subscript. The bare case uses scalar parent obj%avec(-5:-3), for which p2
makes the part-ref rank the rank of the part-name; the program checks SHAPE [3], LBOUND
[-5], UBOUND [-3] and the distinct values. The triplet case uses
obj%grid(-4:-2,5:8)(-4:-3,7), where one triplet and one scalar subscript give rank one,
default array-section bounds 1:2, and values from column 7. The vector case uses
obj%grid(picks,8) with picks=[-2,-4], where one vector subscript gives rank one with
shape [2] in vector order. All expected shapes, bounds and values are hand-derived from
p2 plus 9.5.3.1-.2; no storage layout, TRANSFER or address oracle is used.

**Oracle limitation:** The original source-only catalogue created no Fortran test program, execution, evidence
link, fixture approval, oracle approval, or coverage claim; this generator supplies
selected runtime fixtures and mutation plans without granting those approvals or claims.
4.2 requires detection and reporting for numbered syntax rules and constraints, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number; unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

S9.4.2-002 part-reference rank fixture boundaries: only bare-part-name-rank,
triplet-rank-contribution and vector-subscript-rank-contribution are represented. The
multiple-section-subscript-rank-contribution facet remains pending because the reference
compiler in this packet rejects the Fortran 2023 @ multiple-subscript syntax, so no
portable reference-validated executable can yet observe it. The fixtures do not use or
accept the prohibited general form x%array_component when x is an array; C919's
at-most-one-nonzero-rank restriction remains with its catalogue facets.

**Dependencies:** 9.4.2C913/C919/p3;9.5.3.1R921-R925/C927-C931;9.5.3.2;9.5.3.3;16.9.109 SHAPE;16.9.194
SIZE.

### C919: A data reference has at most one nonzero-rank part reference and no allocatable or pointer part names to its right

**Source:** 9.4.2 C919, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** An R911 data-ref shall not contain more than one part-ref with nonzero rank. In
addition, a part-name to the right of a part-ref with nonzero rank shall not have the
ALLOCATABLE or POINTER attribute.

**Diagnostic obligation:** required.

**Facets:** `one-nonzero-rank-part-ref`, `two-nonzero-rank-part-refs-rejected`, `allocatable-to-right-rejected`, `pointer-to-right-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;7.5.1;7.5.4.2;8.5.3;8.5.14;9.4.2R911-R912/p2-p3;9.5.3.1.

### S9.4.2-003: A data reference takes rank, base object, type and type parameters from specified part references

**Source:** 9.4.2 p3-p4, J3/24-007, 18 December 2023, physical PDF152. **Class:** Effect.

**Definition:** The rank of a data-ref is the rank of its nonzero-rank part-ref if one exists, and zero
otherwise. The base object is the data object named by the leftmost part-name. The type
and type parameters of the data-ref are those of the rightmost part-name.

**Diagnostic obligation:** not-required.

**Facets:** `rank-from-nonzero-part-ref`, `rank-zero-when-no-nonzero-part-ref`, `base-object-leftmost`, `type-from-rightmost`, `type-parameters-from-rightmost`.

**Oracle:** The original source-only catalogue recorded pending plans. The selected runtime fixtures
below now supply executable standard-oracle observations for the covered effect facets;
facets still listed in pending remain unimplemented.

S9.4.2-003 data-reference attribute runtime fixtures: five complete run/effect/f2023
programs observe p3-p4 data-ref attributes. The nonzero-rank case uses parent array
section x(2:6:2)%alpha; p3 gives the data-ref rank from that leftmost nonzero part-ref,
so SHAPE [3], LBOUND [1], UBOUND [3] and values x(2),x(4),x(6) are checked. The
zero-rank case observes scalar x(2)%alpha in scalar contexts. The base-object case
initializes two different leftmost objects, left%inner%alpha and right%inner%alpha, and
observes only the selected leftmost object's literal. The type case uses rightmost
INTEGER and CHARACTER components in type-appropriate expressions. The type-parameter
case checks LEN(x(2)%text)==5 and compares the five-character value with a same-length
literal, while a seven-character sibling component proves the length comes from the
rightmost part name.

**Oracle limitation:** The original source-only catalogue created no Fortran test program, execution, evidence
link, fixture approval, oracle approval, or coverage claim; this generator supplies
selected runtime fixtures and mutation plans without granting those approvals or claims.
4.2 requires detection and reporting for numbered syntax rules and constraints, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number; unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

S9.4.2-003 data-reference attribute fixture boundaries: only the five S9.4.2-003 effect
facets are represented. The shape checks cover array-valued data-refs whose sole
nonzero-rank part-ref is either a parent section or a rightmost component section; they
do not establish diagnostics for invalid two-nonzero-rank references such as x%a where
both the parent and component are arrays. Character comparisons use same-length nonblank
literals and explicit LEN checks to avoid blank-padding ambiguity.

**Dependencies:** 7.2;7.4.4.1-.2;7.5.1;7.5.3;9.4.2R911-R912/C913/C919/p2;10.1.11;16.9.118;16.9.122;16.9.194.

### S9.4.2-004: Pointer components determine when a multi-part data reference is a subobject of its base object

**Source:** 9.4.2 p5, J3/24-007, 18 December 2023, physical PDF152. **Class:** Effect.

**Definition:** A data-ref with more than one part-ref is a subobject of its base object if none of the
part-names except possibly the rightmost is a pointer. If the rightmost part-name is the
only pointer, the data-ref is a subobject of its base object in contexts that pertain to
pointer association status, but not in other contexts. The dereferenced target of such a
pointer component is not made a subobject by this rule.

**Diagnostic obligation:** not-required.

**Facets:** `no-pointer-subobject`, `nonrightmost-pointer-not-subobject`, `rightmost-only-pointer-status-context`, `rightmost-only-pointer-dereference-context`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 7.5.1p5-p6;8.5.14p1-p4;9.2p1-p2;9.4.2R911-R912/C919;16.9.16
ASSOCIATED;19.5.2.3-.4;19.6.5.

### R913: A structure component is a data reference

**Source:** 9.4.2 R913, J3/24-007, 18 December 2023, physical PDF152. **Class:** Syntax.

**Definition:** The syntactic form of a structure-component is data-ref. C920 further narrows which
data-refs are structure components by requiring more than one part-ref and no
section-subscript-list on the rightmost part-ref.

**Diagnostic obligation:** required.

**Facets:** `data-ref-form`, `single-part-data-ref-not-component`, `rightmost-section-list-not-component`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;9.1R901;9.4.2R911-R912/C920;9.5.3.1R917-R918/C924-C925.

### C920: A structure component has multiple part references and no rightmost section-subscript list

**Source:** 9.4.2 C920, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** For an R913 structure-component, the data-ref shall contain more than one part-ref and
the rightmost part-ref shall not have a section-subscript-list. A data-ref ending in a
component name without a following subscript list can be a structure component even if
earlier part-refs contain subscript lists.

**Diagnostic obligation:** required.

**Facets:** `more-than-one-part-ref`, `rightmost-no-section-list`, `rightmost-section-list-rejected`, `earlier-section-list-allowed`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;9.4.2R911-R913/C913/C919;9.5.3.1R917-R918/C924-C925;note3 is informative only.

### S9.4.2-005: A structure component is not referenced or defined before declaration of its base object

**Source:** 9.4.2 p6 first sentence, J3/24-007, 18 December 2023, physical PDF152. **Class:** Restriction.

**Definition:** A structure component shall be neither referenced nor defined before the declaration of
the base object. This source-order restriction is stated in prose rather than as a
numbered constraint; future diagnostics therefore need explicit diagnostic-policy
treatment unless another numbered owner applies.

**Diagnostic obligation:** not-required.

**Facets:** `reference-after-base-declaration`, `definition-after-base-declaration`, `reference-before-base-declaration`, `definition-before-base-declaration`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 4.2;5.1 program unit ordering;7.5.2.1;8.1;8.2;9.2;9.4.2p3;19.6.5.

### S9.4.2-006: A structure component has pointer status only from its rightmost part name

**Source:** 9.4.2 p6 second sentence, J3/24-007, 18 December 2023, physical PDF152. **Class:** Effect.

**Definition:** A structure component is a pointer only if the rightmost part-name has the POINTER
attribute. Pointer attributes on earlier part-names affect subobject status and
association paths, but do not make a nonpointer rightmost component itself a pointer.

**Diagnostic obligation:** not-required.

**Facets:** `rightmost-pointer-component`, `rightmost-nonpointer-after-pointer-parent`, `earlier-pointer-does-not-propagate`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, or runtime failure.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. 4.2 requires
detection and reporting for numbered syntax rules and constraints, but these plans do
not require a fixed message, fatal rejection, nonzero status, or printed rule number;
unnumbered prose restrictions are not promoted to numbered-constraint diagnostics.

**Dependencies:** 7.5.1;7.5.4.2 pointer components;8.5.14;9.4.2p5/C919;16.9.16
ASSOCIATED;19.5.2.3-.4;19.6.5.

<!-- END GENERATED 9.4.2 -->
