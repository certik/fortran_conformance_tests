# Fortran 2023 17.8: Exceptional values

The canonical catalogue is `doc/catalogues/exceptional_values_17_8.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 17.8 -->

### S17.8-001: Exceptional floating-point classes are subnormal, infinity, and NaN

**Source:** S17.8, J3/24-007, 18 December 2023, physical PDF484. **Class:** Effect.

**Definition:** ISO/IEC 60559 specifies subnormal values as very small values with reduced precision,
infinities as values created by overflow or division by zero, and NaNs as undefined
values or values created by invalid operations.

**Diagnostic obligation:** not-required.

**Facets:** `subnormal-class-definition`, `infinity-class-definition`, `nan-class-definition`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming single-image programs, and assert only portable Clause 17
properties: named derived-type values, exact logical results, exact type/kind/rank
characteristics, supported IEEE special-value classifications, exact representable
arithmetic under the named support inquiry, or get/set round trips. Unsupported guarded
branches are skips, not passes. Processor-dependent support availability, NaN payloads,
exact halting mechanics, initial modes, approximation latitude, and IOMSG text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument and Restriction paragraphs do not create diagnostic obligations.
Negative source mutations are non-diagnostic unless a future numbered rule requires
rejection and the rejection is portably observable. Existing IEEE profile and formatting
fixtures are recorded as nearby coverage only and are not rebound here.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
declarations, expressions, execution, modules, intrinsic assignment, I/O harnesses,
formatting references, and generic intrinsic foundations. Unregistered dependencies
explicitly retained: the remaining Clause 17 procedure descriptions, IEEE derived types
and named constants, processor support inquiries, exception-flag semantics,
rounding-mode semantics, and ISO/IEC 60559 terminology.

### S17.8-002: A normal number is outside the exceptional classes

**Source:** S17.8, J3/24-007, 18 December 2023, physical PDF484. **Class:** Effect.

**Definition:** A value outside the subnormal, infinity, and NaN classes is called a normal number.

**Diagnostic obligation:** not-required.

**Facets:** `normal-number-definition`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming single-image programs, and assert only portable Clause 17
properties: named derived-type values, exact logical results, exact type/kind/rank
characteristics, supported IEEE special-value classifications, exact representable
arithmetic under the named support inquiry, or get/set round trips. Unsupported guarded
branches are skips, not passes. Processor-dependent support availability, NaN payloads,
exact halting mechanics, initial modes, approximation latitude, and IOMSG text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument and Restriction paragraphs do not create diagnostic obligations.
Negative source mutations are non-diagnostic unless a future numbered rule requires
rejection and the rejection is portably observable. Existing IEEE profile and formatting
fixtures are recorded as nearby coverage only and are not rebound here.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
declarations, expressions, execution, modules, intrinsic assignment, I/O harnesses,
formatting references, and generic intrinsic foundations. Unregistered dependencies
explicitly retained: the remaining Clause 17 procedure descriptions, IEEE derived types
and named constants, processor support inquiries, exception-flag semantics,
rounding-mode semantics, and ISO/IEC 60559 terminology.

### S17.8-003: Exceptional-value inquiry and generation procedures are provided

**Source:** S17.8, J3/24-007, 18 December 2023, physical PDF484. **Class:** Effect.

**Definition:** IEEE_IS_FINITE, IEEE_IS_NAN, IEEE_IS_NEGATIVE, and IEEE_IS_NORMAL test value properties;
IEEE_VALUE generates IEEE numbers of any class; IEEE_SUPPORT_SUBNORMAL,
IEEE_SUPPORT_INF, and IEEE_SUPPORT_NAN inquire availability for a real kind.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-is-finite-provided`, `ieee-is-nan-provided`, `ieee-is-negative-provided`, `ieee-is-normal-provided`, `ieee-value-provided`, `exceptional-support-inquiries-provided`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming single-image programs, and assert only portable Clause 17
properties: named derived-type values, exact logical results, exact type/kind/rank
characteristics, supported IEEE special-value classifications, exact representable
arithmetic under the named support inquiry, or get/set round trips. Unsupported guarded
branches are skips, not passes. Processor-dependent support availability, NaN payloads,
exact halting mechanics, initial modes, approximation latitude, and IOMSG text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument and Restriction paragraphs do not create diagnostic obligations.
Negative source mutations are non-diagnostic unless a future numbered rule requires
rejection and the rejection is portably observable. Existing IEEE profile and formatting
fixtures are recorded as nearby coverage only and are not rebound here.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
declarations, expressions, execution, modules, intrinsic assignment, I/O harnesses,
formatting references, and generic intrinsic foundations. Unregistered dependencies
explicitly retained: the remaining Clause 17 procedure descriptions, IEEE derived types
and named constants, processor support inquiries, exception-flag semantics,
rounding-mode semantics, and ISO/IEC 60559 terminology.

<!-- END GENERATED 17.8 -->
