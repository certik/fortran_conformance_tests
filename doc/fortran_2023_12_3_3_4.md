# Fortran 2023 12.3.3.4: Stream access - access-position fixtures

The canonical catalogue is `doc/catalogues/stream_access_12_3_3_4.json`. This author packet adds finite scratch-file runtime fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 13 facets in 8 program(s); 13 of 20 facets are represented and 7 remain pending.

<!-- BEGIN GENERATED 12.3.3.4 -->

### S12.3.3.4-001: Stream access accesses file storage units of an external stream file

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** Stream access is a method of accessing file storage units of an external stream file.

**Diagnostic obligation:** not-required.

**Facets:** `stream-access-file-storage-units`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`stream-access-file-storage-units`. They create the external files in the run directory
with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited to exact
integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-002: Stream-access properties depend on formatted versus unformatted connection

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** The properties of an external file connected for stream access depend on whether the
connection is unformatted or formatted.

**Diagnostic obligation:** not-required.

**Facets:** `stream-properties-depend-on-form`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`stream-properties-depend-on-form`. They create the external files in the run directory
with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited to exact
integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-003: Stream file storage units are read or written only by stream access data transfer statements

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Restriction.

**Definition:** While connected for stream access, file storage units shall be read or written only by
stream access data transfer statements.

**Diagnostic obligation:** not-required.

**Facets:** `stream-storage-units-stream-transfer-only`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-004: Unformatted stream file storage units have consecutive positive positions

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** For unformatted stream access, each file storage unit is uniquely identified by a
positive integer position; the first is position 1 and each subsequent position is one
greater than the preceding position.

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-stream-positive-positions`, `unformatted-stream-first-position-one`, `unformatted-stream-subsequent-position-plus-one`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`unformatted-stream-first-position-one`, `unformatted-stream-positive-positions`,
`unformatted-stream-subsequent-position-plus-one`. They create the external files in the
run directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are
limited to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END,
and POS= inequalities or values fixed by this source. No file name, external
representation, processor-specific IOSTAT number, IOMSG text, formatted-stream byte
spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-005: Unformatted stream units may be accessed out of order when positioning is possible and reads are permitted for written units

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** If positioning is possible, unformatted stream file storage units need not be read or
written in position order; any storage unit may be written, and a written storage unit
may be read while connected if READ is permitted.

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-stream-out-of-order-permitted`, `unformatted-stream-gap-write-permitted`, `unformatted-stream-written-unit-read-permitted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`unformatted-stream-gap-write-permitted`, `unformatted-stream-out-of-order-permitted`,
`unformatted-stream-written-unit-read-permitted`. They create the external files in the
run directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are
limited to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END,
and POS= inequalities or values fixed by this source. No file name, external
representation, processor-specific IOSTAT number, IOMSG text, formatted-stream byte
spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-006: Formatted stream record markers impose record structure and no end marker makes a final record incomplete

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** For formatted stream access, some file storage units can contain record markers that
impose record structure; if there is no record marker at the end of the file, the final
record is incomplete.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-record-markers-impose-records`, `formatted-stream-final-record-incomplete-without-marker`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`formatted-stream-record-markers-impose-records`. They create the external files in the
run directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are
limited to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END,
and POS= inequalities or values fixed by this source. No file name, external
representation, processor-specific IOSTAT number, IOMSG text, formatted-stream byte
spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-007: No maximum length is applicable to formatted stream records

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** No maximum length is applicable to records imposed on a formatted stream file.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-no-maximum-record-length`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-008: Writing an empty formatted stream record with no marker has no effect

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** Writing an empty record with no record marker to a formatted stream file has no effect.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-empty-record-without-marker-no-effect`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`formatted-stream-empty-record-without-marker-no-effect`. They create the external files
in the run directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles
are limited to exact integer/character values, character lengths, IOSTAT zero,
IOSTAT_END, and POS= inequalities or values fixed by this source. No file name, external
representation, processor-specific IOSTAT number, IOMSG text, formatted-stream byte
spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-009: Formatted stream file storage units have positive positions beginning at one

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** For formatted stream access, each file storage unit is uniquely identified by a positive
integer position and the first file storage unit is at position 1.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-positive-positions`, `formatted-stream-first-position-one`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`formatted-stream-first-position-one`, `formatted-stream-positive-positions`. They
create the external files in the run directory with STATUS='SCRATCH' and also close with
STATUS='DELETE'. Oracles are limited to exact integer/character values, character
lengths, IOSTAT zero, IOSTAT_END, and POS= inequalities or values fixed by this source.
No file name, external representation, processor-specific IOSTAT number, IOMSG text,
formatted-stream byte spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-010: Formatted stream positioning may use positions previously identified by INQUIRE POS=

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** If positioning is possible for formatted stream access, the file position can be set to
a position previously identified by the POS= specifier in an INQUIRE statement.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-position-to-inquired-pos-permitted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`formatted-stream-position-to-inquired-pos-permitted`. They create the external files in
the run directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are
limited to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END,
and POS= inequalities or values fixed by this source. No file name, external
representation, processor-specific IOSTAT number, IOMSG text, formatted-stream byte
spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-011: Processors may prohibit some control characters in formatted stream files

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** A processor may prohibit some control characters from appearing in a formatted stream
file.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-control-character-prohibition-processor-latitude`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-012: Formatted stream successive-position relationships are processor dependent

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** For formatted stream access, the relationship between positions of successive file
storage units is processor dependent, and not all positive integers need correspond to
valid positions.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-successive-position-relationship-processor-dependent`, `formatted-stream-not-all-positive-positions-valid`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.4-013: Formatted stream end-of-file record marker presence is optional

**Source:** S12.3.3.4, J3/24-007, 18 December 2023, physical PDF245. **Class:** Effect.

**Definition:** For formatted stream access, there might or might not be a record marker at the end of
the file.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-end-marker-optional`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.3.3.4 -->

## Access-position fixture derivations

### `stream_storage_units` / `S12.3.3.4-001`

**Facets:** `stream-access-file-storage-units`.

12.3.3.4 p1 defines stream access over file storage units; single-character stream writes at POS=1 and POS=2 are read by position.

Feature mutations: `read-first-position`, `pre-read-1-ch-remove-sentinel-init`, `pre-read-1-ch-expected-sentinel-init`, `pre-read-1-ch-default-sentinel-init`.

### `stream_form_dependence` / `S12.3.3.4-002`

**Facets:** `stream-properties-depend-on-form`.

12.3.3.4 p2 makes stream properties form-dependent; the fixture observes a formatted record and an unformatted positioned storage unit separately.

Feature mutations: `unformatted-read-wrong-pos`, `pre-read-1-rec-remove-sentinel-init`, `pre-read-1-rec-expected-sentinel-init`, `pre-read-1-rec-default-sentinel-init`, `pre-read-2-ch-remove-sentinel-init`, `pre-read-2-ch-expected-sentinel-init`, `pre-read-2-ch-default-sentinel-init`.

### `unformatted_stream_positions` / `S12.3.3.4-004`

**Facets:** `unformatted-stream-positive-positions`, `unformatted-stream-first-position-one`, `unformatted-stream-subsequent-position-plus-one`.

12.3.3.4 p3 says unformatted stream positions are positive, first at 1, and subsequent positions increment by one; POS=1 and POS=2 are read distinctly.

Feature mutations: `positive-position-read-second`, `first-position-write-second`, `subsequent-position-read-first`, `pre-read-1-ch-remove-sentinel-init`, `pre-read-1-ch-expected-sentinel-init`, `pre-read-1-ch-default-sentinel-init`, `pre-read-2-ch-remove-sentinel-init`, `pre-read-2-ch-expected-sentinel-init`, `pre-read-2-ch-default-sentinel-init`.

### `unformatted_stream_out_of_order_gap` / `S12.3.3.4-005`

**Facets:** `unformatted-stream-out-of-order-permitted`, `unformatted-stream-gap-write-permitted`, `unformatted-stream-written-unit-read-permitted`.

12.3.3.4 p3 permits positioned unformatted stream writes out of order and at a later position; written positions are then read by POS=.

Feature mutations: `out-of-order-stream-read-gap`, `stream-gap-read-lower`, `stream-written-read-lower`, `pre-read-1-ch-remove-sentinel-init`, `pre-read-1-ch-expected-sentinel-init`, `pre-read-1-ch-default-sentinel-init`, `pre-read-2-ch-remove-sentinel-init`, `pre-read-2-ch-expected-sentinel-init`, `pre-read-2-ch-default-sentinel-init`, `pre-read-3-ch-remove-sentinel-init`, `pre-read-3-ch-expected-sentinel-init`, `pre-read-3-ch-default-sentinel-init`.

### `formatted_stream_records` / `S12.3.3.4-006`

**Facets:** `formatted-stream-record-markers-impose-records`.

12.3.3.4 p4 says formatted stream record markers impose record structure; two advancing writes are read as two records.

Feature mutations: `remove-first-record-marker`, `pre-read-1-first-remove-sentinel-init`, `pre-read-1-first-expected-sentinel-init`, `pre-read-1-first-default-sentinel-init`, `pre-read-2-second-remove-sentinel-init`, `pre-read-2-second-expected-sentinel-init`, `pre-read-2-second-default-sentinel-init`.

### `formatted_empty_no_marker` / `S12.3.3.4-008`

**Facets:** `formatted-stream-empty-record-without-marker-no-effect`.

12.3.3.4 p4 says an empty formatted stream record with no marker has no effect; POS= before and after that write is unchanged while following data advances it.

Feature mutations: `empty-write-creates-marker`.

### `formatted_stream_positions` / `S12.3.3.4-009`

**Facets:** `formatted-stream-positive-positions`, `formatted-stream-first-position-one`.

12.3.3.4 p4 gives formatted stream positive positions and first position 1; INQUIRE(POS=) observes those portable properties without byte-count assumptions.

Feature mutations: `advance-initial-before-inquire`, `drop-positive-pos-inquire`.

### `formatted_stream_inquired_pos` / `S12.3.3.4-010`

**Facets:** `formatted-stream-position-to-inquired-pos-permitted`.

12.3.3.4 p4 permits formatted stream positioning to a previously inquired POS= value; overwriting and rereading that saved position yields C.

Feature mutations: `write-to-initial-not-saved`, `pre-read-1-ch-remove-sentinel-init`, `pre-read-1-ch-expected-sentinel-init`, `pre-read-1-ch-default-sentinel-init`.

