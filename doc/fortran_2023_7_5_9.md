# Fortran 2023: 7.5.9 Derived-type specifier - compile implementation

**Catalogue source review: reviewed.** Current case/evidence adjudications are separate content-bound records.

The packet has **34 compile-only cases**: **17 diagnostic inputs** and **17 positive controls/admissions**, with **zero runtime cases**. **28 of 59 facets are represented; 31 remain PENDING.** Representation is not compiler success, fixture approval or source closure.

## Source and concrete qualification

Authority: J3/24-007, 18 December 2023, 688 physical PDF pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
Only 7.5.9 on PDF106 is implemented, ending before the actual 7.5.10 heading.
Its 11 original units, 36 fine units, 47 accounting rows and 11 requirement IDs
are preserved. Independent source eligibility is not fixture approval.

Original R401/list notation (PDF44-45), R701/C701/C702 and R703 alternatives
(PDF76-77), R733 and parameter definition/order (PDF91-92), type-name privacy
(PDF89), C939/allocation rules (PDF160-162), inquiry limits (PDF153), SELECT TYPE
(PDF224-226), and actual/dummy agreement (PDF342-343) supply the concrete premises.
All parameter expressions used as values/defaults are scalar INTEGER constants;
KIND tags are abstract user parameters, never intrinsic representation selectors.

R754's empty group is on a genuinely parameterized all-defaulted record. Its
repair deletes only that group. The missing comma separates two complete known
keyword items, with `(5)` closed before the next keyword so digits cannot merge
lexically. R755 changes are in actual derived-type parameter lists, not calls,
constructors or intrinsic selectors. C795's object name is `scalar_name`, not a
legitimate TYPE(INTEGER/enum/enumeration) alternative. The PRIVATE-type provider
must compile; an unchanged client USE without ONLY fails at TYPE(hidden).
Only provider type-name PRIVATE-to-PUBLIC repairs that case.

C796 uses a positional value on a true non-PDT, without a spurious keyword.
C797 uses required k/n and a defaulted third formal pad: duplicate tests do not
even exceed the formal count. Missing values are repaired by inserting only
their keywords/values, never by changing defaults or shifting positional values.
C798 uses three/four all-defaulted abstract KIND formals, a later keyword, and
an unused earlier keyword in the repair. Invalid lists receive no runtime or
model value mapping. C799 defaults every real formal; `payload` is a genuine
ordinary component, but not a type-parameter keyword.

C7100's shared dummy declaration/allocation control has TYPE(record(k=7,n=*)),
ALLOCATABLE, INTENT(INOUT), with a scalar allocatable fixed-n=5 actual.
Both deferred-parameter sets are empty; neither n is deferred. C939 requires
the allocation's n=* exactly for the one assumed-LEN dummy object, with constant
KIND7 and no SOURCE/MOLD. The body allocates only when unallocated, checks STAT
with explicit failure, then defines payload; it never inquires about absent LEN.
The SELECT TYPE control uses a genuinely polymorphic nonoptional ordinary dummy,
not an unallocated selector. Its one ordinary extensible guard has matching KIND
and all LENs assumed, with a fully defined compatible fixed actual available.
These are compile admissions, not allocation, parameter-value or finalization
runtime observations. The sole C7100 negative is an ordinary local LEN asterisk,
repaired to a scalar integer; coupled function-result, named-constant and C939
negative contexts remain pending.

## Reporting and ownership limits

The eight numbered rules require reporting capability under 4.2, not fatal
rejection, rule codes or fixed wording. Contracts match located finite
type-specifier-role/property messages. Their prospective phrases are not
fabricated native observations. Generic parser recovery, wrong subjects or
properties, echoed source, unsupported facilities, Internal/verifier failures,
resource errors and timeouts never corroborate the intended cause. A native
message that does not establish the cause remains a failing observation.
Only an independently justified exact family-native warning allowance may
admit a nonfatal warning; no additional diagnostic policy is adopted.

Controls are shared within a primary rule when the exact repaired sources
coincide. A repair control for C796/R755/C799 is not a duplicate wrapper used
to clear that rule's separately pending canonical-reuse admission. No existing
R/C target, family or documentary graph is registered. All fifteen S-owned
facets, including runtime correspondence/conversion/default effects, remain
pending, as do the other conditional and canonical-use facets. No 7.5.8,
constructor or inherited-parameter program is added or repurposed.

## Definitions

<!-- BEGIN GENERATED 7.5.9 -->

### R754: A derived-type specifier has a type name and an optional nonempty parameter list

**Source:** 7.5.9, R754, R754.type-name, R754.optional-parameter-list, R754.nonempty-list; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Syntax.

**Definition:** The derived-type-spec grammar is a type name optionally followed by a parenthesized,
nonempty type-param-spec-list. Omitting the entire list is different from supplying
empty parentheses. The named type's accessibility, parameterization, required/defaulted
parameters and enclosing context impose their own constraints.

**Diagnostic obligation:** required.

**Facets:** `bare-nonparameterized-name`, `bare-defaulted-parameterized-name`, `explicit-positional-list`, `explicit-keyword-list`, `mixed-positional-keyword-list`, `empty-parenthesized-list`, `missing-list-separator`, `specifier-context-use-graph`.

**Oracle:** Keep grammar changes independent of unknown types, absent required values,
abstract/nonextensible misuse and duplicate keyword associations. Compile admissions are
not evidence that a runtime parameter value was observed.

Finite compile implementation: 7 compile-only cases represent 7 facets: 2 diagnostic
inputs and 5 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** TYPE(intrinsic-type-spec), TYPE(enum-type-spec) and TYPE(enumeration-type-spec) are
separate legitimate R703 alternatives, not automatically invalid R754 examples. Future
negatives need complete otherwise conforming contexts and source-minimal controls. A
report must identify the actual type-specifier role and violated property at the
declared source point/span. Required reporting capability does not require fatal
rejection or printed rule codes. Generic recovery, wrong subjects,
unsupported-capability wrappers, compiler internal/verifier/resource failures and
timeouts are not that cause.

**Dependencies:** 7.2/R701/C701/C702, PDF76;7.3.2.1/R702/R703/C703-C713,
PDF77-78;7.5.3;R755/C795-C7100;7.5.10.

### R755: A type-parameter specification has an optional keyword-equals prefix and a value

**Source:** 7.5.9, R755, R755.optional-keyword, R755.keyword-equals, R755.parameter-value; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Syntax.

**Definition:** A type-param-spec consists of a type-param-value, optionally preceded by keyword and
equals. The keyword prefix is not a pointer-association arrow, and neither an explicit
keyword nor its value can be empty. R701 owns the value alternatives; C798 and C799 own
ordering and keyword membership.

**Diagnostic obligation:** required.

**Facets:** `positional-value-admission`, `keyword-value-admission`, `keyword-equals-not-arrow`, `nonempty-keyword`, `nonempty-parameter-value`.

**Oracle:** The source context must identify a derived-type parameter list, not an actual argument
list, component list or intrinsic kind/length selector. Preserve the one intended
grammar repair and all C795-C7100 premises.

Finite compile implementation: 4 compile-only cases represent 3 facets: 3 diagnostic
inputs and 1 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** The conditional conversion prose in p2 does not admit REAL or nonscalar values in place
of the scalar INTEGER expression required by R701. Future negatives need complete
otherwise conforming contexts and source-minimal controls. A report must identify the
actual type-specifier role and violated property at the declared source point/span.
Required reporting capability does not require fatal rejection or printed rule codes.
Generic recovery, wrong subjects, unsupported-capability wrappers, compiler
internal/verifier/resource failures and timeouts are not that cause.

**Dependencies:** R754;7.2/R701/C701/C702, PDF76;C798/C799;7.3.2.1/C704;4.2p2(3).

### C795: The specifier's type name denotes an accessible derived type

**Source:** 7.5.9, C795, C795.named-derived-type, C795.accessible-name; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Restriction.

**Definition:** In a derived-type-spec, the type name must denote a derived type accessible in the
relevant scope. A use alias can still denote that same definition. A data object's name
is not a derived-type name merely because it is in scope. Enclosing previous-definition
and type-category conditions remain.

**Diagnostic obligation:** required.

**Facets:** `local-derived-type`, `host-associated-type`, `use-associated-type`, `renamed-use-associated-type`, `ordinary-object-not-type-name`, `inaccessible-provider-type`.

**Oracle:** Use one actual type definition, a complete legal provider and otherwise valid
declarations. Private type-name access is not private component or binding access;
matching spellings across definitions are not type identity.

Finite compile implementation: 4 compile-only cases represent 4 facets: 2 diagnostic
inputs and 2 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** Named ENUM and ENUMERATION types are valid separate TYPE alternatives in F2023 and must
not be used as supposedly invalid non-derived TYPE selectors. A submitted-program
reporting capability does not imply all cross-file inconsistencies are detectable in an
isolated source file. Future negatives need complete otherwise conforming contexts and
source-minimal controls. A report must identify the actual type-specifier role and
violated property at the declared source point/span. Required reporting capability does
not require fatal rejection or printed rule codes. Generic recovery, wrong subjects,
unsupported-capability wrappers, compiler internal/verifier/resource failures and
timeouts are not that cause.

**Dependencies:** 7.3.2.1/.2/.3, PDF77-79;7.5.2.2/.4;8.5.2;14.2.2;19.3/19.5.1;4.2p2(3)/(6).

### C796: Only a parameterized type can have a parameter specification list

**Source:** 7.5.9, C796, C796.parameter-list-present, C796.parameterized-type-only; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Restriction.

**Definition:** A type-param-spec-list is allowed only for a parameterized derived type. This does not
require a list whenever a type is parameterized: valid defaults can supply omitted
values. Inherited parameters are not erased merely because an extension has no new names
in its own header.

**Diagnostic obligation:** required.

**Facets:** `nonparameterized-bare-name`, `nonparameterized-list-exclusion`, `inherited-only-parameterized-type`.

**Oracle:** Distinguish no parameters, all-defaulted parameters and inherited-only parameterization.
R754 owns empty-list grammar and C797 owns missing or duplicated parameter
correspondences.

Finite compile implementation: 2 compile-only cases represent 1 facets: 1 diagnostic
inputs and 1 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** 7.5.3.1p1 gives a sufficient header-name condition, not a basis for ignoring parameters
inherited by an extension. No intrinsic-kind numbering assumption is used. Future
negatives need complete otherwise conforming contexts and source-minimal controls. A
report must identify the actual type-specifier role and violated property at the
declared source point/span. Required reporting capability does not require fatal
rejection or printed rule codes. Generic recovery, wrong subjects,
unsupported-capability wrappers, compiler internal/verifier/resource failures and
timeouts are not that cause.

**Dependencies:** 7.5.3.1p1 and7.5.3.2, PDF92;7.5.7.2, PDF104-105;R754/C797.

### C797: Parameter specifications are unique and supply every parameter without a default

**Source:** 7.5.9, C797, C797.at-most-one-value, C797.each-parameter-correspondence, C797.no-default-condition, C797.mandatory-explicit-value; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Restriction.

**Definition:** No parameter of the type may receive more than one type-param-spec. Every parameter
without a default must have a corresponding specification. Positional and named
correspondence refer to the same formal parameters, including inherited ones; defaulted
parameters need not all be written.

**Diagnostic obligation:** required.

**Facets:** `required-values-supplied`, `missing-required-kind`, `missing-required-length`, `duplicate-keyword`, `positional-keyword-duplicate`, `defaulted-values-omitted`, `inherited-correspondence-source-use`.

**Oracle:** The missing-required cases use valid remaining keywords to avoid accidental positional
reassignment. Duplicate cases name a real parameter and keep all nondefaulted parameters
supplied exactly once apart from the intended duplicate.

Finite compile implementation: 6 compile-only cases represent 5 facets: 4 diagnostic
inputs and 2 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** Do not repair a missing value by adding a default to the type definition when the
intended contrast is explicit use. A supplied value and its default are not two explicit
type-param-spec occurrences. Future negatives need complete otherwise conforming
contexts and source-minimal controls. A report must identify the actual type-specifier
role and violated property at the declared source point/span. Required reporting
capability does not require fatal rejection or printed rule codes. Generic recovery,
wrong subjects, unsupported-capability wrappers, compiler internal/verifier/resource
failures and timeouts are not that cause.

**Dependencies:** R754/R755/C798/C799;7.5.3.1/R733 andp4;7.5.3.2;7.5.7.2;7.2/C701/C702.

### C798: Positional type-parameter values precede all keyworded values

**Source:** 7.5.9, C798, C798.omitted-keyword-condition, C798.every-preceding-omitted, C798.no-positional-after-keyword; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Restriction.

**Definition:** A type-param-spec can omit keyword= only when every preceding specification also omitted
it. Thus positional specifications form an initial prefix; after any keyword appears,
every following specification is keyworded.

**Diagnostic obligation:** required.

**Facets:** `all-positional-admission`, `keyword-suffix-admission`, `keyword-then-positional`, `positional-keyword-positional`.

**Oracle:** Use small constant abstract KIND parameters with independent ordinary components so no
intrinsic selector, component bound or required-value failure competes with ordering.
The repaired sequence is source-valid; no parameter-value correspondence is asserted for
the invalid sequence.

Finite compile implementation: 4 compile-only cases represent 2 facets: 2 diagnostic
inputs and 2 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** A syntactically invalid ordering has no promised runtime mapping. A generic
duplicate/missing-parameter report is not this ordering cause, and a two-item example
that also names the same parameter twice is not an isolated contrast. Future negatives
need complete otherwise conforming contexts and source-minimal controls. A report must
identify the actual type-specifier role and violated property at the declared source
point/span. Required reporting capability does not require fatal rejection or printed
rule codes. Generic recovery, wrong subjects, unsupported-capability wrappers, compiler
internal/verifier/resource failures and timeouts are not that cause.

**Dependencies:** R754/R755/C797/C799 andp2;7.5.3.1/.2;7.2/C701;4.2p2(3).

### C799: A parameter keyword names a formal type parameter, not a component or arbitrary object

**Source:** 7.5.9, C799, C799.each-keyword, C799.formal-type-parameter-name; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Restriction.

**Definition:** Every keyword in a type-param-spec is a parameter name of the specified type. A
component name or unrelated host name does not become a type parameter keyword.
Inherited formal parameters remain parameters of an extended type, subject to their
actual name and ordering rules.

**Diagnostic obligation:** required.

**Facets:** `formal-keyword-admission`, `unknown-keyword`, `component-name-not-parameter`, `inherited-keyword-source-use`.

**Oracle:** All values are valid constant scalar INTEGER expressions and every real formal has a
default for the isolated bad-keyword cases. The only changed feature is membership in
this type's formal parameter-name set.

Finite compile implementation: 3 compile-only cases represent 2 facets: 2 diagnostic
inputs and 1 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** Named argument keywords and structure-constructor component keywords have different
namespaces and canonical rules. A vague unknown-symbol diagnostic does not establish the
required type-parameter role. Future negatives need complete otherwise conforming
contexts and source-minimal controls. A report must identify the actual type-specifier
role and violated property at the declared source point/span. Required reporting
capability does not require fatal rejection or printed rule codes. Generic recovery,
wrong subjects, unsupported-capability wrappers, compiler internal/verifier/resource
failures and timeouts are not that cause.

**Dependencies:** R755/C797/C798;7.5.3.1/.2;7.5.7.2;7.5.10/C7106;19.3.

### C7100: An assumed derived length parameter is confined to the three specified contexts

**Source:** 7.5.9, C7100, C7100.asterisk-value, C7100.dummy-declaration-exception, C7100.associate-name-declaration-exception, C7100.dummy-allocation-exception, C7100.other-context-exclusion; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Restriction.

**Definition:** An asterisk used as a derived type's type-param-value is allowed only in a
dummy-argument declaration, an associate-name declaration, or allocation of a dummy
argument. These are necessary context permissions, not waivers of the KIND-constant,
SELECT TYPE or ALLOCATE constraints. In particular ALLOCATE has the additional C939
if-and-only-if condition for assumed parameters of every allocated dummy.

**Diagnostic obligation:** required.

**Facets:** `dummy-declaration`, `associate-name-type-guard`, `dummy-allocation`, `ordinary-local-declaration-exclusion`, `function-result-declaration-exclusion`, `derived-named-constant-exclusion`, `allocation-context-canonical-link`.

**Oracle:** Separate declaration and allocation permissions. For the positive dummy allocation, the
assumed LEN comes from a compatible actual, not an inquiry of an unallocated deferred
parameter. The allocation asterisk denotes its current assumed value under9.7.1.1p6.

Finite compile implementation: 4 compile-only cases represent 4 facets: 1 diagnostic
inputs and 3 positive controls/admissions. Each negative has a same-primary
source-minimal conforming repair; a control may support several such contrasts and
explicit admission facets. No runtime parameter-value effect or unsupported reuse graph
is credited. Located role/property predicates are causal evidence guards, not mandatory
wording, fatal-exit or printed-rule-code policies. Current source/case/evidence
adjudications remain separate content-bound records, not inferred from generation or
observations.

**Oracle limitation:** Read only defined values and independently established allocation/association states.
Keep pointer targets alive, use exact eligible procedure interfaces, guard allocation
before dependent inquiries, and do not dereference null or undefined pointers. A
component value is not an ABI layout or a byte encoding. A SELECT TYPE guard is not an
arbitrary redeclaration inside ASSOCIATE. Function-result contexts also require the
C727/C728 source connection before negative authoring. No abstract/SEQUENCE/BIND guard,
constant KIND asterisk, invalid allocatable association, assumed-type TYPE(*)
substitution or automatic allocation-failure skip is allowed. Future negatives need
complete otherwise conforming contexts and source-minimal controls. A report must
identify the actual type-specifier role and violated property at the declared source
point/span. Required reporting capability does not require fatal rejection or printed
rule codes. Generic recovery, wrong subjects, unsupported-capability wrappers, compiler
internal/verifier/resource failures and timeouts are not that cause.

**Dependencies:** 7.2/R701/C701/C702/p7, PDF76;7.3.2.1/C704-C708, PDF77;7.4.4.2/C727/C728,
PDF85;9.4.5/R916/C923/p2, PDF153;9.7.1.1/C936-C940/p5/p6,
PDF160-162;11.1.11/C1164-C1168, PDF225;15.5.2.4/.6;19.5.1.6.

### S7.5.9-001: Positional and keyworded parameter values correspond to the specified formal parameters

**Source:** 7.5.9, p2.positional-no-keyword, p2.type-parameter-order, p2.keyword-present, p2.named-parameter-correspondence; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Effect.

**Definition:** Without a type-parameter keyword, a value corresponds according to type parameter order.
With a keyword, it corresponds to the formal parameter named by that keyword. Conforming
lists preserve C797-C799, and inherited parameters follow the parent-prefix/local-header
order from7.5.3.2.

**Diagnostic obligation:** not-required.

**Facets:** `nonextended-positional-order`, `keyword-reordering`, `mixed-prefix-and-keywords`, `inherited-positional-prefix`, `keyword-name-correspondence`.

**Oracle:** Explicitly named parameter inquiries and defined component observations use distinct
literal expectations, never equality between two potentially mis-associated
constructors. Existing order witnesses require separate semantic link review for this
consumer.

**Oracle limitation:** The paragraph specifies parameter correspondence, not component order or
procedure-argument matching. A failing feature implementation does not justify replacing
a planned runtime effect by compilation alone.

**Dependencies:** 7.5.3.2, PDF92;C797-C799;7.2;9.4.5/R916/C923, PDF153;6.2 name correspondence.

### S7.5.9-002: An explicit parameter value is converted to the parameter's integer kind when needed

**Source:** 7.5.9, p2.conversion-if-necessary, p2.intrinsic-assignment-conversion, p2.parameter-integer-kind; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Effect.

**Definition:** If necessary, an explicitly supplied parameter value is converted using
intrinsic-assignment rules to the integer kind declared for that parameter. For
expression-valued specifications, R701 still requires scalar INTEGER, with the
additional constant-expression requirement for KIND and specification-expression
requirements in declarations. The * and : forms retain their separate assumed/deferred
semantics, not numeric-expression admission. This is distinct from conversion of a
default expression in the parameter definition.

**Diagnostic obligation:** not-required.

**Facets:** `default-to-selected-integer-carrier`, `selected-to-default-integer-carrier`, `keyword-supplied-conversion`, `distinct-kind-profile-and-canonical-use`.

**Oracle:** Integer range and kind inquiries can qualify representable small values and actual
carrier distinctions independently of the parameter conversion being tested. Existing
default-expression conversions are not automatically explicit-use conversion witnesses.

**Oracle limitation:** No REAL-to-INTEGER admission, overflow-dependent result, bit-pattern assumption or
circular profile using the tested parameter conversion is authorized. Failure to
distinguish carriers remains visible.

**Dependencies:** 7.2/R701/C701, PDF76;7.3.2.1/C704, PDF77;7.5.3.1p2/p4, PDF92;10.2.1.3 INTEGER intrinsic
assignment;16.9 KIND/RANGE/SELECTED_INT_KIND.

### S7.5.9-003: An omitted parameter value uses that parameter's default

**Source:** 7.5.9, p3, p3.no-explicit-value, p3.parameter-default-value; J3/24-007, 18 December 2023, physical PDF106, printed92. **Class:** Effect.

**Definition:** If a type parameter has no explicit type-param-value in the specifier, its value is its
default. C797 requires an explicit value where no default exists. Omission does not
replace explicitly supplied values, and a default expression must already be valid under
R733 and7.5.3.1.

**Diagnostic obligation:** not-required.

**Facets:** `omitted-kind-default`, `omitted-length-default`, `explicit-value-overrides-default`, `partial-keyword-omission`, `prior-kind-dependent-default`, `inherited-default-source-use`.

**Oracle:** Reuse observed omission/default/override cases only after checking their actual
specifier and parameter declaration contexts. All linked witnesses retain their
canonical IDs, phase, profiles and independent adjudications.

**Oracle limitation:** R733 requires a scalar INTEGER constant default expression. A prior KIND parameter can
be a constant primary in the definition; a prior LEN parameter does not become constant
just because it has a default. No invented zero default for an unspecified nondefaulted
parameter is permitted.

**Dependencies:** R733 and7.5.3.1p2/p4/p5, PDF91-92;C797;7.5.3.2;7.5.7.2;10.1.12.

<!-- END GENERATED 7.5.9 -->

## Exact finite case and repair census

### `R754_valid__type_specifier_bare_nonparameterized`

**Primary:** R754; **phase/evidence:** compile / positive-control.

**Facets:** `bare-nonparameterized-name`.

Previously defined ordinary nonparameterized record, without a parameter list.

### `R754_valid__type_specifier_bare_defaulted`

**Primary:** R754; **phase/evidence:** compile / positive-control.

**Facets:** `bare-defaulted-parameterized-name`, `empty-parenthesized-list`.

Both genuine PDT formals have scalar INTEGER constant defaults.

### `R754_valid__type_specifier_positional`

**Primary:** R754; **phase/evidence:** compile / positive-control.

**Facets:** `explicit-positional-list`.

Abstract KIND5 and LEN11 are scalar integer values, not intrinsic representation selectors.

### `R754_valid__type_specifier_keywords`

**Primary:** R754; **phase/evidence:** compile / positive-control.

**Facets:** `explicit-keyword-list`, `missing-list-separator`.

Known unique keywords and valid constant scalar values.

### `R754_valid__type_specifier_mixed`

**Primary:** R754; **phase/evidence:** compile / positive-control.

**Facets:** `mixed-positional-keyword-list`.

A positional prefix is followed only by an unused formal keyword.

### `R754_invalid__type_specifier_empty_list`

**Primary:** R754; **phase/evidence:** compile / effect.

**Facets:** `empty-parenthesized-list`.

Parameterized and all-defaulted: neither C796 nor a missing-required C797 value is the defect.

**Minimal repair:** `R754_valid__type_specifier_bare_defaulted`; `source.f90` changes `record()` to `record` once. **Diagnostic anchor:** `source.f90:8`.

### `R754_invalid__type_specifier_missing_separator`

**Primary:** R754; **phase/evidence:** compile / effect.

**Facets:** `missing-list-separator`.

The closed constant expression (5) prevents digit/name lexical merging; both complete keyword items are known, unique and scalar INTEGER. Inserting their comma is the only selected repair.

**Minimal repair:** `R754_valid__type_specifier_keywords`; `source.f90` changes `k=(5) n=11` to `k=(5),n=11` once. **Diagnostic anchor:** `source.f90:8`.

### `R755_valid__type_specifier_keyword_repair`

**Primary:** R755; **phase/evidence:** compile / positive-control.

**Facets:** `keyword-equals-not-arrow`, `nonempty-keyword`, `nonempty-parameter-value`.

One shared repair for three R755 syntax defects, not numbered-target reuse admission credit.

### `R755_invalid__type_specifier_arrow`

**Primary:** R755; **phase/evidence:** compile / effect.

**Facets:** `keyword-equals-not-arrow`.

Actual derived-type parameter list; every other formal is defaulted. Neither call arguments, constructor components nor intrinsic selectors are involved.

**Minimal repair:** `R755_valid__type_specifier_keyword_repair`; `source.f90` changes `n=>3` to `n=3` once. **Diagnostic anchor:** `source.f90:8`.

### `R755_invalid__type_specifier_empty_keyword`

**Primary:** R755; **phase/evidence:** compile / effect.

**Facets:** `nonempty-keyword`.

Actual derived-type parameter list; every other formal is defaulted. Neither call arguments, constructor components nor intrinsic selectors are involved.

**Minimal repair:** `R755_valid__type_specifier_keyword_repair`; `source.f90` changes `record(=3)` to `record(n=3)` once. **Diagnostic anchor:** `source.f90:8`.

### `R755_invalid__type_specifier_empty_value`

**Primary:** R755; **phase/evidence:** compile / effect.

**Facets:** `nonempty-parameter-value`.

Actual derived-type parameter list; every other formal is defaulted. Neither call arguments, constructor components nor intrinsic selectors are involved.

**Minimal repair:** `R755_valid__type_specifier_keyword_repair`; `source.f90` changes `record(n=)` to `record(n=3)` once. **Diagnostic anchor:** `source.f90:8`.

### `C795_valid__type_specifier_local_type_repair`

**Primary:** C795; **phase/evidence:** compile / positive-control.

**Facets:** `local-derived-type`, `ordinary-object-not-type-name`.

An ordinary record definition exists before the declaration.

### `C795_invalid__type_specifier_ordinary_object`

**Primary:** C795; **phase/evidence:** compile / effect.

**Facets:** `ordinary-object-not-type-name`.

scalar_name is an INTEGER object, not an intrinsic/enum/enumeration type alternative. Only the type selector changes; the object declaration remains legal.

**Minimal repair:** `C795_valid__type_specifier_local_type_repair`; `source.f90` changes `type(scalar_name)` to `type(record)` once. **Diagnostic anchor:** `source.f90:7`.

### `C795_valid__type_specifier_public_provider_repair`

**Primary:** C795; **phase/evidence:** compile / positive-control.

**Facets:** `use-associated-type`, `inaccessible-provider-type`.

Provider first, unchanged client second. The exported name denotes the same derived definition.

### `C795_invalid__type_specifier_private_type`

**Primary:** C795; **phase/evidence:** compile / effect.

**Facets:** `inaccessible-provider-type`.

The provider must compile. Client USE has no ONLY/private-name import. Only provider type-name PRIVATE->PUBLIC repairs the client TYPE(hidden) failure.

**Minimal repair:** `C795_valid__type_specifier_public_provider_repair`; `provider.f90` changes `type, private :: hidden` to `type, public :: hidden` once. **Diagnostic anchor:** `client.f90:4`.

### `C796_valid__type_specifier_nonparameterized_repair`

**Primary:** C796; **phase/evidence:** compile / positive-control.

**Facets:** `nonparameterized-list-exclusion`.

Own diagnostic repair only; the separate bare-name numbered-reuse facet remains pending.

### `C796_invalid__type_specifier_nonparameterized_list`

**Primary:** C796; **phase/evidence:** compile / effect.

**Facets:** `nonparameterized-list-exclusion`.

A true non-PDT gets one positional integer; no unknown keyword creates a secondary C799 cause.

**Minimal repair:** `C796_valid__type_specifier_nonparameterized_repair`; `source.f90` changes `record(3)` to `record` once. **Diagnostic anchor:** `source.f90:6`.

### `C797_valid__type_specifier_required_keywords_repair`

**Primary:** C797; **phase/evidence:** compile / positive-control.

**Facets:** `required-values-supplied`, `missing-required-kind`, `missing-required-length`, `duplicate-keyword`.

Required KIND k and LEN n are supplied once. The third formal pad has a valid default.

### `C797_invalid__type_specifier_missing_kind`

**Primary:** C797; **phase/evidence:** compile / effect.

**Facets:** `missing-required-kind`.

Other required formals use valid keywords. Defaults are not occurrences; three explicit items do not exceed the three-formal count in the duplicate case.

**Minimal repair:** `C797_valid__type_specifier_required_keywords_repair`; `source.f90` changes `record(n=3)` to `record(k=7,n=3)` once. **Diagnostic anchor:** `source.f90:9`.

### `C797_invalid__type_specifier_missing_length`

**Primary:** C797; **phase/evidence:** compile / effect.

**Facets:** `missing-required-length`.

Other required formals use valid keywords. Defaults are not occurrences; three explicit items do not exceed the three-formal count in the duplicate case.

**Minimal repair:** `C797_valid__type_specifier_required_keywords_repair`; `source.f90` changes `record(k=7)` to `record(k=7,n=3)` once. **Diagnostic anchor:** `source.f90:9`.

### `C797_invalid__type_specifier_duplicate_keyword`

**Primary:** C797; **phase/evidence:** compile / effect.

**Facets:** `duplicate-keyword`.

Other required formals use valid keywords. Defaults are not occurrences; three explicit items do not exceed the three-formal count in the duplicate case.

**Minimal repair:** `C797_valid__type_specifier_required_keywords_repair`; `source.f90` changes `,n=5` to `` once. **Diagnostic anchor:** `source.f90:9`.

### `C797_valid__type_specifier_required_mixed_repair`

**Primary:** C797; **phase/evidence:** compile / positive-control.

**Facets:** `required-values-supplied`, `positional-keyword-duplicate`.

The first formal is supplied positionally, then n by keyword.

### `C797_invalid__type_specifier_positional_keyword_duplicate`

**Primary:** C797; **phase/evidence:** compile / effect.

**Facets:** `positional-keyword-duplicate`.

All keywords are real; a legal positional prefix avoids C798, and three items fit three formals. The only repeated correspondence is k.

**Minimal repair:** `C797_valid__type_specifier_required_mixed_repair`; `source.f90` changes `,k=5` to `` once. **Diagnostic anchor:** `source.f90:9`.

### `C798_valid__type_specifier_keyword_then_positional_repair`

**Primary:** C798; **phase/evidence:** compile / positive-control.

**Facets:** `keyword-then-positional`.

All abstract KIND formals have independent constant defaults; the repair uses an unused earlier keyword.

### `C798_invalid__type_specifier_keyword_then_positional`

**Primary:** C798; **phase/evidence:** compile / effect.

**Facets:** `keyword-then-positional`.

Three/four defaulted formals; the named later formal is distinct from the valid positional prefix. No missing-required or obvious duplicate cause is introduced. Invalid ordering has no value mapping.

**Minimal repair:** `C798_valid__type_specifier_keyword_then_positional_repair`; `source.f90` changes `c=31,11` to `c=31,a=11` once. **Diagnostic anchor:** `source.f90:9`.

### `C798_valid__type_specifier_positional_keyword_positional_repair`

**Primary:** C798; **phase/evidence:** compile / positive-control.

**Facets:** `positional-keyword-positional`.

All abstract KIND formals have independent constant defaults; the repair uses an unused earlier keyword.

### `C798_invalid__type_specifier_positional_keyword_positional`

**Primary:** C798; **phase/evidence:** compile / effect.

**Facets:** `positional-keyword-positional`.

Three/four defaulted formals; the named later formal is distinct from the valid positional prefix. No missing-required or obvious duplicate cause is introduced. Invalid ordering has no value mapping.

**Minimal repair:** `C798_valid__type_specifier_positional_keyword_positional_repair`; `source.f90` changes `d=44,22` to `d=44,b=22` once. **Diagnostic anchor:** `source.f90:10`.

### `C799_valid__type_specifier_keyword_repair`

**Primary:** C799; **phase/evidence:** compile / positive-control.

**Facets:** `unknown-keyword`, `component-name-not-parameter`.

All actual formals are defaulted; n is an unused real formal.

### `C799_invalid__type_specifier_unknown_keyword`

**Primary:** C799; **phase/evidence:** compile / effect.

**Facets:** `unknown-keyword`.

Only the keyword's membership changes. payload is a real ordinary component, not a formal parameter; this is a TYPE declaration, never a constructor component list.

**Minimal repair:** `C799_valid__type_specifier_keyword_repair`; `source.f90` changes `nn=3` to `n=3` once. **Diagnostic anchor:** `source.f90:8`.

### `C799_invalid__type_specifier_component_keyword`

**Primary:** C799; **phase/evidence:** compile / effect.

**Facets:** `component-name-not-parameter`.

Only the keyword's membership changes. payload is a real ordinary component, not a formal parameter; this is a TYPE declaration, never a constructor component list.

**Minimal repair:** `C799_valid__type_specifier_keyword_repair`; `source.f90` changes `payload=3` to `n=3` once. **Diagnostic anchor:** `source.f90:8`.

### `C7100_valid__type_specifier_associate_guard`

**Primary:** C7100; **phase/evidence:** compile / positive-control.

**Facets:** `associate-name-type-guard`.

A genuinely polymorphic ordinary nonoptional dummy is always present, not an unallocated selector. One ordinary extensible TYPE IS guard uses matching constant KIND7 and every LEN assumed; a defined fixed actual is possible.

### `C7100_valid__type_specifier_dummy_allocation`

**Primary:** C7100; **phase/evidence:** compile / positive-control.

**Facets:** `dummy-declaration`, `dummy-allocation`.

Shared declaration/allocation admission: scalar TYPE dummy and actual are both allocatable/nonpolymorphic, KIND7, with identical empty deferred-parameter sets. Actual LEN5 is fixed, not deferred. C939 requires n=* for the one assumed-LEN dummy object. Allocation is guarded and STAT failure is explicit; there is no SOURCE/MOLD or inquiry of unallocated deferred LEN.

### `C7100_valid__type_specifier_local_explicit_length_repair`

**Primary:** C7100; **phase/evidence:** compile / positive-control.

**Facets:** `ordinary-local-declaration-exclusion`.

A local non-dummy variable has complete constant KIND/LEN values.

### `C7100_invalid__type_specifier_local_assumed_length`

**Primary:** C7100; **phase/evidence:** compile / effect.

**Facets:** `ordinary-local-declaration-exclusion`.

Only the LEN asterisk changes. The variable is neither dummy nor associate; no FUNCTION/RESULT, PARAMETER/constructor or ALLOCATE/C939 negative is smuggled into C7100.

**Minimal repair:** `C7100_valid__type_specifier_local_explicit_length_repair`; `source.f90` changes `n=*` to `n=3` once. **Diagnostic anchor:** `source.f90:10`.

## Complete finite pending plans

These maps are copied exactly from current canonical JSON; no declaration or observation supplies missing reuse evidence.

### Pending R754

* **`specifier-context-use-graph`** - PENDING - Retain canonical declarations, structure constructors, ALLOCATE, array constructors and SELECT TYPE guards as separate context links. The finite link schema still does not support arbitrary numbered targets/wrappers; no duplicate context program is authorized just to clear this facet.

### Pending R755

* **`positional-value-admission`** - PENDING - Reuse a reviewed R754 positional-list control after it exists and a numbered-target connection is supported; do not manufacture an otherwise identical primary-R755 program.
* **`keyword-value-admission`** - PENDING - Likewise preserve a valid R754 keyword-list control and its unique formal-parameter mapping.

### Pending C795

* **`host-associated-type`** - PENDING - Prefer a canonical same-definition/host-association witness from S7.5.2.4-001, retaining its actual ownership and current phase, rather than an identical accessibility copy.
* **`renamed-use-associated-type`** - PENDING - Assess existing same-definition renamed-use witnesses and require a numbered-target connection before assigning reuse credit.

### Pending C796

* **`nonparameterized-bare-name`** - PENDING - Reuse the R754 nonparameterized no-list admission after canonical connection support, rather than duplicate it.
* **`inherited-only-parameterized-type`** - PENDING - Assess S7_5_3_2_002_valid__inherited_only with the exact inherited parameter declarations/order and7.5.7.2 basis. This is prospective numbered-target reuse, not an invented nonparameterized child or a duplicate runtime.

### Pending C797

* **`defaulted-values-omitted`** - PENDING - Reuse independently reviewed default-use witnesses under S7.5.3.1-002 where their actual omission meets this context; no arbitrary duplicate control.
* **`inherited-correspondence-source-use`** - PENDING - Retain inherited parameter uniqueness/default/required-value contexts for a complete source connection after the extension source gate. A local-only list does not establish the whole inherited signature.

### Pending C798

* **`all-positional-admission`** - PENDING - Reuse the R754 all-positional control, preserving its formal order and supplied/defaulted values.
* **`keyword-suffix-admission`** - PENDING - Reuse valid all-keyword and positional-prefix/keyword-suffix controls once numbered-target connections exist.

### Pending C799

* **`formal-keyword-admission`** - PENDING - Reuse the valid R754/R755 keyword controls with independently established formal names and unique associations.
* **`inherited-keyword-source-use`** - PENDING - Retain the complete inherited-parameter keyword connection to7.5.7.2 and the actual definition. Do not infer a keyword from a parent's local USE alias or from a component name.

### Pending C7100

* **`function-result-declaration-exclusion`** - PENDING - Retain a source-use plan for assumed derived LEN on a function result. C727 also constrains assumed-parameter function names, and C728 adds function characteristics; establish their exact scope for the complete explicit-RESULT/declaration context before claiming C7100-only isolation. Prefer canonical ownership/reuse when coupled, rather than label a multi-constraint function negative isolated.
* **`derived-named-constant-exclusion`** - PENDING - Retain a conditional complete PARAMETER context with only ordinary constant-initializable components and a valid scalar constructor; the special intrinsic CHARACTER named-constant permission in7.2p7 does not extend to a derived LEN parameter. Verify all constant/constructor premises before authoring the pair.
* **`allocation-context-canonical-link`** - PENDING - An asterisk on a non-dummy ALLOCATE object also violates C939. Preserve canonical C939 ownership and a future source connection rather than advertise a second isolated C7100 negative. Replacing * by an expression is not a valid repair when the dummy's parameter is assumed, because C939 requires * in that case.

### Pending S7.5.9-001

* **`nonextended-positional-order`** - PENDING - Assess existing S7_5_3_2_001_valid__header/reversed_statements/reversed_list witnesses and their actual independent parameter/member observations. Preserve the source primary and avoid copying a header-order program.
* **`keyword-reordering`** - PENDING - Plan a complete three-parameter type with distinct representable values, all keyworded in a different order, and independent observations by parameter/member name. The expected mapping is not another equally reordered constructor.
* **`mixed-prefix-and-keywords`** - PENDING - Plan one positional prefix followed by keywords for still-unassigned parameters in reversed order. All values and defaults remain unique; independently check the resulting parameter values.
* **`inherited-positional-prefix`** - PENDING - Assess existing S7_5_3_2_002_valid__parent_prefix/local_suffix/ancestor_chain witnesses at their actual scope and phase. Source-use for a whole family is not automatically a singleton link or an extra execution.
* **`keyword-name-correspondence`** - PENDING - Retain keyword identifier correspondence, including case-equivalent spelling, as a bounded source-use connection to lexical name rules and the actual formal set. Do not manufacture a renamed duplicate just to clear this facet.

### Pending S7.5.9-002

* **`default-to-selected-integer-carrier`** - PENDING - Plan explicit values for KIND and LEN parameters declared with a supported SELECTED_INT_KIND carrier. Use small representable values, not the carrier's numeric code as an intrinsic kind parameter. Establish whether source/target carriers are actually distinct before claiming a conversion-path observation.
* **`selected-to-default-integer-carrier`** - PENDING - Plan an explicitly supplied selected-kind INTEGER constant or eligible LEN specification expression for a default-INTEGER parameter. Check the parameter's declared integer kind and independent representable value, preserving C701/C704.
* **`keyword-supplied-conversion`** - PENDING - Exercise an explicit valid keyword route under the same independently qualified carrier conditions; do not silently rely on a default expression that would test7.5.3.1p4 instead.
* **`distinct-kind-profile-and-canonical-use`** - PENDING - Review existing S7.5.3.1-001/-002 and intrinsic INTEGER conversion/profile facilities. A selector returning the default kind is not evidence of cross-kind conversion; add a separately justified profile or retain the facet pending if the necessary distinction is unavailable. No arbitrary fixed kind number or unsupported-selector compilation-as-skip.

### Pending S7.5.9-003

* **`omitted-kind-default`** - PENDING - Assess S7_5_3_1_002_valid__kind_default_positive/zero/negative as existing finite abstract-KIND omission witnesses. No duplicate cases or intrinsic representation meaning for those discriminator values.
* **`omitted-length-default`** - PENDING - Assess S7_5_3_1_002_valid__length_default with its actual defined parameter/member observations and source qualifications.
* **`explicit-value-overrides-default`** - PENDING - Assess S7_5_3_1_002_valid__expression_and_override; verify the explicit use actually bypasses the default rather than relabel a definition-only conversion test.
* **`partial-keyword-omission`** - PENDING - Assess S7_5_3_1_004_valid__dependent_default: its TYPE(packet(tag=5)) declaration supplies tag by keyword and omits extent, independently checking extent=6. The separate S7_5_3_1_002_valid__per_declarator case omits its entire list and is not itself a partial-keyword witness. Preserve both actual contexts instead of inventing duplicate coverage.
* **`prior-kind-dependent-default`** - PENDING - Assess S7_5_3_1_004_valid__dependent_default, preserving the previously declared KIND-primary condition. A prior LEN with a default is not thereby a constant-expression primary.
* **`inherited-default-source-use`** - PENDING - Retain the inherited-default connection to7.5.7.2 and the parent's valid parameter defaults. The earlier S7.5.3.1-002 inherited-default facet is also pending; do not cite an unwritten inherited-default program as existing evidence.

## Reproduction and remaining gates

`python3 -B tools/generate_type_specifier_fixtures.py --check` verifies exact input bytes, source/phase metadata and both Markdown regions. Targeted regressions check all minimal repairs, shared controls, constraint interactions, invalid-order noninterpretation, C7100 complete contexts and located role/property/failure guards. The original case bindings remain unchanged.

Independent source/fixture/cause review is recorded in `doc/source_audits/batch_020.json`. Twenty-three cases have qualifying GNU f2023 observations; eleven negatives retain source-only adjudication and explicit limits on conservative diagnostic noncredit. All seventeen controls compile with GNU. Observations preserve exact IDs/current fingerprints, complete commands, source roots/input hashes and actual reference modes. Twelve calibrated negative predicates have genuinely refreshed observations; archived rows are not relabelled. No compile admission becomes a runtime effect, and all31 pending facets remain open.
