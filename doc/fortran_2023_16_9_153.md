# Fortran 2023 16.9.153: NORM2

The canonical catalogue is `doc/catalogues/norm2_intrinsic_16_9_153.json`. Effective review state is computed by the registry; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.153 -->

### S16.9.153-001: NORM2 argument restrictions

**Source:** S16.9.153, J3/24-007, 18 December 2023, physical PDF440. **Class:** Restriction.

**Definition:** X shall be a real array; DIM shall be an integer scalar in the range 1 through the rank
of X.

**Diagnostic obligation:** not-required.

**Facets:** `NORM2-X-real-array`, `NORM2-DIM-integer-scalar`, `NORM2-DIM-valid-rank-range`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.153-002: NORM2 result type, scalar form, and DIM shape

**Source:** S16.9.153, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** The NORM2 result has the same type and type parameters as X; without DIM it is scalar,
and with DIM it has rank n-1 and the shape of X with dimension DIM removed.

**Diagnostic obligation:** not-required.

**Facets:** `NORM2-result-same-type-parameters`, `NORM2-result-scalar-without-DIM`, `NORM2-result-rank-and-shape-with-DIM`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.153-003: NORM2 nonzero norm approximation latitude

**Source:** S16.9.153, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** For nonzero arrays, NORM2(X) is a processor-dependent approximation to the generalized
L2 norm of X.

**Diagnostic obligation:** not-required.

**Facets:** `NORM2-processor-dependent-nonzero-approximation`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155. 16.9.153#p6 is the known
census-hash-drift formula unit; visual extraction confirmed the recommendation text and
the catalogue retains the census inventory hash.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.153-004: NORM2 zero-size array result is zero

**Source:** S16.9.153, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** For size-zero X, NORM2(X) has the value zero.

**Diagnostic obligation:** not-required.

**Facets:** `NORM2-zero-size-result-zero`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.153-005: NORM2 DIM result equals whole-array or slice NORM2 values

**Source:** S16.9.153, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** With DIM present, rank-one X gives the same value as NORM2(X); otherwise each result
element equals NORM2 of the corresponding rank-one slice.

**Diagnostic obligation:** not-required.

**Facets:** `NORM2-DIM-rank-one-equals-whole-array`, `NORM2-DIM-elements-equal-slice-norms`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.153-006: NORM2 overflow and underflow avoidance recommendation

**Source:** S16.9.153, J3/24-007, 18 December 2023, physical PDF441. **Class:** Effect.

**Definition:** It is recommended that processors compute NORM2 without undue overflow or underflow.

**Diagnostic obligation:** not-required.

**Facets:** `NORM2-avoid-undue-overflow-underflow-recommendation`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155. 16.9.153#p6 is the known
census-hash-drift formula unit; visual extraction confirmed the recommendation text and
the catalogue retains the census inventory hash.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.153 -->
