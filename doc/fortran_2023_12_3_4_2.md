# Fortran 2023 12.3.4.2: Advancing and nonadvancing input/output - I/O positioning/internal fixtures

The canonical catalogue is `doc/catalogues/advancing_nonadvancing_io_12_3_4_2.json`. This author packet adds finite runtime fixtures; fixture approval remains separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 5 facets in 3 rule-bound manifest(s); 5 of 7 facets are represented and 2 remain pending.

<!-- BEGIN GENERATED 12.3.4.2 -->

### S12.3.4.2-001: Advancing input/output positions after the last record read or written absent error

**Source:** S12.3.4.2, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** An advancing input/output statement always positions a record file after the last record
read or written unless an error condition occurs.

**Diagnostic obligation:** not-required.

**Facets:** `advancing-io-positions-after-last-record`, `advancing-io-error-exception`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.2-001 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`advancing-io-positions-after-last-record`. These fixtures are finite single-image
positive runtime observations over scratch or local external files and character
internal files. They assert only exact integers, logical inequalities, exact characters
with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants, IOLENGTH=/POS=/SIZE=
values or inequalities derived from the executing processor, and FILE_STORAGE_SIZE
positivity. They do not assert physical byte encodings, record-marker layout, IOMSG
text, processor-specific numeric error statuses, unsupported deferred-length internal
WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.2-001 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.2-002: Nonadvancing input/output may leave a record file within the current or a subsequent record

**Source:** S12.3.4.2, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** A nonadvancing input/output statement may position a record file at a character position
within the current record or a subsequent record, and nonadvancing input/output can read
or write a record by a sequence of data transfer statements each accessing part of the
record.

**Diagnostic obligation:** not-required.

**Facets:** `nonadvancing-may-position-within-current-record`, `nonadvancing-may-position-subsequent-record`, `nonadvancing-partial-record-sequence`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.2-002 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`nonadvancing-may-position-within-current-record`,
`nonadvancing-partial-record-sequence`. These fixtures are finite single-image positive
runtime observations over scratch or local external files and character internal files.
They assert only exact integers, logical inequalities, exact characters with LEN checks,
IOSTAT zero, IOSTAT_END/EOR named constants, IOLENGTH=/POS=/SIZE= values or inequalities
derived from the executing processor, and FILE_STORAGE_SIZE positivity. They do not
assert physical byte encodings, record-marker layout, IOMSG text, processor-specific
numeric error statuses, unsupported deferred-length internal WRITE behavior, or
diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.2-002 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.2-003: Incomplete nonadvancing output is completed as corresponding advancing output before close or repositioning

**Source:** S12.3.4.2, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** If a nonadvancing output statement leaves a file positioned within a current record and
no further output statement for that file precedes close, BACKSPACE, ENDFILE, or REWIND,
the effect is as if it were the corresponding advancing output statement.

**Diagnostic obligation:** not-required.

**Facets:** `nonadvancing-output-completed-before-close`, `nonadvancing-output-completed-before-backspace-endfile-rewind`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.2-003 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`nonadvancing-output-completed-before-backspace-endfile-rewind`,
`nonadvancing-output-completed-before-close`. These fixtures are finite single-image
positive runtime observations over scratch or local external files and character
internal files. They assert only exact integers, logical inequalities, exact characters
with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants, IOLENGTH=/POS=/SIZE=
values or inequalities derived from the executing processor, and FILE_STORAGE_SIZE
positivity. They do not assert physical byte encodings, record-marker layout, IOMSG
text, processor-specific numeric error statuses, unsupported deferred-length internal
WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.2-003 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

<!-- END GENERATED 12.3.4.2 -->

<!-- BEGIN IO POSITIONING INTERNAL FIXTURES -->
## I/O positioning, file-storage-unit, and internal-file fixtures

This packet adds 12 runtime programs and 12 rule-bound manifests representing 21 selected facets in 12.3.4.2, 12.3.4.3, 12.3.4.4, 12.3.5, and 12.4. The programs use scratch or local files deleted by the program and character internal files. READ targets use nondefault sentinels and pre-READ guards; stream positions are derived with IOLENGTH=, POS=, and SIZE= inquiries; character equality checks first compare LEN. Deferred-length allocatable internal WRITE facets remain pending because the reference compiler does not validate them.
<!-- END IO POSITIONING INTERNAL FIXTURES -->

### `advancing_sequential_read_order` / `S12.3.4.2-001`

**Facets:** `advancing-io-positions-after-last-record`.

12.3.4.2 p1 and 12.3.4.4 p5 position an advancing sequential read after the record just read; a following read gets the next record, and BACKSPACE then rereads the preceding record.

Feature/sentinel mutations: `advance-between-reads-reset`, `next-record-uses-backspace`, `preceding-record-backspace-to-rewind`, `sentinel-first-record-remove`, `sentinel-first-record-expected`, `sentinel-first-record-default`, `sentinel-second-record-remove`, `sentinel-second-record-expected`, `sentinel-second-record-default`, `sentinel-backspace-reread-remove`, `sentinel-backspace-reread-expected`, `sentinel-backspace-reread-default`.

### `nonadvancing_input_segments` / `S12.3.4.2-002`

**Facets:** `nonadvancing-may-position-within-current-record`, `nonadvancing-partial-record-sequence`.

12.3.4.2 p2 permits a record to be read by successive nonadvancing transfers; 12.3.4.3 p2 and 12.3.4.4 p4 leave the current record/position for the second transfer when no condition occurred.

Feature/sentinel mutations: `first-read-advances`, `first-segment-too-wide`, `second-segment-too-narrow`, `sentinel-first-segment-remove`, `sentinel-first-segment-expected`, `sentinel-first-segment-default`, `sentinel-second-segment-remove`, `sentinel-second-segment-expected`, `sentinel-second-segment-default`.

### `nonadvancing_output_completion` / `S12.3.4.2-003`

**Facets:** `nonadvancing-output-completed-before-close`, `nonadvancing-output-completed-before-backspace-endfile-rewind`.

12.3.4.2 p2 gives advancing-output effect when a partial nonadvancing output is followed by close or rewind, and 12.3.4.3 p4/12.3.4.4 p4 keep successive nonadvancing output in the current record.

Feature/sentinel mutations: `close-completion-intervening-output`, `rewind-completion-intervening-output`, `first-output-advances`, `second-output-different-suffix`, `sentinel-closed-record-remove`, `sentinel-closed-record-expected`, `sentinel-closed-record-default`, `sentinel-rewind-record-remove`, `sentinel-rewind-record-expected`, `sentinel-rewind-record-default`, `sentinel-combined-output-remove`, `sentinel-combined-output-expected`, `sentinel-combined-output-default`.
