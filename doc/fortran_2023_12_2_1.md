# Fortran 2023 12.2.1: Definition of a record

The canonical catalogue is `doc/catalogues/definition_of_a_record_12_2_1.json`. This source packet records source accounting. Selected I/O records/files fixtures now supply executable standard-oracle observations and mutation plans; fixture approval remains separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN IO RECORDS FILES FIXTURES -->
## I/O records and files runtime observations

Five complete single-image fixtures discharge 31 selected facets in 12.1-12.3.2. They use internal character files, scratch sequential files, and local named files deleted by the program. All input targets and inquiry logicals start from distinctive sentinels with pre-transfer guards; character comparisons check LEN first; endfile observations compare only with ISO_FORTRAN_ENV IOSTAT_END. Permanent mutations remove or substitute the relevant WRITE/READ/REWIND/ENDFILE/INQUIRE/OPEN/CLOSE feature and alter sentinels/oracles; no fixture asserts byte sizes, record-marker layout, IOMSG text, numeric IOSTAT values, preconnected-file identity, or coarray behavior.
<!-- END IO RECORDS FILES FIXTURES -->

<!-- BEGIN GENERATED 12.2.1 -->

### S12.2.1-001: Records are value or character sequences with three kinds

**Source:** S12.2.1, J3/24-007, 18 December 2023, physical PDF242. **Class:** Effect.

**Definition:** A record is a sequence of values or a sequence of characters, need not correspond to a
physical entity, and has one of three kinds: formatted, unformatted, or endfile.

**Diagnostic obligation:** not-required.

**Facets:** `record-value-sequence`, `record-character-sequence`, `record-not-necessarily-physical`, `record-kind-formatted-unformatted-endfile`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.2.1-001 I/O records/files runtime fixtures: formatted fixtures recover exact
character sequences and the unformatted fixture recovers integer/logical value sequences
from records; the endfile fixture observes the named IOSTAT_END condition after explicit
and implicit endfile records. These three executable observations cover the three record
kinds without observing or assuming any physical record entity.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.2.1-001 I/O records/files fixture boundaries: These fixtures are single-image
positive runtime observations over internal CHARACTER files, scratch external files, and
conservative local named files created in the execution working directory and deleted by
the program. They assert only exact integers/logicals/characters, LEN before character
equality, IOSTAT zero, and equality with the named IOSTAT_END constant. They do not
assert byte sizes, record marker layout, IOMSG text, numeric IOSTAT values, ambient file
existence, path syntax beyond the chosen local names, preconnected file identity,
coarray image interactions, stream/record correspondence, or required diagnostics for
mismatched forms/nonexistent files. Remaining facets stay pending with their
source-accounting rationale.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.2.1 -->
