# Fortran 2023: 10.2.2.2 Syntax of the pointer assignment statement

Source-only draft catalogue: see `doc/catalogues/pointer_assignment_statement_syntax_10_2_2_2.json`.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

Authority: J3/24-007, 18 December 2023, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`, physical PDF pages 193-195.

This packet owns the pointer-assignment-statement grammar and numbered constraints R1034-R1041 and C1016-C1034. The unnumbered note is informative. Paragraphs 1-2 define local terms; detailed data-pointer and procedure-pointer assignment effects remain dependencies on 10.2.2.3 and 10.2.2.4.

<!-- BEGIN GENERATED 10.2.2.2 -->

### R1034: A pointer assignment statement has one data-pointer or procedure-pointer form

**Source:** 10.2.2.2 R1034; J3/24-007 PDF p193. **Class:** Syntax.

**Definition:** The pointer-assignment-stmt syntax admits four data-pointer forms using => with optional
bounds specifications, lower-bounds expressions, bounds remapping, or lower/upper bounds
expressions, and one procedure-pointer form using => with a proc-target.

**Diagnostic obligation:** required.

**Facets:** `data-pointer-bounds-spec-list-form`, `data-pointer-lower-bounds-expr-form`, `data-pointer-bounds-remapping-form`, `data-pointer-array-bounds-expr-form`, `procedure-pointer-target-form`.

**Oracle:** Syntax plans establish parse/admission only; they must not claim association status,
bounds, remapping, or procedure characteristics without the relevant constraints and
10.2.2.3/10.2.2.4 effects.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.14 POINTER;8.5.18 TARGET;9.2 variable;10.1.2.9 expr;10.2.2.3 data pointer assignment
NOT YET REGISTERED;10.2.2.4 procedure pointer assignment NOT YET REGISTERED;15 procedure
interfaces NOT YET REGISTERED.

### R1035: A data pointer object is a pointer variable name or data pointer component

**Source:** 10.2.2.2 R1035; J3/24-007 PDF p194. **Class:** Syntax.

**Definition:** A data-pointer-object is either a variable-name or a scalar-variable followed by a
data-pointer-component-name.

**Diagnostic obligation:** required.

**Facets:** `variable-name-pointer-object`, `data-pointer-component-object`.

**Oracle:** This production names the syntactic subjects; POINTER status, data-ref status, component
pointer status, and coindexed exclusion are owned by C1024-C1027.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.14 POINTER;9.2 variable;9.4.2 structure components;10.2.2.2 C1024-C1027.

### C1016: A non-unlimited-polymorphic data target requires type compatibility and equal kind parameters

**Source:** 10.2.2.2 C1016; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** If the data-target is not unlimited polymorphic, the data-pointer-object shall be type
compatible with it and their corresponding kind type parameters shall be equal.

**Diagnostic obligation:** required.

**Facets:** `compatible-type-control`, `incompatible-type-rejected`, `kind-parameter-match-control`, `kind-parameter-mismatch-rejected`.

**Oracle:** Negative plans must keep target designator eligibility, rank rules, and polymorphism
route constant so rejection is attributable to C1016 alone.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 7.3.3 type compatibility;7.5.3 kind type parameters;8.5.14 POINTER;8.5.18
TARGET;10.2.2.3 NOT YET REGISTERED.

### C1017: An unlimited polymorphic data target restricts the data pointer object type

**Source:** 10.2.2.2 C1017; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** If the data-target is unlimited polymorphic, the data-pointer-object shall either be
unlimited polymorphic or be of a type with the BIND or SEQUENCE attribute.

**Diagnostic obligation:** required.

**Facets:** `unlimited-polymorphic-pointer-control`, `bind-type-pointer-control`, `sequence-type-pointer-control`, `ordinary-type-pointer-rejected`.

**Oracle:** Do not infer dynamic-type effects here; this is the admission constraint for an
unlimited-polymorphic data-target.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 7.3.2.3 polymorphism;7.5.2.4 BIND/SEQUENCE types;8.5.14;10.2.2.3 NOT YET REGISTERED.

### C1018: Bounds-spec list length equals the data pointer object rank

**Source:** 10.2.2.2 C1018; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** If a bounds-spec-list is specified, the number of bounds-specs shall equal the rank of
the data-pointer-object.

**Diagnostic obligation:** required.

**Facets:** `matching-bounds-spec-count`, `too-few-bounds-specs-rejected`, `too-many-bounds-specs-rejected`.

**Oracle:** The plan observes only source admission; lower-bound values and resulting bounds are
10.2.2.3 dependencies.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.17 rank;10.2.2.3 NOT YET REGISTERED;16.9.119 LBOUND where later used.

### C1019: Bounds-remapping list length equals the data pointer object rank

**Source:** 10.2.2.2 C1019; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** If a bounds-remapping-list is specified, the number of bounds-remappings shall equal the
rank of the data-pointer-object.

**Diagnostic obligation:** required.

**Facets:** `matching-bounds-remapping-count`, `too-few-bounds-remappings-rejected`, `too-many-bounds-remappings-rejected`.

**Oracle:** Keep target rank/contiguity/storage-size premises valid so only the count is varied.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.17 rank;9.5 arrays;10.2.2.3 NOT YET REGISTERED.

### C1020: Array lower and upper bounds expressions require a rank-sized rank-one array

**Source:** 10.2.2.2 C1020; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** If both lower-bounds-expr and upper-bounds-expr appear in a pointer-assignment-stmt, at
least one of them shall be a rank-one array of constant size equal to the rank of the
data-pointer-object.

**Diagnostic obligation:** required.

**Facets:** `lower-array-supplies-rank-control`, `upper-array-supplies-rank-control`, `neither-rank-one-array-rejected`, `wrong-constant-size-rejected`.

**Oracle:** This constraint admits the array-bound form; it does not define the resulting pointer
bounds.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 10.1.11 specification/constant-size expressions;8.5.17 rank;10.2.2.3 NOT YET REGISTERED.

### C1021: Lower-bounds-expr without upper-bounds-expr is a rank-sized rank-one array

**Source:** 10.2.2.2 C1021; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** If lower-bounds-expr appears in a pointer-assignment-stmt but upper-bounds-expr does
not, lower-bounds-expr shall be a rank-one array of constant size equal to the rank of
the data-pointer-object.

**Diagnostic obligation:** required.

**Facets:** `lower-array-rank-sized-control`, `lower-scalar-rejected`, `lower-wrong-size-rejected`.

**Oracle:** Use nondefault lower bounds to avoid a vacuous later bounds oracle; source admission
remains this constraint.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 10.1.11;8.5.17 rank;10.2.2.3 NOT YET REGISTERED.

### C1022: Without remapping or upper bounds, data pointer object and target ranks match

**Source:** 10.2.2.2 C1022; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** If neither a bounds-remapping-list nor an upper-bounds-expr appears in a
pointer-assignment-stmt, the ranks of the data-pointer-object and data-target shall be
the same.

**Diagnostic obligation:** required.

**Facets:** `same-rank-no-remap-control`, `rank-mismatch-rejected`, `remapping-rank-exception-source`, `upper-bounds-rank-exception-source`.

**Oracle:** The exception facets are source classification only; they do not assert remapping
correctness or resulting shape.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.17 rank;9.5 arrays;10.2.2.3 NOT YET REGISTERED.

### C1023: Coarray data targets and data pointer objects have matching VOLATILE status

**Source:** 10.2.2.2 C1023; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** For a coarray data-target, the target shall have the VOLATILE attribute if and only if
the data-pointer-object has the VOLATILE attribute.

**Diagnostic obligation:** required.

**Facets:** `both-volatile-control`, `both-nonvolatile-control`, `target-volatile-pointer-nonvolatile-rejected`, `pointer-volatile-target-nonvolatile-rejected`.

**Oracle:** Do not require multi-image execution for this source constraint; if future execution is
used, coarray launch capabilities and C1029 boundaries must be explicit.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.4 VOLATILE;8.5.6 coarrays;10.2.2.3 NOT YET REGISTERED;11 image execution NOT YET
REGISTERED.

### C1024: A variable-name data pointer object has POINTER

**Source:** 10.2.2.2 C1024; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** When the data-pointer-object route is a variable-name, that variable-name shall have the
POINTER attribute.

**Diagnostic obligation:** required.

**Facets:** `pointer-variable-name-control`, `nonpointer-variable-name-rejected`.

**Oracle:** TARGET and POINTER remain distinct; this rule applies to the pointer object, not to the
target.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.14 POINTER;8.5.18 TARGET;9.2 variable.

### C1025: The scalar variable before a data pointer component is a data-ref

**Source:** 10.2.2.2 C1025; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** For the data pointer component route, the scalar-variable before the percent sign shall
be a data-ref.

**Diagnostic obligation:** required.

**Facets:** `data-ref-base-control`, `non-data-ref-base-rejected`.

**Oracle:** Component selection validity and scalar status are kept fixed; this rule is not evidence
for target association effects.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 9.1 designator/data-ref;9.4.2 structure component.

### C1026: The component name in a data pointer object names a data pointer component

**Source:** 10.2.2.2 C1026; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** A data-pointer-component-name in a data-pointer-object shall name a component of the
scalar-variable that is a data pointer.

**Diagnostic obligation:** required.

**Facets:** `data-pointer-component-control`, `nonpointer-component-rejected`, `absent-component-rejected`.

**Oracle:** Do not mix this with procedure-pointer components; those belong to R1040/C1031.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 7.5.4 components;8.5.14 POINTER;9.4.2 structure components.

### C1027: A data pointer object is not coindexed

**Source:** 10.2.2.2 C1027; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** The data-pointer-object in a pointer assignment statement shall not be a coindexed
object.

**Diagnostic obligation:** required.

**Facets:** `local-data-pointer-object-control`, `coindexed-data-pointer-object-rejected`.

**Oracle:** This is a source restriction on the pointer object. Remote target consequences and
undefined association examples are owned elsewhere.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 9.4.3 coindexed named objects;9.6 image selectors;10.2.2.3 NOT YET REGISTERED;11 images
NOT YET REGISTERED.

### R1036: A bounds specification is a lower-bound expression followed by a colon

**Source:** 10.2.2.2 R1036; J3/24-007 PDF p194. **Class:** Syntax.

**Definition:** A bounds-spec consists of a lower-bound-expr followed by a colon.

**Diagnostic obligation:** required.

**Facets:** `lower-bound-expr-colon-form`.

**Oracle:** This grammar does not define the value or resulting lower bound.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 10.1.2.9 expr;10.2.2.3 NOT YET REGISTERED.

### R1037: A bounds remapping has lower and upper bound expressions

**Source:** 10.2.2.2 R1037; J3/24-007 PDF p194. **Class:** Syntax.

**Definition:** A bounds-remapping consists of a lower-bound-expr, a colon, and an upper-bound-expr.

**Diagnostic obligation:** required.

**Facets:** `lower-upper-bound-expr-form`.

**Oracle:** This grammar does not by itself assert contiguity, element count, or resulting bounds.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 10.1.2.9 expr;9.5 arrays;10.2.2.3 NOT YET REGISTERED.

### R1038: A data target is an expression

**Source:** 10.2.2.2 R1038; J3/24-007 PDF p194. **Class:** Syntax.

**Definition:** The data-target syntactic class is expr; C1028 further restricts which expressions can
be data targets.

**Diagnostic obligation:** required.

**Facets:** `expr-target-form`.

**Oracle:** Do not treat arbitrary expressions as valid pointer targets; C1028 supplies the
target-expression restriction.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 10.1.2.9 expr;C1028 in this packet;8.5.18 TARGET;8.5.14 POINTER.

### C1028: A data target is an eligible designator or data-pointer function reference

**Source:** 10.2.2.2 C1028; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** The data-target expression shall either be a designator that designates a variable with
the TARGET or POINTER attribute and is not an array section with a vector subscript, or
a reference to a function that returns a data pointer.

**Diagnostic obligation:** required.

**Facets:** `target-variable-designator-control`, `pointer-variable-designator-control`, `data-pointer-function-result-control`, `non-target-non-pointer-designator-rejected`, `vector-subscript-section-rejected`, `non-designator-expression-rejected`, `nonpointer-function-result-rejected`.

**Oracle:** The target attribute, pointer attribute, vector-subscript, and function-result routes
must be isolated. A pointer-valued function result must have defined association status
before any observation.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.14 POINTER;8.5.18 TARGET;9.1 designator;9.5.3.4.3 vector subscript;15.6.2.2 function
results NOT YET REGISTERED;10.2.2.3 NOT YET REGISTERED;19.5.2 pointer status NOT YET
REGISTERED.

### C1029: A data target is not coindexed

**Source:** 10.2.2.2 C1029; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** A data-target in a pointer assignment statement shall not be a coindexed object.

**Diagnostic obligation:** required.

**Facets:** `local-data-target-control`, `coindexed-data-target-rejected`.

**Oracle:** This prevents direct remote data-target pointer assignment in this syntax; do not use it
to test remote component examples from the note as requirements.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 9.4.3 coindexed objects;9.6 image selectors;11 images NOT YET REGISTERED;10.2.2.3 NOT
YET REGISTERED.

### R1039: A procedure pointer object is a procedure pointer name or procedure component reference

**Source:** 10.2.2.2 R1039; J3/24-007 PDF p194. **Class:** Syntax.

**Definition:** A proc-pointer-object is either a proc-pointer-name or a proc-component-ref.

**Diagnostic obligation:** required.

**Facets:** `proc-pointer-name-form`, `proc-component-ref-form`.

**Oracle:** This production does not itself prove EXTERNAL, procedure characteristics, or
association effects.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.14 C855;15 procedure pointers NOT YET REGISTERED;10.2.2.4 NOT YET REGISTERED.

### R1040: A procedure component reference is scalar-variable percent procedure-component-name

**Source:** 10.2.2.2 R1040; J3/24-007 PDF p194. **Class:** Syntax.

**Definition:** A proc-component-ref has the form scalar-variable percent procedure-component-name.

**Diagnostic obligation:** required.

**Facets:** `scalar-variable-procedure-component-form`.

**Oracle:** Procedure-component existence and coindexed exclusion are constraints, not grammar
effects.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 7.5.4 components;9.4.2;15 procedure pointer components NOT YET REGISTERED.

### C1030: The procedure component base is a noncoindexed data-ref

**Source:** 10.2.2.2 C1030; J3/24-007 PDF p194. **Class:** Restriction.

**Definition:** In a proc-component-ref, the scalar-variable shall be a data-ref that is not a coindexed
object.

**Diagnostic obligation:** required.

**Facets:** `data-ref-base-control`, `non-data-ref-base-rejected`, `coindexed-base-rejected`.

**Oracle:** Do not combine the coindexed-base violation with an ineligible procedure target.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 9.1 data-ref;9.4.3 coindexed objects;9.6 image selectors;15 procedure components NOT YET
REGISTERED.

### C1031: The procedure component name identifies a procedure pointer component of the declared type

**Source:** 10.2.2.2 C1031; J3/24-007 PDF p195. **Class:** Restriction.

**Definition:** In a proc-component-ref, procedure-component-name shall name a procedure pointer
component of the declared type of the scalar-variable.

**Diagnostic obligation:** required.

**Facets:** `declared-type-procedure-pointer-component-control`, `nonprocedure-pointer-component-rejected`, `not-declared-type-component-rejected`.

**Oracle:** The declared-type requirement must not be satisfied solely by a dynamic extension
component.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 7.5.4 components;15 procedure pointer components NOT YET REGISTERED;9.4.2 structure
components.

### R1041: A procedure target is an expression, procedure name, or procedure component reference

**Source:** 10.2.2.2 R1041; J3/24-007 PDF p195. **Class:** Syntax.

**Definition:** A proc-target is an expr, a procedure-name, or a proc-component-ref.

**Diagnostic obligation:** required.

**Facets:** `expr-proc-target-form`, `procedure-name-target-form`, `proc-component-ref-target-form`.

**Oracle:** This grammar does not by itself make arbitrary expressions or procedure names eligible
procedure targets.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 10.1.2.9 expr;15 procedures NOT YET REGISTERED;10.2.2.4 NOT YET REGISTERED.

### C1032: A procedure-target expression is a function reference returning a procedure pointer

**Source:** 10.2.2.2 C1032; J3/24-007 PDF p195. **Class:** Restriction.

**Definition:** When proc-target is an expr, the expr shall be a reference to a function whose result is
a procedure pointer.

**Diagnostic obligation:** required.

**Facets:** `procedure-pointer-function-reference-control`, `nonfunction-expression-rejected`, `function-non-procedure-pointer-result-rejected`.

**Oracle:** A data-pointer function result does not satisfy this procedure-pointer rule.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 15.6.2.2 function results NOT YET REGISTERED;8.5.14 POINTER;10.2.2.4 NOT YET REGISTERED.

### C1033: A procedure-name target is one of the listed procedure entities

**Source:** 10.2.2.2 C1033; J3/24-007 PDF p195. **Class:** Restriction.

**Definition:** A procedure-name proc-target shall name an internal, module, or dummy procedure; a
procedure pointer; a specific intrinsic function listed in Table 16.2; or an external
procedure made accessible or established by one of the listed routes.

**Diagnostic obligation:** required.

**Facets:** `internal-procedure-control`, `module-procedure-control`, `dummy-procedure-control`, `procedure-pointer-control`, `specific-intrinsic-table16-control`, `external-use-or-host-associated-control`, `external-referenced-as-procedure-control`, `external-attribute-control`, `ineligible-procedure-name-rejected`.

**Oracle:** Procedure characteristics and explicit-interface details are dependencies. The plan must
distinguish a specific intrinsic from a generic intrinsic name.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 8.5.9 EXTERNAL;8.5.14 procedure pointers;15.4 interfaces/15.5 procedure references NOT
YET REGISTERED;15.6 procedures NOT YET REGISTERED;16.2 Table 16.2 NOT YET
REGISTERED;10.2.2.4 NOT YET REGISTERED.

### C1034: The procedure target is not a nonintrinsic elemental procedure

**Source:** 10.2.2.2 C1034; J3/24-007 PDF p195. **Class:** Restriction.

**Definition:** A proc-target in a pointer assignment statement shall not be a nonintrinsic elemental
procedure.

**Diagnostic obligation:** required.

**Facets:** `nonintrinsic-nonelemental-control`, `intrinsic-specific-control`, `nonintrinsic-elemental-rejected`.

**Oracle:** Do not conflate this with characteristic mismatch; the procedure target must otherwise
satisfy C1033 and 10.2.2.4 premises.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived source relations, literal values, inquiry
results, or reached-control-flow sentinels only after all pointer association statuses
and targets are defined for that inquiry. They must not rely on compiler agreement,
diagnostic wording, addresses, descriptor contents, emitted code, undefined association
status, undefined target values, resource exhaustion, or processor-dependent
representations.

**Dependencies:** 15.8/15.9 elemental procedures NOT YET REGISTERED;16 intrinsics NOT YET
REGISTERED;10.2.2.4 NOT YET REGISTERED.

<!-- END GENERATED 10.2.2.2 -->
