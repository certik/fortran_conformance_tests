# Fortran 2023 16.9.2: ABS intrinsic

The canonical catalogue is `doc/catalogues/abs_intrinsic_16_9_2.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.2 -->

### S16.9.2-001: ABS arguments are integer, real, or complex

**Source:** 16.9.2 p3; J3/24-007, 18 December 2023, physical PDF376. **Class:** Restriction.

**Definition:** The actual argument A to ABS shall be of type integer, real, or complex.

**Diagnostic obligation:** not-required.

**Facets:** `abs-argument-integer-real-or-complex`.

**Oracle:** Argument admissibility is source-control metadata only until a future fixture binds
valid calls; no diagnostic is claimed.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument restrictions in 16.9 intrinsic procedure descriptions are program restrictions
but are not numbered constraints and therefore carry no local diagnostic facet.
Floating-point approximation results are not compared for exact value unless the source
fixes an exact integer, logical, or character result; future fixtures must establish
non-default sentinels before observing effects.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, and exact
character/numeric observation where used. Unregistered dependencies explicitly retained:
later 16.9 intrinsic procedure specifications, 16.8 generic resolution, clause 17 IEEE
support where flags are tested, and clause 19 scoping/association terminology where
reached.

### S16.9.2-002: ABS result characteristics follow the argument with complex magnitude real

**Source:** 16.9.2 p4; J3/24-007, 18 December 2023, physical PDF376. **Class:** Effect.

**Definition:** The ABS result has the same characteristics as A except that a complex A produces a real
result.

**Diagnostic obligation:** not-required.

**Facets:** `abs-integer-result-characteristics`, `abs-real-result-characteristics`, `abs-complex-result-real-characteristics`.

**Oracle:** Type and kind observations use compile-time or exact intrinsic inquiries, not
approximation-sensitive magnitude comparisons.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument restrictions in 16.9 intrinsic procedure descriptions are program restrictions
but are not numbered constraints and therefore carry no local diagnostic facet.
Floating-point approximation results are not compared for exact value unless the source
fixes an exact integer, logical, or character result; future fixtures must establish
non-default sentinels before observing effects.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, and exact
character/numeric observation where used. Unregistered dependencies explicitly retained:
later 16.9 intrinsic procedure specifications, 16.8 generic resolution, clause 17 IEEE
support where flags are tested, and clause 19 scoping/association terminology where
reached.

### S16.9.2-003: ABS returns exact absolute value for integer and real arguments

**Source:** 16.9.2 p5 first clause; J3/24-007, 18 December 2023, physical PDF376. **Class:** Effect.

**Definition:** If A is of type integer or real, the value of the ABS result is the absolute value of A.

**Diagnostic obligation:** not-required.

**Facets:** `abs-integer-negative-to-positive`, `abs-integer-nonnegative-unchanged`, `abs-real-exact-sign-removal`.

**Oracle:** Integer and selected exactly representable real controls provide portable exact results
and mutate the intrinsic/operator so the facet is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument restrictions in 16.9 intrinsic procedure descriptions are program restrictions
but are not numbered constraints and therefore carry no local diagnostic facet.
Floating-point approximation results are not compared for exact value unless the source
fixes an exact integer, logical, or character result; future fixtures must establish
non-default sentinels before observing effects.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, and exact
character/numeric observation where used. Unregistered dependencies explicitly retained:
later 16.9 intrinsic procedure specifications, 16.8 generic resolution, clause 17 IEEE
support where flags are tested, and clause 19 scoping/association terminology where
reached.

### S16.9.2-004: ABS complex magnitude approximation is processor dependent

**Source:** 16.9.2 p5 complex approximation; J3/24-007, 18 December 2023, physical PDF376. **Class:** Effect.

**Definition:** For complex A with value (x,y), ABS returns a processor-dependent approximation to
sqrt(x**2+y**2).

**Diagnostic obligation:** not-required.

**Facets:** `abs-complex-magnitude-processor-dependent-approximation`.

**Oracle:** The source explicitly makes the approximation processor dependent, so exact floating
comparison would over-constrain a conforming processor.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument restrictions in 16.9 intrinsic procedure descriptions are program restrictions
but are not numbered constraints and therefore carry no local diagnostic facet.
Floating-point approximation results are not compared for exact value unless the source
fixes an exact integer, logical, or character result; future fixtures must establish
non-default sentinels before observing effects.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, and exact
character/numeric observation where used. Unregistered dependencies explicitly retained:
later 16.9 intrinsic procedure specifications, 16.8 generic resolution, clause 17 IEEE
support where flags are tested, and clause 19 scoping/association terminology where
reached.

### S16.9.2-005: ABS complex magnitude is computed without undue overflow or underflow

**Source:** 16.9.2 p5 final phrase; J3/24-007, 18 December 2023, physical PDF376. **Class:** Effect.

**Definition:** For complex A, the processor-dependent magnitude approximation is computed without undue
overflow or underflow.

**Diagnostic obligation:** not-required.

**Facets:** `abs-complex-magnitude-avoids-undue-overflow-underflow`.

**Oracle:** The obligation is recorded, but without a portable threshold for 'undue' a single-image
test cannot distinguish every conforming implementation from a nonconforming one.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument restrictions in 16.9 intrinsic procedure descriptions are program restrictions
but are not numbered constraints and therefore carry no local diagnostic facet.
Floating-point approximation results are not compared for exact value unless the source
fixes an exact integer, logical, or character result; future fixtures must establish
non-default sentinels before observing effects.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form,
type/kind concepts, declarations, expressions, execution, I/O harnesses, and exact
character/numeric observation where used. Unregistered dependencies explicitly retained:
later 16.9 intrinsic procedure specifications, 16.8 generic resolution, clause 17 IEEE
support where flags are tested, and clause 19 scoping/association terminology where
reached.

<!-- END GENERATED 16.9.2 -->
