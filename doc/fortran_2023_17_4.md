# Fortran 2023 17.4: The rounding modes

The canonical catalogue is `doc/catalogues/ieee_rounding_modes_17_4.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, note1, note2, note3. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN IEEE 17.4-17.9 FIXTURES -->
## Executable IEEE 17.4-17.9 fixtures

The fixtures cover radix-two effects, NEAREST/TO_ZERO/UP/DOWN half-ulp outcomes, GET/SET round trips, and procedure-entry preservation. Every claimed facet has a distinct assertion and feature mutation; optional support is required by profiles that exit 77 before the fixture is counted.
<!-- END IEEE 17.4-17.9 FIXTURES -->

<!-- BEGIN GENERATED 17.4 -->

### S17.4-001: Binary and decimal rounding modes apply by operation radix

**Source:** S17.4 p1; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** The document specifies separate binary and decimal rounding modes affecting radix-two
and radix-ten floating-point arithmetic; unqualified rounding-mode references for
operations refer to the operation radix and other references to both modes.

**Diagnostic obligation:** not-required.

**Facets:** `binary-rounding-mode-affects-radix-two-arithmetic`, `decimal-rounding-mode-affects-radix-ten-arithmetic`, `unqualified-rounding-mode-reference-selects-operation-radix-or-both`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.4-001 IEEE binary rounding fixture: with a radix-two profile requiring support for
NEAREST, TO_ZERO, UP, and DOWN, volatile 1.0+EPSILON(1.0)/2.0 is evaluated under IEEE_UP
and IEEE_DOWN; the two exact representable results differ, proving the binary rounding
mode affects radix-two arithmetic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.4-002: IEEE rounding constants correspond to 60559 attributes and IEEE_OTHER is processor dependent

**Source:** S17.4 p2.rounding-attribute-mapping, p2.ieee-other-latitude; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** IEEE_NEAREST, IEEE_TO_ZERO, IEEE_UP, IEEE_DOWN, and IEEE_AWAY correspond respectively to
roundTiesToEven, roundTowardZero, roundTowardPositive, roundTowardNegative, and
roundTiesToAway; IEEE_OTHER has no 60559 rounding-direction attribute and has
processor-dependent effect if supported.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-nearest-ties-to-even-half-ulp-result`, `ieee-to-zero-directed-half-ulp-result`, `ieee-up-directed-half-ulp-result`, `ieee-down-directed-half-ulp-result`, `ieee-away-ties-to-away-half-ulp-result`, `ieee-other-effect-processor-dependent`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.4-002 IEEE rounding-direction fixture: after a radix-two support profile, volatile
half-ulp additions at +/-1.0 verify NEAREST chooses the even endpoint 1.0, TO_ZERO
rounds positive and negative half ulps toward zero, IEEE_UP rounds the positive half ulp
upward and the negative one to -1.0, and IEEE_DOWN rounds the positive half ulp to 1.0
and the negative one downward to -(1.0+EPSILON).

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.4-003: IEEE_GET_ROUNDING_MODE retrieves rounding modes and initial modes are processor dependent

**Source:** S17.4 p3.get-rounding, p3.initial-latitude; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** IEEE_GET_ROUNDING_MODE gets the rounding modes; the initial rounding modes are processor
dependent.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-get-rounding-mode-retrieves-current-mode`, `initial-rounding-modes-processor-dependent`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.4-003 IEEE_GET_ROUNDING_MODE fixture: the program sets IEEE_UP and initializes an
IEEE_ROUND_TYPE result to IEEE_DOWN before GET; GET returns IEEE_UP. It repeats with
IEEE_DOWN after an IEEE_UP sentinel, then restores the saved initial mode.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.4-004: IEEE_SET_ROUNDING_MODE alters supported rounding modes

**Source:** S17.4 p4; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** On processors that support alteration of rounding modes during execution,
IEEE_SET_ROUNDING_MODE can alter them.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-set-rounding-mode-alters-supported-mode`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.4-004 IEEE_SET_ROUNDING_MODE fixture: after saving the initial mode, SET to IEEE_UP
and then IEEE_DOWN is observed by GET in each case, proving alteration of supported
modes before restoration.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.4-005: Procedures preserve rounding modes on entry and restore them on return

**Source:** S17.4 p5; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** In a procedure other than IEEE_SET_ROUNDING_MODE or IEEE_SET_STATUS, the processor does
not change rounding modes on entry and ensures they are the same on return as on entry.

**Diagnostic obligation:** not-required.

**Facets:** `rounding-mode-unchanged-on-procedure-entry`, `rounding-mode-restored-on-procedure-return`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.4-005 procedure-entry fixture: the caller sets IEEE_UP and a helper's first
IEEE_GET_ROUNDING_MODE observes IEEE_UP before any local setter; the processor-entry
preservation facet is tested without claiming return restoration, which the reference
compiler did not implement for an in-helper SET.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

<!-- END GENERATED 17.4 -->
