# Fortran 2023 13.7.2.4: B, O, and Z editing

The canonical catalogue is `doc/catalogues/b_o_and_z_editing_13_7_2_4.json`. Effective review state comes from `Registry.catalogue_review_state("13.7.2.4")`; the original source packet recorded source accounting and pending plans, while this fixture packet adds unreviewed executable fixtures without granting fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Eight base units including numbered item R1323. The pre-existing-fixture check for R1323 found no tests, so it is registered; real/complex bit-sequence output/input remains processor-representation-sensitive.

<!-- BEGIN INTEGER BOZ EDITING FIXTURES -->
## Executable BOZ editing fixtures

This packet adds ten complete internal-file programs for selected 13.7.2.4 BOZ editing facets. The fixtures use nonnegative default integers with hand-derived binary, octal, and hexadecimal strings, avoid real/complex representation latitude, and include a permanent B4.4/O4.4/Z4.4 descriptor-substitution matrix. Every output oracle checks LEN before character equality, so no case can pass by blank-padding a shorter expected literal.
<!-- END INTEGER BOZ EDITING FIXTURES -->

<!-- BEGIN GENERATED 13.7.2.4 -->

### S13.7.2.4-001: B/O/Z descriptors edit listed types and prohibit zero input width

**Source:** S13.7.2.4, J3/24-007, 18 December 2023, physical PDF301. **Class:** Effect.

**Definition:** B w, B w.m, O w, O w.m, Z w, and Z w.m occupy w positions except w=0 selects output
width; w shall not be zero on input; effective item shall be integer, real, complex,
enum type, or enumeration type.

**Diagnostic obligation:** not-required.

**Facets:** `BOZ-field-width`, `BOZ-input-width-zero-prohibited`, `BOZ-effective-item-types`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.4-001 BOZ-width runtime fixture: one complete program writes integer 5 with B5
and expects '  101', then writes the same value with B0 to a length-3 internal file and
expects '101'. Binary digits for 5 are 101; B5 right-justifies them in five positions,
while B0 selects the three-character width. Value and exact-string mutations fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

BOZ-editing fixture boundaries: this packet discharges only the selected integer BOZ
facets removed from pending above. It avoids real and complex BOZ bit-sequence results,
enum/enumeration constructors, B0/O0/Z0 input controls, unacceptable-field IOSTAT
behavior, m>w restrictions, zero m special cases, R1323 syntax-only coverage,
diagnostics, external files, list-directed I/O, and processor representation claims.
Expected strings are hand-derived from 13.7.2.4 for nonnegative default integers.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.4-002: The m component has no effect on B/O/Z input

**Source:** S13.7.2.4, J3/24-007, 18 December 2023, physical PDF301. **Class:** Effect.

**Definition:** On input using B, O, or Z editing, m has no effect.

**Diagnostic obligation:** not-required.

**Facets:** `BOZ-input-m-ignored`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.4-002 BOZ-input m runtime fixture: one complete program reads '101' with B3.3
and with B3 into separate integers and expects both results to be 5. The only format
difference is .m; field and integer-oracle mutations fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

BOZ-editing fixture boundaries: this packet discharges only the selected integer BOZ
facets removed from pending above. It avoids real and complex BOZ bit-sequence results,
enum/enumeration constructors, B0/O0/Z0 input controls, unacceptable-field IOSTAT
behavior, m>w restrictions, zero m special cases, R1323 syntax-only coverage,
diagnostics, external files, list-directed I/O, and processor representation claims.
Expected strings are hand-derived from 13.7.2.4 for nonnegative default integers.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.4-003: B/O/Z input standard digits, lowercase hex equivalence, and unacceptable-field error

**Source:** S13.7.2.4, J3/24-007, 18 December 2023, physical PDF301. **Class:** Effect.

**Definition:** Standard B/O/Z input consists of binary, octal, or hexadecimal digits respectively;
lowercase a through f equal uppercase in hexadecimal fields; unacceptable nonstandard
fields cause an error condition.

**Diagnostic obligation:** not-required.

**Facets:** `B-input-binary-digits`, `O-input-octal-digits`, `Z-input-hex-digits`, `Z-input-lowercase-equivalence`, `BOZ-input-nonstandard-error-condition`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.4-003 BOZ input digit runtime fixtures: four complete programs read '101' with
B3 as 5, '10' with O2 as 8, '0A' with Z2 as 10, and '0a' with Z2 as 10. The lowercase
case is compared to the same exact integer value as uppercase A. Each fixture mutates
the field and the expected integer.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

BOZ-editing fixture boundaries: this packet discharges only the selected integer BOZ
facets removed from pending above. It avoids real and complex BOZ bit-sequence results,
enum/enumeration constructors, B0/O0/Z0 input controls, unacceptable-field IOSTAT
behavior, m>w restrictions, zero m special cases, R1323 syntax-only coverage,
diagnostics, external files, list-directed I/O, and processor representation claims.
Expected strings are hand-derived from 13.7.2.4 for nonnegative default integers.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.4-004: B/O/Z input converts a matching bit sequence to the effective-item type

**Source:** S13.7.2.4, J3/24-007, 18 December 2023, physical PDF301. **Class:** Effect.

**Definition:** B/O/Z input produces INT(X) for integer, REAL(X) for real or complex, and ET(INT(X)) for
enum or enumeration type ET, where X is a BOZ literal constant specifying the same bit
sequence as the input digits.

**Diagnostic obligation:** not-required.

**Facets:** `BOZ-input-integer-INT`, `BOZ-input-real-REAL`, `BOZ-input-complex-REAL`, `BOZ-input-enum-enumeration-constructor`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.4-005: Bw/Ow/Zw output emits binary, octal, or hexadecimal digits for the internal bit sequence without leading zero bits

**Source:** S13.7.2.4, J3/24-007, 18 December 2023, physical PDF301. **Class:** Effect.

**Definition:** B w, O w, and Z w output fields contain leading blanks followed by the internal value in
binary, octal, or hexadecimal constant digit form for the same bit sequence, without
leading zero bits.

**Diagnostic obligation:** not-required.

**Facets:** `B-output-digits-no-leading-zero-bits`, `O-output-digits-no-leading-zero-bits`, `Z-output-digits-no-leading-zero-bits`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.4-005 BOZ output digit runtime fixtures: three complete programs write nonzero
default integers whose binary, octal, and hexadecimal constant digits are unambiguous.
B5 with 5 expects '  101'; O4 with 8 expects '  10'; Z3 with 10 expects '  A'. These are
the digit strings after removing leading zero bits and right-justifying in the requested
field width. Value, descriptor, and literal-oracle mutations fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

BOZ-editing fixture boundaries: this packet discharges only the selected integer BOZ
facets removed from pending above. It avoids real and complex BOZ bit-sequence results,
enum/enumeration constructors, B0/O0/Z0 input controls, unacceptable-field IOSTAT
behavior, m>w restrictions, zero m special cases, R1323 syntax-only coverage,
diagnostics, external files, list-directed I/O, and processor representation claims.
Expected strings are hand-derived from 13.7.2.4 for nonnegative default integers.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### R1323: Hex digit strings are nonempty sequences of hex digits

**Source:** R1323, J3/24-007, 18 December 2023, physical PDF302. **Class:** Syntax.

**Definition:** A hex-digit-string is a nonempty sequence of one or more hex digits.

**Diagnostic obligation:** not-required.

**Facets:** `single-hex-digit-string`, `multiple-hex-digit-string`.

**Oracle:** R1323 defines the output string form used by Z editing; it is not a source-program
syntax diagnostic in this packet. Pre-existing fixture search for R1323 returned no
matches.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.4-007: Bw.m/Ow.m/Zw.m output minimum digits, m<=w restriction, and all-zero special cases

**Source:** S13.7.2.4, J3/24-007, 18 December 2023, physical PDF301. **Class:** Effect.

**Definition:** B w.m, O w.m, and Z w.m output are like B/O/Z w but with at least m digits; leading
zeros are included as needed; m shall not exceed w except when w=0; if m=0 and the
internal value has all zero bits the field is all blanks, and when m=w=0 one blank is
produced.

**Diagnostic obligation:** not-required.

**Facets:** `BOZ-output-minimum-m-digits`, `BOZ-output-leading-zeros-to-m`, `BOZ-output-m-not-greater-than-w`, `BOZ-output-m0-zero-all-blanks`, `BOZ-output-m0-w0-zero-one-blank`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.4-007 BOZ w.m minimum-digit runtime fixture: one complete program writes 5 with
B4.4 expecting '0101', 8 with O4.4 expecting '0010', and 10 with Z4.4 expecting '000A'.
Each digit string is padded with leading zeros to four digits and right-justified in
width 4. The permanent descriptor substitution matrix replaces each of B4.4, O4.4, and
Z4.4 by the other two descriptors; every substitution must fail against the original
exact oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

BOZ-editing fixture boundaries: this packet discharges only the selected integer BOZ
facets removed from pending above. It avoids real and complex BOZ bit-sequence results,
enum/enumeration constructors, B0/O0/Z0 input controls, unacceptable-field IOSTAT
behavior, m>w restrictions, zero m special cases, R1323 syntax-only coverage,
diagnostics, external files, list-directed I/O, and processor representation claims.
Expected strings are hand-derived from 13.7.2.4 for nonnegative default integers.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

<!-- END GENERATED 13.7.2.4 -->
