# Fortran 2023: 10.1.2.8 Level-5 expressions

Catalogue: `doc/catalogues/level_5_expressions_10_1_2_8.json`. The original source-only registration supplied pending plans; the bounded batch304 fixtures below supply selected cases and mutation/diagnostic plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN EXPRESSIONS 10.1.2.8-10.1.3 FIXTURES -->
## Batch304 expression fixtures for 10.1.2.8

Generated batch304 fixtures cover selected facets for: S10.1.2.8-001, R1015, R1016, R1017, R1018, R1019, R1020, R1021, R1022. Runtime cases use exact integer, logical, character LEN/value, and derived-operation component oracles. Diagnostic cases are line-anchored one-property malformed forms with compiled conforming controls. Feature mutations are permanent and checked on both the reference gfortran and frozen LFortran target. Facets not listed remain pending for the recorded reasons.
<!-- END EXPRESSIONS 10.1.2.8-10.1.3 FIXTURES -->

<!-- BEGIN GENERATED 10.1.2.8 -->

### S10.1.2.8-001: Level-5 expressions are the logical-operator layers over level-4 expressions

**Source:** 10.1.2.8 p1; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** Level-5 expressions are level-4 expressions optionally involving .NOT., .AND., .OR.,
.EQV., and .NEQV. The grammar layers not before level-4, then and, then or, then
equivalence.

**Diagnostic obligation:** not-required.

**Facets:** `level-4-base`, `not-layer`, `and-layer`, `or-layer`, `equivalence-layer`.

**Oracle:** Future controls must use logical operands with literal truth tables and must not claim
evaluation short-circuit behavior beyond later evaluation rules.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 10.1.2.7R1013;10.1.3 Table 10.1;10.1.5.4;10.1.9.

### R1015: An and-operand is an optional .NOT. followed by a level-4 expression

**Source:** 10.1.2.8 R1015; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The and-operand syntax is an optional not-op followed by level-4-expr. A second
unparenthesized not-op is not admitted by this rule because the operand after not-op is
level-4, not another and-operand.

**Diagnostic obligation:** required.

**Facets:** `level-4-only`, `not-level-4`, `double-not-without-parentheses-rejected`, `not-before-level-5-rejected`.

**Oracle:** Negative plans must carefully avoid valid parses such as (.NOT. a) .AND. b; only forms
where the token after .NOT. cannot be level-4 are attributable to R1015.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;10.1.2.7R1013;10.1.2.8R1019;10.1.5.4.

### R1016: An or-operand is a left-recursive chain of and-operands joined by .AND.

**Source:** 10.1.2.8 R1016; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The or-operand syntax is either an and-operand or an or-operand followed by and-op and
and-operand, giving .AND. left-recursive grouping above .OR. and equivalence.

**Diagnostic obligation:** required.

**Facets:** `single-and-operand`, `and-chain`, `left-recursive-and`, `missing-right-and-operand-rejected`.

**Oracle:** Intrinsic .AND. controls use LOGICAL operands and literal truth values for admission and
truth-table checks only; they cannot observe grouping. The grouping facet can be
addressed only by a value-based non-associative defined extension on a derived type with
independently computed component oracles and valid defined-operation interfaces under
10.1.6 and 15.4.3.4.2. Type errors remain 10.1.5.4 dependencies, not R1016 syntax
evidence.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;10.1.2.8R1015/R1020;10.1.5.4;10.1.6;15.4.3.4.2.

### R1017: An equiv-operand is a left-recursive chain of or-operands joined by .OR.

**Source:** 10.1.2.8 R1017; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The equiv-operand syntax is either an or-operand or an equiv-operand followed by or-op
and or-operand, placing .OR. below .AND. and above .EQV./.NEQV. in the grammar.

**Diagnostic obligation:** required.

**Facets:** `single-or-operand`, `or-chain`, `and-before-or-precedence`, `missing-right-or-operand-rejected`.

**Oracle:** Future controls must not rely on short-circuit behavior; only final logical values with
literal operands are oracle material.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;10.1.2.8R1016/R1021;10.1.5.4.

### R1018: A level-5 expression is a left-recursive chain of equiv-operands joined by .EQV. or .NEQV.

**Source:** 10.1.2.8 R1018; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The level-5-expr syntax is either an equiv-operand or a level-5-expr followed by
equiv-op and equiv-operand, making equivalence operators the lowest intrinsic logical
layer and left-recursive.

**Diagnostic obligation:** required.

**Facets:** `single-equiv-operand`, `eqv-chain`, `neqv-chain`, `left-recursive-equiv`, `missing-right-equiv-operand-rejected`.

**Oracle:** Intrinsic LOGICAL controls can admit .EQV. and .NEQV. and check their truth tables, but
they cannot close the left-recursive grouping facet because intrinsic .EQV./.NEQV.
chains are value-associative in two-valued logic and 10.1.5.4.2 allows equivalent
evaluation subject to parentheses. A future grouping oracle must use a non-associative
defined extension on a derived type, independently computed component results for the
two parenthesisations, and valid defined-operation interfaces under 10.1.6 and
15.4.3.4.2; do not use side effects or operand evaluation paths.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;10.1.2.8R1017/R1022;10.1.5.4;10.1.6;15.4.3.4.2.

### R1019: The logical negation operator token is .NOT.

**Source:** 10.1.2.8 R1019; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The not-op token is .NOT.; operand placement is supplied by R1015.

**Diagnostic obligation:** required.

**Facets:** `dot-not-token`, `other-token-rejected`.

**Oracle:** Do not attribute defined unary operator spellings to R1019 unless the grammar slot
specifically requires not-op.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;6.2.4R608;10.1.2.8R1015;10.1.5.4.

### R1020: The logical conjunction operator token is .AND.

**Source:** 10.1.2.8 R1020; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The and-op token is .AND.; operand placement is supplied by R1016.

**Diagnostic obligation:** required.

**Facets:** `dot-and-token`, `other-token-rejected`.

**Oracle:** The negative must force the and-op position so a valid lower-precedence logical operator
is not misattributed.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;6.2.4R608;10.1.2.8R1016;10.1.5.4.

### R1021: The logical disjunction operator token is .OR.

**Source:** 10.1.2.8 R1021; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The or-op token is .OR.; operand placement is supplied by R1017.

**Diagnostic obligation:** required.

**Facets:** `dot-or-token`, `other-token-rejected`.

**Oracle:** Membership negatives need a forced or-op grammar slot; otherwise use the owning rule for
the operator actually parsed.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;6.2.4R608;10.1.2.8R1017;10.1.5.4.

### R1022: Logical equivalence operators are .EQV. and .NEQV.

**Source:** 10.1.2.8 R1022; J3/24-007 PDF p171. **Class:** Syntax.

**Definition:** The equiv-op alternatives are .EQV. and .NEQV.; operand placement is supplied by R1018.

**Diagnostic obligation:** required.

**Facets:** `dot-eqv-token`, `dot-neqv-token`, `other-token-rejected`.

**Oracle:** Token controls cite 10.1.5.4 for truth tables; R1022 only owns spellings.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 4.2;6.2.4R608;10.1.2.8R1018;10.1.5.4.

<!-- END GENERATED 10.1.2.8 -->
