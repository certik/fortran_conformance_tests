# Fortran 2023: 9.4.4 Complex parts

Catalogue: `doc/catalogues/complex_part_9_4_4.json`.
The original source-only registration supplied pending plans; the bounded runtime fixtures below supply cases and mutation plans, not oracle approval, fixture approval or universal coverage.

<!-- BEGIN COMPLEX PART EFFECTS -->
## Complex part runtime observations

Six complete run/effect/f2023 programs cover the selected S9.4.4-001 facets.
The real and imaginary cases define `z=(7.0,-2.0)` and compare `%RE` or
`%IM` directly with independent exact literals. The kind case uses
`COMPLEX(KIND(0.0D0))` and compares both part-designator kinds with
`KIND(z)`, never with a baked-in numeric kind. The scalar-shape case
assigns `z%RE` to a scalar REAL; the array-shape case checks
`SIZE(a%RE)==3` and `SUM(a%IM)==1.5` for three defined elements with
distinct real and imaginary parts. The defining-context case assigns
through `z%IM` and verifies the imaginary update and unchanged real part.

Every assertion prints a unique `CPE:` failure token and stops on mismatch,
increments a check counter, and is followed by a final check-total guard
before exact completion stdout, empty stderr and exit0. Wrong-oracle plans
cover every guard and completion literal. Omission plans delete observation
blocks, grouped observations where present, safe defining assignments and
the completion line. Mutations are complete-parent byte spans.

Only the six selected pending entries are removed. `result-real-type` remains
pending. The programs intentionally use no REAL, AIMAG, CMPLX, TRANSFER,
layout assumptions, undefined reads, tolerances, coarrays or multi-image I/O.
No evidence link, baseline update, SourceUse renewal or approval is created.
<!-- END COMPLEX PART EFFECTS -->

<!-- BEGIN GENERATED 9.4.4 -->

### R915: A complex part designator is a designator followed by percent RE or percent IM

**Source:** 9.4.4 R915, J3/24-007, 18 December 2023, physical PDF153. **Class:** Syntax.

**Definition:** The syntactic category complex-part-designator is either designator followed by percent
RE or designator followed by percent IM. The designator operand is the existing R901
designator category, and no additional component name, arbitrary suffix, or
intrinsic-function spelling is introduced by this rule.

**Diagnostic obligation:** required.

**Facets:** `percent-re-form`, `percent-im-form`, `designator-operand`, `other-suffix-rejected`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, runtime failure, or compiler agreement.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. Numbered syntax
rules and constraints carry the processor detection/reporting duty from 4.2, but these
pending plans do not require a fixed message, fatal rejection, nonzero status, or
printed rule number. Prose conditions and note text are not promoted to
numbered-constraint diagnostics. Case-insensitive source spelling may be accepted by
source-form/name rules; the facet is about the RE and IM alternatives, not letter case.

**Dependencies:** 4.1.3R401;4.2;6.2.2;7.4.3.3;9.1R901;9.2;9.4.4C922/p1;9.5.3.1;16.9.10;16.9.172.

### C922: The operand designator of a complex part designator is complex typed

**Source:** 9.4.4 C922, J3/24-007, 18 December 2023, physical PDF153. **Class:** Restriction.

**Definition:** For an R915 complex-part-designator, the designator before %RE or %IM shall be of
complex type. A real, integer, logical, character, or derived-type designator is not
made eligible merely by having a component-like suffix named RE or IM.

**Diagnostic obligation:** required.

**Facets:** `complex-scalar-designator`, `complex-array-designator`, `noncomplex-intrinsic-rejected`, `derived-type-component-boundary`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, runtime failure, or compiler agreement.

**Oracle limitation:** This is a source-only catalogue packet. It creates no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim. Numbered syntax
rules and constraints carry the processor detection/reporting duty from 4.2, but these
pending plans do not require a fixed message, fatal rejection, nonzero status, or
printed rule number. Prose conditions and note text are not promoted to
numbered-constraint diagnostics. Diagnostics must distinguish the C922 complex-type
predicate from ordinary component lookup failures when the left operand is derived type.

**Dependencies:** 4.2;7.4.1;7.4.3.3;7.5.1;9.1R901;9.4.2;9.4.4R915/p1;9.5.3.1.

### S9.4.4-001: Complex part designators select real or imaginary parts with real type and inherited kind and shape

**Source:** 9.4.4 p1, J3/24-007, 18 December 2023, physical PDF153. **Class:** Effect.

**Definition:** A complex-part-designator of the form designator%RE designates the real part of the
complex designator, and designator%IM designates its imaginary part. The complex part
designator has type real; its kind and shape are those of the original complex
designator, which may be an array or scalar. As an R901 designator it can participate in
broader designator or variable contexts only when the separate 9.2 and
definition-context rules allow that use; 9.4.4 itself does not turn constants or
nondefinable objects into variables.

**Diagnostic obligation:** not-required.

**Facets:** `real-part-selection`, `imaginary-part-selection`, `result-real-type`, `kind-inherited`, `scalar-shape-inherited`, `array-shape-inherited`, `defining-context-boundary`.

**Oracle:** Source-qualified pending plans pair each positive or diagnostic source shape with an
independent literal value, inquiry result, or single-token/single-attribute repair.
Future executable observations must first establish definedness and reached execution;
future diagnostics must identify the stated grammar or semantic predicate rather than an
unrelated parse, declaration, support, runtime failure, or compiler agreement.

S9.4.4-001 complex part runtime effects: six complete run/effect/f2023 programs observe
the selected p1 effects with actual %RE/%IM designators. Scalar real and imaginary
selection compare z%RE and z%IM from a defined COMPLEX z=(7.0,-2.0) against independent
literal 7.0 and -2.0 oracles, never REAL, AIMAG or CMPLX. The kind case declares
COMPLEX(KIND(0.0D0)), defines finite literal parts, and compares KIND(z%RE) and
KIND(z%IM) directly with KIND(z) without assuming a numeric kind value. The scalar-shape
case assigns z%RE to an ordinary scalar REAL and checks the literal selected value. The
array-shape case defines three explicit complex elements with distinct real and
imaginary parts, then checks SIZE(a%RE)==3 and SUM(a%IM)==1.5 against independent exact
literals. The defining-context case assigns literal 4.0 through z%IM, then checks that
z%IM is 4.0 and z%RE remains its original literal 3.0. Each assertion increments a
counter and a final total guard precedes exact completion stdout, empty stderr and
exit0. Wrong-oracle mutations cover every assertion and completion literal;
whole-program omissions remove observations, grouped observations, the defining
assignment where safe, or the completion line. Only a current passing complete parent
and the intended runtime failure establish mutation sensitivity.

**Oracle limitation:** The original source-only registration created no Fortran test program, execution,
evidence link, fixture approval, oracle approval, or coverage claim; this generator
supplies the six selected runtime fixtures and mutation plans without granting those
approvals or claims. Numbered syntax rules and constraints carry the processor
detection/reporting duty from 4.2, but these pending plans do not require a fixed
message, fatal rejection, nonzero status, or printed rule number. Prose conditions and
note text are not promoted to numbered-constraint diagnostics. REAL and AIMAG examples
in the note may be useful controls but are not independent mandatory owners for p1;
future tests must not rely on address layout, TRANSFER, emitted assembly, or undefined
storage to identify complex parts.

S9.4.4-001 complex part runtime boundaries: only real-part-selection,
imaginary-part-selection, kind-inherited, scalar-shape-inherited, array-shape-inherited
and defining-context-boundary are represented. The result-real-type facet remains
pending for diagnostic/control design. These cases do not use source rewrites, layout,
TRANSFER, address arithmetic, REAL, AIMAG, CMPLX, undefined values, epsilon tolerances,
coarrays, multi-image behavior, extra I/O, STOP codes or nonliteral value oracles. KIND
is used only for the kind facet; SIZE and SUM are used only for the array-shape facet.
The scalar-shape observation is a scalar-context witness paired with the separate array
source, not a general rank diagnostic. Guard markers are program output, not mandated
ERROR STOP wording or a universal numeric error status. Unsupported processors must
still fail honestly; no workaround weakens the Fortran source. Generation grants no
review approval, evidence link, baseline update, SourceUse renewal or universal
compiler-conformance credit.

**Dependencies:** 7.4.1p2;7.4.3.3p1-p5;9.1R901;9.2R902/C901/p1-p2;9.5.3.1R918/C925/p1;10.1.12;16.9.10
AIMAG;16.9.118 KIND;16.9.172 REAL;19.6.5;19.6.6.

<!-- END GENERATED 9.4.4 -->
