# Fortran 2023 16.9.142: MINEXPONENT

The canonical catalogue is `doc/catalogues/minexponent_16_9_142.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.142")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.142 -->

### S16.9.142-001: MINEXPONENT X type restriction

**Source:** S16.9.142, J3/24-007, 18 December 2023, physical PDF434. **Class:** Restriction.

**Definition:** X shall be real.

**Diagnostic obligation:** not-required.

**Facets:** `MINEXPONENT-X-real`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINEXPONENT occurrence and no
S16.9.142 binding. The e_min numeric value is processor model latitude, but relations
among model inquiries are portable and are planned, including EXPONENT(TINY(X)) equality
with MINEXPONENT(X).

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, TINY, EXPONENT, SHAPE, and relational operators, plus clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

### S16.9.142-002: MINEXPONENT admits scalar and array X

**Source:** S16.9.142, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** X may be a scalar or an array.

**Diagnostic obligation:** not-required.

**Facets:** `MINEXPONENT-X-scalar-admitted`, `MINEXPONENT-X-array-admitted`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINEXPONENT occurrence and no
S16.9.142 binding. The e_min numeric value is processor model latitude, but relations
among model inquiries are portable and are planned, including EXPONENT(TINY(X)) equality
with MINEXPONENT(X).

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, TINY, EXPONENT, SHAPE, and relational operators, plus clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

### S16.9.142-003: MINEXPONENT result is a default integer scalar

**Source:** S16.9.142, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** The MINEXPONENT result is a default integer scalar.

**Diagnostic obligation:** not-required.

**Facets:** `MINEXPONENT-result-default-integer`, `MINEXPONENT-result-scalar`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINEXPONENT occurrence and no
S16.9.142 binding. The e_min numeric value is processor model latitude, but relations
among model inquiries are portable and are planned, including EXPONENT(TINY(X)) equality
with MINEXPONENT(X).

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, TINY, EXPONENT, SHAPE, and relational operators, plus clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

### S16.9.142-004: MINEXPONENT returns the e_min model parameter for X kind

**Source:** S16.9.142, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** The MINEXPONENT result has the value e_min defined in 16.4 for the real model of X type
and kind.

**Diagnostic obligation:** not-required.

**Facets:** `MINEXPONENT-result-e-min-for-X-kind`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINEXPONENT occurrence and no
S16.9.142 binding. The e_min numeric value is processor model latitude, but relations
among model inquiries are portable and are planned, including EXPONENT(TINY(X)) equality
with MINEXPONENT(X).

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, TINY, EXPONENT, SHAPE, and relational operators, plus clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

<!-- END GENERATED 16.9.142 -->
