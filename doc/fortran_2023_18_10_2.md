# Fortran 2023 18.10.2: Binding labels for procedures

The canonical catalogue is `doc/catalogues/binding_labels_procedures_18_10_2.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

<!-- BEGIN GENERATED 18.10.2 -->

### S18.10.2-001: Procedure binding labels name BIND procedures to the companion processor

**Source:** S18.10.2 p1; J3/24-007, 18 December 2023, physical PDF546. **Class:** Effect.

**Definition:** The binding label of a procedure is a default character value specifying the name by
which a procedure with the BIND attribute is known to the companion processor.

**Diagnostic obligation:** not-required.

**Facets:** `procedure-binding-label-companion-name`.

**Oracle:** Plans are pending and source-derived. Mixed Fortran/C plans use the suite C interop
harness with separate C and Fortran sources. They guard every ISO_C_BINDING kind
constant with k > 0 before using it as a kind and observe only portable properties fixed
by Clause 18: exact integer or exactly representable real sentinel values, successful
linkage through required binding labels, Fortran ALLOCATED or ASSOCIATED status after
CFI calls, and required diagnostics for numbered constraints. Program obligations on C
descriptors, object lifetimes, setjmp/longjmp, signal handling, floating-point status,
and asynchronous communication are not treated as required diagnostics unless a numbered
constraint owns the rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Undefined behavior and processor-dependent behavior are not execution oracles. Invalid
source-control variants for unnumbered program obligations are retained only as future
design notes because a processor is not required to diagnose them.

**Dependencies:** Registered dependencies: clauses 6-12 for source form, declarations, execution, program
units, and existing I/O harnesses; clause 15 for procedure interfaces and BIND(C)
procedure characteristics; clause 16 ISO_C_BINDING and ISO_FORTRAN_ENV names; clause 17
IEEE status terminology. Unregistered dependencies explicitly retained: companion C
processor details, CFI descriptor function semantics from 18.5.5, global
identifier/linkage association from clause 19, and future mixed Fortran/C fixture
profiles.

### S18.10.2-002: NAME= gives a nonblank trimmed binding label for procedures

**Source:** S18.10.2 p2.name-specifier-trimmed-nonzero-label; J3/24-007, 18 December 2023, physical PDF546. **Class:** Effect.

**Definition:** For a procedure with BIND and a NAME= expression whose value has nonzero length after
discarding leading and trailing blanks, that trimmed value is the binding label.

**Diagnostic obligation:** not-required.

**Facets:** `procedure-name-specifier-trimmed-label`.

**Oracle:** Plans are pending and source-derived. Mixed Fortran/C plans use the suite C interop
harness with separate C and Fortran sources. They guard every ISO_C_BINDING kind
constant with k > 0 before using it as a kind and observe only portable properties fixed
by Clause 18: exact integer or exactly representable real sentinel values, successful
linkage through required binding labels, Fortran ALLOCATED or ASSOCIATED status after
CFI calls, and required diagnostics for numbered constraints. Program obligations on C
descriptors, object lifetimes, setjmp/longjmp, signal handling, floating-point status,
and asynchronous communication are not treated as required diagnostics unless a numbered
constraint owns the rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Undefined behavior and processor-dependent behavior are not execution oracles. Invalid
source-control variants for unnumbered program obligations are retained only as future
design notes because a processor is not required to diagnose them.

**Dependencies:** Registered dependencies: clauses 6-12 for source form, declarations, execution, program
units, and existing I/O harnesses; clause 15 for procedure interfaces and BIND(C)
procedure characteristics; clause 16 ISO_C_BINDING and ISO_FORTRAN_ENV names; clause 17
IEEE status terminology. Unregistered dependencies explicitly retained: companion C
processor details, CFI descriptor function semantics from 18.5.5, global
identifier/linkage association from clause 19, and future mixed Fortran/C fixture
profiles.

### S18.10.2-003: Procedure binding labels preserve case significance

**Source:** S18.10.2 p2.binding-label-case-significant; J3/24-007, 18 December 2023, physical PDF546. **Class:** Effect.

**Definition:** The case of letters in a procedure binding label is significant.

**Diagnostic obligation:** not-required.

**Facets:** `procedure-binding-label-case-significant`.

**Oracle:** Plans are pending and source-derived. Mixed Fortran/C plans use the suite C interop
harness with separate C and Fortran sources. They guard every ISO_C_BINDING kind
constant with k > 0 before using it as a kind and observe only portable properties fixed
by Clause 18: exact integer or exactly representable real sentinel values, successful
linkage through required binding labels, Fortran ALLOCATED or ASSOCIATED status after
CFI calls, and required diagnostics for numbered constraints. Program obligations on C
descriptors, object lifetimes, setjmp/longjmp, signal handling, floating-point status,
and asynchronous communication are not treated as required diagnostics unless a numbered
constraint owns the rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Undefined behavior and processor-dependent behavior are not execution oracles. Invalid
source-control variants for unnumbered program obligations are retained only as future
design notes because a processor is not required to diagnose them.

**Dependencies:** Registered dependencies: clauses 6-12 for source form, declarations, execution, program
units, and existing I/O harnesses; clause 15 for procedure interfaces and BIND(C)
procedure characteristics; clause 16 ISO_C_BINDING and ISO_FORTRAN_ENV names; clause 17
IEEE status terminology. Unregistered dependencies explicitly retained: companion C
processor details, CFI descriptor function semantics from 18.5.5, global
identifier/linkage association from clause 19, and future mixed Fortran/C fixture
profiles.

### S18.10.2-004: BIND procedures without NAME= use lower-case procedure names as labels for eligible procedure kinds

**Source:** S18.10.2 p2.default-lowercase-procedure-name-label; J3/24-007, 18 December 2023, physical PDF546. **Class:** Effect.

**Definition:** For a BIND procedure without NAME= that is not a dummy procedure, internal procedure, or
procedure pointer, the binding label is the procedure name converted to lower case
letters.

**Diagnostic obligation:** not-required.

**Facets:** `procedure-default-binding-label-lowercase-name`.

**Oracle:** Plans are pending and source-derived. Mixed Fortran/C plans use the suite C interop
harness with separate C and Fortran sources. They guard every ISO_C_BINDING kind
constant with k > 0 before using it as a kind and observe only portable properties fixed
by Clause 18: exact integer or exactly representable real sentinel values, successful
linkage through required binding labels, Fortran ALLOCATED or ASSOCIATED status after
CFI calls, and required diagnostics for numbered constraints. Program obligations on C
descriptors, object lifetimes, setjmp/longjmp, signal handling, floating-point status,
and asynchronous communication are not treated as required diagnostics unless a numbered
constraint owns the rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Undefined behavior and processor-dependent behavior are not execution oracles. Invalid
source-control variants for unnumbered program obligations are retained only as future
design notes because a processor is not required to diagnose them.

**Dependencies:** Registered dependencies: clauses 6-12 for source form, declarations, execution, program
units, and existing I/O harnesses; clause 15 for procedure interfaces and BIND(C)
procedure characteristics; clause 16 ISO_C_BINDING and ISO_FORTRAN_ENV names; clause 17
IEEE status terminology. Unregistered dependencies explicitly retained: companion C
processor details, CFI descriptor function semantics from 18.5.5, global
identifier/linkage association from clause 19, and future mixed Fortran/C fixture
profiles.

### S18.10.2-005: Procedures outside the BIND label cases have no binding label

**Source:** S18.10.2 p2.no-label-otherwise; J3/24-007, 18 December 2023, physical PDF546. **Class:** Effect.

**Definition:** A procedure has no binding label outside the nonblank NAME= case and the eligible
BIND-without-NAME= external procedure case.

**Diagnostic obligation:** not-required.

**Facets:** `procedure-no-binding-label-otherwise`.

**Oracle:** Plans are pending and source-derived. Mixed Fortran/C plans use the suite C interop
harness with separate C and Fortran sources. They guard every ISO_C_BINDING kind
constant with k > 0 before using it as a kind and observe only portable properties fixed
by Clause 18: exact integer or exactly representable real sentinel values, successful
linkage through required binding labels, Fortran ALLOCATED or ASSOCIATED status after
CFI calls, and required diagnostics for numbered constraints. Program obligations on C
descriptors, object lifetimes, setjmp/longjmp, signal handling, floating-point status,
and asynchronous communication are not treated as required diagnostics unless a numbered
constraint owns the rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Undefined behavior and processor-dependent behavior are not execution oracles. Invalid
source-control variants for unnumbered program obligations are retained only as future
design notes because a processor is not required to diagnose them.

**Dependencies:** Registered dependencies: clauses 6-12 for source form, declarations, execution, program
units, and existing I/O harnesses; clause 15 for procedure interfaces and BIND(C)
procedure characteristics; clause 16 ISO_C_BINDING and ISO_FORTRAN_ENV names; clause 17
IEEE status terminology. Unregistered dependencies explicitly retained: companion C
processor details, CFI descriptor function semantics from 18.5.5, global
identifier/linkage association from clause 19, and future mixed Fortran/C fixture
profiles.

### C1807: Submodule procedures with binding labels require an ancestor-module interface

**Source:** S18.10.2 C1807; J3/24-007, 18 December 2023, physical PDF546. **Class:** Restriction.

**Definition:** A procedure defined in a submodule shall not have a binding label unless its interface
is declared in the ancestor module.

**Diagnostic obligation:** required.

**Facets:** `submodule-binding-label-requires-ancestor-module-interface`.

**Oracle:** Plans are pending and source-derived. Mixed Fortran/C plans use the suite C interop
harness with separate C and Fortran sources. They guard every ISO_C_BINDING kind
constant with k > 0 before using it as a kind and observe only portable properties fixed
by Clause 18: exact integer or exactly representable real sentinel values, successful
linkage through required binding labels, Fortran ALLOCATED or ASSOCIATED status after
CFI calls, and required diagnostics for numbered constraints. Program obligations on C
descriptors, object lifetimes, setjmp/longjmp, signal handling, floating-point status,
and asynchronous communication are not treated as required diagnostics unless a numbered
constraint owns the rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Undefined behavior and processor-dependent behavior are not execution oracles. Invalid
source-control variants for unnumbered program obligations are retained only as future
design notes because a processor is not required to diagnose them.

**Dependencies:** Registered dependencies: clauses 6-12 for source form, declarations, execution, program
units, and existing I/O harnesses; clause 15 for procedure interfaces and BIND(C)
procedure characteristics; clause 16 ISO_C_BINDING and ISO_FORTRAN_ENV names; clause 17
IEEE status terminology. Unregistered dependencies explicitly retained: companion C
processor details, CFI descriptor function semantics from 18.5.5, global
identifier/linkage association from clause 19, and future mixed Fortran/C fixture
profiles.

### S18.10.2-006: C function binding labels are the C function names

**Source:** S18.10.2 p3; J3/24-007, 18 December 2023, physical PDF546. **Class:** Effect.

**Definition:** The binding label for a C function whose name has external linkage is the same as the C
function name.

**Diagnostic obligation:** not-required.

**Facets:** `c-function-binding-label-is-c-function-name`.

**Oracle:** Plans are pending and source-derived. Mixed Fortran/C plans use the suite C interop
harness with separate C and Fortran sources. They guard every ISO_C_BINDING kind
constant with k > 0 before using it as a kind and observe only portable properties fixed
by Clause 18: exact integer or exactly representable real sentinel values, successful
linkage through required binding labels, Fortran ALLOCATED or ASSOCIATED status after
CFI calls, and required diagnostics for numbered constraints. Program obligations on C
descriptors, object lifetimes, setjmp/longjmp, signal handling, floating-point status,
and asynchronous communication are not treated as required diagnostics unless a numbered
constraint owns the rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Undefined behavior and processor-dependent behavior are not execution oracles. Invalid
source-control variants for unnumbered program obligations are retained only as future
design notes because a processor is not required to diagnose them.

**Dependencies:** Registered dependencies: clauses 6-12 for source form, declarations, execution, program
units, and existing I/O harnesses; clause 15 for procedure interfaces and BIND(C)
procedure characteristics; clause 16 ISO_C_BINDING and ISO_FORTRAN_ENV names; clause 17
IEEE status terminology. Unregistered dependencies explicitly retained: companion C
processor details, CFI descriptor function semantics from 18.5.5, global
identifier/linkage association from clause 19, and future mixed Fortran/C fixture
profiles.

<!-- END GENERATED 18.10.2 -->
