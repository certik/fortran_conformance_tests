# Fortran 2023 16.9.67: CPU_TIME

The canonical catalogue is `doc/catalogues/cpu_time_16_9_67.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, note-unnumbered. Fine census units are recorded in the catalogue where mixed or dense source units are split.

<!-- BEGIN GENERATED 16.9.67 -->

### S16.9.67-001: CPU_TIME reports processor time used

**Source:** S16.9.67, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** CPU_TIME is the intrinsic for processor time used.

**Diagnostic obligation:** not-required.

**Facets:** `cpu-time-processor-time-description`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.67-002: CPU_TIME is a subroutine

**Source:** S16.9.67, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** CPU_TIME is classified as a subroutine.

**Diagnostic obligation:** not-required.

**Facets:** `cpu-time-subroutine-class`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.67-003: CPU_TIME TIME argument is a real scalar

**Source:** S16.9.67, J3/24-007, 18 December 2023, physical PDF400. **Class:** Restriction.

**Definition:** The TIME argument shall be a real scalar.

**Diagnostic obligation:** not-required.

**Facets:** `cpu-time-time-real-scalar`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.67-004: CPU_TIME TIME argument has INTENT(OUT)

**Source:** S16.9.67, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** The TIME argument has INTENT(OUT).

**Diagnostic obligation:** not-required.

**Facets:** `cpu-time-time-intent-out`.

**Oracle:** INTENT(OUT) is recorded as an argument-interface obligation; this source packet does not
claim a separate runtime value oracle for it.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.67-005: CPU_TIME assigns an unavailable-time sentinel or processor-time approximation

**Source:** S16.9.67, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** If meaningful time cannot be provided, TIME is assigned a processor-dependent negative
value; otherwise TIME is assigned a processor-dependent approximation to processor time
in seconds.

**Diagnostic obligation:** not-required.

**Facets:** `cpu-time-unavailable-assigns-negative`, `cpu-time-available-assigns-processor-time-approximation`.

**Oracle:** The assignment branches are requirements, but p3 supplies no portable exact value,
branch-forcing mechanism, or minimum-change guarantee that a single-image execution can
assert.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.67-006: CPU_TIME value details are processor dependent

**Source:** S16.9.67, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** The negative unavailable value, the approximation to time, and whether time is for the
invoking image or whole program are processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `cpu-time-negative-value-processor-dependent`, `cpu-time-approximation-processor-dependent`, `cpu-time-image-vs-program-time-out-of-scope`.

**Oracle:** Processor-dependent timing values are not portable conformance oracles; the
image-dependent facet is explicitly outside the single-image fixture scope.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

<!-- END GENERATED 16.9.67 -->
