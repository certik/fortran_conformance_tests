# Fortran 2023: 10.2.4.2.4 Execution of the FORALL body constructs

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 10.2.4.2.4 -->

### S10.2.4.2.4-001: FORALL body constructs execute in source order for all active index combinations

**Source:** 10.2.4.2.4 p1; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** The forall-body-constructs are executed in the order in which they appear. Each
construct is executed for all active combinations of the index-name values, subject to
the interpretations in p2-p6.

**Diagnostic obligation:** not-required.

**Facets:** `later-body-uses-earlier-body-result`, `inactive-combination-not-executed`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.4.2.1 stages in this packet;10.2.4.2.2/10.2.4.2.3 delegated active combinations in
this packet;10.2.1.3 assignment effects registered.

### S10.2.4.2.4-002: FORALL assignment statements evaluate RHS and variable expressions for every active combination before assignment

**Source:** 10.2.4.2.4 p2; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** Executing a forall-assignment-stmt that is an assignment-stmt evaluates expr and all
expressions within variable for all active combinations of index-name values; after all
evaluations have been performed, each expr value is assigned to the corresponding
variable.

**Diagnostic obligation:** not-required.

**Facets:** `rhs-snapshot-before-any-assignment`, `variable-subscript-snapshot-before-assignment`, `masked-inactive-sentinel-preserved`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.1.3 intrinsic assignment execution/effects registered;10.1.4 evaluation latitude
registered;10.2.4.4 many-to-one restriction in this packet.

### S10.2.4.2.4-003: FORALL assignment evaluations may be performed in any order

**Source:** 10.2.4.2.4 p2; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** The evaluations of expr and expressions within variable for active index-name
combinations may be done in any order.

**Diagnostic obligation:** not-required.

**Facets:** `evaluation-order-unobservable-source-control`.

**Oracle:** This is processor latitude, not a required observable behavior. It supports rejecting
order-sensitive oracles and cannot support a test that requires any particular order.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.1.4 evaluation latitude registered;15 pure procedures NOT YET REGISTERED.

### S10.2.4.2.4-004: FORALL assignment effects may occur in any order after all assignment evaluations complete

**Source:** 10.2.4.2.4 p2; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** After all assignment-statement evaluations have been performed for active combinations,
each expr value is assigned to the corresponding variable, and the assignments may occur
in any order.

**Diagnostic obligation:** not-required.

**Facets:** `assignment-order-unobservable-source-control`, `final-values-after-all-evaluations`.

**Oracle:** This permission records processor latitude in assignment order. The observable part is
the final state after all evaluations and assignments, assuming no many-to-one
violation.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.1.3 assignment effects registered;10.2.4.4 many-to-one restriction in this packet.

### S10.2.4.2.4-005: FORALL pointer-assignment statements evaluate pointer objects, targets, and targets for every active combination before association

**Source:** 10.2.4.2.4 p3; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** Executing a forall-assignment-stmt that is a pointer-assignment-stmt evaluates all
expressions within the target and pointer object, determines pointers within the pointer
object, and determines the target for all active combinations; after all evaluations
have been performed, each pointer object is associated with the corresponding target.

**Diagnostic obligation:** not-required.

**Facets:** `data-pointer-targets-determined-before-association`, `procedure-pointer-targets-determined-before-association`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.2 pointer assignment registered;15 procedure pointer/procedure characteristics NOT
YET REGISTERED;19.6 pointer association status NOT YET REGISTERED;10.2.4.4 many-to-one
restriction in this packet.

### S10.2.4.2.4-006: FORALL pointer-assignment evaluations may be performed in any order

**Source:** 10.2.4.2.4 p3; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** The evaluations and determinations for pointer assignment over active index-name
combinations may be done in any order.

**Diagnostic obligation:** not-required.

**Facets:** `pointer-evaluation-order-unobservable-source-control`.

**Oracle:** This is processor latitude, not an observable requirement. It only forbids
order-sensitive oracles.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.1.4 evaluation latitude registered;10.2.2 pointer assignment registered.

### S10.2.4.2.4-007: FORALL pointer associations may occur in any order after all pointer-assignment evaluations complete

**Source:** 10.2.4.2.4 p3; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** After all pointer-assignment evaluations and target determinations have been performed,
each data-pointer-object or proc-pointer-object is associated with the corresponding
target, and these associations may occur in any order.

**Diagnostic obligation:** not-required.

**Facets:** `association-order-unobservable-source-control`, `final-associations-after-determination`.

**Oracle:** This permission records latitude in association order. Only final association
relationships are potentially observable, with all pointer inquiries limited to defined
association status.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.2 pointer assignment registered;19.6 pointer association status NOT YET
REGISTERED;10.2.4.4 many-to-one restriction in this packet.

### S10.2.4.2.4-008: Defined assignment in a FORALL assignment statement must not reference a variable defined by that statement

**Source:** 10.2.4.2.4 p4; J3/24-007 PDF p201. **Class:** Restriction.

**Definition:** In a forall-assignment-stmt, a defined assignment subroutine shall not reference any
variable that becomes defined by the statement.

**Diagnostic obligation:** required.

**Facets:** `defined-assignment-no-reference-control`, `defined-assignment-references-defined-variable-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under the source
unit containing the prohibiting shall/shall-not text.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;10.2.1.4 defined assignment registered;15 procedure semantics
NOT YET REGISTERED.

### S10.2.4.2.4-009: WHERE constructs inside FORALL are sequenced and applied over active FORALL combinations and WHERE masks

**Source:** 10.2.4.2.4 p5; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** Each statement in a where-construct within a forall-construct executes in sequence.
WHERE, WHERE construct, and masked ELSEWHERE mask expressions are evaluated for all
active index-name combinations from the outer FORALL constructs, masked by applicable
outer WHERE control masks; WHERE assignment statements execute for all active index-name
combinations masked by the current WHERE control mask.

**Diagnostic obligation:** not-required.

**Facets:** `where-in-forall-statement-sequence`, `outer-forall-mask-limits-where-mask`, `where-control-mask-limits-assignments`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.3 WHERE registered;10.2.4.2.3 mask evaluation in this packet;10.2.1.3 assignment
effects registered.

### S10.2.4.2.4-010: Nested FORALL execution forms inner active combinations from each active outer combination

**Source:** 10.2.4.2.4 p6; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** Executing a nested FORALL statement or construct evaluates inner concurrent limits and
steps for all active outer index-name combinations; the inner combination set is the
union of those sets and includes outer index-name values. The inner scalar-mask-expr is
evaluated for all inner combinations to produce the inner active set, an absent mask
acts as true, and each inner statement executes for each active inner combination.

**Diagnostic obligation:** not-required.

**Facets:** `inner-limits-depend-on-outer-index`, `inner-combination-union-final-values`, `absent-inner-mask-is-true`, `inner-mask-selects-active-combinations`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 11.1.7 concurrent-header and DO CONCURRENT execution NOT YET REGISTERED;19.4 index names
NOT YET REGISTERED;10.2.4.4 nested same-index restriction in this packet.

<!-- END GENERATED 10.2.4.2.4 -->
