# Fortran 2023 16.9.90: FRACTION

The canonical catalogue is `doc/catalogues/fraction_16_9_90.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6.

Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN INTRINSICS 16.9.J FIXTURES 16.9.90 -->

## `intrinsics_16_9_j` executable fixture observations

This packet adds 7 generated valid/f2023 fixtures for 16.9.90, covering 8 facets with exact FINDLOC/FLOOR integer results, direct result-characteristic inquiries, FRACTION real-model power-of-radix checks, IEEE NaN-class checks, and GAMMA characteristic-only checks. 1 facet(s) remain pending where no portable oracle exists.
<!-- END INTRINSICS 16.9.J FIXTURES 16.9.90 -->

<!-- BEGIN GENERATED 16.9.90 -->

### S16.9.90-001: FRACTION returns the fractional part of a model number

**Source:** S16.9.90, J3/24-007, 18 December 2023, physical PDF412. **Class:** Effect.

**Definition:** FRACTION returns the fractional part of a real number as defined by the real model.

**Diagnostic obligation:** not-required.

**Facets:** `fraction-fractional-part-inquiry`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.90-001 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`fraction_inquiry` cover fraction-fractional-part-inquiry. p1 is observed through the
real-model fractional part of the exact power b**0: FRACTION(1.0) equals 1/RADIX for the
kind. The source uses direct intrinsic-expression inquiries for result characteristics
and exact integer, logical, character, shape, kind, IEEE NaN-class, or real-model
power-of-radix observations; processor-dependent approximations are not compared
exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.90-001 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.90-002: FRACTION is an elemental function

**Source:** S16.9.90, J3/24-007, 18 December 2023, physical PDF412. **Class:** Effect.

**Definition:** The intrinsic is classified as an elemental function.

**Diagnostic obligation:** not-required.

**Facets:** `fraction-elemental-function-class`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.90-002 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`fraction_elemental` cover fraction-elemental-function-class. p2 classifies FRACTION as
elemental; zero and powers of the radix give exact elementwise model values. The source
uses direct intrinsic-expression inquiries for result characteristics and exact integer,
logical, character, shape, kind, IEEE NaN-class, or real-model power-of-radix
observations; processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.90-002 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.90-003: FRACTION argument is real

**Source:** S16.9.90, J3/24-007, 18 December 2023, physical PDF412. **Class:** Restriction.

**Definition:** X shall be of type real.

**Diagnostic obligation:** not-required.

**Facets:** `fraction-x-real`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.90-003 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`fraction_argument_real` cover fraction-x-real. p3 is exercised by a selected-kind real
X; exact powers of the model radix keep the positive control portable. The source uses
direct intrinsic-expression inquiries for result characteristics and exact integer,
logical, character, shape, kind, IEEE NaN-class, or real-model power-of-radix
observations; processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.90-003 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.90-004: FRACTION result has the same type and kind as X

**Source:** S16.9.90, J3/24-007, 18 December 2023, physical PDF412. **Class:** Effect.

**Definition:** The result has the same type and kind as X.

**Diagnostic obligation:** not-required.

**Facets:** `fraction-result-same-kind-as-x`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.90-004 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`fraction_result_kind` cover fraction-result-same-kind-as-x. p4 says the result is same
as X; the fixture inquires KIND directly on the FRACTION expression. The source uses
direct intrinsic-expression inquiries for result characteristics and exact integer,
logical, character, shape, kind, IEEE NaN-class, or real-model power-of-radix
observations; processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.90-004 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.90-005: FRACTION finite nonzero result follows the extended real model

**Source:** S16.9.90, J3/24-007, 18 December 2023, physical PDF412. **Class:** Effect.

**Definition:** For finite nonzero X, the result is X times b to the negative e, where b and e are the
model radix and exponent for X.

**Diagnostic obligation:** not-required.

**Facets:** `fraction-finite-model-value`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.90-005 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`fraction_finite_model` cover fraction-finite-model-value. p5 finite nonzero rule gives
X*b**(-e); for exact powers of the model radix the result is exactly 1/RADIX for the
kind. The source uses direct intrinsic-expression inquiries for result characteristics
and exact integer, logical, character, shape, kind, IEEE NaN-class, or real-model
power-of-radix observations; processor-dependent approximations are not compared
exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.90-005 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.90-006: FRACTION of zero is zero

**Source:** S16.9.90, J3/24-007, 18 December 2023, physical PDF412. **Class:** Effect.

**Definition:** If X has the value zero, the result is zero.

**Diagnostic obligation:** not-required.

**Facets:** `fraction-zero-result-zero`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.90-006 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`fraction_zero` cover fraction-zero-result-zero. p5 separately fixes zero to zero; a
nonzero exact model companion prevents the all-zero branch from being the only
observation. The source uses direct intrinsic-expression inquiries for result
characteristics and exact integer, logical, character, shape, kind, IEEE NaN-class, or
real-model power-of-radix observations; processor-dependent approximations are not
compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.90-006 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.90-007: FRACTION maps IEEE NaN and infinity to IEEE NaN results

**Source:** S16.9.90, J3/24-007, 18 December 2023, physical PDF412. **Class:** Effect.

**Definition:** If X is an IEEE NaN, the result is that NaN, which provides a portable NaN-class
observation but no portable payload-identity oracle in this suite; if X is an IEEE
infinity, the result is an IEEE NaN.

**Diagnostic obligation:** not-required.

**Facets:** `fraction-ieee-nan-result-is-nan`, `fraction-ieee-nan-identity-no-portable-oracle`, `fraction-ieee-infinity-yields-nan`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.90-007 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`fraction_ieee_specials` cover fraction-ieee-nan-result-is-nan,
fraction-ieee-infinity-yields-nan. p5 fixes only NaN class for IEEE NaN and infinity
branches; the ieee-binary profile supplies the IEEE datatype support and no NaN payload
identity is asserted. The source uses direct intrinsic-expression inquiries for result
characteristics and exact integer, logical, character, shape, kind, IEEE NaN-class, or
real-model power-of-radix observations; processor-dependent approximations are not
compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.90-007 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

<!-- END GENERATED 16.9.90 -->
