# Fortran 2023 16.9.138: MAXVAL

The canonical catalogue is `doc/catalogues/maxval_intrinsic_16_9_138.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.138")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.138 -->

### S16.9.138-001: MAXVAL argument restrictions

**Source:** S16.9.138, J3/24-007, 18 December 2023, physical PDF432-433. **Class:** Restriction.

**Definition:** ARRAY shall be an integer, real, or character array; DIM shall be an integer scalar in
the range 1 through the rank of ARRAY; optional MASK shall be logical and conformable
with ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `MAXVAL-ARRAY-integer-real-or-character-array`, `MAXVAL-DIM-integer-scalar-in-range`, `MAXVAL-MASK-logical-conformable`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for p7
example matrix glyph extraction; catalogue source units retain the inventory hash.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

### S16.9.138-002: MAXVAL result type, parameters, rank, and shape

**Source:** S16.9.138, J3/24-007, 18 December 2023, physical PDF432-433. **Class:** Effect.

**Definition:** MAXVAL returns the same type and type parameters as ARRAY; absent DIM gives a scalar
result, and present DIM gives rank n-1 with the DIM extent removed.

**Diagnostic obligation:** not-required.

**Facets:** `MAXVAL-result-same-type-and-parameters`, `MAXVAL-no-DIM-scalar`, `MAXVAL-DIM-rank-and-shape`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for p7
example matrix glyph extraction; catalogue source units retain the inventory hash.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

### S16.9.138-003: MAXVAL whole-array result and zero-size defaults

**Source:** S16.9.138, J3/24-007, 18 December 2023, physical PDF432-433. **Class:** Effect.

**Definition:** MAXVAL of a nonzero-size ARRAY has the maximum element value; zero-size integer or real
ARRAY returns the negative number of the largest supported magnitude, and zero-size
character ARRAY returns LEN(ARRAY) characters each equal to CHAR(0,KIND(ARRAY)).

**Diagnostic obligation:** not-required.

**Facets:** `MAXVAL-nonzero-integer-maximum`, `MAXVAL-nonzero-exact-real-maximum`, `MAXVAL-zero-size-integer-negative-largest`, `MAXVAL-zero-size-real-negative-largest`, `MAXVAL-zero-size-character-CHAR-zero`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for p7
example matrix glyph extraction; catalogue source units retain the inventory hash.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

### S16.9.138-004: MAXVAL MASK and DIM results follow PACK and slice reductions

**Source:** S16.9.138, J3/24-007, 18 December 2023, physical PDF432-433. **Class:** Effect.

**Definition:** MAXVAL with MASK has the value of MAXVAL(PACK(ARRAY,MASK)); with DIM, rank-one ARRAY
gives the no-DIM result and higher-rank ARRAY reduces each DIM slice with the
corresponding MASK slice.

**Diagnostic obligation:** not-required.

**Facets:** `MAXVAL-MASK-equals-PACK-reduction`, `MAXVAL-DIM-rank-one-equals-no-DIM`, `MAXVAL-DIM-higher-rank-slice-results`, `MAXVAL-DIM-forwards-MASK`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for p7
example matrix glyph extraction; catalogue source units retain the inventory hash.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

### S16.9.138-005: MAXVAL character arrays use intrinsic relational ordering

**Source:** S16.9.138, J3/24-007, 18 December 2023, physical PDF432-433. **Class:** Effect.

**Definition:** For character ARRAY, MAXVAL uses the value selected by intrinsic relational operators
and the collating sequence for the character kind.

**Diagnostic obligation:** not-required.

**Facets:** `MAXVAL-default-character-letter-value`, `MAXVAL-default-character-digit-value`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations and processor-dependent models are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MASKR, MATMUL, MAX, MAXEXPONENT,
MAXLOC, and MAXVAL found no existing fixture metadata binding for these
16.9.133-16.9.138 sections in this worktree. Existing uses of MAX and MAXVAL are
unrelated clause 7/10 tests or clause 11 fixture sources; MASKR, MATMUL, MAXEXPONENT,
and MAXLOC had no test or fixture matches. Census hash drift was visually checked for p7
example matrix glyph extraction; catalogue source units retain the inventory hash.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context for
future bit helpers, 10.1.9.3 intrinsic operation result rules, 16.3 integer bit model
and bit manipulation context, 16.4 numeric model terminology, sibling 16.9 intrinsics
used by pending plans such as ANY, BIT_SIZE, BTEST, CHAR, HUGE, KIND, PACK, POPCNT, SUM,
and SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.138 -->
