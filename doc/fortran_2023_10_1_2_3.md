# Fortran 2023: 10.1.2.3 Conditional expressions

Catalogue: `doc/catalogues/conditional_expressions_10_1_2_3.json`. The original source-only registration supplied pending plans; the bounded batch291 fixtures below supply selected cases and mutation/diagnostic plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN EXPRESSIONS 10.1.1-10.1.2.4 FIXTURES -->
## Batch291 expression fixtures for 10.1.2.3

This section has generated batch291 fixtures for: S10.1.2.3-001, R1002, C1004. Runtime cases use exact integer, logical, character length/kind, rank, and shape oracles; diagnostic cases are line-anchored one-property malformed forms with conforming repairs documented in generator metadata. Feature mutations are permanent and checked on both the reference gfortran 16.1 and the frozen LFortran target. Facets not listed remain pending for the reasons recorded in each requirement.
<!-- END EXPRESSIONS 10.1.1-10.1.2.4 FIXTURES -->

<!-- BEGIN GENERATED 10.1.2.3 -->

### S10.1.2.3-001: A conditional expression is a primary selecting a chosen subexpression

**Source:** 10.1.2.3 p1; J3/24-007 PDF p169. **Class:** Effect.

**Definition:** A conditional expression is classified as a primary and selects a chosen subexpression.
The detailed conditional evaluation rules, including which guards or expressions are
evaluated, are dependencies rather than local fixture coverage.

**Diagnostic obligation:** not-required.

**Facets:** `primary-classification`, `true-arm-selection`, `false-arm-selection`, `nested-selection`.

**Oracle:** Future controls use distinct literal arm values and separately satisfy C1004. They must
not claim short-circuit evaluation coverage beyond the rules later catalogued for
10.1.4.

Batch291 expressions runtime/diagnostic fixtures: one run/effect/f2023 program uses
conditional expressions as primaries and side-effecting branch functions to show that
true, false, and chained selections evaluate exactly the chosen branch in the observed
single-image cases.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch291 expressions fixture boundaries: Only the listed selected facets are represented
by batch291 fixtures. Existing source review state, evidence approval, rollout progress,
xfail policy, and unrelated facets are not changed. Remaining pending facets are left
for later packets where this generator does not provide a portable, both-toolchain
parent/control.

**Dependencies:** 10.1.2.2R1001;10.1.2.9R1023;10.1.4 conditional evaluation;10.1.5 logical
operations;10.1.9 result characteristics;10.2.1.3.

### R1002: A conditional expression has parenthesized guarded arms and a final fallback expression

**Source:** 10.1.2.3 R1002; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The conditional-expr syntax is an opening parenthesis, a scalar-logical-expr guard,
question mark, an expr, zero or more ': scalar-logical-expr ? expr' repetitions, a
colon, a final expr, and a closing parenthesis.

**Diagnostic obligation:** required.

**Facets:** `two-arm-form`, `chained-guard-form`, `scalar-logical-guard`, `missing-final-colon-rejected`, `missing-question-rejected`.

**Oracle:** Syntax negatives must keep all expressions declared and C1004-compatible so the
malformed conditional punctuation or shape is the only cited defect.

Batch291 expressions runtime/diagnostic fixtures: three positive controls cover the
two-arm form, chained guarded form, and scalar logical guard, while two compile
diagnostic fixtures omit only the final colon or only the question mark. Repairs are the
corresponding positive forms.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch291 expressions fixture boundaries: Only the listed selected facets are represented
by batch291 fixtures. Existing source review state, evidence approval, rollout progress,
xfail policy, and unrelated facets are not changed. Remaining pending facets are left
for later packets where this generator does not provide a portable, both-toolchain
parent/control.

**Dependencies:** 4.2;7.4.5 logical literals;10.1.2.9R1023 expr;10.1.5.4 logical
interpretation;10.1.9;10.2.1.3.

### C1004: All conditional-expression result arms have the same declared type, kind parameters, and rank

**Source:** 10.1.2.3 C1004; J3/24-007 PDF p169. **Class:** Restriction.

**Definition:** Each expr result arm of a conditional-expr shall have the same declared type, the same
kind type parameters, and the same rank. The constraint applies to all selectable result
expressions, not to the scalar logical guards.

**Diagnostic obligation:** required.

**Facets:** `same-characteristics-control`, `declared-type-mismatch-rejected`, `kind-parameter-mismatch-rejected`, `rank-mismatch-rejected`, `nested-arm-characteristics`.

**Oracle:** Negative plans must use syntactically valid conditionals with scalar logical guards so
the reported defect is the arm characteristic mismatch, not malformed R1002 syntax or
invalid logical expressions.

Batch291 expressions runtime/diagnostic fixtures: two positive-control programs check
scalar INTEGER arms of the same type, kind, and rank plus a chained same-characteristics
form. Three diagnostics isolate declared-type, kind-parameter, and rank mismatches; the
rank negative changes only the false arm from scalar 2 to rank-one [2], and the [2] -> 2
repair runs on both compilers.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch291 expressions fixture boundaries: Only the listed selected facets are represented
by batch291 fixtures. Existing source review state, evidence approval, rollout progress,
xfail policy, and unrelated facets are not changed. Remaining pending facets are left
for later packets where this generator does not provide a portable, both-toolchain
parent/control. Array-valued conforming-arm conditionals remain outside this packet; the
shipped rank mismatch uses one scalar arm and one rank-one arm solely as a diagnostic
negative with scalar repair.

**Dependencies:** 4.2;7.2 type parameters;7.4 intrinsic types;10.1.2.3R1002;10.1.9;10.1.10 conformability.

<!-- END GENERATED 10.1.2.3 -->
