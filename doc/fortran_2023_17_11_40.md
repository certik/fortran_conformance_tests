# Fortran 2023 17.11.40: IEEE_SET_HALTING_MODE

The canonical catalogue is `doc/catalogues/ieee_set_halting_mode_17_11_40.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.40")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.40 -->

### S17.11.40-001: IEEE_SET_HALTING_MODE description defines the operation

**Source:** S17.11.40, J3/24-007, 18 December 2023, physical PDF503. **Class:** Effect.

**Definition:** IEEE_SET_HALTING_MODE is the IEEE_EXCEPTIONS simple subroutine that sets a halting mode.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SET_HALTING_MODE-operation-description`.

**Oracle:** Source registration only. Future fixtures use guarded IEEE capability branches: each
branch first probes the specific IEEE_SUPPORT_* inquiry required for every invoked IEEE
procedure, executes only the supported branch, and treats the unsupported branch as a
skip rather than a pass. Exact observations use concrete binary values, non-default
sentinels, IEEE_GET_* round trips, and IEEE_GET_FLAG after explicit flag clearing;
halting mechanics are not triggered.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SET_HALTING_MODE,
IEEE_SET_MODES, IEEE_SET_ROUNDING_MODE, IEEE_SET_STATUS, IEEE_SET_UNDERFLOW_MODE,
IEEE_SIGNALING_EQ, IEEE_SIGNALING_GE, IEEE_FLAG_TYPE, IEEE_MODES_TYPE, IEEE_ROUND_TYPE,
IEEE_STATUS_TYPE, IEEE_SUPPORT_HALTING, IEEE_SUPPORT_ROUNDING,
IEEE_SUPPORT_UNDERFLOW_CONTROL, and IEEE_SUPPORT_DATATYPE found no matches in this
worktree.

**Dependencies:** Registered dependencies retained: clauses 6 through 13 where already catalogued for
source form, intrinsic types, declarations, expressions, arrays, execution, data
transfer, and exact helper observations. Unregistered dependencies retained: clause 15
module procedure and dummy-argument association rules; clause 16 inquiry and model
intrinsics such as RADIX, KIND, SHAPE, and logical/integer helpers; earlier Clause 17
IEEE modules, derived IEEE types and constants, support inquiries, exception flags,
rounding, underflow, halting, and NaN value constructors; and clauses 18 and 19 where
deleted-feature or scope terminology is reached.

### S17.11.40-002: IEEE_SET_HALTING_MODE is a simple subroutine

**Source:** S17.11.40, J3/24-007, 18 December 2023, physical PDF503. **Class:** Effect.

**Definition:** IEEE_SET_HALTING_MODE has the simple subroutine class.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SET_HALTING_MODE-simple-subroutine-class`.

**Oracle:** Source registration only. Future fixtures use guarded IEEE capability branches: each
branch first probes the specific IEEE_SUPPORT_* inquiry required for every invoked IEEE
procedure, executes only the supported branch, and treats the unsupported branch as a
skip rather than a pass. Exact observations use concrete binary values, non-default
sentinels, IEEE_GET_* round trips, and IEEE_GET_FLAG after explicit flag clearing;
halting mechanics are not triggered.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SET_HALTING_MODE,
IEEE_SET_MODES, IEEE_SET_ROUNDING_MODE, IEEE_SET_STATUS, IEEE_SET_UNDERFLOW_MODE,
IEEE_SIGNALING_EQ, IEEE_SIGNALING_GE, IEEE_FLAG_TYPE, IEEE_MODES_TYPE, IEEE_ROUND_TYPE,
IEEE_STATUS_TYPE, IEEE_SUPPORT_HALTING, IEEE_SUPPORT_ROUNDING,
IEEE_SUPPORT_UNDERFLOW_CONTROL, and IEEE_SUPPORT_DATATYPE found no matches in this
worktree.

**Dependencies:** Registered dependencies retained: clauses 6 through 13 where already catalogued for
source form, intrinsic types, declarations, expressions, arrays, execution, data
transfer, and exact helper observations. Unregistered dependencies retained: clause 15
module procedure and dummy-argument association rules; clause 16 inquiry and model
intrinsics such as RADIX, KIND, SHAPE, and logical/integer helpers; earlier Clause 17
IEEE modules, derived IEEE types and constants, support inquiries, exception flags,
rounding, underflow, halting, and NaN value constructors; and clauses 18 and 19 where
deleted-feature or scope terminology is reached.

### S17.11.40-003: IEEE_SET_HALTING_MODE FLAG argument restrictions

**Source:** S17.11.40, J3/24-007, 18 December 2023, physical PDF503. **Class:** Restriction.

**Definition:** FLAG shall be a scalar or array of type IEEE_FLAG_TYPE, shall contain only IEEE_INVALID,
IEEE_OVERFLOW, IEEE_DIVIDE_BY_ZERO, IEEE_UNDERFLOW, or IEEE_INEXACT, and shall not
contain duplicate values.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SET_HALTING_MODE-FLAG-ieee-flag-type`, `IEEE_SET_HALTING_MODE-FLAG-allowed-values`, `IEEE_SET_HALTING_MODE-FLAG-no-duplicates`.

**Oracle:** Source registration only. Future fixtures use guarded IEEE capability branches: each
branch first probes the specific IEEE_SUPPORT_* inquiry required for every invoked IEEE
procedure, executes only the supported branch, and treats the unsupported branch as a
skip rather than a pass. Exact observations use concrete binary values, non-default
sentinels, IEEE_GET_* round trips, and IEEE_GET_FLAG after explicit flag clearing;
halting mechanics are not triggered.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SET_HALTING_MODE,
IEEE_SET_MODES, IEEE_SET_ROUNDING_MODE, IEEE_SET_STATUS, IEEE_SET_UNDERFLOW_MODE,
IEEE_SIGNALING_EQ, IEEE_SIGNALING_GE, IEEE_FLAG_TYPE, IEEE_MODES_TYPE, IEEE_ROUND_TYPE,
IEEE_STATUS_TYPE, IEEE_SUPPORT_HALTING, IEEE_SUPPORT_ROUNDING,
IEEE_SUPPORT_UNDERFLOW_CONTROL, and IEEE_SUPPORT_DATATYPE found no matches in this
worktree.

**Dependencies:** Registered dependencies retained: clauses 6 through 13 where already catalogued for
source form, intrinsic types, declarations, expressions, arrays, execution, data
transfer, and exact helper observations. Unregistered dependencies retained: clause 15
module procedure and dummy-argument association rules; clause 16 inquiry and model
intrinsics such as RADIX, KIND, SHAPE, and logical/integer helpers; earlier Clause 17
IEEE modules, derived IEEE types and constants, support inquiries, exception flags,
rounding, underflow, halting, and NaN value constructors; and clauses 18 and 19 where
deleted-feature or scope terminology is reached.

### S17.11.40-004: IEEE_SET_HALTING_MODE HALTING argument restrictions

**Source:** S17.11.40, J3/24-007, 18 December 2023, physical PDF503. **Class:** Restriction.

**Definition:** HALTING shall be a logical scalar or array conformable with FLAG.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SET_HALTING_MODE-HALTING-logical`, `IEEE_SET_HALTING_MODE-HALTING-conformable-with-FLAG`.

**Oracle:** Source registration only. Future fixtures use guarded IEEE capability branches: each
branch first probes the specific IEEE_SUPPORT_* inquiry required for every invoked IEEE
procedure, executes only the supported branch, and treats the unsupported branch as a
skip rather than a pass. Exact observations use concrete binary values, non-default
sentinels, IEEE_GET_* round trips, and IEEE_GET_FLAG after explicit flag clearing;
halting mechanics are not triggered.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SET_HALTING_MODE,
IEEE_SET_MODES, IEEE_SET_ROUNDING_MODE, IEEE_SET_STATUS, IEEE_SET_UNDERFLOW_MODE,
IEEE_SIGNALING_EQ, IEEE_SIGNALING_GE, IEEE_FLAG_TYPE, IEEE_MODES_TYPE, IEEE_ROUND_TYPE,
IEEE_STATUS_TYPE, IEEE_SUPPORT_HALTING, IEEE_SUPPORT_ROUNDING,
IEEE_SUPPORT_UNDERFLOW_CONTROL, and IEEE_SUPPORT_DATATYPE found no matches in this
worktree.

**Dependencies:** Registered dependencies retained: clauses 6 through 13 where already catalogued for
source form, intrinsic types, declarations, expressions, arrays, execution, data
transfer, and exact helper observations. Unregistered dependencies retained: clause 15
module procedure and dummy-argument association rules; clause 16 inquiry and model
intrinsics such as RADIX, KIND, SHAPE, and logical/integer helpers; earlier Clause 17
IEEE modules, derived IEEE types and constants, support inquiries, exception flags,
rounding, underflow, halting, and NaN value constructors; and clauses 18 and 19 where
deleted-feature or scope terminology is reached.

### S17.11.40-005: IEEE_SET_HALTING_MODE false halting mode continues execution

**Source:** S17.11.40, J3/24-007, 18 December 2023, physical PDF503. **Class:** Effect.

**Definition:** For a HALTING element with the value false, execution continues after the corresponding
exception instead of halting.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SET_HALTING_MODE-false-mode-round-trip`, `IEEE_SET_HALTING_MODE-true-halting-no-portable-run-oracle`.

**Oracle:** Source registration only. Future fixtures use guarded IEEE capability branches: each
branch first probes the specific IEEE_SUPPORT_* inquiry required for every invoked IEEE
procedure, executes only the supported branch, and treats the unsupported branch as a
skip rather than a pass. Exact observations use concrete binary values, non-default
sentinels, IEEE_GET_* round trips, and IEEE_GET_FLAG after explicit flag clearing;
halting mechanics are not triggered.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SET_HALTING_MODE,
IEEE_SET_MODES, IEEE_SET_ROUNDING_MODE, IEEE_SET_STATUS, IEEE_SET_UNDERFLOW_MODE,
IEEE_SIGNALING_EQ, IEEE_SIGNALING_GE, IEEE_FLAG_TYPE, IEEE_MODES_TYPE, IEEE_ROUND_TYPE,
IEEE_STATUS_TYPE, IEEE_SUPPORT_HALTING, IEEE_SUPPORT_ROUNDING,
IEEE_SUPPORT_UNDERFLOW_CONTROL, and IEEE_SUPPORT_DATATYPE found no matches in this
worktree.

**Dependencies:** Registered dependencies retained: clauses 6 through 13 where already catalogued for
source form, intrinsic types, declarations, expressions, arrays, execution, data
transfer, and exact helper observations. Unregistered dependencies retained: clause 15
module procedure and dummy-argument association rules; clause 16 inquiry and model
intrinsics such as RADIX, KIND, SHAPE, and logical/integer helpers; earlier Clause 17
IEEE modules, derived IEEE types and constants, support inquiries, exception flags,
rounding, underflow, halting, and NaN value constructors; and clauses 18 and 19 where
deleted-feature or scope terminology is reached.

### S17.11.40-006: IEEE_SET_HALTING_MODE requires halting support

**Source:** S17.11.40, J3/24-007, 18 December 2023, physical PDF503. **Class:** Restriction.

**Definition:** IEEE_SET_HALTING_MODE(FLAG, HALTING) shall not be invoked for a FLAG value whose
IEEE_SUPPORT_HALTING(FLAG) value is false.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SET_HALTING_MODE-requires-IEEE_SUPPORT_HALTING`.

**Oracle:** Source registration only. Future fixtures use guarded IEEE capability branches: each
branch first probes the specific IEEE_SUPPORT_* inquiry required for every invoked IEEE
procedure, executes only the supported branch, and treats the unsupported branch as a
skip rather than a pass. Exact observations use concrete binary values, non-default
sentinels, IEEE_GET_* round trips, and IEEE_GET_FLAG after explicit flag clearing;
halting mechanics are not triggered.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SET_HALTING_MODE,
IEEE_SET_MODES, IEEE_SET_ROUNDING_MODE, IEEE_SET_STATUS, IEEE_SET_UNDERFLOW_MODE,
IEEE_SIGNALING_EQ, IEEE_SIGNALING_GE, IEEE_FLAG_TYPE, IEEE_MODES_TYPE, IEEE_ROUND_TYPE,
IEEE_STATUS_TYPE, IEEE_SUPPORT_HALTING, IEEE_SUPPORT_ROUNDING,
IEEE_SUPPORT_UNDERFLOW_CONTROL, and IEEE_SUPPORT_DATATYPE found no matches in this
worktree.

**Dependencies:** Registered dependencies retained: clauses 6 through 13 where already catalogued for
source form, intrinsic types, declarations, expressions, arrays, execution, data
transfer, and exact helper observations. Unregistered dependencies retained: clause 15
module procedure and dummy-argument association rules; clause 16 inquiry and model
intrinsics such as RADIX, KIND, SHAPE, and logical/integer helpers; earlier Clause 17
IEEE modules, derived IEEE types and constants, support inquiries, exception flags,
rounding, underflow, halting, and NaN value constructors; and clauses 18 and 19 where
deleted-feature or scope terminology is reached.

<!-- END GENERATED 17.11.40 -->
