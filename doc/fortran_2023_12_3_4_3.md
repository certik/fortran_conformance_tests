# Fortran 2023 12.3.4.3: Positioning before data transfer - I/O positioning/internal fixtures

The canonical catalogue is `doc/catalogues/positioning_before_data_transfer_12_3_4_3.json`. This author packet adds finite runtime fixtures; fixture approval remains separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 5 facets in 3 rule-bound manifest(s); 5 of 15 facets are represented and 10 remain pending.

<!-- BEGIN GENERATED 12.3.4.3 -->

### S12.3.4.3-001: Pre-transfer file positioning depends on access method

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Effect.

**Definition:** Before data transfer, file positioning depends on whether access is sequential, direct,
or stream.

**Diagnostic obligation:** not-required.

**Facets:** `pretransfer-positioning-access-method-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.3-002: Sequential input keeps a current record or advances to the next record

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Effect.

**Definition:** For sequential input, an existing current record leaves the file position unchanged;
otherwise the file is positioned at the beginning of the next record and that record
becomes current.

**Diagnostic obligation:** not-required.

**Facets:** `sequential-input-current-record-position-unchanged`, `sequential-input-next-record-becomes-current`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.3-002 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`sequential-input-current-record-position-unchanged`,
`sequential-input-next-record-becomes-current`. These fixtures are finite single-image
positive runtime observations over scratch or local external files and character
internal files. They assert only exact integers, logical inequalities, exact characters
with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants, IOLENGTH=/POS=/SIZE=
values or inequalities derived from the executing processor, and FILE_STORAGE_SIZE
positivity. They do not assert physical byte encodings, record-marker layout, IOMSG
text, processor-specific numeric error statuses, unsupported deferred-length internal
WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.3-002 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.3-003: Sequential input shall not occur without a next record or after output on the current record

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Restriction.

**Definition:** For sequential input, input shall not occur if there is no next record or if there is a
current record and the last data transfer statement accessing the file performed output.

**Diagnostic obligation:** not-required.

**Facets:** `sequential-input-prohibited-with-no-next-record`, `sequential-input-prohibited-after-current-record-output`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.3-004: A file containing an endfile record shall not be positioned after it before data transfer

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Restriction.

**Definition:** If a file contains an endfile record, it shall not be positioned after the endfile
record prior to data transfer.

**Diagnostic obligation:** not-required.

**Facets:** `no-data-transfer-position-after-endfile-record`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.3-005: REWIND or BACKSPACE may reposition a file containing an endfile record

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Effect.

**Definition:** A REWIND or BACKSPACE statement may be used to reposition a file that contains an
endfile record.

**Diagnostic obligation:** not-required.

**Facets:** `rewind-may-reposition-before-endfile`, `backspace-may-reposition-before-endfile`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.3-006: Sequential output keeps or creates the current last record

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Effect.

**Definition:** For sequential output, an existing current record leaves the file position unchanged and
that record becomes the last record; otherwise a new next record is created, becomes the
last and current record, and the file is positioned at its beginning.

**Diagnostic obligation:** not-required.

**Facets:** `sequential-output-current-record-position-unchanged`, `sequential-output-current-record-becomes-last`, `sequential-output-creates-new-next-last-current-record`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.3-006 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`sequential-output-creates-new-next-last-current-record`,
`sequential-output-current-record-becomes-last`. These fixtures are finite single-image
positive runtime observations over scratch or local external files and character
internal files. They assert only exact integers, logical inequalities, exact characters
with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants, IOLENGTH=/POS=/SIZE=
values or inequalities derived from the executing processor, and FILE_STORAGE_SIZE
positivity. They do not assert physical byte encodings, record-marker layout, IOMSG
text, processor-specific numeric error statuses, unsupported deferred-length internal
WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.3-006 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.3-007: Direct access positions at the REC= record before data transfer

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Effect.

**Definition:** For direct access, the file is positioned at the beginning of the record specified by
the REC= specifier, and that record becomes current.

**Diagnostic obligation:** not-required.

**Facets:** `direct-access-position-at-rec-record`, `direct-access-rec-record-becomes-current`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.3-007 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`direct-access-position-at-rec-record`. These fixtures are finite single-image positive
runtime observations over scratch or local external files and character internal files.
They assert only exact integers, logical inequalities, exact characters with LEN checks,
IOSTAT zero, IOSTAT_END/EOR named constants, IOLENGTH=/POS=/SIZE= values or inequalities
derived from the executing processor, and FILE_STORAGE_SIZE positivity. They do not
assert physical byte encodings, record-marker layout, IOMSG text, processor-specific
numeric error statuses, unsupported deferred-length internal WRITE behavior, or
diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.3-007 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.3-008: Stream access positions before POS= storage unit or leaves position unchanged without POS=

**Source:** S12.3.4.3, J3/24-007, 18 December 2023, physical PDF246-247. **Class:** Effect.

**Definition:** For stream access, the file is positioned immediately before the file storage unit
specified by POS=; if no POS= is present, the file position is not changed.

**Diagnostic obligation:** not-required.

**Facets:** `stream-access-position-before-pos-unit`, `stream-access-no-pos-position-unchanged`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.3-008 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`stream-access-no-pos-position-unchanged`, `stream-access-position-before-pos-unit`.
These fixtures are finite single-image positive runtime observations over scratch or
local external files and character internal files. They assert only exact integers,
logical inequalities, exact characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR
named constants, IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing
processor, and FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings,
record-marker layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.3-008 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

<!-- END GENERATED 12.3.4.3 -->

<!-- BEGIN IO POSITIONING INTERNAL FIXTURES -->
## I/O positioning, file-storage-unit, and internal-file fixtures

This packet adds 12 runtime programs and 12 rule-bound manifests representing 21 selected facets in 12.3.4.2, 12.3.4.3, 12.3.4.4, 12.3.5, and 12.4. The programs use scratch or local files deleted by the program and character internal files. READ targets use nondefault sentinels and pre-READ guards; stream positions are derived with IOLENGTH=, POS=, and SIZE= inquiries; character equality checks first compare LEN. Deferred-length allocatable internal WRITE facets remain pending because the reference compiler does not validate them.
<!-- END IO POSITIONING INTERNAL FIXTURES -->

### `sequential_output_truncates_following` / `S12.3.4.3-006`

**Facets:** `sequential-output-current-record-becomes-last`, `sequential-output-creates-new-next-last-current-record`.

12.3.4.3 p4 makes sequential output create the next record as the last record when there is no current record after an input transfer; the previous following record is no longer read before EOF.

Feature/sentinel mutations: `new-output-wrong-value`, `output-appends-after-terminal`, `sentinel-existing-first-remove`, `sentinel-existing-first-expected`, `sentinel-existing-first-default`, `sentinel-final-first-remove`, `sentinel-final-first-expected`, `sentinel-final-first-default`, `sentinel-rewritten-next-remove`, `sentinel-rewritten-next-expected`, `sentinel-rewritten-next-default`, `sentinel-output-eof-remove`, `sentinel-output-eof-expected`, `sentinel-output-eof-default`.

### `direct_access_rec_positions` / `S12.3.4.3-007`

**Facets:** `direct-access-position-at-rec-record`.

12.3.4.3 p5 positions direct access at the REC= record and makes it current; reads of REC=3 and REC=1 recover only those written records.

Feature/sentinel mutations: `direct-selected-other-rec`, `direct-selected-written-value`, `sentinel-direct-selected-remove`, `sentinel-direct-selected-expected`, `sentinel-direct-selected-default`, `sentinel-direct-first-remove`, `sentinel-direct-first-expected`, `sentinel-direct-first-default`.

### `stream_pos_no_pos_storage` / `S12.3.4.3-008`

**Facets:** `stream-access-position-before-pos-unit`, `stream-access-no-pos-position-unchanged`.

12.3.4.3 p6 positions stream access by POS= or leaves it unchanged without POS=; 12.3.5 p1/p3 define file-storage-unit positions and forbid unformatted stream alignment restrictions.

Feature/sentinel mutations: `stream-pos-read-first`, `stream-no-pos-reset`, `stream-second-written-value`, `sentinel-stream-pos-second-remove`, `sentinel-stream-pos-second-expected`, `sentinel-stream-pos-second-default`, `sentinel-stream-pos-first-remove`, `sentinel-stream-pos-first-expected`, `sentinel-stream-pos-first-default`, `sentinel-stream-no-pos-remove`, `sentinel-stream-no-pos-expected`, `sentinel-stream-no-pos-default`, `sentinel-unaligned-integer-remove`, `sentinel-unaligned-integer-expected`, `sentinel-unaligned-integer-default`.
