# Fortran 2023 16.9.47: BIT_SIZE intrinsic

The canonical catalogue is `doc/catalogues/bit_size_intrinsic_16_9_47.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.47")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Six base units, p1-p6, plus two fine units splitting mixed p3 obligation/permission. PDF page 392 and next-boundary page 394 were checked.

Coverage grep: BIT_SIZE appears only as a guard in data_statement_s008_boz_domain/source.f90 and data_statement_s004_runtime_effects/source.f90; no fixture.json binding in this scope.

<!-- BEGIN INTRINSICS 16.9.E FIXTURES -->
## Intrinsics 16.9.E runtime observations

This batch adds 4 generated run/effect or positive-control fixtures for 16.9.47, covering 6 pending facets with exact logical, kind, rank, integer, character round-trip, bit-sequence, CEILING, and CMPLX component observations. Mutation metadata perturbs each fixture's load-bearing intrinsic expression and completion output; mutation checking compiles each mutant in a repository-local scratch directory and requires the mutant not to pass.
<!-- END INTRINSICS 16.9.E FIXTURES -->

<!-- BEGIN GENERATED 16.9.47 -->

### S16.9.47-001: BIT_SIZE argument is integer

**Source:** S16.9.47, J3/24-007, 18 December 2023, physical PDF392. **Class:** Restriction.

**Definition:** The I argument to BIT_SIZE shall be of type integer.

**Diagnostic obligation:** not-required.

**Facets:** `bit_size-i-integer`.

**Oracle:** Plans are pending and source-derived. Future fixtures initialize scalar result variables
or arrays to non-default, opposite, or otherwise load-bearing sentinels before calling
the intrinsic and assert only exact logical, integer, kind, scalar-rank, or bit-sequence
properties fixed by 16.9 and its 16.3/16.3.2 dependencies. BIT_SIZE model-length plans
are cross-checked with independent bit-shift semantics from ISHFT/SHIFTL rather than
accepting BIT_SIZE's value by itself.

S16.9.47-001 intrinsics_16_9_e fixture: BIT_SIZE accepts an integer argument and returns
a positive scalar model bit count for default integer.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Clause 16 argument shall-restrictions in these intrinsic sections are unnumbered program
restrictions, so this packet records no local diagnostic facet for wrong argument type,
argument class, or out-of-range POS; a future negative requires an independently
numbered rule requiring rejection. Negative-integer bit interpretation beyond the exact
source examples, BOZ interpretation outside the cited 7.7 bit-sequence rules, and
example-only text used without a forcing Result Value rule are not portable conformance
oracles.

S16.9.47-001 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, and I/O foundations from clauses 6 through 13 where already
present. Unregistered dependencies retained for future fixtures: 7.7 BOZ literal
interpretation, 16.3 integer bit model, 16.3.2 bit sequence comparison, sibling 16.9 bit
intrinsics such as IAND/IOR/IBSET/BTEST where a plan uses them, and clause 19
scoping/association terminology where reached.

### S16.9.47-002: BIT_SIZE permits scalar or array integer arguments

**Source:** S16.9.47, J3/24-007, 18 December 2023, physical PDF392. **Class:** Effect.

**Definition:** The I argument to BIT_SIZE may be a scalar or an array.

**Diagnostic obligation:** not-required.

**Facets:** `bit_size-scalar-argument-permitted`, `bit_size-array-argument-permitted`.

**Oracle:** Plans are pending and source-derived. Future fixtures initialize scalar result variables
or arrays to non-default, opposite, or otherwise load-bearing sentinels before calling
the intrinsic and assert only exact logical, integer, kind, scalar-rank, or bit-sequence
properties fixed by 16.9 and its 16.3/16.3.2 dependencies. BIT_SIZE model-length plans
are cross-checked with independent bit-shift semantics from ISHFT/SHIFTL rather than
accepting BIT_SIZE's value by itself.

S16.9.47-002 intrinsics_16_9_e fixture: BIT_SIZE permits both scalar and array integer
arguments; the array call is used in an expression whose rank is observed as scalar.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Clause 16 argument shall-restrictions in these intrinsic sections are unnumbered program
restrictions, so this packet records no local diagnostic facet for wrong argument type,
argument class, or out-of-range POS; a future negative requires an independently
numbered rule requiring rejection. Negative-integer bit interpretation beyond the exact
source examples, BOZ interpretation outside the cited 7.7 bit-sequence rules, and
example-only text used without a forcing Result Value rule are not portable conformance
oracles.

S16.9.47-002 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, and I/O foundations from clauses 6 through 13 where already
present. Unregistered dependencies retained for future fixtures: 7.7 BOZ literal
interpretation, 16.3 integer bit model, 16.3.2 bit sequence comparison, sibling 16.9 bit
intrinsics such as IAND/IOR/IBSET/BTEST where a plan uses them, and clause 19
scoping/association terminology where reached.

### S16.9.47-003: BIT_SIZE result is a scalar integer of the same kind as I

**Source:** S16.9.47, J3/24-007, 18 December 2023, physical PDF392. **Class:** Effect.

**Definition:** The BIT_SIZE result is scalar integer with the same kind type parameter as I.

**Diagnostic obligation:** not-required.

**Facets:** `bit_size-result-scalar`, `bit_size-result-integer-same-kind`.

**Oracle:** Plans are pending and source-derived. Future fixtures initialize scalar result variables
or arrays to non-default, opposite, or otherwise load-bearing sentinels before calling
the intrinsic and assert only exact logical, integer, kind, scalar-rank, or bit-sequence
properties fixed by 16.9 and its 16.3/16.3.2 dependencies. BIT_SIZE model-length plans
are cross-checked with independent bit-shift semantics from ISHFT/SHIFTL rather than
accepting BIT_SIZE's value by itself.

S16.9.47-003 intrinsics_16_9_e fixture: BIT_SIZE has scalar integer result
characteristics and the result kind is the kind of I, checked with a selected nondefault
integer kind.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Clause 16 argument shall-restrictions in these intrinsic sections are unnumbered program
restrictions, so this packet records no local diagnostic facet for wrong argument type,
argument class, or out-of-range POS; a future negative requires an independently
numbered rule requiring rejection. Negative-integer bit interpretation beyond the exact
source examples, BOZ interpretation outside the cited 7.7 bit-sequence rules, and
example-only text used without a forcing Result Value rule are not portable conformance
oracles.

S16.9.47-003 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, and I/O foundations from clauses 6 through 13 where already
present. Unregistered dependencies retained for future fixtures: 7.7 BOZ literal
interpretation, 16.3 integer bit model, 16.3.2 bit sequence comparison, sibling 16.9 bit
intrinsics such as IAND/IOR/IBSET/BTEST where a plan uses them, and clause 19
scoping/association terminology where reached.

### S16.9.47-004: BIT_SIZE returns the bit-model length z

**Source:** S16.9.47, J3/24-007, 18 December 2023, physical PDF392. **Class:** Effect.

**Definition:** The BIT_SIZE result is the value of the number of bits z of the integer bit model
defined for bit manipulation contexts in 16.3.

**Diagnostic obligation:** not-required.

**Facets:** `bit_size-result-model-z`.

**Oracle:** Plans are pending and source-derived. Future fixtures initialize scalar result variables
or arrays to non-default, opposite, or otherwise load-bearing sentinels before calling
the intrinsic and assert only exact logical, integer, kind, scalar-rank, or bit-sequence
properties fixed by 16.9 and its 16.3/16.3.2 dependencies. BIT_SIZE model-length plans
are cross-checked with independent bit-shift semantics from ISHFT/SHIFTL rather than
accepting BIT_SIZE's value by itself.

S16.9.47-004 intrinsics_16_9_e fixture: BIT_SIZE supplies the model bit count z: z-1 is
used as the leftmost valid bit position and BTEST observes that bit after IBSET sets it.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Clause 16 argument shall-restrictions in these intrinsic sections are unnumbered program
restrictions, so this packet records no local diagnostic facet for wrong argument type,
argument class, or out-of-range POS; a future negative requires an independently
numbered rule requiring rejection. Negative-integer bit interpretation beyond the exact
source examples, BOZ interpretation outside the cited 7.7 bit-sequence rules, and
example-only text used without a forcing Result Value rule are not portable conformance
oracles.

S16.9.47-004 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, and I/O foundations from clauses 6 through 13 where already
present. Unregistered dependencies retained for future fixtures: 7.7 BOZ literal
interpretation, 16.3 integer bit model, 16.3.2 bit sequence comparison, sibling 16.9 bit
intrinsics such as IAND/IOR/IBSET/BTEST where a plan uses them, and clause 19
scoping/association terminology where reached.

<!-- END GENERATED 16.9.47 -->
