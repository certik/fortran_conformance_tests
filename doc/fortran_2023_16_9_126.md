# Fortran 2023 16.9.126: LLE

The canonical catalogue is `doc/catalogues/lle_intrinsic_16_9_126.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.126")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, note-unnumbered, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN INTRINSICS 16.9.P FIXTURES 16.9.126 -->
## Intrinsics 16.9.P runtime observations

This batch adds 3 generated single-image runtime fixtures for 16.9.126, covering 7 facets with exact BIT_SIZE-relative LEADZ, LEN, LEN_TRIM, or ASCII-collating character comparison observations. Result characteristics are inquired directly on intrinsic expressions, and every discharged facet has a conforming feature mutation compiled and run by the mutation checker. Non-ASCII truth values remain pending because p5 makes them processor dependent.
<!-- END INTRINSICS 16.9.P FIXTURES 16.9.126 -->

<!-- BEGIN GENERATED 16.9.126 -->

### S16.9.126-001: LLE argument restrictions

**Source:** S16.9.126, J3/24-007, 18 December 2023, physical PDF427. **Class:** Restriction.

**Definition:** STRING_A shall be default character or ASCII character, and STRING_B shall be character
with the same kind type parameter as STRING_A.

**Diagnostic obligation:** not-required.

**Facets:** `LLE-STRING_A-default-or-ASCII-character`, `LLE-STRING_B-same-character-kind`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations are separated from exact result obligations.
Unnumbered argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

S16.9.126-001 intrinsics_16_9_p runtime fixture: LLE is exercised with default ASCII
STRING_A and same-kind character STRING_B operands.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for LLE, LLT, LOG, LOG_GAMMA, LOG10,
LOGICAL, and MASKL found no existing 16.9.126-16.9.132 fixture binding in this worktree;
LOGICAL appears only in unrelated logical-literal, logical-operator, and I/O fixture
names or output strings.

S16.9.126-001 intrinsics_16_9_p boundaries: This packet supplies only the named
single-image runtime/effect or positive-control fixtures. It does not claim diagnostics
for unnumbered argument restrictions, non-ASCII character truth values, coarray
behavior, compiler consensus, source-review approval, or unrelated facets.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context where
future bit helpers use BOZ controls, 16.3 integer bit model and bit manipulation
context, 16.4 numeric model terminology, sibling 16.9 intrinsics used by pending plans
such as AIMAG, ACOS, BIT_SIZE, BTEST, POPCNT, and SELECTED_*_KIND, and clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

### S16.9.126-002: LLE result has default logical characteristics

**Source:** S16.9.126, J3/24-007, 18 December 2023, physical PDF428. **Class:** Effect.

**Definition:** The LLE result is default logical.

**Diagnostic obligation:** not-required.

**Facets:** `LLE-result-default-logical`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations are separated from exact result obligations.
Unnumbered argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

S16.9.126-002 intrinsics_16_9_p runtime fixture: KIND is inquired directly on the LLE
expression and compared with default logical kind.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for LLE, LLT, LOG, LOG_GAMMA, LOG10,
LOGICAL, and MASKL found no existing 16.9.126-16.9.132 fixture binding in this worktree;
LOGICAL appears only in unrelated logical-literal, logical-operator, and I/O fixture
names or output strings.

S16.9.126-002 intrinsics_16_9_p boundaries: This packet supplies only the named
single-image runtime/effect or positive-control fixtures. It does not claim diagnostics
for unnumbered argument restrictions, non-ASCII character truth values, coarray
behavior, compiler consensus, source-review approval, or unrelated facets.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context where
future bit helpers use BOZ controls, 16.3 integer bit model and bit manipulation
context, 16.4 numeric model terminology, sibling 16.9 intrinsics used by pending plans
such as AIMAG, ACOS, BIT_SIZE, BTEST, POPCNT, and SELECTED_*_KIND, and clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

### S16.9.126-003: LLE uses blank padding and ASCII less-than-or-equal result semantics

**Source:** S16.9.126, J3/24-007, 18 December 2023, physical PDF428. **Class:** Effect.

**Definition:** For ASCII strings, unequal lengths are compared after right blank padding of the shorter
string; LLE is true for equal strings or for STRING_A preceding STRING_B in the ASCII
collating sequence, and false otherwise.

**Diagnostic obligation:** not-required.

**Facets:** `LLE-unequal-length-blank-padding`, `LLE-equal-strings-true`, `LLE-ascii-precedes-true`, `LLE-ascii-follows-false`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations are separated from exact result obligations.
Unnumbered argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

S16.9.126-003 intrinsics_16_9_p runtime fixture: LLE exact results use LEN-verified
blank padding, equality true, and ASCII-preceding pairs with reverse false companions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for LLE, LLT, LOG, LOG_GAMMA, LOG10,
LOGICAL, and MASKL found no existing 16.9.126-16.9.132 fixture binding in this worktree;
LOGICAL appears only in unrelated logical-literal, logical-operator, and I/O fixture
names or output strings.

S16.9.126-003 intrinsics_16_9_p boundaries: This packet supplies only the named
single-image runtime/effect or positive-control fixtures. It does not claim diagnostics
for unnumbered argument restrictions, non-ASCII character truth values, coarray
behavior, compiler consensus, source-review approval, or unrelated facets.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context where
future bit helpers use BOZ controls, 16.3 integer bit model and bit manipulation
context, 16.4 numeric model terminology, sibling 16.9 intrinsics used by pending plans
such as AIMAG, ACOS, BIT_SIZE, BTEST, POPCNT, and SELECTED_*_KIND, and clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

### S16.9.126-004: LLE non-ASCII comparison result is processor dependent

**Source:** S16.9.126, J3/24-007, 18 December 2023, physical PDF428. **Class:** Effect.

**Definition:** A LLE comparison result is processor dependent once either string contains a character
outside the ASCII character set.

**Diagnostic obligation:** not-required.

**Facets:** `LLE-non-ASCII-result-processor-dependent`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, or bit-pattern observations.
Processor-dependent approximations are separated from exact result obligations.
Unnumbered argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for LLE, LLT, LOG, LOG_GAMMA, LOG10,
LOGICAL, and MASKL found no existing 16.9.126-16.9.132 fixture binding in this worktree;
LOGICAL appears only in unrelated logical-literal, logical-operator, and I/O fixture
names or output strings.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.7 BOZ literal context where
future bit helpers use BOZ controls, 16.3 integer bit model and bit manipulation
context, 16.4 numeric model terminology, sibling 16.9 intrinsics used by pending plans
such as AIMAG, ACOS, BIT_SIZE, BTEST, POPCNT, and SELECTED_*_KIND, and clauses 14
through 19 where future fixtures need program, procedure, IEEE, deleted-feature, or
scope terms.

<!-- END GENERATED 16.9.126 -->
