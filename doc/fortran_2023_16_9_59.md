# Fortran 2023 16.9.59: COMMAND_ARGUMENT_COUNT

The canonical catalogue is `doc/catalogues/command_argument_count_16_9_59.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.59 -->

### S16.9.59-001: COMMAND_ARGUMENT_COUNT result is a default integer scalar

**Source:** S16.9.59, J3/24-007, 18 December 2023, physical PDF398. **Class:** Effect.

**Definition:** The result characteristics of COMMAND_ARGUMENT_COUNT are default integer scalar.

**Diagnostic obligation:** not-required.

**Facets:** `command_argument_count-default-integer-scalar`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found no tests/ or tests/fixtures/*/fixture.json binding
for the COMMAND_ARGUMENT_COUNT intrinsic.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.59-002: COMMAND_ARGUMENT_COUNT reports the number of available command arguments, with zero for none

**Source:** S16.9.59, J3/24-007, 18 December 2023, physical PDF398. **Class:** Effect.

**Definition:** The result value is the number of command arguments available; with no command
arguments, or no processor support for command arguments, the value is zero; a command
name, if the processor has one, is not counted as an argument.

**Diagnostic obligation:** not-required.

**Facets:** `command_argument_count-no-arguments-zero`, `command_argument_count-counts-available-arguments`, `command_argument_count-command-name-not-counted`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found no tests/ or tests/fixtures/*/fixture.json binding
for the COMMAND_ARGUMENT_COUNT intrinsic.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.59-003: COMMAND_ARGUMENT_COUNT command-argument support and command-name concept are processor latitude

**Source:** S16.9.59, J3/24-007, 18 December 2023, physical PDF398. **Class:** Effect.

**Definition:** The processor may lack command-argument support, and only processors that have a
command-name concept apply the command-name exclusion.

**Diagnostic obligation:** not-required.

**Facets:** `command_argument_count-support-may-be-absent`, `command_argument_count-command-name-concept-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found no tests/ or tests/fixtures/*/fixture.json binding
for the COMMAND_ARGUMENT_COUNT intrinsic.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

<!-- END GENERATED 16.9.59 -->
