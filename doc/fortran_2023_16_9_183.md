# Fortran 2023 16.9.183: SELECTED_REAL_KIND ([P, R, RADIX])

The canonical catalogue is `doc/catalogues/selected_real_kind_16_9_183.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7, p8, p9. Fine census units are recorded in the catalogue where mixed source units are split.

Catalogue source review: draft.

<!-- BEGIN GENERATED 16.9.183 -->

### S16.9.183-001: SELECTED_REAL_KIND description defines the intrinsic operation

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** SELECTED_REAL_KIND selects a real kind.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-operation-description`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-002: SELECTED_REAL_KIND is a transformational function

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** SELECTED_REAL_KIND has the transformational function class.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-transformational-function-class`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-003: SELECTED_REAL_KIND argument restrictions

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Restriction.

**Definition:** At least one argument shall be present; P, R, and RADIX shall each be integer scalar
arguments.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-at-least-one-argument`, `SELECTED_REAL_KIND-P-integer-scalar`, `SELECTED_REAL_KIND-R-integer-scalar`, `SELECTED_REAL_KIND-RADIX-integer-scalar`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-004: SELECTED_REAL_KIND result is default integer scalar

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** The SELECTED_REAL_KIND result is a default integer scalar.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-result-default-integer-scalar`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-005: SELECTED_REAL_KIND absent P and R default to zero

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** Absent P and absent R are treated as present with value zero.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-P-absent-as-zero`, `SELECTED_REAL_KIND-R-absent-as-zero`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-006: SELECTED_REAL_KIND absent RADIX imposes no radix requirement

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** Absent RADIX imposes no requirement on the radix of the selected kind.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-RADIX-absent-no-requirement`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-007: SELECTED_REAL_KIND returns a kind meeting requested precision range and radix

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** For an available real kind, SELECTED_REAL_KIND returns a kind whose PRECISION, RANGE,
and RADIX meet the requested criteria.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-available-kind-criteria`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-008: SELECTED_REAL_KIND unavailable requests return distinct negative codes

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** Unavailable real-kind requests return -1 for unmet precision only, -2 for unmet exponent
range only, -3 for neither precision nor range met, -4 for precision and range each
available but not together, and -5 for no real type with the requested radix.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-unmet-precision-minus-one`, `SELECTED_REAL_KIND-unmet-range-minus-two`, `SELECTED_REAL_KIND-unmet-precision-and-range-minus-three`, `SELECTED_REAL_KIND-separate-precision-range-minus-four`, `SELECTED_REAL_KIND-unsupported-radix-minus-five`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

### S16.9.183-009: SELECTED_REAL_KIND applies smallest precision and kind tie rules

**Source:** S16.9.183, J3/24-007, 18 December 2023, physical PDF455. **Class:** Effect.

**Definition:** Among multiple real kind values meeting the criteria, SELECTED_REAL_KIND returns the one
with smallest decimal precision and then the smallest kind value.

**Diagnostic obligation:** not-required.

**Facets:** `SELECTED_REAL_KIND-smallest-decimal-precision-rule`, `SELECTED_REAL_KIND-smallest-kind-value-tiebreak`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, IEEE classification, and real-model inquiry observations. Real numerical values
are compared only for model-exact operations such as powers of the real radix, zero, or
standard inquiry relationships; processor-dependent approximations and processor kind
inventories are not assigned hard-coded expected values. Every positive plan initializes
an observed variable to a distinguished sentinel, includes a non-vacuous companion for
zero or default-valued results, and names a feature mutation such as the intrinsic name,
argument order, BACK, KIND, RADIX, BITS, R, P, or exponent value. Clause 16 unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep coverage: SELECTED_REAL_KIND appears in five tests/ files as helper/profile code
and zero fixture manifests, with no facet binding for this intrinsic. Assumption for
concrete -1, -2, and -3 unavailable-code plans: no supported real kind has decimal
precision or decimal exponent range greater than or equal to HUGE(0); this is an
explicit target-processor assumption, not a language guarantee.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, assignment, internal I/O, and exact formatting helpers. Unregistered
dependencies retained: clause 16.4 real-model definitions; sibling 16.9 inquiry and
elemental intrinsic procedures used by future fixture oracles; clause 17 IEEE support
for SET_EXPONENT infinity and NaN facets; clauses 3, 5, 14, 15, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, deleted features, or
scope rules.

<!-- END GENERATED 16.9.183 -->
