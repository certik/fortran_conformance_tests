# Fortran 2023 8.6.11: PARAMETER statement

Source review is recorded in batch075.
Effective source status is `Registry.catalogue_review_state("8.6.11")`;
source classification, fixture approval and compiler observations are
separate. This source-only packet introduces pending plans, not execution.

The canonical record is `doc/catalogues/parameter_statement_8_6_11.json`.
The seven original units occupy physical PDF139 of J3/24-007,
December18,2023,688pages,SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The next heading is8.6.12 POINTER statement on the same page.

R854 requires parentheses and a nonempty definition list; it has no
optional double-colon group. Each R855 definition has a bare R606 name,
an equals sign and a constant expression. It is not a pointer assignment,
an array-element definition or an executable variable assignment.
The PARAMETER statement supplies the attribute; a previous PARAMETER
attribute would not be a required control and can cause duplication.

P2 has two independent ordering conditions. A subsequent type/parameter
declaration is compared with the applicable implicit mapping, including
explicit IMPLICIT mappings. This does not require a legitimate prior
explicit LOGICAL n declaration to become INTEGER merely because of its
first letter. Intentional late-typing controls cannot use a null
IMPLICIT NONE(TYPE) mapping as a substitute. A named array's rank must
already be specified; implied extents do not infer a missing declared rank,
and C864 does not generally admit a RANK clause on a named constant.

P3 imports intrinsic-assignment type/parameter compatibility and keeps two
shape branches: implied shape needs equal expression rank; otherwise the
expression is scalar or has the same shape. Scalar expansion does not
legalize scalar initialization of an implied-shape array, arbitrary
reshaping or allocatable reallocation. Exact type tables, character-kind
and derived/enum/enumeration conditions retain their original owners.

P4 supplies the actual corresponding named-constant values with necessary
conversion. Its pending meaningful observations use independent INTEGER,
LOGICAL, default-CHARACTER, array-element and simple-derived values,
not an empty program or the initializer re-evaluated as its own oracle.
The conversion reference does not promise runtime allocation, finalization,
defined-assignment callback order or physical copying. Prior definition,
typing, constant-expression eligibility and compatibility remain prerequisites.

R605 includes typeless BOZ literals, but C7119 still restricts their consumer
contexts. Direct named INTEGER/REAL constant initialization is not a
general typed BOZ arithmetic expression or permission for arbitrary
receiver types. INTEGER capacity/high-bit interpretation and the existing
REAL representation/ordering bridge remain explicit independent gates.
No real-BOZ profile or NEEDS_ORACLE review is changed. REAL/COMPLEX
approximation and optional additional-kind observations need their own
qualified oracles, not compiler consensus or kind-number folklore.

The final note illustrates an intrinsic constant expression and a separate
literal definition. Its omitted surroundings do not impose one universal
typing order. All current implementation states remain in the catalogue.
No cases, compiler runs, profiles, reviews, baseline changes, canonical links
or SourceUses are created by this author packet.

<!-- BEGIN GENERATED 8.6.11 -->

### R854: A PARAMETER statement encloses a nonempty definition list in parentheses

**Source:** 8.6.11 R854, physical PDF139 **Class:** Syntax.

**Definition:** PARAMETER is followed by a parenthesized nonempty named-constant-def list. Commas
separate definitions under R401. This production does not contain an optional double
colon, a listless form, or an executable assignment statement.

**Diagnostic obligation:** required.

**Facets:** `single-definition-form`, `multiple-definition-form`, `empty-list-exclusion`, `parenthesis-and-colon-repairs`, `type-declaration-route-source`.

**Oracle:** Complete compile admissions and source-minimal grammar repairs with correct types,
unique attributes and valid constant expressions. Statement envelopes do not supply
value/conversion, prior-rank or namespace evidence by themselves.

**Oracle limitation:** Reporting capability does not mandate fatal rejection, fixed English or a printed R854.
Missing types, duplicate attributes, a nonconstant expression, inappropriate entity
role, parser recovery or unsupported facilities are not the intended delimiter/list
cause.

**Dependencies:** 4.1.3R401;4.2p2;8.2R801/R802/R805/C807;8.5.1C815;8.5.13;R855;8.7;10.1.12.

### R855: Each PARAMETER definition has a named constant, equals sign and constant expression

**Source:** 8.6.11 R855, physical PDF139 **Class:** Syntax.

**Definition:** A named-constant-def consists of a named-constant, an equals sign and a constant-expr.
R606 defines the name form and10.1.12 defines constant-expression eligibility. The
statement supplies PARAMETER; a previous PARAMETER attribute is not required. Type,
shape, prior definition and special BOZ consumer restrictions remain independent.

**Diagnostic obligation:** required.

**Facets:** `literal-definition`, `named-expression-definition`, `missing-name-or-value`, `equals-not-pointer-arrow`, `name-not-element-designator`, `constant-expression-and-BOZ-owner-relations`.

**Oracle:** Qualified name/equal/expression syntax controls and minimal malformed-definition
repairs; semantic constant-expression, typing, rank and BOZ predicates retain their
canonical source and evidence owners.

**Oracle limitation:** No general expression, pointer arrow, component reference, function call or literal is
interchangeable with a named constant definition. Numbered reports need actual
causes/origins and valid controls; generic constancy/role words, echoes, unsupported
features or compiler failures do not qualify.

**Dependencies:** 6.2.3R604-R606;4.1.3;4.2p2;8.5.13/C852/C853;8.6.11p2-p4;10.1.12R1030/C1012;7.7C7119;10.2.1.2/.3.

### S8.6.11-001: Subsequent type declarations for a PARAMETER-defined constant preserve its implicit type mapping

**Source:** 8.6.11 p2 first sentence, physical PDF139 **Class:** Restriction.

**Definition:** After a named constant is defined by a PARAMETER statement, a subsequent declaration
must not give it a type or type parameter value different from what its applicable
implicit mapping would supply. This is a restriction on a subsequent declaration, not a
demand that every previously explicitly typed constant match a default first-letter
type.

**Diagnostic obligation:** context-dependent.

**Facets:** `late-matching-integer-control`, `late-different-type-source-gate`, `late-character-length-source-gate`, `prior-explicit-type-boundary`, `implicit-mapping-and-kind-boundaries`.

**Oracle:** Complete matching-order/type controls and a finite source/reporting gate for later type
or parameter changes, with explicit applicable implicit mappings and minimal repairs.

**Oracle limitation:** The condition must not be generalized to declarations preceding PARAMETER. No new
mandatory prose diagnosis, compiler-consensus type rule, duplicated explicit type,
unsupported kind or invalid IMPLICIT setup is a valid oracle.

**Dependencies:** 8.7p1/p3/p4;8.2;8.5.13;10.2.1.2;7.2;19.5.1.4;4.2p2.

### S8.6.11-002: A named array constant has its rank specified before its PARAMETER statement

**Source:** 8.6.11 p2 second sentence, physical PDF139 **Class:** Restriction.

**Definition:** An array named constant defined using PARAMETER has its rank specified in a prior
specification statement. Inferring rank from a value expression or declaring it
afterward does not satisfy this ordering. Implied extents and declared rank are
different properties.

**Diagnostic obligation:** context-dependent.

**Facets:** `prior-type-array-spec`, `prior-dimension-statement`, `later-rank-source-gate`, `implied-shape-and-RANK-clause-boundaries`.

**Oracle:** Complete prior-rank admissions and independently qualified source-order contrasts, using
the actual prior declaration and final named-constant role.

**Oracle limitation:** Prior rank is not initialized data, lower-bound equality, allocation or a later inquiry
result. The prose does not automatically require a fatal compiler report, and canonical
DIMENSION, implied-shape and C853 rules remain operative.

**Dependencies:** 8.2R803/R804;8.5.13C852/C853;8.5.8.1/.2/.6;8.5.17C864;8.6.8R851;10.1.12;4.2.

### S8.6.11-003: Each PARAMETER expression has compatible type parameters and the required rank or shape

**Source:** 8.6.11 p3, physical PDF139 **Class:** Restriction.

**Definition:** Each corresponding constant expression conforms in type and type parameters under
intrinsic-assignment rules. An implied-shape named constant requires the same expression
rank. Otherwise the expression is scalar or has the same shape as the named constant.
Scalar expansion is not permission to initialize an implied-shape array from a scalar or
reshape arbitrary arrays.

**Diagnostic obligation:** context-dependent.

**Facets:** `intrinsic-type-conformance-source`, `derived-enum-parameter-conformance-source`, `implied-shape-equal-rank`, `implied-shape-scalar-exclusion-source`, `explicit-shape-scalar-admission`, `explicit-shape-array-conformance`, `zero-extent-and-character-boundaries`.

**Oracle:** Complete parameter-definition compatibility controls plus source-qualified contrasts,
retaining canonical type tables and both conditional rank/shape branches. Prose alone
does not create universal diagnostic obligations.

**Oracle limitation:** No array-count broadcasting, automatic allocation, runtime trap, invalid constructor or
unqualified numeric representation oracle. Generic assignment/construction cases are not
automatically PARAMETER-statement occurrences or new evidence links.

**Dependencies:** 8.2p4/S8.2-004;8.5.13;8.5.8.6;8.6.11p2;10.1.12;10.2.1.2/Table10.8;10.2.1.3p5/p8-p11;7.5.10;7.6;7.7C7119.

### S8.6.11-004: PARAMETER definitions supply the corresponding constant values with prescribed conversion

**Source:** 8.6.11 p1 value specification and p4, physical PDF139 **Class:** Effect.

**Definition:** Each named constant has the value specified by its corresponding constant expression,
converted where necessary by intrinsic-assignment value rules to agree in type, type
parameters and shape. The statement-specific initial-value observation does not execute
an assignment to a variable, waive p3 compatibility, or import
allocation/finalization/callback behavior.

**Diagnostic obligation:** not-required.

**Facets:** `corresponding-integer-values`, `logical-values`, `character-padding`, `character-truncation`, `conforming-array-values`, `scalar-to-explicit-array-values`, `simple-derived-values`, `numeric-kind-and-BOZ-oracle-gates`.

**Oracle:** Actual named constants initialized by PARAMETER, observed through independent finite
values and complete execution/check guards, with whole-program wrong-oracle sensitivity.
Conditional numeric/representation work stays explicitly pending until independently
qualified.

**Oracle limitation:** No cases are implemented here. Constant-expression eligibility, p2 ordering and p3
compatibility are prerequisites; a declaration-only empty run or a re-evaluated
initializer is not an independent value effect. Named constants are not mutable saved
variables, and intrinsic-assignment conversion references promise no allocator,
finalization, user callback, address or ABI mechanism.

**Dependencies:** 8.4;8.5.13p1/p2/C852/C853;8.6.11p2/p3;10.1.12;10.2.1.3/Table10.9/p5/p8-p11;7.4.4.2p5;7.5.8/.10;7.7C7119;16.3;19.6.2;existing
S10.2.1.3-017 oracle/profile boundaries.

<!-- END GENERATED 8.6.11 -->
