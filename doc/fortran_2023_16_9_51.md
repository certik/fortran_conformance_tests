# Fortran 2023 16.9.51: CEILING

The canonical catalogue is `doc/catalogues/ceiling_16_9_51.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.51")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Six base units: p1-p6 on physical PDF394; no numbered R/C rules occur.

<!-- BEGIN GENERATED 16.9.51 -->

### S16.9.51-001: CEILING arguments are real A and optional scalar integer constant KIND

**Source:** S16.9.51, J3/24-007, 18 December 2023, physical PDF394. **Class:** Restriction.

**Definition:** A shall be of type real and KIND, if present, shall be a scalar integer constant
expression.

**Diagnostic obligation:** not-required.

**Facets:** `A-real-argument`, `KIND-scalar-integer-constant-expression`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.51-002: CEILING returns an integer of the selected or default kind

**Source:** S16.9.51, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** The result is integer; present KIND selects the result kind type parameter, and absent
KIND selects default integer kind.

**Diagnostic obligation:** not-required.

**Facets:** `result-integer-type`, `present-KIND-selects-result-kind`, `absent-KIND-default-integer-kind`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.51-003: CEILING returns the least integer greater than or equal to A

**Source:** S16.9.51, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** The result value is the least integer greater than or equal to A.

**Diagnostic obligation:** not-required.

**Facets:** `least-integer-greater-or-equal-value`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

<!-- END GENERATED 16.9.51 -->
