# Fortran 2023 16.9.99: IALL

The canonical catalogue is `doc/catalogues/iall_16_9_99.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where compound or mixed source units are split.

<!-- BEGIN INTRINSICS 16.9 L FIXTURES -->
## Intrinsics 16.9 L runtime observations

The `intrinsics_16_9_l` generator supplies 5 requirement bindings covering 13 portable facets in this section. Oracles use direct intrinsic-expression kind/shape inquiries, exact ASCII code points, bit-context IALL/IAND checks, NOT(0_kind) for IALL zero-size identity, and HUGE model decomposition under explicit profiles. Enumeration HUGE, HYPOT approximation latitude, non-ASCII IACHAR values, and IAND invalid-call restrictions remain pending where no portable runtime oracle is available.
<!-- END INTRINSICS 16.9 L FIXTURES -->

<!-- BEGIN GENERATED 16.9.99 -->

### S16.9.99-001: IALL argument restrictions

**Source:** S16.9.99, J3/24-007, 18 December 2023, physical PDF417. **Class:** Restriction.

**Definition:** ARRAY shall be an integer array; DIM shall be an integer scalar in the valid rank range;
MASK shall be logical and conformable with ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `IALL-ARRAY-integer-array`, `IALL-DIM-integer-scalar-valid-range`, `IALL-MASK-logical-conformable`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations. Bit-producing intrinsic results are observed in bit context by
BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric interpretation of a
result whose leftmost bit may be set. Each positive plan establishes a non-default state
first, uses a non-vacuous companion for zero, .FALSE., blank, or identity results, and
includes a feature mutation such as changing the intrinsic name, argument order, DIM,
MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

S16.9.99-001 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Exact bit plans use real BOZ actual arguments for
BOZ rules and observe bit results only in bit context. Grep of tests/ and
tests/fixtures/*/fixture.json for IALL, IAND, IANY, IBCLR, IBITS, IBSET, and ICHAR found
no existing fixture.json binding in this worktree; IAND appears only in a Clause 11 DO
CONCURRENT reduction fixture source and related test assertions, so this packet records
that coverage without rebinding it.

S16.9.99-001 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

### S16.9.99-002: IALL result type, kind, rank, and shape

**Source:** S16.9.99, J3/24-007, 18 December 2023, physical PDF417. **Class:** Effect.

**Definition:** The IALL result has ARRAY's integer type and kind, is scalar without DIM or for rank-one
ARRAY, and otherwise has rank n-1 with the DIM extent removed.

**Diagnostic obligation:** not-required.

**Facets:** `IALL-result-type-kind-from-array`, `IALL-result-scalar-without-dim-or-rank-one`, `IALL-result-shape-removes-dim`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations. Bit-producing intrinsic results are observed in bit context by
BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric interpretation of a
result whose leftmost bit may be set. Each positive plan establishes a non-default state
first, uses a non-vacuous companion for zero, .FALSE., blank, or identity results, and
includes a feature mutation such as changing the intrinsic name, argument order, DIM,
MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

S16.9.99-002 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Exact bit plans use real BOZ actual arguments for
BOZ rules and observe bit results only in bit context. Grep of tests/ and
tests/fixtures/*/fixture.json for IALL, IAND, IANY, IBCLR, IBITS, IBSET, and ICHAR found
no existing fixture.json binding in this worktree; IAND appears only in a Clause 11 DO
CONCURRENT reduction fixture source and related test assertions, so this packet records
that coverage without rebinding it.

S16.9.99-002 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

### S16.9.99-003: IALL full-array reduction and zero-size identity

**Source:** S16.9.99, J3/24-007, 18 December 2023, physical PDF417. **Class:** Effect.

**Definition:** IALL over the whole ARRAY yields the bitwise AND of all elements, with the zero-size
identity NOT(INT(0,KIND(ARRAY))).

**Diagnostic obligation:** not-required.

**Facets:** `IALL-bitwise-and-all-elements`, `IALL-zero-size-identity-all-one-bits`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations. Bit-producing intrinsic results are observed in bit context by
BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric interpretation of a
result whose leftmost bit may be set. Each positive plan establishes a non-default state
first, uses a non-vacuous companion for zero, .FALSE., blank, or identity results, and
includes a feature mutation such as changing the intrinsic name, argument order, DIM,
MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

S16.9.99-003 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Exact bit plans use real BOZ actual arguments for
BOZ rules and observe bit results only in bit context. Grep of tests/ and
tests/fixtures/*/fixture.json for IALL, IAND, IANY, IBCLR, IBITS, IBSET, and ICHAR found
no existing fixture.json binding in this worktree; IAND appears only in a Clause 11 DO
CONCURRENT reduction fixture source and related test assertions, so this packet records
that coverage without rebinding it.

S16.9.99-003 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

### S16.9.99-004: IALL MASK is equivalent to reducing PACK(ARRAY,MASK)

**Source:** S16.9.99, J3/24-007, 18 December 2023, physical PDF417. **Class:** Effect.

**Definition:** IALL with MASK has the value of IALL applied to PACK(ARRAY,MASK).

**Diagnostic obligation:** not-required.

**Facets:** `IALL-mask-pack-equivalence`, `IALL-all-false-mask-uses-zero-size-identity`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations. Bit-producing intrinsic results are observed in bit context by
BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric interpretation of a
result whose leftmost bit may be set. Each positive plan establishes a non-default state
first, uses a non-vacuous companion for zero, .FALSE., blank, or identity results, and
includes a feature mutation such as changing the intrinsic name, argument order, DIM,
MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

S16.9.99-004 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Exact bit plans use real BOZ actual arguments for
BOZ rules and observe bit results only in bit context. Grep of tests/ and
tests/fixtures/*/fixture.json for IALL, IAND, IANY, IBCLR, IBITS, IBSET, and ICHAR found
no existing fixture.json binding in this worktree; IAND appears only in a Clause 11 DO
CONCURRENT reduction fixture source and related test assertions, so this packet records
that coverage without rebinding it.

S16.9.99-004 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

### S16.9.99-005: IALL DIM reduction applies IALL to each section

**Source:** S16.9.99, J3/24-007, 18 December 2023, physical PDF417. **Class:** Effect.

**Definition:** IALL with DIM reduces each rank-one section selected along DIM, with optional MASK
sections applied correspondingly; rank-one ARRAY with DIM equals the whole-array form.

**Diagnostic obligation:** not-required.

**Facets:** `IALL-dim-rank-one-equals-whole-array`, `IALL-dim-section-wise-reduction`, `IALL-dim-mask-section-wise-reduction`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations. Bit-producing intrinsic results are observed in bit context by
BTEST, POPCNT, or bit-pattern comparisons, not by signed numeric interpretation of a
result whose leftmost bit may be set. Each positive plan establishes a non-default state
first, uses a non-vacuous companion for zero, .FALSE., blank, or identity results, and
includes a feature mutation such as changing the intrinsic name, argument order, DIM,
MASK, POS, LEN, KIND, or a BOZ literal. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

S16.9.99-005 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Exact bit plans use real BOZ actual arguments for
BOZ rules and observe bit results only in bit context. Grep of tests/ and
tests/fixtures/*/fixture.json for IALL, IAND, IANY, IBCLR, IBITS, IBSET, and ICHAR found
no existing fixture.json binding in this worktree; IAND appears only in a Clause 11 DO
CONCURRENT reduction fixture source and related test assertions, so this packet records
that coverage without rebinding it.

S16.9.99-005 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic and character types, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 integer bit model, 7.4.4.4 character collating sequence rules,
sibling 16.9 intrinsics named by result formulas, and clauses 3, 5, 14, 15, 17, 18, and
19 where future fixtures need terms, constants, programs, procedure interfaces, IEEE
support, deleted features, or scope rules.

<!-- END GENERATED 16.9.99 -->
