# Fortran 2023: 11.1.10.2 Execution of the SELECT RANK construct

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.1.10.2 -->

### S11.1.10.2-001: SELECT RANK executes at most one block and associates the associate name during that block

**Source:** 11.1.10.2 p1 first two sentences; J3/24-007 PDF p223. **Class:** Effect.

**Definition:** A SELECT RANK construct selects at most one block to be executed. During execution of
that selected block, the associate name identifies an entity associated with the
selector.

**Diagnostic obligation:** not-required.

**Facets:** `at-most-one-selected-rank-block-executes`, `associate-name-associated-during-selected-block`.

**Oracle:** Block execution is observable through sentinels; association mechanics depend on Clause
19 and are source-only here.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future SELECT RANK fixtures must preload targets with sentinels distinct from every
value any block could write and observe only final values to prove which block executed.
They must not infer selector evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. They must not use
the RANK intrinsic as the oracle for rank selection; expected execution must follow from
the declared dummy/actual association controlled by the fixture and the selected block
sentinel. Assumed-size, assumed-rank, and sequence-association cases require Clause 15
dependencies before execution approval.

**Dependencies:** 11.1.10.1 in this packet;11.1.1 blocks registered;19.5.1.6 association NOT YET
REGISTERED.

### S11.1.10.2-002: SELECT RANK case statements match assumed-size, specific non-assumed-size ranks, or default fallback

**Source:** 11.1.10.2 p1 matching sentences; J3/24-007 PDF p223. **Class:** Effect.

**Definition:** A RANK(*) statement matches a selector argument associated with an assumed-size array. A
RANK(n) statement matches a selector with rank n that is not argument associated with an
assumed-size array. RANK DEFAULT matches only when no other select-rank-case-stmt of the
construct matches.

**Diagnostic obligation:** not-required.

**Facets:** `rank-star-matches-assumed-size-selector`, `scalar-rank-matches-non-assumed-size-selector`, `rank-default-matches-only-after-no-other-match`.

**Oracle:** Use selected-block sentinels. Do not use RANK, SHAPE, or assumed-size inquiry behavior
as the primary oracle for matching.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future SELECT RANK fixtures must preload targets with sentinels distinct from every
value any block could write and observe only final values to prove which block executed.
They must not infer selector evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. They must not use
the RANK intrinsic as the oracle for rank selection; expected execution must follow from
the declared dummy/actual association controlled by the fixture and the selected block
sentinel. Assumed-size, assumed-rank, and sequence-association cases require Clause 15
dependencies before execution approval.

**Dependencies:** 15.5 dummy arguments, argument association, assumed-rank, and assumed-size NOT YET
REGISTERED;8.5.8.5 assumed-size and 8.5.8.7 assumed-rank registered as
declarations;11.1.10.3 in this packet.

### S11.1.10.2-003: The matching SELECT RANK block executes, otherwise control transfers to END SELECT

**Source:** 11.1.10.2 p1 final sentence; J3/24-007 PDF p223. **Class:** Effect.

**Definition:** If a select-rank-case-stmt matches the selector, the block following that statement is
executed; otherwise, control is transferred to the end-select-rank-stmt.

**Diagnostic obligation:** not-required.

**Facets:** `matching-rank-block-executes`, `nonmatching-rank-blocks-not-executed`, `no-rank-match-transfers-to-end-select`.

**Oracle:** Which block executed and whether control reached after END SELECT are observable; which
selector inquiries were evaluated is not used.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future SELECT RANK fixtures must preload targets with sentinels distinct from every
value any block could write and observe only final values to prove which block executed.
They must not infer selector evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. They must not use
the RANK intrinsic as the oracle for rank selection; expected execution must follow from
the declared dummy/actual association controlled by the fixture and the selected block
sentinel. Assumed-size, assumed-rank, and sequence-association cases require Clause 15
dependencies before execution approval.

**Dependencies:** 11.1.10.1 C1157-C1158 in this packet guarantee no duplicate matching rank/default
forms;11.1.1 blocks registered.

### S11.1.10.2-004: Branching to SELECT RANK END SELECT is permitted only from within its construct

**Source:** 11.1.10.2 p2; J3/24-007 PDF p223. **Class:** Restriction.

**Definition:** It is permissible to branch to an end-select-rank-stmt only from within its SELECT RANK
construct; an outside branch to that END SELECT is not permitted.

**Diagnostic obligation:** context-dependent.

**Facets:** `internal-end-select-rank-branch-control`, `outside-end-select-rank-branch-rejected`.

**Oracle:** The word only supplies the outside-branch prohibition; the internal branch half is a
permission and no plan asserts a processor-specific branch behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Branching fixtures must isolate source and target locations, keep labels and construct
names otherwise valid, and avoid evaluation-order, side-effect, image-ordering, or
message-wording oracles.

**Dependencies:** 8 labels registered;11.2 branch statements NOT YET REGISTERED;11.1.2.1 control flow in
blocks registered.

<!-- END GENERATED 11.1.10.2 -->
