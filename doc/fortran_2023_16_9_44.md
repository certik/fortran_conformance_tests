# Fortran 2023 16.9.44: BESSEL_YN

The canonical catalogue is `doc/catalogues/bessel_yn_16_9_44.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.44 -->

### S16.9.44-001: BESSEL_YN identifies Bessel functions of the second kind

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** BESSEL_YN is the intrinsic for Bessel functions of the second kind.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-second-kind-family`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.44-002: BESSEL_YN has elemental two-argument and transformational three-argument forms

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** BESSEL_YN(N,X) is elemental and BESSEL_YN(N1,N2,X) is transformational.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-two-argument-elemental-class`, `bessel-yn-three-argument-transformational-class`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.44-003: BESSEL_YN arguments have integer-order and real-X restrictions

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Restriction.

**Definition:** N shall be of type integer and nonnegative; N1 and N2 shall be integer scalars with
nonnegative values; X shall be real; for the transformational form X shall be scalar. X
shall be greater than zero.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-n-integer-nonnegative`, `bessel-yn-n1-integer-scalar-nonnegative`, `bessel-yn-n2-integer-scalar-nonnegative`, `bessel-yn-x-real`, `bessel-yn-transformational-x-scalar`, `bessel-yn-x-positive`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.44-004: BESSEL_YN result characteristics match X and define the transformational extent

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** The result has the same type and kind as X; BESSEL_YN(N1,N2,X) returns a rank-one array
with extent MAX(N2 - N1 + 1, 0).

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-result-same-type-kind-as-x`, `bessel-yn-transformational-rank-one`, `bessel-yn-transformational-extent-max`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.44-005: BESSEL_YN(N,X) result approximates the order-N second-kind Bessel value

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** The two-argument result is an approximation to the Bessel function of the second kind
and order N of X.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-case-i-order-n-approximation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.44-006: BESSEL_YN(N,X) approximation is processor dependent

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** The approximation chosen for the two-argument BESSEL_YN result is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-case-i-processor-dependent-approximation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.44-007: BESSEL_YN(N1,N2,X) elements approximate successive second-kind Bessel orders

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** Element i of the three-argument result is an approximation to the Bessel function of the
second kind and order N1 + i - 1 of X.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-case-ii-successive-order-approximations`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.44-008: BESSEL_YN(N1,N2,X) element approximations are processor dependent

**Source:** S16.9.44, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** The approximation chosen for each three-argument BESSEL_YN result element is processor
dependent.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-yn-case-ii-processor-dependent-approximations`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

<!-- END GENERATED 16.9.44 -->
