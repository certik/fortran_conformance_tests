# Fortran 2023 12.6.1: Data transfer statements, general

The canonical catalogue is `doc/catalogues/data_transfer_statements_general_12_6_1.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, R1210, R1211, R1212, note1, note2. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.6.1 -->

### S12.6.1-001: READ is the data-transfer input statement and WRITE/PRINT are data-transfer output statements

**Source:** S12.6.1, J3/24-007, 18 December 2023, physical PDF257. **Class:** Effect.

**Definition:** READ is the data transfer input statement; WRITE and PRINT are the data transfer output
statements.

**Diagnostic obligation:** not-required.

**Facets:** `read-data-transfer-input-statement`, `write-data-transfer-output-statement`, `print-data-transfer-output-statement`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### R1210: A READ statement has parenthesized io-control-spec form or READ format form

**Source:** S12.6.1, J3/24-007, 18 December 2023, physical PDF257. **Class:** Syntax.

**Definition:** A read-stmt is READ with a parenthesized io-control-spec-list and optional
input-item-list, or READ followed by a format and optional comma input-item-list.

**Diagnostic obligation:** required.

**Facets:** `read-parenthesized-control-form`, `read-format-form`, `read-optional-input-item-list`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### R1211: A WRITE statement has parenthesized io-control-spec form and optional output items

**Source:** S12.6.1, J3/24-007, 18 December 2023, physical PDF257. **Class:** Syntax.

**Definition:** A write-stmt is WRITE with a parenthesized io-control-spec-list and optional
output-item-list.

**Diagnostic obligation:** required.

**Facets:** `write-parenthesized-control-form`, `write-optional-output-item-list`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### R1212: A PRINT statement is PRINT format with optional output items

**Source:** S12.6.1, J3/24-007, 18 December 2023, physical PDF257. **Class:** Syntax.

**Definition:** A print-stmt is PRINT followed by a format and an optional comma output-item-list.

**Diagnostic obligation:** required.

**Facets:** `print-format-required`, `print-optional-output-item-list`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

<!-- END GENERATED 12.6.1 -->
