# Fortran 2023 17.6: Halting

The canonical catalogue is `doc/catalogues/halting_17_6.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN IEEE 17.4-17.9 FIXTURES -->
## Executable IEEE 17.4-17.9 fixtures

The fixtures cover supported nonhalting SET/GET and procedure-entry preservation without asserting initial modes or halting timing. Every claimed facet has a distinct assertion and feature mutation; optional support is required by profiles that exit 77 before the fixture is counted.
<!-- END IEEE 17.4-17.9 FIXTURES -->

<!-- BEGIN GENERATED 17.6 -->

### S17.6-001: Processors may provide halting control with processor-dependent mechanics

**Source:** S17.6, J3/24-007, 18 December 2023, physical PDF484. **Class:** Effect.

**Definition:** Some processors allow control over aborting or continuing after an exception through
IEEE_SET_HALTING_MODE; halting is imprecise after the exception, and the initial halting
mode is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `halting-control-processor-permitted`, `halting-occurrence-imprecise`, `initial-halting-mode-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming single-image programs, and assert only portable Clause 17
properties: named derived-type values, exact logical results, exact type/kind/rank
characteristics, supported IEEE special-value classifications, exact representable
arithmetic under the named support inquiry, or get/set round trips. Unsupported guarded
branches are skips, not passes. Processor-dependent support availability, NaN payloads,
exact halting mechanics, initial modes, approximation latitude, and IOMSG text are not
asserted.

S17.6-001 halting-control fixture: on a profile requiring
IEEE_SUPPORT_HALTING(IEEE_OVERFLOW), SET_HALTING_MODE(...,.FALSE.) followed by
GET_HALTING_MODE observes .FALSE.; no initial mode or precise halting occurrence is
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument and Restriction paragraphs do not create diagnostic obligations.
Negative source mutations are non-diagnostic unless a future numbered rule requires
rejection and the rejection is portably observable. Existing IEEE profile and formatting
fixtures are recorded as nearby coverage only and are not rebound here.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
declarations, expressions, execution, modules, intrinsic assignment, I/O harnesses,
formatting references, and generic intrinsic foundations. Unregistered dependencies
explicitly retained: the remaining Clause 17 procedure descriptions, IEEE derived types
and named constants, processor support inquiries, exception-flag semantics,
rounding-mode semantics, and ISO/IEC 60559 terminology.

### S17.6-002: Procedures preserve halting mode except the halting and status setters

**Source:** S17.6, J3/24-007, 18 December 2023, physical PDF484. **Class:** Effect.

**Definition:** A procedure other than IEEE_SET_HALTING_MODE or IEEE_SET_STATUS does not change the
halting mode on entry and ensures the same halting mode on return.

**Diagnostic obligation:** not-required.

**Facets:** `procedure-entry-preserves-halting-mode`, `procedure-return-restores-halting-mode`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state
first, use conforming single-image programs, and assert only portable Clause 17
properties: named derived-type values, exact logical results, exact type/kind/rank
characteristics, supported IEEE special-value classifications, exact representable
arithmetic under the named support inquiry, or get/set round trips. Unsupported guarded
branches are skips, not passes. Processor-dependent support availability, NaN payloads,
exact halting mechanics, initial modes, approximation latitude, and IOMSG text are not
asserted.

S17.6-002 halting procedure-entry fixture: the caller sets overflow halting mode to
.FALSE.; a helper's first GET_HALTING_MODE observes .FALSE., proving entry preservation
without running any halting exception.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument and Restriction paragraphs do not create diagnostic obligations.
Negative source mutations are non-diagnostic unless a future numbered rule requires
rejection and the rejection is portably observable. Existing IEEE profile and formatting
fixtures are recorded as nearby coverage only and are not rebound here.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
declarations, expressions, execution, modules, intrinsic assignment, I/O harnesses,
formatting references, and generic intrinsic foundations. Unregistered dependencies
explicitly retained: the remaining Clause 17 procedure descriptions, IEEE derived types
and named constants, processor support inquiries, exception-flag semantics,
rounding-mode semantics, and ISO/IEC 60559 terminology.

<!-- END GENERATED 17.6 -->
