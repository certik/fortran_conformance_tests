# Fortran 2023 11.7.5: SYNC MEMORY statement

Source registration packet for J3/24-007 (sha256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`), PDF page start from the source inventory. This packet is source accounting only: it adds no fixtures and approves no executable oracle.

## Source accounting summary

| Section | Title | Base units read | Numbered R/C found | Already-counted base units | Newly-accounted base units | Declared facets |
| --- | --- | ---: | --- | --- | --- | ---: |
| 11.7.5 | SYNC MEMORY statement | 7 | R1172 | R1172 | p1, p2, p3, p4, note1, note2 | 10 |

Pre-existing-fixture check: R1172 was checked with `grep -rl <identifier> tests/`; it returned no matches.

Correction split: p4 is a mixed paragraph. The base paragraph is structural; `p4.dependency-requirement` carries the normative shall dependency and `p4.processor-dependent-mechanisms` carries only the processor-dependent mechanism latitude.

## Attribution table for diagnostic facets

| Unit | Facet | Exact prohibiting words | Single differing property |
| --- | --- | --- | --- |
| _None_ | _None_ | _No diagnostic facets in this section._ | _None_ |

## Implementability table

| Facet | Classification | Reason |
| --- | --- | --- |
| `sync-memory-segment-boundary-source-control` | source-control | Local segment-boundary classification can be recorded but not independently observed. |
| `sync-memory-user-defined-ordering-source-control` | unimplementable-context-dependent | Ordering with other images needs unregistered cooperative synchronization. |
| `sync-memory-without-list` | implementable | Pure syntax form. |
| `sync-memory-empty-parentheses` | implementable | Pure syntax form. |
| `sync-memory-with-sync-stat-list` | implementable | Pure syntax form; STAT/ERRMSG effects delegated. |
| `sync-memory-action-precedence-source-control` | unimplementable-context-dependent | Cross-image memory/action ordering has no portable single-image oracle. |
| `user-defined-ordering-initiate-complete-source-control` | unimplementable-context-dependent | Cooperative synchronization between images cannot be observed without forbidden timing/interleaving evidence. |
| `user-defined-ordering-boundary-image-control-source-control` | source-control | Boundary source shape can be reviewed; ordering effect cannot. |
| `cooperative-synchronization-dependency-source-control` | unimplementable-context-dependent | Dynamic dependency between images is not a required diagnostic and cannot be observed portably. |
| `processor-dependent-dependency-mechanism-permission` | source-control | Processor-dependent permission is recorded; no oracle may require a mechanism. |

## Compound-obligation sweep

| Unit | Obligation count summary |
| --- | --- |
| p1 | Two effects: ends/begins segments and enables possible user-defined ordering. |
| R1172 | Three syntax facets: no list, empty parentheses, populated sync-stat-list. |
| p2 | One conditional action-precedence consequence from three premises. |
| p3 | Two user-defined ordering conditions: cooperative initiate/complete and image-control boundaries. |
| p4 | Two obligations: dependency required, available mechanisms processor dependent. |

## Permissions recorded

| Unit | Facet | Why this is permission/latitude |
| --- | --- | --- |
| 11.7.5 p4.processor-dependent-mechanisms | `processor-dependent-dependency-mechanism-permission` | The standard states mechanisms available for creating the dependency are processor dependent; no fixture may assert a specific mechanism. |

## Dependency and ownership notes

This packet cites, and does not duplicate, registered 11.1.5 CHANGE TEAM and 11.1.6 CRITICAL synchronization material, all of Clause 10, and source-complete Clauses 6, 8, and 9. It flags unregistered reaches to 11.2-11.6, 11.7.7-11.7.11, Clauses 12-19, Clause 15 procedures, and Clause 16 ISO_FORTRAN_ENV/team/status facilities where they are prerequisites.

<!-- BEGIN GENERATED 11.7.5 -->

### S11.7.5-001: SYNC MEMORY ends one segment and begins another for possible user-defined ordering

**Source:** 11.7.5 p1; J3/24-007 PDF p234. **Class:** Effect.

**Definition:** Execution of SYNC MEMORY ends one segment and begins another; those two segments can be
ordered by a user-defined way with respect to segments on other images.

**Diagnostic obligation:** not-required.

**Facets:** `sync-memory-segment-boundary-source-control`, `sync-memory-user-defined-ordering-source-control`.

**Oracle:** The local segment boundary is source-use evidence; ordering with other images remains
unobservable without a reviewed multi-image/user-defined oracle.

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** 11.7.2 segments in this packet; external synchronization and procedures in Clause 15 NOT
YET REGISTERED.

### R1172: A SYNC MEMORY statement is SYNC MEMORY with optional parenthesized sync status list

**Source:** 11.7.5 R1172; J3/24-007 PDF p234. **Class:** Syntax.

**Definition:** A sync-memory-stmt is SYNC MEMORY optionally followed by parentheses containing an
optional sync-stat-list.

**Diagnostic obligation:** required.

**Facets:** `sync-memory-without-list`, `sync-memory-empty-parentheses`, `sync-memory-with-sync-stat-list`.

**Oracle limitation:** This source-only catalogue records pending plans only. Local source-form or value-domain
facts may become implementable only through a future reviewed fixture that observes a
purely local property without duplicating the feature under test and without using
unregistered STAT=/ERRMSG= semantics or multi-image synchronization as the oracle.

**Dependencies:** R1169/C1179/C1180 in 11.7.3; 11.7.11 effects NOT YET REGISTERED.

### S11.7.5-002: SYNC MEMORY orders actions on a variable across successful execution when the paragraph conditions hold

**Source:** 11.7.5 p2; J3/24-007 PDF p234. **Class:** Effect.

**Definition:** If statements on image P perform an action regarding X on image Q, that statement
precedes a successful SYNC MEMORY, and a later statement performs an action regarding Y
on image Q after the SYNC MEMORY, then the action regarding X on image Q precedes the
action regarding Y on image Q.

**Diagnostic obligation:** not-required.

**Facets:** `sync-memory-action-precedence-source-control`.

**Oracle:** The rule is a conditional ordering effect involving images P and Q. Timing, cache
visibility, and external synchronization observations are not portable evidence.

**Oracle limitation:** This source-only catalogue records pending plans only. The specified synchronization,
segment delay, segment precedence, or cross-image memory-order effect is not approved as
an executable oracle in the current single-image suite: a one-image run would be vacuous
and multi-image timing, scheduling, interleaving, progress, deadlock, race, or failure
observations are not portable evidence.

**Dependencies:** 11.7.2 action/segment ordering in this packet; Clause 15 external procedure interactions
NOT YET REGISTERED.

### S11.7.5-003: User-defined ordering occurs through image-control boundaries and cooperative synchronization

**Source:** 11.7.5 p3; J3/24-007 PDF p235. **Class:** Effect.

**Definition:** User-defined ordering of segment Pi on image P to precede segment Qj on image Q occurs
when image P executes an image control statement ending Pi, then initiates cooperative
synchronization between P and Q, and image Q completes the cooperative synchronization
and then executes an image control statement beginning Qj.

**Diagnostic obligation:** not-required.

**Facets:** `user-defined-ordering-initiate-complete-source-control`, `user-defined-ordering-boundary-image-control-source-control`.

**Oracle:** This definition cannot be validated by assuming an external procedure synchronized
images; any future fixture needs a reviewed non-timing dependency mechanism.

**Oracle limitation:** This source-only catalogue records pending plans only. The specified synchronization,
segment delay, segment precedence, or cross-image memory-order effect is not approved as
an executable oracle in the current single-image suite: a one-image run would be vacuous
and multi-image timing, scheduling, interleaving, progress, deadlock, race, or failure
observations are not portable evidence.

**Dependencies:** 11.7.1 image control statements and 11.7.2 segments in this packet; Clause 15 external
procedures NOT YET REGISTERED.

### S11.7.5-004: Cooperative synchronization for user-defined ordering includes a dependency; dependency mechanisms are processor dependent

**Source:** 11.7.5 p4; J3/24-007 PDF p235. **Class:** Effect.

**Definition:** Execution of the cooperative synchronization between images P and Q shall include a
dependency forcing execution on image P of the initiating statements to precede
execution on image Q of the completing statements. The mechanisms available for creating
such a dependency are processor dependent.

**Diagnostic obligation:** context-dependent.

**Facets:** `cooperative-synchronization-dependency-source-control`, `processor-dependent-dependency-mechanism-permission`.

**Oracle:** The dependency requirement is dynamic and external; the mechanism sentence is processor
latitude. This packet records neither as executable evidence.

**Oracle limitation:** This source-only catalogue records pending plans only. The specified synchronization,
segment delay, segment precedence, or cross-image memory-order effect is not approved as
an executable oracle in the current single-image suite: a one-image run would be vacuous
and multi-image timing, scheduling, interleaving, progress, deadlock, race, or failure
observations are not portable evidence.

**Dependencies:** Clause 15 procedures and processor-dependent facilities NOT YET REGISTERED; 11.7.2
segments in this packet.

<!-- END GENERATED 11.7.5 -->
