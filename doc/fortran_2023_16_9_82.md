# Fortran 2023 16.9.82: EVENT_QUERY

The canonical catalogue is `doc/catalogues/event_query_16_9_82.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.

Coarray/image note: image-dependent event ordering, unordered segments, and portable error-condition induction are out of scope for this suite's single-image fixtures; the local no-post success count remains a concrete single-image plan.

<!-- BEGIN GENERATED 16.9.82 -->

### S16.9.82-001: EVENT_QUERY is the event count inquiry intrinsic

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** EVENT_QUERY is the intrinsic subroutine that queries an event count.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-description`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-002: EVENT_QUERY is a subroutine

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** EVENT_QUERY is a subroutine.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-subroutine-class`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-003: EVENT argument restrictions for EVENT_QUERY

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Restriction.

**Definition:** EVENT shall be an event variable, shall not be coindexed, and is an INTENT(IN) argument.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-event-variable`, `event-query-event-not-coindexed`, `event-query-event-intent-in`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-004: EVENT_QUERY accesses EVENT atomically with unordered event posts

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** The EVENT argument is accessed atomically with respect to EVENT POST statements in
unordered segments, analogously to atomic subroutines.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-atomic-with-unordered-posts`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-005: COUNT argument restrictions for EVENT_QUERY

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Restriction.

**Definition:** COUNT shall be an integer scalar with decimal exponent range no smaller than default
integer and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-count-integer-scalar`, `event-query-count-range`, `event-query-count-intent-out`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-006: EVENT_QUERY assigns COUNT on success and error

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** If no error condition occurs, COUNT is assigned the count of EVENT; otherwise COUNT is
assigned -1.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-count-success-count`, `event-query-count-error-minus-one`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-007: STAT argument restrictions for EVENT_QUERY

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Restriction.

**Definition:** Optional STAT shall be a noncoindexed integer scalar with decimal exponent range at
least four and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-stat-integer-scalar`, `event-query-stat-noncoindexed`, `event-query-stat-range`, `event-query-stat-intent-out`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-008: EVENT_QUERY assigns STAT zero when no error occurs

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** If STAT is present and no error condition occurs, STAT is assigned zero.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-stat-zero-on-success`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-009: EVENT_QUERY assigns positive STAT on error

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** If STAT is present and an error condition occurs, STAT is assigned a positive value.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-stat-positive-on-error`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-010: EVENT_QUERY error STAT value is processor dependent

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** The positive STAT value assigned for an EVENT_QUERY error condition is processor
dependent.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-stat-error-value-processor-dependent`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.82-011: EVENT_QUERY initiates error termination when STAT is absent on error

**Source:** S16.9.82, J3/24-007, 18 December 2023, physical PDF407-408. **Class:** Effect.

**Definition:** If STAT is absent and an error condition occurs, error termination is initiated.

**Diagnostic obligation:** not-required.

**Facets:** `event-query-stat-absent-error-termination`.

**Oracle:** Plans are pending and source-derived. EVENT_QUERY runtime execution is out of scope for
this suite's single-image fixtures by user decision: 16.9.82 requires an event variable,
and C1604 requires a named EVENT_TYPE variable to be a coarray. This packet therefore
records no local-event executable plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
EVENT_QUERY runtime facets are out of scope rather than no-oracle claims: the required
EVENT argument is an EVENT_TYPE event variable, and C1604 requires named EVENT_TYPE
variables to be coarrays, outside this suite's single-image fixture scope.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

<!-- END GENERATED 16.9.82 -->
