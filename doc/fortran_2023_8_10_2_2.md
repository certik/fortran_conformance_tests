# Fortran 2023 8.10.2.2: Common block storage sequence

The canonical catalogue is `doc/catalogues/common_statement_8_10_2_2.json`. Effective review state comes
from `Registry.catalogue_review_state("8.10.2.2")`; source accounting
does not approve cases, canonical relationships or an execution inventory.
This source-only packet introduces 4 pending facets and no cases.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`. The original section hash is
`2fb56d48e2ba86c2bb911630afdc205da77ac7b8cb216daf860899b1cc4f1433`. Physical page numbers are 1-based;
physical148/149 correspond to printed134/135.

The two original paragraphs are on physical148; the next heading,
8.10.2.3, begins physical149. p1 has two numbered formation steps, including
the tail-only extension restriction and the membership of associated
objects. p2 limits the contributing COMMON/EQUIVALENCE statements to the
actual scoping unit.

The ordered construction is tested only through future, fully defined
value effects. The separate tail-only restriction is assigned once to
S8.10.3-003, whose primary source is8.10.3p3; this catalogue retains the
explicit p1 source connection without inventing a second restriction or
duplicate negative. Associated-object membership and the scope of the
construction are definitions, not extra empty runtime classifiers.

The sequence is in language storage units under19.5.3.2. Array element
order follows9.5.3.3, including its lower-bound-dependent indexing, rather
than C row order or a processor's address layout. An EQUIVALENCE closure
can enlarge the tail, and8.10.2.3 includes that enlargement in block size.
Matching another scope requires the complete resulting sequence, not just
the objects textually present in COMMON.

USE/host access to an entity does not copy another scope's declaration
statements into this one. EQUIVALENCE8.10.1 retains its canonical syntax,
eligibility and storage-consistency rules and is not authored here. All
proposed direct effects and any later finite reuse of the list-continuation
parent require separately adjudicated complete programs and occurrences.

All pending plans require independent source/fixture/relationship review.
No compiler was invoked for this source packet. No F2018 observation
is promoted to F2023 qualification, and no model, kind, C companion,
launcher or processor-header prerequisite is inferred from missing data.
Existing real/BOZ, SAVE, TARGET, BIND and initialization owners remain
unchanged. The whole-source census and universal execution obligation
are not closed by these catalogues.

<!-- BEGIN GENERATED 8.10.2.2 -->

### S8.10.2.2-001: The per-scope sequence preserves order and includes the equivalence tail

**Source:** 8.10.2.2 p1, J3/24-007, 18 December 2023, physical PDF148. **Class:** Effect.

**Definition:** The block sequence concatenates its listed objects' storage sequences in appearance
order and includes all units brought in by eligible equivalence association. This is a
language storage-sequence construction, not an ABI layout measurement. Other scopes
supply matching views, not extra local lists.

**Diagnostic obligation:** not-required.

**Facets:** `list-order-finite-parent`, `array-element-order`, `direct-equivalence-tail`, `transitive-equivalence-tail`.

**Oracle:** Source-derived storage-unit closure, then actual same-type defined-value observations in
the complete future programs. Every future effect requires a complete driver and all
declaring/defining procedures, actual successful compile/link/run traces in the required
f2023 configuration, independent literal observations and executed-call/completion
checks. For named COMMON initial families, put consistent SAVE /block/ in every
nonexempt declaring procedure and match the complete storage sequences; avoid relying on
an accidentally active scope or implicit SAVE. Seed every observed storage unit with a
known sentinel before the operation under test, so omitted-write sensitivity never reads
undefined data. Keep actual arguments distinct from COMMON storage to avoid
dummy-aliasing violations. Wrong-order/value/omission/omitted-call mutations must fail
the whole counted parent, not a detached expression. No such program or mutation has
been implemented here.

**Oracle limitation:** Neither membership definitions nor a complete static graph are counted as runtime
effects. Zero-sized sequences contribute no payload; addresses, STORAGE_SIZE-to-byte
guesses and same-processor TRANSFER are not substitutes. All proposed evidence remains
PENDING. No case, direct/linked facet, SourceUse relationship, inventory renewal, source
approval, baseline change or universal conformance credit is created. Each
reference/target/configuration is separate; f2018 success cannot corroborate an f2023
expectation. Numerical-kind, real/BOZ/IEEE, coarray and companion/CFI-header gates
remain with their actual owners. Missing or unsupported prerequisite evidence cannot be
treated as success.

**Dependencies:** 8.10.1.1-.5;8.10.2.1p3/p4;8.10.2.3/.4/.5;8.10.3;8.5.16;9.2p2;9.5.3.3/Table9.1;15.5.2.14;19.5.3;19.6.5(12).

<!-- END GENERATED 8.10.2.2 -->
