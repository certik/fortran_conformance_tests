# Fortran 2023: 10.1.3 Precedence of operators

Catalogue: `doc/catalogues/precedence_of_operators_10_1_3.json`. The original source-only registration supplied pending plans; the bounded batch304 fixtures below supply selected cases and mutation/diagnostic plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN EXPRESSIONS 10.1.2.8-10.1.3 FIXTURES -->
## Batch304 expression fixtures for 10.1.3

Generated batch304 fixtures cover selected facets for: S10.1.3-001, S10.1.3-002, S10.1.3-003. Runtime cases use exact integer, logical, character LEN/value, and derived-operation component oracles. Diagnostic cases are line-anchored one-property malformed forms with compiled conforming controls. Feature mutations are permanent and checked on both the reference gfortran and frozen LFortran target. Facets not listed remain pending for the recorded reasons.
<!-- END EXPRESSIONS 10.1.2.8-10.1.3 FIXTURES -->

<!-- BEGIN GENERATED 10.1.3 -->

### S10.1.3-001: Intrinsic and extension operation categories have the Table 10.1 precedence order

**Source:** 10.1.3 p1 and Table 10.1; J3/24-007 PDF p172. **Class:** Effect.

**Definition:** For expressions formed under 10.1.2, intrinsic and extension operations are interpreted
according to the relative precedence categories in Table 10.1 unless parentheses change
the order in which operands are combined. The order is: defined unary, exponentiation,
multiplication/division, unary plus/minus, binary plus/minus, character concatenation,
relational, logical NOT, logical AND, logical OR, logical EQV/NEQV, and defined binary.

**Diagnostic obligation:** not-required.

**Facets:** `power-before-unary-minus`, `multiply-before-binary-plus`, `unary-before-binary-plus`, `character-before-relational`, `relational-before-logical-or`, `not-before-and`, `and-before-or`, `or-before-eqv`, `defined-binary-lowest`, `parentheses-override-category-precedence`.

**Oracle:** Use integer, character, and logical literals or non-associative defined operations to
make precedence observable. Do not infer grouping from an associative intrinsic
operation whose value would be unchanged.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Future fixtures must use independently derived literal or
inquiry oracles and must not infer evaluation order, short-circuiting, which operand was
evaluated, or whether an operand was evaluated. They must not rely on compiler
agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or a
floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted evaluation
or mathematically equivalent rewriting.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 10.1.2.1-10.1.2.9;10.1.5.1;10.1.5.2;10.1.5.3;10.1.5.4;10.1.5.5;10.1.6;10.1.8;15.4.3.4.2.

### S10.1.3-002: A defined operation has the precedence of its operator

**Source:** 10.1.3 p2; J3/24-007 PDF p172. **Class:** Effect.

**Definition:** When an operation is a defined operation, its precedence is not determined by the
defining procedure but by the operator token used for the operation. A defined operation
that extends an intrinsic operator uses that intrinsic operator's Table 10.1 precedence,
while dotted defined unary and defined binary operators use their extension rows.

**Diagnostic obligation:** not-required.

**Facets:** `extended-intrinsic-operator-precedence`, `defined-unary-operator-highest`, `defined-binary-operator-lowest`, `dotted-name-not-intrinsic-symbol-precedence`.

**Oracle:** Defined-operation precedence must be observed only through valid defined-operation
interfaces with literal-distinguishable, non-associative results. The future case may
not attribute generic resolution, distinguishability, or procedure binding failures to
this subclause.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Future fixtures must use independently derived literal or
inquiry oracles and must not infer evaluation order, short-circuiting, which operand was
evaluated, or whether an operand was evaluated. They must not rely on compiler
agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or a
floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted evaluation
or mathematically equivalent rewriting.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 10.1.2.4R1003-R1004;10.1.2.5R1008-R1010;10.1.2.9R1023-R1024/C1006;10.1.6.1-10.1.6.3;15.4.3.4.2.

### S10.1.3-003: The general expression form establishes same-class operand grouping for interpretation

**Source:** 10.1.3 p3; J3/24-007 PDF p172. **Class:** Effect.

**Definition:** Within a syntactic class, the general expression form in 10.1.2 establishes the order in
which operands are combined for interpreting the expression, unless parentheses change
that order. This is an interpretation rule; it does not require any particular runtime
evaluation order for operand computations.

**Diagnostic obligation:** not-required.

**Facets:** `left-grouped-subtraction`, `left-grouped-division`, `right-grouped-exponentiation`, `left-grouped-concatenation-boundary`, `logical-same-class-grouping-boundary`, `parentheses-override-same-class-grouping`.

**Oracle:** Use non-associative integer operations or valid non-associative defined extensions when
grouping must be observable. Literal equality for associative intrinsic operations is
not evidence of grouping.

Batch304 expressions 10.1.2.8-10.1.3 fixtures: the batch304 fixtures observe the
selected facets with exact integer, logical, character LEN/value, or derived-operation
integer component assertions. Each selected runtime facet has a distinct assertion and a
conforming feature mutation that changes an operator, operand, or parenthesization and
fails at run time on both toolchains; selected diagnostic facets have one-property
compiled controls.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Future fixtures must use independently derived literal or
inquiry oracles and must not infer evaluation order, short-circuiting, which operand was
evaluated, or whether an operand was evaluated. They must not rely on compiler
agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or a
floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted evaluation
or mathematically equivalent rewriting.

Batch304 expressions 10.1.2.8-10.1.3 boundaries: Only the selected facets are
discharged. Existing source review state, unrelated bindings, approval state, and
rollout progress are not changed. Remaining pending facets stay pending for the recorded
reasons. No oracle depends on operand evaluation order, short-circuiting, side effects,
address identity, real rounding, or compiler consensus.

**Dependencies:** 10.1.2.5R1005-R1010;10.1.2.6R1011-R1012;10.1.2.8R1015-R1022;10.1.2.9R1023;10.1.5.2;10.1.5.3;10.1.5.4;10.1.6;10.1.7;10.1.8;15.4.3.4.2.

<!-- END GENERATED 10.1.3 -->
