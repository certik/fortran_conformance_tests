# Fortran 2023 18.5.4: Macros and typedefs in ISO_Fortran_binding.h

The canonical catalogue is `doc/catalogues/macros_and_typedefs_iso_fortran_binding_18_5_4.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, note1, p3, p4, p5, p6, table18.3, p7, p8, p9, p10, table18.4, note2, p11, p12, table18.5. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 18.5.4 -->

### S18.5.4-001: Non-CFI_CDESC_T macros are preprocessing-suitable integer constant expressions

**Source:** S18.5.4 p1; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** Except for CFI_CDESC_T, each macro defined in ISO_Fortran_binding.h expands to an
integer constant expression that is a single token or a parenthesized expression
suitable for preprocessing conditional directives.

**Diagnostic obligation:** not-required.

**Facets:** `binding-macros-integer-constant-expressions`, `binding-macros-preprocessing-suitable`, `binding-macros-single-token-or-parenthesized`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-002: CFI_CDESC_T creates suitably sized and aligned descriptor storage castable to CFI_cdesc_t *

**Source:** S18.5.4 p2 CFI_CDESC_T storage; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** CFI_CDESC_T is a function-like macro whose rank argument selects an unqualified type of
suitable size and alignment for a C descriptor variable of that rank, and a pointer to
such a variable can be cast to CFI_cdesc_t *.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-cdesc-t-function-like-macro`, `cfi-cdesc-t-rank-sized-storage`, `cfi-cdesc-t-suitable-alignment`, `cfi-cdesc-t-pointer-cast-to-cfi-cdesc-pointer`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-003: CFI_CDESC_T rank argument is a bounded integer constant expression

**Source:** S18.5.4 p2 rank argument restriction; J3/24-007, 18 December 2023, physical PDF533. **Class:** Restriction.

**Definition:** The argument to CFI_CDESC_T shall be an integer constant expression whose value is at
least zero and at most CFI_MAX_RANK.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-cdesc-t-rank-argument-integer-constant-expression`, `cfi-cdesc-t-rank-argument-nonnegative`, `cfi-cdesc-t-rank-argument-at-most-cfi-max-rank`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-004: A CFI_CDESC_T variable shall not have an initializer

**Source:** S18.5.4 p2 initializer restriction; J3/24-007, 18 December 2023, physical PDF533. **Class:** Restriction.

**Definition:** A variable declared using CFI_CDESC_T shall not have an initializer.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-cdesc-t-variable-no-initializer`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-005: CFI_index_t is a signed integer type capable of representing pointer differences

**Source:** S18.5.4 p3; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** CFI_index_t is a typedef name for a standard signed integer type capable of representing
the result of subtracting two pointers.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-index-t-standard-signed-integer`, `cfi-index-t-represents-pointer-difference`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-006: CFI_MAX_RANK is at least 15 and CFI_rank_t represents supported ranks

**Source:** S18.5.4 p4 required rank properties; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** CFI_MAX_RANK is the largest supported rank with value at least 15, and CFI_rank_t is a
standard integer type capable of representing the largest supported rank.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-max-rank-at-least-fifteen`, `cfi-rank-t-represents-cfi-max-rank`, `cfi-rank-t-standard-integer-type`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-007: The exact CFI_MAX_RANK value above the lower bound is processor dependent

**Source:** S18.5.4 p4 rank latitude; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** The CFI_MAX_RANK macro has a processor-dependent value equal to the largest rank
supported, subject to the required lower bound of 15.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-max-rank-exact-value-processor-dependent`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-008: CFI_VERSION has a processor-dependent header-version value

**Source:** S18.5.4 p5 version value; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** CFI_VERSION has a processor-dependent value that encodes the version of the
ISO_Fortran_binding.h source file containing the macro.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-version-macro-defined`, `cfi-version-exact-value-processor-dependent`, `descriptor-version-compares-to-header-cfi-version`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-009: CFI_VERSION should increase for incompatible header revisions

**Source:** S18.5.4 p5 recommendation; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** CFI_VERSION should be increased for a new ISO_Fortran_binding.h version that is
incompatible with the previous version.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-version-increase-recommendation`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-010: Attribute code macros are nonnegative, distinct, and representable by CFI_attribute_t

**Source:** S18.5.4 p6 and Table 18.3; J3/24-007, 18 December 2023, physical PDF533. **Class:** Effect.

**Definition:** CFI_attribute_pointer, CFI_attribute_allocatable, and CFI_attribute_other are
attribute-code macros whose values are nonnegative and distinct, and CFI_attribute_t is
a standard integer type capable of representing those values.

**Diagnostic obligation:** not-required.

**Facets:** `attribute-code-macros-present`, `attribute-code-values-nonnegative`, `attribute-code-values-pairwise-distinct`, `cfi-attribute-t-represents-attribute-codes`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-011: Attribute code macros identify pointer, allocatable, and other data objects

**Source:** S18.5.4 p7 and Table 18.3; J3/24-007, 18 December 2023, physical PDF533-534. **Class:** Effect.

**Definition:** CFI_attribute_pointer specifies a data object with the Fortran POINTER attribute,
CFI_attribute_allocatable specifies an object with the Fortran ALLOCATABLE attribute,
and CFI_attribute_other specifies a nonallocatable nonpointer object.

**Diagnostic obligation:** not-required.

**Facets:** `attribute-pointer-specifies-data-pointer`, `attribute-allocatable-specifies-allocatable`, `attribute-other-specifies-nonallocatable-nonpointer`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-012: CFI_type_other and CFI_type_struct have fixed sign and distinctness requirements

**Source:** S18.5.4 p8 other and struct values; J3/24-007, 18 December 2023, physical PDF534. **Class:** Effect.

**Definition:** CFI_type_other has a negative value distinct from all other type specifiers, and
CFI_type_struct has a positive value distinct from all other type specifiers and
specifies an interoperable C structure.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-type-other-negative`, `cfi-type-other-distinct`, `cfi-type-struct-positive`, `cfi-type-struct-distinct`, `cfi-type-struct-interoperable-structure`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-013: Listed CFI_type_* macros identify listed C types with support-dependent signs

**Source:** S18.5.4 p8 and Table 18.4 listed C types; J3/24-007, 18 December 2023, physical PDF534. **Class:** Effect.

**Definition:** Each Table 18.4 macro is a type specifier for the listed C type; its value is positive
for a C type interoperable with a Fortran type and kind supported by the processor and
negative for a listed C type not interoperable with any supported Fortran type and kind.

**Diagnostic obligation:** not-required.

**Facets:** `listed-type-macros-present`, `listed-type-macros-map-to-c-types`, `supported-listed-type-macros-positive`, `unsupported-listed-type-macros-negative`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-014: Exact listed CFI_type_* numeric values and aliases are processor latitude except where constrained

**Source:** S18.5.4 p8 type-code latitude; J3/24-007, 18 December 2023, physical PDF534. **Class:** Effect.

**Definition:** For listed C type specifiers other than the stated sign rules and the distinctness of
CFI_type_other and CFI_type_struct, the exact numeric values and possible aliases among
different listed C types are not fixed by this subclause.

**Diagnostic obligation:** not-required.

**Facets:** `listed-type-code-exact-values-not-fixed`, `listed-type-code-aliases-not-forbidden`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-015: Processor-supported extra interoperable C types require positive distinct specifiers

**Source:** S18.5.4 p9; J3/24-007, 18 December 2023, physical PDF534. **Class:** Effect.

**Definition:** Processor support for an interoperable C type not listed in Table 18.4 is extension
latitude, and such a supported extra C type shall have a positive type specifier value
distinct from all other type specifiers.

**Diagnostic obligation:** not-required.

**Facets:** `extra-c-type-support-processor-latitude`, `extra-c-type-specifier-positive`, `extra-c-type-specifier-distinct`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-016: CFI_type_t represents supported type specifier values

**Source:** S18.5.4 p10; J3/24-007, 18 December 2023, physical PDF534. **Class:** Effect.

**Definition:** CFI_type_t is a typedef name for a standard integer type capable of representing the
values for supported type specifiers.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-type-t-standard-integer-type`, `cfi-type-t-represents-supported-type-specifiers`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-017: Standard error code macros have fixed zero, nonzero, and distinctness relations

**Source:** S18.5.4 p11 error-code values and Table 18.5 names; J3/24-007, 18 December 2023, physical PDF535. **Class:** Effect.

**Definition:** CFI_SUCCESS is zero, and every Table 18.5 error macro other than CFI_SUCCESS is nonzero
and has a value different from the other macros specified in 18.5.4.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-success-zero`, `standard-error-codes-nonzero`, `standard-error-codes-pairwise-distinct`, `standard-error-code-macros-present`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-018: Nonstandard descriptor-function errors should use codes distinct from standard Table 18.5 codes

**Source:** S18.5.4 p11 recommendation; J3/24-007, 18 December 2023, physical PDF535. **Class:** Effect.

**Definition:** Error conditions other than those listed in 18.5.4 should be indicated by error codes
different from the standard Table 18.5 macro values.

**Diagnostic obligation:** not-required.

**Facets:** `nonstandard-error-codes-distinct-recommendation`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.4-019: Table 18.5 error code values indicate the described error conditions

**Source:** S18.5.4 p12 and Table 18.5 error conditions; J3/24-007, 18 December 2023, physical PDF535. **Class:** Effect.

**Definition:** The Table 18.5 macro values indicate the named conditions: success, null or nonnull base
address errors, invalid element length, rank, type, attribute, extent, or descriptor,
memory allocation failure, and out-of-bounds reference.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-success-means-no-error`, `error-base-addr-null-condition`, `error-base-addr-not-null-condition`, `invalid-elem-len-condition`, `invalid-rank-condition`, `invalid-type-condition`, `invalid-attribute-condition`, `invalid-extent-condition`, `invalid-descriptor-condition`, `error-mem-allocation-condition`, `error-out-of-bounds-condition`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

<!-- END GENERATED 18.5.4 -->
