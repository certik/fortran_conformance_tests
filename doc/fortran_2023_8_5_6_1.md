# Fortran 2023 8.5.6.1: General

**Independently source-reviewed; fixture plans remain pending.** Effective
content-bound status is reported by `Registry.catalogue_review_state("8.5.6.1")`,
not the raw `review_state` field alone. Source adjudication and the separate
inventory-context renewal are recorded in `doc/source_audits/batch_035.json`.
This source-only integration adds no fixture, compiler observation or
source-use instance.

The source of truth is `doc/catalogues/codimension_general_8_5_6_1.json`.
Original J3/24-007 is dated December 18, 2023, has688 pages and SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The ten original units span physical PDF121-122. Their section hash is
`b5fa470e7e0048de7c783f29fc60159a42f36f9e54a60ebb2005dc0f20d60949`.
Parent8.5.6 is only the structural CODIMENSION heading, with no source units;
it receives no empty catalogue or no-op program.

| Original units | Treatment |
| --- | --- |
| `p1` | Coarray/corank/cobound definition, not remote execution evidence |
| `R809` | Deferred-list and explicit-coshape grammar alternatives |
| `C822` | Rank plus corank, not extents/image count, is at most15 |
| `C823`-`C826` | Exact entity/type/potential-subobject/lifetime predicates |
| `note1`-`note3` | Original component, automatic/SAVE and declaration examples, with complete-context qualifications |

Pointer targets are not arbitrary remotely readable subobjects. Potential
subobject recursion differs from ultimate-component recursion; C753's
component constraints are not waived by a root ALLOCATABLE/SAVE context.
The C_PTR/C_FUNPTR/TEAM_TYPE exclusions refer to actual intrinsic-module
type identity, not same-spelled user types or every containing type.

The automatic-coarray example cannot be repaired just by adding SAVE.
Conversely, nonconstant **cobounds** alone are not8.3's automatic-object
predicate, which refers to type parameters or ordinary array bounds.
Dummy and associate-name routes keep their own allocation, association,
corank and establishment conditions.

Existing C751/C752 and cross-image context cases retain their IDs, inputs,
roles and reviews. The documented GNU14.4.0, OpenCoarrays2.10.3 and
MPICH4.3.2 calibration is historical configuration-specific evidence, not
GNU16 runtime evidence. It is neither mixed with another toolchain nor
rerun here. New multi-image plans require actual matched images/runtime,
defined data and valid synchronization; compile acceptance is not remote
behavior and unsupported coarray setup is not a causal diagnostic.

Definitions below are generated from the catalogue. Source-use plans and
inventories, including reviewed ones, create no facet-completion credit.

<!-- BEGIN GENERATED 8.5.6.1 -->

### R809: Coarray specifications use deferred-list or explicit-coshape syntax

**Source:** 8.5.6.1 R809, physical PDF121, lines22-23 **Class:** Syntax.

**Definition:** A coarray-spec is either a nonempty deferred-coshape-spec list or an
explicit-coshape-spec. The declaration's ALLOCATABLE and entity context determine which
route is eligible; ordinary array shape is a separate specification.

**Diagnostic obligation:** required.

**Facets:** `deferred-admission`, `explicit-admission`, `empty-specification`, `rank-corank-source-use`.

**Oracle:** Qualified grammar admission/report-control plans; every repair is otherwise conforming
and selected coarray configuration must actually support the language context.

**Oracle limitation:** Compile admission is not cross-image behavior. Unsupported/disabled coarray facilities,
toolchain/runtime mismatches, echoes, crashes and recovery do not diagnose this grammar.

**Dependencies:** 8.2;8.5.6.2 R810/C827;8.5.6.3 R811/C828;7.5.4.1 C751;4.2.

### C822: The sum of ordinary rank and corank is at most fifteen

**Source:** 8.5.6.1 C822, physical PDF121, line24 **Class:** Restriction.

**Definition:** An entity's ordinary rank plus its corank cannot exceed15. Data extents, total element
count, cobound values and image count do not substitute for either rank count.

**Diagnostic obligation:** required.

**Facets:** `sum-sixteen`, `sum-fifteen`, `smaller-sums`, `extent-not-rank`.

**Oracle:** Count actual ordinary dimensions and codimensions in complete declarations, with focused
dimension-removal repairs and separately qualified report causes.

**Oracle limitation:** No guessed storage sizes, kind widths, image-count substitution or new runtime profile.
Ordinary0/nonzero reporting capability, not forced fatal rejection or a hardcoded
diagnostic code.

**Dependencies:** 5.4.7;8.5.8;R809-R813;C826-C828;4.2.

### C823: A coarray is a component or a non-result variable

**Source:** 8.5.6.1 C823, physical PDF121, line25 **Class:** Restriction.

**Definition:** A coarray is a component or a variable that is not a function result. The permitted
categories retain their own component, allocation, lifetime and argument constraints.

**Diagnostic obligation:** required.

**Facets:** `variable-admission`, `component-owner-source-use`, `function-result-excluded`, `other-role-source-use`.

**Oracle:** Role-specific source and compile plans with a genuinely controlled function-result
relation.

**Oracle limitation:** No unallocated result use, expired pointer target, missing interface or unsupported
facility can corroborate C823. Source admission and execution evidence remain distinct.

**Dependencies:** 8.2 C808/C809;7.5.4.1 C751;C826/C827;15.4/15.6.2.2;4.2.

### C824: Three intrinsic-module types cannot be coarray types

**Source:** 8.5.6.1 C824, physical PDF121, lines26-27 **Class:** Restriction.

**Definition:** A coarray cannot have the intrinsic-module type C_PTR, C_FUNPTR or TEAM_TYPE. The
restriction concerns actual type identity, not identifier spelling or a recursive ban on
every containing type.

**Diagnostic obligation:** required.

**Facets:** `c-ptr`, `c-funptr`, `team-type`, `module-identity-source-use`, `wrapped-handles-source-use`.

**Oracle:** Three exact type-identity report/control families and canonical source boundaries, with
otherwise eligible coarray declarations.

**Oracle limitation:** No new representation profile, C companion, team-value fabrication or remote
opaque-value trap. The historical GNU14.4 calibration is not GNU16 evidence and is not
rerun.

**Dependencies:** 18.3.2;16.10.2.34;7.5.4.1 C752;5.4.7;C826/C828;10.2.1.3 and19.6 for the retained
context-only case.

### C825: Coarray-potential-subobject types exclude pointer, coarray and result entities

**Source:** 8.5.6.1 C825, physical PDF121, lines28-29 **Class:** Restriction.

**Definition:** An entity whose type has a coarray potential subobject component is neither a pointer, a
coarray nor a function result. Potential-subobject recursion follows7.5.1p5; it is not
the ultimate-component rule or arbitrary pointer reachability. C753 imposes additional
restrictions on data components of such types.

**Diagnostic obligation:** required.

**Facets:** `pointer-excluded`, `coarray-excluded`, `function-result-gate`, `recursive-potential-source-use`, `eligible-root-contexts`.

**Oracle:** Focused forbidden root properties with complete legal component definitions and repaired
contexts; recursive source relationships remain separately qualified.

**Oracle limitation:** C753/component violations, missing C826 lifetime attributes, incomplete function
interfaces and unsupported coarray components are not substitute causes. No raw
reachability graph or new SourceUses instance is authored.

**Dependencies:** 7.5.1p3-p6;7.5.4.1 C751/C753;C826;8.5.16;15.4/15.6.2.2.

### C826: Coarray-bearing objects need an allowed lifetime or association context

**Source:** 8.5.6.1 C826, physical PDF122, lines1-2 **Class:** Restriction.

**Definition:** A coarray or object with a coarray potential subobject component is an associate name or
dummy argument, or has ALLOCATABLE or SAVE. These alternatives do not waive C823/C825,
component constraints, automatic-object restrictions or the specific association rules.

**Diagnostic obligation:** required.

**Facets:** `local-scalar-save`, `local-array-save`, `potential-object-save`, `dummy-and-allocatable-admission`, `associate-name-source-use`, `automatic-and-implied-save-boundaries`.

**Oracle:** Three missing-SAVE report/control families with constant data characteristics, plus
finite allowed-route/source-boundary plans.

**Oracle limitation:** No declaration-time coarray value read, false automatic-object repair, implicit entry
barrier, missing association state or unsupported configuration is accepted. All future
multi-image behavior needs a matched compiler/runtime/launcher and defined, ordered
data; compile controls do not supply that evidence.

**Dependencies:** 3.151.2;5.4.8;7.5.1/7.5.4.1;8.2 C808;8.3 C814;8.4;8.5.16;11.1.3.3/11.1.5;15.5;19.5.1.6.

<!-- END GENERATED 8.5.6.1 -->
