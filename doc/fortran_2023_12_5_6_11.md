# Fortran 2023 12.5.6.11: FORM= specifier - OPEN specifier fixtures

The canonical catalogue is `doc/catalogues/form_specifier_12_5_6_11.json`. This author packet adds finite runtime fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 2 facets in 2 program(s); 2 of 5 facets are represented and 3 remain pending.

<!-- BEGIN GENERATED 12.5.6.11 -->

### S12.5.6.11-001: FORM= value is FORMATTED or UNFORMATTED

**Source:** S12.5.6.11, J3/24-007, 18 December 2023, physical PDF254. **Class:** Effect.

**Definition:** The FORM= scalar default character expression shall evaluate to FORMATTED or
UNFORMATTED.

**Diagnostic obligation:** not-required.

**Facets:** `form-value-set-formatted-unformatted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.11-002: FORM= determines formatted versus unformatted connection

**Source:** S12.5.6.11, J3/24-007, 18 December 2023, physical PDF254. **Class:** Effect.

**Definition:** FORM= determines whether the file is connected for formatted or unformatted
input/output.

**Diagnostic obligation:** not-required.

**Facets:** `form-determines-formatted-unformatted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

OPEN-specifier fixture implementation: 1 valid run-phase program(s) cover
`form-determines-formatted-unformatted`. Runtime oracles use run-directory or scratch
files created before STATUS='OLD', INQUIRE tokens, guarded reads with sentinels, exact
integer/character/logical values, and only IOSTAT zero; no IOMSG text, nonzero IOSTAT
number, processor-dependent file case behavior, optional plus sign, or ambiguous real
formatting is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.11-003: Omitted FORM= default depends on access method

**Source:** S12.5.6.11, J3/24-007, 18 December 2023, physical PDF254. **Class:** Effect.

**Definition:** If FORM= is omitted, the default is UNFORMATTED for direct or stream access and
FORMATTED for sequential access.

**Diagnostic obligation:** not-required.

**Facets:** `form-default-by-access`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

OPEN-specifier fixture implementation: 1 valid run-phase program(s) cover
`form-default-by-access`. Runtime oracles use run-directory or scratch files created
before STATUS='OLD', INQUIRE tokens, guarded reads with sentinels, exact
integer/character/logical values, and only IOSTAT zero; no IOMSG text, nonzero IOSTAT
number, processor-dependent file case behavior, optional plus sign, or ambiguous real
formatting is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.11-004: Existing file FORM= value must be allowed for the file

**Source:** S12.5.6.11, J3/24-007, 18 December 2023, physical PDF254. **Class:** Restriction.

**Definition:** For an existing file, the specified form shall be included in the set of allowed forms
for the file.

**Diagnostic obligation:** not-required.

**Facets:** `form-existing-file-form-allowed`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.11-005: New files are created with the specified FORM= value allowed

**Source:** S12.5.6.11, J3/24-007, 18 December 2023, physical PDF254. **Class:** Effect.

**Definition:** For a new file, the processor creates the file with a set of allowed forms that includes
the specified form.

**Diagnostic obligation:** not-required.

**Facets:** `form-new-file-form-included`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.5.6.11 -->

## OPEN specifier fixture derivations

### `form_modes` / `S12.5.6.11-002`

**Facets:** `form-determines-formatted-unformatted`.

12.5.6.11 p1 fixes omitted FORM by ACCESS, and specified FORM determines matching formatted or unformatted transfers; existing/new files are created before reopening and exact values are read back.

Feature mutations: `sequential-default-form-changed-to-unformatted`, `direct-default-form-changed-to-formatted`, `stream-default-form-changed-to-formatted`, `explicit-formatted-changed-to-unformatted`, `existing-form-open-replaces-file`, `new-unformatted-changed-to-formatted`.

### `form_defaults_by_access` / `S12.5.6.11-003`

**Facets:** `form-default-by-access`.

12.5.6.11 p1 makes omitted FORM default FORMATTED for sequential access and UNFORMATTED for direct or stream access.

Feature mutations: `sequential-default-form-made-unformatted`, `direct-default-form-made-formatted`, `stream-default-form-made-formatted`.
