# Fortran 2023: 11.1.9.2 Execution of a SELECT CASE construct

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.1.9.2 -->

### S11.1.9.2-001: SELECT CASE evaluates its case expression and a range list matches if any range matches

**Source:** 11.1.9.2 p1 introductory text; J3/24-007 PDF p220. **Class:** Effect.

**Definition:** Executing SELECT CASE evaluates the case expression. A case value range list matches if
the case expression value matches any range in the list.

**Diagnostic obligation:** not-required.

**Facets:** `case-expression-value-selects`, `range-list-any-range-matches`.

**Oracle:** Observe block selection, not selector evaluation.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** 11.1.9.1 in this packet;10.1 expressions registered.

### S11.1.9.2-002: Single CASE values match by .EQV. for logical and equality for other types

**Source:** 11.1.9.2 p1 item (1); J3/24-007 PDF p220. **Class:** Effect.

**Definition:** A single logical case value v matches c when c .EQV. v is true; for other allowed types
a single value matches when c == v is true.

**Diagnostic obligation:** not-required.

**Facets:** `logical-true-false-single-values`, `integer-single-value-equality`, `character-single-value-equality`, `enum-single-value-source`.

**Oracle:** Equality operations are dependencies; this rule owns their use for CASE matching.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** 10.1.5.4/.5 registered;7.4.4.4 registered;7.6 registered;16 LEN intrinsic NOT YET
REGISTERED for planned character-length controls.

### S11.1.9.2-003: Colon CASE ranges match bounded, lower-open, and upper-open intervals

**Source:** 11.1.9.2 p1 items (2)-(4); J3/24-007 PDF p220. **Class:** Effect.

**Definition:** low:high matches when low <= c and c <= high; low: matches when low <= c; :high matches
when c <= high.

**Diagnostic obligation:** not-required.

**Facets:** `bounded-range-inside`, `bounded-range-outside`, `low-open-range`, `high-open-range`.

**Oracle:** Use allowed CASE types only; logical colon ranges are C1153 violations.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** C1153 in this packet;10.1.5.5.1 registered.

### S11.1.9.2-004: CASE DEFAULT matches only if no other selector matches; without DEFAULT there may be no match

**Source:** 11.1.9.2 p1 items (5)-(6); J3/24-007 PDF p220. **Class:** Effect.

**Definition:** If no other selector matches and DEFAULT appears, DEFAULT matches; if no other selector
matches and DEFAULT does not appear, there is no match.

**Diagnostic obligation:** not-required.

**Facets:** `default-selected-after-no-value-match`, `value-match-preempts-default`, `no-match-without-default`.

**Oracle:** Observe final sentinel values only.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** C1151 in this packet;11.1.9.2 p2 in this packet.

### S11.1.9.2-005: The block after the matching CASE statement executes and completes SELECT CASE execution

**Source:** 11.1.9.2 p2; J3/24-007 PDF p220. **Class:** Effect.

**Definition:** The block following the CASE statement containing the matching selector, if any, is
executed, and this completes execution of the construct.

**Diagnostic obligation:** not-required.

**Facets:** `matching-case-block-executes`, `nonmatching-blocks-not-executed`, `construct-completes-after-selected-block`.

**Oracle:** Which block executed is observable; selector evaluation is not used as evidence.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future selection fixtures must preload targets with sentinels distinct from every block
write and observe only final values to prove which block executed. They must not infer
condition, selector, or case-value evaluation from side effects, call counts,
short-circuiting, deliberate errors, temporaries, or evaluation order. Character CASE
plans must control blank padding with equal lengths or explicit LEN checks. No plan may
assume a particular real or complex kind, use an intrinsic that duplicates the feature
under test, rely on compiler agreement, or require permission text to be exercised.

**Dependencies:** 11.1.9.1 p1 in this packet;11.1.1 blocks registered.

### S11.1.9.2-006: Branching to END SELECT is permitted only within its SELECT CASE construct

**Source:** 11.1.9.2 p3; J3/24-007 PDF p220. **Class:** Restriction.

**Definition:** It is permissible to branch to an end-select-stmt only from within its SELECT CASE
construct; an outside branch to that END SELECT is not permitted.

**Diagnostic obligation:** context-dependent.

**Facets:** `internal-end-select-branch-control`, `outside-end-select-branch-rejected`.

**Oracle:** The word only supplies the outside-branch prohibition; internal branching is permission.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Branching fixtures must isolate source and target locations, keep labels and construct
names otherwise valid, and avoid evaluation-order, side-effect, image-ordering, or
message-wording oracles.

**Dependencies:** 8 labels registered;11.2 branch statements NOT YET REGISTERED;11.1.2.1 registered.

<!-- END GENERATED 11.1.9.2 -->
