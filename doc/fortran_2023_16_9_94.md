# Fortran 2023 16.9.94: GET ENVIRONMENT VARIABLE

The canonical catalogue is `doc/catalogues/get_environment_variable_16_9_94.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.94")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.94 -->

### S16.9.94-001: GET_ENVIRONMENT_VARIABLE argument restrictions

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Restriction.

**Definition:** NAME and VALUE and ERRMSG shall be default character scalars, LENGTH and STATUS shall be
integer scalars with decimal exponent range at least four, TRIM_NAME shall be a logical
scalar, and the listed intents apply.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-NAME-default-character-scalar`, `GET_ENVIRONMENT_VARIABLE-VALUE-default-character-scalar`, `GET_ENVIRONMENT_VARIABLE-LENGTH-integer-scalar-range`, `GET_ENVIRONMENT_VARIABLE-STATUS-integer-scalar-range`, `GET_ENVIRONMENT_VARIABLE-TRIM_NAME-logical-scalar`, `GET_ENVIRONMENT_VARIABLE-ERRMSG-default-character-scalar`, `GET_ENVIRONMENT_VARIABLE-argument-intents`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-002: GET_ENVIRONMENT_VARIABLE NAME case interpretation is processor dependent

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** The interpretation of case in NAME is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-NAME-case-processor-dependent`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-003: GET_ENVIRONMENT_VARIABLE assigns VALUE and LENGTH from environment state

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** VALUE receives the specified environment variable value or all blanks, and LENGTH
receives that value length or zero according to environment existence, value presence,
and environment-variable support.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-VALUE-assigned-value-or-blanks`, `GET_ENVIRONMENT_VARIABLE-LENGTH-value-length-or-zero`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-004: GET_ENVIRONMENT_VARIABLE STATUS uses the standard result codes

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** STATUS is zero for existing variables with no value, successful assignment, or absent
VALUE; -1 for too-short VALUE; 1 for nonexistence; 2 for no environment-variable
support; and processor-dependent values greater than 2 for other errors.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-STATUS-zero-cases`, `GET_ENVIRONMENT_VARIABLE-STATUS-minus-one-for-truncation`, `GET_ENVIRONMENT_VARIABLE-STATUS-one-for-nonexistence`, `GET_ENVIRONMENT_VARIABLE-STATUS-two-for-no-support`, `GET_ENVIRONMENT_VARIABLE-STATUS-greater-than-two-processor-dependent`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-005: GET_ENVIRONMENT_VARIABLE TRIM_NAME controls trailing blanks in NAME

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** With TRIM_NAME absent or true, trailing blanks in NAME are not part of the environment
variable name; with TRIM_NAME false, trailing blanks are significant on processors that
support such names.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-TRIM_NAME-default-or-true-trims`, `GET_ENVIRONMENT_VARIABLE-TRIM_NAME-false-considers-trailing-blanks`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-006: GET_ENVIRONMENT_VARIABLE trailing-blank NAME support is processor dependent

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** Processor support for trailing blanks in environment variable names controls whether
TRIM_NAME false makes those blanks significant.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-trailing-blank-name-support-processor-dependent`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-007: GET_ENVIRONMENT_VARIABLE ERRMSG changes only for positive STATUS

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** ERRMSG receives a processor-dependent explanatory message for a positive STATUS
condition and otherwise remains unchanged.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-ERRMSG-assigned-for-positive-status`, `GET_ENVIRONMENT_VARIABLE-ERRMSG-unchanged-without-positive-status`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-008: GET_ENVIRONMENT_VARIABLE positive-status message text is processor dependent

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** The ERRMSG explanatory message for a positive STATUS condition is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-ERRMSG-text-processor-dependent`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.94-009: GET_ENVIRONMENT_VARIABLE cross-image environment state is processor dependent

**Source:** S16.9.94, J3/24-007, 18 December 2023, physical PDF414. **Class:** Effect.

**Definition:** Across images, it is processor dependent whether an environment variable exists on
another image and whether values are the same or different.

**Diagnostic obligation:** not-required.

**Facets:** `GET_ENVIRONMENT_VARIABLE-cross-image-existence-processor-dependent`, `GET_ENVIRONMENT_VARIABLE-cross-image-value-processor-dependent`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

<!-- END GENERATED 16.9.94 -->
