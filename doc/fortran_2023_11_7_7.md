# Fortran 2023: 11.7.7 EVENT POST statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.7.7 -->

### S11.7.7-001: EVENT POST posts an event

**Source:** 11.7.7 p1; J3/24-007 PDF p236. **Class:** Effect.

**Definition:** The EVENT POST statement posts an event.

**Diagnostic obligation:** not-required.

**Facets:** `event-post-purpose-source-control`.

**Oracle:** This purpose sentence classifies the statement effect. It is not a standalone executable
oracle for event delivery.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1174;11.7.8 EVENT WAIT in this packet;16 ISO_FORTRAN_ENV EVENT_TYPE NOT YET REGISTERED.

### R1174: EVENT POST statement syntax

**Source:** 11.7.7 R1174; J3/24-007 PDF p236. **Class:** Syntax.

**Definition:** An event-post-stmt is EVENT POST with an event-variable and an optional sync-stat-list.

**Diagnostic obligation:** required.

**Facets:** `event-post-minimal-form`, `event-post-sync-stat-list-form`.

**Oracle:** This numbered grammar rule admits source form only; it does not prove that an event was
posted.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1175;11.7.11 sync-stat in this packet;16 EVENT_TYPE NOT YET REGISTERED.

### R1175: Event variables are scalar variables

**Source:** 11.7.7 R1175; J3/24-007 PDF p236. **Class:** Syntax.

**Definition:** An event-variable is a scalar variable.

**Diagnostic obligation:** required.

**Facets:** `event-variable-scalar-form`.

**Oracle:** This numbered grammar rule owns scalar variable form; EVENT_TYPE identity is C1183.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 9 variables registered;7 scalar concepts registered;16 EVENT_TYPE NOT YET REGISTERED.

### C1183: EVENT POST event variables have EVENT_TYPE

**Source:** 11.7.7 C1183; J3/24-007 PDF p236. **Class:** Restriction.

**Definition:** An event-variable shall be of type EVENT_TYPE from the intrinsic module ISO_FORTRAN_ENV.

**Diagnostic obligation:** required.

**Facets:** `event-type-control`, `non-event-type-event-variable-rejected`.

**Oracle:** The negative/control plan changes only the declared type of the event-variable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 16 ISO_FORTRAN_ENV EVENT_TYPE NOT YET REGISTERED;R1175 in this packet.

### S11.7.7-002: EVENT POST event-variable independence from status variables

**Source:** 11.7.7 p2; J3/24-007 PDF p236. **Class:** Restriction.

**Definition:** The event-variable shall not depend on the value of a stat-variable or errmsg-variable.

**Diagnostic obligation:** not-required.

**Facets:** `event-variable-stat-errmsg-independence-source-control`, `event-variable-stat-dependence-source-control`, `event-variable-errmsg-dependence-source-control`.

**Oracle:** The two p2 dependency prohibitions are program obligations recorded as source-control
facets only. No conforming processor diagnostic is required, and no rejection facet is
claimed.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 sync-stat in this packet;19 association/dependence terminology NOT YET
REGISTERED.

### S11.7.7-003: EVENT POST success increments the event count and does not wait for EVENT WAIT

**Source:** 11.7.7 p3; J3/24-007 PDF p236. **Class:** Effect.

**Definition:** Successful EVENT POST atomically increments the event variable count by one; on an
error, the count value is processor dependent; completion does not depend on execution
of a corresponding EVENT WAIT.

**Diagnostic obligation:** not-required.

**Facets:** `event-post-increment-source-control`, `event-post-error-count-processor-dependent`, `event-post-no-wait-dependency-source-control`.

**Oracle:** Atomicity, the event count, and wait independence are synchronization effects. The
processor-dependent error count is recorded as latitude, not as a required value.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.8 EVENT WAIT in this packet;11.7.2 segments UNREGISTERED (parallel);16 EVENT_TYPE
NOT YET REGISTERED.

<!-- END GENERATED 11.7.7 -->
