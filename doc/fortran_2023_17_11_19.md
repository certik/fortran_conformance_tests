# Fortran 2023 17.11.19: IEEE_MAX_NUM

The canonical catalogue is `doc/catalogues/ieee_max_num_17_11_19.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.19")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.19 -->

### S17.11.19-001: IEEE_MAX_NUM description defines maximum numeric value

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Effect.

**Definition:** IEEE_MAX_NUM is the IEEE maximum numeric value function.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-operation-description`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-002: IEEE_MAX_NUM is an elemental function

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Effect.

**Definition:** IEEE_MAX_NUM has the elemental function class.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-elemental-function-class`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-003: IEEE_MAX_NUM argument restrictions

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Restriction.

**Definition:** X shall be real; Y shall have the same type and kind type parameter as X for
IEEE_MAX_NUM.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-X-real`, `IEEE_MAX_NUM-Y-same-type-kind-as-X`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-004: IEEE_MAX_NUM requires supported datatype

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Restriction.

**Definition:** IEEE_MAX_NUM shall not be invoked unless IEEE_SUPPORT_DATATYPE(X) has the value true.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-requires-supported-datatype`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-005: IEEE_MAX_NUM result has the same characteristics as X

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Effect.

**Definition:** The result of IEEE_MAX_NUM has the same characteristics as X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-result-same-characteristics-as-X`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-006: IEEE_MAX_NUM returns the greater numeric operand and positive zero for mixed signed zeros

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Effect.

**Definition:** IEEE_MAX_NUM returns X for X>Y, Y for Y>X, and positive zero for a negative-zero and
positive-zero pair.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-X-greater-result-X`, `IEEE_MAX_NUM-Y-greater-result-Y`, `IEEE_MAX_NUM-mixed-signed-zero-result-positive-zero`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-007: IEEE_MAX_NUM numeric NaN selection rules

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Effect.

**Definition:** For IEEE_MAX_NUM, exactly one NaN operand yields the other argument, and two NaN
operands yield a quiet NaN.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-one-nan-result-other-argument`, `IEEE_MAX_NUM-two-nans-result-quiet-nan`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-008: IEEE_MAX_NUM signaling-NaN and exception rules

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Effect.

**Definition:** For IEEE_MAX_NUM, a signaling NaN operand signals IEEE_INVALID; unless both operands are
signaling NaNs, the signaling NaN is otherwise ignored and not converted to a quiet NaN.
No other exceptions are signaled.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-signaling-nan-signals-invalid`, `IEEE_MAX_NUM-single-signaling-nan-ignored-for-numeric-result`, `IEEE_MAX_NUM-no-other-exceptions`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.19-009: IEEE_MAX_NUM equal same-sign operands allow either argument

**Source:** S17.11.19, J3/24-007, 18 December 2023, physical PDF494. **Class:** Effect.

**Definition:** For equal IEEE_MAX_NUM operands with the same signs, the processor may return either X
or Y.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_MAX_NUM-equal-same-sign-either-operand-latitude`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

<!-- END GENERATED 17.11.19 -->
