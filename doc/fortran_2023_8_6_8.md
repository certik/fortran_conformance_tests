# Fortran 2023 8.6.8: DIMENSION statement

Source review is recorded in batch074. Effective status comes from
`Registry.catalogue_review_state("8.6.8")`. All new facets are pending.
The canonical catalogue is `doc/catalogues/dimension_statement_8_6_8.json`.

The three original base units are R851, p1 and the unnumbered note,
beginning on physical PDF138, printed124, of J3/24-007, December18,2023,
688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The complete section hash is
`76c02875e02997a910bbe5678f6c8a56385314516488c4dc56541bde62e06644`.
The verified next heading is 8.6.9 INTENT statement on physical PDF139.
No preceding DATA-section body is included in this scope.

R851 explicitly requires a first `array-name(array-spec)` and permits
zero or more comma-led repetitions of that complete pair. The double
colon is optional; each item's parentheses and array-spec are not.
This production is not a generic xyz-list wrapper, a shared shape for all
names, or an array-element reference.

The original global grammar-head scan confirms no explicit array-name
production, so the assumed R402 name convention applies. The word
array-name does not itself require positive rank.8.5.8.2p1 allows a
zero-sized rank-one bound vector to declare a scalar. That is different
from an ordinary rank-one array with zero extent. RANK(0) has its own
canonical scalar meaning; it neither removes R851's required group nor
permits conflicting DIMENSION specifications.

The array-spec's actual category and context remain8.5.8's responsibility.
Colon, star, vector and double-dot forms retain their dummy, pointer,
allocatable, named-constant, rank and state conditions. Corank and cobounds
are not specified by square brackets in this statement. Prior typing,
property specifications and constant/restricted inquiry requirements are
retained; a fixed property inquiry is not an undefined payload read, and
an unavailable shape is not an independent expected-value oracle.

The note's A(10), B(10,70) and C(:) example needs a complete context.
In particular, C's colon does not create an otherwise legal ordinary local
array. P1 is a normative attribute-statement route, not a new allocation,
bound-capture or rank-observation effect.

The actual C815 run witness contains `DIMENSION :: b(2)`, but keeps its
mixed-specification owner and effect role. The repeated-DIMENSION invalid
and its deletion control retain their original identities; the latter has
no DIMENSION statement. No automatic numbered-wrapper link, cloned
program, metadata change, source-use credit or new execution is supplied.

Only this source catalogue and generated view are owned. The additive
index overlay is excluded from the commit. No compiler invocation, fixture,
approval or baseline update occurs in this packet.

<!-- BEGIN GENERATED 8.6.8 -->

### R851: A DIMENSION statement contains one required name/specification pair and optional repeated pairs

**Source:** 8.6.8 R851, J3/24-007, 18 December 2023, physical PDF138, printed124. **Class:** Syntax.

**Definition:** DIMENSION has an optional double colon, then a required array-name followed by its
required parenthesized array-spec. Zero or more additional groups each begin with a
comma and contain their own name and parenthesized array-spec. This is the explicit R851
repetition, not an xyz-list wrapper or optional per-item shape suffix. The name uses the
assumed R402 convention; the complete original grammar-head census has no explicit
array-name production. R814 and its canonical conditions determine the entity's
rank/category, which need not be positive merely because the term is array-name.

**Diagnostic obligation:** required.

**Facets:** `double-colon-form`, `no-double-colon-form`, `repeated-name-specification-pairs`, `missing-first-item`, `missing-per-item-specification`, `delimiter-and-separator-repairs`, `name-versus-designator-source`, `explicit-shape-and-specification-bounds`, `deferred-shape-controls`, `remaining-array-spec-category-source`, `scalar-zero-bound-vector-boundary`, `prior-specification-and-inquiry-source`, `scope-and-canonical-witness-source`.

**Oracle:** Complete compile/positive-controls for the actual explicit-pair grammar and narrowly
repaired syntax failures, with names, type/shape roles, scope and earlier specifications
independently valid. Canonical array-category/property and inquiry relationships remain
explicit pending source gates. A statement spelling is not a runtime
rank/shape/allocation observation.

**Oracle limitation:** All facets are pending. Numbered reporting capability does not mandate fatal rejection,
exact English or a printed R851. Generic array, bound or expected-token text alone is
insufficient; actual causal grammar and live-source origins must be qualified. Wrong
type/rank, duplicate attributes, unprovided modules/interfaces, unsupported facilities,
source echoes, quoted examples, Internal/ASR/verifier failures, crashes and resource
failures earn no credit. Scalar rank zero and zero-sized positive-rank arrays remain
distinct. No unallocated/undefined payload, guessed shape, constructor-rank proxy or
compile-only downgrade of a required canonical runtime effect is introduced.

**Dependencies:** 4.1.1;4.1.3R402/p2;4.2p2;8.1/8.2;8.5.1C815/C816;8.5.8.1-.7/R814/C831-C841;8.5.17;8.5.6.1C822;7.8R778/p1/p8;10.1.11p1/p2/p6/p7;10.1.12p1/p2;11.1.4p2;14.2.2p9;15.4.2.2;15.6.2.2;16.9.171;19.4p3;19.5.1.4p2.

<!-- END GENERATED 8.6.8 -->
