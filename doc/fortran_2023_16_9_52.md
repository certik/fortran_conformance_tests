# Fortran 2023 16.9.52: CHAR

The canonical catalogue is `doc/catalogues/char_16_9_52.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.52")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Six base units: p1-p6 on physical PDF394; p5 is split into collating-position and inverse-relation fine units.

<!-- BEGIN INTRINSICS 16.9.E FIXTURES -->
## Intrinsics 16.9.E runtime observations

This batch adds 4 generated run/effect or positive-control fixtures for 16.9.52, covering 6 pending facets with exact logical, kind, rank, integer, character round-trip, bit-sequence, CEILING, and CMPLX component observations. Mutation metadata perturbs each fixture's load-bearing intrinsic expression and completion output; mutation checking compiles each mutant in a repository-local scratch directory and requires the mutant not to pass. The present-KIND character-kind facet remains pending because no nondefault character kind is guaranteed.
<!-- END INTRINSICS 16.9.E FIXTURES -->

<!-- BEGIN GENERATED 16.9.52 -->

### S16.9.52-001: CHAR arguments are in-range integer I and optional scalar integer constant KIND

**Source:** S16.9.52, J3/24-007, 18 December 2023, physical PDF394. **Class:** Restriction.

**Definition:** I shall be an integer value in the range of the collating sequence for the specified
kind, and KIND, if present, shall be a scalar integer constant expression.

**Diagnostic obligation:** not-required.

**Facets:** `I-integer-in-collating-range`, `KIND-scalar-integer-constant-expression`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

S16.9.52-001 intrinsics_16_9_e fixture: CHAR accepts an integer collating-sequence
position obtained from ICHAR and a scalar integer constant KIND expression.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

S16.9.52-001 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.52-002: CHAR returns one character of the selected or default kind

**Source:** S16.9.52, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** The result is character of length one; present KIND selects the character kind, and
absent KIND selects default character kind.

**Diagnostic obligation:** not-required.

**Facets:** `result-character-length-one`, `present-KIND-selects-character-kind`, `absent-KIND-default-character-kind`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

S16.9.52-002 intrinsics_16_9_e fixture: CHAR returns a character result of length one;
absent-default-kind remains pending because the only load-bearing alternate kind is not
available on both validation profiles.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

S16.9.52-002 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.52-003: CHAR selects the character at position I of the associated collating sequence

**Source:** S16.9.52, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** The result is the character in position I of the collating sequence associated with the
specified kind type parameter.

**Diagnostic obligation:** not-required.

**Facets:** `collating-position-result`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

S16.9.52-003 intrinsics_16_9_e fixture: CHAR(I) returns the character at position I of
the collating sequence; ICHAR of that character returns the original I.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

S16.9.52-003 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.52-004: CHAR and ICHAR obey the specified inverse relationships

**Source:** S16.9.52, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** ICHAR(CHAR(I,KIND(C))) has value I for every valid I, and CHAR(ICHAR(C),KIND(C)) has
value C for any representable character C.

**Diagnostic obligation:** not-required.

**Facets:** `ICHAR-CHAR-integer-roundtrip`, `CHAR-ICHAR-character-roundtrip`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

S16.9.52-004 intrinsics_16_9_e fixture: The result-value paragraph explicitly requires
ICHAR(CHAR(I,KIND(C)))==I and CHAR(ICHAR(C),KIND(C))==C for representable characters.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

S16.9.52-004 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

<!-- END GENERATED 16.9.52 -->
