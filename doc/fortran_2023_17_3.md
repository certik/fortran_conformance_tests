# Fortran 2023 17.3: The exceptions

The canonical catalogue is `doc/catalogues/ieee_exceptions_17_3.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, note-unnumbered, table17.1, p7, p8, p9, p10. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 17.3 -->

### S17.3-001: IEEE_OVERFLOW occurrence is fixed for supported real operations and processor dependent otherwise

**Source:** S17.3 p1.overflow-supported, p1.overflow-latitude; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** IEEE_OVERFLOW occurs for real addition, subtraction, multiplication, division, REAL
conversion, and complex operation or CMPLX conversion part calculations as specified by
60559 for operands whose IEEE_SUPPORT_DATATYPE is true; unsupported operands and real
exponentiation are processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `overflow-signals-for-supported-real-arithmetic`, `overflow-signals-for-supported-real-conversion`, `overflow-signals-for-complex-part-calculation`, `overflow-unsupported-or-exponentiation-latitude`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-002: IEEE_DIVIDE_BY_ZERO occurrence is fixed for supported real division and complex part calculations

**Source:** S17.3 p1.divide-supported, p1.divide-latitude; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** IEEE_DIVIDE_BY_ZERO occurs for supported real division as specified by 60559 and for
complex division caused by a part calculation; unsupported real division and
negative-exponent exponentiation are processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `divide-by-zero-signals-for-supported-real-division`, `divide-by-zero-signals-for-complex-part-calculation`, `divide-by-zero-unsupported-or-negative-exponent-latitude`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-003: IEEE_INVALID occurs for invalid real or complex operations, assignments, and specified relations

**Source:** S17.3 p1.invalid; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** IEEE_INVALID occurs for invalid real or complex operations or assignments, including
real SQRT of a nonzero negative value and too-large integer conversion, and for numeric
relational operations as specified later.

**Diagnostic obligation:** not-required.

**Facets:** `invalid-signals-for-negative-real-sqrt`, `invalid-signals-for-too-large-integer-conversion`, `invalid-relational-operations-deferred-to-table`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-004: IEEE_UNDERFLOW and IEEE_INEXACT occurrence definitions use processor limits and exactness

**Source:** S17.3 p1.underflow, p1.inexact; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** IEEE_UNDERFLOW occurs for real or complex operation or assignment results whose relevant
part has absolute value below a processor-dependent limit, and IEEE_INEXACT occurs for
inexact real or complex operation or assignment results.

**Diagnostic obligation:** not-required.

**Facets:** `underflow-signals-for-inexact-tiny-real-result-under-support`, `underflow-signals-for-real-assignment-under-support`, `underflow-limit-is-processor-dependent`, `inexact-signals-for-inexact-real-result-under-support`, `inexact-signals-for-real-assignment-under-support`, `inexact-signals-for-inexact-complex-part-under-support`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-005: Exception flags are quiet or signaling, initially quiet, set by exceptions, and mutable by status procedures

**Source:** S17.3 p2.flag-state, p2.initial-quiet, p2.exception-sets-flag, p2.setters, p2.persistence; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** Each exception has a quiet/signaling flag; IEEE_GET_FLAG determines it, the initial
value is quiet, exceptions set the associated flag except exact underflow under default
handling, IEEE_SET_FLAG and IEEE_SET_STATUS can change status, and a signaling flag
remains signaling within a procedure until one of those setters makes it quiet.

**Diagnostic obligation:** not-required.

**Facets:** `flags-have-quiet-or-signaling-state`, `flags-initially-quiet`, `exception-occurrence-sets-associated-flag`, `exact-underflow-default-handling-caveat`, `set-flag-and-set-status-change-flag-status`, `signaling-flag-persists-until-set-quiet`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-006: Procedure entry and return quiet and restore signaling flags, but do not quiet flags raised inside

**Source:** S17.3 p3; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** A procedure other than IEEE_GET_FLAG or IEEE_GET_STATUS sees entry-signaling flags
quieted on entry and restored on return; a flag that signals during procedure execution
is not set quiet on return.

**Diagnostic obligation:** not-required.

**Facets:** `signaling-flag-quieted-on-procedure-entry`, `entry-signaling-flag-restored-on-return`, `flag-signaled-inside-procedure-not-quieted-on-return`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-007: Specification-expression evaluation may signal exceptions

**Source:** S17.3 p4; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** Evaluation of a specification expression is permitted to cause an exception to signal.

**Diagnostic obligation:** not-required.

**Facets:** `specification-expression-exception-signaling-permitted`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-008: Intrinsic procedures and intermediate calculations preserve relevant flags except permitted final-result signaling

**Source:** S17.3 p5.preserve-entry-flags, p5.final-result-latitude, p5.intermediate-calculation-rule; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** With IEEE_EXCEPTIONS or IEEE_ARITHMETIC accessible, normally executing intrinsic
procedures and intrinsic-module procedures preserve entry values of IEEE_OVERFLOW,
IEEE_DIVIDE_BY_ZERO, and IEEE_INVALID; final too-large or invalid-NaN results may
signal, and intermediate calculations that do not affect the result neither quiet
signaling flags nor signal quiet flags.

**Diagnostic obligation:** not-required.

**Facets:** `intrinsic-procedure-preserves-entry-overflow-divide-invalid`, `final-overlarge-result-may-signal-overflow`, `final-invalid-nan-result-may-signal-invalid`, `intermediate-calculation-not-affecting-result-does-not-change-flags`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-009: Numeric relational operations on real-result operands signal IEEE_INVALID according to Table 17.1

**Source:** S17.3 p6.relational-rule, p6.conversion-latitude, table17.1; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** For numeric entities whose x1+x2 type is real and whose NaN support inquiry is true,
relational operators signal IEEE_INVALID according to the ISO/IEC 60559 predicates in
Table 17.1; operand conversions may signal exceptions instead of or in addition to the
comparison.

**Diagnostic obligation:** not-required.

**Facets:** `real-less-less-equal-greater-greater-equal-quiet-nan-signal-invalid`, `real-equality-inequality-quiet-nan-do-not-signal-invalid`, `relational-operator-predicate-mapping-table`, `relational-conversions-may-also-signal`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-010: Complex equality and inequality may signal invalid for signaling NaN parts

**Source:** S17.3 p7; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** Complex equality or inequality may signal IEEE_INVALID for signaling NaN real or
imaginary parts, and conversions before comparison may signal instead of or in addition
to the comparison.

**Diagnostic obligation:** not-required.

**Facets:** `complex-equality-inequality-signaling-nan-invalid-latitude`, `complex-comparison-conversions-may-also-signal`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-011: Dead exception-causing operations without flag/status calls have processor-dependent flag results

**Source:** S17.3 p8; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** In a sequence without IEEE flag, status, or halting procedure calls, an
exception-causing operation whose value affects no variable has processor-dependent
post-sequence signaling state.

**Diagnostic obligation:** not-required.

**Facets:** `dead-operation-exception-flag-state-processor-dependent`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-012: Exceptions do not signal solely from operations beyond those required or permitted, while permitted evaluation may signal

**Source:** S17.3 p9.no-extra-operations, p9.permitted-short-circuit-latitude; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** An exception shall not signal solely from operations beyond those required or permitted
by the standard; permitted evaluation such as the right operand of an .OR. expression
may signal.

**Diagnostic obligation:** not-required.

**Facets:** `no-signal-from-unexecuted-if-branch-operation`, `no-signal-from-masked-where-operation`, `permitted-or-evaluation-may-signal-divide-by-zero`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.3-013: Unsupported invalid, underflow, and inexact exceptions have always-quiet flags

**Source:** S17.3 p10; J3/24-007, 18 December 2023, physical PDF480-482. **Class:** Effect.

**Definition:** The processor is not required to support IEEE_INVALID, IEEE_UNDERFLOW, or IEEE_INEXACT;
an unsupported exception flag is always quiet.

**Diagnostic obligation:** not-required.

**Facets:** `unsupported-invalid-flag-always-quiet`, `unsupported-underflow-flag-always-quiet`, `unsupported-inexact-flag-always-quiet`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

<!-- END GENERATED 17.3 -->
