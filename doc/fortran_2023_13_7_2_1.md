# Fortran 2023 13.7.2.1: General rules

The canonical catalogue is `doc/catalogues/numeric_editing_general_rules_13_7_2_1.json`. Effective review state comes from `Registry.catalogue_review_state("13.7.2.1")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Two base units: p1 contains the descriptor/data-class permissions and general rules (1)-(5); the note-extraction unit contains the informative PLUS note plus normative rules (6)-(7), both accounted as requirements. The optional-plus and excess-digit latitude is recorded as permission, not as an exact-output requirement.

<!-- BEGIN GENERATED 13.7.2.1 -->

### S13.7.2.1-001: Numeric descriptors are permitted for listed intrinsic and enumeration data classes

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** I, B, O, Z, F, E, EN, ES, EX, D, and G edit integer/real/complex data as listed; I, B,
O, Z, and G edit enum type data; I, B, O, and Z edit enumeration type data.

**Diagnostic obligation:** not-required.

**Facets:** `numeric-descriptors-integer-real-complex`, `numeric-descriptors-enum-type`, `numeric-descriptors-enumeration-type`.

**Oracle:** This unit records allowed descriptor/data-class pairings. Descriptor-specific spelling
and restrictions are owned by later units in this packet or by sibling 13.7.5 for G.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.1-002: General numeric input blank, sign, and all-blank interpretations

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** On numeric input, leading blanks are not significant; other blanks are interpreted by
blank interpretation mode except for IEEE exceptional and hexadecimal-significand
fields; plus signs may be omitted; an all-blank field is zero.

**Diagnostic obligation:** not-required.

**Facets:** `leading-blanks-not-significant`, `embedded-blanks-follow-blank-mode`, `plus-sign-may-be-omitted`, `all-blank-field-is-zero`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.1-003: Real-family numeric input decimal symbols override descriptor decimal location and may contain excess digits

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** With F, E, EN, ES, EX, D, and G input, a decimal symbol in the field overrides the
descriptor decimal-location component, and the field may contain more digits than the
processor uses to approximate the value.

**Diagnostic obligation:** not-required.

**Facets:** `input-decimal-symbol-overrides-d`, `excess-input-digits-permitted`.

**Oracle:** Decimal-symbol override is required; acceptance of more digits than used records
permission/latitude and cannot require a particular rounded real value beyond
approximation rules.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.1-004: Numeric output signs: optional plus latitude and mandatory minus for negative values

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** On I, F, E, EN, ES, EX, D, and G output, a nonnegative value may be prefixed with a plus
sign as controlled by sign mode or the processor; a negative value shall be prefixed
with a minus sign.

**Diagnostic obligation:** not-required.

**Facets:** `nonnegative-plus-sign-permitted`, `negative-minus-sign-required`.

**Oracle:** The optional plus is recorded as permission, not a requirement; the negative minus is a
mandatory output property.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.1-005: Numeric output is right justified with leading blanks when shorter than field width

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** On output, numeric representations are right justified; if fewer characters are produced
than the field width, leading blanks fill the field.

**Diagnostic obligation:** not-required.

**Facets:** `right-justified-output`, `leading-blanks-fill-short-field`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.1-006: Numeric output overflow fills the field with asterisks unless optional omissions avoid overflow

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** If an exponent exceeds its specified or implied width for E, EN, ES, EX, D, or G, or if
produced characters exceed field width, the processor shall fill width w with asterisks;
it shall not do so if optional characters can be omitted so the field width is not
exceeded.

**Diagnostic obligation:** not-required.

**Facets:** `field-overflow-asterisks`, `exponent-overflow-asterisks`, `optional-omission-avoids-asterisks`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.1-007: Zero field width output selects the smallest non-asterisk width and is prohibited on input

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** For I, B, O, Z, D, E, EN, ES, EX, F, and G output, w may be zero and the processor
selects the smallest positive actual field width that avoids all asterisks; w shall not
be zero on input.

**Diagnostic obligation:** not-required.

**Facets:** `zero-width-output-selects-smallest`, `zero-width-input-prohibited`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.1-008: Real zero output uses all-zero exponent digits

**Source:** S13.7.2.1, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** On output of a real zero value, all digits in the exponent field are zero.

**Diagnostic obligation:** not-required.

**Facets:** `real-zero-exponent-digits-zero`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

<!-- END GENERATED 13.7.2.1 -->
