# Fortran 2023 16.9.96: HUGE

The canonical catalogue is `doc/catalogues/huge_intrinsic_16_9_96.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.96")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN INTRINSICS 16.9 L FIXTURES -->
## Intrinsics 16.9 L runtime observations

The `intrinsics_16_9_l` generator supplies 4 requirement bindings covering 8 portable facets in this section. Oracles use direct intrinsic-expression kind/shape inquiries, exact ASCII code points, bit-context IALL/IAND checks, NOT(0_kind) for IALL zero-size identity, and HUGE model decomposition under explicit profiles. Enumeration HUGE, HYPOT approximation latitude, non-ASCII IACHAR values, and IAND invalid-call restrictions remain pending where no portable runtime oracle is available.
<!-- END INTRINSICS 16.9 L FIXTURES -->

<!-- BEGIN GENERATED 16.9.96 -->

### S16.9.96-001: HUGE argument is integer, real, or enumeration scalar or array

**Source:** S16.9.96, J3/24-007, 18 December 2023, physical PDF416. **Class:** Restriction.

**Definition:** X shall be of type integer or real, or of enumeration type, and may be a scalar or an
array.

**Diagnostic obligation:** not-required.

**Facets:** `HUGE-X-integer-real-or-enumeration`, `HUGE-X-scalar-or-array`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, kind, shape,
status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

S16.9.96-001 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Processor-dependent command-line spelling,
environment case handling, trailing blank significance, cross-image environment state,
TEAM_TYPE representation, HYPOT approximation details, ERRMSG text, and
processor-specific status numbers beyond standard sign or named values are not asserted.
Grep of tests/ and tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

S16.9.96-001 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.96-002: HUGE result is scalar with X type and kind

**Source:** S16.9.96, J3/24-007, 18 December 2023, physical PDF416. **Class:** Effect.

**Definition:** The HUGE result is scalar and has the same type and kind type parameter as X.

**Diagnostic obligation:** not-required.

**Facets:** `HUGE-result-scalar`, `HUGE-result-same-type-and-kind-as-X`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, kind, shape,
status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

S16.9.96-002 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Processor-dependent command-line spelling,
environment case handling, trailing blank significance, cross-image environment state,
TEAM_TYPE representation, HYPOT approximation details, ERRMSG text, and
processor-specific status numbers beyond standard sign or named values are not asserted.
Grep of tests/ and tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

S16.9.96-002 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.96-003: HUGE integer result is r to the q minus one

**Source:** S16.9.96, J3/24-007, 18 December 2023, physical PDF416. **Class:** Effect.

**Definition:** For integer X, HUGE(X) has the value r**q - 1 for the integer model of X's type and
kind.

**Diagnostic obligation:** not-required.

**Facets:** `HUGE-integer-model-upper-bound`, `HUGE-integer-distinguishes-kinds`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, kind, shape,
status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

S16.9.96-003 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Processor-dependent command-line spelling,
environment case handling, trailing blank significance, cross-image environment state,
TEAM_TYPE representation, HYPOT approximation details, ERRMSG text, and
processor-specific status numbers beyond standard sign or named values are not asserted.
Grep of tests/ and tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

S16.9.96-003 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.96-004: HUGE real result is the model largest value

**Source:** S16.9.96, J3/24-007, 18 December 2023, physical PDF416. **Class:** Effect.

**Definition:** For real X, HUGE(X) has value (1 - b**(-p)) * b**emax for X's real model.

**Diagnostic obligation:** not-required.

**Facets:** `HUGE-real-model-largest-value`, `HUGE-real-distinguishes-kinds`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, kind, shape,
status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

S16.9.96-004 intrinsics_16_9_l runtime fixture: Generated run/effect or positive-control
fixtures check only source-stated portable properties: direct KIND/SHAPE inquiries on
intrinsic expressions, exact ASCII IACHAR positions, exact integer bit observations
through BTEST, IALL zero-size identity against NOT(0_kind), and HUGE integer/real model
decompositions under explicit model profiles.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_l generator supplies selected runtime fixtures and mutation plans
without granting universal coverage. Processor-dependent command-line spelling,
environment case handling, trailing blank significance, cross-image environment state,
TEAM_TYPE representation, HYPOT approximation details, ERRMSG text, and
processor-specific status numbers beyond standard sign or named values are not asserted.
Grep of tests/ and tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

S16.9.96-004 intrinsics_16_9_l fixture boundaries: Only the listed facets are
discharged. Enumeration HUGE waits for a reference compiler accepting Fortran 2023
enumeration type syntax; HYPOT numeric approximation and undue-overflow details remain
pending; non-ASCII IACHAR values remain processor dependent; and IAND unnumbered
invalid-call restrictions are not diagnostic claims.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.96-005: HUGE enumeration result is the last enumerator

**Source:** S16.9.96, J3/24-007, 18 December 2023, physical PDF416. **Class:** Effect.

**Definition:** For enumeration type X, HUGE(X) has the value of the last enumerator in the type
definition.

**Diagnostic obligation:** not-required.

**Facets:** `HUGE-enumeration-last-enumerator`, `HUGE-enumeration-singleton-last-enumerator`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

<!-- END GENERATED 16.9.96 -->
