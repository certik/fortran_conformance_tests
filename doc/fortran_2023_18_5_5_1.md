# Fortran 2023 18.5.5.1: CFI functions general rules

The canonical catalogue is `doc/catalogues/cfi_functions_general_18_5_5_1.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted are listed in the catalogue. Fine census units are recorded there for mixed or compound source units.

<!-- BEGIN GENERATED 18.5.5.1 -->

### S18.5.5.1-001: CFI error indicators distinguish success from detected standard errors

**Source:** S18.5.5.1, J3/24-007, 18 December 2023, physical PDF535-536. **Class:** Effect.

**Definition:** For functions in 18.5.5 that return an error indicator, zero indicates no error
condition was detected, a nonzero value indicates the detected error condition, and
Table 18.5 supplies standard error conditions and macro names.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-error-indicator-zero-success`, `cfi-error-indicator-nonzero-detected-error`, `cfi-standard-error-macro-names`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Error-preservation facets lacking a portable conforming way to force
a detected error remain pending with a no-portable-oracle reason.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.1-002: Processors have latitude for additional and competing CFI error detections

**Source:** S18.5.5.1, J3/24-007, 18 December 2023, physical PDF535-536. **Class:** Effect.

**Definition:** A processor is permitted to detect error conditions beyond the standard list, and
selection of one detected condition is processor dependent for an invocation capable of
detecting more than one.

**Diagnostic obligation:** not-required.

**Facets:** `processor-may-detect-other-cfi-errors`, `multiple-cfi-error-choice-processor-dependent`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Error-preservation facets lacking a portable conforming way to force
a detected error remain pending with a no-portable-oracle reason.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.1-003: CFI subscript-like argument arrays follow descriptor dimension order

**Source:** S18.5.5.1, J3/24-007, 18 December 2023, physical PDF535-536. **Class:** Effect.

**Definition:** Function arguments representing subscripts, bounds, extents, or strides use the same
element ordering as the dim member of a C descriptor.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-argument-arrays-dim-order`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Error-preservation facets lacking a portable conforming way to force
a detected error remain pending with a no-portable-oracle reason.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.1-004: ISO_Fortran_binding.h provides prototypes or equivalent macros for all 18.5.5 functions

**Source:** S18.5.5.1, J3/24-007, 18 December 2023, physical PDF535-536. **Class:** Effect.

**Definition:** ISO_Fortran_binding.h provides prototypes, or equivalent macros, for CFI_address,
CFI_allocate, CFI_deallocate, CFI_establish, CFI_is_contiguous, CFI_section,
CFI_select_part, and CFI_setpointer.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-function-interfaces-provided`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Error-preservation facets lacking a portable conforming way to force
a detected error remain pending with a no-portable-oracle reason.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.1-005: CFI function implementation form is unspecified

**Source:** S18.5.5.1, J3/24-007, 18 December 2023, physical PDF535-536. **Class:** Effect.

**Definition:** It is unspecified whether the 18.5.5 functions supplied by ISO_Fortran_binding.h are
macros or identifiers declared with external linkage.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-function-macro-versus-external-unspecified`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Error-preservation facets lacking a portable conforming way to force
a detected error remain pending with a no-portable-oracle reason.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.1-006: Suppressing a CFI function macro has undefined behavior

**Source:** S18.5.5.1, J3/24-007, 18 December 2023, physical PDF535-536. **Class:** Undefined result.

**Definition:** Behavior is undefined after suppressing a macro definition in order to access an actual
function for an 18.5.5 function.

**Diagnostic obligation:** not-required.

**Facets:** `cfi-suppressed-macro-access-undefined`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Error-preservation facets lacking a portable conforming way to force
a detected error remain pending with a no-portable-oracle reason.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

<!-- END GENERATED 18.5.5.1 -->
