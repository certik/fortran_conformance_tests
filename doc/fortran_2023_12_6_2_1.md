# Fortran 2023 12.6.2.1: Control information list - I/O control-list fixtures

The canonical catalogue is `doc/catalogues/control_information_list_12_6_2_1.json`. This author packet adds finite runtime and diagnostic fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 73 distinct facets in 144 program(s); 73 of 95 facets are represented and 22 remain pending.

<!-- BEGIN GENERATED 12.6.2.1 -->

### S12.6.2.1-001: A control information list governs data transfer

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF257. **Class:** Effect.

**Definition:** A control information list is an io-control-spec-list and governs data transfer.

**Diagnostic obligation:** not-required.

**Facets:** `control-list-governs-data-transfer`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 0 diagnostic
program(s) cover `control-list-governs-data-transfer`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### R1213: io-control-spec syntax enumerates unit, format, namelist, control, branch, status, position, record, and size specifiers

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF257. **Class:** Syntax.

**Definition:** An io-control-spec is one of the listed forms: optionally keyworded UNIT= io-unit, FMT=
format, NML= namelist-group-name, ADVANCE=, ASYNCHRONOUS=, BLANK=, DECIMAL=, DELIM=,
END=, EOR=, ERR=, ID=, IOMSG=, IOSTAT=, LEADING_ZERO=, PAD=, POS=, REC=, ROUND=, SIGN=,
or SIZE= with the indicated expression, variable, label, or name operand. The bracketed
UNIT=, FMT=, and NML= alternatives are the syntax source for omitted-keyword admission;
C1211, C1218, and C1219 supply the placement restrictions.

**Diagnostic obligation:** required.

**Facets:** `unit-specifier-form`, `format-specifier-form`, `namelist-specifier-form`, `advance-specifier-form`, `asynchronous-specifier-form`, `blank-specifier-form`, `decimal-specifier-form`, `delim-specifier-form`, `end-label-specifier-form`, `eor-label-specifier-form`, `err-label-specifier-form`, `id-variable-specifier-form`, `iomsg-variable-specifier-form`, `iostat-variable-specifier-form`, `leading-zero-specifier-form`, `pad-specifier-form`, `pos-specifier-form`, `rec-specifier-form`, `round-specifier-form`, `sign-specifier-form`, `size-variable-specifier-form`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 24 valid/control program(s) and 20 diagnostic
program(s) cover `advance-specifier-form`, `asynchronous-specifier-form`,
`blank-specifier-form`, `decimal-specifier-form`, `delim-specifier-form`,
`end-label-specifier-form`, `eor-label-specifier-form`, `err-label-specifier-form`,
`format-specifier-form`, `id-variable-specifier-form`, `iomsg-variable-specifier-form`,
`iostat-variable-specifier-form`, `namelist-specifier-form`, `pad-specifier-form`,
`pos-specifier-form`, `rec-specifier-form`, `round-specifier-form`,
`sign-specifier-form`, `size-variable-specifier-form`, `unit-specifier-form`. Runtime
oracles use exact integer/character/logical values, sentinel preconditions, scratch
files, stream/direct positions chosen by the processor, and only zero/nonzero or named
status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### R1214: ID variables are scalar integer variables

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Syntax.

**Definition:** An id-variable is a scalar integer variable.

**Diagnostic obligation:** required.

**Facets:** `scalar-integer-id-variable`, `nonscalar-id-variable-rejected`, `noninteger-id-variable-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `noninteger-id-variable-rejected`, `nonscalar-id-variable-rejected`.
Runtime oracles use exact integer/character/logical values, sentinel preconditions,
scratch files, stream/direct positions chosen by the processor, and only zero/nonzero or
named status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231
syntax or constraints and have real one-property controls. No IOMSG text,
processor-dependent unit number, physical filename, or specific nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1210: No io-control-specifier appears more than once

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** No specifier shall appear more than once in a given io-control-spec-list.

**Diagnostic obligation:** required.

**Facets:** `duplicate-specifier-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 1 diagnostic
program(s) cover `duplicate-specifier-rejected`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1211: An io-control-spec-list contains an io-unit and positional io-units are first

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** An io-unit shall be specified in an io-control-spec-list; if UNIT= is omitted, the
io-unit shall be the first item in the list. The permission to omit UNIT= is the
bracketed [UNIT=] syntax in R1213; C1211 records the presence obligation and the
first-item restriction for that admitted form.

**Diagnostic obligation:** required.

**Facets:** `io-unit-present`, `positional-io-unit-first`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `io-unit-present`, `positional-io-unit-first`. Runtime oracles use
exact integer/character/logical values, sentinel preconditions, scratch files,
stream/direct positions chosen by the processor, and only zero/nonzero or named status
properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1212: DELIM, LEADING_ZERO, and SIGN do not appear in READ statements

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** A DELIM=, LEADING_ZERO=, or SIGN= specifier shall not appear in a read-stmt.

**Diagnostic obligation:** required.

**Facets:** `read-delim-rejected`, `read-leading-zero-rejected`, `read-sign-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `read-delim-rejected`, `read-sign-rejected`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1213: BLANK, PAD, END, EOR, and SIZE do not appear in WRITE statements

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** A BLANK=, PAD=, END=, EOR=, or SIZE= specifier shall not appear in a write-stmt.

**Diagnostic obligation:** required.

**Facets:** `write-blank-rejected`, `write-pad-rejected`, `write-end-rejected`, `write-eor-rejected`, `write-size-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 5 valid/control program(s) and 5 diagnostic
program(s) cover `write-blank-rejected`, `write-end-rejected`, `write-eor-rejected`,
`write-pad-rejected`, `write-size-rejected`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1214: SIZE does not appear in list-directed or namelist input

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** A SIZE= specifier shall not appear in a list-directed or namelist input statement.

**Diagnostic obligation:** required.

**Facets:** `list-directed-size-rejected`, `namelist-size-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `list-directed-size-rejected`, `namelist-size-rejected`. Runtime
oracles use exact integer/character/logical values, sentinel preconditions, scratch
files, stream/direct positions chosen by the processor, and only zero/nonzero or named
status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1215: ERR, EOR, and END labels are same-scope branch target statements

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** For each ERR=, EOR=, or END= specifier, the label shall identify a branch target
statement, and that branch target statement shall appear in the same inclusive scope as
the data transfer statement.

**Diagnostic obligation:** required.

**Facets:** `err-label-branch-target`, `err-label-same-inclusive-scope`, `eor-label-branch-target`, `eor-label-same-inclusive-scope`, `end-label-branch-target`, `end-label-same-inclusive-scope`.

**Oracle:** C1215 has two independent obligations for each ERR=, EOR=, and END= label: branch-target
statement identity and same-inclusive-scope placement. The branch-target negatives keep
the label in scope but change only the statement kind; the same-scope negatives keep a
branch target statement with the label but move it outside the data transfer statement's
inclusive scope. Future fixtures must compile a conforming same-scope control and must
not rely on IOMSG text, branch execution, or a particular diagnostic wording.

I/O control-list fixture implementation: 6 valid/control program(s) and 6 diagnostic
program(s) cover `end-label-branch-target`, `end-label-same-inclusive-scope`,
`eor-label-branch-target`, `eor-label-same-inclusive-scope`, `err-label-branch-target`,
`err-label-same-inclusive-scope`. Runtime oracles use exact integer/character/logical
values, sentinel preconditions, scratch files, stream/direct positions chosen by the
processor, and only zero/nonzero or named status properties. Diagnostics are
line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have real
one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1216: Namelist control names a namelist group

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** A namelist-group-name shall be the name of a namelist group.

**Diagnostic obligation:** required.

**Facets:** `namelist-group-name-valid`, `non-namelist-name-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 1 diagnostic
program(s) cover `namelist-group-name-valid`, `non-namelist-name-rejected`. Runtime
oracles use exact integer/character/logical values, sentinel preconditions, scratch
files, stream/direct positions chosen by the processor, and only zero/nonzero or named
status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1217: Namelist group names exclude REC, format, input-item lists, and output-item lists

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** A namelist-group-name shall not appear if a REC= specifier, format, input-item-list, or
output-item-list appears in the data transfer statement.

**Diagnostic obligation:** required.

**Facets:** `namelist-with-rec-rejected`, `namelist-with-format-rejected`, `namelist-with-input-items-rejected`, `namelist-with-output-items-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 4 valid/control program(s) and 4 diagnostic
program(s) cover `namelist-with-format-rejected`, `namelist-with-input-items-rejected`,
`namelist-with-output-items-rejected`, `namelist-with-rec-rejected`. Runtime oracles use
exact integer/character/logical values, sentinel preconditions, scratch files,
stream/direct positions chosen by the processor, and only zero/nonzero or named status
properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1218: A positional format is second and follows an io-unit

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If format appears without a preceding FMT=, it shall be the second item in the
io-control-spec-list and the first item shall be io-unit. The permission to omit FMT= is
the bracketed [FMT=] syntax in R1213; C1218 supplies only the placement restrictions for
that admitted positional format.

**Diagnostic obligation:** required.

**Facets:** `positional-format-second`, `positional-format-first-item-io-unit`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `positional-format-first-item-io-unit`, `positional-format-second`.
Runtime oracles use exact integer/character/logical values, sentinel preconditions,
scratch files, stream/direct positions chosen by the processor, and only zero/nonzero or
named status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231
syntax or constraints and have real one-property controls. No IOMSG text,
processor-dependent unit number, physical filename, or specific nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1219: A positional namelist group name is second and follows an io-unit

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If namelist-group-name appears without a preceding NML=, it shall be the second item in
the io-control-spec-list and the first item shall be io-unit. The permission to omit
NML= is the bracketed [NML=] syntax in R1213; C1219 supplies only the placement
restrictions for that admitted positional namelist form.

**Diagnostic obligation:** required.

**Facets:** `positional-namelist-second`, `positional-namelist-first-item-io-unit`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `positional-namelist-first-item-io-unit`, `positional-namelist-second`.
Runtime oracles use exact integer/character/logical values, sentinel preconditions,
scratch files, stream/direct positions chosen by the processor, and only zero/nonzero or
named status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231
syntax or constraints and have real one-property controls. No IOMSG text,
processor-dependent unit number, physical filename, or specific nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1220: Non-file-unit io-units do not combine with REC or POS

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If io-unit is not a file-unit-number, the io-control-spec-list shall not contain a REC=
specifier or a POS= specifier.

**Diagnostic obligation:** required.

**Facets:** `internal-file-rec-rejected`, `internal-file-pos-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `internal-file-pos-rejected`, `internal-file-rec-rejected`. Runtime
oracles use exact integer/character/logical values, sentinel preconditions, scratch
files, stream/direct positions chosen by the processor, and only zero/nonzero or named
status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1221: Internal-file controls contain a format or namelist group name

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If io-unit is an internal-file-variable, the io-control-spec-list shall contain a format
or a namelist-group-name.

**Diagnostic obligation:** required.

**Facets:** `internal-file-format-control`, `internal-file-namelist-control`, `internal-file-missing-format-or-namelist-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 1 diagnostic
program(s) cover `internal-file-missing-format-or-namelist-rejected`. Runtime oracles
use exact integer/character/logical values, sentinel preconditions, scratch files,
stream/direct positions chosen by the processor, and only zero/nonzero or named status
properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1222: REC excludes END and list-directed asterisk format

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If the REC= specifier appears, an END= specifier shall not appear, and the format, if
any, shall not be an asterisk.

**Diagnostic obligation:** required.

**Facets:** `rec-with-end-rejected`, `rec-with-asterisk-format-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `rec-with-asterisk-format-rejected`, `rec-with-end-rejected`. Runtime
oracles use exact integer/character/logical values, sentinel preconditions, scratch
files, stream/direct positions chosen by the processor, and only zero/nonzero or named
status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or
constraints and have real one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### S12.6.2.1-005: ADVANCE is admitted for external formatted sequential or stream transfers with explicit format

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Effect.

**Definition:** C1223 admits an ADVANCE= specifier in a formatted sequential or stream data transfer
statement with explicit format specification whose io-control-spec-list uses an external
file-unit-number rather than an internal-file-variable.

**Diagnostic obligation:** not-required.

**Facets:** `advance-admitted-external-formatted-sequential-explicit-format`, `advance-admitted-external-formatted-stream-explicit-format`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 0 diagnostic
program(s) cover `advance-admitted-external-formatted-sequential-explicit-format`,
`advance-admitted-external-formatted-stream-explicit-format`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1223: ADVANCE outside its admitted external formatted explicit-format context is rejected

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** An ADVANCE= specifier shall not appear outside the admitted C1223 context: the data
transfer must be formatted, must be sequential or stream, must have explicit format
specification, and must not use an internal-file-variable as the io-unit.

**Diagnostic obligation:** required.

**Facets:** `advance-formatted-transfer-required`, `advance-sequential-or-stream-required`, `advance-explicit-format-required`, `advance-external-unit-required`.

**Oracle:** C1223 is split: S12.6.2.1-005 records the admission/permission for ADVANCE= in its
allowed external formatted sequential or stream explicit-format context, while C1223
records the required diagnostics for each way of leaving that context. Each negative
changes one property from the admitted control.

I/O control-list fixture implementation: 3 valid/control program(s) and 3 diagnostic
program(s) cover `advance-explicit-format-required`, `advance-external-unit-required`,
`advance-formatted-transfer-required`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1224: EOR requires an ADVANCE specifier

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If an EOR= specifier appears, an ADVANCE= specifier also shall appear.

**Diagnostic obligation:** required.

**Facets:** `eor-requires-advance`.

**Oracle:** C1224 requires presence of ADVANCE= whenever EOR= appears. The only textual way to
violate C1224 is absence of ADVANCE=, which necessarily also violates p2's unnumbered
ADVANCE='NO' obligation. The diagnostic facet is retained for C1224 because it is the
sole numbered/diagnosable rule in that overlap; p2 is covered separately by changing
ADVANCE='NO' to ADVANCE='YES', which satisfies C1224 and isolates the unnumbered
restriction without a required diagnostic claim.

I/O control-list fixture implementation: 1 valid/control program(s) and 1 diagnostic
program(s) cover `eor-requires-advance`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1225: ASYNCHRONOUS values are YES or NO

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** The scalar-default-char-constant-expr in an ASYNCHRONOUS= specifier shall have the value
YES or NO.

**Diagnostic obligation:** required.

**Facets:** `asynchronous-yes-value`, `asynchronous-no-value`, `asynchronous-other-value-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 1 diagnostic
program(s) cover `asynchronous-other-value-rejected`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1226: ASYNCHRONOUS=YES requires a file-unit-number io-unit

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** An ASYNCHRONOUS= specifier with value YES shall not appear unless io-unit is a
file-unit-number. The admission of ASYNCHRONOUS= as a specifier is supplied by R1213 and
the YES value by C1225; C1226 records the file-unit-number restriction for the YES case.

**Diagnostic obligation:** required.

**Facets:** `async-yes-file-unit-control`, `async-yes-internal-file-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 1 diagnostic
program(s) cover `async-yes-internal-file-rejected`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1227: ID requires ASYNCHRONOUS=YES

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If an ID= specifier appears, an ASYNCHRONOUS= specifier with the value YES shall also
appear.

**Diagnostic obligation:** required.

**Facets:** `id-with-async-yes-control`, `id-missing-asynchronous-rejected`, `id-with-asynchronous-no-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 2 valid/control program(s) and 2 diagnostic
program(s) cover `id-missing-asynchronous-rejected`, `id-with-asynchronous-no-rejected`.
Runtime oracles use exact integer/character/logical values, sentinel preconditions,
scratch files, stream/direct positions chosen by the processor, and only zero/nonzero or
named status properties. Diagnostics are line-anchored for R1213/R1214/C1210-C1231
syntax or constraints and have real one-property controls. No IOMSG text,
processor-dependent unit number, physical filename, or specific nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1228: POS excludes REC

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If a POS= specifier appears, the io-control-spec-list shall not contain a REC=
specifier.

**Diagnostic obligation:** required.

**Facets:** `pos-with-rec-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 1 diagnostic
program(s) cover `pos-with-rec-rejected`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1229: DECIMAL, BLANK, LEADING_ZERO, PAD, SIGN, and ROUND require a format or namelist group name

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If a DECIMAL=, BLANK=, LEADING_ZERO=, PAD=, SIGN=, or ROUND= specifier appears, a format
or namelist-group-name shall also appear.

**Diagnostic obligation:** required.

**Facets:** `decimal-requires-format-or-namelist`, `blank-requires-format-or-namelist`, `leading-zero-requires-format-or-namelist`, `pad-requires-format-or-namelist`, `sign-requires-format-or-namelist`, `round-requires-format-or-namelist`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 4 valid/control program(s) and 4 diagnostic
program(s) cover `blank-requires-format-or-namelist`,
`decimal-requires-format-or-namelist`, `pad-requires-format-or-namelist`,
`sign-requires-format-or-namelist`. Runtime oracles use exact integer/character/logical
values, sentinel preconditions, scratch files, stream/direct positions chosen by the
processor, and only zero/nonzero or named status properties. Diagnostics are
line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have real
one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### S12.6.2.1-006: DELIM is admitted for list-directed formatting or namelist formatting

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Effect.

**Definition:** C1230 admits a DELIM= specifier when the format is an asterisk or when a
namelist-group-name appears.

**Diagnostic obligation:** not-required.

**Facets:** `delim-list-directed-control`, `delim-namelist-control`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; otherwise the facet remains
positive-only or records the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1230: DELIM outside list-directed or namelist formatting is rejected

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF258. **Class:** Restriction.

**Definition:** If a DELIM= specifier appears, an explicit nonasterisk format without a
namelist-group-name is not permitted.

**Diagnostic obligation:** required.

**Facets:** `delim-explicit-format-rejected`.

**Oracle:** C1230 is split: S12.6.2.1-006 records the list-directed and namelist admissions for
DELIM=, while C1230 records the required diagnostic when DELIM= is used with an explicit
nonasterisk format and no namelist group.

I/O control-list fixture implementation: 1 valid/control program(s) and 1 diagnostic
program(s) cover `delim-explicit-format-rejected`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### C1231: ID variables have at least default-integer decimal exponent range

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF259. **Class:** Restriction.

**Definition:** The scalar-int-variable in an id-variable shall have a decimal exponent range no smaller
than that of default integer.

**Diagnostic obligation:** required.

**Facets:** `default-integer-id-range-control`, `narrow-id-range-no-portable-negative`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; otherwise the facet remains
positive-only or records the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### S12.6.2.1-002: EOR also requires ADVANCE=NO

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF259. **Class:** Restriction.

**Definition:** If an EOR= specifier appears, an ADVANCE= specifier with value NO shall also appear.

**Diagnostic obligation:** not-required.

**Facets:** `eor-advance-no-required`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; otherwise the facet remains
positive-only or records the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### S12.6.2.1-003: Format or namelist controls make a data transfer formatted; absence makes it unformatted

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF259. **Class:** Effect.

**Definition:** If the data transfer statement contains a format or namelist-group-name, the statement
is a formatted input/output statement; otherwise, it is an unformatted input/output
statement.

**Diagnostic obligation:** not-required.

**Facets:** `format-control-makes-formatted`, `namelist-control-makes-formatted`, `absence-makes-unformatted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 0 diagnostic
program(s) cover `absence-makes-unformatted`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

### S12.6.2.1-004: Control-character specifier values use limited lists, ignore trailing blanks, and ignore case

**Source:** S12.6.2.1, J3/24-007, 18 December 2023, physical PDF259. **Class:** Effect.

**Definition:** The ADVANCE=, ASYNCHRONOUS=, DECIMAL=, BLANK=, DELIM=, LEADING_ZERO=, PAD=, SIGN=, and
ROUND= specifiers have limited lists of character values; trailing blanks are ignored
and values are interpreted without regard to case.

**Diagnostic obligation:** not-required.

**Facets:** `limited-character-value-lists`, `trailing-blanks-ignored`, `case-insensitive-values`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, or required diagnosis of a numbered syntax
rule or constraint. Negative plans change one source property from the conforming
control and do not require a particular diagnostic message, severity, exit status, or
printed rule number.

I/O control-list fixture implementation: 1 valid/control program(s) and 0 diagnostic
program(s) cover `case-insensitive-values`. Runtime oracles use exact
integer/character/logical values, sentinel preconditions, scratch files, stream/direct
positions chosen by the processor, and only zero/nonzero or named status properties.
Diagnostics are line-anchored for R1213/R1214/C1210-C1231 syntax or constraints and have
real one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. IOMSG wording and
processor-dependent numeric status values are never an oracle. A diagnostic facet is
only a future source-control plan when the cited numbered rule alone is violated and the
invalid program reaches that rule; otherwise the facet remains positive-only or records
the no-portable-oracle limitation.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and existing clause 13 catalogues for
source form, labels, names, expressions, variables, execution constructs, and formatting
syntax where used by pending controls. Unregistered dependencies explicitly retained:
12.6.1 data-transfer statement forms; later 12.6.2 specifier subclauses; 12.7-12.11
connection, transfer, status, and asynchronous semantics; clause 15 defined I/O; clause
16 named I/O status constants if future fixtures use them; clause 19 scope and lifetime
controls where labels or units are reached.

<!-- END GENERATED 12.6.2.1 -->

## I/O control-list fixture derivations

### `internal_read_fmt_governs_transfer` / `S12.6.2.1-001`

**Facets:** `control-list-governs-data-transfer`.

R1213 UNIT= and FMT= choose the internal file and explicit format; p1 says the list governs transfer, C1221 permits the internal formatted read, and p3 classifies the format-controlled transfer as formatted.

Feature mutations: `format-width-load-bearing`.

### `external_unformatted_absence_roundtrip` / `S12.6.2.1-003`

**Facets:** `absence-makes-unformatted`.

p3 says absence of a format or namelist group makes the transfer unformatted; an external unformatted write/read round trip recovers the exact integer without asserting file bytes.

Feature mutations: `insert-format-on-write`.

### `advance_no_sequential_two_reads` / `S12.6.2.1-005`

**Facets:** `advance-admitted-external-formatted-sequential-explicit-format`.

C1223 admits ADVANCE= for an external formatted sequential statement with explicit format; ADVANCE='NO' leaves the second digit available to the next read from the same record.

Feature mutations: `sequential-advance-no-to-yes`.

### `advance_no_stream_two_reads` / `S12.6.2.1-005`

**Facets:** `advance-admitted-external-formatted-stream-explicit-format`.

C1223 also admits ADVANCE= for external formatted stream transfers with explicit format; the nonadvancing read leaves the next stream character for the following read.

Feature mutations: `stream-advance-no-to-yes`.

### `advance_value_case_insensitive` / `S12.6.2.1-004`

**Facets:** `case-insensitive-values`.

p4 requires control-character values to be interpreted without regard to case; lowercase ADVANCE='no' behaves as ADVANCE='NO' and leaves the second digit for the next read.

Feature mutations: `lowercase-value-to-yes`.

### `internal_namelist_read_group` / `C1216`

**Facets:** `namelist-group-name-valid`.

R1213 admits NML=, C1216 requires the operand to name a namelist group, C1221 permits namelist control for an internal file, and p3 classifies the namelist transfer as formatted.

Feature mutations: `namelist-to-explicit-format`.

### `iostat_and_err_status_controls` / `R1213`

**Facets:** `iostat-variable-specifier-form`, `err-label-specifier-form`.

R1213 admits IOSTAT= and ERR=; a successful formatted read sets IOSTAT to zero, and a too-small internal WRITE takes the ERR branch without asserting message text.

Feature mutations: `remove-iostat-specifier`, `remove-err-specifier`.

### `end_label_branch_on_eof` / `R1213`

**Facets:** `end-label-specifier-form`.

R1213 admits END=; reading an empty external formatted file takes the END branch without asserting a processor-dependent status value.

Feature mutations: `remove-end-branch`.

### `eor_label_branch_on_short_record` / `R1213`

**Facets:** `eor-label-specifier-form`, `size-variable-specifier-form`.

R1213 admits EOR= and SIZE=; a nonadvancing read of a short record reaches EOR and reports the single character transferred in SIZE.

Feature mutations: `remove-eor-specifier`, `remove-size-specifier`.

### `pos_and_rec_controls` / `R1213`

**Facets:** `pos-specifier-form`, `rec-specifier-form`.

R1213 admits POS= and REC=; POS=2 on a formatted stream reads the second digit and REC=1 on direct access retrieves the written record.

Feature mutations: `pos-change`, `rec-change`.

### `unit_missing_operand_control` / `R1213`

**Facets:** `unit-specifier-form`.

Conforming one-property control for diagnostic fixture `unit_missing_operand`.

### `unit_missing_operand` / `R1213`

**Facets:** `unit-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_unit_missing_operand_control`.

### `fmt_missing_operand_control` / `R1213`

**Facets:** `format-specifier-form`.

Conforming one-property control for diagnostic fixture `fmt_missing_operand`.

### `fmt_missing_operand` / `R1213`

**Facets:** `format-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_fmt_missing_operand_control`.

### `nml_missing_operand_control` / `R1213`

**Facets:** `namelist-specifier-form`.

Conforming one-property control for diagnostic fixture `nml_missing_operand`.

### `nml_missing_operand` / `R1213`

**Facets:** `namelist-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_nml_missing_operand_control`.

### `advance_missing_operand_control` / `R1213`

**Facets:** `advance-specifier-form`.

Conforming one-property control for diagnostic fixture `advance_missing_operand`.

### `advance_missing_operand` / `R1213`

**Facets:** `advance-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_advance_missing_operand_control`.

### `asynchronous_missing_operand_control` / `R1213`

**Facets:** `asynchronous-specifier-form`.

Conforming one-property control for diagnostic fixture `asynchronous_missing_operand`.

### `asynchronous_missing_operand` / `R1213`

**Facets:** `asynchronous-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_asynchronous_missing_operand_control`.

### `blank_missing_operand_control` / `R1213`

**Facets:** `blank-specifier-form`.

Conforming one-property control for diagnostic fixture `blank_missing_operand`.

### `blank_missing_operand` / `R1213`

**Facets:** `blank-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_blank_missing_operand_control`.

### `decimal_missing_operand_control` / `R1213`

**Facets:** `decimal-specifier-form`.

Conforming one-property control for diagnostic fixture `decimal_missing_operand`.

### `decimal_missing_operand` / `R1213`

**Facets:** `decimal-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_decimal_missing_operand_control`.

### `delim_missing_operand_control` / `R1213`

**Facets:** `delim-specifier-form`.

Conforming one-property control for diagnostic fixture `delim_missing_operand`.

### `delim_missing_operand` / `R1213`

**Facets:** `delim-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_delim_missing_operand_control`.

### `end_missing_operand_control` / `R1213`

**Facets:** `end-label-specifier-form`.

Conforming one-property control for diagnostic fixture `end_missing_operand`.

### `end_missing_operand` / `R1213`

**Facets:** `end-label-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_end_missing_operand_control`.

### `eor_missing_operand_control` / `R1213`

**Facets:** `eor-label-specifier-form`.

Conforming one-property control for diagnostic fixture `eor_missing_operand`.

### `eor_missing_operand` / `R1213`

**Facets:** `eor-label-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_eor_missing_operand_control`.

### `err_missing_operand_control` / `R1213`

**Facets:** `err-label-specifier-form`.

Conforming one-property control for diagnostic fixture `err_missing_operand`.

### `err_missing_operand` / `R1213`

**Facets:** `err-label-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_err_missing_operand_control`.

### `id_missing_operand_control` / `R1213`

**Facets:** `id-variable-specifier-form`.

Conforming one-property control for diagnostic fixture `id_missing_operand`.

### `id_missing_operand` / `R1213`

**Facets:** `id-variable-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_id_missing_operand_control`.

### `iomsg_missing_operand_control` / `R1213`

**Facets:** `iomsg-variable-specifier-form`.

Conforming one-property control for diagnostic fixture `iomsg_missing_operand`.

### `iomsg_missing_operand` / `R1213`

**Facets:** `iomsg-variable-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_iomsg_missing_operand_control`.

### `iostat_missing_operand_control` / `R1213`

**Facets:** `iostat-variable-specifier-form`.

Conforming one-property control for diagnostic fixture `iostat_missing_operand`.

### `iostat_missing_operand` / `R1213`

**Facets:** `iostat-variable-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_iostat_missing_operand_control`.

### `pad_missing_operand_control` / `R1213`

**Facets:** `pad-specifier-form`.

Conforming one-property control for diagnostic fixture `pad_missing_operand`.

### `pad_missing_operand` / `R1213`

**Facets:** `pad-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_pad_missing_operand_control`.

### `pos_missing_operand_control` / `R1213`

**Facets:** `pos-specifier-form`.

Conforming one-property control for diagnostic fixture `pos_missing_operand`.

### `pos_missing_operand` / `R1213`

**Facets:** `pos-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_pos_missing_operand_control`.

### `rec_missing_operand_control` / `R1213`

**Facets:** `rec-specifier-form`.

Conforming one-property control for diagnostic fixture `rec_missing_operand`.

### `rec_missing_operand` / `R1213`

**Facets:** `rec-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_rec_missing_operand_control`.

### `round_missing_operand_control` / `R1213`

**Facets:** `round-specifier-form`.

Conforming one-property control for diagnostic fixture `round_missing_operand`.

### `round_missing_operand` / `R1213`

**Facets:** `round-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_round_missing_operand_control`.

### `sign_missing_operand_control` / `R1213`

**Facets:** `sign-specifier-form`.

Conforming one-property control for diagnostic fixture `sign_missing_operand`.

### `sign_missing_operand` / `R1213`

**Facets:** `sign-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_sign_missing_operand_control`.

### `size_missing_operand_control` / `R1213`

**Facets:** `size-variable-specifier-form`.

Conforming one-property control for diagnostic fixture `size_missing_operand`.

### `size_missing_operand` / `R1213`

**Facets:** `size-variable-specifier-form`.

R1213 enumerates this io-control-spec form; deleting only its operand/punctuation is a syntax error anchored at the changed control item.

Conforming control: `R1213_valid__io_control_list_12_6_2_1_size_missing_operand_control`.

### `id_nonscalar_control` / `R1214`

**Facets:** `nonscalar-id-variable-rejected`.

Conforming one-property control for diagnostic fixture `id_nonscalar`.

### `id_nonscalar` / `R1214`

**Facets:** `nonscalar-id-variable-rejected`.

R1214 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `R1214_valid__io_control_list_12_6_2_1_id_nonscalar_control`.

### `id_noninteger_control` / `R1214`

**Facets:** `noninteger-id-variable-rejected`.

Conforming one-property control for diagnostic fixture `id_noninteger`.

### `id_noninteger` / `R1214`

**Facets:** `noninteger-id-variable-rejected`.

R1214 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `R1214_valid__io_control_list_12_6_2_1_id_noninteger_control`.

### `duplicate_fmt_control` / `C1210`

**Facets:** `duplicate-specifier-rejected`.

Conforming one-property control for diagnostic fixture `duplicate_fmt`.

### `duplicate_fmt` / `C1210`

**Facets:** `duplicate-specifier-rejected`.

C1210 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1210_valid__io_control_list_12_6_2_1_duplicate_fmt_control`.

### `missing_unit_control` / `C1211`

**Facets:** `io-unit-present`.

Conforming one-property control for diagnostic fixture `missing_unit`.

### `missing_unit` / `C1211`

**Facets:** `io-unit-present`.

C1211 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1211_valid__io_control_list_12_6_2_1_missing_unit_control`.

### `positional_unit_not_first_control` / `C1211`

**Facets:** `positional-io-unit-first`.

Conforming one-property control for diagnostic fixture `positional_unit_not_first`.

### `positional_unit_not_first` / `C1211`

**Facets:** `positional-io-unit-first`.

C1211 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1211_valid__io_control_list_12_6_2_1_positional_unit_not_first_control`.

### `read_delim_control` / `C1212`

**Facets:** `read-delim-rejected`.

Conforming one-property control for diagnostic fixture `read_delim`.

### `read_delim` / `C1212`

**Facets:** `read-delim-rejected`.

C1212 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1212_valid__io_control_list_12_6_2_1_read_delim_control`.

### `read_sign_control` / `C1212`

**Facets:** `read-sign-rejected`.

Conforming one-property control for diagnostic fixture `read_sign`.

### `read_sign` / `C1212`

**Facets:** `read-sign-rejected`.

C1212 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1212_valid__io_control_list_12_6_2_1_read_sign_control`.

### `write_blank_control` / `C1213`

**Facets:** `write-blank-rejected`.

Conforming one-property control for diagnostic fixture `write_blank`.

### `write_blank` / `C1213`

**Facets:** `write-blank-rejected`.

C1213 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1213_valid__io_control_list_12_6_2_1_write_blank_control`.

### `write_pad_control` / `C1213`

**Facets:** `write-pad-rejected`.

Conforming one-property control for diagnostic fixture `write_pad`.

### `write_pad` / `C1213`

**Facets:** `write-pad-rejected`.

C1213 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1213_valid__io_control_list_12_6_2_1_write_pad_control`.

### `write_end_control` / `C1213`

**Facets:** `write-end-rejected`.

Conforming one-property control for diagnostic fixture `write_end`.

### `write_end` / `C1213`

**Facets:** `write-end-rejected`.

C1213 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1213_valid__io_control_list_12_6_2_1_write_end_control`.

### `write_eor_control` / `C1213`

**Facets:** `write-eor-rejected`.

Conforming one-property control for diagnostic fixture `write_eor`.

### `write_eor` / `C1213`

**Facets:** `write-eor-rejected`.

C1213 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1213_valid__io_control_list_12_6_2_1_write_eor_control`.

### `write_size_control` / `C1213`

**Facets:** `write-size-rejected`.

Conforming one-property control for diagnostic fixture `write_size`.

### `write_size` / `C1213`

**Facets:** `write-size-rejected`.

C1213 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1213_valid__io_control_list_12_6_2_1_write_size_control`.

### `list_directed_size_control` / `C1214`

**Facets:** `list-directed-size-rejected`.

Conforming one-property control for diagnostic fixture `list_directed_size`.

### `list_directed_size` / `C1214`

**Facets:** `list-directed-size-rejected`.

C1214 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1214_valid__io_control_list_12_6_2_1_list_directed_size_control`.

### `namelist_size_control` / `C1214`

**Facets:** `namelist-size-rejected`.

Conforming one-property control for diagnostic fixture `namelist_size`.

### `namelist_size` / `C1214`

**Facets:** `namelist-size-rejected`.

C1214 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1214_valid__io_control_list_12_6_2_1_namelist_size_control`.

### `non_namelist_name_control` / `C1216`

**Facets:** `non-namelist-name-rejected`.

Conforming one-property control for diagnostic fixture `non_namelist_name`.

### `non_namelist_name` / `C1216`

**Facets:** `non-namelist-name-rejected`.

C1216 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1216_valid__io_control_list_12_6_2_1_non_namelist_name_control`.

### `namelist_with_format_control` / `C1217`

**Facets:** `namelist-with-format-rejected`.

Conforming one-property control for diagnostic fixture `namelist_with_format`.

### `namelist_with_format` / `C1217`

**Facets:** `namelist-with-format-rejected`.

C1217 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1217_valid__io_control_list_12_6_2_1_namelist_with_format_control`.

### `namelist_with_input_control` / `C1217`

**Facets:** `namelist-with-input-items-rejected`.

Conforming one-property control for diagnostic fixture `namelist_with_input`.

### `namelist_with_input` / `C1217`

**Facets:** `namelist-with-input-items-rejected`.

C1217 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1217_valid__io_control_list_12_6_2_1_namelist_with_input_control`.

### `namelist_with_output_control` / `C1217`

**Facets:** `namelist-with-output-items-rejected`.

Conforming one-property control for diagnostic fixture `namelist_with_output`.

### `namelist_with_output` / `C1217`

**Facets:** `namelist-with-output-items-rejected`.

C1217 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1217_valid__io_control_list_12_6_2_1_namelist_with_output_control`.

### `positional_format_third_control` / `C1218`

**Facets:** `positional-format-second`.

Conforming one-property control for diagnostic fixture `positional_format_third`.

### `positional_format_third` / `C1218`

**Facets:** `positional-format-second`.

C1218 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1218_valid__io_control_list_12_6_2_1_positional_format_third_control`.

### `positional_format_first_not_unit_control` / `C1218`

**Facets:** `positional-format-first-item-io-unit`.

Conforming one-property control for diagnostic fixture `positional_format_first_not_unit`.

### `positional_format_first_not_unit` / `C1218`

**Facets:** `positional-format-first-item-io-unit`.

C1218 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1218_valid__io_control_list_12_6_2_1_positional_format_first_not_unit_control`.

### `positional_namelist_third_control` / `C1219`

**Facets:** `positional-namelist-second`.

Conforming one-property control for diagnostic fixture `positional_namelist_third`.

### `positional_namelist_third` / `C1219`

**Facets:** `positional-namelist-second`.

C1219 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1219_valid__io_control_list_12_6_2_1_positional_namelist_third_control`.

### `positional_namelist_first_not_unit_control` / `C1219`

**Facets:** `positional-namelist-first-item-io-unit`.

Conforming one-property control for diagnostic fixture `positional_namelist_first_not_unit`.

### `positional_namelist_first_not_unit` / `C1219`

**Facets:** `positional-namelist-first-item-io-unit`.

C1219 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1219_valid__io_control_list_12_6_2_1_positional_namelist_first_not_unit_control`.

### `internal_rec_control` / `C1220`

**Facets:** `internal-file-rec-rejected`.

Conforming one-property control for diagnostic fixture `internal_rec`.

### `internal_rec` / `C1220`

**Facets:** `internal-file-rec-rejected`.

C1220 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1220_valid__io_control_list_12_6_2_1_internal_rec_control`.

### `internal_pos_control` / `C1220`

**Facets:** `internal-file-pos-rejected`.

Conforming one-property control for diagnostic fixture `internal_pos`.

### `internal_pos` / `C1220`

**Facets:** `internal-file-pos-rejected`.

C1220 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1220_valid__io_control_list_12_6_2_1_internal_pos_control`.

### `internal_missing_format_control` / `C1221`

**Facets:** `internal-file-missing-format-or-namelist-rejected`.

Conforming one-property control for diagnostic fixture `internal_missing_format`.

### `internal_missing_format` / `C1221`

**Facets:** `internal-file-missing-format-or-namelist-rejected`.

C1221 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1221_valid__io_control_list_12_6_2_1_internal_missing_format_control`.

### `rec_with_end_control` / `C1222`

**Facets:** `rec-with-end-rejected`.

Conforming one-property control for diagnostic fixture `rec_with_end`.

### `rec_with_end` / `C1222`

**Facets:** `rec-with-end-rejected`.

C1222 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1222_valid__io_control_list_12_6_2_1_rec_with_end_control`.

### `rec_with_asterisk_control` / `C1222`

**Facets:** `rec-with-asterisk-format-rejected`.

Conforming one-property control for diagnostic fixture `rec_with_asterisk`.

### `rec_with_asterisk` / `C1222`

**Facets:** `rec-with-asterisk-format-rejected`.

C1222 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1222_valid__io_control_list_12_6_2_1_rec_with_asterisk_control`.

### `advance_unformatted_control` / `C1223`

**Facets:** `advance-formatted-transfer-required`.

Conforming one-property control for diagnostic fixture `advance_unformatted`.

### `advance_unformatted` / `C1223`

**Facets:** `advance-formatted-transfer-required`.

C1223 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1223_valid__io_control_list_12_6_2_1_advance_unformatted_control`.

### `advance_list_directed_control` / `C1223`

**Facets:** `advance-explicit-format-required`.

Conforming one-property control for diagnostic fixture `advance_list_directed`.

### `advance_list_directed` / `C1223`

**Facets:** `advance-explicit-format-required`.

C1223 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1223_valid__io_control_list_12_6_2_1_advance_list_directed_control`.

### `advance_internal_control` / `C1223`

**Facets:** `advance-external-unit-required`.

Conforming one-property control for diagnostic fixture `advance_internal`.

### `advance_internal` / `C1223`

**Facets:** `advance-external-unit-required`.

C1223 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1223_valid__io_control_list_12_6_2_1_advance_internal_control`.

### `eor_no_advance_control` / `C1224`

**Facets:** `eor-requires-advance`.

Conforming one-property control for diagnostic fixture `eor_no_advance`.

### `eor_no_advance` / `C1224`

**Facets:** `eor-requires-advance`.

C1224 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1224_valid__io_control_list_12_6_2_1_eor_no_advance_control`.

### `asynchronous_other_control` / `C1225`

**Facets:** `asynchronous-other-value-rejected`.

Conforming one-property control for diagnostic fixture `asynchronous_other`.

### `asynchronous_other` / `C1225`

**Facets:** `asynchronous-other-value-rejected`.

C1225 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1225_valid__io_control_list_12_6_2_1_asynchronous_other_control`.

### `id_missing_async_control` / `C1227`

**Facets:** `id-missing-asynchronous-rejected`.

Conforming one-property control for diagnostic fixture `id_missing_async`.

### `id_missing_async` / `C1227`

**Facets:** `id-missing-asynchronous-rejected`.

C1227 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1227_valid__io_control_list_12_6_2_1_id_missing_async_control`.

### `id_async_no_control` / `C1227`

**Facets:** `id-with-asynchronous-no-rejected`.

Conforming one-property control for diagnostic fixture `id_async_no`.

### `id_async_no` / `C1227`

**Facets:** `id-with-asynchronous-no-rejected`.

C1227 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1227_valid__io_control_list_12_6_2_1_id_async_no_control`.

### `pos_with_rec_control` / `C1228`

**Facets:** `pos-with-rec-rejected`.

Conforming one-property control for diagnostic fixture `pos_with_rec`.

### `pos_with_rec` / `C1228`

**Facets:** `pos-with-rec-rejected`.

C1228 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1228_valid__io_control_list_12_6_2_1_pos_with_rec_control`.

### `decimal_unformatted_control` / `C1229`

**Facets:** `decimal-requires-format-or-namelist`.

Conforming one-property control for diagnostic fixture `decimal_unformatted`.

### `decimal_unformatted` / `C1229`

**Facets:** `decimal-requires-format-or-namelist`.

C1229 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1229_valid__io_control_list_12_6_2_1_decimal_unformatted_control`.

### `blank_unformatted_control` / `C1229`

**Facets:** `blank-requires-format-or-namelist`.

Conforming one-property control for diagnostic fixture `blank_unformatted`.

### `blank_unformatted` / `C1229`

**Facets:** `blank-requires-format-or-namelist`.

C1229 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1229_valid__io_control_list_12_6_2_1_blank_unformatted_control`.

### `pad_unformatted_control` / `C1229`

**Facets:** `pad-requires-format-or-namelist`.

Conforming one-property control for diagnostic fixture `pad_unformatted`.

### `pad_unformatted` / `C1229`

**Facets:** `pad-requires-format-or-namelist`.

C1229 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1229_valid__io_control_list_12_6_2_1_pad_unformatted_control`.

### `sign_unformatted_control` / `C1229`

**Facets:** `sign-requires-format-or-namelist`.

Conforming one-property control for diagnostic fixture `sign_unformatted`.

### `sign_unformatted` / `C1229`

**Facets:** `sign-requires-format-or-namelist`.

C1229 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1229_valid__io_control_list_12_6_2_1_sign_unformatted_control`.

### `delim_explicit_format_control` / `C1230`

**Facets:** `delim-explicit-format-rejected`.

Conforming one-property control for diagnostic fixture `delim_explicit_format`.

### `delim_explicit_format` / `C1230`

**Facets:** `delim-explicit-format-rejected`.

C1230 requires this restriction; the invalid source changes only the listed control-list property from its conforming control.

Conforming control: `C1230_valid__io_control_list_12_6_2_1_delim_explicit_format_control`.

### `err_label_format_target_control` / `C1215`

**Facets:** `err-label-branch-target`.

Conforming one-property control for diagnostic fixture `err_label_format_target`.

### `err_label_format_target` / `C1215`

**Facets:** `err-label-branch-target`.

C1215 requires ERR/EOR/END labels to identify branch target statements; a FORMAT label is defined but is not a branch target.

Conforming control: `C1215_valid__io_control_list_12_6_2_1_err_label_format_target_control`.

### `err_label_other_scope_control` / `C1215`

**Facets:** `err-label-same-inclusive-scope`.

Conforming one-property control for diagnostic fixture `err_label_other_scope`.

### `err_label_other_scope` / `C1215`

**Facets:** `err-label-same-inclusive-scope`.

C1215 also requires the branch target statement to appear in the same inclusive scope as the data transfer statement.

Conforming control: `C1215_valid__io_control_list_12_6_2_1_err_label_other_scope_control`.

### `end_label_format_target_control` / `C1215`

**Facets:** `end-label-branch-target`.

Conforming one-property control for diagnostic fixture `end_label_format_target`.

### `end_label_format_target` / `C1215`

**Facets:** `end-label-branch-target`.

C1215 requires ERR/EOR/END labels to identify branch target statements; a FORMAT label is defined but is not a branch target.

Conforming control: `C1215_valid__io_control_list_12_6_2_1_end_label_format_target_control`.

### `end_label_other_scope_control` / `C1215`

**Facets:** `end-label-same-inclusive-scope`.

Conforming one-property control for diagnostic fixture `end_label_other_scope`.

### `end_label_other_scope` / `C1215`

**Facets:** `end-label-same-inclusive-scope`.

C1215 also requires the branch target statement to appear in the same inclusive scope as the data transfer statement.

Conforming control: `C1215_valid__io_control_list_12_6_2_1_end_label_other_scope_control`.

### `eor_label_format_target_control` / `C1215`

**Facets:** `eor-label-branch-target`.

Conforming one-property control for diagnostic fixture `eor_label_format_target`.

### `eor_label_format_target` / `C1215`

**Facets:** `eor-label-branch-target`.

C1215 requires ERR/EOR/END labels to identify branch target statements; a FORMAT label is defined but is not a branch target.

Conforming control: `C1215_valid__io_control_list_12_6_2_1_eor_label_format_target_control`.

### `eor_label_other_scope_control` / `C1215`

**Facets:** `eor-label-same-inclusive-scope`.

Conforming one-property control for diagnostic fixture `eor_label_other_scope`.

### `eor_label_other_scope` / `C1215`

**Facets:** `eor-label-same-inclusive-scope`.

C1215 also requires the branch target statement to appear in the same inclusive scope as the data transfer statement.

Conforming control: `C1215_valid__io_control_list_12_6_2_1_eor_label_other_scope_control`.

### `namelist_with_rec_control` / `C1217`

**Facets:** `namelist-with-rec-rejected`.

Conforming one-property control for diagnostic fixture `namelist_with_rec`.

### `namelist_with_rec` / `C1217`

**Facets:** `namelist-with-rec-rejected`.

C1217 forbids a namelist-group-name when REC= appears; the control removes only REC= from namelist input.

Conforming control: `C1217_valid__io_control_list_12_6_2_1_namelist_with_rec_control`.

