# Fortran 2023 13.7.2.3.4: EN editing

The canonical catalogue is `doc/catalogues/en_editing_13_7_2_3_4.json`. Effective review state comes from `Registry.catalogue_review_state("13.7.2.3.4")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Seven base units including Table 13.2 and an example note. The table was checked row-by-row against the PDF image; example rows are informative and not used as independent requirements.

<!-- BEGIN EN ES EDITING FIXTURES -->
## Executable EN editing fixtures

This packet adds nine complete internal-file programs for selected 13.7.2.3.4 EN output facets. Each writes an exact binary default-real value to a CHARACTER variable initialized with # sentinels, uses SS to suppress optional plus signs, checks LEN before equality, and keeps the EN/ES/E descriptor-substitution matrix permanent in the generator.
<!-- END EN ES EDITING FIXTURES -->

<!-- BEGIN GENERATED 13.7.2.3.4 -->

### S13.7.2.3.4-001: EN output uses engineering notation and ignores scale factor

**Source:** S13.7.2.3.4, J3/24-007, 18 December 2023, physical PDF298. **Class:** Effect.

**Definition:** EN output uses engineering notation with exponent divisible by three and significand
magnitude in [1,1000) except zero; scale factor has no output effect.

**Diagnostic obligation:** not-required.

**Facets:** `EN-engineering-notation`, `EN-exponent-divisible-by-three`, `EN-significand-range`, `EN-scale-factor-no-effect`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.4-001 EN runtime fixtures: complete internal-file programs write exact
default-real values using SS and explicit EN w.dE2 fields. 0.5 expects ' 500.000E-03',
exercising engineering notation and an exponent divisible by three; 0.125 expects '
125.000E-03', exercising the 1<=yyy<1000 significand interval; the scale-factor fixture
writes 0.5 with (SS,1P,EN12.3E2) and with a 0P control and expects the same field
because EN output ignores scale factor. Descriptor substitutions EN<->ES, EN<->E, w, d,
and e mutations are permanent and fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

EN-editing fixture boundaries: this packet discharges only the selected EN output facets
removed from pending above. It deliberately leaves EN input, IEEE infinity/NaN output,
EN w.d alternative exponent spellings, exponent magnitudes above 99, E0/w=0
selected-width forms, zero exponent plus, diagnostics, external files, DECIMAL=COMMA,
nondefault sign mode, processor-defined leading-zero mode for E/D/G, and rounded/inexact
decimal cases pending. All positive outputs use SS and exact binary values whose
discarded decimal digits are zero.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.4-002: EN descriptor forms and exponent digit count

**Source:** S13.7.2.3.4, J3/24-007, 18 December 2023, physical PDF298. **Class:** Effect.

**Definition:** EN w.d and EN w.d E e occupy w positions except w=0 selects output width; fractional
part has d digits; positive e fixes exponent digits, otherwise minimum digits are used.

**Diagnostic obligation:** not-required.

**Facets:** `EN-field-width`, `EN-fractional-digits`, `EN-exponent-digit-count`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.4-002 EN descriptor-form runtime fixtures: complete internal-file programs
write exact values with SS. EN12.3E2 of 100.0 expects ' 100.000E+00' and proves the
12-position field; EN11.2E2 of 100.0 expects ' 100.00E+00' and proves d=2 fractional
digits; EN12.3E2 of 0.125 expects ' 125.000E-03' and proves explicit E2 gives two
exponent digits. All descriptor substitutions in the generated matrix fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

EN-editing fixture boundaries: this packet discharges only the selected EN output facets
removed from pending above. It deliberately leaves EN input, IEEE infinity/NaN output,
EN w.d alternative exponent spellings, exponent magnitudes above 99, E0/w=0
selected-width forms, zero exponent plus, diagnostics, external files, DECIMAL=COMMA,
nondefault sign mode, processor-defined leading-zero mode for E/D/G, and rounded/inexact
decimal cases pending. All positive outputs use SS and exact binary values whose
discarded decimal digits are zero.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.4-003: EN input is interpreted as F input

**Source:** S13.7.2.3.4, J3/24-007, 18 December 2023, physical PDF298. **Class:** Effect.

**Definition:** The form and interpretation of EN input fields is the same as for F w.d input.

**Diagnostic obligation:** not-required.

**Facets:** `EN-input-same-as-F`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.4-004: EN infinity and NaN output follows F output form

**Source:** S13.7.2.3.4, J3/24-007, 18 December 2023, physical PDF298. **Class:** Effect.

**Definition:** For IEEE infinity or NaN internal values, EN output fields use the same form as F w.d
output.

**Diagnostic obligation:** not-required.

**Facets:** `EN-ieee-output-same-as-F`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.4-005: Finite EN output field components and Table 13.2 exponent form

**Source:** S13.7.2.3.4, J3/24-007, 18 December 2023, physical PDF298. **Class:** Effect.

**Definition:** Finite EN output has optional sign, one to three leading digits yyy, decimal symbol, d
following digits, and an exponent divisible by three in a Table 13.2 form.

**Diagnostic obligation:** not-required.

**Facets:** `EN-output-significand-digits`, `EN-output-decimal-symbol`, `EN-output-d-fractional-digits`, `EN-output-table13_2-exponent`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.4-005 EN finite-output runtime fixtures: complete internal-file programs write
exact 0.5 with (SS,EN12.3E2) and expect ' 500.000E-03'. Separate fixtures bind the
one-to-three leading significand digits and the decimal symbol. Expected fields check
LEN before equality; the source keeps other facets in this requirement pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

EN-editing fixture boundaries: this packet discharges only the selected EN output facets
removed from pending above. It deliberately leaves EN input, IEEE infinity/NaN output,
EN w.d alternative exponent spellings, exponent magnitudes above 99, E0/w=0
selected-width forms, zero exponent plus, diagnostics, external files, DECIMAL=COMMA,
nondefault sign mode, processor-defined leading-zero mode for E/D/G, and rounded/inexact
decimal cases pending. All positive outputs use SS and exact binary values whose
discarded decimal digits are zero.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.4-006: Table 13.2 enumerates EN exponent forms

**Source:** S13.7.2.3.4, J3/24-007, 18 December 2023, physical PDF298. **Class:** Effect.

**Definition:** Table 13.2 gives row-by-row EN exponent forms for EN w.d, EN w.d E e, EN w.d E0, and
EN0.d, with z digits, s minimum digits, and plus sign for zero exponent.

**Diagnostic obligation:** not-required.

**Facets:** `table13_2_EN_w_d_abs_le_99`, `table13_2_EN_w_d_abs_100_to_999`, `table13_2_EN_Ee_positive`, `table13_2_EN_E0_or_EN0_d`, `table13_2_zero_exponent_plus`.

**Oracle:** The table was checked row-by-row against the PDF image; rows with alternative forms are
permissions/latitude unless e fixes the digit count.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

<!-- END GENERATED 13.7.2.3.4 -->
