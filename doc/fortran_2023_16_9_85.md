# Fortran 2023 16.9.85: EXPONENT

The canonical catalogue is `doc/catalogues/exponent_16_9_85.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

Exact-result note: p5 is split into finite, zero, and IEEE-special exact obligations. Plans use model-derived inputs, negative operands, and multiple kinds rather than approximate decimal examples.

<!-- BEGIN GENERATED 16.9.85 -->

### S16.9.85-001: EXPONENT is the floating-point exponent inquiry intrinsic

**Source:** S16.9.85, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** EXPONENT is the intrinsic that returns the exponent of a floating-point number.

**Diagnostic obligation:** not-required.

**Facets:** `exponent-description`.

**Oracle:** Plans are pending and source-derived. Exact-value plans use discriminating nondefault
inputs: zero, negative real values, values constructed from the model radix or
SET_EXPONENT to force a known model exponent, and default plus double precision real
kinds. IEEE infinity and NaN facets use guarded IEEE_SUPPORT_* plans: exercise with
IEEE_VALUE when support is present and record non-exercise otherwise.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.85-002: EXPONENT is elemental

**Source:** S16.9.85, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** EXPONENT is an elemental function.

**Diagnostic obligation:** not-required.

**Facets:** `exponent-elemental-class`.

**Oracle:** Plans are pending and source-derived. Exact-value plans use discriminating nondefault
inputs: zero, negative real values, values constructed from the model radix or
SET_EXPONENT to force a known model exponent, and default plus double precision real
kinds. IEEE infinity and NaN facets use guarded IEEE_SUPPORT_* plans: exercise with
IEEE_VALUE when support is present and record non-exercise otherwise.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.85-003: EXPONENT argument X is real

**Source:** S16.9.85, J3/24-007, 18 December 2023, physical PDF409. **Class:** Restriction.

**Definition:** The X argument of EXPONENT shall be of type real.

**Diagnostic obligation:** not-required.

**Facets:** `exponent-x-real`.

**Oracle:** Plans are pending and source-derived. Exact-value plans use discriminating nondefault
inputs: zero, negative real values, values constructed from the model radix or
SET_EXPONENT to force a known model exponent, and default plus double precision real
kinds. IEEE infinity and NaN facets use guarded IEEE_SUPPORT_* plans: exercise with
IEEE_VALUE when support is present and record non-exercise otherwise.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.85-004: EXPONENT result is default integer

**Source:** S16.9.85, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** The EXPONENT result is default integer.

**Diagnostic obligation:** not-required.

**Facets:** `exponent-result-default-integer`.

**Oracle:** Plans are pending and source-derived. Exact-value plans use discriminating nondefault
inputs: zero, negative real values, values constructed from the model radix or
SET_EXPONENT to force a known model exponent, and default plus double precision real
kinds. IEEE infinity and NaN facets use guarded IEEE_SUPPORT_* plans: exercise with
IEEE_VALUE when support is present and record non-exercise otherwise.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.85-005: EXPONENT returns the model exponent for representable nonzero finite X

**Source:** S16.9.85, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** For nonzero X whose model exponent is within default integer range, EXPONENT returns the
exponent e of X in the extended real model for the kind of X.

**Diagnostic obligation:** not-required.

**Facets:** `exponent-nonzero-model-exponent`, `exponent-negative-uses-value-exponent`, `exponent-different-kinds`.

**Oracle:** Plans are pending and source-derived. Exact-value plans use discriminating nondefault
inputs: zero, negative real values, values constructed from the model radix or
SET_EXPONENT to force a known model exponent, and default plus double precision real
kinds. IEEE infinity and NaN facets use guarded IEEE_SUPPORT_* plans: exercise with
IEEE_VALUE when support is present and record non-exercise otherwise.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.85-006: EXPONENT returns zero for zero X

**Source:** S16.9.85, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** If X has the value zero, EXPONENT returns zero.

**Diagnostic obligation:** not-required.

**Facets:** `exponent-zero-result-zero`.

**Oracle:** Plans are pending and source-derived. Exact-value plans use discriminating nondefault
inputs: zero, negative real values, values constructed from the model radix or
SET_EXPONENT to force a known model exponent, and default plus double precision real
kinds. IEEE infinity and NaN facets use guarded IEEE_SUPPORT_* plans: exercise with
IEEE_VALUE when support is present and record non-exercise otherwise.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.85-007: EXPONENT returns HUGE(0) for IEEE infinity or NaN

**Source:** S16.9.85, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** If X is an IEEE infinity or NaN, EXPONENT returns HUGE(0).

**Diagnostic obligation:** not-required.

**Facets:** `exponent-ieee-infinity-huge`, `exponent-ieee-nan-huge`.

**Oracle:** Plans are pending and source-derived. Exact-value plans use discriminating nondefault
inputs: zero, negative real values, values constructed from the model radix or
SET_EXPONENT to force a known model exponent, and default plus double precision real
kinds. IEEE infinity and NaN facets use guarded IEEE_SUPPORT_* plans: exercise with
IEEE_VALUE when support is present and record non-exercise otherwise.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

<!-- END GENERATED 16.9.85 -->
