# Fortran 2023 16.9.88: FINDLOC

The canonical catalogue is `doc/catalogues/findloc_16_9_88.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6, p7, p8.

Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN INTRINSICS 16.9.J FIXTURES 16.9.88 -->

## `intrinsics_16_9_j` executable fixture observations

This packet adds 17 generated valid/f2023 fixtures for 16.9.88, covering 28 facets with exact FINDLOC/FLOOR integer results, direct result-characteristic inquiries, FRACTION real-model power-of-radix checks, IEEE NaN-class checks, and GAMMA characteristic-only checks. 0 facet(s) remain pending where no portable oracle exists.
<!-- END INTRINSICS 16.9.J FIXTURES 16.9.88 -->

<!-- BEGIN GENERATED 16.9.88 -->

### S16.9.88-001: FINDLOC has the ARRAY/VALUE optional-argument interface

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** The section signature identifies FINDLOC with ARRAY, VALUE, and optional MASK, KIND, and
BACK arguments; DIM is governed by the argument/result paragraphs.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-signature-array-value-optional-arguments`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-001 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_signature_options` cover findloc-signature-array-value-optional-arguments. The
signature line admits positional ARRAY/VALUE plus named MASK, KIND, BACK and DIM forms;
exact FINDLOC results distinguish the selected optional-argument branch. The source uses
direct intrinsic-expression inquiries for result characteristics and exact integer,
logical, character, shape, kind, IEEE NaN-class, or real-model power-of-radix
observations; processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-001 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-002: FINDLOC locates a specified value

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** FINDLOC returns the location or locations of a specified value.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-specified-value-location`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-002 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_specified_value` cover findloc-specified-value-location. p1 describes location
of a specified VALUE; exact subscript [3] is returned for the only element equal to 9.
The source uses direct intrinsic-expression inquiries for result characteristics and
exact integer, logical, character, shape, kind, IEEE NaN-class, or real-model
power-of-radix observations; processor-dependent approximations are not compared
exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-002 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-003: FINDLOC is a transformational function

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** The intrinsic is classified as a transformational function.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-transformational-function-class`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-003 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_transformational_class` cover findloc-transformational-function-class. The p2
transformational classification is reached by applying FINDLOC to a whole rank-two array
and observing its rank-sized subscript-vector result. The source uses direct
intrinsic-expression inquiries for result characteristics and exact integer, logical,
character, shape, kind, IEEE NaN-class, or real-model power-of-radix observations;
processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-003 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-004: FINDLOC array argument restriction

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Restriction.

**Definition:** ARRAY shall be an array of intrinsic type.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-array-intrinsic-array`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-004 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_array_argument_types` cover findloc-array-intrinsic-array. The ARRAY
restriction is exercised by conforming intrinsic-type arrays, including character and
integer arrays, with exact equality-based locations. The source uses direct
intrinsic-expression inquiries for result characteristics and exact integer, logical,
character, shape, kind, IEEE NaN-class, or real-model power-of-radix observations;
processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-004 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-005: FINDLOC value argument restriction

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Restriction.

**Definition:** VALUE shall be scalar and type-conformant with ARRAY for == or .EQV..

**Diagnostic obligation:** not-required.

**Facets:** `findloc-value-scalar-type-conformant`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-005 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_value_argument` cover findloc-value-scalar-type-conformant. The VALUE
restriction is exercised with a scalar logical VALUE conforming to a logical ARRAY, and
the exact first .EQV. match is observed. The source uses direct intrinsic-expression
inquiries for result characteristics and exact integer, logical, character, shape, kind,
IEEE NaN-class, or real-model power-of-radix observations; processor-dependent
approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-005 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-006: FINDLOC dim argument restriction

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Restriction.

**Definition:** DIM shall be an integer scalar in the range 1 through rank(ARRAY).

**Diagnostic obligation:** not-required.

**Facets:** `findloc-dim-scalar-valid-range`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-006 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_dim_argument` cover findloc-dim-scalar-valid-range. The DIM restriction is
exercised by a scalar integer DIM=1 in range for a rank-two ARRAY; switching to the
other valid DIM changes the section reductions. The source uses direct
intrinsic-expression inquiries for result characteristics and exact integer, logical,
character, shape, kind, IEEE NaN-class, or real-model power-of-radix observations;
processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-006 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-007: FINDLOC mask argument restriction

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Restriction.

**Definition:** MASK shall be logical and conformable with ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-mask-logical-conformable`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-007 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_mask_argument` cover findloc-mask-logical-conformable. The MASK restriction is
exercised by a logical conformable mask whose true element selects a later matching
ARRAY element. The source uses direct intrinsic-expression inquiries for result
characteristics and exact integer, logical, character, shape, kind, IEEE NaN-class, or
real-model power-of-radix observations; processor-dependent approximations are not
compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-007 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-008: FINDLOC kind argument restriction

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Restriction.

**Definition:** KIND shall be a scalar integer constant expression.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-kind-scalar-integer-constant`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-008 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_kind_argument` cover findloc-kind-scalar-integer-constant. The KIND restriction
is exercised with the scalar integer constant parameter IK and a direct KIND inquiry on
the FINDLOC expression. The source uses direct intrinsic-expression inquiries for result
characteristics and exact integer, logical, character, shape, kind, IEEE NaN-class, or
real-model power-of-radix observations; processor-dependent approximations are not
compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-008 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-009: FINDLOC back argument restriction

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Restriction.

**Definition:** BACK shall be a logical scalar.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-back-logical-scalar`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-009 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_back_argument` cover findloc-back-logical-scalar. The BACK restriction is
exercised with a logical scalar BACK=.TRUE.; repeated matches make the exact
last-location result load-bearing. The source uses direct intrinsic-expression inquiries
for result characteristics and exact integer, logical, character, shape, kind, IEEE
NaN-class, or real-model power-of-radix observations; processor-dependent approximations
are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-009 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-010: FINDLOC result integer kind follows KIND or default

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** The result is integer with the requested KIND, or default integer kind when KIND is
absent.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-default-integer-kind`, `findloc-kind-selects-result-kind`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-010 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_result_kind` cover findloc-default-integer-kind,
findloc-kind-selects-result-kind. p4 fixes default integer kind when KIND is absent and
the requested kind when KIND is present; both are inquired directly on FINDLOC
expressions. The source uses direct intrinsic-expression inquiries for result
characteristics and exact integer, logical, character, shape, kind, IEEE NaN-class, or
real-model power-of-radix observations; processor-dependent approximations are not
compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-010 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-011: FINDLOC without DIM returns a rank-one array sized by ARRAY rank

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** If DIM is absent, the result is rank one and has size equal to rank(ARRAY).

**Diagnostic obligation:** not-required.

**Facets:** `findloc-no-dim-rank-one`, `findloc-no-dim-size-array-rank`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-011 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_no_dim_shape` cover findloc-no-dim-rank-one, findloc-no-dim-size-array-rank. p4
says absent DIM yields a rank-one result sized by ARRAY rank; direct RANK and SIZE
inquiries observe those characteristics. The source uses direct intrinsic-expression
inquiries for result characteristics and exact integer, logical, character, shape, kind,
IEEE NaN-class, or real-model power-of-radix observations; processor-dependent
approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-011 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-012: FINDLOC with DIM drops that dimension from the result shape

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** If DIM appears, the result has rank n-1 and shape equal to ARRAY shape with the DIM
extent removed.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-dim-result-rank-n-minus-one`, `findloc-dim-result-shape-drops-dim`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-012 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_dim_shape` cover findloc-dim-result-rank-n-minus-one,
findloc-dim-result-shape-drops-dim. p4 says present DIM drops that dimension; direct
RANK and SHAPE inquiries on FINDLOC expressions observe the scalar rank-one case and a
2x3 DIM=1 shape. The source uses direct intrinsic-expression inquiries for result
characteristics and exact integer, logical, character, shape, kind, IEEE NaN-class, or
real-model power-of-radix observations; processor-dependent approximations are not
compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-012 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-013: FINDLOC case i returns matching subscripts or all zeros

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** FINDLOC(ARRAY,VALUE) returns subscripts of a matching element within extents, or all
zeros if no element matches or ARRAY has size zero.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-case-i-match-subscript-values`, `findloc-case-i-no-match-all-zero`, `findloc-case-i-zero-size-all-zero`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-013 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_case_i_values` cover findloc-case-i-match-subscript-values,
findloc-case-i-no-match-all-zero, findloc-case-i-zero-size-all-zero. p5 case (i) fixes
exact subscript values for a match and all-zero vectors for no-match and zero-size
arrays. The source uses direct intrinsic-expression inquiries for result characteristics
and exact integer, logical, character, shape, kind, IEEE NaN-class, or real-model
power-of-radix observations; processor-dependent approximations are not compared
exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-013 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-014: FINDLOC case ii applies MASK before matching

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** With MASK, FINDLOC returns subscripts of a matching element corresponding to a true MASK
element, or all zeros if no unmasked element matches, ARRAY has size zero, or every MASK
element is false.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-case-ii-masked-match-subscripts`, `findloc-case-ii-masked-no-match-zero`, `findloc-case-ii-all-false-mask-zero`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-014 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_case_ii_mask` cover findloc-case-ii-masked-match-subscripts,
findloc-case-ii-masked-no-match-zero, findloc-case-ii-all-false-mask-zero. p5 case (ii)
applies MASK before matching: a true matching mask selects [1,3], while no true matching
element or all-false MASK yields all zeros. The source uses direct intrinsic-expression
inquiries for result characteristics and exact integer, logical, character, shape, kind,
IEEE NaN-class, or real-model power-of-radix observations; processor-dependent
approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-014 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-015: FINDLOC case iii computes along DIM

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** For rank-one ARRAY with DIM, the scalar equals the first element of the no-DIM result;
otherwise each result element is FINDLOC of the corresponding section along DIM.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-case-iii-rank-one-dim-scalar`, `findloc-case-iii-section-wise-dim-values`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-015 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_case_iii_dim` cover findloc-case-iii-rank-one-dim-scalar,
findloc-case-iii-section-wise-dim-values. p5 case (iii) fixes the rank-one DIM scalar
and section-wise DIM reductions, including both nonzero and zero section results. The
source uses direct intrinsic-expression inquiries for result characteristics and exact
integer, logical, character, shape, kind, IEEE NaN-class, or real-model power-of-radix
observations; processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-015 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-016: FINDLOC matching uses .EQV. for logical and == otherwise

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** Logical ARRAY/VALUE comparisons use .EQV.; all other intrinsic types use ==, and a true
comparison means the element matches VALUE.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-logical-comparison-eqv`, `findloc-nonlogical-comparison-equality`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-016 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_comparison_semantics` cover findloc-logical-comparison-eqv,
findloc-nonlogical-comparison-equality. p6 selects .EQV. for logical ARRAY/VALUE and ==
for nonlogical intrinsic types; exact first matches distinguish the comparison. The
source uses direct intrinsic-expression inquiries for result characteristics and exact
integer, logical, character, shape, kind, IEEE NaN-class, or real-model power-of-radix
observations; processor-dependent approximations are not compared exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-016 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.88-017: FINDLOC BACK selects first or last matching element in array element order

**Source:** S16.9.88, J3/24-007, 18 December 2023, physical PDF410. **Class:** Effect.

**Definition:** Without DIM, multiple matches return the first element in array element order when BACK
is absent or false, and the last when BACK is true.

**Diagnostic obligation:** not-required.

**Facets:** `findloc-back-absent-or-false-first`, `findloc-back-true-last`, `findloc-back-array-element-order`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.88-017 intrinsics_16_9_j runtime fixture: the generated valid/f2023 fixture(s)
`findloc_back_order` cover findloc-back-absent-or-false-first, findloc-back-true-last,
findloc-back-array-element-order. p7 selects first or last matching element in Fortran
array element order; repeated vector and rank-two matches make BACK and element order
exact. The source uses direct intrinsic-expression inquiries for result characteristics
and exact integer, logical, character, shape, kind, IEEE NaN-class, or real-model
power-of-radix observations; processor-dependent approximations are not compared
exactly.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here. Visual source verification note: 16.9.88#p8 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; this packet keeps the census hash and does not base requirements on that
example.

S16.9.88-017 intrinsics_16_9_j fixture boundaries: Only ordinary single-image valid
programs are covered. Clause 16 unnumbered argument restrictions are positive controls
only and no diagnostic rejection is claimed. The fixtures do not assert compiler
consensus, coarray behavior, evaluation order beyond FINDLOC array element order, NaN
payload identity, or exact values for processor-dependent transcendental approximations.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

<!-- END GENERATED 16.9.88 -->
