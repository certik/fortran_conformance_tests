# Fortran 2023 12.3.4.1: File position concepts - access-position fixtures

The canonical catalogue is `doc/catalogues/file_position_concepts_12_3_4_1.json`. This author packet adds finite scratch-file runtime fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 12 facets in 6 program(s); 12 of 16 facets are represented and 4 remain pending.

<!-- BEGIN GENERATED 12.3.4.1 -->

### S12.3.4.1-001: Execution of input/output statements can affect external file position

**Source:** S12.3.4.1, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** Execution of certain input/output statements affects the position of an external file,
and certain circumstances can cause the position to become indeterminate.

**Diagnostic obligation:** not-required.

**Facets:** `io-execution-affects-external-file-position`, `file-position-can-become-indeterminate`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`io-execution-affects-external-file-position`. They create the external files in the run
directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited
to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.1-002: Initial and terminal file points bracket records or file storage units

**Source:** S12.3.4.1, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** The initial point is just before the first record or file storage unit; the terminal
point is just after the last record or file storage unit; for an empty file they are the
same position.

**Diagnostic obligation:** not-required.

**Facets:** `initial-point-before-first-unit`, `terminal-point-after-last-unit`, `empty-file-initial-terminal-same`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`empty-file-initial-terminal-same`, `initial-point-before-first-unit`,
`terminal-point-after-last-unit`. They create the external files in the run directory
with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited to exact
integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.1-003: A current record exists only when positioned within a record file record

**Source:** S12.3.4.1, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** For a record file positioned within a record, that record is the current record;
otherwise there is no current record.

**Diagnostic obligation:** not-required.

**Facets:** `current-record-when-positioned-within-record`, `no-current-record-otherwise`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`current-record-when-positioned-within-record`, `no-current-record-otherwise`. They
create the external files in the run directory with STATUS='SCRATCH' and also close with
STATUS='DELETE'. Oracles are limited to exact integer/character values, character
lengths, IOSTAT zero, IOSTAT_END, and POS= inequalities or values fixed by this source.
No file name, external representation, processor-specific IOSTAT number, IOMSG text,
formatted-stream byte spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.1-004: The preceding record is determined by file position in a record file

**Source:** S12.3.4.1, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** For a record file, the preceding record is the record before an in-record or
between-record position, is the last record at the terminal point when records exist,
and does not exist for an empty file, the initial point, or within the first record.

**Diagnostic obligation:** not-required.

**Facets:** `preceding-record-before-current-or-between`, `terminal-point-preceding-is-last-record`, `no-preceding-record-at-initial-empty-or-first`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`preceding-record-before-current-or-between`. They create the external files in the run
directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited
to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.1-005: The next record is determined by file position in a record file

**Source:** S12.3.4.1, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** For a record file, the next record is the following record from an in-record or
between-record position, is the first record at the initial point when records exist,
and does not exist for an empty file, the terminal point, or within the last record.

**Diagnostic obligation:** not-required.

**Facets:** `next-record-after-current-or-between`, `initial-point-next-is-first-record`, `no-next-record-at-terminal-empty-or-last`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`initial-point-next-is-first-record`, `next-record-after-current-or-between`,
`no-next-record-at-terminal-empty-or-last`. They create the external files in the run
directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited
to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.1-006: Stream file position is between storage units, at endpoints, or undefined

**Source:** S12.3.4.1, J3/24-007, 18 December 2023, physical PDF246. **Class:** Effect.

**Definition:** For stream access, the file position is between two file storage units, at the initial
point, at the terminal point, or undefined.

**Diagnostic obligation:** not-required.

**Facets:** `stream-position-between-storage-units`, `stream-position-at-initial-or-terminal`, `stream-position-can-be-undefined`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`stream-position-at-initial-or-terminal`, `stream-position-between-storage-units`. They
create the external files in the run directory with STATUS='SCRATCH' and also close with
STATUS='DELETE'. Oracles are limited to exact integer/character values, character
lengths, IOSTAT zero, IOSTAT_END, and POS= inequalities or values fixed by this source.
No file name, external representation, processor-specific IOSTAT number, IOMSG text,
formatted-stream byte spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

<!-- END GENERATED 12.3.4.1 -->

## Access-position fixture derivations

### `io_execution_moves_position` / `S12.3.4.1-001`

**Facets:** `io-execution-affects-external-file-position`.

12.3.4.1 p1 says some I/O statements affect external-file position; the second read observes that the first read advanced to the next record.

Feature mutations: `rewind-between-reads`, `pre-read-1-first-remove-sentinel-init`, `pre-read-1-first-expected-sentinel-init`, `pre-read-1-first-default-sentinel-init`, `pre-read-2-second-remove-sentinel-init`, `pre-read-2-second-expected-sentinel-init`, `pre-read-2-second-default-sentinel-init`.

### `initial_terminal_empty_points` / `S12.3.4.1-002`

**Facets:** `initial-point-before-first-unit`, `terminal-point-after-last-unit`, `empty-file-initial-terminal-same`.

12.3.4.1 p2 defines initial before first and terminal after last; REWIND/read observes the first record, reading past the last or in an empty file returns IOSTAT_END.

Feature mutations: `drop-initial-rewind`, `add-record-before-terminal`, `make-empty-nonempty`, `pre-read-1-value-remove-sentinel-init`, `pre-read-1-value-expected-sentinel-init`, `pre-read-1-value-default-sentinel-init`, `pre-read-2-value-remove-sentinel-init`, `pre-read-2-value-expected-sentinel-init`, `pre-read-2-value-default-sentinel-init`.

### `current_record` / `S12.3.4.1-003`

**Facets:** `current-record-when-positioned-within-record`, `no-current-record-otherwise`.

12.3.4.1 p3 defines a current record only while positioned within a record; a nonadvancing read leaves ABCD current, and the following advancing read ends it before WXYZ.

Feature mutations: `make-head-advancing`, `backspace-before-next`, `pre-read-1-head-remove-sentinel-init`, `pre-read-1-head-expected-sentinel-init`, `pre-read-1-head-default-sentinel-init`, `pre-read-2-tail-remove-sentinel-init`, `pre-read-2-tail-expected-sentinel-init`, `pre-read-2-tail-default-sentinel-init`, `pre-read-3-next-remove-sentinel-init`, `pre-read-3-next-expected-sentinel-init`, `pre-read-3-next-default-sentinel-init`.

### `preceding_records` / `S12.3.4.1-004`

**Facets:** `preceding-record-before-current-or-between`.

12.3.4.1 p4 defines the preceding record before a between-record position; after reading record 2, BACKSPACE/read observes record 22 again.

Feature mutations: `rewind-not-backspace-between`, `pre-read-1-prior-remove-sentinel-init`, `pre-read-1-prior-expected-sentinel-init`, `pre-read-1-prior-default-sentinel-init`.

### `next_records` / `S12.3.4.1-005`

**Facets:** `next-record-after-current-or-between`, `initial-point-next-is-first-record`, `no-next-record-at-terminal-empty-or-last`.

12.3.4.1 p5 defines the next record at initial and between records and says no next exists at terminal or in an empty file; reads observe those cases.

Feature mutations: `drop-next-rewind`, `backspace-before-middle`, `add-fourth-record`, `pre-read-1-first-remove-sentinel-init`, `pre-read-1-first-expected-sentinel-init`, `pre-read-1-first-default-sentinel-init`, `pre-read-2-middle-remove-sentinel-init`, `pre-read-2-middle-expected-sentinel-init`, `pre-read-2-middle-default-sentinel-init`, `pre-read-3-last-remove-sentinel-init`, `pre-read-3-last-expected-sentinel-init`, `pre-read-3-last-default-sentinel-init`.

### `stream_positions` / `S12.3.4.1-006`

**Facets:** `stream-position-between-storage-units`, `stream-position-at-initial-or-terminal`.

12.3.4.1 p6 places stream positions at initial, terminal, or between storage units; INQUIRE observes endpoints and POS=2 reads between known units.

Feature mutations: `stream-inquire-after-write`, `stream-read-first-not-second`, `pre-read-1-ch-remove-sentinel-init`, `pre-read-1-ch-expected-sentinel-init`, `pre-read-1-ch-default-sentinel-init`.

