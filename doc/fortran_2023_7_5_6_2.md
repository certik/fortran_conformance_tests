# Fortran 2023: 7.5.6.2 The finalization process

**Catalogue source review: reviewed.** Case and evidence adjudications are separate content-bound records.

The bounded corpus has **17 run-phase cases**, **16 effects** and **1 positive control**, with **29 mandatory-event checkpoints**. **17 of 22 facets are represented; 5 remain pending.**

## Source and bounded ownership

Authority: J3/24-007, **18 December 2023**, **688 PDF pages**, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The three 7.5.6.2 base units are on physical PDF102-103. Original PDF102-104,
15.9.1/15.9.3 on PDF362-363 and necessary deallocation/definition/lifetime
conditions were read. The preceding POINT_LENGTH note and other finalization
sections are not new local requirements.

The independent source review is `finalization-source-review.json`
(SHA-256 `6280f2250fbf5464c58144a0207a56a51c88fd4771830bf04942906c2ec092ea`).
FSRC-001 was corrected by the preserved `57ba04c24354a1ef30ef41cb19bf890ac2ba18c5`
source checkpoint; the correction receipt hash is
`bc1b70834d015af14d9cf7a2fbe5f70229ee9a85109fb85b325eb3dd5aed490d`.
Only its 7.5.6.2 catalogue/view are owned here. FINAL statement constraints,
event definitions, termination and conditional external observers retain their
canonical owners.
Independent concrete fixture, observer, refresh and provenance decisions are
recorded in `doc/source_audits/batch_016.json`; source/case approval remains
separate from generation and processor observations.

## Concrete oracle premises

* **Known event first.** Every observation follows a guarded whole-allocated
  DEALLOCATE, with successful allocation/deallocation status and correct live
  object state. There is no explicit CALL to a final subroutine and no
  declaration-only finalization proof. DEALLOCATE is the prerequisite event,
  not a new 7.5.6.3 event-owned fixture.
* **Defined, independently named inputs.** Every primitive token is set through
  its actual named component/element before the event. Setup never assigns a
  whole finalizable object, creates a finalizable result or uses a structure
  constructor. All later observations read only separate saved INTEGER logs;
  callbacks read their own current dummy and safe SIZE/RANK/type-parameter
  properties, never a dead peer or a retained alias.
* **Exact selection.** Final dummies are nonpolymorphic TYPE, nonoptional,
  nonpointer, nonallocatable and noncoarray, with INOUT (not OUT or VALUE).
  All PDT LEN parameters are assumed. KIND tags1/2 are abstract type parameters,
  never intrinsic representation selectors. Assumed rank is the sole same-KIND
  final in its family; it does not compete illegally with a fixed-rank final.
* **Elemental qualifications.** Logging elemental callbacks are explicitly
  IMPURE ELEMENTAL with scalar data dummies and valid intent under15.9.
  A same-KIND rank-one ordinary/scalar-elemental family is legal; exact rank-one
  wins, rank-two falls back, and zero fallback elements produce no scalar calls.
  The zero fallback is paired with nonzero exact and elemental controls.
  Logs use counts/multisets, not an assumed elemental global-side-effect order.
* **Nonvacuous components.** An exact array final still records one invocation
  for an empty array. A zero outer array has a containing marker and a nonzero
  component control. Scalar components of each outer array element require
  scalar child calls, while each real array component invokes its array final
  separately. A no-own-match scalar still produces its real component callback.
* **Pointer target survives separately.** Owner destruction has a positive own
  callback. Its pointer target is an independently reachable, actually allocated
  allocatable TARGET, not a saved nonallocated variable; a later legal deallocation
  supplies the target-positive event. No owner/alias is dereferenced after death.
  Allocatable component cleanup has its own event graph and remains pending.
* **Partial parent order, not layout.** Own processing precedes declared eligible
  components; all those siblings precede actual recursive parent processing.
  Inherited components are processed at the parent stage exactly once, not again
  at child stage. Parent finals are not inherited own finals. Every allowed
  sibling permutation is accepted with exact membership/multiplicity.
* **Role and access limits.** Private FINAL names are invoked automatically;
  the client imports only the type and never calls the private procedure.
  S7.5.6.2-004 remains restriction/not-required and its run is positive-control.
  There is no forbidden peer access, expected trap, new diagnostic policy,
  ERROR STOP profile or post-termination observer. ERROR STOP in these programs
  only marks a failed ordinary self-check; it is not the property being tested.

FQ01-08 remain binding source qualifications. Function-result/executable-
construct and PURE/image-termination observer work is outside these fixtures.
No hypothetical graph or Python countermodel replaces an automatic Fortran event.
Each final name is declared in its own permitted FINAL statement (R746/R748/R753),
so comma-list parsing is not an incidental prerequisite for these process cases.
Failed log checks print only saved primitive log entries after the event; they
do not inspect finalized objects. Original list-form failures and later
source-equivalent declaration/logging refinements remain separate observations.

## Evidence and integration boundaries

All runtime cases stay run-phase if a compiler rejects, crashes or misses a
callback. Source-valid reference failures do not justify weakening the expected
set/order, changing event ownership, making a logging finalizer PURE, or replacing
the program with a compile-only/no-op control. Original and refresh reports,
complete command traces and per-case current-fingerprint selection are preserved
separately in `final-process-phase2-handoff.json`; a current-row index is not one
invented processor run. The target is the frozen LF411 a0afaa840b toolchain;
GNU f2023 and actual Flang f2018 modes are recorded without relabelling.

Only this process catalogue/view, generator/test and `tests/fixtures/final_process_*`
inputs are owned by the author packet. Its validation-only local index overlay
is not merged wholesale. The packet preserved all1546 author-base case bindings
and inputs; serial main integration separately preserves the current prior
case universe. Adjudication and baseline/evidence updates remain explicit
coordinator work, never side effects of this generator.

## Definitions

<!-- BEGIN GENERATED 7.5.6.2 -->

### S7.5.6.2-001: Finalization selects a matching own final or a qualified fallback

**Source:** 7.5.6.2, p1.finalizable-only, p1.exact-kind-rank-selection, p1.elemental-fallback, p1.assumed-rank-fallback, p1.no-selected-procedure; J3/24-007, 18 December 2023, PDF 102-103. **Class:** Effect.

**Definition:** For a finalizable entity, first select an own final of its dynamic type whose dummy has
equal KIND parameter values and rank. Otherwise select a same-KIND elemental final or a
same-KIND assumed-rank final. If neither condition applies, no procedure is called at
this step. This does not omit the subsequent component or parent steps.

**Diagnostic obligation:** not-required.

**Facets:** `scalar-exact-match`, `array-exact-match`, `kind-tuple-dispatch`, `exact-array-before-elemental`, `assumed-rank-fallback`, `no-matching-own-final`, `private-final-name`, `dynamic-type-selection-graph`.

**Oracle:** Use distinct finalizer counters and defined primitive tokens. Compare callback kind/rank
identity and counts rather than addresses. Use separate ordinary saved integer
logs/counters, not reads of other objects already finalized. Initialize payloads through
primitive named components rather than unintended whole-object assignments that
themselves finalize the old left side. Keep every callback argument and accessed
component defined. Pointer association and allocation inquiries require independently
established valid states. Counter/order observers are IMPURE where side effects require
it; do not silently place an impure finalizer behind a PURE function or procedure.

Finite process implementation: 7 run-phase cases represent 7 finite facets with
mandatory guarded whole-allocatable DEALLOCATE events before observations. Exact log
multisets and required partial-order edges are checked without peeking at finalized
peers. 1 source/use graph facets remain pending. Source/case/evidence adjudications are
separate content-bound records; authorship and observations do not confer approval.

**Oracle limitation:** Do not construct an illegal assumed-rank/exact-rank competition. Ordinary scalar finals
are not elemental fallbacks, and finalizability alone is not a guarantee that this
own-procedure step calls anything.

**Dependencies:** C791-C794;7.3.2.3 and7.3.3;15.9.1/15.9.3, PDF362-363;7.5.6.1 p2;9.7.3 and19.6.

### S7.5.6.2-002: Declared nonallocatable finalizable components are finalized separately

**Source:** 7.5.6.2, p1.nonallocatable-finalizable-components, p1.components-in-type-definition, p1.component-order-choice, p1.array-components-per-element; J3/24-007, 18 December 2023, PDF 102-103. **Class:** Effect.

**Definition:** After the own-procedure step, finalize the nonallocatable finalizable components
appearing in the type definition. Their order is processor dependent. For an array
entity, finalize each such component of each element separately, not one synthetic whole
component array.

**Diagnostic obligation:** not-required.

**Facets:** `plain-component-recursion`, `sibling-order-independent`, `array-element-components`, `zero-sized-outer-array`, `pointer-target-exclusion`, `allocatable-component-event-graph`.

**Oracle:** Use per-token occurrence counts and only the specified partial order. Use separate
ordinary saved integer logs/counters, not reads of other objects already finalized.
Initialize payloads through primitive named components rather than unintended
whole-object assignments that themselves finalize the old left side. Keep every callback
argument and accessed component defined. Pointer association and allocation inquiries
require independently established valid states. Counter/order observers are IMPURE where
side effects require it; do not silently place an impure finalizer behind a PURE
function or procedure.

Finite process implementation: 5 run-phase cases represent 5 finite facets with
mandatory guarded whole-allocatable DEALLOCATE events before observations. Exact log
multisets and required partial-order edges are checked without peeking at finalized
peers. 1 source/use graph facets remain pending. Source/case/evidence adjudications are
separate content-bound records; authorship and observations do not confer approval.

**Oracle limitation:** The implicit parent component has its own later step; pointer targets are not ordinary
nonallocatable components for value purposes. Never assert that allocatable children
produce no cleanup callbacks in every context.

**Dependencies:** 7.5.6.1 p2;3.30;7.5.7.2;9.4.2 p5;9.7.3.2 p8-p9, PDF166.

### S7.5.6.2-003: Own processing, declared components and finalizable parent processing are ordered stages

**Source:** 7.5.6.2, p1.ordered-steps, p1.extended-type-condition, p1.finalizable-parent-condition, p1.parent-last; J3/24-007, 18 December 2023, PDF 102-103. **Class:** Effect.

**Definition:** Finalization follows the specified step sequence: own selected final, declared
nonallocatable finalizable components, then the parent component when the entity is
extended and the parent type is finalizable. The parent step invokes its own complete
finalization process. Final subroutines are not inherited into the earlier own-selection
step.

**Diagnostic obligation:** not-required.

**Facets:** `own-before-components`, `components-before-parent`, `parent-without-own-child-final`, `recursive-parent-process`, `stage-and-inheritance-use-graph`.

**Oracle:** Distinct integer tokens identify stage membership. Validate partial orders and exact
multiplicities, not arbitrary total log sequences. Use separate ordinary saved integer
logs/counters, not reads of other objects already finalized. Initialize payloads through
primitive named components rather than unintended whole-object assignments that
themselves finalize the old left side. Keep every callback argument and accessed
component defined. Pointer association and allocation inquiries require independently
established valid states. Counter/order observers are IMPURE where side effects require
it; do not silently place an impure finalizer behind a PURE function or procedure.

Finite process implementation: 4 run-phase cases represent 4 finite facets with
mandatory guarded whole-allocatable DEALLOCATE events before observations. Exact log
multisets and required partial-order edges are checked without peeking at finalized
peers. 1 source/use graph facets remain pending. Source/case/evidence adjudications are
separate content-bound records; authorship and observations do not confer approval.

**Oracle limitation:** Parent processing is conditional on actual finalizability. An absence of an own child
callback cannot justify skipping its components or parent.

**Dependencies:** 7.5.6.1 note2;7.5.7.2, PDF104;9.7.3.2 p9;7.5.6.3 events.

### S7.5.6.2-004: Finalizers must not access other event entities already finalized

**Source:** 7.5.6.2, p2.multiple-entity-event, p2.no-reference-to-finalized-other, p2.no-definition-of-finalized-other; J3/24-007, 18 December 2023, PDF 102-103. **Class:** Restriction.

**Definition:** When an event finalizes several entities, their ordering is processor dependent. A final
subroutine for one entity must not reference or define another of those entities after
that other entity has been finalized. An assumed declaration or deallocation-list order
cannot justify such access.

**Diagnostic obligation:** not-required.

**Facets:** `independent-entity-control`, `reference-context-graph`, `definition-context-graph`.

**Oracle:** Allowed controls check an order-independent set of own-token callbacks. No event
callback is allowed to inspect the other finalized objects.

Finite process implementation: 1 run-phase cases represent 1 finite facets with
mandatory guarded whole-allocatable DEALLOCATE events before observations. Exact log
multisets and required partial-order edges are checked without peeking at finalized
peers. These are positive controls for the restriction, not forbidden-access effects. 2
source/use graph facets remain pending. Source/case/evidence adjudications are separate
content-bound records; authorship and observations do not confer approval.

**Oracle limitation:** There is no general standard-required diagnostic for this prose restriction. Do not
create a must-crash negative, infer a deterministic order, or conflate pointer
association status with the finalized target value.

**Dependencies:** 7.5.6.2 p2;7.5.6.3;9.4.2;19.5.2/19.6.6.

<!-- END GENERATED 7.5.6.2 -->

## Exact finite case observations

### `S7_5_6_2_001_valid__final_process_scalar`

**Primary:** S7.5.6.2-001; **facet:** `scalar-exact-match`; **phase/evidence:** run / effect.

Scalar exact own final; no component/parent or setup finalization.

* `scalar` after `DEALLOCATE(value)`: multiset `[(10, 17)]`; required before-edges `[]`.

### `S7_5_6_2_001_valid__final_process_array_exact`

**Primary:** S7.5.6.2-001; **facet:** `array-exact-match`; **phase/evidence:** run / effect.

Rank-one/rank-two ordinary finals; exact empty rank-one invokes its array final once.

* `vector` after `DEALLOCATE(vector)`: multiset `[(10, 2), (11, 17), (11, 19)]`; required before-edges `[]`.
* `matrix` after `DEALLOCATE(matrix)`: multiset `[(20, 4), (21, 23), (21, 29), (21, 31), (21, 37)]`; required before-edges `[]`.
* `empty-vector-exact` after `DEALLOCATE(vector)`: multiset `[(10, 0)]`; required before-edges `[]`.

### `S7_5_6_2_001_valid__final_process_kind_tuple`

**Primary:** S7.5.6.2-001; **facet:** `kind-tuple-dispatch`; **phase/evidence:** run / effect.

Four abstract KIND tuples, not intrinsic selectors; both LEN parameters are assumed in every final. A repeated tuple with different LEN values selects the same final.

* `kind-a` after `DEALLOCATE(a)`: multiset `[(101, 11), (901, 3), (902, 5)]`; required before-edges `[]`.
* `kind-b` after `DEALLOCATE(b)`: multiset `[(102, 13), (901, 7), (902, 9)]`; required before-edges `[]`.
* `kind-c` after `DEALLOCATE(c)`: multiset `[(201, 17), (901, 4), (902, 6)]`; required before-edges `[]`.
* `kind-d` after `DEALLOCATE(d)`: multiset `[(202, 19), (901, 8), (902, 10)]`; required before-edges `[]`.
* `kind-e` after `DEALLOCATE(e)`: multiset `[(101, 23), (901, 12), (902, 14)]`; required before-edges `[]`.

### `S7_5_6_2_001_valid__final_process_exact_before_elemental`

**Primary:** S7.5.6.2-001; **facet:** `exact-array-before-elemental`; **phase/evidence:** run / effect.

Legal rank-one ordinary plus scalar IMPURE ELEMENTAL family; no assumed-rank competitor. Empty fallback has an independently verified nonzero fallback control.

* `exact-vector-before-elemental` after `DEALLOCATE(vector)`: multiset `[(10, 2)]`; required before-edges `[]`.
* `rank-two-elemental-fallback` after `DEALLOCATE(matrix)`: multiset `[(20, 17), (20, 19), (20, 23), (20, 29)]`; required before-edges `[]`.
* `empty-rank-two-elemental` after `DEALLOCATE(matrix)`: multiset `[]`; required before-edges `[]`.

### `S7_5_6_2_001_valid__final_process_assumed_rank`

**Primary:** S7.5.6.2-001; **facet:** `assumed-rank-fallback`; **phase/evidence:** run / effect.

Sole same-KIND final; RANK/SELECT RANK precede rank-specific access. Empty array still invokes the assumed-rank final once.

* `assumed-scalar` after `DEALLOCATE(scalar)`: multiset `[(50, 0), (60, 11)]`; required before-edges `[]`.
* `assumed-vector` after `DEALLOCATE(vector)`: multiset `[(50, 1), (60, 17), (60, 19)]`; required before-edges `[]`.
* `assumed-empty-matrix` after `DEALLOCATE(matrix)`: multiset `[(50, 2)]`; required before-edges `[]`.

### `S7_5_6_2_001_valid__final_process_no_own_match`

**Primary:** S7.5.6.2-001; **facet:** `no-matching-own-final`; **phase/evidence:** run / effect.

A scalar cannot select the sole ordinary rank-one own final; its defined plain leaf provides positive automatic process evidence.

* `no-own-but-component` after `DEALLOCATE(value)`: multiset `[(30, 17)]`; required before-edges `[]`.

### `S7_5_6_2_001_valid__final_process_private_name`

**Primary:** S7.5.6.2-001; **facet:** `private-final-name`; **phase/evidence:** run / effect.

Client imports only the public type; private final procedure is invoked only by automatic finalization.

* `private-final-automatic` after `DEALLOCATE(value)`: multiset `[(10, 23)]`; required before-edges `[]`.

### `S7_5_6_2_002_valid__final_process_plain_recursion`

**Primary:** S7.5.6.2-002; **facet:** `plain-component-recursion`; **phase/evidence:** run / effect.

Two ordinary nonallocatable component levels; only the terminal leaf has an own final.

* `nested-component` after `DEALLOCATE(value)`: multiset `[(20, 17)]`; required before-edges `[]`.

### `S7_5_6_2_002_valid__final_process_siblings`

**Primary:** S7.5.6.2-002; **facet:** `sibling-order-independent`; **phase/evidence:** run / effect.

Exact two-member multiset; both sibling orders accepted without peer reads.

* `siblings` after `DEALLOCATE(value)`: multiset `[(20, 11), (20, 13)]`; required before-edges `[]`.

### `S7_5_6_2_002_valid__final_process_array_components`

**Primary:** S7.5.6.2-002; **facet:** `array-element-components`; **phase/evidence:** run / effect.

Each actual outer element supplies one scalar leaf and one rank-one leaf component. Two scalar calls and two distinct array-final calls are required; no synthetic aggregate of scalar components.

* `per-outer-element-components` after `DEALLOCATE(values)`: multiset `[(10, 2), (20, 11), (20, 13), (30, 2), (30, 2), (31, 21), (31, 23), (31, 25), (31, 27)]`; required before-edges `[]`.

### `S7_5_6_2_002_valid__final_process_empty_outer`

**Primary:** S7.5.6.2-002; **facet:** `zero-sized-outer-array`; **phase/evidence:** run / effect.

The exact outer array final proves the empty containing process ran; the nonzero control proves leaf callback membership. No empty-only success case.

* `empty-container` after `DEALLOCATE(values)`: multiset `[(10, 0)]`; required before-edges `[]`.
* `nonempty-container-control` after `DEALLOCATE(values)`: multiset `[(10, 2), (20, 17), (20, 19)]`; required before-edges `[]`.

### `S7_5_6_2_002_valid__final_process_pointer_exclusion`

**Primary:** S7.5.6.2-002; **facet:** `pointer-target-exclusion`; **phase/evidence:** run / effect.

Owner has its own positive final. Pointer target remains independently reachable through its actual allocated variable, which is later legally deallocated; no saved nonallocated target or dead owner alias is accessed.

* `pointer-excluded-from-owner` after `DEALLOCATE(owner)`: multiset `[(10, 11)]`; required before-edges `[]`.
* `independent-target-positive-event` after `DEALLOCATE(target)`: multiset `[(20, 31)]`; required before-edges `[]`.

### `S7_5_6_2_003_valid__final_process_own_before_components`

**Primary:** S7.5.6.2-003; **facet:** `own-before-components`; **phase/evidence:** run / effect.

Own precedes both declared components; either sibling order is legal. Exact event membership/multiplicity is checked before the edges.

* `own-before-siblings` after `DEALLOCATE(value)`: multiset `[(10, 1), (20, 11), (20, 13)]`; required before-edges `[((10, 1), (20, 11)), ((10, 1), (20, 13))]`.

### `S7_5_6_2_003_valid__final_process_components_before_parent`

**Primary:** S7.5.6.2-003; **facet:** `components-before-parent`; **phase/evidence:** run / effect.

Scalar own stage, declared child component, then the actual scalar parent process; no read of an already finalized child component in parent callback.

* `components-before-parent` after `DEALLOCATE(value)`: multiset `[(10, 1), (20, 11), (30, 21)]`; required before-edges `[((10, 1), (20, 11)), ((20, 11), (30, 21))]`.

### `S7_5_6_2_003_valid__final_process_parent_without_child_final`

**Primary:** S7.5.6.2-003; **facet:** `parent-without-own-child-final`; **phase/evidence:** run / effect.

Child has no own final. Its declared leaf is processed before parent final exactly once, not before/again as an inherited own final.

* `no-own-child-parent-later` after `DEALLOCATE(value)`: multiset `[(20, 11), (30, 21)]`; required before-edges `[((20, 11), (30, 21))]`.

### `S7_5_6_2_003_valid__final_process_recursive_parent`

**Primary:** S7.5.6.2-003; **facet:** `recursive-parent-process`; **phase/evidence:** run / effect.

Child siblings precede complete parent process; parent's own callback precedes its declared siblings; root follows them. Four sibling permutations are permitted. Inherited components may not be traversed in both child and parent stages.

* `recursive-parent-stages` after `DEALLOCATE(value)`: multiset `[(10, 1), (20, 11), (20, 13), (30, 21), (20, 31), (20, 33), (40, 41)]`; required before-edges `[((10, 1), (20, 11)), ((10, 1), (20, 13)), ((20, 11), (30, 21)), ((20, 13), (30, 21)), ((30, 21), (20, 31)), ((30, 21), (20, 33)), ((20, 31), (40, 41)), ((20, 33), (40, 41))]`.

### `S7_5_6_2_004_valid__final_process_independent_entities`

**Primary:** S7.5.6.2-004; **facet:** `independent-entity-control`; **phase/evidence:** run / positive-control.

One multi-entity mandatory event. Each callback reads only its own token and writes separate saved nonfinalizable logs; both peer orders accepted. This is a positive control, not a forbidden-peer-access or trapping test.

* `independent-event-peers` after `DEALLOCATE(left,right)`: multiset `[(20, 17), (20, 19)]`; required before-edges `[]`.

## Complete finite pending plans

These five canonical source/use graphs are unrepresented; no model or declaration clears them.

### Pending S7.5.6.2-001

* **`dynamic-type-selection-graph`** — Pending canonical allocation/dynamic-type/finalizability and inherited-final relationships with complete current type states.

### Pending S7.5.6.2-002

* **`allocatable-component-event-graph`** — Pending separate9.7.3.2 automatic-deallocation evidence; the excluded allocatable component may still finalize through its own event after the containing final procedure.

### Pending S7.5.6.2-003

* **`stage-and-inheritance-use-graph`** — Pending canonical parent-component accessibility/inheritance, allocation and explicit event links; no declaration/storage ordering is substituted.

### Pending S7.5.6.2-004

* **`reference-context-graph`** — Pending source/use evidence for the prohibition on references to already-finalized other entities, without executing an undefined/dangling read or demanding a runtime trap.
* **`definition-context-graph`** — Pending the separately accounted prohibited definition contexts and aliases, with no fabricated output for a nonconforming program.

## Reproduction and remaining gates

`python3 -B tools/generate_final_process_fixtures.py --check` verifies exact owned inputs, pending/phase metadata and both document regions. The targeted Python regressions independently enumerate permitted partial orders and reject missing, duplicate, aggregated, wrong-stage and vacuous traces. Shared parameter inputs remain unchanged. Source/case approval is not inferred from any check or observation; consult the current adjudication records before integration.
