# Fortran 2023 17.11.26: IEEE_NEXT_DOWN

The canonical catalogue is `doc/catalogues/ieee_next_down_17_11_26.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.26")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.26 -->

### S17.11.26-001: IEEE_NEXT_DOWN argument restriction

**Source:** S17.11.26, J3/24-007, 18 December 2023, physical PDF497-498. **Class:** Restriction.

**Definition:** X shall be real.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_NEXT_DOWN-X-real`.

**Oracle:** Source registration only. Future fixtures guard the restricted IEEE procedure under test
and every restricted IEEE observer invoked in the plan with that procedure's own
IEEE_SUPPORT_DATATYPE or more specific IEEE_SUPPORT_* inquiry before invocation,
initialize observed results and flags to non-default or opposite sentinels, and assert
only portable properties: exact kind/rank/type, exact finite results, IEEE_IS_NAN or
IEEE_CLASS observations for NaNs, IEEE_IS_NEGATIVE observations for signs, and named
exception flags. Signed zeros are constructed with IEEE_COPY_SIGN or IEEE_VALUE and
pre-verified with IEEE_IS_NEGATIVE under its observer guard; bare negative-zero literals
are not used. Exception flag observations are keyed to IEEE_SUPPORT_FLAG(flag,X). Named
preamble HALTING-OFF-PREAMBLE(flag): the IEEE_SUPPORT_HALTING(flag)-true branch calls
IEEE_SET_HALTING_MODE(flag,.FALSE.); the IEEE_SUPPORT_HALTING(flag)-false branch reads
IEEE_GET_HALTING_MODE(flag,h) under that inquiry's guard and proceeds only for
h=.FALSE., otherwise skips. Every flag row that can signal runs HALTING-OFF-PREAMBLE for
each observed flag, clears all flags before the row, and no-other-exception plans assert
the other four flags quiet under their own IEEE_SUPPORT_FLAG guards. Unsupported inquiry
branches are skips, not passes; unnumbered argument and invocation restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing fixture binding for
IEEE_MIN_MAG, IEEE_MIN_NUM, IEEE_MIN_NUM_MAG, IEEE_NEXT_AFTER, IEEE_NEXT_DOWN,
IEEE_NEXT_UP, or S17.11.22-S17.11.27. NaN payloads, choice of NaN operand, signaling-NaN
payload preservation, exact halting behavior, and support for optional IEEE features are
not portable oracles.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, procedure, intrinsic-operation, and intrinsic-procedure
foundations from clauses 6 through 16 where already catalogued. Unregistered
dependencies retained: Clause 17 IEEE module constants, flags, class and rounding types,
IEEE_VALUE, IEEE_IS_NAN, IEEE_IS_NEGATIVE, IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG,
IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, IEEE_SUPPORT_SUBNORMAL, IEEE_GET_FLAG,
IEEE_SET_FLAG, IEEE_MIN, IEEE_NEXT_AFTER, IEEE_NEXT_UP, IEEE_NEXT_DOWN, HUGE, EPSILON,
TINY, RADIX, and ISO/IEC 60559 operation terminology used by future guarded fixtures.

### S17.11.26-002: IEEE_NEXT_DOWN invocation support restrictions

**Source:** S17.11.26, J3/24-007, 18 December 2023, physical PDF497-498. **Class:** Restriction.

**Definition:** IEEE_NEXT_DOWN(X) shall not be invoked with IEEE_SUPPORT_DATATYPE(X) false, and
IEEE_NEXT_DOWN(-HUGE(X)) shall not be invoked with IEEE_SUPPORT_INF(X) false.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_NEXT_DOWN-requires-supported-datatype`, `IEEE_NEXT_DOWN-negative-huge-requires-inf-support`.

**Oracle:** Source registration only. Future fixtures guard the restricted IEEE procedure under test
and every restricted IEEE observer invoked in the plan with that procedure's own
IEEE_SUPPORT_DATATYPE or more specific IEEE_SUPPORT_* inquiry before invocation,
initialize observed results and flags to non-default or opposite sentinels, and assert
only portable properties: exact kind/rank/type, exact finite results, IEEE_IS_NAN or
IEEE_CLASS observations for NaNs, IEEE_IS_NEGATIVE observations for signs, and named
exception flags. Signed zeros are constructed with IEEE_COPY_SIGN or IEEE_VALUE and
pre-verified with IEEE_IS_NEGATIVE under its observer guard; bare negative-zero literals
are not used. Exception flag observations are keyed to IEEE_SUPPORT_FLAG(flag,X). Named
preamble HALTING-OFF-PREAMBLE(flag): the IEEE_SUPPORT_HALTING(flag)-true branch calls
IEEE_SET_HALTING_MODE(flag,.FALSE.); the IEEE_SUPPORT_HALTING(flag)-false branch reads
IEEE_GET_HALTING_MODE(flag,h) under that inquiry's guard and proceeds only for
h=.FALSE., otherwise skips. Every flag row that can signal runs HALTING-OFF-PREAMBLE for
each observed flag, clears all flags before the row, and no-other-exception plans assert
the other four flags quiet under their own IEEE_SUPPORT_FLAG guards. Unsupported inquiry
branches are skips, not passes; unnumbered argument and invocation restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing fixture binding for
IEEE_MIN_MAG, IEEE_MIN_NUM, IEEE_MIN_NUM_MAG, IEEE_NEXT_AFTER, IEEE_NEXT_DOWN,
IEEE_NEXT_UP, or S17.11.22-S17.11.27. NaN payloads, choice of NaN operand, signaling-NaN
payload preservation, exact halting behavior, and support for optional IEEE features are
not portable oracles.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, procedure, intrinsic-operation, and intrinsic-procedure
foundations from clauses 6 through 16 where already catalogued. Unregistered
dependencies retained: Clause 17 IEEE module constants, flags, class and rounding types,
IEEE_VALUE, IEEE_IS_NAN, IEEE_IS_NEGATIVE, IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG,
IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, IEEE_SUPPORT_SUBNORMAL, IEEE_GET_FLAG,
IEEE_SET_FLAG, IEEE_MIN, IEEE_NEXT_AFTER, IEEE_NEXT_UP, IEEE_NEXT_DOWN, HUGE, EPSILON,
TINY, RADIX, and ISO/IEC 60559 operation terminology used by future guarded fixtures.

### S17.11.26-003: IEEE_NEXT_DOWN result characteristics match X

**Source:** S17.11.26, J3/24-007, 18 December 2023, physical PDF497-498. **Class:** Effect.

**Definition:** The IEEE_NEXT_DOWN result has the same type and kind type parameter as X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_NEXT_DOWN-result-same-type-kind-as-X`.

**Oracle:** Source registration only. Future fixtures guard the restricted IEEE procedure under test
and every restricted IEEE observer invoked in the plan with that procedure's own
IEEE_SUPPORT_DATATYPE or more specific IEEE_SUPPORT_* inquiry before invocation,
initialize observed results and flags to non-default or opposite sentinels, and assert
only portable properties: exact kind/rank/type, exact finite results, IEEE_IS_NAN or
IEEE_CLASS observations for NaNs, IEEE_IS_NEGATIVE observations for signs, and named
exception flags. Signed zeros are constructed with IEEE_COPY_SIGN or IEEE_VALUE and
pre-verified with IEEE_IS_NEGATIVE under its observer guard; bare negative-zero literals
are not used. Exception flag observations are keyed to IEEE_SUPPORT_FLAG(flag,X). Named
preamble HALTING-OFF-PREAMBLE(flag): the IEEE_SUPPORT_HALTING(flag)-true branch calls
IEEE_SET_HALTING_MODE(flag,.FALSE.); the IEEE_SUPPORT_HALTING(flag)-false branch reads
IEEE_GET_HALTING_MODE(flag,h) under that inquiry's guard and proceeds only for
h=.FALSE., otherwise skips. Every flag row that can signal runs HALTING-OFF-PREAMBLE for
each observed flag, clears all flags before the row, and no-other-exception plans assert
the other four flags quiet under their own IEEE_SUPPORT_FLAG guards. Unsupported inquiry
branches are skips, not passes; unnumbered argument and invocation restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing fixture binding for
IEEE_MIN_MAG, IEEE_MIN_NUM, IEEE_MIN_NUM_MAG, IEEE_NEXT_AFTER, IEEE_NEXT_DOWN,
IEEE_NEXT_UP, or S17.11.22-S17.11.27. NaN payloads, choice of NaN operand, signaling-NaN
payload preservation, exact halting behavior, and support for optional IEEE features are
not portable oracles.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, procedure, intrinsic-operation, and intrinsic-procedure
foundations from clauses 6 through 16 where already catalogued. Unregistered
dependencies retained: Clause 17 IEEE module constants, flags, class and rounding types,
IEEE_VALUE, IEEE_IS_NAN, IEEE_IS_NEGATIVE, IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG,
IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, IEEE_SUPPORT_SUBNORMAL, IEEE_GET_FLAG,
IEEE_SET_FLAG, IEEE_MIN, IEEE_NEXT_AFTER, IEEE_NEXT_UP, IEEE_NEXT_DOWN, HUGE, EPSILON,
TINY, RADIX, and ISO/IEC 60559 operation terminology used by future guarded fixtures.

### S17.11.26-004: IEEE_NEXT_DOWN returns the adjacent lower representable value

**Source:** S17.11.26, J3/24-007, 18 December 2023, physical PDF497-498. **Class:** Effect.

**Definition:** For non-NaN values other than negative infinity, the result is the greatest
representable value that compares less than X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_NEXT_DOWN-one-binary-exact-lower-neighbor`, `IEEE_NEXT_DOWN-result-less-than-finite-X`, `IEEE_NEXT_DOWN-negative-HUGE-result-negative-infinity`, `IEEE_NEXT_DOWN-positive-zero-lower-neighbor`.

**Oracle:** Source registration only. Future fixtures guard the restricted IEEE procedure under test
and every restricted IEEE observer invoked in the plan with that procedure's own
IEEE_SUPPORT_DATATYPE or more specific IEEE_SUPPORT_* inquiry before invocation,
initialize observed results and flags to non-default or opposite sentinels, and assert
only portable properties: exact kind/rank/type, exact finite results, IEEE_IS_NAN or
IEEE_CLASS observations for NaNs, IEEE_IS_NEGATIVE observations for signs, and named
exception flags. Signed zeros are constructed with IEEE_COPY_SIGN or IEEE_VALUE and
pre-verified with IEEE_IS_NEGATIVE under its observer guard; bare negative-zero literals
are not used. Exception flag observations are keyed to IEEE_SUPPORT_FLAG(flag,X). Named
preamble HALTING-OFF-PREAMBLE(flag): the IEEE_SUPPORT_HALTING(flag)-true branch calls
IEEE_SET_HALTING_MODE(flag,.FALSE.); the IEEE_SUPPORT_HALTING(flag)-false branch reads
IEEE_GET_HALTING_MODE(flag,h) under that inquiry's guard and proceeds only for
h=.FALSE., otherwise skips. Every flag row that can signal runs HALTING-OFF-PREAMBLE for
each observed flag, clears all flags before the row, and no-other-exception plans assert
the other four flags quiet under their own IEEE_SUPPORT_FLAG guards. Unsupported inquiry
branches are skips, not passes; unnumbered argument and invocation restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing fixture binding for
IEEE_MIN_MAG, IEEE_MIN_NUM, IEEE_MIN_NUM_MAG, IEEE_NEXT_AFTER, IEEE_NEXT_DOWN,
IEEE_NEXT_UP, or S17.11.22-S17.11.27. NaN payloads, choice of NaN operand, signaling-NaN
payload preservation, exact halting behavior, and support for optional IEEE features are
not portable oracles.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, procedure, intrinsic-operation, and intrinsic-procedure
foundations from clauses 6 through 16 where already catalogued. Unregistered
dependencies retained: Clause 17 IEEE module constants, flags, class and rounding types,
IEEE_VALUE, IEEE_IS_NAN, IEEE_IS_NEGATIVE, IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG,
IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, IEEE_SUPPORT_SUBNORMAL, IEEE_GET_FLAG,
IEEE_SET_FLAG, IEEE_MIN, IEEE_NEXT_AFTER, IEEE_NEXT_UP, IEEE_NEXT_DOWN, HUGE, EPSILON,
TINY, RADIX, and ISO/IEC 60559 operation terminology used by future guarded fixtures.

### S17.11.26-005: IEEE_NEXT_DOWN negative-infinity and NaN results

**Source:** S17.11.26, J3/24-007, 18 December 2023, physical PDF497-498. **Class:** Effect.

**Definition:** For X equal to negative infinity the result is negative infinity, and for NaN X the
result is a NaN.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_NEXT_DOWN-negative-infinity-fixed-point`, `IEEE_NEXT_DOWN-quiet-NaN-result-is-NaN`.

**Oracle:** Source registration only. Future fixtures guard the restricted IEEE procedure under test
and every restricted IEEE observer invoked in the plan with that procedure's own
IEEE_SUPPORT_DATATYPE or more specific IEEE_SUPPORT_* inquiry before invocation,
initialize observed results and flags to non-default or opposite sentinels, and assert
only portable properties: exact kind/rank/type, exact finite results, IEEE_IS_NAN or
IEEE_CLASS observations for NaNs, IEEE_IS_NEGATIVE observations for signs, and named
exception flags. Signed zeros are constructed with IEEE_COPY_SIGN or IEEE_VALUE and
pre-verified with IEEE_IS_NEGATIVE under its observer guard; bare negative-zero literals
are not used. Exception flag observations are keyed to IEEE_SUPPORT_FLAG(flag,X). Named
preamble HALTING-OFF-PREAMBLE(flag): the IEEE_SUPPORT_HALTING(flag)-true branch calls
IEEE_SET_HALTING_MODE(flag,.FALSE.); the IEEE_SUPPORT_HALTING(flag)-false branch reads
IEEE_GET_HALTING_MODE(flag,h) under that inquiry's guard and proceeds only for
h=.FALSE., otherwise skips. Every flag row that can signal runs HALTING-OFF-PREAMBLE for
each observed flag, clears all flags before the row, and no-other-exception plans assert
the other four flags quiet under their own IEEE_SUPPORT_FLAG guards. Unsupported inquiry
branches are skips, not passes; unnumbered argument and invocation restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing fixture binding for
IEEE_MIN_MAG, IEEE_MIN_NUM, IEEE_MIN_NUM_MAG, IEEE_NEXT_AFTER, IEEE_NEXT_DOWN,
IEEE_NEXT_UP, or S17.11.22-S17.11.27. NaN payloads, choice of NaN operand, signaling-NaN
payload preservation, exact halting behavior, and support for optional IEEE features are
not portable oracles.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, procedure, intrinsic-operation, and intrinsic-procedure
foundations from clauses 6 through 16 where already catalogued. Unregistered
dependencies retained: Clause 17 IEEE module constants, flags, class and rounding types,
IEEE_VALUE, IEEE_IS_NAN, IEEE_IS_NEGATIVE, IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG,
IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, IEEE_SUPPORT_SUBNORMAL, IEEE_GET_FLAG,
IEEE_SET_FLAG, IEEE_MIN, IEEE_NEXT_AFTER, IEEE_NEXT_UP, IEEE_NEXT_DOWN, HUGE, EPSILON,
TINY, RADIX, and ISO/IEC 60559 operation terminology used by future guarded fixtures.

### S17.11.26-006: IEEE_NEXT_DOWN exception signaling

**Source:** S17.11.26, J3/24-007, 18 December 2023, physical PDF497-498. **Class:** Effect.

**Definition:** A signaling NaN argument signals IEEE_INVALID; otherwise IEEE_NEXT_DOWN signals no
exception.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_NEXT_DOWN-signaling-NaN-signals-invalid`, `IEEE_NEXT_DOWN-quiet-supported-inputs-signal-no-exception`.

**Oracle:** Source registration only. Future fixtures guard the restricted IEEE procedure under test
and every restricted IEEE observer invoked in the plan with that procedure's own
IEEE_SUPPORT_DATATYPE or more specific IEEE_SUPPORT_* inquiry before invocation,
initialize observed results and flags to non-default or opposite sentinels, and assert
only portable properties: exact kind/rank/type, exact finite results, IEEE_IS_NAN or
IEEE_CLASS observations for NaNs, IEEE_IS_NEGATIVE observations for signs, and named
exception flags. Signed zeros are constructed with IEEE_COPY_SIGN or IEEE_VALUE and
pre-verified with IEEE_IS_NEGATIVE under its observer guard; bare negative-zero literals
are not used. Exception flag observations are keyed to IEEE_SUPPORT_FLAG(flag,X). Named
preamble HALTING-OFF-PREAMBLE(flag): the IEEE_SUPPORT_HALTING(flag)-true branch calls
IEEE_SET_HALTING_MODE(flag,.FALSE.); the IEEE_SUPPORT_HALTING(flag)-false branch reads
IEEE_GET_HALTING_MODE(flag,h) under that inquiry's guard and proceeds only for
h=.FALSE., otherwise skips. Every flag row that can signal runs HALTING-OFF-PREAMBLE for
each observed flag, clears all flags before the row, and no-other-exception plans assert
the other four flags quiet under their own IEEE_SUPPORT_FLAG guards. Unsupported inquiry
branches are skips, not passes; unnumbered argument and invocation restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing fixture binding for
IEEE_MIN_MAG, IEEE_MIN_NUM, IEEE_MIN_NUM_MAG, IEEE_NEXT_AFTER, IEEE_NEXT_DOWN,
IEEE_NEXT_UP, or S17.11.22-S17.11.27. NaN payloads, choice of NaN operand, signaling-NaN
payload preservation, exact halting behavior, and support for optional IEEE features are
not portable oracles.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, procedure, intrinsic-operation, and intrinsic-procedure
foundations from clauses 6 through 16 where already catalogued. Unregistered
dependencies retained: Clause 17 IEEE module constants, flags, class and rounding types,
IEEE_VALUE, IEEE_IS_NAN, IEEE_IS_NEGATIVE, IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG,
IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, IEEE_SUPPORT_SUBNORMAL, IEEE_GET_FLAG,
IEEE_SET_FLAG, IEEE_MIN, IEEE_NEXT_AFTER, IEEE_NEXT_UP, IEEE_NEXT_DOWN, HUGE, EPSILON,
TINY, RADIX, and ISO/IEC 60559 operation terminology used by future guarded fixtures.

<!-- END GENERATED 17.11.26 -->
