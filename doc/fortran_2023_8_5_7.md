# Fortran 2023 8.5.7: CONTIGUOUS attribute

**Source review: reviewed.** Effective content-bound status is computed by
`Registry.catalogue_review_state("8.5.7")`, not the raw review field.
The original independent source gate is recorded in batch038. Fixture,
source and observational-inventory adjudications remain separate.

## Bounded C830 eligibility matrix

Fourteen compile/f2023 cases represent eight C830 facets: five invalid
entity categories and exact CONTIGUOUS-only repairs, plus four admissions
covering the three eligible categories. Assumed-rank admissions include
both ordinary and ALLOCATABLE dummies. Every repair deletes only
`, contiguous`, keeping the original rank, POINTER/ALLOCATABLE role,
dummy/interface context and all other source bytes.

The nine valid cases are compile/positive-control evidence. No link, run,
allocation, association, payload, argument-copy or contiguity inquiry is
observed. Negatives require a located entity-category cause, not a bare
attribute word, source echo, unrelated error or native compiler failure.
A full extracted cause must match an exact reviewed alternative in the
actual staged source. Quoted example text and foreign same-basename paths
cannot supply the cause or its origin. Other wording remains unqualified,
not a claim that the standard mandates these English messages.
Required reporting does not prescribe fatal rejection or a printed code.
Only the exact observed Flang portability cause is a qualified nonfatal
report; its f2018 mode is not relabelled as f2023 corroboration.

0 C830 facets remain PENDING; the other requirements currently
have 26 pending facets (26 total in 8.5.7).
Generation changes only the selected requirement's pending/oracle fields
and preserves all independent administrative records without renewing them.

The assumed-size pair retains the original complete module/procedure
sources and exact attribute-only repair. Its primary declaration anchor
is line6; additional line4 identifies the same dummy in the procedure
header. Each reported range must fit one anchor. Gap line5 and a4..6
recovery range do not qualify. This uses the independently reviewed
batch048 infrastructure without source coalescing or a false GNU failure.
The registered contracts and fresh observations still require independent
source/fixture adjudication; earlier raw calibration remains historical.

The catalogue is `doc/catalogues/contiguous_attribute_8_5_7.json`.
Original J3/24-007, December 18, 2023, 688 pages, has SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The eight original units span physical PDF pages 123-124 and end before 8.5.8
DIMENSION. The reproduced section hash is
`206e957a2a711479539a5cf88973a770f29f15ece909525aa2f2595f9ef02968`.
Original excerpts remain session evidence; no source inventory is rewritten.

| Source | Treatment |
| --- | --- |
| `C830` | Three eligible entity categories, distinct from actual contiguity |
| `p1` | Dummy contiguity guarantee and separate pointer-target restriction |
| `p2` | Seven positive routes, including every nonzero-section conjunct |
| `p3` | Complete negative conjunction with cardinality and zero-storage-type exceptions |
| `p4` | Processor-dependent residual classification, not a universal Boolean oracle |
| `note1`-`note3` | Original padding/optimization/declaration/argument explanations; no new mandatory cases |

Actual contiguity, simply-contiguous syntax and the CONTIGUOUS attribute
are different. An explicit stride 1 can satisfy actual contiguity but not
the 9.5.4 last-triplet no-stride rule. A singleton nonunit-stride section can
satisfy the full positive rule; empty sections and other residual cases do
not receive a guessed stride/size formula. A known full leading dimension
is not automatically the literal-colon syntax required for simple sections.

`IS_CONTIGUOUS` accepts an array or assumed-rank object and requires a
pointer argument to be associated. Its rank-zero true result applies through
the assumed-rank route and does not prove an attribute effect. Permitted
contiguity inquiries on unallocated nonpointer whole arrays do not allow
data, unavailable shape or deferred-parameter reads. A CONTIGUOUS pointer
can be disassociated without making such an inquiry valid.

Assumed-shape/rank CONTIGUOUS dummies can receive permitted noncontiguous
actuals. The plans check the dummy property and defined caller updates after
return, not a chosen temporary, address, copy count or performance effect.
C1548/C1549, C1550 and the other actual/dummy conditions remain independent.
Ordinary CONTIGUOUS pointer assignment needs a contiguous target; pointer
dummy and remapping contexts have their own simple-contiguity rules.

Vector subscripts, multiple-subscript expansion, partial CHARACTER
substrings, complex parts and earlier-ranked component selections require
their exact source predicates. Failing a positive p2 condition does not
automatically satisfy p3. Zero-length CHARACTER and zero-ultimate-component
derived types retain their explicit exceptions; no padding/byte-layout
model or tautological true-or-false test is substituted.

Existing C715, C748, C757, C769 and R738 witnesses retain their original
IDs, inputs, roles and owners. No C814/C815/CODIM/R402 work is changed.
The current finite R402 declaration-name SourceUse is retained without
execution or facet-completion credit. This packet adds no inventory
instance, source-use record or canonical link.

The following definitions are generated from the catalogue. Admission,
runtime-effect, reporting and source-only plans remain separate.

<!-- BEGIN GENERATED 8.5.7 -->

### C830: CONTIGUOUS has three eligible entity categories

**Source:** 8.5.7 C830, physical PDF123, printed109, lines16-17 **Class:** Restriction.

**Definition:** An entity with CONTIGUOUS is an array pointer, an assumed-shape array or an assumed-rank
dummy data object. An object being actually contiguous does not by itself make it
eligible for the attribute. The assumed-rank category can have a scalar effective
argument; an ordinary fixed-rank scalar pointer is not that category.

**Diagnostic obligation:** required.

**Facets:** `array-pointer-admission`, `assumed-shape-admission`, `assumed-rank-admission`, `ordinary-scalar-excluded`, `scalar-pointer-excluded`, `explicit-shape-excluded`, `allocatable-fixed-rank-excluded`, `assumed-size-excluded`.

**Oracle:** Fourteen complete compile/f2023 program units represent eight C830 facets. Five
negatives give CONTIGUOUS to an ordinary scalar, scalar pointer, ordinary explicit-shape
array, fixed-rank deferred-shape ALLOCATABLE array, or nonpointer assumed-size dummy.
Each control deletes only ', contiguous', preserving every other byte and the actual
entity category. Four additional admissions declare an array pointer and
explicit-interface assumed-shape, ordinary assumed-rank and ALLOCATABLE assumed-rank
dummies. A negative needs the actual located CONTIGUOUS/entity-category cause in the
actual staged input, matching one complete extracted diagnostic message. The four
ordinary declarations retain their line3 anchor. The assumed-size dummy has primary
declaration line6 and additional procedure-header line4 as separate subject anchors; gap
line5 and cross-anchor ranges do not qualify. An ordinary zero or nonzero reporting
status can satisfy that capability. Only the exact observed Flang portability message is
an allowed nonfatal report. Every valid case requires successful object compilation and
is positive-control evidence, not a runtime effect.

**Oracle limitation:** The assumed-size pair preserves the original complete module/procedure source and its
CONTIGUOUS-only repair. Its declaration/header relation uses the independently reviewed
disjoint-anchor infrastructure from batch048, not a broadened span, coalesced statements
or a false GNU conformance failure for selecting the header. Source, fixture and
processor adjudications remain separate from representing that relation. This finite
eligibility matrix is not an enumeration of all possible declarations, actual arguments
or processor modes. Ordinary fixed-rank exclusions do not ban eligible assumed-rank
dummies, and actual whole-array contiguity does not authorize the attribute.
Module-contained procedures supply explicit interfaces without requiring any unavailable
payload, allocation or pointer-association inquiry. No initializer, DATA, SAVE, coarray,
C companion, user-defined type, argument call, pointer assignment or runtime observer is
present. All four S-owned contiguity requirements, residual processor choices and
canonical component/source-use owners remain separate. No fatal exit, printed rule code
or prescribed English text is required. Exact finite message alternatives are
qualification predicates, not standardized wording. Quoted examples inside another error
and foreign paths sharing the source basename cannot supply this cause or source
identity. Source echoes, bare attribute words, unrelated shape/type/kind failures,
unsupported facilities, Internal/ASR/verifier, timeout/crash/resource failures and
wrong-source reports do not count. Generation preserves independent source, case, link,
SourceUses and inventory adjudications; it neither approves them nor relabels earlier
compiler observations.

**Dependencies:** 4.2;8.2 R802/R803;8.5.8.3/.4/.7;7.5.4.1 C757;15.4.2.2;15.5.2.5-.9.

### S8.5.7-001: Eligible CONTIGUOUS dummies have a contiguous view

**Source:** 8.5.7 p1 assumed-shape and assumed-rank clauses, physical PDF123, lines18-19 **Class:** Effect.

**Definition:** The CONTIGUOUS attribute makes an assumed-shape array or assumed-rank dummy data object
contiguous. A permitted actual argument need not itself be contiguous. The source
defines the dummy's property, not a particular copying, allocation, address or
optimization mechanism.

**Diagnostic obligation:** not-required.

**Facets:** `contiguous-actual-control`, `strided-actual-assumed-shape`, `strided-actual-assumed-rank`, `scalar-assumed-rank-boundary`, `argument-interface-source-use`.

**Oracle:** Two complete run/effect/f2023 programs implement only strided-actual-assumed-shape and
strided-actual-assumed-rank. Main defines an ordinary INTEGER a(4) by the executable
assignment a=[11,12,13,14] and passes only the definable nonvector section a(1:4:2)
through a complete internal procedure interface to a CONTIGUOUS INTENT(INOUT) dummy.
Direct IS_CONTIGUOUS(x) is compared with literal true, and actual dummy RANK/SIZE are
checked against1/2. The ordinary assumed-shape case checks x(1:2) values11/13
separately, adds10 to each, and checks21/23. The assumed-rank case uses property
inquiries on x as permitted by C840, then SELECT RANK(r=>x), RANK(1), for every payload
read/write. The default branch ERROR STOPs. Main independently checks all four returned
values21/12/23/14, so untouched elements and updates are both consumed. Entry, update,
normal procedure-completion, rank-branch and guard-count observations plus exact
completion stdout, empty stderr and normal exit0 reject missing calls, updates and
no-op/early-success paths. Every contiguity, rank, size, value, event, check-total and
output assertion has an exact one-span full-program wrong-oracle probe, qualified only
when the unchanged current parent passes and the intended runtime guard/output mismatch
occurs.

**Oracle limitation:** Other S8.5.7-001 facets are outside this generator's coverage, and C830 plus the other S
requirements retain their supplied source, fields, pending states and ownership. Both
programs are effects; no control-role opt-in or C715 control reuse is supplied. The
actual is a nonvector INTEGER triplet section with defined elements, no ASYNCHRONOUS,
VOLATILE, POINTER, ALLOCATABLE, coarray, VALUE or overlapping dummy actuals. Matching
default-INTEGER type/kind/rank and definability/explicit-interface conditions are
preserved. The C1548/C1549 antecedents do not apply; the source's dummy guarantee does
not assert that the caller has CONTIGUOUS. No optional actual-section contiguity
inquiry, pointer assignment, fixed-rank observer temporary or RANK constructor appears.
The rank-one associate designates the actual dummy, not a separate destination. No
copying count, temporary/address, ABI, storage-stride or allocation mechanism is
observed or required. Complete source and literal truth precede compiler calibration.
Build/unsupported/resource/runtime failures remain real; failed parents and compile
rejections do not qualify sensitivity. ERROR STOP token/status observations are
empirical guard evidence, not mandatory printed rule codes or a numeric termination
policy. Source, fixture, mode-qualified observations and inventory approval remain
independent; no source-use, canonical-link, review or baseline record is renewed.

**Dependencies:** C830;16.9.115;15.4.2.2;15.5.2.4/.5/.8/.9/.12/.13/.14;11.1.10;19.5.2.5; existing C715
witness.

### S8.5.7-002: A CONTIGUOUS array pointer is associated only with a contiguous target

**Source:** 8.5.7 p1 pointer clause, physical PDF123, lines18-19 **Class:** Restriction.

**Definition:** When an array pointer has CONTIGUOUS, a target with which it is pointer associated is
contiguous. The attribute does not establish association, require an associated target
at all times, or make arbitrary targets contiguous. Ordinary pointer assignment requires
actual contiguity here; the stronger simply-contiguous conditions in other contexts
retain their own owners.

**Diagnostic obligation:** not-required.

**Facets:** `whole-target-admission`, `actual-not-simple-target`, `noncontiguous-target-policy`, `initialization-owner-source-use`, `pointer-dummy-simple-source-use`.

**Oracle:** Complete pointer/target source relations with valid admissions and policy-gated
nonconforming contrasts. Required initialization or pointer-dummy diagnostics retain
C769/C812/C1550 rather than being relabelled as a universal local prose report.

**Oracle limitation:** IS_CONTIGUOUS of a pointer requires association even when its attribute is known. Do not
inquire on a disassociated/undefined pointer, inspect a target after
return/deallocation, substitute an invalid vector target or rely on temporary addresses.
No new policy or header/representation assumption.

**Dependencies:** 10.2.2.2 C1016-C1029;10.2.2.3p2/p4-p10;9.5.4;15.5.2.8 C1550;7.5.4.6 C769/C770;8.2
C812;16.9.115;19.5.2.

### S8.5.7-003: The seven stated routes guarantee actual contiguity

**Source:** 8.5.7 p2 and list, physical PDF123, lines20-38 **Class:** Effect.

**Definition:** Contiguity is guaranteed for an object with CONTIGUOUS; a nonpointer whole array that is
not assumed-shape; an assumed-shape array associated with a contiguous actual; an
assumed-rank dummy with a contiguous effective argument; an array allocated by an
ALLOCATE statement; or a pointer associated with a contiguous target. A nonzero-size
array section is also contiguous when its base is contiguous, it has no vector
subscript, selected elements retain base array-element order with no interleaved
excluded elements, any CHARACTER substring includes the whole parent string, only the
final part-ref has nonzero rank, and it is not an array complex-part designator.

**Diagnostic obligation:** not-required.

**Facets:** `attributed-object`, `whole-nonpointer-array`, `assumed-shape-contiguous-actual`, `assumed-rank-contiguous-effective`, `allocated-array`, `associated-pointer`, `gap-free-numeric-section`, `full-leading-dimensions`, `stride-and-singleton-boundary`, `full-character-substring`, `vector-and-multiple-subscript-source-use`, `ranked-part-and-complex-source-use`.

**Oracle:** Direct IS_CONTIGUOUS on eligible actual objects, with positive booleans justified by a
specific complete source route. Independent known bounds/element positions and defined
value checks support the chosen finite contexts; no address/stride/size formula is
treated as a universal contiguity oracle.

**Oracle limitation:** A pointer argument must be associated; an ordinary scalar is not an eligible direct
ARRAY argument, whereas an assumed-rank scalar is. Nonpointer unallocated property
inquiries permitted by the actual intrinsic do not permit unallocated data reads. No
missing positive premise is silently interpreted as noncontiguity; p3/p4 and the
simply-contiguous syntax remain separate.

**Dependencies:** 9.4.1/.2/.4;9.5.1/.2/.3/.4;9.7.1.2/.3;15.5;16.1;16.9.115/16.9.13/16.9.20;19.5.2.

### S8.5.7-004: The stated array-subobject conjunction guarantees noncontiguity

**Source:** 8.5.7 p3 and bullets, physical PDF123, lines39-44 **Class:** Effect.

**Definition:** An array subobject is not contiguous when it has at least two elements, those elements
in array-element order are not consecutive among the base object's elements, it is not
zero-length CHARACTER, and it is not of a derived type whose only ultimate components
are zero-size arrays or zero-length characters. All predicates are required. This is a
valid-object property, not a language prohibition on noncontiguous arrays.

**Diagnostic obligation:** not-required.

**Facets:** `ordinary-gapped-section`, `multidimensional-interleaving`, `nonconsecutive-vector-gate`, `cardinality-boundaries`, `zero-storage-type-exceptions`, `ordered-reversal-source-use`.

**Oracle:** A false IS_CONTIGUOUS expectation is attached only after the entire noncontiguity
conjunction and inquiry eligibility are established for the actual object. Residual or
unresolved classifications remain pending with processor/source qualifications, not
guessed booleans.

**Oracle limitation:** No rejection policy, undefined target/value trap, hidden-copy assumption or
performance/address measurement. Source p4 and informative notes do not create
additional mandatory false or true cases. The current R402 SourceUses inventory provides
no execution/facet credit for these plans and is not modified.

**Dependencies:** 7.5.1 ultimate components;9.4.1/.2/.4;9.5.2/.3/.4;16.9.115;15.5 actual/dummy
semantics;19.5.2/19.6.

<!-- END GENERATED 8.5.7 -->

<!-- BEGIN CONTIGUOUS DUMMY EFFECTS -->

## Bounded CONTIGUOUS dummy runtime effects

Two complete run/effect programs initialize ordinary INTEGER a(4) to11/12/13/14
and pass the nonvector section a(1:4:2) to a CONTIGUOUS INOUT dummy through
a complete internal-procedure interface. The dummy itself must satisfy
IS_CONTIGUOUS=true, RANK1 and SIZE2. Its initial values11/13 are checked
separately, each is increased by10, and21/23 are checked in the procedure.
The caller independently verifies21/12/23/14 after return.

For the assumed-rank case, x appears only in permitted property inquiries
and as SELECT RANK selector. All payload access uses its rank-one associate;
the unexpected-rank branch ERROR STOPs. No constructor RANK, fixed-rank
observer temporary, vector subscript, pointer or allocation route is used.

Entry/update/procedure-completion/check totals, a rank-branch counter and
exact stdout/empty-stderr/normal-zero contracts reject missing calls and
no-op/early-success paths. Every predicate and completion literal has one
bounded full-program wrong-oracle edit. A failed parent, build failure or
wrong runtime guard cannot qualify sensitivity.

Original8.5.7p1/NOTE3 supplies the dummy property without requiring actual
CONTIGUOUS. C1548/C1549's extra-attribute antecedents are absent; ordinary
type/kind/rank, definability and distinct-argument premises still apply.
No actual-section contiguity query is added, and no copy count, temporary,
address, ABI, storage stride or allocation mechanism is promised.

3 other S8.5.7-001 facets remain PENDING. The C830
matrix's supplied fields, cases and adjudications are preserved, as are C715 and all
other owners. Neither case is positive-control or S3/S4 coverage. Source,
fixture, native mode qualification and inventory review remain separate.

<!-- END CONTIGUOUS DUMMY EFFECTS -->
