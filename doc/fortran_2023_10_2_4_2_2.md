# Fortran 2023: 10.2.4.2.2 Determination of the values for index variables

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 10.2.4.2.2 -->

### S10.2.4.2.2-001: FORALL index variable values are determined as for DO CONCURRENT

**Source:** 10.2.4.2.2 p1; J3/24-007 PDF p200. **Class:** Effect.

**Definition:** The values of the FORALL index variables are determined as they are for the DO
CONCURRENT statement in 11.1.7.4.2.

**Diagnostic obligation:** not-required.

**Facets:** `do-concurrent-index-value-dependency`, `index-value-final-state-plan`.

**Oracle:** This unit delegates the actual index-value rule to 11.1.7.4.2. It permits final-state
plans only after the delegated DO CONCURRENT semantics are registered and must not be
used to infer processing order.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 11.1.7.4.2 DO CONCURRENT statement execution NOT YET REGISTERED;19.4 index names NOT YET
REGISTERED.

<!-- END GENERATED 10.2.4.2.2 -->
