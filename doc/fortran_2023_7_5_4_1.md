# 7.5.4.1 Component definition statement

Independent source, fixture and correction decisions are recorded in
`doc/source_audits/batch_014.json`; current approvals are content-bound
catalogue, case and evidence records, not inferred from this view.

The five-catalogue packet retains the checksum-pinned J3/24-007 source accounting.
Direct declaration, attribute, shape, parameter and procedure-component fixtures
are authored. Canonical wrapper/positive-only links and documentary expression
graphs remain pending; they do not receive duplicate execution credit.

The typed-procedure constant-inquiry layout has an independently established
source oracle; its unqualified reference diagnostics remain explicit.
Its already-constant result length must not receive credit from a
nonconstant-result or missing-interface diagnostic.

<!-- BEGIN GENERATED 7.5.4.1 -->

### R735: A component part contains zero or more component definition statements

**Source:** 7.5.4.1 R735; J3/24-007, 18 December 2023, PDF 93, printed 79, line 3. **Class:** Syntax.

**Definition:** The component part permits an empty sequence or repeated component definition
statements. R726 determines its position, R736 selects the statement categories, and
special type constraints can require components despite this optional grammar.

**Diagnostic obligation:** required.

**Facets:** `empty-part-link`, `multiple-statements`.

**Oracle:** An initialized run checks two independently declared INTEGER members. The empty
component-part canonical link remains pending; no duplicate empty-type execution is
added.

**Oracle limitation:** C745 restricts SEQUENCE and C1801-C1806 restrict BIND(C) definitions. An empty component
part is not a universal permission for every type class. The prior definition
implementation is outside this authoring scope.

**Dependencies:** R726 (PDF 88), R736 (PDF 93), C745 (PDF 89), C1801-C1806 (PDF 526); 4.1.2-4.2 (PDF
45-46).

### R736: Component definitions have data and procedure-pointer alternatives

**Source:** 7.5.4.1 R736; PDF 93, printed 79, lines 4-5. **Class:** Syntax.

**Definition:** A component definition is a data-component definition or a procedure-component
definition. Procedure-pointer components are not the type-bound-procedure part.
Constraints attached to this union, including C759, apply in either alternative.

**Diagnostic obligation:** required.

**Facets:** `data-alternative-link`, `procedure-alternative-link`.

**Oracle:** Source plan only. Two canonical alternative edges will retain their defining grammar and
execution owners.

**Oracle limitation:** A syntax union supplies no blanket permission for every nested declaration-type-spec,
initialization or attribute. A parser accepting PROCEDURE somewhere does not establish
the component alternative.

**Dependencies:** R737/R741/C759 (PDF 93-94); R746 and 7.5.5 (PDF 99-101); 4.1.2 p2 (PDF 45).

### R737: A data component statement has a type, optional attribute separator and declarator list

**Source:** 7.5.4.1 R737; PDF 93, printed 79, lines 6-7. **Class:** Syntax.

**Definition:** A declaration-type-spec precedes a nonempty component-decl list. A double colon may
occur without attributes; if component attributes occur, their comma-led list is
followed by the double colon. Initializers independently require that separator under
C766. The component context still restricts alternatives of the canonical
declaration-type-spec.

**Diagnostic obligation:** required.

**Facets:** `plain-without-colons`, `plain-with-colons`, `attribute-separator`, `type-specifier-use-graph`.

**Oracle:** Two initialized runs distinguish bare and double-colon two-member declarations. A
compile pair inserts only the missing :: after POINTER, retaining the correct deferred
shape. The full declaration-type-spec descendant graph remains pending.

**Oracle limitation:** A bare expected/type/attribute message is insufficient, even at the declaration line.
F2023 TYPEOF/CLASSOF or named enum feature failures cannot corroborate a syntax
restriction. C766 is not re-owned here.

**Dependencies:** R401/C401 (PDF 45); R703/C704-C714 and 7.3.2 (PDF 77-79); R738-R739/C749/C759 (PDF 93);
C766 (PDF 96); 7.6 (PDF 109-112); 4.2 p2(3) (PDF 46).

### R738: Data component attributes use six specified syntactic alternatives

**Source:** 7.5.4.1 R738; PDF 93, printed 79, lines 8-13. **Class:** Syntax.

**Definition:** Component attributes comprise an access-spec, ALLOCATABLE, CODIMENSION with a bracketed
coarray-spec, CONTIGUOUS, DIMENSION with a component-array-spec, or POINTER. The
CODIMENSION payload is enclosed in square brackets, not parentheses. PUBLIC and PRIVATE
are the two access-spec spellings. Membership does not waive C748-C757 or canonical
scope, shape, association and accessibility rules.

**Diagnostic obligation:** required.

**Facets:** `public`, `private`, `allocatable`, `codimension`, `contiguous`, `dimension`, `pointer`.

**Oracle:** Module/consumer runs exercise PUBLIC and PRIVATE data components without invalid
private-name references. Separate runs establish allocation, scalar pointer association
and CONTIGUOUS array-pointer association before inquiries and typed mutation. Shared
DIMENSION and CODIMENSION reuse facets remain pending.

**Oracle limitation:** No C ABI, byte width, storage contiguity of arrays of structures, or processor
kind-number convention is assumed. Contiguity of an appropriate whole TARGET array is a
language premise, not a physical-layout inference.

**Dependencies:** R807/C817 and 8.5.3 (PDF 120); 8.5.6-.8/C822-C834 (PDF 121-126); 8.5.14/8.5.18 (PDF 131,
133); 7.5.4.6/.8 (PDF 96-99); 9.7 (PDF 160-165); 10.2.2 (PDF 193-196); ASSOCIATED (PDF
382), ALLOCATED (PDF 379-380), IS_CONTIGUOUS (PDF 424); 19.5.2/19.6 (PDF 556-557,
562-563).

### R739: Individual data component declarators can specify shape, coshape, length and initialization

**Source:** 7.5.4.1 R739; PDF 93, printed 79, lines 14-16. **Class:** Syntax.

**Definition:** Each declarator has a component name, followed in order by an optional parenthesized
component-array-spec, optional bracketed coarray-spec, optional * char-length, and
optional component-initialization. These suffixes belong to that named component, not
automatically to every member of the statement. Their canonical constraints remain in
force.

**Diagnostic obligation:** required.

**Facets:** `named-list`, `individual-array-link`, `individual-coarray-link`, `individual-character-link`, `initialization-use-graph`.

**Oracle:** Source plan only. Declarator syntax and suffix locality are linked to their actual
canonical controls/effects. Default initialization is not inferred from successful
declaration parsing.

**Oracle limitation:** The optional grammar alone does not permit initialization of allocatables, arbitrary
initial pointer targets, or nonconstant default-initialized component bounds. Existing
character source bytes and case IDs remain unchanged.

**Dependencies:** R737/R740/C750-C759 (PDF 93); 7.4.4.2 p4-p5 (PDF 85); 7.5.4.2-.3 (PDF 94-95);
R743-R744/C766-C770 and 7.5.4.6 p3-p8/notes (PDF 96-98); R1515/R1517-C1519 (PDF 333);
inheritance (PDF 104).

### R740: Component array syntax selects explicit-shape or deferred-shape lists

**Source:** 7.5.4.1 R740; PDF 93, printed 79, lines 17-18. **Class:** Syntax.

**Definition:** The component-array-spec alternatives are an explicit-shape-spec-list and a
deferred-shape-spec-list. They are narrower than the general array-spec union; C750 and
C754 select the required alternative according to POINTER/ALLOCATABLE. Each explicit
bound also has C755's component-specific expression context.

**Diagnostic obligation:** required.

**Facets:** `explicit-shape-link`, `deferred-shape-link`, `general-array-grammar-exclusion-graph`.

**Oracle:** Source plan only. Actual component shapes and canonical scalar-expression constraints
remain separately traceable; a union is not multiplied into duplicate executions.

**Oracle limitation:** R814's newer general array forms do not automatically extend R740. A rank/type complaint
about a vector bound can be an inherited scalar-expression restriction, not proof of
another shape facet.

**Dependencies:** R401/R403/C401 (PDF 45); R814-R825/C831-C836 (PDF 124-126); C750/C754/C755 (PDF 93);
10.1.11 (PDF 185-187).

### C748: A data component statement does not repeat a component attribute

**Source:** 7.5.4.1 C748 (R737); PDF 93, printed 79, line 20. **Class:** Restriction.

**Definition:** A component-attr-spec does not recur in a single component definition statement. The
subject is a data component attribute list, not a TYPE header, a procedure component
list or a type-bound binding.

**Diagnostic obligation:** required.

**Facets:** `duplicate-access`, `duplicate-allocatable`, `duplicate-codimension`, `duplicate-contiguous`, `duplicate-dimension`, `duplicate-pointer`.

**Oracle:** Seven compile diagnostic/control pairs isolate repeated PUBLIC, PRIVATE, ALLOCATABLE,
CODIMENSION, CONTIGUOUS, DIMENSION and POINTER. Each control deletes one duplicate while
preserving independently legal module, shape, coshape and array-pointer premises. Causes
identify the actual data component and attribute.

**Oracle limitation:** Broad duplicate/attribute/type strings cannot identify the subject. Repeated TYPE
attributes belong to C735, procedure component attributes to C760, and binding
attributes to C783. Same-line wrong-role reports and unsupported facilities remain
failures.

**Dependencies:** R737-R738/C750-C757 (PDF 93); C735 (PDF 88), C760 (PDF 94), C783 (PDF 100); R807/C817
(PDF 120); 4.1.2/4.2 p2(3) (PDF 45-46).

### C749: An ordinary data component uses an intrinsic or previously defined nonintrinsic type

**Source:** 7.5.4.1 C749 (R737); PDF 93, printed 79, lines 21-23. **Class:** Restriction.

**Definition:** When neither POINTER nor ALLOCATABLE occurs, the component's declaration-type-spec
identifies an intrinsic type or a derived, enum or enumeration type already defined. The
prior-definition qualification applies to those nonintrinsic alternatives. The
conditional restriction does not itself forbid recursive or later-defined derived types
behind permitted indirection. It does not waive C7112/C7116, which independently require
prior enum/enumeration definitions, or other declaration requirements.

**Diagnostic obligation:** required.

**Facets:** `intrinsic-admission`, `prior-derived-admission`, `prior-enum-admission`, `prior-enumeration-admission`, `self-value-exclusion`, `forward-value-exclusion`, `indirection-boundaries`.

**Oracle:** Initialized intrinsic and previously defined derived member runs accompany type-only
named enum/enumeration admissions. Self/forward nonindirect components have exact
repaired controls. The self POINTER repair also supplies that real indirection boundary;
a separate ALLOCATABLE repair and two forward-indirection admissions complete the four
finite boundary contexts without duplicate self-pointer execution.

**Oracle limitation:** CLASS(*) and TYPE(*) do not become ordinary by-value data component types merely because
they occur in R703; C708/C714 remain applicable. Forward-use allowances are not a
permission to leave a type undefined. No matching storage layout proves type identity.

**Dependencies:** R703/C705-C714 and type compatibility (PDF 77-79); 7.5.1 (PDF 87); R736/C750/C753/C759
(PDF 93); R754/C795 (PDF 106); 7.6.1/.2, C7112/C7116 (PDF 109-112); 18.2.2 p3 (PDF 516);
4.2 p2(3) (PDF 46).

### C750: Pointer and allocatable component arrays have deferred shape

**Source:** 7.5.4.1 C750 (R737); PDF 93, printed 79, lines 24-25. **Class:** Restriction.

**Definition:** If POINTER or ALLOCATABLE is specified, every component-array-spec is a deferred-shape
list. This applies to the DIMENSION attribute and individual component suffixes. It does
not require a scalar component to acquire array shape.

**Diagnostic obligation:** required.

**Facets:** `pointer-local-shape`, `pointer-shared-shape`, `allocatable-local-shape`, `allocatable-shared-shape`, `overridden-shape-still-constrained`.

**Oracle:** Five compile pairs change only the actual explicit component shape to a deferred shape,
separately for POINTER/ALLOCATABLE and local/shared specifications. The overridden
DIMENSION pair keeps an already-deferred local suffix and changes only the invalid
shared specification. Scalars are not made missing-shape negatives.

**Oracle limitation:** Messages about missing ALLOCATABLE, illegal POINTER/ALLOCATABLE coexistence or an
unsupported deferred-array facility cannot count as the required shape cause.
Sizes/bounds of unallocated or unassociated arrays are not expected values.

**Dependencies:** R738-R740/C756 (PDF 93); 8.5.8.4/R822/C834/p3-p6 (PDF 126); allocation/association (PDF
160-164, 194-196); 4.2 p2(3) (PDF 46).

### C751: A coarray component has deferred coshape and ALLOCATABLE

**Source:** 7.5.4.1 C751 (R737); PDF 93, printed 79, lines 26-27. **Class:** Restriction.

**Definition:** A coarray-spec in a data component statement requires a deferred-coshape list and the
ALLOCATABLE attribute. Both the CODIMENSION attribute and an individual coarray suffix
are covered. Corank, array rank and runtime cobounds remain different properties.

**Diagnostic obligation:** required.

**Facets:** `individual-deferred-coshape`, `attribute-deferred-coshape`, `individual-allocatable-required`, `attribute-allocatable-required`, `overridden-coshape-still-constrained`.

**Oracle:** Five coarray type-only pairs separate explicit-versus-deferred coshape, missing
ALLOCATABLE and invalid overridden shared coshape. Controls change only that list or
attribute. No containing object, allocation or private intrinsic-module value is needed
for these declaration diagnostics.

**Oracle limitation:** C827 and C828 have overlapping general coarray-shape conditions. A report must express
the actual cause for the selected component, not be credited merely for a coarray noun.
Unsupported facilities, ASR failures and resource failures stay failures.

**Dependencies:** R739 and C752-C753 (PDF 93); R809-R811/C822-C828 (PDF 121-122); 7.5.4.3 (PDF 95); 4.1.2
and 4.2 p2(3) (PDF 45-46).

### C752: Coarray components exclude three particular intrinsic-module types

**Source:** 7.5.4.1 C752 (R737); PDF 93, printed 79, lines 28-30. **Class:** Restriction.

**Definition:** A component with a coarray-spec cannot have the ISO_C_BINDING type C_PTR or C_FUNPTR, or
the ISO_FORTRAN_ENV type TEAM_TYPE. These are module-provided type identities, not a ban
on coincidentally spelled unrelated type names, integer kind values or every derived
type.

**Diagnostic obligation:** required.

**Facets:** `c-ptr-exclusion`, `c-funptr-exclusion`, `team-type-exclusion`, `module-identity-boundaries`.

**Oracle:** Six type-only pairs use the actual C_PTR, C_FUNPTR or TEAM_TYPE identity through
original and renamed imports. Replacing only the component type with INTEGER retains
ALLOCATABLE and deferred coshape. One separate admission defines unrelated local types
with those spellings, without inspecting opaque values or claiming C ABI compatibility.

**Oracle limitation:** The general exclusion C824 is a canonical dependency. C_PTR/C_FUNPTR/TEAM_TYPE presence
does not establish representation or companion pairing, and unsupported module/type
facilities cannot corroborate this restriction.

**Dependencies:** C751/C749 (PDF 93); C824 and R809 (PDF 121); ISO_C_BINDING 18.2.1-.2 (PDF 516);
TEAM_TYPE 16.10.2.34 (PDF 477); use association and type identity retain their canonical
owners; 4.2 p2(3) (PDF 46).

### C753: A component with coarray potential subobjects is an ordinary scalar

**Source:** 7.5.4.1 C753; PDF 93, printed 79, lines 31-32. **Class:** Restriction.

**Definition:** If the type of a data component has a coarray potential subobject component, that data
component is scalar, lacks both POINTER and ALLOCATABLE, and is not itself a coarray.
Potential-subobject traversal follows nonpointer derived components, including the
canonical inherited-component paths; it is not a byte-layout or direct-member-only test.

**Diagnostic obligation:** required.

**Facets:** `nonpointer`, `nonallocatable`, `scalar`, `not-coarray-interaction`, `nested-and-inherited-use-graph`.

**Oracle:** Three type-only pairs retain the complete leaf with its legal allocatable coarray and
remove only the outer component's POINTER, ALLOCATABLE or array shape. Source relations
distinguish the outer field from the leaf coarray. The coupled nonallocatable/noncoarray
consequence and nested/inherited use graphs remain pending.

**Oracle limitation:** These declarations do not establish valid containing-object contexts automatically;
C825/C826 still govern variables, pointers, coarrays and
SAVE/ALLOCATABLE/dummy/associate contexts. The compound coarray branch remains pending
graph work, not a claimed single-cause fixture.

**Dependencies:** 7.5.1 p3-p6 (PDF 87); C741-C742 (PDF 88-89); 7.5.7.2 (PDF 104); C749-C751 (PDF 93);
C825-C826 (PDF 121-122); 4.2 p2(3) (PDF 46).

### C754: Nonpointer nonallocatable component arrays have explicit shape

**Source:** 7.5.4.1 C754 (R737); PDF 93, printed 79, lines 33-34. **Class:** Restriction.

**Definition:** When a component statement has neither POINTER nor ALLOCATABLE, every
component-array-spec is an explicit-shape list. This selects the ordinary array
alternative without requiring an array specification on scalar components.

**Diagnostic obligation:** required.

**Facets:** `individual-explicit-shape`, `shared-explicit-shape`, `overridden-shape-still-constrained`.

**Oracle:** Three compile pairs repair the local or shared shape of an ordinary component, including
a deferred shared DIMENSION overridden by an already-valid local explicit shape. The
diagnostic must identify the actually invalid specification, not pretend the active
local shape is invalid.

**Oracle limitation:** An ordinary scalar is not a missing-shape negative. Generic deferred-shape or
expected-token text cannot substitute for the specific ordinary-component context.

**Dependencies:** R738-R740/C750/C755 (PDF 93); explicit/deferred/assumed shape distinctions (PDF
125-126); 4.2 p2(3) (PDF 46).

### C755: Each explicit component bound is a component specification expression

**Source:** 7.5.4.1 C755 (R740); PDF 93, printed 79, line 35. **Class:** Restriction.

**Definition:** Every lower and upper bound in an explicit component shape satisfies the component
specification expression definition in 10.1.11 p9, including its inherited
scalar-integer and restricted-expression premises. That context excludes specification
functions; ALLOCATED, ASSOCIATED, COMMAND_ARGUMENT_COUNT, EXTENDS_TYPE_OF, GET_TEAM,
NUM_IMAGES, PRESENT, SAME_TYPE_AS, TEAM_NUMBER and THIS_IMAGE; nonconstant specification
inquiries; and dependence on a variable value. TRANSFER has the additional
constant-argument and disassociated-ultimate-pointer conditions. A type parameter is not
an object: permitted own or inherited type parameters are not excluded merely because a
LEN value is not a constant expression.

**Diagnostic obligation:** required.

**Facets:** `parameter-bound-admission`, `constant-inquiry-admission`, `lower-and-upper-variable-exclusion`, `specification-function-exclusion`, `nonconstant-inquiry-exclusion`, `forbidden-reference-use-graph`, `transfer-qualification-graph`.

**Oracle:** Three runs exercise abstract KIND/LEN parameter bounds, an inherited LEN bound and a
constant property inquiry without reading the prototype's value. Four compile pairs
replace a host-variable lower/upper bound, a valid specification-function call or an
assumed-shape inquiry with a small literal. The actual component-bound dependency is the
cause; the ten-intrinsic and TRANSFER graphs remain pending.

**Oracle limitation:** A blanket 'components must be constant' oracle would wrongly reject valid LEN-parameter
bounds. Mere type/kind/constant/expected words and same-line placement cannot establish
the cause. If a proposed expression violates another function argument or state
precondition, its failure cannot be repurposed as C755 evidence.

**Dependencies:** R403/C401 (PDF 45); 7.5.3.1 p5 and inherited parameters (PDF 92, 104); R815-R817 (PDF
125); 10.1.11 p1-p9, including the p9 continuation, and 10.1.12 p1 (PDF 185-188); C767
(PDF 96); 4.2 p2(3) (PDF 46).

### C756: A component is not both ALLOCATABLE and POINTER

**Source:** 7.5.4.1 C756 (R737); PDF 93, printed 79, line 36. **Class:** Restriction.

**Definition:** ALLOCATABLE and POINTER cannot both describe a component. Either attribute alone remains
possible subject to its own shape, type and state restrictions.

**Diagnostic obligation:** required.

**Facets:** `scalar-exclusion`, `array-exclusion`.

**Oracle:** Two negatives put POINTER and ALLOCATABLE on the same scalar or deferred-shape array
component. Each has two independently minimal controls, deleting one attribute or the
other. No pointer/allocation status is queried by these compile-only pairs.

**Oracle limitation:** C854 has a corresponding general POINTER restriction. The shared semantic relationship
is not another execution or proof of allocation/pointer lifetime behavior.

**Dependencies:** C750 (PDF 93); 8.5.3 and C854 (PDF 120, 131); 4.2 p2(3) (PDF 46).

### C757: A CONTIGUOUS component is an array pointer

**Source:** 7.5.4.1 C757 (R737); PDF 93, printed 79, lines 37-38. **Class:** Restriction.

**Definition:** Specifying CONTIGUOUS on a component requires both array rank and POINTER. This is a
declaration condition, separate from the later requirement that an associated target be
contiguous.

**Diagnostic obligation:** required.

**Facets:** `scalar-pointer-exclusion`, `nonpointer-array-exclusion`, `array-pointer-admission-link`.

**Oracle:** Two declaration pairs independently violate the array and POINTER predicates of
CONTIGUOUS. Each minimal repair removes CONTIGUOUS, preserving the existing valid
scalar-pointer or ordinary explicit-array category. The array-pointer positive-only
reuse link remains pending.

**Oracle limitation:** C830's ordinary assumed-shape/assumed-rank alternatives do not make nonpointer
components legal here. Unsupported CONTIGUOUS or generic attribute reports are
insufficient. Contiguity of arrays of structures can be processor dependent and is not
inferred.

**Dependencies:** C750/C754 (PDF 93); C830/8.5.7 p1-p4 and notes (PDF 123-124); 10.2.2.3 p6 (PDF 195); 4.2
p2(3) (PDF 46).

### C758: An individual star-length suffix is only for character components

**Source:** 7.5.4.1 C758 (R739); PDF 93, printed 79, line 39. **Class:** Restriction.

**Definition:** The individual * char-length suffix in a component declarator is permitted only for a
character component. It is not a kind/byte-size suffix for other component types.

**Diagnostic obligation:** required.

**Facets:** `noncharacter-suffix-exclusion`, `character-suffix-admission-link`.

**Oracle:** Two compile pairs remove only the individual *2 suffix from INTEGER or previously
defined derived components. Causes must concern the character-length suffix on that
noncharacter field. Existing character-component admission retains its owner and remains
a pending link.

**Oracle limitation:** A suffix's expression validity remains subject to C759 and character syntax; this type
restriction does not permit assumed length or arbitrary processor storage notation. No
assumed byte width is used.

**Dependencies:** R739/C759 (PDF 93); R721-R723 and 7.4.4.2 p4-p5 (PDF 84-85); 4.2 p2(3) (PDF 46).

### C759: Type-parameter values throughout component definitions are deferred or component specification expressions

**Source:** 7.5.4.1 C759 (R736); PDF 93, printed 79, lines 40-41. **Class:** Restriction.

**Definition:** Each type-param-value within either kind of component definition is a colon or a
component specification expression. The scope includes a typed proc-interface inside
R741, not just data components. C702 still limits colon placement to POINTER/ALLOCATABLE
entities and C701 still requires constant KIND values; local colon syntax is not a
deferred-KIND permission. An asterisk is not one of C759's alternatives. Own or
inherited type parameters can be valid expression primaries under the canonical rules.

**Diagnostic obligation:** required.

**Facets:** `data-parameter-expression`, `deferred-length-boundaries`, `procedure-interface-parameter`, `assumed-star-canonical-link`, `expression-context-use-graph`, `overridden-parameter-still-constrained`.

**Oracle:** An own-LEN component run and two deferred data-length state/value runs retain the actual
parameter contexts. Two data-selector pairs repair a host-variable length, including an
overridden shared selector. The typed-procedure pair preserves fixed nonpolymorphic
carrier objects and POINTER,NOPASS, replacing only MERGE(2,3,SAME_TYPE_AS(a,b)) with 2.
That result is already constant; only the explicit component-expression inquiry
exclusion is an intended cause. Canonical star and complete expression-context links
remain pending.

**Oracle limitation:** No assumption that every component parameter is a constant, that every constant
expression is a component specification expression, or that every colon is legal in
every parameter slot. In particular an already-constant result expression must not
receive credit from a false nonconstant-expression report. A result-type report for the
wrong role, broad kind/constant/function text, missing-feature error, crash, verifier
failure or unallocated-value query cannot corroborate this rule.

**Dependencies:** R736/R741 (PDF 93-94); R701/C701/C702 (PDF 76); C726 and character lengths (PDF 84-85);
7.5.3.1 (PDF 91-92); 10.1.11 p3/p9 and 10.1.12 p1(6)/(7) (PDF 186-188); MERGE (PDF 433),
SAME_TYPE_AS and extensible dynamic types (PDF 452, 79, 104); explicit-interface result
requirements (PDF 325); R1513/15.4.3.6 p2-p4 (PDF 332-333); deferred parameter
establishment (PDF 160-164, 195-196); 4.2 p2(3) (PDF 46).

### R741: A procedure component statement requires attributes and a double-colon declarator list

**Source:** 7.5.4.1 R741; PDF 94, printed 80, lines 1-2. **Class:** Syntax.

**Definition:** PROCEDURE encloses an optional proc-interface in parentheses, then requires a comma, a
nonempty procedure-component attribute list, a double colon and a nonempty proc-decl
list. The canonical interface is an interface name or a declaration-type-spec; omission
and a typed implicit function are distinct from a named explicit interface. C761
independently requires POINTER.

**Diagnostic obligation:** required.

**Facets:** `explicit-interface`, `omitted-interface`, `typed-implicit-interface`, `separator-and-list`, `initialization-use-graph`.

**Oracle:** Explicit, omitted and typed implicit interfaces have real associated procedure-component
invocations with compatible nonelemental targets. The implicit-interface calls are
positional and avoid independently required explicit interfaces. A two-component list
run uses distinct targets, and a compile pair inserts only the missing ::. Default
initialization uses NULL() without arguments; its broader canonical-use graph stays
pending.

**Oracle limitation:** R1513 is not redefined here, and its declaration-type-spec union is subject to C759 and
all relevant function-result constraints. A nonexistent/unsupported interface is not a
separator diagnostic. Undefined association status is never queried or invoked.

**Dependencies:** R742/C759-C765 (PDF 93-96); R1512-R1518/C1515-C1519 and 15.4.3.6 p2-p10 (PDF 332-334);
explicit interface requirements (PDF 324-325); 8.5.9/8.5.14 (PDF 128, 131); 10.2.2.4
(PDF 196); 7.5.4.6 (PDF 96-98); 4.2 p2(3) (PDF 46).

### R742: Procedure component attributes select access, passing mode and POINTER

**Source:** 7.5.4.1 R742; PDF 94, printed 80, lines 3-6. **Class:** Syntax.

**Definition:** A procedure component attribute is an access-spec, NOPASS, PASS optionally naming a
dummy argument, or POINTER. These are not the different binding-attr alternatives, and
POINTER is mandatory for every such component under C761. Passing-mode semantics and
passed-object characteristics have separate defining owners.

**Diagnostic obligation:** required.

**Facets:** `public-access`, `private-access`, `nopass-link`, `bare-pass-link`, `named-pass-link`, `pointer-link`.

**Oracle:** Two module/consumer runs admit public and private procedure components with association
established by a module procedure. A public component is called directly; the private
one only through an accessible wrapper. Passing-mode and POINTER canonical links remain
pending rather than duplicating their executions.

**Oracle limitation:** DEFERRED and NON_OVERRIDABLE are binding attributes, not new R742 alternatives.
Component-part and binding-part PRIVATE defaults are distinct; no source use graph may
merge them.

**Dependencies:** R807/C817 (PDF 120); 7.5.4.8 (PDF 99); C760-C765 (PDF 94-96); R752/C783-C790 (PDF
100-101); 15.5.2.2 (PDF 338).

### C760: A procedure component statement does not repeat a procedure component attribute

**Source:** 7.5.4.1 C760 (R741); PDF 94, printed 80, lines 7-8. **Class:** Restriction.

**Definition:** The same procedure-component attribute cannot occur twice in one procedure component
statement. Access attributes, POINTER and the same passing-mode attribute remain
distinct subjects from data component or type-bound binding duplicates.

**Diagnostic obligation:** required.

**Facets:** `duplicate-access`, `duplicate-pointer`, `duplicate-nopass`, `duplicate-pass`.

**Oracle:** Six compile pairs delete one repeated PUBLIC, PRIVATE, POINTER, NOPASS, PASS or
PASS(self) attribute from a procedure component. Explicit interfaces and the surviving
POINTER/passing mode remain valid. Mixed PASS/NOPASS is not the duplicate-occurrence
cause.

**Oracle limitation:** An exact source point does not disambiguate a duplicate on a component, binding, TYPE
header or dummy. Generic attribute/type/function reports and missing-facility errors
cannot substitute for the procedure-component duplicate.

**Dependencies:** R741-R742/C761-C765 (PDF 94-96); C748 (PDF 93), C735 (PDF 88), C783 (PDF 100); C817 (PDF
120); 4.2 p2(3) (PDF 46).

### C761: Every procedure component attribute list includes POINTER

**Source:** 7.5.4.1 C761 (R741); PDF 94, printed 80, line 9. **Class:** Restriction.

**Definition:** POINTER is required in every procedure-component attribute list. A procedure component
is not an ordinary external-procedure declaration or a type-bound procedure declaration.

**Diagnostic obligation:** required.

**Facets:** `pointer-required`.

**Oracle:** One compile pair adds only POINTER to PROCEDURE(iface), NOPASS :: action with a complete
explicit zero-argument interface and no initializer. The cause is this procedure
component's missing POINTER, not an empty attribute list or an invocation.

**Oracle limitation:** A message about a standalone procedure's EXTERNAL attribute, an initializer, or a
binding is a different condition. Empty interface recovery does not count.

**Dependencies:** R741-R742/C762 (PDF 94); 7.5.4.4 (PDF 95); R1512/C1518 (PDF 332-333); 4.2 p2(3) (PDF
46).

### C762: Implicit-interface and zero-argument procedure components require NOPASS

**Source:** 7.5.4.1 C762 (R741); PDF 94, printed 80, lines 10-11. **Class:** Restriction.

**Definition:** A procedure-pointer component requires NOPASS if its interface is implicit or it has no
arguments. These are alternative antecedents: a typed implicit interface is still
implicit, while a named explicit interface can have an empty dummy list.

**Diagnostic obligation:** required.

**Facets:** `implicit-interface`, `zero-argument-explicit-interface`, `explicit-passed-object-boundary`.

**Oracle:** Three compile pairs add only NOPASS, separately for an omitted interface, a typed
implicit interface and an explicit zero-argument interface. Their cause sets do not
cross-credit implicitness and absence of dummies. The explicit passed-object boundary
reuse remains pending.

**Oracle limitation:** No fabricated invocation of a component with an undefined interface or association state
is used. Broad function/argument/expected words do not establish either premise.

**Dependencies:** R741 and 15.4.3.6 p2-p4 (PDF 94, 333); 7.5.4.5/C765 (PDF 96); explicit-interface
requirements (PDF 324-325); 4.2 p2(3) (PDF 46).

### C763: The name in PASS identifies a dummy argument of the component interface

**Source:** 7.5.4.1 C763 (R741); PDF 94, printed 80, lines 12-13. **Class:** Restriction.

**Definition:** PASS(arg-name) on a procedure component requires that exact Fortran dummy-argument name
in its interface. It is a dummy name, not a component, procedure, result or positional
label. Selection of the passed-object role is separately specified in 7.5.4.5.

**Diagnostic obligation:** required.

**Facets:** `dummy-name-existence`, `case-equivalent-name`.

**Oracle:** One compile pair replaces PASS(absent) with PASS(self), retaining the explicit tag,self
interface. A separate run uses case-equivalent PASS(sElF) on the real second dummy and
verifies both hidden-object and ordinary argument values. A wrong-first-dummy recovery
is not the missing-name cause.

**Oracle limitation:** A missing procedure or result name is not the missing dummy name. Same-line
expected/function/type reports and wrong-first-dummy fallbacks are insufficient.

**Dependencies:** R402 and name equivalence (PDF 45, 67-68); R741-R742 and C765 (PDF 94, 96); 15.4.3.6 and
15.5.2.2 (PDF 332-334, 338); 4.2 p2(3) (PDF 46).

### C764: PASS and NOPASS do not coexist on a procedure component

**Source:** 7.5.4.1 C764 (R741); PDF 94, printed 80, line 14. **Class:** Restriction.

**Definition:** A single procedure-component attribute list cannot contain both PASS and NOPASS. PASS
with or without a name is covered. This is distinct from repetition of the same
attribute under C760.

**Diagnostic obligation:** required.

**Facets:** `bare-pass-conflict`, `named-pass-conflict`.

**Oracle:** Two negatives combine bare or named PASS with NOPASS. Each has two exact controls,
removing one conflicting attribute while retaining POINTER and the valid explicit
interface. No call is made with a NOPASS control's self argument accidentally omitted.

**Oracle limitation:** C786 is the separate binding-attribute owner. Do not accept a broad duplicate attribute
or generic procedure report for a PASS/NOPASS conflict.

**Dependencies:** R742/C760/C762-C763 (PDF 94); 7.5.4.5/C765 (PDF 96); C786 (PDF 100); 4.2 p2(3) (PDF 46).

### S7.5.4.1-001: A data component statement distributes shared properties with individual overrides

**Source:** 7.5.4.1 p1; PDF 94, printed 80, lines 15-21. **Class:** Effect.

**Definition:** The statement's declaration-type-spec supplies the type and type parameters of every
listed data component, with individual character-length suffixes able to set or override
length. Its component attributes apply to the list, but an individual array
specification sets or overrides DIMENSION and an individual coarray specification sets
or overrides CODIMENSION. These exceptions are per component; rank, corank and character
length are not interchangeable.

**Diagnostic obligation:** not-required.

**Facets:** `shared-type-and-parameters`, `shared-attributes`, `character-override-link`, `dimension-override-link`, `codimension-override-link`.

**Oracle:** Two initialized effects observe shared CHARACTER type/length and INTEGER declarations,
and shared ALLOCATABLE applied independently to two members. Allocation status is
checked before allocation, shape and value inquiries. The three canonical override links
remain pending.

**Oracle limitation:** No physical memory ordering, byte representation, kind-identifier numbering or
allocation-success assumption is embedded. A default initialized pointer is not inferred
to give the containing variable SAVE. Existing source or case approvals do not
automatically approve the proposed semantic links.

**Dependencies:** R737-R740/C750-C759 (PDF 93); 7.4.4.2 p4-p5 (PDF 85); 7.5.4.2-.3/.6 (PDF 94-98);
8.5.6/.8 (PDF 121-126); ALLOCATED (PDF 379-380); 9.7 and 19.6 (PDF 160-165, 562-563);
4.2 p2(1) (PDF 46).

<!-- END GENERATED 7.5.4.1 -->
