# Fortran 2023 12.5.1: Units - I/O units and connection fixtures

The canonical catalogue is `doc/catalogues/units_12_5_1.json`. This author packet adds finite runtime and diagnostic fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 22 facets in 12 program(s); 21 of 26 facets are represented and 5 remain pending.

<!-- BEGIN GENERATED 12.5.1 -->

### S12.5.1-001: A unit specified by an io-unit refers to a file

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF248. **Class:** Effect.

**Definition:** A unit, specified by an io-unit, provides a means for referring to a file.

**Diagnostic obligation:** not-required.

**Facets:** `io-unit-refers-to-file`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `io-unit-refers-to-file`. Runtime oracles use scratch or
run-directory files, controlled stdin/stdout/stderr, INQUIRE properties, exact
integer/character/logical values, and IOSTAT zero only. No processor-dependent ordinary
unit number, physical device, IOMSG text, or numeric nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### R1201: An io-unit is a file-unit-number, an asterisk, or an internal-file-variable

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF248. **Class:** Syntax.

**Definition:** The io-unit syntax admits exactly a file-unit-number, an asterisk, or an
internal-file-variable.

**Diagnostic obligation:** required.

**Facets:** `io-unit-file-unit-number-form`, `io-unit-asterisk-form`, `io-unit-internal-file-variable-form`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `io-unit-asterisk-form`, `io-unit-file-unit-number-form`,
`io-unit-internal-file-variable-form`. Runtime oracles use scratch or run-directory
files, controlled stdin/stdout/stderr, INQUIRE properties, exact
integer/character/logical values, and IOSTAT zero only. No processor-dependent ordinary
unit number, physical device, IOMSG text, or numeric nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### R1202: A file-unit-number is a scalar integer expression

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF248. **Class:** Syntax.

**Definition:** A file-unit-number is a scalar integer expression.

**Diagnostic obligation:** required.

**Facets:** `file-unit-number-scalar-int-expr`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `file-unit-number-scalar-int-expr`. Runtime oracles use
scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE properties,
exact integer/character/logical values, and IOSTAT zero only. No processor-dependent
ordinary unit number, physical device, IOMSG text, or numeric nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### R1203: An internal-file-variable is a character variable

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF248. **Class:** Syntax.

**Definition:** An internal-file-variable is a character variable.

**Diagnostic obligation:** required.

**Facets:** `internal-file-variable-char-variable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `internal-file-variable-char-variable`. Runtime oracles use
scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE properties,
exact integer/character/logical values, and IOSTAT zero only. No processor-dependent
ordinary unit number, physical device, IOMSG text, or numeric nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### C1201: An internal-file-variable shall not be a vector-subscripted array section

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Restriction.

**Definition:** The character variable used as an internal-file-variable shall not be an array section
with a vector subscript.

**Diagnostic obligation:** required.

**Facets:** `internal-file-variable-not-vector-subscript-section`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 1
diagnostic program(s) cover `internal-file-variable-not-vector-subscript-section`.
Runtime oracles use scratch or run-directory files, controlled stdin/stdout/stderr,
INQUIRE properties, exact integer/character/logical values, and IOSTAT zero only. No
processor-dependent ordinary unit number, physical device, IOMSG text, or numeric
nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### C1202: An internal-file-variable shall have a permitted character kind

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Restriction.

**Definition:** The character variable used as an internal-file-variable shall be default character,
ASCII character, or ISO 10646 character.

**Diagnostic obligation:** required.

**Facets:** `internal-file-variable-permitted-character-kind`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `internal-file-variable-permitted-character-kind`. Runtime
oracles use scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE
properties, exact integer/character/logical values, and IOSTAT zero only. No
processor-dependent ordinary unit number, physical device, IOMSG text, or numeric
nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.1-002: Units are external or internal and their designators determine the file category

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** A unit is either external or internal. An external unit refers to an external file and
is specified by an asterisk or file-unit-number. An internal unit refers to an internal
file and is specified by an internal-file-variable or by a defined-I/O unit argument
value.

**Diagnostic obligation:** not-required.

**Facets:** `unit-external-or-internal-classification`, `external-unit-designates-external-file`, `internal-unit-designates-internal-file`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `external-unit-designates-external-file`,
`internal-unit-designates-internal-file`, `unit-external-or-internal-classification`.
Runtime oracles use scratch or run-directory files, controlled stdin/stdout/stderr,
INQUIRE properties, exact integer/character/logical values, and IOSTAT zero only. No
processor-dependent ordinary unit number, physical device, IOMSG text, or numeric
nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.1-003: A file-unit-number value shall identify a valid unit from the permitted value set

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Restriction.

**Definition:** A file-unit-number value shall be nonnegative, an active defined-I/O unit argument, a
NEWUNIT value, or one of INPUT_UNIT, OUTPUT_UNIT, or ERROR_UNIT; the value shall
identify a valid unit.

**Diagnostic obligation:** not-required.

**Facets:** `file-unit-number-nonnegative-or-special`, `file-unit-number-valid-unit`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `file-unit-number-valid-unit`. Runtime oracles use scratch
or run-directory files, controlled stdin/stdout/stderr, INQUIRE properties, exact
integer/character/logical values, and IOSTAT zero only. No processor-dependent ordinary
unit number, physical device, IOMSG text, or numeric nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.1-004: A scalar integer expression value identifies the same external unit across program units on an image

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** On an image, the external unit identified by a particular scalar integer expression
value is the same external unit in all program units.

**Diagnostic obligation:** not-required.

**Facets:** `external-unit-identity-across-program-units`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `external-unit-identity-across-program-units`. Runtime
oracles use scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE
properties, exact integer/character/logical values, and IOSTAT zero only. No
processor-dependent ordinary unit number, physical device, IOMSG text, or numeric
nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.1-005: A READ asterisk designates the preconnected input unit on image 1 of the initial team

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** In a READ statement, an asterisk io-unit identifies the external unit preconnected for
sequential formatted input on image 1 in the initial team only, also identified by
INPUT_UNIT and used by READ without an io-control-spec list.

**Diagnostic obligation:** not-required.

**Facets:** `read-asterisk-input-unit`, `input-unit-preconnected-initial-image`, `input-unit-input_unit-constant`, `read-without-control-list-uses-input-unit`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `input-unit-input_unit-constant`,
`input-unit-preconnected-initial-image`, `read-asterisk-input-unit`,
`read-without-control-list-uses-input-unit`. Runtime oracles use scratch or
run-directory files, controlled stdin/stdout/stderr, INQUIRE properties, exact
integer/character/logical values, and IOSTAT zero only. No processor-dependent ordinary
unit number, physical device, IOMSG text, or numeric nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.1-006: A WRITE asterisk designates the preconnected output unit and PRINT uses that unit

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** In a WRITE statement, an asterisk io-unit identifies the external unit preconnected for
sequential formatted output, also identified by OUTPUT_UNIT and used by PRINT.

**Diagnostic obligation:** not-required.

**Facets:** `write-asterisk-output-unit`, `output-unit-preconnected`, `output-unit-output_unit-constant`, `print-uses-output-unit`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `output-unit-output_unit-constant`,
`output-unit-preconnected`, `print-uses-output-unit`, `write-asterisk-output-unit`.
Runtime oracles use scratch or run-directory files, controlled stdin/stdout/stderr,
INQUIRE properties, exact integer/character/logical values, and IOSTAT zero only. No
processor-dependent ordinary unit number, physical device, IOMSG text, or numeric
nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.1-007: The processor-dependent error-reporting unit is preconnected for sequential formatted output

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** The standard identifies a processor-dependent external unit for error reporting; it
shall be preconnected for sequential formatted output and is identified by ERROR_UNIT.

**Diagnostic obligation:** not-required.

**Facets:** `error-reporting-external-unit`, `error-unit-preconnected-formatted-output`, `error-unit-error_unit-constant`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `error-reporting-external-unit`,
`error-unit-error_unit-constant`, `error-unit-preconnected-formatted-output`. Runtime
oracles use scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE
properties, exact integer/character/logical values, and IOSTAT zero only. No
processor-dependent ordinary unit number, physical device, IOMSG text, or numeric
nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.1-008: A processor may make the error-reporting unit the same as the output unit

**Source:** S12.5.1, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** A processor may define the error-reporting unit to be the same as the output unit
identified by an asterisk.

**Diagnostic obligation:** not-required.

**Facets:** `error-unit-may-equal-output-unit`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.5.1 -->

## I/O units and connection fixture derivations

### `external_internal_designators` / `S12.5.1-001`

**Facets:** `io-unit-refers-to-file`.

12.5.1 p1-p2/R1201-R1203 and C1202 define external file-unit-number and default-character internal units; the fixture observes distinct exact records through each designator.

Feature mutations: `external-read-uses-internal-file`, `internal-read-uses-external-unit`, `internal-write-other-file`, `classification-reread-internal`, `external-written-record-changed`, `internal-file-initial-record-changed`, `default-character-width-changed`.

### `preconnected_input_units` / `S12.5.1-005`

**Facets:** `read-asterisk-input-unit`, `input-unit-preconnected-initial-image`, `input-unit-input_unit-constant`, `read-without-control-list-uses-input-unit`.

12.5.1 p4 maps READ * and INPUT_UNIT, and a READ without a control list, to the preconnected input unit; controlled stdin supplies three exact integers.

Feature mutations: `star-read-internal-file`, `input-unit-read-internal-file`, `bare-read-internal-file`, `star-read-other-internal`.

### `preconnected_output_units` / `S12.5.1-006`

**Facets:** `write-asterisk-output-unit`, `output-unit-preconnected`, `output-unit-output_unit-constant`, `print-uses-output-unit`.

12.5.1 p4 maps WRITE * , OUTPUT_UNIT, and PRINT to the preconnected formatted output unit; the harness observes exact output records.

Feature mutations: `star-output-to-error`, `named-output-to-error`, `print-output-to-error`, `output-record-text-changed`.

### `external_unit_identity_program_units` / `S12.5.1-004`

**Facets:** `external-unit-identity-across-program-units`.

12.5.1 p2-p3 and 12.5.6.13 allow a negative NEWUNIT value that identifies the same external unit in a called procedure; exact read-back proves identity.

Feature mutations: `write-through-other-unit`, `read-back-other-unit`, `procedure-value-changed`.

### `vector_subscript_internal_file` / `C1201`

**Facets:** `internal-file-variable-not-vector-subscript-section`.

C1201 requires the R1203 character variable not be an array section with a vector subscript; the control changes only the subscript to c(1:2).

Conforming control: `write(c(1:2),'(a)') 'AB'`.

### `nonvector_section_internal_file_control` / `C1201`

**Facets:** `internal-file-variable-not-vector-subscript-section`.

C1201 prohibits only vector-subscript array sections; the conforming control changes that one property to a triplet section c(1:2) and observes exact internal-file records.

Feature mutations: `triplet-section-reversed`.

### `io_unit_forms` / `R1201`

**Facets:** `io-unit-file-unit-number-form`, `io-unit-asterisk-form`, `io-unit-internal-file-variable-form`.

R1201 admits a file-unit-number, an asterisk, or an internal-file-variable; the fixture observes exact effects for all three io-unit forms.

Feature mutations: `file-unit-number-form-to-internal`, `internal-form-to-external`, `asterisk-form-to-error`.

### `scalar_integer_file_unit` / `R1202`

**Facets:** `file-unit-number-scalar-int-expr`.

R1202 makes a file-unit-number a scalar integer expression; u+0 selects the same unit and reads the exact record.

Feature mutations: `scalar-expression-selects-other`, `scalar-expression-record-changed`.

### `character_internal_file_variable` / `R1203`

**Facets:** `internal-file-variable-char-variable`.

R1203 requires an internal-file-variable to be a character variable; exact internal READ and WRITE effects prove that variable is the file.

Feature mutations: `read-other-character-variable`, `write-other-character-variable`.

### `default_character_internal_file` / `C1202`

**Facets:** `internal-file-variable-permitted-character-kind`.

C1202 permits default character internal-file variables; kind('a') is default character and exact internal READ observes it.

Feature mutations: `default-character-read-other`.

### `external_internal_classification` / `S12.5.1-002`

**Facets:** `unit-external-or-internal-classification`, `external-unit-designates-external-file`, `internal-unit-designates-internal-file`.

12.5.1 p2 classifies units as external or internal; a NEWUNIT external file and a character internal file produce distinct exact records.

Feature mutations: `external-designates-internal`, `internal-designates-external`, `external-record-changed`.

### `newunit_valid_file_unit` / `S12.5.1-003`

**Facets:** `file-unit-number-valid-unit`.

12.5.1 p2 permits a NEWUNIT value as a valid file-unit-number; the value is distinct from the other opened unit and supports exact I/O.

Feature mutations: `newunit-record-other-unit`, `newunit-read-other-unit`.

