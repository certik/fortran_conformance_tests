# Fortran 2023 16.9.164: PREVIOUS

The canonical catalogue is `doc/catalogues/previous_intrinsic_16_9_164.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.164")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted are listed in the catalogue. Fine census units are recorded there where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.164 -->

### S16.9.164-001: PREVIOUS argument restrictions

**Source:** S16.9.164, J3/24-007, 18 December 2023, physical PDF445. **Class:** Restriction.

**Definition:** A shall be of enumeration type; optional STAT shall be an integer scalar with decimal
exponent range at least four and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `PREVIOUS-A-enumeration-type`, `PREVIOUS-STAT-integer-scalar-range-at-least-four`, `PREVIOUS-STAT-intent-out`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, enumeration,
type/kind, rank/shape, and array-element-order observations. Argument shall-restrictions
are source-control restrictions only and are not promoted to diagnostic facets. Exact
result-value plans use discriminating inputs, non-default sentinels where assignment is
observed, non-vacuous companions for zero or false results, and feature mutations such
as changing the intrinsic name, DIM, MASK, VECTOR, STAT, kind, argument order, or
enumeration order.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for PACK, PARITY, POPCNT, POPPAR,
PRECISION, PRESENT, and PREVIOUS found no fixture-manifest binding for these 16.9
sections. Existing source uses of PRECISION and PRESENT occur in older clause 6 and 7
tests, profiles, and test helpers; PACK and PREVIOUS appear in negative helper regexes
or ordinary prose; those uses are recorded as existing coverage and are not rebound by
this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, optional-argument, enumeration, and I/O foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 16.3 integer bit model, clause 16.4 numeric model terminology, sibling 16.9
intrinsics named by pending plans such as DIGITS, RADIX, LOG10, IBSET, POPCNT, LBOUND,
SHAPE, SIZE, SELECTED_*_KIND, NEXT, and SAME_TYPE_AS, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, processor-dependency, or
scope terms.

### S16.9.164-002: PREVIOUS STAT assignment and absent-STAT effects

**Source:** S16.9.164, J3/24-007, 18 December 2023, physical PDF445. **Class:** Effect.

**Definition:** For first enumerator A, STAT is assigned a positive value; for other A values, STAT is
assigned zero; absent STAT initiates error termination for the first-enumerator case
that would assign a nonzero value.

**Diagnostic obligation:** not-required.

**Facets:** `PREVIOUS-STAT-first-enumerator-positive`, `PREVIOUS-STAT-nonfirst-enumerator-zero`, `PREVIOUS-first-without-STAT-error-termination`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, enumeration,
type/kind, rank/shape, and array-element-order observations. Argument shall-restrictions
are source-control restrictions only and are not promoted to diagnostic facets. Exact
result-value plans use discriminating inputs, non-default sentinels where assignment is
observed, non-vacuous companions for zero or false results, and feature mutations such
as changing the intrinsic name, DIM, MASK, VECTOR, STAT, kind, argument order, or
enumeration order.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for PACK, PARITY, POPCNT, POPPAR,
PRECISION, PRESENT, and PREVIOUS found no fixture-manifest binding for these 16.9
sections. Existing source uses of PRECISION and PRESENT occur in older clause 6 and 7
tests, profiles, and test helpers; PACK and PREVIOUS appear in negative helper regexes
or ordinary prose; those uses are recorded as existing coverage and are not rebound by
this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, optional-argument, enumeration, and I/O foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 16.3 integer bit model, clause 16.4 numeric model terminology, sibling 16.9
intrinsics named by pending plans such as DIGITS, RADIX, LOG10, IBSET, POPCNT, LBOUND,
SHAPE, SIZE, SELECTED_*_KIND, NEXT, and SAME_TYPE_AS, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, processor-dependency, or
scope terms.

### S16.9.164-003: PREVIOUS first-enumerator STAT value has processor-dependent magnitude

**Source:** S16.9.164, J3/24-007, 18 December 2023, physical PDF445. **Class:** Effect.

**Definition:** The exact positive STAT value assigned for first enumerator A is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `PREVIOUS-STAT-first-exact-value-processor-dependent`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, enumeration,
type/kind, rank/shape, and array-element-order observations. Argument shall-restrictions
are source-control restrictions only and are not promoted to diagnostic facets. Exact
result-value plans use discriminating inputs, non-default sentinels where assignment is
observed, non-vacuous companions for zero or false results, and feature mutations such
as changing the intrinsic name, DIM, MASK, VECTOR, STAT, kind, argument order, or
enumeration order.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for PACK, PARITY, POPCNT, POPPAR,
PRECISION, PRESENT, and PREVIOUS found no fixture-manifest binding for these 16.9
sections. Existing source uses of PRECISION and PRESENT occur in older clause 6 and 7
tests, profiles, and test helpers; PACK and PREVIOUS appear in negative helper regexes
or ordinary prose; those uses are recorded as existing coverage and are not rebound by
this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, optional-argument, enumeration, and I/O foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 16.3 integer bit model, clause 16.4 numeric model terminology, sibling 16.9
intrinsics named by pending plans such as DIGITS, RADIX, LOG10, IBSET, POPCNT, LBOUND,
SHAPE, SIZE, SELECTED_*_KIND, NEXT, and SAME_TYPE_AS, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, processor-dependency, or
scope terms.

### S16.9.164-004: PREVIOUS result characteristics

**Source:** S16.9.164, J3/24-007, 18 December 2023, physical PDF446. **Class:** Effect.

**Definition:** The PREVIOUS result has the same type and kind as A.

**Diagnostic obligation:** not-required.

**Facets:** `PREVIOUS-result-same-enumeration-type-as-A`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, enumeration,
type/kind, rank/shape, and array-element-order observations. Argument shall-restrictions
are source-control restrictions only and are not promoted to diagnostic facets. Exact
result-value plans use discriminating inputs, non-default sentinels where assignment is
observed, non-vacuous companions for zero or false results, and feature mutations such
as changing the intrinsic name, DIM, MASK, VECTOR, STAT, kind, argument order, or
enumeration order.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for PACK, PARITY, POPCNT, POPPAR,
PRECISION, PRESENT, and PREVIOUS found no fixture-manifest binding for these 16.9
sections. Existing source uses of PRECISION and PRESENT occur in older clause 6 and 7
tests, profiles, and test helpers; PACK and PREVIOUS appear in negative helper regexes
or ordinary prose; those uses are recorded as existing coverage and are not rebound by
this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, optional-argument, enumeration, and I/O foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 16.3 integer bit model, clause 16.4 numeric model terminology, sibling 16.9
intrinsics named by pending plans such as DIGITS, RADIX, LOG10, IBSET, POPCNT, LBOUND,
SHAPE, SIZE, SELECTED_*_KIND, NEXT, and SAME_TYPE_AS, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, processor-dependency, or
scope terms.

### S16.9.164-005: PREVIOUS result value returns the predecessor or first enumerator

**Source:** S16.9.164, J3/24-007, 18 December 2023, physical PDF446. **Class:** Effect.

**Definition:** For first enumerator A, PREVIOUS returns A; otherwise it returns the enumerator
preceding A.

**Diagnostic obligation:** not-required.

**Facets:** `PREVIOUS-first-enumerator-result-same`, `PREVIOUS-nonfirst-result-predecessor`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, enumeration,
type/kind, rank/shape, and array-element-order observations. Argument shall-restrictions
are source-control restrictions only and are not promoted to diagnostic facets. Exact
result-value plans use discriminating inputs, non-default sentinels where assignment is
observed, non-vacuous companions for zero or false results, and feature mutations such
as changing the intrinsic name, DIM, MASK, VECTOR, STAT, kind, argument order, or
enumeration order.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for PACK, PARITY, POPCNT, POPPAR,
PRECISION, PRESENT, and PREVIOUS found no fixture-manifest binding for these 16.9
sections. Existing source uses of PRECISION and PRESENT occur in older clause 6 and 7
tests, profiles, and test helpers; PACK and PREVIOUS appear in negative helper regexes
or ordinary prose; those uses are recorded as existing coverage and are not rebound by
this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, optional-argument, enumeration, and I/O foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 16.3 integer bit model, clause 16.4 numeric model terminology, sibling 16.9
intrinsics named by pending plans such as DIGITS, RADIX, LOG10, IBSET, POPCNT, LBOUND,
SHAPE, SIZE, SELECTED_*_KIND, NEXT, and SAME_TYPE_AS, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, processor-dependency, or
scope terms.

<!-- END GENERATED 16.9.164 -->
