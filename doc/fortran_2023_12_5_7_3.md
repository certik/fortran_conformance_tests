# Fortran 2023 12.5.7.3: CLOSE STATUS specifier

The canonical catalogue is `doc/catalogues/close_status_specifier_12_5_7_3.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.5.7.3 -->

### S12.5.7.3-001: CLOSE STATUS values are KEEP or DELETE

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Restriction.

**Definition:** The STATUS= scalar-default-character-expression shall evaluate to KEEP or DELETE.

**Diagnostic obligation:** not-required.

**Facets:** `close-status-keep-value`, `close-status-delete-value`, `close-status-other-values-prohibited`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.5.7.3-002: The STATUS specifier determines disposition of the connected file

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Effect.

**Definition:** The STATUS= specifier determines the disposition of the file connected to the specified
unit.

**Diagnostic obligation:** not-required.

**Facets:** `close-status-determines-connected-file-disposition`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.5.7.3-003: KEEP is not specified for a file whose prior status is SCRATCH

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Restriction.

**Definition:** KEEP shall not be specified for a file whose status before CLOSE execution is SCRATCH.

**Diagnostic obligation:** not-required.

**Facets:** `close-keep-scratch-prohibited`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.5.7.3-004: KEEP preserves an existing connected file

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Effect.

**Definition:** If KEEP is specified for a file that exists, the file continues to exist after CLOSE
execution.

**Diagnostic obligation:** not-required.

**Facets:** `close-keep-existing-file-continues`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.5.7.3-005: KEEP does not create a nonexistent file

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Effect.

**Definition:** If KEEP is specified for a file that does not exist, the file will not exist after CLOSE
execution.

**Diagnostic obligation:** not-required.

**Facets:** `close-keep-nonexistent-file-remains-nonexistent`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.5.7.3-006: DELETE leaves no file after CLOSE

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Effect.

**Definition:** If DELETE is specified, the file will not exist after CLOSE execution.

**Diagnostic obligation:** not-required.

**Facets:** `close-delete-file-nonexistent-after-close`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.5.7.3-007: Omitted CLOSE STATUS defaults to KEEP for nonscratch prior status

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Effect.

**Definition:** If STATUS= is omitted, the default value is KEEP unless the file status before CLOSE
execution is SCRATCH.

**Diagnostic obligation:** not-required.

**Facets:** `close-omitted-status-nonscratch-default-keep`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.5.7.3-008: Omitted CLOSE STATUS defaults to DELETE for prior SCRATCH status

**Source:** S12.5.7.3, J3/24-007, 18 December 2023, physical PDF256. **Class:** Effect.

**Definition:** If STATUS= is omitted for a file whose status before CLOSE execution is SCRATCH, the
default value is DELETE.

**Diagnostic obligation:** not-required.

**Facets:** `close-omitted-status-scratch-default-delete`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening a known file, writing known records, initializing variables to
nonzero/nonblank sentinels, or using a distinct labelled control path) and then observe
only portable properties: successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, exact internal records or file existence where the source fixes it, or named
intrinsic status values where the standard names them. Processor-dependent unit numbers,
file names, storage encodings, buffering, timing, IOMSG text, and specific numeric
status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint, or otherwise requires rejection and a future fixture can
observe the rejection portably. IOMSG wording and processor-dependent numeric status
values are never an oracle; unnumbered program restrictions are not promoted to required
diagnostics.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier and later clause 12 connection,
specifier, file position, data-transfer, internal-file, status, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry and
ISO_FORTRAN_ENV status intrinsics where future fixtures need them; clauses 3, 5, 14, 17,
18, and 19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

<!-- END GENERATED 12.5.7.3 -->
