# Fortran 2023 13.7.2.2: Integer editing

The canonical catalogue is `doc/catalogues/integer_editing_13_7_2_2.json`. Effective review state comes from `Registry.catalogue_review_state("13.7.2.2")`; the original source packet recorded source accounting and pending plans, while this fixture packet adds unreviewed executable fixtures without granting fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Eight base units: seven normative paragraphs and one informative note. Exact pending output plans use internal WRITE and fixed character lengths; enum/enumeration facets are deferred to type support and ownership.

<!-- BEGIN INTEGER BOZ EDITING FIXTURES -->
## Executable integer editing fixtures

This packet adds eight complete internal-file programs for selected 13.7.2.2 integer editing facets. Each output fixture writes an exact nonzero integer value to a CHARACTER variable, checks LEN before comparing the whole expected field, and uses SS where a positive sign would otherwise be optional. Input fixtures read exact internal character fields and compare integer sentinels. The generator owns only the listed selected facets and mutation plans; review state and execution approval remain integrator-owned.
<!-- END INTEGER BOZ EDITING FIXTURES -->

<!-- BEGIN GENERATED 13.7.2.2 -->

### S13.7.2.2-001: I editing field width, input-width restriction, effective item type, and related descriptors

**Source:** S13.7.2.2, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** I w and I w.m edit fields occupy w positions except that w=0 lets the processor select
output width; w shall not be zero on input; the effective item shall be integer, enum
type, or enumeration type; G/B/O/Z can also edit integer data.

**Diagnostic obligation:** not-required.

**Facets:** `I-width-fixed-or-selected`, `I-input-width-zero-prohibited`, `I-effective-item-type`, `integer-data-may-use-GBOZ`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.2-001 integer-width runtime fixture: one complete run/effect/f2023 program
writes the exact value 42 with (SS,I5) to a length-5 internal file and expects the five
characters '   42', then writes the same value with (SS,I0) to a length-2 internal file
and expects '42'. The SS sign mode suppresses the optional plus; the I5 field width
supplies three leading blanks. Permanent descriptor substitutions I5->I6 and I5->I5.4,
sign-control substitution SS->SP, input-value mutation 42->43, and exact-string oracle
mutations are all required to fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

Integer-editing fixture boundaries: this packet discharges only the selected integer
facets removed from pending above. It deliberately does not test the unnumbered I0 input
restriction, real/item-type mismatch controls, optional plus under processor S control,
m>w restrictions, zero m special cases, enum/enumeration editing, G/B/O/Z delegation,
diagnostics, external files, list-directed I/O, kind selection, overflow, or universal
compiler conformance. Expected strings are hand-derived from 13.7.2.2 and use internal
WRITE/READ only.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.2-002: The m component has no effect on I input

**Source:** S13.7.2.2, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** On input using I editing, m has no effect.

**Diagnostic obligation:** not-required.

**Facets:** `I-input-m-ignored`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.2-002 integer-input m runtime fixture: one complete program reads the exact
character field '7' with I1.1 and with I1 into separate integers and expects both
results to be 7. The only format difference is the .m component; input-field and
integer-oracle mutations are required to fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

Integer-editing fixture boundaries: this packet discharges only the selected integer
facets removed from pending above. It deliberately does not test the unnumbered I0 input
restriction, real/item-type mismatch controls, optional plus under processor S control,
m>w restrictions, zero m special cases, enum/enumeration editing, G/B/O/Z delegation,
diagnostics, external files, list-directed I/O, kind selection, overflow, or universal
compiler conformance. Expected strings are hand-derived from 13.7.2.2 and use internal
WRITE/READ only.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.2-003: Standard I input form and processor acceptance of nonstandard fields

**Source:** S13.7.2.2, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** The standard I input field is a signed-digit-string except for blank interpretation; a
nonstandard field that is not processor-acceptable causes an error condition.

**Diagnostic obligation:** not-required.

**Facets:** `I-input-signed-digit-string`, `I-input-nonstandard-error-condition`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.2-003 signed-digit input runtime fixture: one complete program reads '-42' with
I3 and expects -42, and reads ' 42' with I3 and expects 42 after leading-blank handling.
The signs and digits are hand-derived signed-digit strings; both field-value and
integer-oracle mutations fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

Integer-editing fixture boundaries: this packet discharges only the selected integer
facets removed from pending above. It deliberately does not test the unnumbered I0 input
restriction, real/item-type mismatch controls, optional plus under processor S control,
m>w restrictions, zero m special cases, enum/enumeration editing, G/B/O/Z delegation,
diagnostics, external files, list-directed I/O, kind selection, overflow, or universal
compiler conformance. Expected strings are hand-derived from 13.7.2.2 and use internal
WRITE/READ only.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.2-004: Iw output has blanks, required minus or optional plus, and magnitude without leading zeros

**Source:** S13.7.2.2, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** I w output consists of leading blanks, a required minus for negative values or optional
plus otherwise, and the magnitude as a digit-string without leading zeros.

**Diagnostic obligation:** not-required.

**Facets:** `I-output-leading-blanks`, `I-output-negative-minus`, `I-output-optional-plus`, `I-output-no-leading-zero-magnitude`.

**Oracle:** Optional plus is permission; the other listed properties are required output form
properties.

S13.7.2.2-004 Iw output runtime fixtures: three complete programs cover leading blanks,
required minus, and no leading-zero magnitude. (SS,I5) with 42 expects '   42'; I5 with
-42 expects '  -42'; and (SS,I5) with 7 expects '    7'. Positive cases use SS so the
optional plus latitude is not an oracle. Exact field length is checked before character
equality. I5 descriptor, SS sign, value, and literal-oracle mutations are load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

Integer-editing fixture boundaries: this packet discharges only the selected integer
facets removed from pending above. It deliberately does not test the unnumbered I0 input
restriction, real/item-type mismatch controls, optional plus under processor S control,
m>w restrictions, zero m special cases, enum/enumeration editing, G/B/O/Z delegation,
diagnostics, external files, list-directed I/O, kind selection, overflow, or universal
compiler conformance. Expected strings are hand-derived from 13.7.2.2 and use internal
WRITE/READ only.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.2-005: Iw.m output minimum digits, m<=w restriction, and zero special cases

**Source:** S13.7.2.2, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** I w.m output is like I w but the digit-string has at least m digits; zeros are inserted
as needed; m shall not exceed w except when w=0; if m=0 and value is zero the field is
all blanks, and when m=w=0 one blank is produced.

**Diagnostic obligation:** not-required.

**Facets:** `I-output-minimum-m-digits`, `I-output-leading-zeros-to-m`, `I-output-m-not-greater-than-w`, `I-output-m0-zero-all-blanks`, `I-output-m0-w0-zero-one-blank`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.2-005 Iw.m output runtime fixtures: two complete programs cover minimum digit
count and zero insertion to m. (SS,I5.4) with 42 expects ' 0042': four magnitude digits
are formed as 0042 and right-justified in width 5. (SS,I3.2) with 7 expects ' 07': two
magnitude digits are formed as 07 and right-justified in width 3. Descriptor, SS sign,
value, and exact-string mutations fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

Integer-editing fixture boundaries: this packet discharges only the selected integer
facets removed from pending above. It deliberately does not test the unnumbered I0 input
restriction, real/item-type mismatch controls, optional plus under processor S control,
m>w restrictions, zero m special cases, enum/enumeration editing, G/B/O/Z delegation,
diagnostics, external files, list-directed I/O, kind selection, overflow, or universal
compiler conformance. Expected strings are hand-derived from 13.7.2.2 and use internal
WRITE/READ only.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.2-006: Enumeration type I editing uses ordinal positions with input bounds

**Source:** S13.7.2.2, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** For enumeration type output, I editing outputs the ordinal position; for input, the
field value shall be positive and no greater than the number of enumerators, and assigns
the enumeration value at that ordinal.

**Diagnostic obligation:** not-required.

**Facets:** `enumeration-output-ordinal-position`, `enumeration-input-positive-bounded`, `enumeration-input-ordinal-assignment`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.2-007: Enum type I editing uses corresponding integer values

**Source:** S13.7.2.2, J3/24-007, 18 December 2023, physical PDF295. **Class:** Effect.

**Definition:** For enum type output, I editing outputs the corresponding integer value; for input, the
assigned value is the enum value corresponding to the input field integer.

**Diagnostic obligation:** not-required.

**Facets:** `enum-output-corresponding-integer`, `enum-input-corresponding-value`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

<!-- END GENERATED 13.7.2.2 -->
