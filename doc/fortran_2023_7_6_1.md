# Fortran 2023: 7.6.1 Interoperable enumerations and enum types

**Catalogue source review: reviewed.** Case and evidence adjudications are separate content-bound records.

**2 runtime effect programs** represent **7 of64 facets**; **57 remain pending**. There are no new diagnostic or compile-only cases.

## Bounded source gate and preserved ownership

Authority: J3/24-007, **18 December 2023**, **688 physical PDF pages**, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The original source commit is `fa1fbf02ec0ee55dffb543c509aa276f418cc35d`.
Independent `enumerations-source-review.json` has SHA-256
`2cba3b4a7bc9d6371ae4c81829e44eb0617e45ed5fd0ee8d99e2563d761b53e9`.
Original7.6.1 on PDF109-111 and directed INTEGER, PARAMETER, constant-expression,
INT and KIND dependencies were read. No7.6.2 or7.7 body is required or newly owned.

All23 original base units,93 fine units,116 accounting rows,15 requirements and64
facet identities are preserved. Only the seven selected pending/oracle
implementation entries change. Named enum types, ENUMERATION TYPE, BOZ,
representation/C-companion profiles, unsigned extremes, source-use links and
diagnostic policies remain outside this packet.

## Concrete finite effects

The common-kind program uses one unnamed `ENUM,BIND(C)` definition across two
ENUMERATOR statements. Its six INTEGER constants have independent expected
values0/1/4/5/9/10. KIND is queried on these intrinsic INTEGER entities, never
on a nonintrinsic enum object, and every member is compared to this definition's
first member without guessing an identifier or comparing independent definitions.

The shared sequence program has four definitions with fresh names. Flat and
two-statement forms each check `[0,1,4,5,9,10,4,5]` against their own literal
expectation. The later explicit4 after10 resets the following value to5. Neither
definition is used as the other's oracle. A separate `[-3,-2,4,4,5]` definition
checks negative and repeated numerical values, and a fresh `[0,1]` definition
checks reset after the preceding5. Unique names, not distinct numerical values,
are required. Every omitted initializer here produces a defined named constant.

INT only converts these small values to default INTEGER for homogeneous observer
arrays;7.4.3.1p4 guarantees that representation's range is ample. It does not
convert KIND codes or select any extra kind. The KIND results already have
default INTEGER type. Each call supplies a separate definition-local comparison;
no global KIND variable or equality between definitions is used.

The observer checks nonempty matching vector sizes, exact ordered values and
the computed within-definition KIND condition. Expected literals and source
initializer/statement partitions are independently modeled in the regressions.
Runtime wrong-expectation sensitivity probes are session-only and require a
successful reference build before an intended runtime self-check failure; they
are not new language-invalid cases or diagnostic policies.

## Source qualifications and evidence limits

ENS-Q01's12345 illustration, ENS-Q02's NEXT endpoint/error termination,
ENS-Q03's BOZ contexts and ENS-Q04's representation/companion gaps stay out of
this implementation. ENS-Q05 is enforced through fresh constant names and
defined implicit values; ENS-Q06 keeps runtime effects distinct from restriction
controls, definitions, profiles and source-use work.

`S6_3_2_2_005_valid__enum` and all1907 author-base source/manifest/ID/fingerprint
bindings are unchanged. That existing Clause6 spelling case checks explicit1/2;
it is neither copied nor re-owned. Integration separately retains main's later
three metadata renewals and nine current links rather than restoring the older
author context. Source/case/inventory adjudications are separate content-bound
records, not actions performed by this generator.

All cases remain run-phase on implementation failure. Frozen LF411, GNU actual
f2023 and Flang actual f2018 observations retain their original inputs, compiler
identities, ordered compile/link/run traces and source_root. No f2018 observation
is relabelled as f2023 and no compiler consensus supplies an expected value.

## Preserved source accounting

* `C7111`: **requirements** - C7111
* `C7111.any-initializer-antecedent`: **requirements** - C7111
* `C7111.separator-before-list`: **requirements** - C7111
* `C7111.per-statement-scope`: **requirements** - C7111
* `C7112`: **requirements** - C7112
* `C7112.actual-enum-type-name`: **requirements** - C7112
* `C7112.previous-definition`: **requirements** - C7112
* `C7113`: **requirements** - C7113
* `C7113.integer-alternative`: **requirements** - C7113
* `C7113.boz-alternative`: **requirements** - C7113
* `R759`: **requirements** - R759
* `R759.opening-statement`: **requirements** - R759
* `R759.first-enumerator-statement`: **requirements** - R759
* `R759.further-enumerator-statements`: **requirements** - R759
* `R759.closing-statement`: **requirements** - R759
* `R760`: **requirements** - R760
* `R760.enum-keyword`: **requirements** - R760
* `R760.comma-and-bind-c`: **requirements** - R760
* `R760.optional-name-group`: **requirements** - R760
* `R760.name-double-colon`: **requirements** - R760
* `R760.enum-type-name`: **requirements** - R760
* `R761`: **requirements** - R761
* `R761.enumerator-keyword`: **requirements** - R761
* `R761.optional-double-colon`: **requirements** - R761
* `R761.nonempty-enumerator-list`: **requirements** - R761
* `R762`: **requirements** - R762
* `R762.named-constant`: **requirements** - R762
* `R762.optional-initializer`: **requirements** - R762
* `R762.initializer-scalar`: **requirements** - R762
* `R762.initializer-integer`: **requirements** - R762
* `R762.initializer-constant`: **requirements** - R762
* `R763`: **requirements** - R763
* `R763.end-enum-keywords`: **requirements** - R763
* `R763.no-trailing-type-name`: **requirements** - R763
* `R764`: **requirements** - R764
* `R764.enum-type-name`: **requirements** - R764
* `R765`: **requirements** - R765
* `R765.enum-type-specifier`: **requirements** - R765
* `R765.one-parenthesized-expression`: **requirements** - R765
* `R765.scalar-expression`: **requirements** - R765
* `note1`: **informative** - Original NOTE1, PDF110: conditional unsigned/signed representation explanation, not a new local arithmetic or diagnostic policy.
* `note1.unsigned-c-selection`: **informative** - The example condition is a companion using an unsigned C type for its enum representation, not a promise that every companion does so.
* `note1.signed-fortran-same-width`: **informative** - The note explains use of the signed Fortran integer type of the corresponding width; it does not prescribe a width or KIND number.
* `note1.c-value-representability-exception`: **informative** - Some C enumerator values may not be representable in that signed integer type. Do not infer equal signed/unsigned mathematical domains or a wraparound result.
* `note1.interoperable-types-despite-value-gap`: **informative** - Type interoperability remains distinct from representability of every C value. Extreme unsigned-value candidates require separate source/companion qualification.
* `note2`: **informative** - Original NOTE2, PDF110: a C2018-based evaluation explanation; no hard-coded kind equality or new implementation algorithm is required.
* `note2.c-int-constant-bound`: **informative** - The note cites the C2018 requirement that enumeration constants fit C int.
* `note2.c-int-evaluation-route`: **informative** - C_INT can be used to evaluate these values before selecting the enum's representation.
* `note2.selection-after-evaluation`: **informative** - The evaluation route does not say the resulting enumerator KIND is C_INT, default INTEGER, or a minimum SELECTED_INT_KIND result.
* `note3`: **informative** - Original NOTE3, PDF110 elaborates p5. Its representation condition must not be replaced by a type-identity or name-matching claim.
* `note3.c-compatibility-names-values-order`: **informative** - The cited C compatibility condition includes names as well as values and order.
* `note3.additional-companion-representation-condition`: **informative** - The Fortran companion condition instead requires the same representation for identical ordered value lists even when names differ.
* `note3.representation-not-type-identity`: **informative** - Neither matching representation nor ignored names for that representation condition erases C compatibility or Fortran enum-type identity distinctions.
* `note4`: **informative** - Original NOTE4, PDF110 preserves the example's values, processor dependence, conditional equivalence and valid KIND inquiry target.
* `note4.four-nine-ten-example`: **informative** - The illustrated explicit values4/9 and following implicit10 exemplify p6.
* `note4.processor-selected-kind`: **informative** - The example kind is processor dependent, not an intrinsic numeric KIND code.
* `note4.sufficient-representation-for-values`: **informative** - The selected representation must suffice for the example's4/9/10; this is not a guarantee for unrelated larger literals.
* `note4.selected-int-kind-might-match`: **informative** - The SELECTED_INT_KIND(2) declaration is only a possible equivalent, not a required equality or prescribed selection algorithm.
* `note4.kind-from-enumerator`: **informative** - KIND(RED) identifies the INTEGER enumerator's actual kind for another INTEGER declaration; KIND(enum-type-object) is not authorized.
* `note5`: **informative** - Original NOTE5, PDF111 explains p6 across statements; do not invent a reset at each ENUMERATOR statement.
* `note5.statement-partition-does-not-change-effect`: **informative** - One versus several ENUMERATOR statements does not change the definition's effect when enumerator order is unchanged.
* `note5.enumerator-order-is-significant`: **informative** - Declaration order, not statement count, controls p6's immediately preceding enumerator.
* `note6`: **informative** - Original NOTE6, PDF111 is fully retained as qualified examples and canonical consumer relationships, not additional local mandatory executions.
* `note6.two-named-type-definitions`: **informative** - The example defines two named enum types; their integer enumerators do not collapse the two type definitions.
* `note6.bind-c-value-and-enum-io`: **informative** - The illustrated BIND(C)/VALUE interface and enum output require18.3 and the canonical I/O rules, not a local enum-type-as-INTEGER identity.
* `note6.enumerator-and-constructor-initialization`: **informative** - An INTEGER expression with a primary from the correct enum can initialize that enum type under7.3.3/8.2/10.2.1.2. This is broader than requiring a bare enumerator and narrower than accepting every INTEGER expression.
* `note6.literal-12345-conditional-range`: **informative** - myenum(12345) is a valid illustration only on a representation that can hold12345. The definition's1/2/3 does not itself establish that range; retain ENS-Q01 rather than copy an unconditional success case.
* `note6.negative-huge-integer-carrier`: **informative** - The negative bound is -HUGE(one), with one an INTEGER enumerator. It is not HUGE of an enum-type object or an assumption that signed values have the same mathematical meaning in a C unsigned carrier.
* `note6.raw-integer-no-enumerator-primary`: **informative** - The invalid raw-integer initializer/call examples lack the necessary enum primary. Their large literals may add a range defect; a future isolated compatibility candidate must instead use a known-representable raw value.
* `note6.wrong-enumerator-provenance`: **informative** - The other enum's f1 remains an INTEGER constant; the issue is wrong defining-enum provenance in the expression, not a claim that f1 has TYPE(flags).
* `p1`: **structural** - Original PDF109: the separate fine units preserve every condition and distinguish definitions, effects, restrictions and examples.
* `p1.enumeration-set`: **definition** - An interoperable enumeration denotes a set of interoperable enumerators.
* `p1.optional-enum-type`: **definition** - A named enum type is optional; an unnamed ENUM,BIND(C) definition does not create a nameable enum type.
* `p1.enum-def-defines-enumeration`: **definition** - The complete enum-def, not just its header, defines that interoperable enumeration.
* `p1.enumerator-integer-constant`: **definition** - An interoperable enumerator is a named INTEGER constant, not a value whose declared type is the optional enum type.
* `p1.common-enumerator-kind`: **requirements** - S7.6.1-001
* `p1.nonintrinsic-enum-type`: **definition** - A named enum type belongs to the nonintrinsic category.
* `p1.not-derived`: **definition** - An enum type is not a derived type. Derived-type-only rules cannot be imported by analogy.
* `p1.no-type-parameter`: **definition** - The enum type itself has no type parameter; the INTEGER kind of its enumerators is a different notion.
* `p2`: **requirements** - S7.6.1-001
* `p2.each-enumerator-kind`: **requirements** - S7.6.1-001
* `p2.corresponding-c-enumerated-type`: **requirements** - S7.6.1-001
* `p2.c2018-enumeration-specifier`: **requirements** - S7.6.1-001
* `p2.same-specified-values`: **requirements** - S7.6.1-001
* `p2.same-declaration-order`: **requirements** - S7.6.1-001
* `p3`: **structural** - Original PDF109: the separate fine units preserve every condition and distinguish definitions, effects, restrictions and examples.
* `p3.name-present-condition`: **definition** - The optional enum-type-name controls whether this definition also defines a named enum type.
* `p3.defines-the-named-type`: **definition** - When present, that name identifies the enum type defined by this enum-def; no nameable type is inferred for an unnamed block.
* `p3.interoperable-type`: **definition** - The named enum type is interoperable.18.3.3 supplies the corresponding C enum and C integer-type interoperability relationships.
* `p3.integer-kind-value-domain`: **requirements** - S7.6.1-002
* `p3.one-to-one-value-correspondence`: **requirements** - S7.6.1-002
* `p3.same-corresponding-internal-representation`: **requirements** - S7.6.1-002
* `p4`: **definition** - The paragraph defines the type-specifier and same-definition relationship; it is not an executable classification requirement.
* `p4.specifier-identifies-type`: **definition** - An enum type specifier identifies its type; the original 'specifiers' typography does not introduce an extra operation.
* `p4.same-definition-reference`: **definition** - Reference to the same enum type definition is the stated same-type criterion. Matching integer values, KIND numbers or companion representations are not that defining reference.
* `p5`: **requirements** - S7.6.1-003
* `p5.selected-companion-subject`: **requirements** - S7.6.1-003
* `p5.all-c-enumeration-specifiers`: **requirements** - S7.6.1-003
* `p5.same-values-condition`: **requirements** - S7.6.1-003
* `p5.same-order-condition`: **requirements** - S7.6.1-003
* `p5.same-representation-required`: **requirements** - S7.6.1-003
* `p6`: **structural** - Original PDF110: the separate fine units preserve every condition and distinguish definitions, effects, restrictions and examples.
* `p6.parameter-treatment`: **definition** - An enumerator is treated as explicitly PARAMETER;8.5.13 supplies named-constant and prior-definition conditions.
* `p6.scalar-named-constant`: **definition** - The entity is a scalar named constant. This does not give an enum-type variable INTEGER identity.
* `p6.explicit-initializer-condition`: **requirements** - S7.6.1-004
* `p6.explicit-initializer-value`: **requirements** - S7.6.1-004
* `p6.absent-initializer-first-condition`: **requirements** - S7.6.1-004
* `p6.first-default-zero`: **requirements** - S7.6.1-004
* `p6.absent-initializer-nonfirst-condition`: **requirements** - S7.6.1-004
* `p6.immediately-preceding-in-same-definition`: **requirements** - S7.6.1-004
* `p6.previous-value-plus-one`: **requirements** - S7.6.1-004
* `p7`: **structural** - Original PDF110: the separate fine units preserve every condition and distinguish definitions, effects, restrictions and examples.
* `p7.constructor-scalar-result`: **requirements** - S7.6.1-002
* `p7.constructor-specified-enum-type`: **requirements** - S7.6.1-002
* `p7.constructor-specified-internal-representation`: **requirements** - S7.6.1-002
* `p7.argument-value-representable`: **requirements** - S7.6.1-005

## Definitions

<!-- BEGIN GENERATED 7.6.1 -->

### R759: An enum definition contains one or more enumerator statements

**Source:** 7.6.1, R759; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Syntax.

**Definition:** An enum-def has an ENUM definition statement, a mandatory first enumerator definition
statement, zero or more further enumerator definition statements, and an END ENUM
statement.

**Diagnostic obligation:** required.

**Facets:** `single-enumerator-statement`, `multiple-enumerator-statements`, `nonempty-definition`, `enclosing-and-closing-source-use`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R760-R763;4.1.3/R401;R508;4.2p2(3);7.6.1p6.

### R760: ENUM requires BIND(C) and optionally defines a named enum type

**Source:** 7.6.1, R760; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Syntax.

**Definition:** The ENUM header requires the comma followed by BIND(C). It may additionally contain a
double-colon separator followed by one enum-type-name. Naming the type is optional;
BIND(C) is not. This is not the ENUMERATION TYPE header.

**Diagnostic obligation:** required.

**Facets:** `unnamed-bind-c`, `named-bind-c`, `required-bind-c`, `required-comma`, `c-language-designator`, `named-header-separator`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R759/R764/C7112;R703;7.3.2.2p2;6.1.2;4.2p2(3).

### R761: ENUMERATOR statements have a nonempty comma-separated enumerator list

**Source:** 7.6.1, R761; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Syntax.

**Definition:** An enum enumerator statement consists of ENUMERATOR, an optional double colon, and a
nonempty comma-separated enumerator list. C7111 additionally requires the double colon
whenever an initializer occurs in that statement.

**Diagnostic obligation:** required.

**Facets:** `colon-and-colon-free-admissions`, `list-separator`, `nonempty-list`, `initializer-separator-source-use`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R762/C7111;4.1.3/R401;4.2p2(3).

### R762: An enum initializer is optional but must be scalar integer constant syntax

**Source:** 7.6.1, R762; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Syntax.

**Definition:** Each enum enumerator names a constant and may include = followed by a scalar integer
constant expression. Scalar, INTEGER and constant-expression conditions are distinct.
Neither a dynamic INTEGER initializer nor a scalar noninteger constant satisfies this
grammar.

**Diagnostic obligation:** required.

**Facets:** `implicit-and-explicit-initializers`, `prior-integer-constant-expression`, `scalar-initializer`, `integer-initializer`, `constant-initializer`, `constant-and-name-consumer-graph`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** C7111;4.1.3/C401;10.1.12/R1032/C1014;8.5.13p1-p2;19.3.1;4.2p2(3)/(6).

### R763: END ENUM closes the definition without an appended enum name

**Source:** 7.6.1, R763; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Syntax.

**Definition:** The enum closing statement is END ENUM; its syntax has no trailing type name. The
optional blank in END ENUM is a separate free-source-form rule.

**Diagnostic obligation:** required.

**Facets:** `closing-statement-admission`, `trailing-name`, `source-spelling-reuse`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R759;6.3.2.2/Table6.2, PDF71;4.1.4;4.2p2(3)/(5).

### C7111: Any explicit enum initializer requires double colons in its statement

**Source:** 7.6.1, C7111; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Restriction.

**Definition:** If any enumerator in a particular enumerator definition statement uses =, a double-colon
separator must precede that statement's enumerator list. The trigger is not limited to
the first enumerator, and does not require colons in other statements whose lists have
no initializer.

**Diagnostic obligation:** required.

**Facets:** `first-initializer`, `later-list-initializer`, `later-statement-initializer`, `no-initializer-controls`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R761/R762;7.6.1p6;4.2p2(3).

### R764: An enum type specifier uses its enum type name

**Source:** 7.6.1, R764; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Syntax.

**Definition:** The enum-type-spec syntax consists of an enum-type-name. Its use inside TYPE, an
allocation/array constructor type-spec, or an enum constructor is governed by the
corresponding consumer and C7112; it is not an intrinsic KIND selector.

**Diagnostic obligation:** required.

**Facets:** `named-specifier-admission`, `identity-and-specifier-consumers`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** C7112;R702/R703;7.3.2.1-.2;7.6.1p1/p4;14.2.2;19.3.1.

### C7112: An enum type reference requires a previously defined enum type

**Source:** 7.6.1, C7112; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Restriction.

**Definition:** The name in an enum-type-spec must name a previously defined enum type. Previously
defined USE-associated types and the function-result as-if declaration rule in7.3.2.2p2
must be respected; a lexical forward occurrence in such a FUNCTION prefix is not
automatically an invalid ordinary declaration.

**Diagnostic obligation:** required.

**Facets:** `previous-definition-admissions`, `ordinary-forward-type-declaration`, `function-result-as-if-exception`, `name-category-and-identity-graph`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R764;7.3.2.2p2, PDF78;14.2.2p2, PDF318-319;19.3.1;4.2p2(3)/(6).

### R765: An enum constructor has one scalar expression argument

**Source:** 7.6.1, R765; J3/24-007, 18 December 2023, physical PDF 110. **Class:** Syntax.

**Definition:** An enum constructor is an enum type specifier followed by one parenthesized scalar
expression. The operand need not be constant. C7113 restricts its type, and p7
separately requires a representable value.

**Diagnostic obligation:** required.

**Facets:** `constant-and-dynamic-integer-admissions`, `scalar-argument`, `single-argument`, `constructor-consumer-and-boz-graph`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R764/C7112/C7113;4.1.3/C401;10.1.12p1(4);10.2.1.3p13;7.6.1p7.

### C7113: Enum constructors accept INTEGER or BOZ, not other operand types

**Source:** 7.6.1, C7113; J3/24-007, 18 December 2023, physical PDF 110. **Class:** Restriction.

**Definition:** The scalar expression in an enum constructor must be INTEGER or a boz-literal-constant.
The explicit BOZ alternative belongs to the enum constructor; it is not an allowance for
ENUMERATOR initializers or ENUMERATION TYPE constructors. Value representability remains
a separate p7 premise.

**Diagnostic obligation:** required.

**Facets:** `integer-admissions`, `real-operand`, `logical-operand`, `character-operand`, `boz-admission-qualification`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R765;7.6.1p7;4.2p2(3);7.7 is explicitly outside this source-read scope.

### S7.6.1-001: Enum enumerators share the kind interoperable with the corresponding C enum

**Source:** 7.6.1, p1, p2; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Effect.

**Definition:** All INTEGER enumerators in one enum-def have one kind. That kind is interoperable with
the hypothetical C2018 enumerated type whose enumeration constants have the same
specified values in the same declaration order. The rule applies with or without an
optional named enum type; it does not select C_INT or a fixed width.

**Diagnostic obligation:** not-required.

**Facets:** `within-definition-common-kind`, `corresponding-c-values-and-order`, `unsigned-and-kind-selection-documentation`.

**Oracle:** A bounded same-definition KIND/value witness and separately qualified C interoperability
witness have distinct evidentiary roles. C_INT is available for the cited evaluation
route, not a prescribed enum kind. A complete C companion proof is not a compiler
consensus experiment.

Finite unnamed-enum implementation: 1 shared runtime program represents 1 selected
facets. Independent literal vectors are checked separately for each definition,
including flat/split forms, negative/repeated values and fresh-definition reset where
applicable. Every definition compares each INTEGER enumerator's KIND to its own first
member; no cross-definition or numeric KIND-code equality is imposed. Small value-only
INT conversions use the required default INTEGER representation. All other original
plans remain pending; observations do not confer source/case approval.

**Oracle limitation:** Use defined values and source-derived finite expectations, not compiler agreement. Keep
enum type, its INTEGER enumerators and ENUMERATION TYPE distinct. KIND identifiers, byte
width, C_INT equality, a minimum representation, unsigned wrap, padding bits and
additional supported kinds are not assumed. Runtime effects remain runtime on
implementation failure. No companion profile or C experiment is supplied in this bounded
runtime packet.

**Dependencies:** 7.6.1p1/p2/p5 and NOTE1-NOTE4;5.5.7;18.3.1/Table18.2 and18.3.3/.5/.7;16.9.118
KIND;4.2p2(1)/(4).

### S7.6.1-002: Named enum values correspond to the enumerator integer kind and constructors produce those values

**Source:** 7.6.1, p3, p7; J3/24-007, 18 December 2023, physical PDF 109, 110. **Class:** Effect.

**Definition:** The value set of a named enum type corresponds one-to-one with all possible values of
its enumerators' INTEGER kind, not only the listed enumerator values. Each enum value
has the same internal representation as its corresponding integer. An enum constructor
produces a scalar of the specified enum type with the specified internal representation;
its operand must separately meet C7113 and p7's range condition.

**Diagnostic obligation:** not-required.

**Facets:** `integer-value-roundtrip`, `values-not-limited-to-enumerators`, `constant-and-dynamic-constructors`, `scalar-result-and-same-type-context`, `internal-representation-qualification-graph`, `canonical-conversion-reuse`.

**Oracle:** Separate value-domain/constructor effects from the stronger internal-representation
statement. The independent finite values0/4/6/9 avoid processor range assumptions. Every
compared operand meets the actual enum/integer-primary or same-type relation premises.

**Oracle limitation:** No executable evidence is authored. Use defined values and source-derived finite
expectations, not compiler agreement. Keep enum type, its INTEGER enumerators and
ENUMERATION TYPE distinct. KIND identifiers, byte width, C_INT equality, a minimum
representation, unsigned wrap, padding bits and additional supported kinds are not
assumed. Future runtime effects must remain runtime on implementation failure. No
padding/bit-pattern witness or arbitrary nonenumerator range is currently qualified.

**Dependencies:** 7.6.1p3/p7;R764/R765/C7112/C7113;7.3.3;10.1.5.1/Table10.2
and10.1.5.5.1p10;10.2.1.2/Table10.8 and10.2.1.3p13;16.9.110p5(iv);16.9.1p2.

### S7.6.1-003: Companion enum representation must be uniform for identical ordered value lists

**Source:** 7.6.1, p5; J3/24-007, 18 December 2023, physical PDF 109. **Class:** Restriction.

**Definition:** The selected companion processor must use the same representation for all C types
declared by enumeration specifiers that specify the same values in the same order.
Enumerator names are not part of this representation condition. This is a
processor/companion restriction, not a new requirement to diagnose a Fortran program or
a declaration that two C or Fortran types are identical.

**Diagnostic obligation:** not-required.

**Facets:** `same-values-order-policy`, `different-names-same-representation`, `representation-and-interface-use-graph`.

**Oracle:** A documented selected-companion contract is the outstanding premise. Any eventual
executable witness is a positive control under this restriction and only proves its
finite observations.

**Oracle limitation:** No companion selection/representation documentation has been independently qualified
here. Rejecting a submitted program still needs a reason under4.2p2(10), but that does
not create mandatory fatal rejection or a local diagnostic for this processor property.

**Dependencies:** 7.6.1p5/NOTE3;5.5.7;C2018 clauses cited by7.6.1p2/NOTE3;18.3.1/.3/.7;4.2p2(10).

### S7.6.1-004: Enum values use explicit initializers or the preceding value plus one

**Source:** 7.6.1, p6; J3/24-007, 18 December 2023, physical PDF 110. **Class:** Effect.

**Definition:** An explicit scalar INTEGER constant initializer determines that enumerator's value.
Without an initializer, the first enumerator of an enum-def has value0; every later one
has the immediately preceding enumerator's value plus one. The sequence is over the
whole enum-def, not restarted by each ENUMERATOR statement.

**Diagnostic obligation:** not-required.

**Facets:** `first-implicit-zero`, `explicit-values`, `successor-after-explicit`, `across-statement-boundary`, `negative-and-repeated-values`, `new-definition-reset`, `named-constant-and-existing-context-graph`.

**Oracle:** One finite ordered-value matrix can share the explicit/default/reset/partition facets.
Compare actual named INTEGER constants with independent literal expectations; choose
small complete value sets whose representation premises do not depend on a default ABI.

Finite unnamed-enum implementation: 1 shared runtime program represents 6 selected
facets. Independent literal vectors are checked separately for each definition,
including flat/split forms, negative/repeated values and fresh-definition reset where
applicable. Every definition compares each INTEGER enumerator's KIND to its own first
member; no cross-definition or numeric KIND-code equality is imposed. Small value-only
INT conversions use the required default INTEGER representation. All other original
plans remain pending; observations do not confer source/case approval.

**Oracle limitation:** Use defined values and source-derived finite expectations, not compiler agreement. Keep
enum type, its INTEGER enumerators and ENUMERATION TYPE distinct. KIND identifiers, byte
width, C_INT equality, a minimum representation, unsigned wrap, padding bits and
additional supported kinds are not assumed. Runtime effects remain runtime on
implementation failure. Do not test successor overflow, assume modular arithmetic,
require distinct numerical values or add invalid twins for pure value effects.

**Dependencies:** 7.6.1p6/NOTE4/NOTE5;R761/R762/C7111;8.5.13;10.1.12;16.9.118 KIND.

### S7.6.1-005: An enum constructor operand must be representable in that enum type

**Source:** 7.6.1, p7; J3/24-007, 18 December 2023, physical PDF 110. **Class:** Restriction.

**Definition:** The value supplied to an enum constructor must be representable in objects of the
specified enum type. The admissible domain is that enum's corresponding INTEGER kind,
not a count of its enumerators and not a presumed C_INT/default range. This plain prose
value restriction does not itself mandate a diagnostic.

**Diagnostic obligation:** not-required.

**Facets:** `known-representable-controls`, `out-of-range-policy-gate`, `boz-and-example-range-qualification`.

**Oracle:** Only independently representable value controls are currently planned. A runtime range
violation is not a required trap, and a compiler's rejection or acceptance cannot
establish the permissible domain.

**Oracle limitation:** No executable evidence is authored. Use defined values and source-derived finite
expectations, not compiler agreement. Keep enum type, its INTEGER enumerators and
ENUMERATION TYPE distinct. KIND identifiers, byte width, C_INT equality, a minimum
representation, unsigned wrap, padding bits and additional supported kinds are not
assumed. Future runtime effects must remain runtime on implementation failure. Extreme
unsigned carriers, wider kinds, BOZ padding/sign interpretation and any desired
diagnostic policy remain separate explicit gates.

**Dependencies:** 7.6.1p3/p7/NOTE1/NOTE6;C7113;16.9.96 HUGE,16.9.118 KIND,16.9.110 INT
and16.9.1p2;4.1.2p3;4.2.

<!-- END GENERATED 7.6.1 -->

## Shared runtime cases

### `S7_6_1_001_valid__enum_value_common_kind`

Primary `S7.6.1-001`; run / effect; facets `within-definition-common-kind`.

* `common`: literal values `[0, 1, 4, 5, 9, 10]`; statement partition `[3, 3]`; KIND comparisons are only to `common_zero`.

### `S7_6_1_004_valid__enum_value_sequence_matrix`

Primary `S7.6.1-004`; run / effect; facets `first-implicit-zero`, `explicit-values`, `successor-after-explicit`, `across-statement-boundary`, `negative-and-repeated-values`, `new-definition-reset`.

* `flat`: literal values `[0, 1, 4, 5, 9, 10, 4, 5]`; statement partition `[8]`; KIND comparisons are only to `flat_zero`.
* `split`: literal values `[0, 1, 4, 5, 9, 10, 4, 5]`; statement partition `[3, 5]`; KIND comparisons are only to `split_zero`.
* `negative`: literal values `[-3, -2, 4, 4, 5]`; statement partition `[2, 3]`; KIND comparisons are only to `negative_start`.
* `fresh`: literal values `[0, 1]`; statement partition `[2]`; KIND comparisons are only to `fresh_zero`.

## Complete finite pending plans

Every unselected original plan is retained. Incidental checks do not create source-use/link or extra requirement credit.

### Pending R759

* **`single-enumerator-statement`** - PENDING - Compile admission: one unnamed ENUM,BIND(C) with one colon-free ENUMERATOR naming a first constant and a complete END ENUM; enclosing program is complete.
* **`multiple-enumerator-statements`** - PENDING - Compile admission: the same finite two-name list split over two ENUMERATOR statements. Keep the name/value order fixed; p6 owns resulting values.
* **`nonempty-definition`** - PENDING - Diagnostic/control: a complete empty ENUM,BIND(C)...END ENUM block in a complete module; repair by inserting one ENUMERATOR first statement. Require the missing enumerator statement/list condition, not a later module/EOF complaint.
* **`enclosing-and-closing-source-use`** - PENDING - Source-use graph: R760/R761/R763, R401 and R508 retain their defining owners. Any omitted closing statement candidate must retain enclosing END MODULE/END PROGRAM and repair only the enum terminator; generic recovery cannot qualify it.

### Pending R760

* **`unnamed-bind-c`** - PENDING - Compile admission: complete unnamed ENUM,BIND(C) with small constant enumerators, without inventing a type name.
* **`named-bind-c`** - PENDING - Compile admission: add :: tone to a complete ENUM,BIND(C) definition and use TYPE(tone) only after END ENUM. Preserve R703/C7112 context.
* **`required-bind-c`** - PENDING - Diagnostic/control: complete unnamed ENUM header missing ,BIND(C); repair by inserting exactly that header suffix, with enumerator list and terminator unchanged.
* **`required-comma`** - PENDING - Diagnostic/control: ENUM BIND(C) in an otherwise complete unnamed definition; insert only the comma after ENUM. Require the enum-header comma cause.
* **`c-language-designator`** - PENDING - Diagnostic/control: replace only C in BIND(C) by another identifier in an otherwise valid header; restore C. This does not introduce a separate alternative companion-language binding syntax.
* **`named-header-separator`** - PENDING - Diagnostic/control: ENUM,BIND(C) tone without ::; insert only :: before the same type name. Any missing-name variant instead retains :: and inserts the sole name, without dependent uses of an undefined name.

### Pending R761

* **`colon-and-colon-free-admissions`** - PENDING - Compile admissions: two otherwise equal unnamed enum definitions, one using ENUMERATOR :: first,second and the other ENUMERATOR first,second, with no explicit initializer in either list.
* **`list-separator`** - PENDING - Diagnostic/control: two plain names with a missing comma, no initializer and a valid surrounding definition; insert only the comma.
* **`nonempty-list`** - PENDING - Diagnostic/control: retain ENUMERATOR :: but omit the list; insert one new constant name. The repaired list is nonempty without introducing an initializer.
* **`initializer-separator-source-use`** - PENDING - Source-use graph: C7111 owns the equals-triggered :: condition, not a new R761 duplicate for the same negative. R401 owns the general nonempty comma-list convention.

### Pending R762

* **`implicit-and-explicit-initializers`** - PENDING - Compile admissions: one enum constant without = and another initialized by a small scalar INTEGER literal, using :: so C7111 is independently satisfied.
* **`prior-integer-constant-expression`** - PENDING - Compile admission: a previously defined INTEGER PARAMETER seed=4, then ENUMERATOR :: first=seed+1. A preceding enumerator in the same list may be used only after satisfying8.5.13p2.
* **`scalar-initializer`** - PENDING - Diagnostic/control: ENUMERATOR :: first=[4]; replace the single-element array constructor by scalar4. INTEGER type and constant status already hold.
* **`integer-initializer`** - PENDING - Diagnostic/control: ENUMERATOR :: first=4.0; change only the scalar constant to4. Avoid a named enum type or unrelated unsupported facility in this basic initializer test.
* **`constant-initializer`** - PENDING - Diagnostic/control: a previously declared and explicitly initialized INTEGER variable seed=4 is used as the initializer; add only PARAMETER to its declaration. Keep seed's type/value and the enum statement unchanged; require nonconstant-enumerator-initializer reporting.
* **`constant-and-name-consumer-graph`** - PENDING - Source-use graph: R1032/C1014, C401,8.5.13 and19.3.1 own their general conditions. Do not use an undefined or forward-referenced seed, duplicate name, unsupported kind selector or overflow to stand in for lack of constancy.

### Pending R763

* **`closing-statement-admission`** - PENDING - Compile admission: named and unnamed definitions both close with END ENUM, not END ENUM name.
* **`trailing-name`** - PENDING - Diagnostic/control: append the defining name to an otherwise complete named enum's END ENUM; remove only that trailing name. Do not remove the whole required terminator.
* **`source-spelling-reuse`** - PENDING - Source-use: preserve S6_3_2_2_005_valid__enum at tests/clause06/S6_3_2_2_005_valid__enum.f90. It already uses both ENDENUM and END ENUM and checks first1/second2. Its Clause6 owner and role remain; no copied local spelling program.

### Pending C7111

* **`first-initializer`** - PENDING - Diagnostic/control: ENUMERATOR first=4 in an otherwise valid unnamed enum; insert only :: before first.
* **`later-list-initializer`** - PENDING - Diagnostic/control: ENUMERATOR first,second=4; insert only :: before the entire list. Do not alter the second initializer or infer that only the first item can trigger the constraint.
* **`later-statement-initializer`** - PENDING - Diagnostic/control: an earlier colon-free uninitialized ENUMERATOR first remains legal; a later ENUMERATOR second=4 lacks ::. Repair only the later statement.
* **`no-initializer-controls`** - PENDING - Compile admissions: colon-free and colon-present lists with no =, including a colon-free statement after an initialized statement. These controls isolate the antecedent, not enum value semantics.

### Pending R764

* **`named-specifier-admission`** - PENDING - Compile admission/source-use: a previously defined named enum used as TYPE(tone). Prefer existing R703_valid__type_enum and R702_valid__enum where they exactly supply the consumer; do not rename or duplicate them.
* **`identity-and-specifier-consumers`** - PENDING - Source-use graph: same-definition references and USE renames retain the actual enum type identity. Different values/names or C representation equality do not by themselves establish type identity. TYPEOF/CLASSOF, component, allocation and function-prefix consumers retain7.3/C749 ownership; no parameterized or CLASS(type-name) case is inferred by analogy.

### Pending C7112

* **`previous-definition-admissions`** - PENDING - Compile admissions/source-use: local prior definition and a USE-renamed prior definition, with TYPE declarations after the complete definition. Preserve S7_3_2_2_001_valid__previous_enum rather than manufacture another primary owner for it.
* **`ordinary-forward-type-declaration`** - PENDING - Diagnostic/control: in an ordinary module specification part, put an uninitialized TYPE(tone) variable declaration before the complete later named enum definition. Repair by moving only that declaration after END ENUM. Do not add an initializer that also references an undefined enumerator.
* **`function-result-as-if-exception`** - PENDING - Source-use: preserve S7_3_2_2_002_valid__body_enum, whose FUNCTION prefix names the enum defined in its body and whose integer consumer observes7. Retain the explicit7.3.2.2p2 exception; do not make this valid body-local result a forward-use negative.
* **`name-category-and-identity-graph`** - PENDING - Source-use graph: a bare TYPE(ordinary_derived_type) may be legal through R703 and is not an isolated wrong-enum-name case. Qualify the actual enum-specifier context before any wrong-category candidate; name-scope misuse retains4.2p2(6)/Clause19 ownership.

### Pending R765

* **`constant-and-dynamic-integer-admissions`** - PENDING - Compile admissions: a prior named enum with values0/4/9, and constructors from scalar literal4 and a defined scalar INTEGER variable with value4. Do not impose a constant-expression condition on a runtime constructor.
* **`scalar-argument`** - PENDING - Diagnostic/control: tone([4]) in an otherwise valid scalar constructor use; change only [4] to4. C7113's INTEGER condition already holds.
* **`single-argument`** - PENDING - Diagnostic/control: tone(4,9), with both values valid separately and no same-name procedure; remove only the second argument and comma. A missing-argument candidate inserts scalar4.
* **`constructor-consumer-and-boz-graph`** - PENDING - Source-use graph: R764/C7112/C7113, constant-expression use and assignment conversion retain their owners. Do not count an old constructor control as a new program; BOZ lexical/bit semantics remain a separate7.7 gate.

### Pending C7113

* **`integer-admissions`** - PENDING - Compile admissions: scalar literal4, a defined INTEGER variable4, and a same-definition INTEGER enumerator whose value4 is known representable. Derive any carrier KIND from the enumerator rather than a numeric code.
* **`real-operand`** - PENDING - Diagnostic/control: tone(4.0) for a prior named enum with a representable4; replace only4.0 by4. Require the constructor operand's disallowed REAL type.
* **`logical-operand`** - PENDING - Diagnostic/control: tone(.true.); replace only the LOGICAL operand by representable1 in a type whose enumerators include1.
* **`character-operand`** - PENDING - Diagnostic/control: tone('1'); replace only the CHARACTER operand by representable1. Do not accept an unrelated lexical or delimiter complaint.
* **`boz-admission-qualification`** - PENDING - Source-use gate: preserve the explicit BOZ alternative, but do not author it before independent7.7/bit-model qualification. Start with a lexically valid zero/one representation only after proving its length, padding, sign and enum representability premises. No arbitrary all-ones or oversized-pattern result is promised.

### Pending S7.6.1-001

* **`corresponding-c-values-and-order`** - PENDING - Qualified interoperability plan: record the selected companion and exact C2018 enum specifier with the same ordered value list. A complete Fortran interface, actual reference, C prototype and C function may check common nonnegative values. Use the actual enum-derived carrier kind or named enum type, never assumed C_INT equality.
* **`unsigned-and-kind-selection-documentation`** - PENDING - Documentary/profile gate: identify companion enum representation options and the unsigned/signed value-domain issue in NOTE1. Match values/order, not names or minimum-kind guesses. Extreme unsigned-value, unsupported carrier and additional-kind candidates remain unimplemented until the complete cross-language premises are established.

### Pending S7.6.1-002

* **`integer-value-roundtrip`** - PENDING - Runtime effect: define a named enum with known values0/4/9, construct4 and consume it through INT with KIND=KIND(the_INTEGER_enumerator). Independently compare with4 of that carrier kind; retain the constructor's actual enum type in an explicit scalar dummy context.
* **`values-not-limited-to-enumerators`** - PENDING - Runtime effect: the same safe named definition has no enumerator for6, but6 lies between its known representable0 and9. Construct6 and check its corresponding integer6. Do not substitute ordinal position or restrict the enum domain to three labels.
* **`constant-and-dynamic-constructors`** - PENDING - Runtime effect: use a literal4 and a separately defined INTEGER variable6 as constructor inputs, then compare exact corresponding integers. Do not require runtime operands to be constant or let a same-name user procedure supply the result.
* **`scalar-result-and-same-type-context`** - PENDING - Runtime effect/source-use: pass a constructor to an explicit scalar TYPE(the_same_enum) dummy that consumes its value. A RANK inquiry of an independently declared scalar, an empty program or a type name alone is not a result-value witness.
* **`internal-representation-qualification-graph`** - PENDING - Documentary/source-use gate: establish the complete enum-to-integer internal representation and storage/defined-bit premises before a direct representation witness. INT equality is value evidence only; C round trips, equal SIZEOF/STORAGE_SIZE or unqualified TRANSFER/memcmp are not interchangeable proofs. Keep this facet pending if only value evidence exists.
* **`canonical-conversion-reuse`** - PENDING - Source-use: preserve S10_2_1_3_022_valid, R703_valid__type_enum, R702_valid__enum and S7_3_2_2_001_valid__previous_enum. Their constructor/assignment contexts stay at their existing owners; add reviewed finite reuse only where the exact claimed value relation matches, never renamed copies.

### Pending S7.6.1-003

* **`same-values-order-policy`** - PENDING - Documentary/profile plan: identify the actual selected companion mechanism, its options and documented representation-selection policy for two C enum specifiers with ordered values0/4/9. Check the universal policy premise rather than extrapolating from one build.
* **`different-names-same-representation`** - PENDING - Qualified positive-control plan: retain the same ordered values while changing all C enumerator names in a second independently valid C definition. Record complete types and prototypes; do not pass incompatible C objects through an unrelated prototype or claim type compatibility from equal widths.
* **`representation-and-interface-use-graph`** - PENDING - Source-use graph: pair the companion policy evidence with18.3.1/.3/.5/.7 and the Fortran value witnesses, if independently qualified. A numeric round trip, successful compilation or sizeof equality alone does not discharge this universal representation restriction; no no-op case or negative diagnostic policy is planned.

### Pending S7.6.1-004

* **`named-constant-and-existing-context-graph`** - PENDING - Source-use:8.5.13 owns PARAMETER immutability/prior-definition conditions; R762/C1014 own constant-expression premises. Existing S6_3_2_2_005_valid__enum checks explicit1/2 only and is not full default/successor coverage.

### Pending S7.6.1-005

* **`known-representable-controls`** - PENDING - Runtime positive controls: construct known0/4/9 and, after the carrier is established, the guaranteed model bounds HUGE(an_INTEGER_enumerator) and its negation. Consume with INT using the carrier's actual KIND; never apply HUGE or KIND to an enum-type object.
* **`out-of-range-policy-gate`** - PENDING - No negative execution is authorized. A future static out-of-range diagnostic policy requires separate review, a truly supported wider INTEGER kind and representable wide arithmetic if used, plus a minimally repaired in-range control. HUGE(e)+1 in e's own kind is not a valid way to isolate this condition.
* **`boz-and-example-range-qualification`** - PENDING - Source-use gate: qualify BOZ representation/range only after the separate7.7 source gate. NOTE6's12345 is conditional on actual representability; its1/2/3 enumerators do not supply that guarantee. Do not downgrade an unqualified candidate into a passing compile-only or skip case.

## Reproduction and separate gates

`python3 -B tools/generate_enum_value_fixtures.py --check` verifies exact bytes, the seven/57 partition and both generated view regions. Source/observer models, current processor reports and sensitivity records are bound in `enum-values-handoff.json`. Independent source/oracle/provenance decisions and explicit integration renewals are recorded in `doc/source_audits/batch_022.json`. Both cases have qualifying GNU f2023 runtime evidence; actual Flang f2018 remains supplementary. These finite value and within-definition KIND effects do not qualify any companion/representation or pending facet.
