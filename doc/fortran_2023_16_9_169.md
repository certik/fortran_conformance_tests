# Fortran 2023 16.9.169: RANDOM_SEED

The canonical catalogue is `doc/catalogues/random_seed_16_9_169.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.169")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.169 -->

### S16.9.169-001: RANDOM_SEED argument restrictions

**Source:** S16.9.169, J3/24-007, 18 December 2023, physical PDF448. **Class:** Restriction.

**Definition:** There shall be exactly one or no arguments; SIZE is default integer scalar INTENT(OUT),
PUT is default integer rank-one size at least N INTENT(IN), and GET is default integer
rank-one size at least N INTENT(OUT).

**Diagnostic obligation:** not-required.

**Facets:** `RANDOM_SEED-zero-or-one-argument`, `RANDOM_SEED-SIZE-default-integer-scalar`, `RANDOM_SEED-PUT-default-integer-rank-one-size`, `RANDOM_SEED-GET-default-integer-rank-one-size`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
logical, integer, kind, character, shape, rank, interval, sequence-equality,
seed-consistency, or bit-pattern observations. Processor-dependent approximations,
random values, distribution quality, and image-dependent behavior are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/*.py and tests/fixtures/*/fixture.json for PRODUCT, RADIX, RANDOM_INIT,
RANDOM_NUMBER, RANDOM_SEED, RANGE, RANK, and S16.9.165 through S16.9.171 found no
existing fixture binding in this worktree; incidental profile/helper mentions are not
existing 16.9 fixture coverage. Image-local seed effects are out of scope for this
single-image suite by user decision.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 16.3 integer bit model, 16.4
numeric model terminology, sibling 16.9 intrinsics used by pending plans such as HUGE,
TINY, LOG10, INT, REAL, RANDOM_NUMBER, RANDOM_SEED, PRODUCT, KIND, SIZE, SHAPE, and
SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, image, or scope terms.

### S16.9.169-002: RANDOM_SEED SIZE reports seed vector extent

**Source:** S16.9.169, J3/24-007, 18 December 2023, physical PDF448. **Class:** Effect.

**Definition:** SIZE is assigned the number N of integers used by the processor to hold the seed value.

**Diagnostic obligation:** not-required.

**Facets:** `RANDOM_SEED-SIZE-assigns-N-for-seed-vector`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
logical, integer, kind, character, shape, rank, interval, sequence-equality,
seed-consistency, or bit-pattern observations. Processor-dependent approximations,
random values, distribution quality, and image-dependent behavior are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/*.py and tests/fixtures/*/fixture.json for PRODUCT, RADIX, RANDOM_INIT,
RANDOM_NUMBER, RANDOM_SEED, RANGE, RANK, and S16.9.165 through S16.9.171 found no
existing fixture binding in this worktree; incidental profile/helper mentions are not
existing 16.9 fixture coverage. Image-local seed effects are out of scope for this
single-image suite by user decision.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 16.3 integer bit model, 16.4
numeric model terminology, sibling 16.9 intrinsics used by pending plans such as HUGE,
TINY, LOG10, INT, REAL, RANDOM_NUMBER, RANDOM_SEED, PRODUCT, KIND, SIZE, SHAPE, and
SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, image, or scope terms.

### S16.9.169-003: RANDOM_SEED PUT and GET access seed state

**Source:** S16.9.169, J3/24-007, 18 December 2023, physical PDF448. **Class:** Effect.

**Definition:** PUT is used in a processor-dependent manner to compute the seed, GET is assigned the
seed value, and RANDOM_NUMBER maintains a seed on each image that is updated and can be
retrieved or changed.

**Diagnostic obligation:** not-required.

**Facets:** `RANDOM_SEED-PUT-computes-single-image-seed`, `RANDOM_SEED-GET-retrieves-single-image-seed`, `RANDOM_SEED-RANDOM_NUMBER-updates-seed`, `RANDOM_SEED-per-image-seed-out-of-scope`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
logical, integer, kind, character, shape, rank, interval, sequence-equality,
seed-consistency, or bit-pattern observations. Processor-dependent approximations,
random values, distribution quality, and image-dependent behavior are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/*.py and tests/fixtures/*/fixture.json for PRODUCT, RADIX, RANDOM_INIT,
RANDOM_NUMBER, RANDOM_SEED, RANGE, RANK, and S16.9.165 through S16.9.171 found no
existing fixture binding in this worktree; incidental profile/helper mentions are not
existing 16.9 fixture coverage. Image-local seed effects are out of scope for this
single-image suite by user decision.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 16.3 integer bit model, 16.4
numeric model terminology, sibling 16.9 intrinsics used by pending plans such as HUGE,
TINY, LOG10, INT, REAL, RANDOM_NUMBER, RANDOM_SEED, PRODUCT, KIND, SIZE, SHAPE, and
SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, image, or scope terms.

### S16.9.169-004: RANDOM_SEED seed computation latitude

**Source:** S16.9.169, J3/24-007, 18 December 2023, physical PDF448. **Class:** Effect.

**Definition:** With no argument, the processor assigns a processor-dependent seed; PUT computation is
processor dependent, and GET need not equal an immediately preceding PUT.

**Diagnostic obligation:** not-required.

**Facets:** `RANDOM_SEED-no-argument-seed-latitude`, `RANDOM_SEED-PUT-computation-latitude`, `RANDOM_SEED-GET-need-not-equal-PUT-permission`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
logical, integer, kind, character, shape, rank, interval, sequence-equality,
seed-consistency, or bit-pattern observations. Processor-dependent approximations,
random values, distribution quality, and image-dependent behavior are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/*.py and tests/fixtures/*/fixture.json for PRODUCT, RADIX, RANDOM_INIT,
RANDOM_NUMBER, RANDOM_SEED, RANGE, RANK, and S16.9.165 through S16.9.171 found no
existing fixture binding in this worktree; incidental profile/helper mentions are not
existing 16.9 fixture coverage. Image-local seed effects are out of scope for this
single-image suite by user decision.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 16.3 integer bit model, 16.4
numeric model terminology, sibling 16.9 intrinsics used by pending plans such as HUGE,
TINY, LOG10, INT, REAL, RANDOM_NUMBER, RANDOM_SEED, PRODUCT, KIND, SIZE, SHAPE, and
SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, image, or scope terms.

### S16.9.169-005: RANDOM_SEED GET value as PUT reproduces sequence

**Source:** S16.9.169, J3/24-007, 18 December 2023, physical PDF448. **Class:** Effect.

**Definition:** After PUT=SEED1 followed by GET=SEED2, using either value as a later PUT shall result in
the same pseudorandom sequence.

**Diagnostic obligation:** not-required.

**Facets:** `RANDOM_SEED-GET-as-PUT-reproduces-sequence`, `RANDOM_SEED-PUT-as-PUT-reproduces-sequence`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
logical, integer, kind, character, shape, rank, interval, sequence-equality,
seed-consistency, or bit-pattern observations. Processor-dependent approximations,
random values, distribution quality, and image-dependent behavior are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/*.py and tests/fixtures/*/fixture.json for PRODUCT, RADIX, RANDOM_INIT,
RANDOM_NUMBER, RANDOM_SEED, RANGE, RANK, and S16.9.165 through S16.9.171 found no
existing fixture binding in this worktree; incidental profile/helper mentions are not
existing 16.9 fixture coverage. Image-local seed effects are out of scope for this
single-image suite by user decision.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 16.3 integer bit model, 16.4
numeric model terminology, sibling 16.9 intrinsics used by pending plans such as HUGE,
TINY, LOG10, INT, REAL, RANDOM_NUMBER, RANDOM_SEED, PRODUCT, KIND, SIZE, SHAPE, and
SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, image, or scope terms.

### S16.9.169-006: RANDOM_SEED affects only invoking image

**Source:** S16.9.169, J3/24-007, 18 December 2023, physical PDF448. **Class:** Effect.

**Definition:** RANDOM_INIT, RANDOM_NUMBER, and RANDOM_SEED only affect the seed value on the invoking
image.

**Diagnostic obligation:** not-required.

**Facets:** `RANDOM_SEED-invoking-image-only-out-of-scope`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
logical, integer, kind, character, shape, rank, interval, sequence-equality,
seed-consistency, or bit-pattern observations. Processor-dependent approximations,
random values, distribution quality, and image-dependent behavior are separated from
exact result obligations. Unnumbered argument shall-restrictions are source-control
restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/*.py and tests/fixtures/*/fixture.json for PRODUCT, RADIX, RANDOM_INIT,
RANDOM_NUMBER, RANDOM_SEED, RANGE, RANK, and S16.9.165 through S16.9.171 found no
existing fixture binding in this worktree; incidental profile/helper mentions are not
existing 16.9 fixture coverage. Image-local seed effects are out of scope for this
single-image suite by user decision.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 16.3 integer bit model, 16.4
numeric model terminology, sibling 16.9 intrinsics used by pending plans such as HUGE,
TINY, LOG10, INT, REAL, RANDOM_NUMBER, RANDOM_SEED, PRODUCT, KIND, SIZE, SHAPE, and
SELECTED_*_KIND, and clauses 14 through 19 where future fixtures need program,
procedure, IEEE, image, or scope terms.

<!-- END GENERATED 16.9.169 -->
