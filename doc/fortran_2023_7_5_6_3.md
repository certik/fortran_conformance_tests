# Fortran 2023: 7.5.6.3 When finalization occurs

**Catalogue source review: reviewed.** Case and evidence adjudications are separate content-bound records.

The bounded corpus has **18 run-phase effect programs**, **44 finite checkpoint sites**, and **19 represented of 33 facets; 14 remain pending**. The old-value and RHS-order facets share one program, not two copied inputs.

## Source and bounded ownership

Authority: J3/24-007, **18 December 2023**, **688 physical PDF pages**, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
All nine original 7.5.6.3 base units are on PDF103. The preserved source inventory
has36 subdivisions and45 accounting rows; its seven requirement definitions and
all33 facet identities are unchanged.

The independent gate is `finalization-source-review.json`, SHA-256
`6280f2250fbf5464c58144a0207a56a51c88fd4771830bf04942906c2ec092ea`.
FSRC-001 was corrected in `57ba04c24354a1ef30ef41cb19bf890ac2ba18c5`; the
correction receipt SHA-256 is
`bc1b70834d015af14d9cf7a2fbe5f70229ee9a85109fb85b325eb3dd5aed490d`.
Actual elemental conditions are15.9.1/.3 on PDF362-363, not15.8 Simple procedures.
All FQ01-08 qualifications remain binding. Original C791-C794, constructor and
intrinsic assignment, allocation/deallocation, SAVE/BLOCK, executable-construct,
purity/elemental, argument-association and definition/undefinition dependencies
were read before these ordinary plans were elaborated.

## Concrete oracle premises

* **Assignment:** old primitive fields are written individually. The shared
  old-value/RHS-order program uses an INTEGER-returning helper inside a genuine
  constructor, never a same-name generic or finalizable RHS function. The old
  final observes7 after RHS evaluation, before new11. Initially unallocated
  assignment is checked before separate positive cleanup; its callback does not
  read nonexistent old payload. Different-extent array reallocation and a
  containing final's still-allocated INTEGER child have explicit state/value
  guards. No unspecified compiler temporary receives an expected callback.
* **Canonical reuse:** `S10_2_1_3_029_valid__finalization` at
  `tests/clause10/S10_2_1_3_029_valid__finalization.f90` counts a replaced
  finalizable allocatable component. It does not observe the new planned own
  containing callback before child release. That case and its ownership stay
  untouched. Deallocation process witnesses are not copied into event-owned
  cases. The associated source/use graphs remain pending without new links.
* **Locals and BLOCK:** all event locals are unsaved, nonpointer, nonallocatable,
  nondummy and nonresult. No declaration initializer accidentally supplies SAVE.
  Multiple locals use a multiset, not declaration order. Nested BLOCK timing is
  checked through separate logs after inner END BLOCK, never through a dead
  object. Explicit saved controls have a live positive unsaved peer.
* **Function results:** ordinary IMPURE module factories define primitive result
  fields. An INTEGER getter really consumes the value and rejects finalization
  at function END. Block IF and counted DO header references are not body
  statement references. Inside/outside checkpoints and nonzero branch/iteration
  checks distinguish the correct whole-construct boundary; the DO callback also
  records the live final index4. Pointer results keep a named saved TARGET
  reachable and use a later legal assignment, never invalid deallocation, as
  their positive observer.
* **INTENT OUT:** callbacks see only their own old defined fields. Ordinary entry
  checks the completed old-token/defaulted-field log before reading the valid
  reset default and defining the new fields. Logging elemental callees/finals
  are explicitly IMPURE ELEMENTAL with C15118/C15120 scalar intents. A legal
  scalar-elemental/rank-one final family tests scalar entry context; disjoint
  INTEGER markers let each body check its own preceding callback without
  reading undefined OUT payload or imposing an inter-element order. An
  array-only type has no hidden parent/component final path and needs zero entry
  callbacks before its later positive rank-one deallocation observer.
* **Selection and state:** all final dummies are nonpolymorphic TYPE, exactly one
  nonoptional/nonpointer/nonallocatable/noncoarray argument, never OUT or VALUE.
  Types are ordinary, concrete and unparameterized: there are no intrinsic
  representation-selector assumptions or unassumed LEN parameters. Explicit
  allocations/deallocations check STAT and allocation state. Automatic assignment
  allocation has no STAT syntax; new state is checked before payload inquiries.
* **Observer limits:** reused primitive log guards enforce exact membership,
  multiplicity and only required before-edges. No finalizer reads an already
  finalized peer or a dead alias. A guard ERROR STOP marks an ordinary failed
  self-check; process-status, PURE-expression and termination effects are not
  tested or inferred. Models and deliberately wrong-expectation controls test
  the emitted oracle, not compiler consensus or extra language-invalid policies.

## Evidence and integration boundaries

Every owned case is a standard-based runtime effect with no diagnostic policy,
optional-success fallback, coarray execution claim, skip or termination profile.
Source-valid implementation failures remain run-phase failures. Original target
and reference reports keep their actual standards, inputs, fingerprints, commands
and identities; an exact current-row selection is not an invented combined run.
Frozen LF411 a0afaa840b, GNU f2023 and actual Flang f2018 observations do not
themselves grant source/case approval.

All six p2 deallocation facets and all three p6 specification-expression facets
remain pending, as do one canonical graph in each represented family. There are
no new deallocation-owned duplicates, PURE/ERROR STOP candidates, unreachable-
pointer permissions or7.5.6.4 termination observers. Only this catalogue/view,
the event generator/regression and `tests/fixtures/final_event_*` are owned.
The local index overlay is validation-only, excluded from the author commit.

## Definitions

<!-- BEGIN GENERATED 7.5.6.3 -->

### S7.5.6.3-001: Intrinsic assignment finalizes the old variable between RHS evaluation and definition

**Source:** 7.5.6.3, p1, p1.intrinsic-assignment-event, p1.unallocated-allocatable-exclusion, p1.after-expression-evaluation, p1.before-variable-definition, p1.allocated-variable-before-deallocation, p1.allocated-subobject-before-deallocation; J3/24-007, 18 December 2023, PDF 103. **Class:** Effect.

**Definition:** Intrinsic assignment finalizes its variable unless that variable is an unallocated
allocatable variable. Finalization follows RHS evaluation and precedes definition of the
left side. If assignment would deallocate an allocated variable or allocated allocatable
subobject, finalization precedes that deallocation.

**Diagnostic obligation:** not-required.

**Facets:** `old-value-before-definition`, `rhs-evaluation-before-final`, `unallocated-left-exclusion`, `allocated-left-reallocation`, `allocated-subobject-before-release`, `assignment-and-deallocation-use-graph`.

**Oracle:** Primitive member assignments establish old values without unintended setup finalization.
Log RHS timing using only a nonfinalizable INTEGER result. Use separate ordinary saved
integer logs/counters, not reads of other objects already finalized. Initialize payloads
through primitive named components rather than unintended whole-object assignments that
themselves finalize the old left side. Keep every callback argument and accessed
component defined. Pointer association and allocation inquiries require independently
established valid states. Counter/order observers are IMPURE where side effects require
it; do not silently place an impure finalizer behind a PURE function or procedure.

Finite ordinary-event implementation: 4 run-phase programs represent 5 direct facets
using qualified events and before/inside/after checkpoints. Exact saved INTEGER log
multisets, required partial-order edges and live primitive value/state checks
distinguish missing, early, late, duplicate and vacuous events. No extra order of event
peers is imposed. 1 source/use graph facet remains pending. Source/case/evidence
adjudications are separate content-bound records; generation and processor observations
do not confer approval.

**Oracle limitation:** Do not confuse constructor expressions with generic function references or count
unspecified compiler temporaries as language objects. Error-condition deallocation
choices are separate.

**Dependencies:** 10.2.1.3;7.5.10/C7108;9.7.3.2 p7-p9, PDF165-166;7.5.6.2;19.6.

### S7.5.6.3-002: Successful deallocation finalizes pointer targets and qualifying allocatable entities

**Source:** 7.5.6.3, p2.pointer-target-on-deallocation, p2.allocatable-on-deallocation, p2.intrinsic-assignment-variable-exception; J3/24-007, 18 December 2023, PDF 103. **Class:** Effect.

**Definition:** Deallocating a pointer finalizes its target. Deallocating an allocatable entity
finalizes it except when it is the variable in intrinsic assignment, whose finalization
is governed by p1. Pointer association, whole-allocation and
argument/construct-association restrictions remain necessary.

**Diagnostic obligation:** not-required.

**Facets:** `allocated-pointer-target`, `allocated-allocatable-entity`, `automatic-allocatable-scope-exit`, `allocatable-subobject-cleanup`, `assignment-no-double-finalization`, `deallocation-event-use-graph`.

**Oracle:** Use bounded successful allocations and initialized named payloads, with STAT checked
before any subsequent state/value query. Use separate ordinary saved integer
logs/counters, not reads of other objects already finalized. Initialize payloads through
primitive named components rather than unintended whole-object assignments that
themselves finalize the old left side. Keep every callback argument and accessed
component defined. Pointer association and allocation inquiries require independently
established valid states. Counter/order observers are IMPURE where side effects require
it; do not silently place an impure finalizer behind a PURE function or procedure.

**Oracle limitation:** Never deallocate through a pointer to an allocatable object, a partial target or an
undefined association. Other aliases become undefined after deallocation and are not
queried. Error cases are not mandatory callback oracles.

**Dependencies:** 9.7.1.4;9.7.3.2-.3, PDF164-166;7.5.6.3 p1;19.5.2.

### S7.5.6.3-003: Qualifying procedure locals finalize before RETURN or END makes them undefined

**Source:** 7.5.6.3, p3, p3.nonpointer-nonallocatable-object, p3.not-dummy-or-result, p3.return-or-end-event, p3.before-would-become-undefined; J3/24-007, 18 December 2023, PDF 103. **Class:** Effect.

**Definition:** A nonpointer nonallocatable object that is neither a dummy argument nor a function
result is finalized immediately before it would become undefined because a RETURN or END
statement is executed. A saved object does not satisfy that would-become-undefined
condition.

**Diagnostic obligation:** not-required.

**Facets:** `explicit-return-local`, `end-subprogram-local`, `multiple-locals-unordered`, `saved-local-exclusion-control`, `dummy-result-and-auto-deallocation-graph`.

**Oracle:** The caller checks only nonfinalizable saved logs, never returned pointers to dead
locals. Use separate ordinary saved integer logs/counters, not reads of other objects
already finalized. Initialize payloads through primitive named components rather than
unintended whole-object assignments that themselves finalize the old left side. Keep
every callback argument and accessed component defined. Pointer association and
allocation inquiries require independently established valid states. Counter/order
observers are IMPURE where side effects require it; do not silently place an impure
finalizer behind a PURE function or procedure.

Finite ordinary-event implementation: 4 run-phase programs represent 4 direct facets
using qualified events and before/inside/after checkpoints. Exact saved INTEGER log
multisets, required partial-order edges and live primitive value/state checks
distinguish missing, early, late, duplicate and vacuous events. No extra order of event
peers is imposed. 1 source/use graph facet remains pending. Source/case/evidence
adjudications are separate content-bound records; generation and processor observations
do not confer approval.

**Oracle limitation:** The END of a main program is image termination, not this event. Default component
initialization alone does not imply SAVE; object declaration initialization can, so do
not accidentally save the intended local.

**Dependencies:** 19.6.6(3), PDF565;8.5.16;7.5.4.6;9.7.3.2p2;7.5.6.4.

### S7.5.6.3-004: Qualifying BLOCK locals finalize before becoming undefined at block termination

**Source:** 7.5.6.3, p4, p4.block-local-object, p4.nonpointer-nonallocatable, p4.block-termination, p4.before-would-become-undefined; J3/24-007, 18 December 2023, PDF 103. **Class:** Effect.

**Definition:** A nonpointer nonallocatable BLOCK-local object is finalized immediately before BLOCK
termination would make it undefined. The condition is distinct from automatic
deallocation of allocatable locals and from saved objects whose definition status
persists.

**Diagnostic obligation:** not-required.

**Facets:** `ordinary-end-block`, `nested-block-timing`, `saved-block-exclusion-control`, `block-exit-use-graph`.

**Oracle:** Check the actual nesting boundary and completed callback counts. Use separate ordinary
saved integer logs/counters, not reads of other objects already finalized. Initialize
payloads through primitive named components rather than unintended whole-object
assignments that themselves finalize the old left side. Keep every callback argument and
accessed component defined. Pointer association and allocation inquiries require
independently established valid states. Counter/order observers are IMPURE where side
effects require it; do not silently place an impure finalizer behind a PURE function or
procedure.

Finite ordinary-event implementation: 3 run-phase programs represent 3 direct facets
using qualified events and before/inside/after checkpoints. Exact saved INTEGER log
multisets, required partial-order edges and live primitive value/state checks
distinguish missing, early, late, duplicate and vacuous events. No extra order of event
peers is imposed. 1 source/use graph facet remains pending. Source/case/evidence
adjudications are separate content-bound records; generation and processor observations
do not confer approval.

**Oracle limitation:** No unspecified sibling-local total ordering or deallocated-alias inquiry is part of the
oracle. A saved local is not a direct END BLOCK finalization case.

**Dependencies:** 19.6.6(23), PDF566;8.5.16;11.1.4;9.7.3.2p3.

### S7.5.6.3-005: A nonpointer function result finalizes after its innermost executable construct

**Source:** 7.5.6.3, p5, p5.nonpointer-function-reference, p5.result-finalization, p5.after-innermost-executable-construct; J3/24-007, 18 December 2023, PDF 103. **Class:** Effect.

**Definition:** A nonpointer function result referenced by an executable construct is finalized after
execution of the innermost executable construct containing that reference. A function
result is not an ordinary local finalized merely by returning from its function. Pointer
function results and their targets must remain distinct.

**Diagnostic obligation:** not-required.

**Facets:** `statement-result-boundary`, `if-construct-boundary`, `do-control-boundary`, `pointer-result-exclusion-control`, `allocated-result-and-construct-use-graph`.

**Oracle:** Produce the result by primitive component definitions, not whole-result assignment that
invokes unrelated finalization. Use an ordinary INTEGER getter where direct
function-result component syntax is not permitted. Use separate ordinary saved integer
logs/counters, not reads of other objects already finalized. Initialize payloads through
primitive named components rather than unintended whole-object assignments that
themselves finalize the old left side. Keep every callback argument and accessed
component defined. Pointer association and allocation inquiries require independently
established valid states. Counter/order observers are IMPURE where side effects require
it; do not silently place an impure finalizer behind a PURE function or procedure.

Finite ordinary-event implementation: 4 run-phase programs represent 4 direct facets
using qualified events and before/inside/after checkpoints. Exact saved INTEGER log
multisets, required partial-order edges and live primitive value/state checks
distinguish missing, early, late, duplicate and vacuous events. No extra order of event
peers is imposed. 1 source/use graph facet remains pending. Source/case/evidence
adjudications are separate content-bound records; generation and processor observations
do not confer approval.

**Oracle limitation:** Avoid short-circuit or unevaluated inquiry contexts whose function result might not be
created. A structure constructor is not automatically a function reference, and dummy
arguments are not local-result cleanup objects.

**Dependencies:** 7.5.10/C7108;11.1.1 and executable-construct grammar;9.7.3.2p4;19.6.6(15);7.5.6.3p3.

### S7.5.6.3-006: Specification-expression function results finalize before execution begins

**Source:** 7.5.6.3, p6, p6.specification-expression-reference, p6.function-result, p6.before-executable-constructs; J3/24-007, 18 December 2023, PDF 103. **Class:** Effect.

**Definition:** A function result referenced in a specification expression of a scoping unit is
finalized before the executable constructs of that scoping unit execute. The expression
and specification-function purity/interface/order restrictions still apply; this is not
permission to call an impure logging factory or to hide an impure finalizer behind a
PURE function.

**Diagnostic obligation:** not-required.

**Facets:** `pure-final-before-body-candidate`, `specification-function-purity-graph`, `unevaluated-inquiry-and-result-state-graph`.

**Oracle:** This is a conditional source plan, not an approved executable. A before-body termination
witness must have an independently qualified external oracle and a body marker that
would reveal missed/delayed finalization. Do not use a module counter from a PURE final
subroutine.

**Oracle limitation:** C1593 explicitly forbids a PURE function result whose finalization would reference an
impure procedure. Any ERROR STOP profile is separate from Fortran finalization timing; a
nonzero process status is not universally prescribed and no runtime or optional-profile
skip is invented here.

**Dependencies:** 10.1.11, PDF185-187;15.7/C1593/C15105/C15109, PDF359-361;11.7.1, PDF230;11.4,
PDF228-229;9.7.3.2p5.

### S7.5.6.3-007: INTENT OUT finalization occurs in the invoked procedure before undefinition

**Source:** 7.5.6.3, p7, p7.invocation-event, p7.nonpointer-nonallocatable-intent-out, p7.before-undefined, p7.within-invoked-procedure, p7.elemental-scalar-context, p7.actual-rank-not-selector; J3/24-007, 18 December 2023, PDF 103. **Class:** Effect.

**Definition:** Invocation finalizes a nonpointer nonallocatable INTENT(OUT) dummy before it becomes
undefined. That finalization is considered to occur within the invoked procedure. For an
elemental procedure this means scalar or elemental final availability governs the
invocation regardless of actual-argument rank.

**Diagnostic obligation:** not-required.

**Facets:** `ordinary-intent-out-old-value`, `elemental-scalar-selection`, `elemental-array-only-no-entry-final`, `pure-and-allocatable-pointer-context-graph`.

**Oracle:** Initialize actual payloads by primitive member assignments and log old values only
during the valid callback. Check the body and caller phases separately rather than
allowing later cleanup to cancel an entry mistake. Use separate ordinary saved integer
logs/counters, not reads of other objects already finalized. Initialize payloads through
primitive named components rather than unintended whole-object assignments that
themselves finalize the old left side. Keep every callback argument and accessed
component defined. Pointer association and allocation inquiries require independently
established valid states. Counter/order observers are IMPURE where side effects require
it; do not silently place an impure finalizer behind a PURE function or procedure.

Finite ordinary-event implementation: 3 run-phase programs represent 3 direct facets
using qualified events and before/inside/after checkpoints. Exact saved INTEGER log
multisets, required partial-order edges and live primitive value/state checks
distinguish missing, early, late, duplicate and vacuous events. No extra order of event
peers is imposed. 1 source/use graph facet remains pending. Source/case/evidence
adjudications are separate content-bound records; generation and processor observations
do not confer approval.

**Oracle limitation:** A non-elemental array final is not a scalar final merely because the actual argument is
an array. PURE callees cannot conceal impure finalization.

**Dependencies:** C791-C794;7.5.6.2;19.6.6(15), PDF566;7.5.4.6;9.7.3.2p6;15.7/C1596 and15.9.1/15.9.3,
PDF362-363.

<!-- END GENERATED 7.5.6.3 -->

## Exact finite case observations

### `S7_5_6_3_001_valid__final_event_assignment_order`

**Primary:** S7.5.6.3-001; **facets:** `old-value-before-definition`, `rhs-evaluation-before-final`; **phase/evidence:** run / effect.

One shared witness uses a genuine item constructor, with no same-name generic. An INTEGER-only RHS helper logs evaluation. The sole old-left callback records7 after that marker; the new live left value is11. No setup record assignment, finalizable RHS function result, or compiler-temporary-count premise is present.

* `before-assignment`: primitive setup, before intrinsic assignment; multiset `[]`; before-edges `[]`; executions `1`.
* `after-assignment`: completed intrinsic assignment; multiset `[(5, 11), (10, 7)]`; before-edges `[((5, 11), (10, 7))]`; executions `1`.

### `S7_5_6_3_001_valid__final_event_unallocated_left`

**Primary:** S7.5.6.3-001; **facets:** `unallocated-left-exclusion`; **phase/evidence:** run / effect.

The exclusion is checked immediately after allocation by intrinsic assignment, before any later cleanup. A constant callback marker avoids reading nonexistent old payload even if an implementation incorrectly calls it for the unallocated LHS. The separate successful deallocation is a nonvacuous observer, not a new p2-owned case.

* `unallocated-before`: unallocated left side before assignment; multiset `[]`; before-edges `[]`; executions `1`.
* `unallocated-assigned`: assignment allocated and defined the left side; multiset `[]`; before-edges `[]`; executions `1`.
* `unallocated-cleanup`: separate later checked DEALLOCATE(left); multiset `[(10, 1)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_001_valid__final_event_allocated_reallocation`

**Primary:** S7.5.6.3-001; **facets:** `allocated-left-reallocation`; **phase/evidence:** run / effect.

The unparameterized nonpolymorphic type is identical on both sides; rank is one, old extent2 differs from constructor-array extent3. Checked allocation precedes primitive initialization. The matching ordinary array final records its own old extent/tokens before release; the new allocation is guarded before value inquiries. The assertion before later cleanup prevents a missed assignment event being hidden.

* `reallocation-before`: defined allocated old rank-one extent2; multiset `[]`; before-edges `[]`; executions `1`.
* `reallocation-assigned`: different-extent intrinsic assignment; multiset `[(10, 2), (11, 7), (11, 11)]`; before-edges `[]`; executions `1`.
* `reallocation-cleanup`: separate later checked DEALLOCATE(left); multiset `[(10, 2), (11, 7), (11, 11), (10, 3), (11, 17), (11, 19), (11, 23)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_001_valid__final_event_allocated_subobject`

**Primary:** S7.5.6.3-001; **facets:** `allocated-subobject-before-release`; **phase/evidence:** run / effect.

The containing own final, not a child final, observes its still-allocated INTEGER child under9.7.3.2p7/p9. Only the callback's own defined fields are read. The constructor's rank-one intrinsic component data source is legal7.5.10p6/p8. S10_2_1_3_029_valid__finalization instead counts a replaced finalizable child, with no containing own callback; it is preserved, not duplicated or relabelled.

* `subobject-before`: defined old containing object and allocated child; multiset `[]`; before-edges `[]`; executions `1`.
* `subobject-assigned`: assignment after containing final and child replacement; multiset `[(10, 7), (11, 2), (12, 17), (12, 19)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_003_valid__final_event_return_local`

**Primary:** S7.5.6.3-003; **facets:** `explicit-return-local`; **phase/evidence:** run / effect.

The event locals are unsaved nonpointer nonallocatable TYPE(item) objects, neither dummies nor results, with no declaration initializer or whole-object setup. The caller reads only the separate saved INTEGER log. The explicit RETURN path is exercised.

* `local-before-exit`: initialized live locals immediately before exit; multiset `[]`; before-edges `[]`; executions `1`.
* `local-after-exit`: caller after RETURN/END-subprogram; multiset `[(10, 17)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_003_valid__final_event_end_local`

**Primary:** S7.5.6.3-003; **facets:** `end-subprogram-local`; **phase/evidence:** run / effect.

The event locals are unsaved nonpointer nonallocatable TYPE(item) objects, neither dummies nor results, with no declaration initializer or whole-object setup. The caller reads only the separate saved INTEGER log. Fallthrough END SUBROUTINE, not END PROGRAM, is exercised.

* `local-before-exit`: initialized live locals immediately before exit; multiset `[]`; before-edges `[]`; executions `1`.
* `local-after-exit`: caller after RETURN/END-subprogram; multiset `[(10, 19)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_003_valid__final_event_multiple_locals`

**Primary:** S7.5.6.3-003; **facets:** `multiple-locals-unordered`; **phase/evidence:** run / effect.

The event locals are unsaved nonpointer nonallocatable TYPE(item) objects, neither dummies nor results, with no declaration initializer or whole-object setup. The caller reads only the separate saved INTEGER log. Two own-token callbacks are a multiset; both orders are accepted.

* `local-before-exit`: initialized live locals immediately before exit; multiset `[]`; before-edges `[]`; executions `1`.
* `local-after-exit`: caller after RETURN/END-subprogram; multiset `[(10, 17), (10, 19)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_003_valid__final_event_saved_local`

**Primary:** S7.5.6.3-003; **facets:** `saved-local-exclusion-control`; **phase/evidence:** run / effect.

The event locals are unsaved nonpointer nonallocatable TYPE(item) objects, neither dummies nor results, with no declaration initializer or whole-object setup. The caller reads only the separate saved INTEGER log. Explicit SAVE retains kept; its41 callback is excluded alongside a real17 event.

* `local-before-exit`: initialized live locals immediately before exit; multiset `[]`; before-edges `[]`; executions `1`.
* `local-after-exit`: caller after RETURN/END-subprogram; multiset `[(10, 17)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_004_valid__final_event_end_block`

**Primary:** S7.5.6.3-004; **facets:** `ordinary-end-block`; **phase/evidence:** run / effect.

A nonpointer nonallocatable unsaved BLOCK-local object becomes undefined at END BLOCK under19.6.6(23). Its primitive token is defined before the event; the post-block observer does not reference the dead local.

* `block-inside`: defined live ordinary BLOCK local; multiset `[]`; before-edges `[]`; executions `1`.
* `block-after`: immediately after END BLOCK; multiset `[(10, 17)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_004_valid__final_event_nested_blocks`

**Primary:** S7.5.6.3-004; **facets:** `nested-block-timing`; **phase/evidence:** run / effect.

The separate-log middle checkpoint requires the inner19 callback already complete and excludes the still-live outer17 callback. No inner object is read after its lifetime. The two distinct nesting events, not sibling declaration order, impose the edge.

* `nested-inner-inside`: both locals live before inner END BLOCK; multiset `[]`; before-edges `[]`; executions `1`.
* `nested-between`: inner ended, outer BLOCK still active; multiset `[(10, 19)]`; before-edges `[]`; executions `1`.
* `nested-after`: both BLOCK constructs have ended; multiset `[(10, 19), (10, 17)]`; before-edges `[((10, 19), (10, 17))]`; executions `1`.

### `S7_5_6_3_004_valid__final_event_saved_block`

**Primary:** S7.5.6.3-004; **facets:** `saved-block-exclusion-control`; **phase/evidence:** run / effect.

Explicitly saved kept retains its definition status under8.5.16; only unsaved local17 receives the direct END BLOCK event. Primitive setup and a positive local callback make the saved exclusion nonvacuous. No termination claim is made.

* `saved-block-inside`: saved and unsaved BLOCK locals are live; multiset `[]`; before-edges `[]`; executions `1`.
* `saved-block-after`: END BLOCK excludes saved kept; multiset `[(10, 17)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_005_valid__final_event_statement_result`

**Primary:** S7.5.6.3-005; **facets:** `statement-result-boundary`; **phase/evidence:** run / effect.

An ordinary IMPURE module function initializes only its nonpointer result's token. The INTEGER getter genuinely consumes the result, expects creation but no final at getter entry, and logs consumption. Exactly one old-result callback follows the complete assignment statement; function END is not that event.

* `statement-getter`: result returned, getter executing before statement ends; multiset `[(5, 17)]`; before-edges `[]`; executions `1`.
* `statement-after`: INTEGER assignment statement completed; multiset `[(5, 17), (20, 17), (10, 17)]`; before-edges `[((20, 17), (10, 17))]`; executions `1`.

### `S7_5_6_3_005_valid__final_event_if_result`

**Primary:** S7.5.6.3-005; **facets:** `if-construct-boundary`; **phase/evidence:** run / effect.

The reference is in the block IF controlling expression, not in a body statement or unevaluated inquiry. Distinct first/last body markers and in-block zero-final checks precede the outside single-final checkpoint. The selected block must execute.

* `if-getter`: block IF condition consumes a created nonpointer result; multiset `[(5, 1)]`; before-edges `[]`; executions `1`.
* `if-inside`: inside the selected block after its first body marker; multiset `[(5, 1), (20, 1), (40, 31)]`; before-edges `[]`; executions `1`.
* `if-last-body`: last body checkpoint, still within block IF; multiset `[(5, 1), (20, 1), (40, 31), (40, 37)]`; before-edges `[]`; executions `1`.
* `if-after`: whole block IF completed; multiset `[(5, 1), (20, 1), (40, 31), (40, 37), (10, 1)]`; before-edges `[((40, 37), (10, 1))]`; executions `1`.

### `S7_5_6_3_005_valid__final_event_do_result`

**Primary:** S7.5.6.3-005; **facets:** `do-control-boundary`; **phase/evidence:** run / effect.

The terminal expression of an ordinary counted DO, not DO CONCURRENT or a body statement, consumes the nonpointer result once. All three iterations exclude a callback. The callback also records the separate live INTEGER DO index4, which distinguishes cleanup before the final increment; caller iteration/sum checks exclude vacuous or misbounded loops.

* `do-getter`: ordinary counted DO terminal expression is consumed; multiset `[(5, 3)]`; before-edges `[]`; executions `1`.
* `do-inside`: each of three counted DO iterations; multiset `[(5, 3), (20, 3)]`; before-edges `[]`; executions `3`.
* `do-after`: entire counted DO including final index increment completed; multiset `[(5, 3), (20, 3), (10, 3), (41, 4)]`; before-edges `[((20, 3), (10, 3))]`; executions `1`.

### `S7_5_6_3_005_valid__final_event_pointer_result`

**Primary:** S7.5.6.3-005; **facets:** `pointer-result-exclusion-control`; **phase/evidence:** run / effect.

An ordinary IMPURE pointer factory explicitly associates its result with a defined saved module TARGET that remains reachable by name. There is no result finalization. A later genuine-constructor assignment legally finalizes the old target23 and provides the positive observer; no pointer or nonallocated target is deallocated, and no PURE association shortcut is used.

* `pointer-getter`: defined saved target consumed through an associated result; multiset `[(5, 23)]`; before-edges `[]`; executions `1`.
* `pointer-after`: pointer function reference statement completed; multiset `[(5, 23), (20, 23)]`; before-edges `[]`; executions `1`.
* `pointer-positive`: later legal intrinsic assignment to saved target; multiset `[(5, 23), (20, 23), (10, 23)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_007_valid__final_event_ordinary_out`

**Primary:** S7.5.6.3-007; **facets:** `ordinary-intent-out-old-value`; **phase/evidence:** run / effect.

The OUT dummy is nonpointer and nonallocatable even though its defined actual is allocated. Before undefinition its final sees old token7/defaulted9. On entry the body sees both completed log records and the defaulted field reset to5 under 7.5.4.6/19.6.5(24); it never reads the undefined nondefaulted token. It defines 11/13 before caller checks and later cleanup, whose callbacks remain separately bounded.

* `ordinary-body-entry`: OUT body after finalization and default initialization; multiset `[(10, 7), (11, 9)]`; before-edges `[]`; executions `1`.
* `ordinary-after-call`: body defined new primitive fields and returned; multiset `[(10, 7), (11, 9), (20, 5)]`; before-edges `[]`; executions `1`.
* `ordinary-cleanup`: separate later checked whole-object DEALLOCATE; multiset `[(10, 7), (11, 9), (20, 5), (10, 11), (11, 13)]`; before-edges `[]`; executions `1`.

### `S7_5_6_3_007_valid__final_event_elemental_scalar`

**Primary:** S7.5.6.3-007; **facets:** `elemental-scalar-selection`; **phase/evidence:** run / effect.

Same-type scalar IMPURE ELEMENTAL and ordinary rank-one finals are a legal C793 distinct-rank family. Entry uses the scalar dummy context, not actual-array rank. Each body checks its own completed old-token callback using independent INTEGER marker arrays, not an aliased actual%token or its undefined OUT field. Only per-element before-edges and final multisets are required; no inter-element side-effect order is imposed. Later whole-array cleanup selects the rank-one final.

* `elemental-after-call`: completed elemental OUT call, before later cleanup; multiset `[(10, 17), (20, 17), (10, 19), (20, 19)]`; before-edges `[((10, 17), (20, 17)), ((10, 19), (20, 19))]`; executions `1`.
* `elemental-cleanup`: whole-array DEALLOCATE selects the ordinary rank-one final; multiset `[(10, 17), (20, 17), (10, 19), (20, 19), (30, 2), (31, 31), (31, 37)]`; before-edges `[((10, 17), (20, 17)), ((10, 19), (20, 19))]`; executions `1`.

### `S7_5_6_3_007_valid__final_event_elemental_array_only`

**Primary:** S7.5.6.3-007; **facets:** `elemental-array-only-no-entry-final`; **phase/evidence:** run / effect.

There is only an ordinary rank-one final, with no finalizable parent or plain component. The logging callee is explicitly IMPURE ELEMENTAL with scalar legal intents; each body sees zero entry callbacks and defines its output token. Before cleanup, the exact two body markers exclude entry finalization. Successful later deallocation of the still-allocated actual supplies one real array callback over the newly defined values, so the exclusion is not a no-op proof.

* `array-only-after-call`: elemental OUT call with only an ordinary array final; multiset `[(20, 31), (20, 37)]`; before-edges `[]`; executions `1`.
* `array-only-cleanup`: separate later checked whole-array DEALLOCATE; multiset `[(20, 31), (20, 37), (30, 2), (31, 31), (31, 37)]`; before-edges `[]`; executions `1`.

## Complete finite pending plans

The following canonical plans retain their exact text. A prerequisite event, model or unregistered reuse relationship does not clear a pending facet.

### Pending S7.5.6.3-001

* **`assignment-and-deallocation-use-graph`** - Pending canonical intrinsic component assignment, reallocation, default and defined-assignment relationships; defined assignment may independently finalize an INTENT(OUT) dummy and is not a no-callback control by name alone.

### Pending S7.5.6.3-002

* **`allocated-pointer-target`** - Pending one whole allocated target with a defined scalar token, a valid associated pointer and STAT-checked deallocation; finalizer records the target, not pointer status.
* **`allocated-allocatable-entity`** - Pending a defined allocated record and successful STAT-checked deallocation, with a callback before the object is released.
* **`automatic-allocatable-scope-exit`** - Pending an unsaved allocated local leaving a procedure or BLOCK; this is the canonical automatic-deallocation path, not the nonallocatable direct-return rule.
* **`allocatable-subobject-cleanup`** - Pending a finalizable outer object with an allocated finalizable component: own outer final precedes the automatic component deallocation/finalizer under9.7.3.2p9.
* **`assignment-no-double-finalization`** - Pending exact linkage to p1/9.7.3.2 assignment deallocation so the left variable is not counted again merely because it is subsequently deallocated.
* **`deallocation-event-use-graph`** - Pending pointer aliases, actual/construct associations, C descriptors and collective coarray deallocation qualifications before broadening the finite witness family.

### Pending S7.5.6.3-003

* **`dummy-result-and-auto-deallocation-graph`** - Pending canonical links separating dummy/result exclusions and automatic allocated-local cleanup from the direct p3 event.

### Pending S7.5.6.3-004

* **`block-exit-use-graph`** - Pending canonical permitted branch/construct-termination and automatic-deallocation paths before extra EXIT/branch fixtures are authored.

### Pending S7.5.6.3-005

* **`allocated-result-and-construct-use-graph`** - Pending complete allocated-result/subobject state and executable-construct nesting relationships. Avoid unallocated result values or syntax that dereferences a function result as an unsupported data-ref.

### Pending S7.5.6.3-006

* **`pure-final-before-body-candidate`** - Pending independent validation of a module PURE record factory and PURE INTEGER getter used as an automatic array bound in an ordinary subroutine. The record FINAL must itself be PURE. A PURE finalizer using ERROR STOP with constant code/QUIET may provide an external before-body oracle because ERROR STOP is not an11.7.1image-control statement; independently qualify process status/output before implementation.
* **`specification-function-purity-graph`** - Pending exact C1593/C1596/C15105 and10.1.11 source/use relationships, including the prohibition on an impure finalizer for a PURE function result.
* **`unevaluated-inquiry-and-result-state-graph`** - Pending proof that a candidate actually creates the finalizable result rather than only inquiring about fixed properties; preserve allocated/nonpointer result and canonical attribute evaluation conditions.

### Pending S7.5.6.3-007

* **`pure-and-allocatable-pointer-context-graph`** - Pending canonical purity, pointer/allocatable dummy exclusions, automatic deallocation and default-initialization relationships; allocated actuals associated with allocatable OUT dummies use their deallocation path instead.

## Reproduction and remaining gates

`python3 -B tools/generate_final_event_fixtures.py --check` checks exact fixture bytes, finite coverage, current administrative review rendering and the complete pending appendix. Focused regressions use independently specified expected histories, permitted unordered alternatives and wrong timing/multiplicity/nonvacuity countermodels bound to actual emitted guard ranges. Independent review and exact observation provenance are recorded in `doc/source_audits/batch_019.json`. Sixteen cases have qualifying GNU f2023 observations. IF/DO result cases retain source-only adjudication and whole-construct oracles despite the recorded missing or premature callbacks; synthetic guard runs are not successful automatic-finalization executions. All 14 pending facets remain open, and current source/case/evidence approvals remain separate content-bound records.
