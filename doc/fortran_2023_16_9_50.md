# Fortran 2023 16.9.50: BTEST intrinsic

The canonical catalogue is `doc/catalogues/btest_intrinsic_16_9_50.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.50")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Six base units, p1-p6. No fine or table units occur; PDF page 393 and next-boundary page 394 were checked. The pinned reader verifies p1-p5 and reports a typography-only example hash mismatch for p6, which is classified informative.

Coverage grep: BTEST appears only in tests/test_parameter_value_effect_fixtures.py as a forbidden-helper regex; no fixture.json binding in this scope.

<!-- BEGIN INTRINSICS 16.9.E FIXTURES -->
## Intrinsics 16.9.E runtime observations

This batch adds 3 generated run/effect or positive-control fixtures for 16.9.50, covering 7 pending facets with exact logical, kind, rank, integer, character round-trip, bit-sequence, CEILING, and CMPLX component observations. Mutation metadata perturbs each fixture's load-bearing intrinsic expression and completion output; mutation checking compiles each mutant in a repository-local scratch directory and requires the mutant not to pass.
<!-- END INTRINSICS 16.9.E FIXTURES -->

<!-- BEGIN GENERATED 16.9.50 -->

### S16.9.50-001: BTEST arguments are an integer and a valid bit position

**Source:** S16.9.50, J3/24-007, 18 December 2023, physical PDF393. **Class:** Restriction.

**Definition:** The I argument to BTEST shall be of type integer; POS shall be of type integer,
nonnegative, and less than BIT_SIZE(I).

**Diagnostic obligation:** not-required.

**Facets:** `btest-i-integer`, `btest-pos-integer`, `btest-pos-nonnegative`, `btest-pos-less-than-bit-size`.

**Oracle:** Plans are pending and source-derived. Future fixtures initialize scalar result variables
or arrays to non-default, opposite, or otherwise load-bearing sentinels before calling
the intrinsic and assert only exact logical, integer, kind, scalar-rank, POS-boundary,
or bit-sequence properties fixed by 16.9 and its 16.3/16.3.2 dependencies. POS controls
include both the lower boundary 0 and the upper valid boundary BIT_SIZE(I)-1.

S16.9.50-001 intrinsics_16_9_e fixture: BTEST accepts integer I and integer POS values
at the lower boundary 0 and upper valid boundary BIT_SIZE(I)-1.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Clause 16 argument shall-restrictions in these intrinsic sections are unnumbered program
restrictions, so this packet records no local diagnostic facet for wrong argument type,
argument class, or out-of-range POS; a future negative requires an independently
numbered rule requiring rejection. Negative-integer bit interpretation beyond the exact
source examples, BOZ interpretation outside the cited 7.7 bit-sequence rules, and
example-only text used without a forcing Result Value rule are not portable conformance
oracles. Units 16.9.50 p5-p6 were verified visually/by text while retaining the census
hashes because of the known census-hash-drift-math-glyphs followup.

S16.9.50-001 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, and I/O foundations from clauses 6 through 13 where already
present. Unregistered dependencies retained for future fixtures: 7.7 BOZ literal
interpretation, 16.3 integer bit model, 16.3.2 bit sequence comparison, sibling 16.9 bit
intrinsics such as IAND/IOR/IBSET/BTEST where a plan uses them, and clause 19
scoping/association terminology where reached.

### S16.9.50-002: BTEST result has default logical characteristics

**Source:** S16.9.50, J3/24-007, 18 December 2023, physical PDF393. **Class:** Effect.

**Definition:** The BTEST result is default logical.

**Diagnostic obligation:** not-required.

**Facets:** `btest-result-default-logical`.

**Oracle:** Plans are pending and source-derived. Future fixtures initialize scalar result variables
or arrays to non-default, opposite, or otherwise load-bearing sentinels before calling
the intrinsic and assert only exact logical, integer, kind, scalar-rank, POS-boundary,
or bit-sequence properties fixed by 16.9 and its 16.3/16.3.2 dependencies. POS controls
include both the lower boundary 0 and the upper valid boundary BIT_SIZE(I)-1.

S16.9.50-002 intrinsics_16_9_e fixture: The BTEST result is default LOGICAL, observed
with KIND(BTEST(1,0)).

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Clause 16 argument shall-restrictions in these intrinsic sections are unnumbered program
restrictions, so this packet records no local diagnostic facet for wrong argument type,
argument class, or out-of-range POS; a future negative requires an independently
numbered rule requiring rejection. Negative-integer bit interpretation beyond the exact
source examples, BOZ interpretation outside the cited 7.7 bit-sequence rules, and
example-only text used without a forcing Result Value rule are not portable conformance
oracles. Units 16.9.50 p5-p6 were verified visually/by text while retaining the census
hashes because of the known census-hash-drift-math-glyphs followup.

S16.9.50-002 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, and I/O foundations from clauses 6 through 13 where already
present. Unregistered dependencies retained for future fixtures: 7.7 BOZ literal
interpretation, 16.3 integer bit model, 16.3.2 bit sequence comparison, sibling 16.9 bit
intrinsics such as IAND/IOR/IBSET/BTEST where a plan uses them, and clause 19
scoping/association terminology where reached.

### S16.9.50-003: BTEST reports whether bit POS of I is one

**Source:** S16.9.50, J3/24-007, 18 December 2023, physical PDF393. **Class:** Effect.

**Definition:** BTEST returns true if bit POS of I has value 1 and false if bit POS of I has value 0,
using the integer bit model of 16.3.

**Diagnostic obligation:** not-required.

**Facets:** `btest-true-for-one-bit`, `btest-false-for-zero-bit`.

**Oracle:** Plans are pending and source-derived. Future fixtures initialize scalar result variables
or arrays to non-default, opposite, or otherwise load-bearing sentinels before calling
the intrinsic and assert only exact logical, integer, kind, scalar-rank, POS-boundary,
or bit-sequence properties fixed by 16.9 and its 16.3/16.3.2 dependencies. POS controls
include both the lower boundary 0 and the upper valid boundary BIT_SIZE(I)-1.

S16.9.50-003 intrinsics_16_9_e fixture: BTEST returns true for a one bit at POS and
false for a zero bit at POS, using exact positions 0 and 1 of integer value one.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Clause 16 argument shall-restrictions in these intrinsic sections are unnumbered program
restrictions, so this packet records no local diagnostic facet for wrong argument type,
argument class, or out-of-range POS; a future negative requires an independently
numbered rule requiring rejection. Negative-integer bit interpretation beyond the exact
source examples, BOZ interpretation outside the cited 7.7 bit-sequence rules, and
example-only text used without a forcing Result Value rule are not portable conformance
oracles. Units 16.9.50 p5-p6 were verified visually/by text while retaining the census
hashes because of the known census-hash-drift-math-glyphs followup.

S16.9.50-003 intrinsics_16_9_e boundaries: This packet supplies only the named
single-image run/effect or positive-control fixtures. It does not claim diagnostics for
unnumbered argument restrictions, coarray behavior, source-review approval, nondefault
character kinds not guaranteed by the processor, inexact conversion values, or
processor-dependent approximations.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, and I/O foundations from clauses 6 through 13 where already
present. Unregistered dependencies retained for future fixtures: 7.7 BOZ literal
interpretation, 16.3 integer bit model, 16.3.2 bit sequence comparison, sibling 16.9 bit
intrinsics such as IAND/IOR/IBSET/BTEST where a plan uses them, and clause 19
scoping/association terminology where reached.

<!-- END GENERATED 16.9.50 -->
