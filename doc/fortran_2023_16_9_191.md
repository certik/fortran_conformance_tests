# Fortran 2023 16.9.191: SIND

The canonical catalogue is `doc/catalogues/sind_intrinsic_16_9_191.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.191 -->

### S16.9.191-001: SIND argument restriction

**Source:** S16.9.191, J3/24-007, 18 December 2023, physical PDF458. **Class:** Restriction.

**Definition:** X shall be real.

**Diagnostic obligation:** not-required.

**Facets:** `SIND-X-allowed-type`.

**Oracle:** Source registration only. Future fixtures may assert exact result type, kind, and
elemental shape properties for SIN and SIND. The numeric value, accuracy, overflow
behavior, last bit, and decimal spelling of trigonometric results are
processor-dependent approximations and are not portable exact oracles. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet. Approximate trigonometric
result facets intentionally keep numeric checks pending without tolerances.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

### S16.9.191-002: SIND result characteristics match X

**Source:** S16.9.191, J3/24-007, 18 December 2023, physical PDF458. **Class:** Effect.

**Definition:** The SIND result has the same type and kind as X.

**Diagnostic obligation:** not-required.

**Facets:** `SIND-result-same-type-kind-as-X`.

**Oracle:** Source registration only. Future fixtures may assert exact result type, kind, and
elemental shape properties for SIN and SIND. The numeric value, accuracy, overflow
behavior, last bit, and decimal spelling of trigonometric results are
processor-dependent approximations and are not portable exact oracles. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet. Approximate trigonometric
result facets intentionally keep numeric checks pending without tolerances.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

### S16.9.191-003: SIND mathematical target and angle interpretation

**Source:** S16.9.191, J3/24-007, 18 December 2023, physical PDF458. **Class:** Effect.

**Definition:** The SIND result approximates the sine of X, with X regarded as degrees.

**Diagnostic obligation:** not-required.

**Facets:** `SIND-mathematical-sine-target`, `SIND-argument-degrees`.

**Oracle:** Source registration only. Future fixtures may assert exact result type, kind, and
elemental shape properties for SIN and SIND. The numeric value, accuracy, overflow
behavior, last bit, and decimal spelling of trigonometric results are
processor-dependent approximations and are not portable exact oracles. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet. Approximate trigonometric
result facets intentionally keep numeric checks pending without tolerances.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

### S16.9.191-004: SIND approximation is processor dependent

**Source:** S16.9.191, J3/24-007, 18 December 2023, physical PDF458. **Class:** Effect.

**Definition:** The processor chooses the approximation used for the SIND result.

**Diagnostic obligation:** not-required.

**Facets:** `SIND-processor-dependent-approximation`.

**Oracle:** Source registration only. Future fixtures may assert exact result type, kind, and
elemental shape properties for SIN and SIND. The numeric value, accuracy, overflow
behavior, last bit, and decimal spelling of trigonometric results are
processor-dependent approximations and are not portable exact oracles. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet. Approximate trigonometric
result facets intentionally keep numeric checks pending without tolerances.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

<!-- END GENERATED 16.9.191 -->
