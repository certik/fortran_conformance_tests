# Fortran 2023 16.9.119: LBOUND

The canonical catalogue is `doc/catalogues/lbound_16_9_119.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.119")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, note-unnumbered, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN INTRINSICS 16.9.O FIXTURES -->
## Intrinsics 16.9.O runtime observations

This batch adds 5 generated run/effect or positive-control fixtures for 16.9.119, covering 11 pending facets for ISHFTC, IS_CONTIGUOUS, IS_IOSTAT_END, IS_IOSTAT_EOR, KIND, and LBOUND. Oracles use BTEST bit observations, direct KIND/RANK inquiries on intrinsic expressions, guarded actual READ IOSTAT values, and exact lower-bound rules including DIM, KIND, zero extent, assumed-size, and assumed-shape cases. Processor-dependent contiguity and coarray LCOBOUND cases are outside this packet.
<!-- END INTRINSICS 16.9.O FIXTURES -->

<!-- BEGIN GENERATED 16.9.119 -->

### S16.9.119-001: LBOUND argument restrictions

**Source:** S16.9.119, J3/24-007, 18 December 2023, physical PDF425. **Class:** Restriction.

**Definition:** ARRAY shall be assumed-rank or an array, shall not be an unallocated allocatable or
disassociated pointer, DIM shall be an integer scalar in the range one through the rank
of ARRAY with a corresponding actual argument that is not absent, disassociated, or
unallocated, and KIND shall be a scalar integer constant expression.

**Diagnostic obligation:** not-required.

**Facets:** `LBOUND-ARRAY-assumed-rank-or-array`, `LBOUND-ARRAY-allocated-or-associated`, `LBOUND-DIM-integer-scalar-in-rank-range`, `LBOUND-DIM-corresponding-actual-present-associated-allocated`, `LBOUND-KIND-scalar-integer-constant-expression`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only for exact operations or
standard inquiry-model values. Every positive plan establishes a non-default state
first, uses a conforming companion for zero, .FALSE., or blank results that could
otherwise be vacuous, and includes a feature mutation such as changing the intrinsic
name, argument order, KIND, DIM, SIZE, or SHIFT value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Bit-result intrinsic plans observe selected bits with BTEST or
BOZ-pattern comparison rather than relying on numeric interpretation of a leftmost bit.

S16.9.119-001 intrinsics_16_9_o fixture: LBOUND argument-controls fixture uses scalar
DIM values in range and a scalar integer constant KIND expression;
allocation/association status restrictions remain pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are not present in these sections; exact plans are
limited to integer/logical/character/shape/kind/bit effects and exact inquiry
properties. Grep of tests/ and tests/fixtures/*/fixture.json for ISHFT, ISHFTC,
IS_CONTIGUOUS, IS_IOSTAT_END, IS_IOSTAT_EOR, KIND, and LBOUND found no fixture-json
binding for ISHFT, ISHFTC, IS_CONTIGUOUS, IS_IOSTAT_END, or IS_IOSTAT_EOR. KIND appears
incidentally in 674 test files and 132 fixture manifests, and LBOUND appears in 98 test
files and two fixture manifests (allocate_execution_source_shape_bounds and
associate_construct_lower_bound_lbound); those uses remain existing coverage and are not
rebound by this source packet.

S16.9.119-001 intrinsics_16_9_o boundaries: This packet supplies only exact
integer/logical/kind/bounds/bit and guarded-I/O observations; it does not assert
processor-dependent contiguity cases, IOSTAT numbers, IOMSG text, coarray behavior,
diagnostics for unnumbered restrictions, approximations, or compiler consensus.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, I/O status, and exact formatting helpers. Unregistered
dependencies retained: clause 16.3 integer bit-sequence model and sibling 16.9 intrinsic
procedures named by result formulas; clauses 3, 5, 14, 15, 17, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, IEEE support, deleted
features, or scope rules.

### S16.9.119-002: LBOUND result characteristics

**Source:** S16.9.119, J3/24-007, 18 December 2023, physical PDF425. **Class:** Effect.

**Definition:** The LBOUND result is integer; KIND selects the result kind or default integer is used,
DIM presence gives a scalar result, and DIM absence gives a rank-one array of size equal
to the rank of ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `LBOUND-result-integer-kind-default-or-KIND`, `LBOUND-result-scalar-with-DIM`, `LBOUND-result-rank-one-size-rank-without-DIM`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only for exact operations or
standard inquiry-model values. Every positive plan establishes a non-default state
first, uses a conforming companion for zero, .FALSE., or blank results that could
otherwise be vacuous, and includes a feature mutation such as changing the intrinsic
name, argument order, KIND, DIM, SIZE, or SHIFT value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Bit-result intrinsic plans observe selected bits with BTEST or
BOZ-pattern comparison rather than relying on numeric interpretation of a leftmost bit.

S16.9.119-002 intrinsics_16_9_o fixture: LBOUND characteristics fixture inquires KIND,
RANK, and SIZE directly on LBOUND expressions with and without DIM/KIND.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are not present in these sections; exact plans are
limited to integer/logical/character/shape/kind/bit effects and exact inquiry
properties. Grep of tests/ and tests/fixtures/*/fixture.json for ISHFT, ISHFTC,
IS_CONTIGUOUS, IS_IOSTAT_END, IS_IOSTAT_EOR, KIND, and LBOUND found no fixture-json
binding for ISHFT, ISHFTC, IS_CONTIGUOUS, IS_IOSTAT_END, or IS_IOSTAT_EOR. KIND appears
incidentally in 674 test files and 132 fixture manifests, and LBOUND appears in 98 test
files and two fixture manifests (allocate_execution_source_shape_bounds and
associate_construct_lower_bound_lbound); those uses remain existing coverage and are not
rebound by this source packet.

S16.9.119-002 intrinsics_16_9_o boundaries: This packet supplies only exact
integer/logical/kind/bounds/bit and guarded-I/O observations; it does not assert
processor-dependent contiguity cases, IOSTAT numbers, IOMSG text, coarray behavior,
diagnostics for unnumbered restrictions, approximations, or compiler consensus.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, I/O status, and exact formatting helpers. Unregistered
dependencies retained: clause 16.3 integer bit-sequence model and sibling 16.9 intrinsic
procedures named by result formulas; clauses 3, 5, 14, 15, 17, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, IEEE support, deleted
features, or scope rules.

### S16.9.119-003: LBOUND DIM-present result value

**Source:** S16.9.119, J3/24-007, 18 December 2023, physical PDF425. **Class:** Effect.

**Definition:** With DIM present, a whole array with nonzero extent in dimension DIM or an assumed-size
array of rank DIM yields that dimension lower bound; other DIM-present cases yield one.

**Diagnostic obligation:** not-required.

**Facets:** `LBOUND-DIM-whole-nonzero-extent-lower-bound`, `LBOUND-DIM-assumed-size-rank-DIM-lower-bound`, `LBOUND-DIM-otherwise-one`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only for exact operations or
standard inquiry-model values. Every positive plan establishes a non-default state
first, uses a conforming companion for zero, .FALSE., or blank results that could
otherwise be vacuous, and includes a feature mutation such as changing the intrinsic
name, argument order, KIND, DIM, SIZE, or SHIFT value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Bit-result intrinsic plans observe selected bits with BTEST or
BOZ-pattern comparison rather than relying on numeric interpretation of a leftmost bit.

S16.9.119-003 intrinsics_16_9_o fixture: LBOUND DIM-value fixture observes whole nonzero
lower bounds, assumed-size dummy lower bounds, and required one for sections/zero
extent.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are not present in these sections; exact plans are
limited to integer/logical/character/shape/kind/bit effects and exact inquiry
properties. Grep of tests/ and tests/fixtures/*/fixture.json for ISHFT, ISHFTC,
IS_CONTIGUOUS, IS_IOSTAT_END, IS_IOSTAT_EOR, KIND, and LBOUND found no fixture-json
binding for ISHFT, ISHFTC, IS_CONTIGUOUS, IS_IOSTAT_END, or IS_IOSTAT_EOR. KIND appears
incidentally in 674 test files and 132 fixture manifests, and LBOUND appears in 98 test
files and two fixture manifests (allocate_execution_source_shape_bounds and
associate_construct_lower_bound_lbound); those uses remain existing coverage and are not
rebound by this source packet.

S16.9.119-003 intrinsics_16_9_o boundaries: This packet supplies only exact
integer/logical/kind/bounds/bit and guarded-I/O observations; it does not assert
processor-dependent contiguity cases, IOSTAT numbers, IOMSG text, coarray behavior,
diagnostics for unnumbered restrictions, approximations, or compiler consensus.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, I/O status, and exact formatting helpers. Unregistered
dependencies retained: clause 16.3 integer bit-sequence model and sibling 16.9 intrinsic
procedures named by result formulas; clauses 3, 5, 14, 15, 17, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, IEEE support, deleted
features, or scope rules.

### S16.9.119-004: LBOUND DIM-absent vector result value

**Source:** S16.9.119, J3/24-007, 18 December 2023, physical PDF425. **Class:** Effect.

**Definition:** With DIM absent, LBOUND(ARRAY) yields a vector whose element i equals LBOUND(ARRAY,i),
and LBOUND(ARRAY,KIND=KIND) yields the same element values with the selected integer
kind.

**Diagnostic obligation:** not-required.

**Facets:** `LBOUND-vector-elements-match-DIM-inquiries`, `LBOUND-vector-KIND-elements-match-selected-kind-inquiries`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only for exact operations or
standard inquiry-model values. Every positive plan establishes a non-default state
first, uses a conforming companion for zero, .FALSE., or blank results that could
otherwise be vacuous, and includes a feature mutation such as changing the intrinsic
name, argument order, KIND, DIM, SIZE, or SHIFT value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Bit-result intrinsic plans observe selected bits with BTEST or
BOZ-pattern comparison rather than relying on numeric interpretation of a leftmost bit.

S16.9.119-004 intrinsics_16_9_o fixture: LBOUND vector-value fixture checks DIM-absent
vector elements against scalar inquiries and repeats them with selected KIND; an
assumed-shape helper observes dummy lower bounds.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are not present in these sections; exact plans are
limited to integer/logical/character/shape/kind/bit effects and exact inquiry
properties. Grep of tests/ and tests/fixtures/*/fixture.json for ISHFT, ISHFTC,
IS_CONTIGUOUS, IS_IOSTAT_END, IS_IOSTAT_EOR, KIND, and LBOUND found no fixture-json
binding for ISHFT, ISHFTC, IS_CONTIGUOUS, IS_IOSTAT_END, or IS_IOSTAT_EOR. KIND appears
incidentally in 674 test files and 132 fixture manifests, and LBOUND appears in 98 test
files and two fixture manifests (allocate_execution_source_shape_bounds and
associate_construct_lower_bound_lbound); those uses remain existing coverage and are not
rebound by this source packet.

S16.9.119-004 intrinsics_16_9_o boundaries: This packet supplies only exact
integer/logical/kind/bounds/bit and guarded-I/O observations; it does not assert
processor-dependent contiguity cases, IOSTAT numbers, IOMSG text, coarray behavior,
diagnostics for unnumbered restrictions, approximations, or compiler consensus.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, I/O status, and exact formatting helpers. Unregistered
dependencies retained: clause 16.3 integer bit-sequence model and sibling 16.9 intrinsic
procedures named by result formulas; clauses 3, 5, 14, 15, 17, 18, and 19 where future
fixtures need terms, constants, programs, procedure interfaces, IEEE support, deleted
features, or scope rules.

<!-- END GENERATED 16.9.119 -->
