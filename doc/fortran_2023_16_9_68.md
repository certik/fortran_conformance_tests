# Fortran 2023 16.9.68: CSHIFT

The canonical catalogue is `doc/catalogues/cshift_16_9_68.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or dense source units are split.

<!-- BEGIN INTRINSICS 16.9.G FIXTURES 16.9.68 -->

## `intrinsics_16_9_g` executable fixture observations

This packet adds 8 generated run/f2023 fixture(s) for 16.9.68, covering 12 portable facet(s). CSHIFT and DBLE use exact values, direct characteristics inquiries, and BOZ bit-equivalence where required. CPU_TIME and DATE_AND_TIME fixtures are guarded by processor profiles that require the available branch, so unavailable fallback branches remain pending where not directly observed.
<!-- END INTRINSICS 16.9.G FIXTURES 16.9.68 -->

<!-- BEGIN GENERATED 16.9.68 -->

### S16.9.68-001: CSHIFT performs a circular shift of an array

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Effect.

**Definition:** CSHIFT circularly shifts an array.

**Diagnostic obligation:** not-required.

**Facets:** `cshift-circular-shift-description`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.68-001 intrinsics_16_9_g runtime fixture: Rank-one exact integer arrays observe
CSHIFT as a circular left shift for positive SHIFT and right shift for negative SHIFT by
the p5 formula.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-001 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.68-002: CSHIFT is transformational

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Effect.

**Definition:** CSHIFT is classified as a transformational function.

**Diagnostic obligation:** not-required.

**Facets:** `cshift-transformational-class`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.68-002 intrinsics_16_9_g runtime fixture: A rank-two ARRAY is transformed as a
whole array: direct RANK and SHAPE inquiries on CSHIFT show a rank-two array result, not
a scalar elemental mapping.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-002 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.68-008: CSHIFT permits ARRAY of any type

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Effect.

**Definition:** The ARRAY argument may be of any type.

**Diagnostic obligation:** not-required.

**Facets:** `cshift-array-any-type-array`.

**Oracle:** Pending plan is source-derived from the admission that ARRAY may be of any type; it
observes portable type-preserving CSHIFT effects without asserting diagnostics for
disallowed arguments.

S16.9.68-008 intrinsics_16_9_g runtime fixture: The same CSHIFT operation is applied to
integer and length-three character arrays; exact values and LEN show the any-type ARRAY
admission is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-008 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.68-003: CSHIFT ARRAY, SHIFT, and DIM arguments satisfy restrictions

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Restriction.

**Definition:** ARRAY shall be an array; SHIFT shall be integer and scalar for rank-one ARRAY, otherwise
scalar or of rank n-1 with the DIM extent removed; DIM, if present, shall be an integer
scalar in range 1 through rank n of ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `cshift-shift-integer-scalar-for-rank-one`, `cshift-shift-scalar-or-rank-minus-one-shape`, `cshift-dim-integer-scalar-in-range`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.68-003 intrinsics_16_9_g runtime fixture: Conforming CSHIFT calls use scalar
integer SHIFT for rank one, a rank-one SHIFT vector whose shape is the non-DIM extent
for rank two, and DIM=1 within the rank range; exact results distinguish each argument
property.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-003 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.68-004: CSHIFT absent DIM behaves as DIM=1

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Effect.

**Definition:** If DIM is absent, CSHIFT behaves as if DIM were present with the value 1.

**Diagnostic obligation:** not-required.

**Facets:** `cshift-absent-dim-defaults-to-one`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.68-004 intrinsics_16_9_g runtime fixture: For a rank-two array where DIM=1 and
DIM=2 differ, CSHIFT(M,1) has exactly the DIM=1 section-shift value, proving the
absent-DIM default.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-004 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.68-005: CSHIFT result characteristics match ARRAY

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Effect.

**Definition:** The CSHIFT result has the type and type parameters of ARRAY and the shape of ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `cshift-result-type-parameters-of-array`, `cshift-result-shape-of-array`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.68-005 intrinsics_16_9_g runtime fixture: Direct LEN and SHAPE inquiries on CSHIFT
expressions show the result keeps ARRAY's character length parameter and non-square
rank-two shape.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-005 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.68-006: CSHIFT rank-one result uses LBOUND and MODULO indexing

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Effect.

**Definition:** For rank-one ARRAY, element i of the result is
ARRAY(LBOUND(ARRAY,1)+MODULO(i+SHIFT-1,SIZE(ARRAY))).

**Diagnostic obligation:** not-required.

**Facets:** `cshift-rank-one-left-right-values`, `cshift-rank-one-lbound-sensitive-indexing`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.68-006 intrinsics_16_9_g runtime fixture: The p5 rank-one MODULO formula gives
exact left and right rotations for SHIFT=2 and SHIFT=-2 on [1..6].

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-006 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.68-007: CSHIFT rank-greater-than-one shifts selected sections

**Source:** S16.9.68, J3/24-007, 18 December 2023, physical PDF401. **Class:** Effect.

**Definition:** For rank greater than one, each section along DIM has the value of a rank-one CSHIFT of
that section, using either scalar SHIFT or the corresponding element of the SHIFT array.

**Diagnostic obligation:** not-required.

**Facets:** `cshift-rank-two-dim-section-values`, `cshift-rank-two-shift-vector-values`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.68-007 intrinsics_16_9_g runtime fixture: Rank-two CSHIFT is checked
section-by-section along DIM=2, once with a scalar SHIFT and once with a per-row SHIFT
vector.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.68-007 intrinsics_16_9_g fixture boundaries: This packet supplies only the named
single-image valid fixture facets. Unnumbered argument restrictions are positive
controls only; no rejection diagnostic, processor-dependent exact time, current calendar
value, coarray behavior, source review, evidence link, or fixture approval is asserted.
Profiled CPU_TIME and DATE_AND_TIME cases require the available branch and do not count
unavailable fallback behavior as covered.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

<!-- END GENERATED 16.9.68 -->
