# Fortran 2023 12.10.1: General

The canonical catalogue is `doc/catalogues/inquire_statement_general_12_10_1.json`. This source packet records source accounting. Selected INQUIRE fixtures now supply executable standard-oracle observations and mutation plans; fixture approval remains separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, R1230, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN IO INQUIRE 12.10.1 FIXTURES -->
## INQUIRE general/specifier-list fixtures

This packet adds 62 selected facet bindings for INQUIRE by file, by unit, by IOLENGTH, reference-validated inquiry specifier syntax, and numbered specifier-list constraints. Runtime fixtures initialize every inquiry target to a distinguished sentinel and check that sentinel before INQUIRE; diagnostic fixtures have one-property compiled controls. NAME spelling/case, IOMSG text, processor-dependent sizes and undefined variables are not asserted.
<!-- END IO INQUIRE 12.10.1 FIXTURES -->

<!-- BEGIN GENERATED 12.10.1 -->

### S12.10.1-001: INQUIRE forms may observe files, units, or output-list storage requirements

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** The INQUIRE statement can be used in three forms: inquire by file with FILE=, inquire by
unit with UNIT= or positional file-unit-number, and inquire by output list with only
IOLENGTH=.

**Diagnostic obligation:** not-required.

**Facets:** `inquire-by-file-form`, `inquire-by-unit-form`, `inquire-by-output-list-form`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-001 INQUIRE 12.10.1 fixture implementation: the forms/timing fixture executes
INQUIRE by FILE= before/after creating a named file, by UNIT= on connected and
unconnected units, and by IOLENGTH= on an integer output list whose returned length is
used as a direct unformatted RECL= value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-001 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.1-002: INQUIRE assignments use intrinsic-assignment conversion, truncation, and padding

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** Assignments to variables named in INQUIRE specifiers are converted, truncated, or padded
according to intrinsic assignment rules.

**Diagnostic obligation:** not-required.

**Facets:** `inquire-character-padding`, `inquire-character-truncation`, `inquire-numeric-logical-assignment`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-002 INQUIRE 12.10.1 fixture implementation: the same fixture initializes every
inquiry target to a nonmatching sentinel, checks the sentinel before INQUIRE, then
observes logical OPENED/EXIST and integer NUMBER assignments plus FORM padding and
ACCESS truncation under intrinsic assignment rules.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-002 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.1-003: Inquiry by unit is permitted for nonexistent or unconnected units

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** For inquiry by unit, the specified unit need not exist and need not be connected to a
file.

**Diagnostic obligation:** not-required.

**Facets:** `inquire-unit-need-not-exist`, `inquire-unit-need-not-be-connected`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-003 INQUIRE 12.10.1 fixture implementation: a real external unit is first
opened and closed, then INQUIRE(UNIT=...) reports OPENED=.false. without assuming the
unit is nonexistent. The nonexistent-unit permission remains pending because no
conforming nonnegative valid unit value that reports EXIST=.false. is available on the
reference processors.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-003 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.1-004: Inquiry by unit observes the connection and connected file when connected

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** If the unit in an inquiry by unit is connected to a file, the inquiry is about the
connection and the connected file.

**Diagnostic obligation:** not-required.

**Facets:** `unit-inquiry-connection-properties`, `unit-inquiry-connected-file-properties`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-004 INQUIRE 12.10.1 fixture implementation: a connected named stream/formatted
unit reports OPENED=.true., NUMBER equal to the unit, and fixed ACCESS/ACTION/FORM/NAMED
properties; the returned NAME is used only as an OPEN FILE= value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-004 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.1-005: Inquiry by file is permitted for nonexistent or unconnected files

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** For inquiry by file, the named file need not exist and need not be connected to a unit.

**Diagnostic obligation:** not-required.

**Facets:** `inquire-file-need-not-exist`, `inquire-file-need-not-be-connected`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-005 INQUIRE 12.10.1 fixture implementation: a controlled absent file reports
EXIST=.false./OPENED=.false.; the same name after close(STATUS='KEEP') reports
EXIST=.true./OPENED=.false.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-005 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.1-006: Inquiry by file observes the connected unit and file when connected

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** If the file named in an inquiry by file is connected to a unit, the inquiry is about the
connection as well as the file.

**Diagnostic obligation:** not-required.

**Facets:** `file-inquiry-connection-properties`, `file-inquiry-connected-file-properties`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-006 INQUIRE 12.10.1 fixture implementation: while the controlled name is
connected on exactly one unit, INQUIRE(FILE=...) reports OPENED=.true., NUMBER equal to
that unit, and the fixed FORM token.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-006 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.1-007: INQUIRE may be executed before, during, or after connection

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** An INQUIRE statement may be executed before, while, or after a file is connected to a
unit.

**Diagnostic obligation:** not-required.

**Facets:** `inquire-before-connection`, `inquire-during-connection`, `inquire-after-connection`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-007 INQUIRE 12.10.1 fixture implementation: three FILE= inquiries around the
OPEN/CLOSE transition observe before, during, and after states with fresh sentinel
initialization for each target.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-007 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### S12.10.1-008: INQUIRE returns values current at execution time

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Effect.

**Definition:** All values assigned by an INQUIRE statement are those current at the time the statement
is executed.

**Diagnostic obligation:** not-required.

**Facets:** `inquire-values-current-at-execution`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

S12.10.1-008 INQUIRE 12.10.1 fixture implementation: the OPENED= target is reset before
each of the before/during/after inquiries and changes false/true/false, so stale target
values cannot satisfy the oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

S12.10.1-008 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

### R1230: An INQUIRE statement is a parenthesized specifier list or IOLENGTH form

**Source:** S12.10.1, J3/24-007, 18 December 2023, physical PDF278. **Class:** Syntax.

**Definition:** The INQUIRE statement syntax is either INQUIRE with a parenthesized inquire-spec-list,
or INQUIRE with IOLENGTH=scalar-int-variable followed by an output-item-list.

**Diagnostic obligation:** required.

**Facets:** `inquire-spec-list-form`, `inquire-iolength-output-list-form`.

**Oracle:** Plans are pending and source-derived. Positive INQUIRE plans first establish the
connection or file state being observed, initialize every inquiry variable to a
nonmatching sentinel value, execute INQUIRE, and assert only values fixed by 12.10.2:
exact tokens such as UNDEFINED or UNKNOWN, logical true/false where specified,
zero/nonzero IOSTAT where 12.11 later permits it, or bounded set membership where the
source gives a set. Processor-dependent file names, case interpretation, multi-unit
choices, file storage unit counts, asynchronous completion policy, IOMSG text, and
numeric status codes are not asserted.

R1230 INQUIRE 12.10.1 fixture implementation: the positive-control source contains both
R1230 forms, INQUIRE(inquire-spec-list) and INQUIRE(IOLENGTH=scalar-int-variable)
output-list; feature mutants redirect the designator or IOLENGTH target.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered syntax
rule or numbered constraint and a future fixture can observe the rejection portably.
Undefined inquiry variables are not read as an oracle; IOMSG wording and
processor-dependent numeric status values are never compared.

R1230 INQUIRE 12.10.1 fixture boundaries: These fixtures assert only exact
logical/integer values fixed by 12.10.2, exact upper-case tokens fixed by the individual
specifier rules, changed-from-sentinel assignment where a token is processor dependent,
and IOLENGTH suitability as a RECL= value. They do not compare NAME spelling or case,
IOMSG text, numeric nonzero IOSTAT codes, processor-dependent file sizes, ambiguous
multiple-unit NUMBER choices, or undefined inquiry variables. ERR=, ID=, IOMSG=,
LEADING_ZERO= syntax facets and internal-unit/error-variable effect facets remain
pending unless covered by the diagnostic controls named here.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, and earlier clause 12 catalogues for
source form, names, expressions, execution constructs, units, connections, file access,
file position, CLOSE/OPEN context where already registered, and data-transfer
foundations. Unregistered dependencies explicitly retained: later clause 12 OPEN,
asynchronous transfer, I/O status, data transfer, internal-file, and input/output-item
rules; clause 13 formatting and modes; clause 15 defined I/O; clause 16 inquiry/status
intrinsics where future fixtures need them; clauses 3, 5, 14, 17, 18, and 19 where
terminology, constants, images, IEEE support, deleted features, or scope/lifetime
controls are reached.

<!-- END GENERATED 12.10.1 -->
