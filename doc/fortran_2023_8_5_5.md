# Fortran 2023 8.5.5: BIND attribute for data entities

**Source review: reviewed.** Effective content-bound status is computed by
`Registry.catalogue_review_state("8.5.5")`, not the raw review field.
The independent source gate in `doc/source_audits/batch_032.json` is historical;
source, fixture and observational-inventory adjudications remain separate.

## Bounded C819 module-placement subset

Seven compile/f2023 cases represent four C819 facets: one proper-module
admission, plus main-program, external-subroutine and module-contained-subroutine
negatives with their exact controls. Each repair deletes only `, bind(c)`.
The same `bound_value`, prior intrinsic USE of symbolic C_INT, scalar type,
complete context and executable body remain. The contained procedure declares
the variable in its own specification-part, not its outer module's.

All four positives are compile/positive-control evidence. No C counterpart,
link, runtime, SAVE-retention or definition-use credit is added. Each negative
needs the located BIND-variable/declaration-context cause, not bare keywords,
an echo, a different entity/type/kind error, unsupported recovery or a native
compiler failure. Ordinary zero/nonzero reports do not require fatal rejection
or a printed rule code. Only specifically qualified nonfatal reports can count.

3 C819 facets remain PENDING; the other requirements currently
have 48 pending facets (51 total in 8.5.5).
Submodule-local and the common/interface/result/dummy source-use boundaries are
not implemented. Generation preserves current unrelated requirements and all
independent administrative records; it does not approve or renew them.

Source: original J3/24-007, December 18, 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
All nine original units are on physical PDF121, printed107, ending at the
actual8.5.6 CODIMENSION heading. There is no CONTIGUOUS ownership here.
The reproduced section hash is
`e60829caa479a782e084ed04657748779d0c165c2988a5fa7f79a3ed9c2eec0d`.
Original body text and directed dependency extracts remain session evidence.

| Original unit | PDF121 lines | Treatment |
| --- | --- | --- |
| `p1` | 8-9 | Variable/common capability definition and external-linkage counterpart; no no-op effect |
| `R808` | 10 | Exact C/NAME syntax and scalar default-character constant expression |
| `C818`-`C821` | 11-14 | Entity categories, module variable placement, variable interoperability and every-member COMMON condition |
| `p2` | 15-16 | Conditional companion identifier validity after leading/trailing blank removal |
| Unnumbered NOTE | After16 | Universal-character-name/default-CHARACTER representation limit |
| `p3` | 17 | Common-block SAVE implication and permission to confirm it explicitly |

| Source distinction | Evidence/plan limit |
| --- | --- |
| Binding forms | Data declarations, BIND statements, derived types, ENUM and procedures retain their own syntax. C802/C803/C877 are not re-owned or waived by C818. |
| Names and labels | Fortran names, abstract binding-label values and decorated linker symbols are different. Explicit label case is significant; no ASCII byte algorithm is assumed. Global identifier restrictions still apply. |
| NAME cases | Omitted NAME has its18.9.2 default; explicit empty/all-blank NAME has no label. Remove blanks only, not arbitrary whitespace. Presence still triggers the relevant single-entity rules. |
| Companion |5.5.7 does not promise that any installed compiler is the selected companion or even that the mechanism is a C-conforming processor. Identifier/ABI evidence must be real and separately bound. |
| Types and kinds | C_INT is guaranteed valid. Other integer mappings, C_CHAR, C_BOOL and real/complex mappings have their stated availability conditions. No guessed kinds, widths, negative-kind declarations or success-shaped skips. |
| Data versus descriptors | Global BIND variables retain18.3.5/.6. The richer BIND-procedure descriptor/dummy facilities do not make global ALLOCATABLE/POINTER/coarray variables interoperable. |
| Arrays | The category criterion differs from correspondence with a particular C array;18.3.6p2 requires nonzero size. Neither zero-length C extensions nor a successful Fortran declaration prove that relationship. |
| COMMON | Bind the block, not its member variables. Every member must interoperate; declarations keep the same label and compatible storage relationship. C struct correspondence uses relative positions, not matching member names or guessed padding. |
| SAVE | The local p3 implication concerns the common block. Confirm with SAVE /block/, not SAVE on a member; ordinary module variables have a separate SAVE source. |

Future negative/control pairs must isolate the actual syntax, role,
attribute, member or interoperability property and retain a conforming
focused repair. A located unsupported facility, wrong kind/shape, missing
module, obsolescence warning, source echo, crash or recovery message is not
that diagnostic. The unnumbered companion-identifier restriction does not
invent a mandatory Fortran rejection policy.

Fortran source eligibility, a documented selected companion, matching C
objects/interfaces, successful ordered build/link and independent runtime
values remain separate claims. Only one processor may initially define a
linked variable, and18.9.1 does not require an associated C entity merely to
admit a Fortran BIND declaration.

The existing procedure bridge and assumed-descriptor fixtures keep their
owners and inputs. The recorded frozen-header qualification in batch008
is not repaired or re-probed: descriptor evidence requires the selected
Fortran processor's own header and consistent ABI, and an interface/version
failure precedes local propagation observations. No header or expected
version is substituted, and simple global-data tests are not automatically
CFI descriptor tests.

The original source-only integration is recorded in batch032.
Its older corpus and empty SourceUses descriptions are not current-main claims.
This subset starts from committed `94b6b1ea`; the existing R402 declaration-name
SourceUse, its review and all canonical links are retained without new credit.
Their effective state is derived from the current native registries.

<!-- BEGIN GENERATED 8.5.5 -->

### R808: C language-binding syntax and the qualified NAME expression

**Source:** 8.5.5 R808, physical PDF121, printed107, line10 **Class:** Syntax.

**Definition:** A language-binding-spec uses BIND with parenthesized C and optionally one following
comma/NAME= specification. The NAME expression is scalar, default CHARACTER and
constant. This grammar is reused in its permitted data/common/procedure consumers; the
superficially similar BIND(C) type/ENUM forms retain their own syntax. The spelling C
does not establish which actual companion mechanism was selected.

**Diagnostic obligation:** required.

**Facets:** `c-token`, `without-name`, `literal-name`, `constant-name-expression`, `empty-all-blank-name`, `single-name-specifier`, `name-order`, `scalar-name`, `character-name`, `constant-name`, `default-kind-name`, `role-and-label-source-use`.

**Oracle:** Qualified compile admissions and focused grammar/type/rank/constancy report-control
contrasts. Planned negatives retain an otherwise conforming complete module, supported
symbolic C_INT and one eligible receiver. Reporting capability is not mandatory fatal
rejection or prescribed English wording.

**Oracle limitation:** Source validity, companion identity/identifier rules and actual C interoperation are
separate. C802/C803/C877 remain defining owners; no duplicate C801 work or new proof
from keyword recognition. Reports must bind the real NAME/C-syntax property, subject and
source location, excluding echoes, generic expected/kind strings, unsupported
facilities, crashes, verifier/resource failures and recovery.

**Dependencies:** 4.1.2/4.2;5.5.7;8.2 C802/C803;8.6.4 R835/C877;10.1.12 R1031/C1013;7.5.2.1 R728;7.6.1
R760;15.6.2.1 R1531;18.9.2/18.10.2.

### C818: BIND is limited to the stated entity categories

**Source:** 8.5.5 C818, physical PDF121, printed107, line11 **Class:** Restriction.

**Definition:** An entity with BIND is a common block, variable, type or procedure. This category
restriction is not permission to use every BIND syntax on every category, to add BIND to
named constants, or to ignore the category's own interoperability and declaration
constraints.

**Diagnostic obligation:** required.

**Facets:** `common-block-role`, `module-variable-role`, `derived-type-role`, `procedure-role`, `named-constant-excluded`, `enum-definition-role`, `other-entity-source-use`.

**Oracle:** Eligible category contexts and one focused named-constant-to-variable repair, with
actual syntax and interoperability prerequisites. Other entity-role contrasts require
independently isolated source before diagnostic authoring.

**Oracle limitation:** The four-category list is not a universal BIND constructor. No incomplete interface,
prohibited elemental procedure, invalid derived type, common-member misuse or unrelated
declaration recovery can corroborate this category condition.

**Dependencies:** 8.2 C803/C807/C810;8.6.4;8.10.2.1 C8123;7.5.2.1
R728;7.6.1/.2;15.4.2.2/15.4.3.6/15.6.2.1;18.3.2-.7.

### C819: A BIND variable is declared in a module specification-part

**Source:** 8.5.5 C819, physical PDF121, printed107, line12 **Class:** Restriction.

**Definition:** A variable that has BIND is declared in the specification-part of a module. The rule
applies to the variable entity, not to a bound common block as a whole. A contained
procedure, BLOCK or submodule does not become a module specification-part merely through
textual nesting or ancestry.

**Diagnostic obligation:** required.

**Facets:** `module-admission`, `main-local`, `external-procedure-local`, `contained-procedure-local`, `submodule-local`, `common-placement-source-use`, `interface-result-dummy-source-use`.

**Oracle:** Seven complete compile/f2023 fixtures represent module-admission, main-local,
external-procedure-local and contained-procedure-local. A proper module
specification-part admits one ordinary scalar INTEGER(C_INT),BIND(C) variable. Three
negatives declare that same eligible variable in a main program, an external subroutine
and a module-contained subroutine's own specification-part. Each control deletes only ',
bind(c)', preserving every other byte, including the local entity, intrinsic USE, type,
rank, executable assignment and complete outer context. Admission requires successful
object compilation; the controls are positive-control evidence, not runtime effects. A
negative requires an actual located BIND-variable/module-declaration-context report at
its sole entity declaration. Ordinary zero/nonzero and specifically qualified nonfatal
reporting may satisfy that capability; neither fatal rejection, a printed C819 code nor
generic keyword output is required or sufficient.

**Oracle limitation:** Submodule placement, common-placement source-use and interface/result/dummy source-use
remain pending. All six other 8.5.5 requirements and their plans are preserved. Prior
intrinsic ISO_C_BINDING makes the guaranteed C_INT kind accessible in the actual
declaring scope. Each variable is scalar, noncoarray, nonpointer, nonallocatable,
nondummy and not a function result or named constant, with no initializer or duplicate
binding. Under 18.3.1/18.3.5 its type and parameters are interoperable; 18.9.1p4
requires no associated C entity. No kind code, width, ABI, foreign counterpart, link,
runtime retention, module SAVE or S8.5.5-002 effect is observed. Wrong
entity/type/kind/attribute, missing module/interface, malformed declaration, source
echo, bare BIND/module words, unsupported/unimplemented recovery, Internal/ASR/verifier,
crash, resource and timeout failures are noncredit. A literal predicate without a
supported causal report remains unqualified, not compiler-consensus proof. Source,
fixture, observational-inventory and processor-mode qualification remain independent;
generation preserves their administrative records without renewing them.

**Dependencies:** 8.2 R801/R803/C803;8.6.4;8.10.2.1 C8123;14.2.1/.3 C1412;15.4/15.6;18.9.1.

### C820: A BIND variable meets the original interoperability criteria

**Source:** 8.5.5 C820, physical PDF121, printed107, line13; directed18.3 **Class:** Restriction.

**Definition:** A BIND variable is interoperable under18.3. Named scalars need interoperable
type/parameters, no coarray, neither ALLOCATABLE nor POINTER, and the stated
nonassumed/constant CHARACTER length. Named arrays require interoperable
type/parameters, noncoarray status and explicit shape or assumed size, with the
CHARACTER length conditions. These category criteria are distinct from compatibility
with a particular C object, which has additional conditions such as nonzero array size.

**Diagnostic obligation:** required.

**Facets:** `integer-scalar-admission`, `integer-array-admission`, `pointer-excluded`, `allocatable-excluded`, `coarray-excluded`, `character-length-domain`, `derived-type-interoperability`, `type-kind-mapping-source-use`, `deferred-assumed-parameter-source-use`, `descriptor-route-boundary`, `companion-variable-runtime-gate`.

**Oracle:** Complete source/category admissions and isolated pointer, allocation, coarray,
CHARACTER-length and derived-type report/control relations where their independent
premises hold. Multilang evidence additionally binds the selected companion, object
declarations, source inputs, ordered build/link and defined runtime values.

**Oracle limitation:** No unsupported kind, invalid COMMON member, illegal shape declaration, incomplete
interface, raw byte layout or undocumented ABI is an oracle. Optional mapping absence is
not permission to skip required C_INT behavior or emit success-shaped coverage;
unqualified cases remain pending. Source, compiler acceptance, C object compatibility
and header/runtime consistency are separate claims.

**Dependencies:** 18.2.2;18.3.1 Tables18.1/18.2,18.3.2-.7;7.2 C702;8.2 C808;8.5.8;15.6.2.1
C1563-C1568;18.9.1/.2;19.5.1.5; existing descriptor/c_interop inputs and batch_008
header qualification.

### C821: Every member of a BIND common block is interoperable

**Source:** 8.5.5 C821, physical PDF121, printed107, line14 **Class:** Restriction.

**Definition:** Each variable in a common block that has BIND is interoperable. BIND belongs to the
block in this condition, not to each member. General COMMON eligibility, same-block
storage consistency and the chosen C counterpart remain additional conditions;
satisfying a member-wise type test alone does not prove multilang interoperation.

**Diagnostic obligation:** required.

**Facets:** `all-members-admission`, `pointer-member-first`, `pointer-member-last`, `sequence-type-member-source-use`, `character-member-domain`, `common-form-exclusions-source-use`, `companion-common-runtime-gate`.

**Oracle:** Member-specific compile reports and focused repairs distinguish which otherwise valid
COMMON member lacks interoperability. Actual C common-block evidence requires the
full18.9.1 relationship and compatible typed companion objects; it is not supplied by
declaration syntax.

**Oracle limitation:** COMMON obsolescence warnings, unsupported binding implementations, invalid general
COMMON membership, layout guesses and missing companion symbols are not interchangeable
with this condition. No new fixture, profile, C companion, header selection or approval
is supplied by the source plan.

**Dependencies:** 8.6.4 R835/R836/C877;8.10.2.1 C8121-C8125
and8.10.2.2-.5;18.2.2;18.3.1/.4/.5/.6;18.9.1p3;19.5.1.5.

### S8.5.5-001: A nonblank NAME value denotes a valid companion identifier

**Source:** 8.5.5 p2, physical PDF121, printed107, lines15-16 **Class:** Restriction.

**Definition:** For the supplied scalar default-character constant NAME expression, discard leading and
trailing blanks. If the remaining length is nonzero, that value is valid as an
identifier on the selected companion processor. This is not a general whitespace
normalization, a Fortran-name spelling restriction, an ASCII folding algorithm or an
unconditional C compiler/ABI assumption.

**Diagnostic obligation:** not-required.

**Facets:** `blank-trimming`, `valid-companion-identifier`, `invalid-companion-policy-gate`, `empty-blank-outside-premise`, `case-and-default-labels`, `repertoire-interface-gate`.

**Oracle:** Original label/value rules plus actual companion/interface evidence determine
applicability. Qualified positive multilang observations can compare independent typed
values through an exact label; optional prose diagnostic policy is not silently promoted
to a standard requirement.

**Oracle limitation:** The source restriction remains real without a mandatory local diagnostic.
Empty/all-blank, default-name and explicit-name cases stay distinct, and19.2/18.9.1
global-entity restrictions are not waived by case significance. A missing mapping or
interface mechanism leaves the relevant facet pending, not a passing skip or an
arbitrary runtime/link-failure oracle.

**Dependencies:** R808;C802/C877;5.5.7;8.5.5 NOTE;18.9.1/.2;18.10.2 for distinct procedure-label
exceptions;19.2/19.5.1.5.

### S8.5.5-002: BIND on a common block implies SAVE for that block

**Source:** 8.5.5 p3, physical PDF121, printed107, line17 **Class:** Effect.

**Definition:** Giving a common block BIND also gives that common block SAVE. Its member retention
follows the common-block SAVE rules, and an explicit SAVE of the block may confirm the
implication. This is not BIND on each member, explicit SAVE on a member, a new
module-variable SAVE rule or proof of C interoperability.

**Diagnostic obligation:** not-required.

**Facets:** `named-common-retention`, `explicit-save-confirmation`, `member-save-source-use`, `consistent-common-labels`, `module-variable-save-distinction`.

**Oracle:** Independently expected defined values across procedure returns, with all BIND/common
declarations consistent and no other active scope supplying an unrelated SAVE premise.
This is a Fortran retention effect; a separate documented multilang object test is
needed for actual C linkage/interoperation.

**Oracle limitation:** No undefined-value trap, storage-address or allocator-layout oracle, implicit-pointer
assumption or main/module-only retention proxy. Source approval and actual fixture
approval are independent; implementation status is carried by pending facets and the
review/evidence registries, not by a fixed review fingerprint.

**Dependencies:** 8.5.16p1/p3/p4,C862;8.6.4;8.6.14;8.10.2.1-.5;18.2.2 C_INT;18.9.1p3/p4;19.6.6p1(3);
source p3 confirmation permission.

<!-- END GENERATED 8.5.5 -->
