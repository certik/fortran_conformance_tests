# Fortran 2023 16.9.121: LEADZ

The canonical catalogue is `doc/catalogues/leadz_16_9_121.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.121")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.121 -->

### S16.9.121-001: LEADZ I argument is integer

**Source:** S16.9.121, J3/24-007, 18 December 2023, physical PDF426. **Class:** Restriction.

**Definition:** I shall be of type integer.

**Diagnostic obligation:** not-required.

**Facets:** `LEADZ-I-integer`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-sequence observations fixed by 16.9 and its dependencies. Argument
shall-restrictions in these intrinsic sections are unnumbered program restrictions and
are not promoted to diagnostic facets. Positive plans establish a non-default or
opposite sentinel first and include a feature mutation such as changing the intrinsic
name, argument order, KIND value, DIM value, or a character operand used to discriminate
the result.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
LEADZ p3 admits integer actual arguments, not BOZ actual arguments; BOZ literal coverage
is therefore not registered for this intrinsic. Negative numeric integer values are not
used as expected bit sequences because 16.3.1 makes that interpretation processor
dependent; plans use NOT(0) and IBSET(0,BIT_SIZE(0)-1) directly in bit context instead.
Grep of tests/ and tests/fixtures/*/fixture.json found no LEADZ fixture binding.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: 7.7 BOZ literal interpretation, 16.3 integer bit model, 16.3.2 bit sequence
comparison, 16.4 model inquiries, sibling 16.9 intrinsic procedures named by result
formulas, and clauses 3, 5, 14, 15, 17, 18, and 19 where future fixtures need terms,
constants, programs, procedure interfaces, IEEE support, deleted features, or scope
rules.

### S16.9.121-002: LEADZ result is default integer

**Source:** S16.9.121, J3/24-007, 18 December 2023, physical PDF426. **Class:** Effect.

**Definition:** The LEADZ result has default integer type.

**Diagnostic obligation:** not-required.

**Facets:** `LEADZ-result-default-integer`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-sequence observations fixed by 16.9 and its dependencies. Argument
shall-restrictions in these intrinsic sections are unnumbered program restrictions and
are not promoted to diagnostic facets. Positive plans establish a non-default or
opposite sentinel first and include a feature mutation such as changing the intrinsic
name, argument order, KIND value, DIM value, or a character operand used to discriminate
the result.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
LEADZ p3 admits integer actual arguments, not BOZ actual arguments; BOZ literal coverage
is therefore not registered for this intrinsic. Negative numeric integer values are not
used as expected bit sequences because 16.3.1 makes that interpretation processor
dependent; plans use NOT(0) and IBSET(0,BIT_SIZE(0)-1) directly in bit context instead.
Grep of tests/ and tests/fixtures/*/fixture.json found no LEADZ fixture binding.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: 7.7 BOZ literal interpretation, 16.3 integer bit model, 16.3.2 bit sequence
comparison, 16.4 model inquiries, sibling 16.9 intrinsic procedures named by result
formulas, and clauses 3, 5, 14, 15, 17, 18, and 19 where future fixtures need terms,
constants, programs, procedure interfaces, IEEE support, deleted features, or scope
rules.

### S16.9.121-003: LEADZ counts leading zero bits in the 16.3 integer bit model

**Source:** S16.9.121, J3/24-007, 18 December 2023, physical PDF426. **Class:** Effect.

**Definition:** For all-zero bits LEADZ returns BIT_SIZE(I); otherwise it returns BIT_SIZE(I)-1-k, where
k is the position of the leftmost 1 bit in I using the 16.3 bit model.

**Diagnostic obligation:** not-required.

**Facets:** `LEADZ-all-zero-bits`, `LEADZ-one-low-bit-count`, `LEADZ-leftmost-one-count-zero`, `LEADZ-all-one-bits`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-sequence observations fixed by 16.9 and its dependencies. Argument
shall-restrictions in these intrinsic sections are unnumbered program restrictions and
are not promoted to diagnostic facets. Positive plans establish a non-default or
opposite sentinel first and include a feature mutation such as changing the intrinsic
name, argument order, KIND value, DIM value, or a character operand used to discriminate
the result.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
LEADZ p3 admits integer actual arguments, not BOZ actual arguments; BOZ literal coverage
is therefore not registered for this intrinsic. Negative numeric integer values are not
used as expected bit sequences because 16.3.1 makes that interpretation processor
dependent; plans use NOT(0) and IBSET(0,BIT_SIZE(0)-1) directly in bit context instead.
Grep of tests/ and tests/fixtures/*/fixture.json found no LEADZ fixture binding.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: 7.7 BOZ literal interpretation, 16.3 integer bit model, 16.3.2 bit sequence
comparison, 16.4 model inquiries, sibling 16.9 intrinsic procedures named by result
formulas, and clauses 3, 5, 14, 15, 17, 18, and 19 where future fixtures need terms,
constants, programs, procedure interfaces, IEEE support, deleted features, or scope
rules.

<!-- END GENERATED 16.9.121 -->
