# Fortran 2023 8.5.20: VOLATILE attribute

Effective source status is `Registry.catalogue_review_state("8.5.20")`,
not a cached claim from the raw review field. Source classification,
fixture approval and compiler observations remain separate.

The canonical catalogue is `doc/catalogues/volatile_attribute_8_5_20.json`.
The eight original units span physical PDF pages133-134 of J3/24-007,
December18,2023,688pages,SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The next heading is8.6 Attribute specification statements.

The possibility of changes by external means is not proof that an external
event occurred, a mandatory test workload, or a memory-layout, atomicity,
synchronization or latency contract. Pointer association and allocatable
allocation properties are distinct. Undefined data and unavailable
properties must not be queried merely because an object is VOLATILE.

C870 excludes INTENT(IN) dummies, including pointer dummies. C871 restricts
new local specification for coarray-bearing USE/HOST-associated objects;
it does not ban access to an inherited VOLATILE coarray. C872 has its own
BLOCK construct-entity condition. A USE statement in BLOCK creates construct
entities under19.4, but that does not waive C871.

Noncoarray association permission does not waive the VOLATILE equivalence
for a dummy with a coarray potential subobject under15.5.2.5p22. Coarray
dummy/actual and pointer/target equivalences retain15.5.2.9 and10.2.2.3
ownership. Scoped BLOCK specification is not a global redeclaration or
automatic outside-attribute removal.

The two `should` statements in p3 and both processor recommendations in p5
remain recommendations, not mandatory failure thresholds or informative
NOTE text. The intervening p3 `shall` has an actual external-target-event
premise. Source accounting does not manufacture an event or a compulsory
processor documentation interface.

Subobject propagation uses the actual9.4.2p5 boundary. A rightmost sole
pointer component is a subobject in association-status contexts, but its
dereferenced target is not. A longer path through a pointer does not
inherit this rule from the original base. Potential-subobject membership
for coarray restrictions follows its separately defined nonpointer paths.

The source-only packet introduces pending facet plans, not execution
evidence. An unchanged-value or no-op program does not supply a portable
attribute observer. Finite source/interface/processor evidence needs its
own implemented and reviewed mechanism. Current facet states remain in
the catalogue; this source registration itself adds no cases, compiler
runs, profiles, reviews, baseline changes or source-use records.

<!-- BEGIN GENERATED 8.5.20 -->

### C870: VOLATILE applies to a variable other than an INTENT(IN) dummy

**Source:** 8.5.20 C870, physical PDF134 **Class:** Restriction.

**Definition:** The VOLATILE entity is a variable and is not an INTENT(IN) dummy argument. The exclusion
applies to a pointer dummy as well as a nonpointer dummy; target operations permitted
through an IN pointer do not remove the dummy-attribute restriction.

**Diagnostic obligation:** required.

**Facets:** `ordinary-variable-admission`, `intent-in-data-exclusion`, `intent-in-pointer-exclusion`, `nonvariable-role-source-boundary`.

**Oracle:** Complete variable declarations and minimally repaired INTENT(IN) conflicts, with
independent subject-role and source-origin review. All new plans are pending.

**Oracle limitation:** Required reporting is not forced rejection, exact English or printed-code policy. Do not
require a runtime trap, inspect an undefined pointer or credit generic
unsupported-variable/attribute diagnostics.

**Dependencies:** 3.42/3.151;5.4.3.2.2;8.2 R802;8.5.10 C845-C848;8.5.13;8.6.17;15.4.2.2;15.5.2.8;4.2p2.

### C871: USE or HOST access restricts new VOLATILE specification for coarray-bearing objects

**Source:** 8.5.20 C871, physical PDF134 **Class:** Restriction.

**Definition:** VOLATILE is not specified locally for a use- or host-associated coarray, or a variable
with a coarray potential subobject component. Inherited attributes are distinct from the
prohibited local specification. Potential-subobject membership follows the actual
nonpointer component paths.

**Diagnostic obligation:** required.

**Facets:** `use-associated-coarray-exclusion`, `host-associated-coarray-exclusion`, `use-associated-component-exclusion`, `host-associated-component-exclusion`, `inherited-attribute-boundary`, `pointer-path-source-boundary`.

**Oracle:** Distinct USE and HOST specification contrasts on real
coarray/coarray-potential-subobject subjects, with valid minimal controls and explicit
source relationships.

**Oracle limitation:** No compiler run or capability profile is supplied here. Canonical module, host, coarray
component, attribute and association conditions retain their owners. Silent acceptance
or unrelated support/link failures are not successful reports.

**Dependencies:** 3.30.3;7.5.1p5/p6;7.5.4.2;8.5.6.1 C823/C826;8.8;14.2.2p2/p9/p10;19.5.1.4;4.2p2.

### C872: BLOCK VOLATILE specification distinguishes construct entities from enclosing coarray-bearing objects

**Source:** 8.5.20 C872, physical PDF134 **Class:** Restriction.

**Definition:** Inside BLOCK, VOLATILE is not specified for a coarray or a variable with a coarray
potential subobject component unless that entity is a construct entity of the BLOCK.
This condition does not override C871 or other declaration restrictions.

**Diagnostic obligation:** required.

**Facets:** `enclosing-coarray-exclusion`, `enclosing-component-exclusion`, `declared-coarray-construct-control`, `declared-component-construct-control`, `use-created-construct-boundary`.

**Oracle:** Complete BLOCK/enclosing identity contrasts and legitimate construct declarations, with
actual object ownership and narrowly located causes.

**Oracle limitation:** Do not force hidden host association, redeclare a shadow object as a repair, waive C871,
or manufacture a coarray runtime/visibility observer. All facets remain pending.

**Dependencies:** C871;11.1.4p2/C1107-C1110;19.4p3;5.4.3.2.2;8.5.6.1;7.5.1p5;8.8;4.2p2.

### S8.5.20-001: A BLOCK specification gives a nonlocal object VOLATILE within that construct

**Source:** 8.5.20 p2 second sentence, physical PDF134 **Class:** Effect.

**Definition:** A VOLATILE specification in BLOCK applies within the construct to an object that is not
a local variable of that BLOCK, including when the object lacks the attribute outside.
It does not create a new object, propagate the specification globally, or remove an
existing outside attribute. The qualified coarray exclusions still apply.

**Diagnostic obligation:** not-required.

**Facets:** `enclosing-object-scoped-attribute`, `associated-object-scoped-attribute`, `outside-scope-nonpropagation`, `declaration-and-import-boundaries`.

**Oracle:** A source/interface/attribute mechanism with a declared finite denominator and
independently established identity; no no-op or generic timing loop substitutes for that
mechanism.

**Oracle limitation:** No implementation is supplied. Recommendations about recent values or prompt external
availability do not make a mandatory visibility/performance oracle for this
scoped-attribute effect.

**Dependencies:** C871/C872;11.1.4p2;19.4p3;5.4.3.2.2;8.8;14.2.2;19.5.1.4;15.5.2.5p22.

### S8.5.20-002: Actual external target events require the pointer to be VOLATILE

**Source:** 8.5.20 p3 conditional shall sentence, physical PDF134 **Class:** Restriction.

**Definition:** If the pointer's target is referenced, defined or made undefined by means not specified
by the program, the pointer has VOLATILE. These actual events, not merely a target
attribute spelling, are the conditional premise. The adjacent general pointer and
EQUIVALENCE advice remains recommendations.

**Diagnostic obligation:** not-required.

**Facets:** `external-reference-premise`, `external-definition-premise`, `external-undefinition-premise`, `coarray-equivalence-source-boundary`.

**Oracle:** Content-bound external-premise and pointer-attribute evidence, with context-only
handling of invalid/undefined states. No external protocol or executable fixture is
approved by this source catalogue.

**Oracle limitation:** The source does not require compile-time detection of an arbitrary external event. It
supplies no descriptor ABI, atomicity, synchronization, exact latency or safe
undefined-value inquiry. The first sentence's target-VOLATILE recommendation is not
merged with this separate shall.

**Dependencies:** 8.5.20p1/p3/p5;8.5.14;10.2.2.3p7;15.5.2.13;19.5.2;19.6.2;4.2p2/p5.

### S8.5.20-003: VOLATILE propagates to the object's actual subobjects

**Source:** 8.5.20 p4, physical PDF134 **Class:** Effect.

**Definition:** Every subobject of a VOLATILE object also has VOLATILE. Actual subobject status is
context dependent for pointer components: a rightmost sole pointer is a subobject for
association-status purposes, but its dereferenced target is not a subobject of that
base. A path through an earlier pointer likewise does not supply propagation from the
original base.

**Diagnostic obligation:** not-required.

**Facets:** `array-subobjects`, `character-substring`, `nonpointer-component-path`, `pointer-association-context`, `pointer-dereference-boundary`.

**Oracle:** Finite, independently reviewed subobject and attribute-use evidence, with context and
lifetime bound to the actual entities; no synthetic passing program or unqualified
optimization experiment.

**Oracle limitation:** There is no implemented general attribute-use evidence mechanism for these facets. No
new SourceUses inventory, case link, compiler observation or passing effect is claimed.
The EQUIVALENCE recommendation and p5 processor advice are not mandatory subobject
propagation.

**Dependencies:** 3.138/3.30.3;5.4.3.2.2;7.5.1p5;9.4.2p5/note1;9.5;19.6.2;8.5.20p3/p5.

<!-- END GENERATED 8.5.20 -->
