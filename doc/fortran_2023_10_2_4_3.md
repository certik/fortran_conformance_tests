# Fortran 2023: 10.2.4.3 The FORALL statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 10.2.4.3 -->

### S10.2.4.3-001: A FORALL statement controls one assignment or pointer assignment with index values and an optional mask

**Source:** 10.2.4.3 p1; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** The FORALL statement allows a single assignment statement or pointer assignment
statement to be controlled by a set of index values and an optional mask expression.

**Diagnostic obligation:** not-required.

**Facets:** `single-assignment-statement-control`, `optional-mask-statement-control`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.4.1 R1054 and 10.2.4.2 execution in this packet;10.2.1/10.2.2 registered.

### R1056: A FORALL statement is FORALL, a concurrent header, and a FORALL assignment statement

**Source:** 10.2.4.3 R1056; J3/24-007 PDF p201. **Class:** Syntax.

**Definition:** The forall-stmt production is FORALL followed by a concurrent-header and a
forall-assignment-stmt.

**Diagnostic obligation:** not-required.

**Facets:** `assignment-forall-stmt-form`, `pointer-assignment-forall-stmt-form`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 11.1.7 concurrent-header NOT YET REGISTERED;10.2.4.1 R1054 in this packet.

### S10.2.4.3-002: A FORALL statement is equivalent to a single-body FORALL construct containing its assignment

**Source:** 10.2.4.3 p2; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** A FORALL statement is equivalent to a FORALL construct containing a single
forall-body-construct that is a forall-assignment-stmt.

**Diagnostic obligation:** not-required.

**Facets:** `statement-equivalent-final-values`, `statement-equivalent-mask-selection`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.4.2 execution in this packet;10.2.4.1 FORALL construct syntax in this packet.

### S10.2.4.3-003: The scope of a FORALL-statement index name is the statement itself

**Source:** 10.2.4.3 p3; J3/24-007 PDF p201. **Class:** Effect.

**Definition:** The scope of an index-name in a forall-stmt is the statement itself, as described in
19.4.

**Diagnostic obligation:** not-required.

**Facets:** `outer-variable-same-name-unaffected`, `index-name-not-visible-after-statement`.

**Oracle:** The observable part owned here is statement-only scope as final outer-variable
preservation. Diagnostics for name visibility outside the statement depend on 19.4 and
are not approved by this unit alone.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 19.4 scope of index names NOT YET REGISTERED;8 names registered.

<!-- END GENERATED 10.2.4.3 -->
