# Fortran 2023 16.9.177: SAME_TYPE_AS

The canonical catalogue is `doc/catalogues/same_type_as_intrinsic_16_9_177.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.177")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, note1, note2, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.177 -->

### S16.9.177-001: SAME_TYPE_AS argument restrictions

**Source:** S16.9.177, J3/24-007, 18 December 2023, physical PDF452. **Class:** Restriction.

**Definition:** A and B shall each be objects of extensible declared type or unlimited polymorphic, and
a polymorphic pointer actual shall not have undefined association status.

**Diagnostic obligation:** not-required.

**Facets:** `SAME_TYPE_AS-A-extensible-or-unlimited-polymorphic`, `SAME_TYPE_AS-B-extensible-or-unlimited-polymorphic`, `SAME_TYPE_AS-polymorphic-pointer-association-status-defined`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.177-002: SAME_TYPE_AS result characteristics

**Source:** S16.9.177, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** The SAME_TYPE_AS result is a default logical scalar.

**Diagnostic obligation:** not-required.

**Facets:** `SAME_TYPE_AS-result-default-logical-scalar`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.177-003: SAME_TYPE_AS compares extensible dynamic types exactly

**Source:** S16.9.177, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** With A or B of extensible dynamic type, the result is true exactly for the same dynamic
type and false for different dynamic types; 7.3.2.3 supplies declared-type dynamic types
for disassociated CLASS(t) pointers and unallocated CLASS(t) allocatables.

**Diagnostic obligation:** not-required.

**Facets:** `SAME_TYPE_AS-same-extensible-dynamic-type-true`, `SAME_TYPE_AS-different-extensible-dynamic-type-false`, `SAME_TYPE_AS-disassociated-class-pointer-declared-type-true`, `SAME_TYPE_AS-disassociated-class-pointer-extension-false`, `SAME_TYPE_AS-unallocated-class-allocatable-declared-type-true`, `SAME_TYPE_AS-unallocated-class-allocatable-extension-false`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms. External normative dependency for disassociated or
unallocated polymorphic entities: 7.3.2.3 p3 says the declared type of CLASS(type-name)
is the specified type, 7.3.2.3 p4 says CLASS(*) is not declared to have a type and is
not considered to have the same declared type as any other entity, and 7.3.2.3 p5 says
the dynamic type of an unallocated allocatable object or disassociated pointer is the
same as its declared type.

### S16.9.177-004: SAME_TYPE_AS nonextensible results and CLASS(*) no-declared-type cases lack exact truth-value plans

**Source:** S16.9.177, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** With neither A nor B having extensible dynamic type, the SAME_TYPE_AS result is
processor dependent. For disassociated CLASS(*) pointers or unallocated CLASS(*)
allocatables, 7.3.2.3 determines that the entity has no declared type, so the CLASS(t)
declared-type oracle is not available.

**Diagnostic obligation:** not-required.

**Facets:** `SAME_TYPE_AS-nonextensible-result-processor-dependent`, `SAME_TYPE_AS-unlimited-polymorphic-no-declared-type-no-exact-oracle`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms. External normative dependency for disassociated or
unallocated polymorphic entities: 7.3.2.3 p3 says the declared type of CLASS(type-name)
is the specified type, 7.3.2.3 p4 says CLASS(*) is not declared to have a type and is
not considered to have the same declared type as any other entity, and 7.3.2.3 p5 says
the dynamic type of an unallocated allocatable object or disassociated pointer is the
same as its declared type.

<!-- END GENERATED 16.9.177 -->
