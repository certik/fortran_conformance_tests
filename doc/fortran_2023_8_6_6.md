# Fortran 2023 8.6.6: CONTIGUOUS statement

Source review is recorded in batch069. Effective review state comes from
`Registry.catalogue_review_state("8.6.6")`; current facet states remain in
the catalogue. Source classification does not approve fixtures or execution.
The canonical catalogue is `doc/catalogues/contiguous_statement_8_6_6.json`.

The two original base units, R839 and p1, are on physical PDF136, printed122,
of J3/24-007, December18,2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The complete section hash is
`4d1916a786ba1e3feefa8d771a7f7bba9abc2eed2f3708837b6c6bfdc149e803`.
The scope ends at the next heading on the same physical page; no
continuation, note, table or neighboring section body belongs to this scope.

The actual production uses **object-name-list**, not array-name-list.
Explicit R804/C810 defines object-name; it overrides the assumed R402
convention. R401 supplies the nonempty comma-separated list. The double
colon is optional, but the names are mandatory. Array/coarray declarators,
component references and other designators are not supplied by R839.

C830 separately admits array pointers, assumed-shape arrays and
assumed-rank dummy data objects. An ordinary scalar, scalar pointer,
explicit-shape array, fixed-rank allocatable array or assumed-size dummy
does not become eligible merely because it is named, is a dummy, or is
actually contiguous. RANK(0) is scalar, not an assumed-shape array.
A genuine assumed-rank dummy can have a scalar effective argument; the
declared category, rather than an assumed array-name production, matters.

The shape/rank/pointer context must be valid independently of this
statement. A syntax control can declare a pointer array and name it in
CONTIGUOUS without reading its initially undefined association or payload.
The statement neither associates a target nor makes an unavailable
inquiry legal. IS_CONTIGUOUS's actual argument/state conditions and
rank-zero result remain its intrinsic owner's rules.

One explicit route is not two: a CONTIGUOUS type-declaration attribute and
a later CONTIGUOUS statement on the same local entity invoke C815.
USE-associated re-attribution, host identity and BLOCK construct entities
retain their separate rules. CONTIGUOUS is not among BLOCK's special
ASYNCHRONOUS/IMPORT/VOLATILE exceptions; a clearly local control supplies
its own eligible declaration rather than guessing that an outer object
was modified.

P1 is a normative statement-to-attribute definition, not another local
runtime effect. Canonical8.5.7 owns dummy contiguity and pointer-target
semantics;9.5.4 defines simply contiguous forms. Actual-argument
definability, type/kind/rank and C1548/C1549/C1550 conditions are not waived.
No particular copy-in/out, address, descriptor or allocation mechanism is
required by the statement.

The existing14C830 compile cases and two S8.5.7-001 runtime effects keep
their source, owners, roles, fingerprints, reviews and observations.
Their relevant CONTIGUOUS occurrences are type-declaration attributes;
none is a standalone R839 statement witness. Runtime failures, compiler
ICEs and UNTESTED sensitivity plans cannot be promoted, hidden or turned
into statement evidence by this registration.

This registration adds no compiler run, fixture, profile, S runtime
classifier, canonical link, SourceUse, fixture approval or baseline update.
The frozen author packet excluded its local index overlay and left its
source/inventory reviews unrenewed. Batch069 separately registers the
reviewed source and inventory context without relabelling prior executions.

<!-- BEGIN GENERATED 8.6.6 -->

### R839: A CONTIGUOUS statement has an optional double colon and a nonempty object-name list

**Source:** 8.6.6 R839, J3/24-007, 18 December 2023, physical PDF136, printed122. **Class:** Syntax.

**Definition:** The CONTIGUOUS keyword is followed by an optional double colon and a nonempty
comma-separated list of object names. R804/C810 supplies the explicit object-name
production, rather than an assumed R402 or an array-name production. R839 specifies the
existing CONTIGUOUS attribute; it supplies no type, array/coarray specification, pointer
association or executable contiguity test. Every subject must independently satisfy C830
and the declaration/scope rules.

**Diagnostic obligation:** required.

**Facets:** `double-colon-form`, `no-double-colon-form`, `multiple-distinct-eligible-objects`, `missing-object-list`, `list-separator-repair`, `bare-name-not-declarator-or-designator`, `entity-category-and-rank-source`, `single-specification-and-order-source`, `use-host-block-source-identity`, `attribute-and-runtime-evidence-boundary`.

**Oracle:** Complete compile/positive-controls for actual standalone statement forms, plus
source-minimal missing-list, list-separator and whole-name grammar contrasts with valid
C830 subjects. Full cases must isolate the intended syntax relation and retain required
source/interface prerequisites. The explicit name production and canonical
attribute/shape/state owners remain separate; all source/use relationships require
independent adjudication before credit.

**Oracle limitation:** All facets are pending. Numbered syntax reporting capability does not mandate fatal
rejection, a printed R839 or specific English. Bare array/CONTIGUOUS words, wrong entity
categories, incompatible shape, missing modules/interfaces, unrelated state/definability
failures, unsupported facilities, source echoes, quoted examples, Internal/ASR/verifier
errors, crashes and resource failures cannot qualify. Optional colons do not make the
list optional or admit inline rank/shape syntax. Declaration admission is not
association, allocation, defined payload, simply-contiguous actual availability or
runtime IS_CONTIGUOUS evidence. Unallocated/disassociated/undefined objects are not read
or queried without the intrinsic's exact premises, and no runtime requirement is
downgraded to compile-only. Existing source, case, role, link, SourceUses, baseline and
observation records are neither changed nor renewed.

**Dependencies:** 4.1.1;4.1.3R401/R402/p2;4.2p2;8.1p1;8.2R802-R804/C810;8.5.1p1/C815;8.5.7C830/p1-p4/note3;8.5.8.1-.5/.7;8.5.17p2/C863/C864;7.5.4.1C757;9.5.4;10.2.2.3p6;11.1.4p2;14.2.2p9;15.4.2.2;15.5.2.4/.5/.8/.13;16.9.115;19.4p3;19.5.1.4;19.5.2.2/.5.

<!-- END GENERATED 8.6.6 -->
