# Fortran 2023 13.7.2.3.5: ES editing

The canonical catalogue is `doc/catalogues/es_editing_13_7_2_3_5.json`. Effective review state comes from `Registry.catalogue_review_state("13.7.2.3.5")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Seven base units including Table 13.3 and an example note. The table was checked row-by-row against the PDF image; example rows are informative and not used as independent requirements.

<!-- BEGIN EN ES EDITING FIXTURES -->
## Executable ES editing fixtures

This packet adds nine complete internal-file programs for selected 13.7.2.3.5 ES output facets. Each writes an exact binary default-real value to a CHARACTER variable initialized with # sentinels, uses SS to suppress optional plus signs, checks LEN before equality, and keeps the ES/EN/E descriptor-substitution matrix permanent in the generator.
<!-- END EN ES EDITING FIXTURES -->

<!-- BEGIN GENERATED 13.7.2.3.5 -->

### S13.7.2.3.5-001: ES output uses scientific notation and ignores scale factor

**Source:** S13.7.2.3.5, J3/24-007, 18 December 2023, physical PDF299. **Class:** Effect.

**Definition:** ES output uses scientific notation with significand magnitude in [1,10) except zero;
scale factor has no output effect.

**Diagnostic obligation:** not-required.

**Facets:** `ES-scientific-notation`, `ES-significand-range`, `ES-scale-factor-no-effect`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.5-001 ES runtime fixtures: complete internal-file programs write exact
default-real values using SS and explicit ES w.dE2 fields. 0.5 expects '   5.000E-01',
exercising scientific notation; 0.125 expects '   1.250E-01', exercising the
1<=significand<10 interval; the scale-factor fixture writes 0.5 with (SS,2P,ES12.3E2)
and with a 0P control and expects the same field because ES output ignores scale factor.
Descriptor substitutions ES<->EN, ES<->E, w, d, and e mutations are permanent and fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

ES-editing fixture boundaries: this packet discharges only the selected ES output facets
removed from pending above. It deliberately leaves ES input, IEEE infinity/NaN output,
ES w.d alternative exponent spellings, exponent magnitudes above 99, E0/w=0
selected-width forms, zero exponent plus, diagnostics, external files, DECIMAL=COMMA,
nondefault sign mode, and rounded/inexact decimal cases pending. All positive outputs
use SS and exact binary values whose discarded decimal digits are zero.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.5-002: ES descriptor forms and exponent digit count

**Source:** S13.7.2.3.5, J3/24-007, 18 December 2023, physical PDF299. **Class:** Effect.

**Definition:** ES w.d and ES w.d E e occupy w positions except w=0 selects output width; fractional
part has d digits; positive e fixes exponent digits, otherwise minimum digits are used.

**Diagnostic obligation:** not-required.

**Facets:** `ES-field-width`, `ES-fractional-digits`, `ES-exponent-digit-count`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.5-002 ES descriptor-form runtime fixtures: complete internal-file programs
write exact values with SS. ES12.3E2 of 100.0 expects '   1.000E+02' and proves the
12-position field; ES11.2E2 of 100.0 expects '   1.00E+02' and proves d=2 fractional
digits; ES12.3E2 of 0.125 expects '   1.250E-01' and proves explicit E2 gives two
exponent digits. All descriptor substitutions in the generated matrix fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

ES-editing fixture boundaries: this packet discharges only the selected ES output facets
removed from pending above. It deliberately leaves ES input, IEEE infinity/NaN output,
ES w.d alternative exponent spellings, exponent magnitudes above 99, E0/w=0
selected-width forms, zero exponent plus, diagnostics, external files, DECIMAL=COMMA,
nondefault sign mode, and rounded/inexact decimal cases pending. All positive outputs
use SS and exact binary values whose discarded decimal digits are zero.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.5-003: ES input is interpreted as F input

**Source:** S13.7.2.3.5, J3/24-007, 18 December 2023, physical PDF299. **Class:** Effect.

**Definition:** The form and interpretation of ES input fields is the same as for F w.d input.

**Diagnostic obligation:** not-required.

**Facets:** `ES-input-same-as-F`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.5-004: ES infinity and NaN output follows F output form

**Source:** S13.7.2.3.5, J3/24-007, 18 December 2023, physical PDF299. **Class:** Effect.

**Definition:** For IEEE infinity or NaN internal values, ES output fields use the same form as F w.d
output.

**Diagnostic obligation:** not-required.

**Facets:** `ES-ieee-output-same-as-F`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.5-005: Finite ES output field components and Table 13.3 exponent form

**Source:** S13.7.2.3.5, J3/24-007, 18 December 2023, physical PDF299. **Class:** Effect.

**Definition:** Finite ES output has optional sign, one leading digit y, decimal symbol, d following
digits, and an exponent in a Table 13.3 form.

**Diagnostic obligation:** not-required.

**Facets:** `ES-output-leading-digit`, `ES-output-decimal-symbol`, `ES-output-d-fractional-digits`, `ES-output-table13_3-exponent`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.5-005 ES finite-output runtime fixtures: complete internal-file programs write
exact 0.5 with (SS,ES12.3E2) and expect '   5.000E-01'. Separate fixtures bind the
single leading digit, the decimal symbol, and the three following digits. Expected
fields check LEN before equality; the source keeps the Table 13.3 exponent-form facet
pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

ES-editing fixture boundaries: this packet discharges only the selected ES output facets
removed from pending above. It deliberately leaves ES input, IEEE infinity/NaN output,
ES w.d alternative exponent spellings, exponent magnitudes above 99, E0/w=0
selected-width forms, zero exponent plus, diagnostics, external files, DECIMAL=COMMA,
nondefault sign mode, and rounded/inexact decimal cases pending. All positive outputs
use SS and exact binary values whose discarded decimal digits are zero.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.5-006: Table 13.3 enumerates ES exponent forms

**Source:** S13.7.2.3.5, J3/24-007, 18 December 2023, physical PDF299. **Class:** Effect.

**Definition:** Table 13.3 gives row-by-row ES exponent forms for ES w.d, ES w.d E e, ES w.d E0, and
ES0.d, with z digits, s minimum digits, and plus sign for zero exponent.

**Diagnostic obligation:** not-required.

**Facets:** `table13_3_ES_w_d_abs_le_99`, `table13_3_ES_w_d_abs_100_to_999`, `table13_3_ES_Ee_positive`, `table13_3_ES_E0_or_ES0_d`, `table13_3_zero_exponent_plus`.

**Oracle:** The table was checked row-by-row against the PDF image; rows with alternative forms are
permissions/latitude unless e fixes the digit count.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

<!-- END GENERATED 13.7.2.3.5 -->
