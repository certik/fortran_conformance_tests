# Fortran 2023: 7.6.2 Enumeration types - ordinary value implementation

**Catalogue source review: reviewed.** Current case/evidence adjudications are separate content-bound records.

The finite corpus has **3 shared run-phase effect cases** and **36 value/ordinal/relation observations**. **8 of 52 facets are represented; 44 remain PENDING.** Representation is not approval or source closure.

## Source and finite qualifications

Authority is J3/24-007, 18 December 2023, 688 physical PDF pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The original7.6.2 spans PDF111-113 and ends before7.7. All16 base units,
54 fine units,70 accounting rows,13 requirements and52 facet IDs are preserved.
The immutable fa1fbf02 source and independent source review establish qualified
eligibility, not fixture approval.

These are ENUMERATION TYPE values: nonintrinsic, nonderived, noninteroperable,
without kind parameters. Enumerators are defined scalar named constants of
their enumeration type. INT returns ordinal positions; HUGE returns a scalar
of the same enumeration type denoting its last enumerator. No INTEGER
enumerator, zero-default ENUM/BIND(C), KIND, byte/representation, C companion,
BOZ, CLASS(enumeration_name), NEXT or PREVIOUS behavior is substituted.

Original access rules (PDF120/134), same-enumeration relations (PDF175/181),
constant constructors (10.1.12p1(5), PDF187-188), same-type intrinsic assignment
(PDF189), PARAMETER (PDF130), scalar dummy association/definedness (PDF340/563),
INT (PDF421-422) and HUGE (PDF416) qualify the actual calls. All type definitions
precede their uses. All explicit header access is in a module specification
part. Each explicit identifier access is supplied once; a member default can
be confirmed without repeating the type name's explicit header access.

The accessibility case contains three distinct definitions in one provider:
untouched PUBLIC-header defaults under module PRIVATE; a separate confirmed
PUBLIC member and PRIVATE override; and a PRIVATE-header type with one
exported constant. The client imports/uses the public type and values, and
consumes the exported private-type constant through INT without naming that
type. Private members' forbidden use is not executed or purportedly proved.
C1409 and the module-default/private-use source graphs remain pending.

The declaration-order case uses nonalphabetic names, fresh split-statement
names in a separate scope, and a separate one-member type. Each INT result
has an independent literal1/2/3 expectation. Same-type relational checks and
HUGE of known-defined constants retain their actual enumeration domains;
different types are never compared as if identical.

The constructor case uses only scalar INTEGER positions1..3, with independent
named first/interior/last values as primary expectations. Dynamic indices are
assigned2 then3 separately. A same-type PARAMETER constructor and a runtime
scalar constructor are actually consumed through explicit scalar TYPE dummies
with INTENT(IN); declarations alone do not establish an effect. Every ordinary
value is assigned before use, and every counter is initialized. Error output
prints only primitive logical/INTEGER observations, not enumeration values by
unsupported list-directed I/O.

All three S7.6.2-004 range/control/policy facets remain pending. Boundary values
already used here may support a later separately reviewed connection, not
another cloned program or automatic range coverage. The original note's
NEXT(last) without STAT error-terminates (ENS-Q02); its equality EXIT loop is
not copied. ENS-Q01/03/04's named-enum, BOZ, companion, representation and
policy qualifications remain outside this corpus. ENS-Q05/06's name/scope,
definedness and evidence-role distinctions are preserved.

Runtime expectations remain run-phase even when a processor fails compilation.
Reference f2018 execution, if any, is not relabelled as f2023 qualification.
Separate observer-sensitivity runs, when supported by a successful reference,
are artifacts rather than conformance cases. No profile, policy, source/case
approval, canonical link or baseline change is authored.

## Definitions

<!-- BEGIN GENERATED 7.6.2 -->

### R766: An enumeration type definition has a nonempty statement sequence

**Source:** 7.6.2, R766; J3/24-007, 18 December 2023, physical PDF 111. **Class:** Syntax.

**Definition:** An enumeration type definition contains its opening statement, one mandatory enumeration
enumerator statement, any further such statements, and its closing statement. R768 and
the list convention require at least one named enumerator.

**Diagnostic obligation:** required.

**Facets:** `one-statement-definition`, `multiple-statement-definition`, `nonempty-definition`, `statement-order-and-terminator-graph`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R767-R769;4.1.3/R401;R508;4.2p2(3).

### R767: ENUMERATION TYPE requires a name and has an optional accessibility group

**Source:** 7.6.2, R767; J3/24-007, 18 December 2023, physical PDF 111. **Class:** Syntax.

**Definition:** The ENUMERATION TYPE header always names the enumeration type. It may omit the whole
separator group, include :: alone, or include ,access-spec followed by ::.
PUBLIC/PRIVATE come from R807, and C7114 limits an explicit access-spec to a module
specification part. No BIND(C) alternative is present.

**Diagnostic obligation:** required.

**Facets:** `bare-name`, `double-colon-no-access`, `public-and-private-headers`, `access-separator`, `missing-type-name`, `bind-c-not-an-alternative`, `keyword-spacing-source-use`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R766/R807/C7114;8.5.2;6.3.2.2/Table6.2;4.2p2(3)/(5).

### C7114: An enumeration header access-spec is confined to the module specification part

**Source:** 7.6.2, C7114; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Restriction.

**Definition:** An access-spec on an enumeration-type-stmt may appear only in a module's specification
part. Merely being inside a module procedure or another nested scoping unit does not
make its declaration part the module specification part.

**Diagnostic obligation:** required.

**Facets:** `module-access-admissions`, `main-program-access`, `procedure-access`, `module-procedure-context`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R767;8.5.2/C817;14.2/R1404-R1408;4.2p2(3).

### R768: Enumeration enumerators are names, not integer initializer clauses

**Source:** 7.6.2, R768; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Syntax.

**Definition:** An enumeration enumerator statement contains ENUMERATOR, optional ::, and a nonempty
comma-separated list of enumerator names. Unlike R762 for interoperable enums, this rule
does not include = followed by an explicit value.

**Diagnostic obligation:** required.

**Facets:** `colon-and-colon-free-names`, `multiple-name-separator`, `explicit-value-not-in-syntax`, `nonempty-name-list`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R766;4.1.3/R401/R402;7.6.2p3;4.2p2(3);contrast R762 without transferring ownership.

### R769: END ENUMERATION TYPE optionally repeats the type name

**Source:** 7.6.2, R769; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Syntax.

**Definition:** The enumeration type closing statement consists of END ENUMERATION TYPE and an optional
enumeration-type-name. C7115 constrains the name if it is present. The enum closing
statement END ENUM is a different form.

**Diagnostic obligation:** required.

**Facets:** `unnamed-terminator`, `named-terminator`, `required-type-keyword`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R766/R767/C7115;6.3.2.2;4.2p2(3).

### C7115: A repeated enumeration end-name must agree with the opening name

**Source:** 7.6.2, C7115; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Restriction.

**Definition:** When the optional enumeration-type-name is present on END ENUMERATION TYPE, it must be
the same name as on the corresponding ENUMERATION TYPE statement. Normal Fortran
letter-case equivalence applies; there is no requirement to add an end-name.

**Diagnostic obligation:** required.

**Facets:** `matching-name`, `case-equivalent-name`, `different-ending-name`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R767/R769;6.1.2p3;4.2p2(3).

### R770: An enumeration type specifier uses the enumeration type name

**Source:** 7.6.2, R770; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Syntax.

**Definition:** The enumeration-type-spec syntax consists of an enumeration-type-name. Its surrounding
TYPE/type-spec/constructor consumer and C7116 supply separate conditions. This type name
is not an INTEGER KIND parameter or an enum constructor.

**Diagnostic obligation:** required.

**Facets:** `bare-type-name-admission`, `same-definition-and-consumer-graph`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** C7116;R702/R703;7.3.2.1-.2;7.6.2p1/p4;14.2.2;19.3.1.

### C7116: An enumeration type reference requires its previous definition

**Source:** 7.6.2, C7116; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Restriction.

**Definition:** The name in an enumeration-type-spec must name a previously defined enumeration type.
Previously defined USE-associated entities and7.3.2.2p2's special as-if declaration for
a body-defined function result are not excluded.

**Diagnostic obligation:** required.

**Facets:** `previous-definition-admissions`, `ordinary-forward-type-declaration`, `function-result-as-if-exception`, `actual-specifier-category-graph`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R770;7.3.2.2p2;14.2.2p2;19.3.1;4.2p2(3)/(6).

### R771: An enumeration constructor takes one scalar INTEGER expression

**Source:** 7.6.2, R771; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Syntax.

**Definition:** An enumeration constructor is an enumeration type specifier followed by one
parenthesized scalar INTEGER expression. The expression need not be constant; its value
must separately meet p5's1-through-N bound. This syntax has no C7113 BOZ alternative.

**Diagnostic obligation:** required.

**Facets:** `constant-and-dynamic-admissions`, `scalar-operand`, `integer-operand`, `enumerator-is-not-integer`, `single-operand`, `boz-and-constructor-consumer-graph`.

**Oracle:** This is a source-only plan. Numbered syntax/constraint reporting capability follows
4.1.2/4.1.3 and4.2p2(3), not mandatory fatal rejection or printed R/C codes. Each
prospective negative needs a complete otherwise-conforming source, a minimally repaired
valid control, the same intended condition, and an exact
subject/location/property-specific cause. Determine actual native diagnostic contracts
only after the independent source gate; nonfatal reporting needs exact family-specific
qualification.

**Oracle limitation:** An unsupported language facility, wrong type/property/subject, unrelated source error,
source echo, generic EOF or missing-END recovery, crash, verifier/internal error,
resource exhaustion or timeout is not the requested diagnostic. Broad 'enum', 'kind',
'constant' or 'expected' matches are insufficient. Unsupported intrinsic representation
value reporting, where actually applicable, retains its separate4.2p2(4) owner.

**Dependencies:** R770/C7116;4.1.3/C401;10.1.12p1(5);7.6.2p5;C7113 belongs only to7.6.1;7.7 not read here.

### S7.6.2-001: An enumeration header sets type-name access and enumerator defaults

**Source:** 7.6.2, p2; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Effect.

**Definition:** An access-spec in an enumeration header specifies the type name's accessibility and the
enumerators' default accessibility. An enumerator's access can be confirmed or
overridden by a separate access statement. If the header has no access-spec, the normal
module accessibility rules still apply; there is no unconditional PUBLIC default
overriding a module PRIVATE statement.

**Diagnostic obligation:** not-required.

**Facets:** `public-header-defaults`, `module-default-without-header-access`, `enumerator-confirmation-and-override`, `private-use-diagnostic-and-scope-graph`.

**Oracle:** Observe actual provider/client accessibility uses, separately from ordinal-value checks
and canonical private-use reporting. A same future program may provide a reviewed
source-use relationship; do not duplicate programs merely to clear names.

Finite runtime implementation: 1 shared run-phase effect case represents 2 authorized
facets with defined same-enumeration values, independent literal ordinal checks and
nonvacuous observer counts. Constructor values are compared to named same-type constants
before secondary INT checks. Compile admission is not a runtime substitute. All other
plans remain pending; source eligibility and compiler observations do not confer
case/source/link approval.

**Oracle limitation:** Finite runtime inputs and observations do not confer adjudication. Use defined values
and source-derived finite expectations, not compiler agreement. Keep enum type, its
INTEGER enumerators and ENUMERATION TYPE distinct. KIND identifiers, byte width, C_INT
equality, a minimum representation, unsigned wrap, padding bits and additional supported
kinds are not assumed. Future runtime effects must remain runtime on implementation
failure. Public constant accessibility does not make its private type name accessible.
No negative accessibility program or evidence link is authored.

**Dependencies:** 7.6.2p2;C7114;8.5.1/C815;8.5.2/R807/C817;8.6.1/R830/C873/C874;14.2.2/C1409;19.3.1;16.9.110p5(v);4.2p2(3)/(6).

### S7.6.2-002: Enumerator declaration order determines enumeration ordinal positions

**Source:** 7.6.2, p3; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Effect.

**Definition:** The order of enumerator names in an enumeration type definition determines their ordinal
positions. These scalar named constants have the enumeration type, not INTEGER.
Splitting the ordered list across statements does not introduce explicit values or the
enum form's zero/default-increment convention.

**Diagnostic obligation:** not-required.

**Facets:** `ordered-three-member-values`, `across-enumerator-statements`, `single-member-and-last-context`, `existing-relation-and-next-graph`.

**Oracle:** Use literal ordinal expectations and correctly typed, defined values. INT returns the
ordinal of an enumeration value, whereas it returns a corresponding INTEGER value for an
enum object. Intrinsic KIND is not applicable to either nonintrinsic object.

Finite runtime implementation: 1 shared run-phase effect case represents 3 authorized
facets with defined same-enumeration values, independent literal ordinal checks and
nonvacuous observer counts. Constructor values are compared to named same-type constants
before secondary INT checks. Compile admission is not a runtime substitute. All other
plans remain pending; source eligibility and compiler observations do not confer
case/source/link approval.

**Oracle limitation:** Finite runtime inputs and observations do not confer adjudication. Use defined values
and source-derived finite expectations, not compiler agreement. Keep enum type, its
INTEGER enumerators and ENUMERATION TYPE distinct. KIND identifiers, byte width, C_INT
equality, a minimum representation, unsigned wrap, padding bits and additional supported
kinds are not assumed. Future runtime effects must remain runtime on implementation
failure. No numeric representation, byte order, C interoperability or scalar INTEGER
identity is asserted for enumeration values.

**Dependencies:** 7.6.2p1/p3/p5;R766/R768;10.1.5.1/Table10.2;10.1.5.5.1p9;16.9.110p5(v);16.9.96;16.9.151/.164.

### S7.6.2-003: An enumeration constructor selects the value at its argument's ordinal position

**Source:** 7.6.2, p5; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Effect.

**Definition:** An enumeration constructor produces the scalar value of the specified enumeration type
at the ordinal position given by its scalar INTEGER argument. Its successful value
semantics are distinct from R771's operand grammar and p5's permitted range.

**Diagnostic obligation:** not-required.

**Facets:** `first-interior-last-values`, `dynamic-integer-position`, `constant-and-scalar-result-contexts`, `canonical-assignment-constructor-reuse`.

**Oracle:** A shared finite constructor matrix may cover the positions and dynamic-input facets. A
named constant or ordinary assignment is not a new type classifier, and no wrong-type or
range-invalid expression is executed.

Finite runtime implementation: 1 shared run-phase effect case represents 3 authorized
facets with defined same-enumeration values, independent literal ordinal checks and
nonvacuous observer counts. Constructor values are compared to named same-type constants
before secondary INT checks. Compile admission is not a runtime substitute. All other
plans remain pending; source eligibility and compiler observations do not confer
case/source/link approval.

**Oracle limitation:** Finite runtime inputs and observations do not confer adjudication. Use defined values
and source-derived finite expectations, not compiler agreement. Keep enum type, its
INTEGER enumerators and ENUMERATION TYPE distinct. KIND identifiers, byte width, C_INT
equality, a minimum representation, unsigned wrap, padding bits and additional supported
kinds are not assumed. Future runtime effects must remain runtime on implementation
failure. Scalar/value/constant-expression admissions do not by themselves validate an
optional diagnostic or a runtime range trap.

**Dependencies:** R770/R771/C7116;7.6.2p3/p5;7.3.2.2;10.1.12p1(5);8.2p4;10.2.1.2/Table10.8;16.9.110p5(v).

### S7.6.2-004: Enumeration constructor positions are bounded by one and the enumerator count

**Source:** 7.6.2, p5; J3/24-007, 18 December 2023, physical PDF 112. **Class:** Restriction.

**Definition:** The scalar INTEGER value supplied to an enumeration constructor must be positive and no
greater than the number of enumerators in that type's definition. The lower bound is not
zero. This ordinary prose range restriction does not itself require a diagnostic or a
runtime trap.

**Diagnostic obligation:** not-required.

**Facets:** `inclusive-boundary-controls`, `zero-and-above-count-policy-gate`, `ordinal-not-representation-range`.

**Oracle:** Only valid boundary controls are planned without a further policy gate. Missing optional
policy evidence stays pending; no rejection consensus, no-op program or skip replaces
it.

**Oracle limitation:** No executable evidence is authored. Use defined values and source-derived finite
expectations, not compiler agreement. Keep enum type, its INTEGER enumerators and
ENUMERATION TYPE distinct. KIND identifiers, byte width, C_INT equality, a minimum
representation, unsigned wrap, padding bits and additional supported kinds are not
assumed. Future runtime effects must remain runtime on implementation failure.
NEXT/PREVIOUS endpoint error termination is a different, explicitly specified intrinsic
behavior and is not a constructor-range oracle.

**Dependencies:** 7.6.2p1/p3/p5;R766/R768/R771;16.9.96;16.9.110p5(v);13.7.2.2p6;4.1.2p3;4.2.

<!-- END GENERATED 7.6.2 -->

## Exact finite observations

### `S7_6_2_001_valid__enumeration_value_access`

**Primary:** S7.6.2-001; **phase/evidence:** run / effect.

**Facets:** `public-header-defaults`, `enumerator-confirmation-and-override`.

A default-PRIVATE module exports a PUBLIC-header type and untouched default-public constants. A different PUBLIC-header type confirms one constant and makes another PRIVATE. A PRIVATE-header type exports one constant, consumed only by INT in the client without naming its type. Allowed exported uses are the effect; no private-name rejection is claimed.

| Observation | Actual expression/context | Independent expected value |
| --- | --- | --- |
| public-first | `int(zebra_default)` | `1` |
| public-second | `int(apple_default)` | `2` |
| confirmed-first | `int(zebra_confirmed)` | `1` |
| unmodified-second | `int(apple_visible)` | `2` |
| private-type-export | `int(exposed_apple)` | `2` |
| public-type-value | `public_value==apple_default` | `true` |
| public-type-ordinal | `int(public_value)` | `2` |

The final observer count must be `7`; an empty or incomplete run cannot pass.

### `S7_6_2_002_valid__enumeration_value_order`

**Primary:** S7.6.2-002; **phase/evidence:** run / effect.

**Facets:** `ordered-three-member-values`, `across-enumerator-statements`, `single-member-and-last-context`.

Fresh names in three separate module scopes establish nonalphabetic declaration order. The split variant preserves the first/second/third roles across two ENUMERATOR statements. All relation operands belong to the same enumeration type. HUGE consumes known-defined enumeration constants and returns the last value, including the one-member ordinal1 case. There is no NEXT/PREVIOUS call.

| Observation | Actual expression/context | Independent expected value |
| --- | --- | --- |
| list-first | `int(zephyr)` | `1` |
| list-second | `int(amber)` | `2` |
| list-third | `int(maple)` | `3` |
| split-first | `int(walrus)` | `1` |
| split-second | `int(banana)` | `2` |
| split-third | `int(quail)` | `3` |
| list-order | `zephyr<amber .and. amber<maple` | `true` |
| list-reverse | `maple<zephyr` | `false` |
| split-order | `walrus<banana .and. banana<quail` | `true` |
| split-reverse | `quail<walrus` | `false` |
| single-ordinal | `int(only_value)` | `1` |
| single-huge-value | `huge(only_value)==only_value` | `true` |
| single-huge-ordinal | `int(huge(only_value))` | `1` |
| list-last-value | `huge(zephyr)==maple` | `true` |
| list-last-ordinal | `int(huge(zephyr))` | `3` |

The final observer count must be `15`; an empty or incomplete run cannot pass.

### `S7_6_2_003_valid__enumeration_value_construction`

**Primary:** S7.6.2-003; **phase/evidence:** run / effect.

**Facets:** `first-interior-last-values`, `dynamic-integer-position`, `constant-and-scalar-result-contexts`.

Every constructor input is a defined scalar INTEGER1..3. Named same-type values, not another constructor or an INT inverse, are the primary oracle; independent INT1/2/3 checks are secondary. The dynamic index is assigned2 then3 in separate evaluations. A genuine same-type PARAMETER initialized by selection_kind(2) and a scalar runtime constructor are both consumed through complete TYPE INTENT(IN) dummies. No S004 range-control or policy facet is claimed.

| Observation | Actual expression/context | Independent expected value |
| --- | --- | --- |
| first:member | `value==zulu_pick` | `true` |
| first:ordinal | `int(value)` | `1` |
| interior:member | `value==alpha_pick` | `true` |
| interior:ordinal | `int(value)` | `2` |
| last:member | `value==mango_pick` | `true` |
| last:ordinal | `int(value)` | `3` |
| dynamic-second:member | `value==alpha_pick` | `true` |
| dynamic-second:ordinal | `int(value)` | `2` |
| dynamic-third:member | `value==mango_pick` | `true` |
| dynamic-third:ordinal | `int(value)` | `3` |
| constant:member | `fixed_middle==alpha_pick` | `true` |
| constant:ordinal | `int(fixed_middle)` | `2` |
| scalar-result:member | `selection_kind(index)==alpha_pick` | `true` |
| scalar-result:ordinal | `int(selection_kind(index))` | `2` |

The final observer count must be `14`; an empty or incomplete run cannot pass.

## Complete finite pending plans

Every entry below remains PENDING exactly as in canonical JSON. No boundary observation or source-use capability clears it automatically.

### Pending R766

* **`one-statement-definition`** - PENDING - Compile admission: one named enumeration type with one ENUMERATOR statement and a complete END ENUMERATION TYPE.
* **`multiple-statement-definition`** - PENDING - Compile admission: the same three names split over two ENUMERATOR statements, with unchanged order and no explicit values.
* **`nonempty-definition`** - PENDING - Diagnostic/control: a complete empty ENUMERATION TYPE tone...END ENUMERATION TYPE block in a complete module; insert one ENUMERATOR first statement only. Require the empty-definition condition, not generic recovery.
* **`statement-order-and-terminator-graph`** - PENDING - Source-use graph: R767/R768/R769 and R401/R508 retain their roles. Any missing-terminator candidate must preserve the surrounding complete program endings and have a single END ENUMERATION TYPE insertion control.

### Pending R767

* **`bare-name`** - PENDING - Compile admission: ENUMERATION TYPE tone with a complete enumerator list and closing statement.
* **`double-colon-no-access`** - PENDING - Compile admission: ENUMERATION TYPE :: tone, otherwise the same definition.
* **`public-and-private-headers`** - PENDING - Compile admissions: separate complete module-level definitions using ,PUBLIC :: and ,PRIVATE ::. Do not move an accessibility declaration into a procedure or interface body.
* **`access-separator`** - PENDING - Diagnostic/control: module-level ENUMERATION TYPE,PUBLIC tone lacks ::; insert only the double colon. C7114's module premise already holds.
* **`missing-type-name`** - PENDING - Diagnostic/control: keep ENUMERATION TYPE :: but omit the type name and use an unnamed closing statement; insert only tone. No dependent declaration or end-name mismatch is introduced.
* **`bind-c-not-an-alternative`** - PENDING - Diagnostic/control: add ,BIND(C) :: to this header in an otherwise valid definition; remove only the BIND(C) group while retaining :: and the same name. Do not repair by changing the entire construct to ENUM,BIND(C).
* **`keyword-spacing-source-use`** - PENDING - Source-use graph: ENUMERATION TYPE and END ENUMERATION TYPE are not optional-blank pairs in6.3.2.2/Table6.2. Do not generalize the END ENUM/ENDENUM permission to these different keywords.

### Pending C7114

* **`module-access-admissions`** - PENDING - Compile admissions: complete module-level PUBLIC and PRIVATE enumeration type headers, with only legal enumerator accessibility uses.
* **`main-program-access`** - PENDING - Diagnostic/control: a complete main-program enumeration header includes ,PUBLIC ::; remove only ,PUBLIC, retaining ::, the name and every enumerator. Require access-spec outside a module specification part.
* **`procedure-access`** - PENDING - Diagnostic/control: the same explicit PRIVATE header in an ordinary subroutine's specification part; remove only ,PRIVATE. No forward type use, PURE restriction or interface error is introduced.
* **`module-procedure-context`** - PENDING - Diagnostic/control: an explicit PUBLIC header in a module procedure body is still outside the module specification part; remove only ,PUBLIC. Preserve the complete containing module and procedure.

### Pending R768

* **`colon-and-colon-free-names`** - PENDING - Compile admissions: the same name list written with and without ::, inside an otherwise complete enumeration definition.
* **`multiple-name-separator`** - PENDING - Diagnostic/control: two names with a missing comma; insert only that comma. Keep the header/end-name and all source-form boundaries valid.
* **`explicit-value-not-in-syntax`** - PENDING - Diagnostic/control: ENUMERATOR :: first=1 in an otherwise valid enumeration type; remove only =1. Do not convert the declaration to an interoperable enum to obtain a control.
* **`nonempty-name-list`** - PENDING - Diagnostic/control: an empty ENUMERATOR :: statement; insert only first. The surrounding definition remains otherwise complete.

### Pending R769

* **`unnamed-terminator`** - PENDING - Compile admission: a complete named enumeration definition closes without repeating the name.
* **`named-terminator`** - PENDING - Compile admission: a complete definition repeats the same name after END ENUMERATION TYPE.
* **`required-type-keyword`** - PENDING - Diagnostic/control: END ENUMERATION tone omits TYPE while the opening definition and optional end-name otherwise agree; insert only TYPE. Do not omit the whole closing statement or create generic EOF recovery.

### Pending C7115

* **`matching-name`** - PENDING - Compile admission: opening and ending names agree, with complete enumerator statements between them.
* **`case-equivalent-name`** - PENDING - Compile admission: tone and TONE are the two spellings of that one name; preserve6.1.2's non-character-context premise.
* **`different-ending-name`** - PENDING - Diagnostic/control: change only the end-name to other_tone; restore tone. All keywords and the type definition remain intact, so the required cause is the opening/closing enumeration-name mismatch.

### Pending R770

* **`bare-type-name-admission`** - PENDING - Compile admission/source-use: a prior definition supplies the bare name inside TYPE(name). Preserve R703_valid__type_enumeration and R702_valid__enumeration, which already supply complete type/array-constructor contexts.
* **`same-definition-and-consumer-graph`** - PENDING - Source-use graph: same-definition references, USE renames, components, TYPEOF/CLASSOF and function results retain their actual canonical premises. Do not create an empty classifier program or assume identical lists in separate definitions establish type identity.

### Pending C7116

* **`previous-definition-admissions`** - PENDING - Compile admissions/source-use: a local prior definition and a USE-renamed prior definition, with no uninitialized value observation. Preserve S7_3_2_2_001_valid__previous_enumeration.
* **`ordinary-forward-type-declaration`** - PENDING - Diagnostic/control: a module has an uninitialized TYPE(tone) variable declaration before its later complete ENUMERATION TYPE definition. Move only the variable declaration below END ENUMERATION TYPE for the control; do not add a forward enumerator initializer.
* **`function-result-as-if-exception`** - PENDING - Source-use: preserve S7_3_2_2_002_valid__body_enumeration, whose body-defined function result is consumed as ordinal2. Its prefix spelling is permitted by7.3.2.2p2, not a generic forward-use negative.
* **`actual-specifier-category-graph`** - PENDING - Source-use graph: TYPE(other_derived_type) can be legal through a different R703 alternative. Before any wrong-category diagnostic candidate, isolate a genuine enumeration-type-spec rather than merely asserting that a type name looks wrong.

### Pending R771

* **`constant-and-dynamic-admissions`** - PENDING - Compile admissions: a three-member type is constructed from literal2 and from a separately defined scalar INTEGER variable2. Do not impose constancy on a runtime constructor.
* **`scalar-operand`** - PENDING - Diagnostic/control: tone([2]) has an INTEGER array operand; replace only [2] by2, with2 in range.
* **`integer-operand`** - PENDING - Diagnostic/control: tone(2.0) has a scalar REAL operand; replace only2.0 by2, with every other type/range/context premise satisfied.
* **`enumerator-is-not-integer`** - PENDING - Diagnostic/control: pass the enumeration's own second enumerator as tone(second); replace only second by integer2. The named constant is of the enumeration type, not an INTEGER enumerator as in7.6.1.
* **`single-operand`** - PENDING - Diagnostic/control: tone(1,2) in a complete three-member definition; remove only the second argument/comma. Both numerical values are otherwise valid.
* **`boz-and-constructor-consumer-graph`** - PENDING - Source-use gate: do not import the enum constructor's BOZ allowance. Any BOZ negative needs its exact7.7/global literal context and owner reviewed later. Keep R770/C7116 and10.1.12p1(5) distinct from the runtime p5 range restriction.

### Pending S7.6.2-001

* **`module-default-without-header-access`** - PENDING - Source-use graph: contrast a provider's ordinary default PUBLIC with its explicit module PRIVATE when the enumeration header omits access-spec. Public-use compile admissions and minimally repaired private-import diagnostics belong at the canonical access/C1409 owners; do not pretend an empty client proves a local runtime effect.
* **`private-use-diagnostic-and-scope-graph`** - PENDING - Canonical source-use: C1409 requires each USE identifier to be public and carries4.2p2(3) reporting. A complete two-file private-import candidate can be repaired by making only that enumerator PUBLIC; any actual scope-name violation also retains4.2p2(6). Keep this unregistered graph pending rather than weakening reporting duty or assigning a new S-owned fatal diagnostic policy.

### Pending S7.6.2-002

* **`existing-relation-and-next-graph`** - PENDING - Source-use: preserve S7_1_5_003_valid__enumeration and S7_3_2_2_001_valid__previous_enumeration. NEXT/PREVIOUS and their optional STAT/error-termination branches stay at16.9.151/.164. Do not copy the note's unqualified endpoint loop into a successful local ordinal witness.

### Pending S7.6.2-003

* **`canonical-assignment-constructor-reuse`** - PENDING - Source-use: preserve R703_valid__type_enumeration, R702_valid__enumeration, S10_2_1_3_001_valid__enumeration and the previous/body-defined enumeration contexts. Their exact assignment/constructor observations keep existing owners; no renamed duplicates or premature local credit.

### Pending S7.6.2-004

* **`inclusive-boundary-controls`** - PENDING - Runtime positive controls: a three-member type admits1 and3, with exact first/last value checks; a one-member type admits1 as both boundaries. Retain the R771 INTEGER/scalar premises.
* **`zero-and-above-count-policy-gate`** - PENDING - No negative execution is authorized. A future independently approved static diagnostic policy could use literal0 and literal4 for a three-member type, repaired to1 and3 respectively, with otherwise identical complete sources. These are not new mandatory-report cases and must not be executed as trap or wrap tests.
* **`ordinal-not-representation-range`** - PENDING - Source-use graph: type definition count, INT ordinal conversion, HUGE's last-enumerator result and I-editing input bounds have separate owners. Do not replace the bound by HUGE(default_INTEGER), an enum carrier limit, a machine word size or a processor-success observation.

## Reproduction and remaining gates

`python3 -B tools/generate_enumeration_value_fixtures.py --check` checks exact generated bytes, source/phase/pending metadata and both document regions. Targeted regressions use independent ordinal/type/access/constructor countermodels and inspect actual declarations, index assignments, typed consumers, primitive observer guards and mandatory counts. The original1907 case bindings and nine links are protected by the scoped receipt.

Consult current independent fixture/oracle adjudications before integration. Source eligibility, compiler agreement and sensitivity tests do not approve cases or links. The author-local index overlay is excluded; source-context/inventory staleness is reported, not renewed.
