# Fortran 2023: 10.1.5.1 Intrinsic operation classification

Catalogue: `doc/catalogues/intrinsic_operation_classification_10_1_5_1.json`. The original source-only registration supplied pending plans; the bounded runtime fixtures below supply selected cases and mutation plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN INTRINSIC OPERATION CLASSIFICATION FIXTURES -->
## Intrinsic operation classification runtime observations

Thirteen complete run/effect/f2023 programs cover a bounded scalar subset: unary integer plus, all five numeric intrinsic operator spellings with exact integer operands, default-character concatenation value/length/kind consequences, and the five logical intrinsic operators, now including the complete unary .NOT. table and complete binary truth tables. Because 10.1.5.1 is a classification subclause, each program observes only a runtime consequence (type, length, kind, branch, or value), not the classification label itself. All operands are literals or variables with exact hand-derived oracles; no real approximation, complex component dependency, short-circuit, evaluation-order, TRANSFER, storage, or compiler-consensus oracle is used. Operator-substitution mutations for the four binary logical fixtures are part of the generated campaign because value/input mutations alone cannot prove operator identity.

Binary result-type combinations, arrays, relation families, nondefault character kinds, complex unary minus, and all diagnostic/control boundaries remain pending with their original plans. No evidence link, baseline update, SourceUse renewal, catalogue source-review renewal, or approval is created.
<!-- END INTRINSIC OPERATION CLASSIFICATION FIXTURES -->

<!-- BEGIN GENERATED 10.1.5.1 -->

### S10.1.5.1-001: Unary intrinsic operations use a unary intrinsic operator and a Table 10.2 numeric operand type

**Source:** 10.1.5.1 p1 and Table 10.2; J3/24-007 PDF p175. **Class:** Effect.

**Definition:** An intrinsic operation is unary or binary. A unary intrinsic operation has the form
intrinsic-operator x2, and Table 10.2 lists unary plus and minus for integer, real, and
complex operands with the same type category as the result.

**Diagnostic obligation:** not-required.

**Facets:** `unary-plus-integer`, `unary-minus-real`, `unary-minus-complex`, `unary-logical-not-separated`.

**Oracle:** Use small literals and standard inquiries to observe operand/result type category and
value. Invalid operand-type diagnostics for unnumbered classification text remain
context-dependent and need a separate reporting basis before authoring.

S10.1.5.1-001 unary classification runtime fixture: one complete run/effect/f2023
program applies unary + to an INTEGER variable initialized to -19, then observes the
integer value consequence directly as -19. The negative operand distinguishes unary plus
from an absolute-value-like or dropped-sign interpretation. The program observes value
preservation and normal execution, not the abstract classification label itself.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal or
inquiry oracles and must not infer evaluation order, short-circuiting, which operand was
evaluated, or whether an operand was evaluated. They must not rely on compiler
agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or a
floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted evaluation
or mathematically equivalent rewriting.

S10.1.5.1-001 unary classification fixture boundaries: only unary-plus-integer is
represented. unary-minus-real and unary-minus-complex remain pending because this packet
intentionally avoids real approximation and complex component dependencies;
unary-logical-not-separated remains pending as a source-boundary contrast with
S10.1.5.1-005. The fixture observes only the value consequence of an admitted unary
numeric operation, not the classification label directly.

**Dependencies:** 7.4.1;7.4.3.1-7.4.3.3;10.1.2.5R1010;10.1.5.2.1;16.9.118 KIND.

### S10.1.5.1-002: Binary intrinsic operations use conformable Table 10.2 operand type combinations

**Source:** 10.1.5.1 p2 and Table 10.2; J3/24-007 PDF p175. **Class:** Effect.

**Definition:** A binary intrinsic operation has the form x1 intrinsic-operator x2, with conformable
operands whose types are listed in Table 10.2 for that binary intrinsic operator. For
numeric binary operators, integer, real, and complex combinations have the result type
shown in the corresponding Table 10.2 position.

**Diagnostic obligation:** not-required.

**Facets:** `integer-left-numeric-results`, `real-left-numeric-results`, `complex-left-numeric-results`, `conformable-array-binary-operation`, `nonconformable-boundary`.

**Oracle:** Check type category and values through exact small literals, declared destinations, and
standard inquiries. Nonconformability negatives require exact source attribution and are
not compiler-agreement tests.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived literal or inquiry oracles and must not
infer evaluation order, short-circuiting, which operand was evaluated, or whether an
operand was evaluated. They must not rely on compiler agreement, address arithmetic,
TRANSFER, emitted assembly, undefined reads, or a floating-point result that 10.1.7 or
10.1.8 permits to differ through omitted evaluation or mathematically equivalent
rewriting.

**Dependencies:** 7.4.1;7.4.3.1-7.4.3.3;9.5.2;10.1.5.2.1;10.1.10;10.2.1.3;16.9.118 KIND;16.9.171 SHAPE.

### S10.1.5.1-003: Numeric intrinsic operations are operations with numeric operators

**Source:** 10.1.5.1 p3 and Table 10.2 numeric rows; J3/24-007 PDF p175. **Class:** Effect.

**Definition:** A numeric intrinsic operation is an intrinsic operation whose intrinsic-operator is +,
-, *, /, or **. The operator in such an operation is a numeric intrinsic operator.

**Diagnostic obligation:** not-required.

**Facets:** `numeric-add-op`, `numeric-subtract-op`, `numeric-multiply-op`, `numeric-divide-op`, `numeric-power-op`.

**Oracle:** Use exact, small operands and separate syntax-token ownership from operation
classification. Do not infer evaluation order or floating-point associativity from
numeric results.

S10.1.5.1-003 numeric-operator classification runtime fixtures: five complete
run/effect/f2023 programs exercise +, binary -, *, /, and ** on INTEGER operands with
hand-computed literal results: 14+(-5)=9, 14-(-5)=19, (-6)*7=-42, 17/5=3 by integer
division, and 2**5=32. Operands are selected so the plausible wrong numeric operator
alternatives do not produce the same value; the division guard compares the expression
directly so a real-division interpretation would produce 3.4 and fail rather than being
hidden by integer assignment.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal or
inquiry oracles and must not infer evaluation order, short-circuiting, which operand was
evaluated, or whether an operand was evaluated. They must not rely on compiler
agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or a
floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted evaluation
or mathematically equivalent rewriting.

S10.1.5.1-003 numeric classification fixture boundaries: all five numeric-operator
classification facets are represented only with default INTEGER operands. The fixtures
do not cover real or complex arithmetic, overflow, division by zero, reassociation,
precedence, evaluation order, kind selection, or processor diagnostics. Expected values
are hand arithmetic, not compiler consensus.

**Dependencies:** 10.1.2.5R1008-R1010;10.1.3;10.1.5.2.1;10.1.8.

### S10.1.5.1-004: Character intrinsic operations are same-kind character concatenations

**Source:** 10.1.5.1 p4 and Table 10.2; J3/24-007 PDF p175. **Class:** Effect.

**Definition:** The character intrinsic operation is the intrinsic operation whose intrinsic-operator is
// and whose two operands are of type character with the same kind type parameter. The
operator in that operation is the character intrinsic operator, and Table 10.2 gives a
character result.

**Diagnostic obligation:** not-required.

**Facets:** `default-character-concat`, `nondefault-same-kind-concat-boundary`, `character-result-length-value`, `different-character-kind-boundary`.

**Oracle:** Use literal character contents, LEN, and KIND only through standard dependencies. Do not
use storage bytes, TRANSFER, or processor collation as an oracle for concatenation
classification.

S10.1.5.1-004 character-operation classification runtime fixtures: two complete
run/effect/f2023 programs concatenate default CHARACTER operands of unequal lengths. The
default-character case uses left 'Q3' and right 'z9X', expecting
KIND(left//right)==KIND(left), LEN 5, and equal-length value 'Q3z9X'. The length/value
case assigns 'Lm'//'h5K' to CHARACTER(LEN=5) result and checks LEN(result)==5 plus value
'Lmh5K'. The operands are non-palindromic and unequal length so swapped, truncated,
padded, or length-one interpretations fail.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal or
inquiry oracles and must not infer evaluation order, short-circuiting, which operand was
evaluated, or whether an operand was evaluated. They must not rely on compiler
agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or a
floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted evaluation
or mathematically equivalent rewriting.

S10.1.5.1-004 character classification fixture boundaries: only default-character-concat
and character-result-length-value are represented. nondefault-same-kind-concat-boundary
remains pending because a nondefault character kind is not required to exist, and
different-character-kind-boundary remains pending because this classification unit by
itself does not supply a portable diagnostic case. The fixtures use LEN and KIND
inquiries as dependencies and do not use TRANSFER, storage bytes, TRIM, INDEX, REPEAT,
or blank-padded unequal-length value comparisons.

**Dependencies:** 7.4.4.1-7.4.4.4;10.1.2.6R1012;10.1.5.3;10.2.1.3;16.9.118 KIND;16.9.122 LEN.

### S10.1.5.1-005: Logical intrinsic operations use logical operands and logical operators

**Source:** 10.1.5.1 p5 and Table 10.2; J3/24-007 PDF p175. **Class:** Effect.

**Definition:** A logical intrinsic operation is an intrinsic operation whose intrinsic-operator is
.AND., .OR., .NOT., .EQV., or .NEQV. and whose operands are of type logical. The
operator in such an operation is a logical intrinsic operator; Table 10.2 gives logical
results.

**Diagnostic obligation:** not-required.

**Facets:** `logical-not`, `logical-and`, `logical-or`, `logical-eqv`, `logical-neqv`, `nonlogical-operand-boundary`.

**Oracle:** Logical truth tables may be checked with literal operands and results. No fixture may
use operand side effects or expected short-circuit behavior as evidence for a logical
intrinsic operation.

S10.1.5.1-005 logical-operator classification runtime fixtures: five complete
run/effect/f2023 programs exercise .NOT., .AND., .OR., .EQV., and .NEQV. with literal
LOGICAL operands and branch-only observations of the result. .NOT. checks both .FALSE.
-> .TRUE. and .TRUE. -> .FALSE. The four binary-operator cases each check the complete
TT, TF, FT and FF truth table. That makes every operand literal load-bearing and makes
each asserted row set inconsistent with each of the other three binary logical
operators; operator-substitution mutations replace the operator under test with each
alternative and are required to fail.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal or
inquiry oracles and must not infer evaluation order, short-circuiting, which operand was
evaluated, or whether an operand was evaluated. They must not rely on compiler
agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or a
floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted evaluation
or mathematically equivalent rewriting.

S10.1.5.1-005 logical classification fixture boundaries: only the five positive logical
operators are represented. nonlogical-operand-boundary remains pending: p5 defines a
logical intrinsic operation but contains no shall-style prohibition requiring a portable
diagnostic for a nonlogical operand under this unit. The fixtures make no short-circuit,
side-effect, associativity, evaluation-order, kind-selection, defined-operator, or
diagnostic claim.

**Dependencies:** 7.4.5;10.1.2.8R1019-R1022;10.1.3;10.1.5.4;10.1.6 defined extensions;4.2p2.

### S10.1.5.1-006: Relational intrinsic operations cover numeric, character, enumeration, and enum operand families

**Source:** 10.1.5.1 p6 and Table 10.2; J3/24-007 PDF p175. **Class:** Restriction.

**Definition:** The relational intrinsic operators are .EQ., .NE., .GT., .GE., .LT., .LE., ==, /=, >,
>=, <, and <=. A relational intrinsic operation uses one of those operators. Numeric
relational operations have numeric operands; character relational operations have
character operands whose kind type parameters shall be the same; enumeration relational
operations have operands of the same enumeration type; enum relational operations have
one enum operand and the other operand of the same enum type or an integer expression
involving an enumerator of that type. Table 10.2 gives logical results and the admitted
equality and ordering families.

**Diagnostic obligation:** context-dependent.

**Facets:** `numeric-equality-relations`, `numeric-order-relations`, `complex-equality-only`, `character-equality-and-order`, `character-kind-match-restriction`, `same-enumeration-relations`, `enum-with-same-enum-relations`, `enum-with-enumerator-integer-expression`, `relational-spelling-pairs`.

**Oracle:** Use literal logical results for admitted operand families and exact source-repair
contrasts for restrictions. Character kind, enumeration, and enum cases require their
owning type definitions and cannot be replaced by compiler agreement.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived literal or inquiry oracles and must not
infer evaluation order, short-circuiting, which operand was evaluated, or whether an
operand was evaluated. They must not rely on compiler agreement, address arithmetic,
TRANSFER, emitted assembly, undefined reads, or a floating-point result that 10.1.7 or
10.1.8 permits to differ through omitted evaluation or mathematically equivalent
rewriting.

**Dependencies:** 7.4.1;7.4.3.1-7.4.3.3;7.4.4.4;7.6.1;7.6.2;10.1.2.7R1014;10.1.5.5;10.1.6.2;16.9.118
KIND;4.2p2.

### S10.1.5.1-007: Intrinsic-operation interpretations apply to arrays element by element

**Source:** 10.1.5.1 p7; J3/24-007 PDF p175. **Class:** Effect.

**Definition:** The interpretations defined in 10.1.5 apply to both scalars and arrays; for arrays, the
scalar interpretation is applied element by element.

**Diagnostic obligation:** not-required.

**Facets:** `numeric-array-interpretation`, `character-array-interpretation`, `logical-array-interpretation`, `relational-array-interpretation`.

**Oracle:** Array facets observe element values and shape only. They do not claim any order of
element evaluation or duplicate coverage of scalar interpretation subclauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived literal or inquiry oracles and must not
infer evaluation order, short-circuiting, which operand was evaluated, or whether an
operand was evaluated. They must not rely on compiler agreement, address arithmetic,
TRANSFER, emitted assembly, undefined reads, or a floating-point result that 10.1.7 or
10.1.8 permits to differ through omitted evaluation or mathematically equivalent
rewriting.

**Dependencies:** 10.1.4 p4-p5;10.1.5.2-10.1.5.5;10.1.10;10.2.1.3;16.9.171 SHAPE.

<!-- END GENERATED 10.1.5.1 -->
