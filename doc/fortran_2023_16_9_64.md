# Fortran 2023 16.9.64: COSHAPE

The canonical catalogue is `doc/catalogues/coshape_16_9_64.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.64 -->

### S16.9.64-001: COSHAPE COARRAY argument is a valid allocated coarray designator with nonzero corank in the rightmost part-ref

**Source:** S16.9.64, J3/24-007, 18 December 2023, physical PDF399. **Class:** Restriction.

**Definition:** COARRAY shall be a coarray of any type, shall not be an unallocated allocatable coarray,
and if its designator has more than one part-ref the rightmost part-ref shall have
nonzero corank.

**Diagnostic obligation:** not-required.

**Facets:** `coshape-coarray-any-type`, `coshape-coarray-not-unallocated-allocatable`, `coshape-rightmost-part-ref-nonzero-corank`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found only pre-existing data-component C751 fixture
metadata using the word coshape; no tests/ or tests/fixtures/*/fixture.json binding for
the 16.9.64 COSHAPE intrinsic. Coarray/image-dependent facets are marked OUT OF SCOPE
for this suite's single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.64-002: COSHAPE KIND argument is a scalar integer constant expression

**Source:** S16.9.64, J3/24-007, 18 December 2023, physical PDF399. **Class:** Restriction.

**Definition:** KIND, if present, shall be a scalar integer constant expression.

**Diagnostic obligation:** not-required.

**Facets:** `coshape-kind-scalar-integer-constant-expression`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found only pre-existing data-component C751 fixture
metadata using the word coshape; no tests/ or tests/fixtures/*/fixture.json binding for
the 16.9.64 COSHAPE intrinsic. Coarray/image-dependent facets are marked OUT OF SCOPE
for this suite's single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.64-003: COSHAPE result integer kind follows KIND or default integer

**Source:** S16.9.64, J3/24-007, 18 December 2023, physical PDF399. **Class:** Effect.

**Definition:** The result is integer; if KIND is present, the kind type parameter is specified by KIND,
otherwise it is default integer kind.

**Diagnostic obligation:** not-required.

**Facets:** `coshape-result-integer`, `coshape-result-kind-from-kind-argument`, `coshape-result-default-integer-kind-when-kind-absent`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found only pre-existing data-component C751 fixture
metadata using the word coshape; no tests/ or tests/fixtures/*/fixture.json binding for
the 16.9.64 COSHAPE intrinsic. Coarray/image-dependent facets are marked OUT OF SCOPE
for this suite's single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.64-004: COSHAPE result is rank one with size equal to COARRAY corank

**Source:** S16.9.64, J3/24-007, 18 December 2023, physical PDF399. **Class:** Effect.

**Definition:** The result is a rank-one array whose size is equal to the corank of COARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `coshape-result-rank-one`, `coshape-result-size-equals-corank`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found only pre-existing data-component C751 fixture
metadata using the word coshape; no tests/ or tests/fixtures/*/fixture.json binding for
the 16.9.64 COSHAPE intrinsic. Coarray/image-dependent facets are marked OUT OF SCOPE
for this suite's single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

### S16.9.64-005: COSHAPE element values follow UCOBOUND minus LCOBOUND plus one

**Source:** S16.9.64, J3/24-007, 18 December 2023, physical PDF399. **Class:** Effect.

**Definition:** Element i of the result is UCOBOUND(COARRAY,i) - LCOBOUND(COARRAY,i) + 1.

**Diagnostic obligation:** not-required.

**Facets:** `coshape-element-values-from-cobounds`.

**Oracle:** Plans are pending and source-derived. Positive plans use conforming programs with
non-default sentinels or discriminating inputs and assert only properties fixed by the
cited intrinsic text: exact type, kind, rank, shape, integer/logical/character results,
or exactly representable numeric results. Processor-dependent approximations,
unsupported command-argument facilities, image counts beyond one image, and undefined
values are not asserted as exact.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument shall restrictions are program restrictions and carry no diagnostic
facet. Existing coverage grep found only pre-existing data-component C751 fixture
metadata using the word coshape; no tests/ or tests/fixtures/*/fixture.json binding for
the 16.9.64 COSHAPE intrinsic. Coarray/image-dependent facets are marked OUT OF SCOPE
for this suite's single-image fixtures by user decision, not as absent source authority.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 7, 8, 9, and 10 for source form,
types, declarations, expressions, arrays, and assignment; clause 14 for images/teams
where coarray collectives or coarray inquiries are reached; clause 16.6 for STAT/ERRMSG
semantics; clause 17 for IEEE arithmetic if a future approximate-math profile adopts
tolerances.

<!-- END GENERATED 16.9.64 -->
