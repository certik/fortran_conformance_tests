# Fortran 2023 16.9.151: NEXT

The canonical catalogue is `doc/catalogues/next_intrinsic_16_9_151.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.151")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.151 -->

### S16.9.151-001: NEXT argument restrictions

**Source:** S16.9.151, J3/24-007, 18 December 2023, physical PDF440. **Class:** Restriction.

**Definition:** A shall be of enumeration type; optional STAT shall be an integer scalar with decimal
exponent range at least four and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `NEXT-A-enumeration-type`, `NEXT-STAT-integer-scalar-range`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic, use conforming companions for zero,
blank, or .FALSE. results, and assert only properties fixed by 16.9: exact integer,
logical, character, kind, shape, allocation, STAT, or bit-pattern observations.
Real-result exact values are limited to separately justified exact cases;
processor-dependent approximations, image-dependent behavior, explanatory message text,
and undefined pointer association status are not asserted as exact values. Unnumbered
intrinsic argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MOD, MODULO, MOVE_ALLOC, MVBITS,
NEAREST, NEW_LINE, and NEXT found no existing 16.9.145-16.9.151 fixture binding in this
worktree. Existing source-use hits are unrelated coverage: MOD appears in older clause
7, clause 10, and S8.6.11 parameter sources; NEAREST appears in rounding-mode facet
names; NEXT appears in Python helpers and unrelated component names.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression, array,
execution, allocation, pointer, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.6 enumeration types, 7.7 BOZ
literal context, 15.9.3 argument association, 16.3 integer bit model, 16.4 numeric model
and machine-representable terminology, sibling 16.9 intrinsics used by pending plans,
ISO_FORTRAN_ENV STAT constants, and clauses 14 through 19 where future fixtures need
program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.151-002: NEXT STAT is zero away from the last enumerator

**Source:** S16.9.151, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** For A not equal to the last enumerator of its type, present STAT is assigned zero.

**Diagnostic obligation:** not-required.

**Facets:** `NEXT-nonlast-STAT-zero`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic, use conforming companions for zero,
blank, or .FALSE. results, and assert only properties fixed by 16.9: exact integer,
logical, character, kind, shape, allocation, STAT, or bit-pattern observations.
Real-result exact values are limited to separately justified exact cases;
processor-dependent approximations, image-dependent behavior, explanatory message text,
and undefined pointer association status are not asserted as exact values. Unnumbered
intrinsic argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MOD, MODULO, MOVE_ALLOC, MVBITS,
NEAREST, NEW_LINE, and NEXT found no existing 16.9.145-16.9.151 fixture binding in this
worktree. Existing source-use hits are unrelated coverage: MOD appears in older clause
7, clause 10, and S8.6.11 parameter sources; NEAREST appears in rounding-mode facet
names; NEXT appears in Python helpers and unrelated component names.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression, array,
execution, allocation, pointer, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.6 enumeration types, 7.7 BOZ
literal context, 15.9.3 argument association, 16.3 integer bit model, 16.4 numeric model
and machine-representable terminology, sibling 16.9 intrinsics used by pending plans,
ISO_FORTRAN_ENV STAT constants, and clauses 14 through 19 where future fixtures need
program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.151-003: NEXT last-enumerator STAT is positive

**Source:** S16.9.151, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** For A equal to the last enumerator of its type, present STAT is assigned a positive
value.

**Diagnostic obligation:** not-required.

**Facets:** `NEXT-last-STAT-positive`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic, use conforming companions for zero,
blank, or .FALSE. results, and assert only properties fixed by 16.9: exact integer,
logical, character, kind, shape, allocation, STAT, or bit-pattern observations.
Real-result exact values are limited to separately justified exact cases;
processor-dependent approximations, image-dependent behavior, explanatory message text,
and undefined pointer association status are not asserted as exact values. Unnumbered
intrinsic argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MOD, MODULO, MOVE_ALLOC, MVBITS,
NEAREST, NEW_LINE, and NEXT found no existing 16.9.145-16.9.151 fixture binding in this
worktree. Existing source-use hits are unrelated coverage: MOD appears in older clause
7, clause 10, and S8.6.11 parameter sources; NEAREST appears in rounding-mode facet
names; NEXT appears in Python helpers and unrelated component names.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression, array,
execution, allocation, pointer, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.6 enumeration types, 7.7 BOZ
literal context, 15.9.3 argument association, 16.3 integer bit model, 16.4 numeric model
and machine-representable terminology, sibling 16.9 intrinsics used by pending plans,
ISO_FORTRAN_ENV STAT constants, and clauses 14 through 19 where future fixtures need
program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.151-004: NEXT last-enumerator STAT value is processor dependent

**Source:** S16.9.151, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** The positive STAT value assigned for the last enumerator is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `NEXT-last-STAT-exact-value-no-oracle`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic, use conforming companions for zero,
blank, or .FALSE. results, and assert only properties fixed by 16.9: exact integer,
logical, character, kind, shape, allocation, STAT, or bit-pattern observations.
Real-result exact values are limited to separately justified exact cases;
processor-dependent approximations, image-dependent behavior, explanatory message text,
and undefined pointer association status are not asserted as exact values. Unnumbered
intrinsic argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MOD, MODULO, MOVE_ALLOC, MVBITS,
NEAREST, NEW_LINE, and NEXT found no existing 16.9.145-16.9.151 fixture binding in this
worktree. Existing source-use hits are unrelated coverage: MOD appears in older clause
7, clause 10, and S8.6.11 parameter sources; NEAREST appears in rounding-mode facet
names; NEXT appears in Python helpers and unrelated component names.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression, array,
execution, allocation, pointer, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.6 enumeration types, 7.7 BOZ
literal context, 15.9.3 argument association, 16.3 integer bit model, 16.4 numeric model
and machine-representable terminology, sibling 16.9 intrinsics used by pending plans,
ISO_FORTRAN_ENV STAT constants, and clauses 14 through 19 where future fixtures need
program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.151-005: NEXT without STAT terminates on last enumerator

**Source:** S16.9.151, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** If STAT would have been assigned a nonzero value but is absent, error termination is
initiated.

**Diagnostic obligation:** not-required.

**Facets:** `NEXT-last-without-STAT-error-termination`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic, use conforming companions for zero,
blank, or .FALSE. results, and assert only properties fixed by 16.9: exact integer,
logical, character, kind, shape, allocation, STAT, or bit-pattern observations.
Real-result exact values are limited to separately justified exact cases;
processor-dependent approximations, image-dependent behavior, explanatory message text,
and undefined pointer association status are not asserted as exact values. Unnumbered
intrinsic argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MOD, MODULO, MOVE_ALLOC, MVBITS,
NEAREST, NEW_LINE, and NEXT found no existing 16.9.145-16.9.151 fixture binding in this
worktree. Existing source-use hits are unrelated coverage: MOD appears in older clause
7, clause 10, and S8.6.11 parameter sources; NEAREST appears in rounding-mode facet
names; NEXT appears in Python helpers and unrelated component names.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression, array,
execution, allocation, pointer, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.6 enumeration types, 7.7 BOZ
literal context, 15.9.3 argument association, 16.3 integer bit model, 16.4 numeric model
and machine-representable terminology, sibling 16.9 intrinsics used by pending plans,
ISO_FORTRAN_ENV STAT constants, and clauses 14 through 19 where future fixtures need
program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.151-006: NEXT result characteristics match A

**Source:** S16.9.151, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** The NEXT result has the same type as A.

**Diagnostic obligation:** not-required.

**Facets:** `NEXT-result-same-enumeration-type-as-A`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic, use conforming companions for zero,
blank, or .FALSE. results, and assert only properties fixed by 16.9: exact integer,
logical, character, kind, shape, allocation, STAT, or bit-pattern observations.
Real-result exact values are limited to separately justified exact cases;
processor-dependent approximations, image-dependent behavior, explanatory message text,
and undefined pointer association status are not asserted as exact values. Unnumbered
intrinsic argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MOD, MODULO, MOVE_ALLOC, MVBITS,
NEAREST, NEW_LINE, and NEXT found no existing 16.9.145-16.9.151 fixture binding in this
worktree. Existing source-use hits are unrelated coverage: MOD appears in older clause
7, clause 10, and S8.6.11 parameter sources; NEAREST appears in rounding-mode facet
names; NEXT appears in Python helpers and unrelated component names.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression, array,
execution, allocation, pointer, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.6 enumeration types, 7.7 BOZ
literal context, 15.9.3 argument association, 16.3 integer bit model, 16.4 numeric model
and machine-representable terminology, sibling 16.9 intrinsics used by pending plans,
ISO_FORTRAN_ENV STAT constants, and clauses 14 through 19 where future fixtures need
program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.151-007: NEXT returns the following enumerator or repeats the last

**Source:** S16.9.151, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** For A not equal to the last enumerator, NEXT returns the following enumerator; for A
equal to the last enumerator, NEXT returns A.

**Diagnostic obligation:** not-required.

**Facets:** `NEXT-nonlast-returns-following-enumerator`, `NEXT-last-returns-same-enumerator`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic, use conforming companions for zero,
blank, or .FALSE. results, and assert only properties fixed by 16.9: exact integer,
logical, character, kind, shape, allocation, STAT, or bit-pattern observations.
Real-result exact values are limited to separately justified exact cases;
processor-dependent approximations, image-dependent behavior, explanatory message text,
and undefined pointer association status are not asserted as exact values. Unnumbered
intrinsic argument shall-restrictions are source-control restrictions only and are not
promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for MOD, MODULO, MOVE_ALLOC, MVBITS,
NEAREST, NEW_LINE, and NEXT found no existing 16.9.145-16.9.151 fixture binding in this
worktree. Existing source-use hits are unrelated coverage: MOD appears in older clause
7, clause 10, and S8.6.11 parameter sources; NEAREST appears in rounding-mode facet
names; NEXT appears in Python helpers and unrelated component names.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression, array,
execution, allocation, pointer, and I/O foundations from clauses 6 through 13 where
already catalogued. Unregistered dependencies retained: 7.6 enumeration types, 7.7 BOZ
literal context, 15.9.3 argument association, 16.3 integer bit model, 16.4 numeric model
and machine-representable terminology, sibling 16.9 intrinsics used by pending plans,
ISO_FORTRAN_ENV STAT constants, and clauses 14 through 19 where future fixtures need
program, procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.151 -->
