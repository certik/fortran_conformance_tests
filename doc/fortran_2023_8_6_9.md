# Fortran 2023 8.6.9: INTENT statement

Source review is recorded in batch074. Effective status comes from
`Registry.catalogue_review_state("8.6.9")`. All new facets are pending.
The canonical catalogue is `doc/catalogues/intent_statement_8_6_9.json`.

The three original base units are R852, p1 and the unnumbered note, on
physical PDF139, printed125, of J3/24-007, December18,2023, 688 pages,
SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The complete section hash is
`a45d38cf9eb2cc53d21be03e678887cb129d9f7236a2e9120009a964cf962ec7`.
The next original heading is 8.6.10 OPTIONAL statement on the same page.

R852 requires parentheses around an intent-spec and a nonempty dummy-name
list; only the intervening double colon is optional. R828 owns IN, OUT and
INOUT, R401 owns the list convention, and explicit R1534 owns
dummy-arg-name instead of the assumed R402 convention. An unnamed
alternate-return asterisk is not a name in this production.

The syntax does not confer eligibility. C845 requires a dummy data object
or dummy procedure pointer, not an ordinary nonpointer dummy procedure.
Complete formal lists, types and procedure interfaces are retained in the
plans. Subobject designators are not additional dummy names, and C815
continues to govern repeated explicit attributes.

P1 is an attribute-definition route, not an executable state change or a
new local S effect. Nonpointer IN definition restrictions differ from
pointer IN association restrictions and lawful access to a pointer's
live definable target. OUT retains default-initialization qualifications,
finalization and deallocation ordering, actual definability and pointer
association rules. Always-defined zero-sized arrays/zero-length strings
are not presence, allocation or association guarantees. Neither omitted
INTENT nor an undefined value is silently converted to INOUT or a trap.

USE/HOST identity and real dummy status remain necessary. BLOCK
specifications specifically exclude an INTENT statement under C1107;
DIMENSION's BLOCK behavior cannot be imported into this scope.

The C815 INTENT invalid contains the duplicate statement, but its valid
deletion control does not. It remains a C815 compile/positive-control,
not a new R852 witness. Existing array, INTENT, default-initialization,
finalization, BIND and SAVE cases keep all source, metadata, roles and
review records. No old observation is relabelled.

All new facets are pending. Grammar negatives need complete valid
controls, one actual predicate and precise causal/live-origin reporting,
not a fatal-status, English-text or printed-code policy. No compiler,
fixture, no-op classifier, automatic link, source-use, approval or
baseline change is introduced.

<!-- BEGIN GENERATED 8.6.9 -->

### R852: An INTENT statement has a required parenthesized intent and a nonempty dummy-name list

**Source:** 8.6.9 R852, J3/24-007, 18 December 2023, physical PDF139, printed125. **Class:** Syntax.

**Definition:** INTENT is followed by a required parenthesized intent-spec, an optional double colon and
a required nonempty comma-separated dummy-arg-name list. R828 owns the three intent-spec
values; explicit R1534 owns the name production and R401 the list. Syntax does not make
an arbitrary identifier an eligible dummy: C845 separately requires a dummy data object
or dummy procedure pointer, not an ordinary nonpointer dummy procedure.

**Diagnostic obligation:** required.

**Facets:** `parenthesized-intent-spec-forms`, `optional-double-colon`, `multiple-distinct-dummy-names`, `missing-parenthesized-specification`, `missing-dummy-name-list`, `list-separator-repairs`, `name-not-designator-or-alternate-return`, `data-and-procedure-pointer-role-source`, `scope-order-and-duplicate-source`, `canonical-intent-effect-and-state-source`.

**Oracle:** Complete compile/positive-controls for the actual statement grammar and source-isolated
delimiter/list failures with minimal valid repairs. Dummy identity, procedure-pointer
eligibility, intent semantics and state transitions retain their canonical owners. Any
eventual reporting predicate must bind the specific grammar/role condition and
legitimate live source origin after independent review and calibration.

**Oracle limitation:** All facets are pending. Required numbered reporting does not prescribe fatal status,
English wording or printed R852. A bare INTENT/dummy/expected token, wrong role/type,
repeated attribute, missing module/interface, unsupported feature, echo, quoted example,
wrong-source recovery, Internal/ASR/verifier failure, crash or resource error cannot
qualify. Statement acceptance is not invocation, argument transfer, allocation,
initialization or finalization evidence. No compile-only substitute for mandatory
canonical runtime effects, no undefined-value/status read and no universal runtime trap
is invented.

**Dependencies:** 4.1.1;4.1.3R401/p2;4.2p2;8.2;8.5.1C815;8.5.10R828/C845-C849/p1-p6;15.6.2.2R1534;15.6.2.3R1539;11.1.4C1107;14.2.2p9;15.4.2.2;15.4.3.6;15.5.2.4/.5/.8/.10/.14;7.5.4.6p7/p8;7.5.6.3p7;9.7.3.2p6;19.5.1.4;19.6.1/.2/.6/.7/.8.

<!-- END GENERATED 8.6.9 -->
