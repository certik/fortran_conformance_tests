# Fortran 2023 17.11.61: IEEE_SUPPORT_SQRT

The canonical catalogue is `doc/catalogues/ieee_support_sqrt_17_11_61.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 17.11.61 -->

### S17.11.61-001: IEEE_SUPPORT_SQRT X argument shall be real

**Source:** S17.11.61, J3/24-007, 18 December 2023, physical PDF511. **Class:** Restriction.

**Definition:** The X argument of IEEE_SUPPORT_SQRT shall be of type real.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-support-sqrt-x-real`.

**Oracle:** Source registration only. Future Clause 17 fixtures probe the relevant IEEE_SUPPORT_*
inquiry before asserting processor-dependent IEEE behavior. True branches assert only
portable results fixed by the source text: default logical scalar characteristics,
callable argument forms, inquiry-result relations, exact IEEE special-value predicates
under their own support guards, and get/set round trips whose support inquiry is true.
Unsupported branches are recorded as skips, never passes.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SUPPORT_NAN,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_SQRT, IEEE_SUPPORT_STANDARD, IEEE_SUPPORT_SUBNORMAL,
and IEEE_ROUND_TYPE found no existing binding in this worktree. No-X all-real-kind
universality is planned portably by statically generating one representative for every
supported real kind listed by ISO_FORTRAN_ENV REAL_KINDS and comparing the no-X result
with the conjunction of all generated X-form inquiries.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, declarations, expressions, execution, I/O harnesses, exact
intrinsic observations, kind inquiries, and ISO_FORTRAN_ENV real kind context.
Unregistered dependencies retained: earlier Clause 17 IEEE derived types, named
constants, support inquiries, exception flags, rounding modes, IEEE_VALUE, IEEE_CLASS
and predicates, IEEE_NEXT_AFTER, IEEE_GET/SET_ROUNDING_MODE, IEEE_GET/SET_FLAG, and
ISO/IEC 60559 terms.

### S17.11.61-002: IEEE_SUPPORT_SQRT accepts scalar or array X

**Source:** S17.11.61, J3/24-007, 18 December 2023, physical PDF511. **Class:** Effect.

**Definition:** The X argument of IEEE_SUPPORT_SQRT may be a scalar or an array.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-support-sqrt-scalar-x`, `ieee-support-sqrt-array-x`.

**Oracle:** Source registration only. Future Clause 17 fixtures probe the relevant IEEE_SUPPORT_*
inquiry before asserting processor-dependent IEEE behavior. True branches assert only
portable results fixed by the source text: default logical scalar characteristics,
callable argument forms, inquiry-result relations, exact IEEE special-value predicates
under their own support guards, and get/set round trips whose support inquiry is true.
Unsupported branches are recorded as skips, never passes.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SUPPORT_NAN,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_SQRT, IEEE_SUPPORT_STANDARD, IEEE_SUPPORT_SUBNORMAL,
and IEEE_ROUND_TYPE found no existing binding in this worktree. No-X all-real-kind
universality is planned portably by statically generating one representative for every
supported real kind listed by ISO_FORTRAN_ENV REAL_KINDS and comparing the no-X result
with the conjunction of all generated X-form inquiries.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, declarations, expressions, execution, I/O harnesses, exact
intrinsic observations, kind inquiries, and ISO_FORTRAN_ENV real kind context.
Unregistered dependencies retained: earlier Clause 17 IEEE derived types, named
constants, support inquiries, exception flags, rounding modes, IEEE_VALUE, IEEE_CLASS
and predicates, IEEE_NEXT_AFTER, IEEE_GET/SET_ROUNDING_MODE, IEEE_GET/SET_FLAG, and
ISO/IEC 60559 terms.

### S17.11.61-003: IEEE_SUPPORT_SQRT result is a default logical scalar

**Source:** S17.11.61, J3/24-007, 18 December 2023, physical PDF511. **Class:** Effect.

**Definition:** The result of IEEE_SUPPORT_SQRT is a default logical scalar.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-support-sqrt-result-default-logical-scalar`.

**Oracle:** Source registration only. Future Clause 17 fixtures probe the relevant IEEE_SUPPORT_*
inquiry before asserting processor-dependent IEEE behavior. True branches assert only
portable results fixed by the source text: default logical scalar characteristics,
callable argument forms, inquiry-result relations, exact IEEE special-value predicates
under their own support guards, and get/set round trips whose support inquiry is true.
Unsupported branches are recorded as skips, never passes.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SUPPORT_NAN,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_SQRT, IEEE_SUPPORT_STANDARD, IEEE_SUPPORT_SUBNORMAL,
and IEEE_ROUND_TYPE found no existing binding in this worktree. No-X all-real-kind
universality is planned portably by statically generating one representative for every
supported real kind listed by ISO_FORTRAN_ENV REAL_KINDS and comparing the no-X result
with the conjunction of all generated X-form inquiries.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, declarations, expressions, execution, I/O harnesses, exact
intrinsic observations, kind inquiries, and ISO_FORTRAN_ENV real kind context.
Unregistered dependencies retained: earlier Clause 17 IEEE derived types, named
constants, support inquiries, exception flags, rounding modes, IEEE_VALUE, IEEE_CLASS
and predicates, IEEE_NEXT_AFTER, IEEE_GET/SET_ROUNDING_MODE, IEEE_GET/SET_FLAG, and
ISO/IEC 60559 terms.

### S17.11.61-004: IEEE_SUPPORT_SQRT reports ISO/IEC 60559 SQRT conformance for X kind

**Source:** S17.11.61, J3/24-007, 18 December 2023, physical PDF511. **Class:** Effect.

**Definition:** IEEE_SUPPORT_SQRT(X) is true exactly for real kind type parameters for which SQRT
conforms to ISO/IEC 60559:2020, and false otherwise.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-support-sqrt-kind-inquiry`, `ieee-support-sqrt-true-branch-exact-cases`.

**Oracle:** Source registration only. Future Clause 17 fixtures probe the relevant IEEE_SUPPORT_*
inquiry before asserting processor-dependent IEEE behavior. True branches assert only
portable results fixed by the source text: default logical scalar characteristics,
callable argument forms, inquiry-result relations, exact IEEE special-value predicates
under their own support guards, and get/set round trips whose support inquiry is true.
Unsupported branches are recorded as skips, never passes.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SUPPORT_NAN,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_SQRT, IEEE_SUPPORT_STANDARD, IEEE_SUPPORT_SUBNORMAL,
and IEEE_ROUND_TYPE found no existing binding in this worktree. No-X all-real-kind
universality is planned portably by statically generating one representative for every
supported real kind listed by ISO_FORTRAN_ENV REAL_KINDS and comparing the no-X result
with the conjunction of all generated X-form inquiries.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, declarations, expressions, execution, I/O harnesses, exact
intrinsic observations, kind inquiries, and ISO_FORTRAN_ENV real kind context.
Unregistered dependencies retained: earlier Clause 17 IEEE derived types, named
constants, support inquiries, exception flags, rounding modes, IEEE_VALUE, IEEE_CLASS
and predicates, IEEE_NEXT_AFTER, IEEE_GET/SET_ROUNDING_MODE, IEEE_GET/SET_FLAG, and
ISO/IEC 60559 terms.

### S17.11.61-005: IEEE_SUPPORT_SQRT without X reports all-real SQRT support

**Source:** S17.11.61, J3/24-007, 18 December 2023, physical PDF511. **Class:** Effect.

**Definition:** IEEE_SUPPORT_SQRT() is true exactly when IEEE_SUPPORT_SQRT(X) is true for all real X,
and false otherwise.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-support-sqrt-no-x-all-real-relation`.

**Oracle:** Source registration only. Future Clause 17 fixtures probe the relevant IEEE_SUPPORT_*
inquiry before asserting processor-dependent IEEE behavior. True branches assert only
portable results fixed by the source text: default logical scalar characteristics,
callable argument forms, inquiry-result relations, exact IEEE special-value predicates
under their own support guards, and get/set round trips whose support inquiry is true.
Unsupported branches are recorded as skips, never passes.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_SUPPORT_NAN,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_SQRT, IEEE_SUPPORT_STANDARD, IEEE_SUPPORT_SUBNORMAL,
and IEEE_ROUND_TYPE found no existing binding in this worktree. No-X all-real-kind
universality is planned portably by statically generating one representative for every
supported real kind listed by ISO_FORTRAN_ENV REAL_KINDS and comparing the no-X result
with the conjunction of all generated X-form inquiries.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, declarations, expressions, execution, I/O harnesses, exact
intrinsic observations, kind inquiries, and ISO_FORTRAN_ENV real kind context.
Unregistered dependencies retained: earlier Clause 17 IEEE derived types, named
constants, support inquiries, exception flags, rounding modes, IEEE_VALUE, IEEE_CLASS
and predicates, IEEE_NEXT_AFTER, IEEE_GET/SET_ROUNDING_MODE, IEEE_GET/SET_FLAG, and
ISO/IEC 60559 terms.

<!-- END GENERATED 17.11.61 -->
