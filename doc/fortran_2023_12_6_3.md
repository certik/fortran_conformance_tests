# Fortran 2023 12.6.3: Data transfer input/output list

The canonical catalogue is `doc/catalogues/data_transfer_input_output_list_12_6_3.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, R1216, R1217, R1218, R1219, R1220, C1233, C1234, C1235, p2, note1, p3, note2, p4, p5, p6, p7, p8, note3, note4, note5, p9, note6, p10, p11. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.6.3 -->

### S12.6.3-001: An input/output list identifies the entities whose values are transferred

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Effect.

**Definition:** An input/output list specifies the entities whose values are transferred by a data
transfer statement.

**Diagnostic obligation:** not-required.

**Facets:** `io-list-specifies-transferred-entities`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### R1216: An input item is a variable or an I/O implied DO

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Syntax.

**Definition:** An input-item is either a variable or an io-implied-do.

**Diagnostic obligation:** required.

**Facets:** `input-variable-form`, `input-io-implied-do-form`, `input-nonvariable-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### R1217: An output item is an expression or an I/O implied DO

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Syntax.

**Definition:** An output-item is either an expr or an io-implied-do.

**Diagnostic obligation:** required.

**Facets:** `output-expression-form`, `output-io-implied-do-form`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### R1218: An I/O implied DO has an object list and control in parentheses

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Syntax.

**Definition:** An io-implied-do is a parenthesized io-implied-do-object-list followed by a comma and an
io-implied-do-control.

**Diagnostic obligation:** required.

**Facets:** `io-implied-do-parenthesized-form`, `io-implied-do-object-list-form`, `io-implied-do-control-form`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### R1219: An I/O implied-DO object is an input item or an output item

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Syntax.

**Definition:** An io-implied-do-object is either an input-item or an output-item.

**Diagnostic obligation:** required.

**Facets:** `implied-do-object-input-item-form`, `implied-do-object-output-item-form`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### R1220: An I/O implied-DO control specifies a do variable, bounds, and optional increment

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Syntax.

**Definition:** An io-implied-do-control is a do-variable, an equals sign, two scalar integer
expressions for initial and terminal values, and an optional scalar integer expression
increment.

**Diagnostic obligation:** required.

**Facets:** `implied-do-control-do-variable`, `implied-do-control-initial-expr`, `implied-do-control-terminal-expr`, `implied-do-control-optional-increment`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### C1233: Whole assumed-size arrays are not input items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Restriction.

**Definition:** A variable that is an input-item shall not be a whole assumed-size array.

**Diagnostic obligation:** required.

**Facets:** `whole-assumed-size-input-item-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### C1234: Implied-DO objects match the surrounding input or output list

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Restriction.

**Definition:** In an input-item-list, an io-implied-do-object shall be an input-item. In an
output-item-list, an io-implied-do-object shall be an output-item.

**Diagnostic obligation:** required.

**Facets:** `input-list-implied-do-object-input-item`, `input-list-output-expression-object-rejected`, `output-list-implied-do-object-output-item`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### C1235: Output item expressions do not have procedure pointer values

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Restriction.

**Definition:** An expression that is an output-item shall not have a value that is a procedure pointer.

**Diagnostic obligation:** required.

**Facets:** `ordinary-output-expression-control`, `procedure-pointer-output-expression-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-002: Input items are not the do variable of a containing I/O implied DO

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Restriction.

**Definition:** An input-item shall not appear as, nor be associated with, the do-variable of any
io-implied-do that contains the input-item.

**Diagnostic obligation:** not-required.

**Facets:** `input-item-not-containing-do-variable`, `input-item-not-associated-with-containing-do-variable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-003: Input pointers are associated with definable targets and receive transferred data

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Restriction.

**Definition:** If an input item is a pointer, it shall be associated with a definable target, and data
are transferred from the file to the associated target.

**Diagnostic obligation:** not-required.

**Facets:** `input-pointer-definable-associated-target`, `input-pointer-transfer-to-target`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-004: Output pointers are associated with targets and transfer target values

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF262. **Class:** Restriction.

**Definition:** If an output item is a pointer, it shall be associated with a target, and data are
transferred from the target to the file.

**Diagnostic obligation:** not-required.

**Facets:** `output-pointer-associated-target`, `output-pointer-transfer-from-target`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-005: Allocatable list items are allocated

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** If an input item or an output item is allocatable, it shall be allocated.

**Diagnostic obligation:** not-required.

**Facets:** `allocatable-input-item-allocated`, `allocatable-output-item-allocated`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-006: Defined input/output admits polymorphic list-item processing

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** A polymorphic list item is admitted when it is processed by a defined input/output
procedure.

**Diagnostic obligation:** not-required.

**Facets:** `polymorphic-list-item-dtio-admitted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-007: Non-DTIO list items are not polymorphic

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** A list item shall not be polymorphic unless it is processed by a defined input/output
procedure.

**Diagnostic obligation:** not-required.

**Facets:** `polymorphic-list-item-without-dtio-restricted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-008: Enumeration list items do not appear in list-directed transfers

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** A list item of an enumeration type shall not appear in a list-directed data transfer
statement.

**Diagnostic obligation:** not-required.

**Facets:** `enum-list-directed-transfer-restricted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-009: Formatted enumeration list items correspond to I, B, O, or Z editing

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** In a formatted data transfer statement, a list item of enumeration type shall correspond
to an I, B, O, or Z edit descriptor.

**Diagnostic obligation:** not-required.

**Facets:** `enum-formatted-i-descriptor`, `enum-formatted-b-o-z-descriptors`, `enum-formatted-other-descriptor-restricted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-010: Nested I/O implied-DO variables are distinct from containing do variables

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** The do-variable of an io-implied-do that is in another io-implied-do shall not appear
as, nor be associated with, the do-variable of the containing io-implied-do.

**Diagnostic obligation:** not-required.

**Facets:** `nested-implied-do-variable-not-containing-variable`, `nested-implied-do-variable-not-associated-with-containing-variable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-011: Input/output list expansion rules are reapplied until none apply

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** The rules for expanding input/output list items are reapplied to each expanded list item
until none of the rules apply.

**Diagnostic obligation:** not-required.

**Facets:** `io-list-expansion-rules-reapplied`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-012: Array list items expand in array element order

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** If an array appears as an input/output list item, it is treated as if its elements, if
any, were specified in array element order.

**Diagnostic obligation:** not-required.

**Facets:** `array-list-item-expands-in-element-order`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-013: Input array elements do not affect expressions in the input item

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** No element of an array input/output list item shall affect the value of any expression
in the input-item.

**Diagnostic obligation:** not-required.

**Facets:** `input-array-element-not-affect-expression`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-014: Array elements do not appear more than once in a given input item

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** No array element shall appear more than once in a given input-item.

**Diagnostic obligation:** not-required.

**Facets:** `input-array-element-not-duplicated`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-015: Unformatted derived effective items expand when a subobject has defined I/O

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** If an effective item of derived type in an unformatted input/output statement is not
processed by a defined input/output procedure, and any subobject would be processed by
one, the effective item is treated as its components in component order; those
components shall be accessible and shall not be pointers or allocatable.

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-derived-dtio-subobject-component-expansion`, `unformatted-derived-dtio-components-accessible`, `unformatted-derived-dtio-components-not-pointer-allocatable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-016: Unformatted non-DTIO derived effective items are treated as a single value

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** An effective item of derived type in an unformatted input/output statement is treated as
a single value unless the effective item or a subobject thereof is processed by a
defined input/output procedure.

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-derived-single-value-treatment`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-028: Unformatted non-DTIO derived effective item form is processor dependent

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** For an effective item of derived type in an unformatted input/output statement that is
not processed by a defined input/output procedure and has no subobject processed by one,
the single-value form is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-derived-representation-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-017: Formatted derived effective items expand into accessible nonpointer nonallocatable components

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** If an effective item of derived type in a formatted input/output statement is not
processed by a defined input/output procedure, it is treated as its components in
component order; those components shall be accessible and shall not be pointers or
allocatable.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-derived-component-order-expansion`, `formatted-derived-components-accessible`, `formatted-derived-components-not-pointer-allocatable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-018: Unexpanded non-DTIO derived list items have accessible nonpointer nonallocatable subcomponents

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Restriction.

**Definition:** If a derived-type list item is not processed by a defined input/output procedure and is
not treated as a list of its individual components, all its subcomponents shall be
accessible in the scoping unit and shall not be pointers or allocatable.

**Diagnostic obligation:** not-required.

**Facets:** `unexpanded-derived-subcomponents-accessible`, `unexpanded-derived-subcomponents-not-pointer-allocatable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-019: I/O implied-DO loop initialization and execution match DO constructs

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF263. **Class:** Effect.

**Definition:** For an io-implied-do, the loop initialization and execution are the same as for a DO
construct.

**Diagnostic obligation:** not-required.

**Facets:** `io-implied-do-do-construct-execution`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-020: Scalar objects after expansion are effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Effect.

**Definition:** The scalar objects resulting when data-transfer list items are expanded according to the
array and derived-type rules are called effective items.

**Diagnostic obligation:** not-required.

**Facets:** `effective-items-are-expanded-scalar-objects`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-021: Zero-sized arrays contribute no effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Effect.

**Definition:** Zero-sized arrays do not contribute to the list of effective items.

**Diagnostic obligation:** not-required.

**Facets:** `zero-sized-array-no-effective-items`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-022: Zero-iteration I/O implied DOs contribute no effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Effect.

**Definition:** I/O implied DOs with an iteration count of zero do not contribute to the list of
effective items.

**Diagnostic obligation:** not-required.

**Facets:** `zero-count-implied-do-no-effective-items`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-023: Zero-length scalar character items are effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Effect.

**Definition:** A scalar character item of zero length is an effective item.

**Diagnostic obligation:** not-required.

**Facets:** `zero-length-character-is-effective-item`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-024: Default-character internal files exclude nondefault character effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Restriction.

**Definition:** An input/output list shall not contain an effective item of nondefault character kind if
the data transfer statement specifies an internal file of default character kind.

**Diagnostic obligation:** not-required.

**Facets:** `default-internal-file-nondefault-character-restricted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-025: ISO 10646 internal files admit only default, ISO 10646, or ASCII character effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Restriction.

**Definition:** If the data transfer statement specifies an internal file of ISO 10646 character kind,
the input/output list shall not contain an effective item that is nondefault character
except for ISO 10646 or ASCII character.

**Diagnostic obligation:** not-required.

**Facets:** `iso10646-internal-file-character-kind-set`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-026: ASCII internal files admit only ASCII character effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Restriction.

**Definition:** If the data transfer statement specifies an ASCII character internal file, the
input/output list shall not contain an effective item of type character of any kind
other than ASCII.

**Diagnostic obligation:** not-required.

**Facets:** `ascii-internal-file-only-ascii-character`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

### S12.6.3-027: Output lists do not contain BOZ literal effective items

**Source:** S12.6.3, J3/24-007, 18 December 2023, physical PDF264. **Class:** Restriction.

**Definition:** An output list shall not contain an effective item that is a boz-literal-constant.

**Diagnostic obligation:** not-required.

**Facets:** `boz-literal-output-effective-item-restricted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by reading known internal records into nonzero/nonblank sentinels, writing
to sentinel-prefilled internal files, initializing pointed-to or allocatable targets, or
using compile-only conforming controls) and then observe only portable properties: exact
internal records, successful IOSTAT zero where status is owned by a later rule, required
diagnosis of a numbered syntax rule or constraint, or source-control admission/rejection
without relying on IOMSG text, external byte representation, processor-dependent unit
numbers, or specific numeric status values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Diagnostic facets are future source-control plans only where the cited numbered rule is
reached by a one-property change; unnumbered shall-not rules are not promoted to
required diagnostics. IOMSG wording, processor-dependent external representations,
defined-I/O side-effect ordering beyond what the source fixes, and processor-dependent
character-kind support are never asserted as portable oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, existing 12.6.1 and 12.6.2.1
catalogues, and existing clause 13 catalogues for source form, declaration attributes,
variables, expressions, execution constructs, data-transfer statement forms,
io-control-spec placement, and formatting syntax where pending controls need them.
Unregistered dependencies explicitly retained: 12.6.2.2-12.6.2.16 specifier subclauses;
later 12.6.4 transfer execution and defined I/O; 12.7-12.11 connection, internal-file,
status, and asynchronous semantics; clause 15 defined input/output procedures; clause 16
inquiry and selected character kind intrinsics where future guarded plans need them;
clause 19 scope and accessibility controls.

<!-- END GENERATED 12.6.3 -->
