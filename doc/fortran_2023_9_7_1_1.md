# Fortran 2023: 9.7.1.1 The ALLOCATE statement

Source-only draft catalogue: `doc/catalogues/allocate_statement_9_7_1_1.json`.
No Fortran test program, compiler invocation, execution, oracle approval, fixture approval, or coverage claim is supplied.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Forty-one base units: p1, R929-R941, C936-C955, note1, p2-p6, and note2. Fine units record grammar components, allocation-object eligibility, type/source/shape/coarray constraints, dependency restrictions, and informative examples.

All facets in this packet are pending source plans. Multi-image coarray allocation, failed/stopped image behavior, and dynamic-type execution observations are separate gated capabilities.

<!-- BEGIN GENERATED 9.7.1.1 -->

### S9.7.1.1-001: ALLOCATE dynamically creates pointer targets and allocatable variables

**Source:** 9.7.1.1 p1, J3/24-007, 18 December 2023, physical PDF160. **Class:** Effect.

**Definition:** An ALLOCATE statement is the statement form that dynamically creates pointer targets and
allocatable variables. This high-level effect does not by itself specify allocation
status, bounds, initialization, error handling, or pointer association details, which
are owned by later allocation-status and execution subclauses.

**Diagnostic obligation:** not-required.

**Facets:** `allocatable-variable-created`, `pointer-target-created`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.3p1;8.5.14p1-p3;9.7.1.2p1-p11;9.7.1.3p1-p6;9.7.4p1-p6.

### R929: An ALLOCATE statement has optional type-spec, allocation list, and optional allocation options

**Source:** 9.7.1.1 R929, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for allocate-stmt is ALLOCATE followed by parentheses containing an optional
type-spec and double-colon, a required allocation-list, and an optional comma followed
by an alloc-opt-list. R929 owns only the statement form and placement of those pieces;
type-spec, allocation, and alloc-opt internals are owned by their own rules.

**Diagnostic obligation:** required.

**Facets:** `bare-allocation-list`, `typed-allocation`, `allocation-with-options`, `missing-allocation-list-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;7.3.2.1R702;9.7.1.1R930/R933/C936-C955;9.7.4R946;9.7.5.

### R930: An allocation option is ERRMSG, MOLD, SOURCE, or STAT

**Source:** 9.7.1.1 R930, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for alloc-opt admits exactly ERRMSG=errmsg-variable, MOLD=source-expr,
SOURCE=source-expr, or STAT=stat-variable. R930 admits the four keyword forms; repeated
options, mutual exclusions, and source/mold semantics are separate requirements.

**Diagnostic obligation:** required.

**Facets:** `stat-option`, `errmsg-option`, `source-option`, `mold-option`, `unknown-option-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;9.7.1.1R931-R932/C948-C954;9.7.1.2p8-p11;9.7.4R946/p1-p6;9.7.5.

### R931: An ERRMSG variable is a scalar default character variable

**Source:** 9.7.1.1 R931, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for errmsg-variable requires a variable that is scalar and of default
character type. The explanatory-message effect for allocation errors is owned by 9.7.5;
this rule only identifies the syntactic variable class usable after ERRMSG=.

**Diagnostic obligation:** required.

**Facets:** `scalar-default-character-variable`, `nondefault-or-nonscalar-errmsg-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;7.4.4 character type;9.2R906/C906;9.7.5.

### R932: A source expression is an expression

**Source:** 9.7.1.1 R932, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for source-expr is expr. SOURCE= and MOLD= both use this source-expr
nonterminal; their type, rank, value-definition, and execution semantics are imposed by
constraints and 9.7.1.2, not by R932 itself.

**Diagnostic obligation:** required.

**Facets:** `literal-source-expr`, `variable-source-expr`, `malformed-source-expr-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;10.1 expressions;9.7.1.1C950-C954;9.7.1.2p6-p9.

### R933: An allocation is either an allocate-object form or a parenthesized bounds form

**Source:** 9.7.1.1 R933, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for allocation admits either an allocate-object optionally followed by an
allocate-shape-spec-list and optionally an allocate-coarray-spec in brackets, or a
parenthesized optional lower-bounds-expr and required upper-bounds-expr optionally
followed by an allocate-coarray-spec in brackets. The second form supplies bounds for a
prior allocation item in the allocation-list context; rank, corank, and coarray validity
are constrained elsewhere.

**Diagnostic obligation:** required.

**Facets:** `object-allocation-scalar`, `object-allocation-with-shape`, `bounds-allocation-form`, `coarray-bracket-form`, `malformed-allocation-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;8.5.6.2;9.7.1.1R934-R941/C943-C947;9.7.1.2p1-p5.

### R934: An allocate object is a variable name or structure component

**Source:** 9.7.1.1 R934, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for allocate-object admits a variable-name or a structure-component. It does
not admit arbitrary designators, array elements, array sections, function references,
coindexed objects, substrings, or complex-part designators; C936 then restricts the
admitted object to a data pointer or allocatable variable and C955 excludes coindexed
objects.

**Diagnostic obligation:** required.

**Facets:** `variable-name-object`, `structure-component-object`, `array-element-or-function-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;9.1R901;9.2R903;9.4.2R911-R913/C909-C920;9.7.1.1C936/C955.

### R935: An allocate shape spec has optional lower bound and required upper bound

**Source:** 9.7.1.1 R935, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for allocate-shape-spec is an optional lower-bound-expr followed by a colon
and a required upper-bound-expr, or just the required upper-bound-expr when the lower
bound is omitted. Semantics for default lower bound one and zero extent are in 9.7.1.2.

**Diagnostic obligation:** required.

**Facets:** `explicit-lower-and-upper`, `omitted-lower-bound`, `missing-upper-bound-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;9.7.1.1R936/R938/C946;9.7.1.2p1.

### R936: A lower bound expression is a scalar integer expression

**Source:** 9.7.1.1 R936, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for lower-bound-expr is scalar-int-expr. It owns integer expression
formation for allocation bounds; evaluation, defaulting, freezing, and zero extent are
execution semantics in 9.7.1.2.

**Diagnostic obligation:** required.

**Facets:** `scalar-integer-lower-bound`, `nonscalar-or-noninteger-lower-bound-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;7.4.1 integer type;10.1 expressions;9.7.1.1R933/R935/C947;9.7.1.2p1-p2.

### R937: A lower-bounds expression is an integer expression

**Source:** 9.7.1.1 R937, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for lower-bounds-expr is int-expr. It owns integer expression formation for
allocation bounds; evaluation, defaulting, freezing, and zero extent are execution
semantics in 9.7.1.2.

**Diagnostic obligation:** required.

**Facets:** `integer-lower-bounds-expr`, `noninteger-lower-bounds-expr-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;7.4.1 integer type;10.1 expressions;9.7.1.1R933/R935/C947;9.7.1.2p1-p2.

### R938: An upper bound expression is a scalar integer expression

**Source:** 9.7.1.1 R938, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for upper-bound-expr is scalar-int-expr. It owns integer expression
formation for allocation bounds; evaluation, defaulting, freezing, and zero extent are
execution semantics in 9.7.1.2.

**Diagnostic obligation:** required.

**Facets:** `scalar-integer-upper-bound`, `nonscalar-or-noninteger-upper-bound-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;7.4.1 integer type;10.1 expressions;9.7.1.1R933/R935/C947;9.7.1.2p1-p2.

### R939: An upper-bounds expression is an integer expression

**Source:** 9.7.1.1 R939, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for upper-bounds-expr is int-expr. It owns integer expression formation for
allocation bounds; evaluation, defaulting, freezing, and zero extent are execution
semantics in 9.7.1.2.

**Diagnostic obligation:** required.

**Facets:** `integer-upper-bounds-expr`, `noninteger-upper-bounds-expr-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;7.4.1 integer type;10.1 expressions;9.7.1.1R933/R935/C947;9.7.1.2p1-p2.

### R940: An allocate coarray spec has optional prior coshape specs and a final asterisk codimension

**Source:** 9.7.1.1 R940, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for allocate-coarray-spec is an optional allocate-coshape-spec-list followed
by a comma, then an optional lower-bound-expr and colon, then a required asterisk. The
final codimension is marked by the asterisk; any prior codimensions are supplied by
allocate-coshape-spec entries.

**Diagnostic obligation:** required.

**Facets:** `final-star-only`, `prior-coshape-specs`, `final-lower-bound`, `missing-final-star-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;8.5.6.2R810/C827;9.7.1.1R941/C945-C946;9.7.1.2p3-p5;11.7.2;11.7.3.

### R941: An allocate coshape spec has optional lower bound and required upper bound

**Source:** 9.7.1.1 R941, J3/24-007, 18 December 2023, physical PDF160. **Class:** Syntax.

**Definition:** The grammar for allocate-coshape-spec is an optional lower-bound-expr followed by a
colon and a required upper-bound-expr, or just the required upper-bound-expr when the
lower bound is omitted. It applies only to the nonfinal codimensions before R940's final
asterisk.

**Diagnostic obligation:** required.

**Facets:** `explicit-cobounds`, `omitted-lower-cobound`, `missing-upper-cobound-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.1.3;4.2;9.7.1.1R936/R938/R940/C946;9.7.1.2p3-p5.

### C936: Each allocate object is a data pointer or allocatable variable

**Source:** 9.7.1.1 C936, J3/24-007, 18 December 2023, physical PDF160. **Class:** Restriction.

**Definition:** For every R934 allocate-object in the statement, the object shall be either a data
pointer or an allocatable variable. Ordinary nonpointer nonallocatable variables,
procedure pointers, function references, constants, array elements not admitted by R934,
and ineligible components do not satisfy this requirement.

**Diagnostic obligation:** required.

**Facets:** `allocatable-variable`, `data-pointer`, `ordinary-variable-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.3p1;8.5.14p1-p3;9.2R903/C903;9.7.1.1R934/C955.

### C937: Deferred, unlimited polymorphic, or abstract allocate objects require type-spec or source-expr

**Source:** 9.7.1.1 C937, J3/24-007, 18 December 2023, physical PDF160. **Class:** Restriction.

**Definition:** If any allocate-object has a deferred type parameter, is unlimited polymorphic, or is of
abstract type, the ALLOCATE statement shall include either a type-spec or a source-expr.
Both SOURCE= and MOLD= contain the R932 source-expr nonterminal; value initialization
remains specific to SOURCE= in 9.7.1.2.

**Diagnostic obligation:** required.

**Facets:** `deferred-parameter-covered`, `unlimited-polymorphic-covered`, `abstract-type-covered`, `missing-type-or-source-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.2.1R702/C703;7.3.2.3p4-p5;7.4.4.2;7.5.7.1;9.7.1.1R929/R932/C949-C951.

### C938: A type-spec must be type compatible with every allocate object

**Source:** 9.7.1.1 C938, J3/24-007, 18 December 2023, physical PDF160. **Class:** Restriction.

**Definition:** When the optional type-spec appears in an ALLOCATE statement, it shall specify a type
with which each allocate-object is type compatible. Type compatibility is the 7.3.3
relation; C938 does not itself define type-spec syntax or dynamic type selection.

**Diagnostic obligation:** required.

**Facets:** `same-type-compatible`, `extension-compatible-polymorphic`, `incompatible-type-spec-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.2.1R702;7.3.3p1-p2;9.7.1.1R929/C940.

### C939: Asterisk type parameter values in type-spec correspond exactly to assumed dummy parameters

**Source:** 9.7.1.1 C939, J3/24-007, 18 December 2023, physical PDF160. **Class:** Restriction.

**Definition:** A type-param-value in an ALLOCATE type-spec shall be an asterisk if and only if every
allocate-object is a dummy argument for which the corresponding type parameter is
assumed. Thus an asterisk is required only in that all-object dummy-assumed case and
prohibited otherwise.

**Diagnostic obligation:** required.

**Facets:** `asterisk-for-all-assumed-dummy-parameters`, `asterisk-for-nonassumed-or-nondummy-rejected`, `missing-required-asterisk-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.4.4.2 type-param-values;7.5.9 assumed type parameters;15.5.2.4 argument
association;9.7.1.1R929/p6.

### C940: Type-spec kind type parameter values match every allocate object

**Source:** 9.7.1.1 C940, J3/24-007, 18 December 2023, physical PDF160. **Class:** Restriction.

**Definition:** If type-spec appears, each allocate-object shall have kind type parameter values equal
to the corresponding type parameter values in the type-spec. Length parameters are
governed by other requirements and possible execution error conditions; C940 is about
kind type parameters.

**Diagnostic obligation:** required.

**Facets:** `matching-kind`, `mismatched-kind-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.2 type parameters;7.4.3.2 integer type parameters;9.7.1.1R929/C938/p5-p6.

### C941: Coarray allocation type-spec excludes C_PTR, C_FUNPTR, and TEAM_TYPE

**Source:** 9.7.1.1 C941, J3/24-007, 18 December 2023, physical PDF160. **Class:** Restriction.

**Definition:** If an allocate-object is a coarray, a type-spec in the ALLOCATE statement shall not
specify type C_PTR or C_FUNPTR from ISO_C_BINDING, or type TEAM_TYPE from
ISO_FORTRAN_ENV. The exclusion is by intrinsic-module type identity, not spelling of
unrelated local types.

**Diagnostic obligation:** required.

**Facets:** `ordinary-coarray-type-spec`, `c-ptr-c-funptr-team-type-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.6.1C824;9.7.1.1R929/C938;16.10.2.34 TEAM_TYPE;18.3.2 C_PTR/C_FUNPTR.

### C942: Unlimited polymorphic type-spec allocation excludes coarray potential subobject components

**Source:** 9.7.1.1 C942, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** If an allocate-object is unlimited polymorphic and type-spec appears, the type-spec
shall not specify a type that has a coarray potential subobject component.
Potential-subobject ownership stays with derived-type rules; this constraint applies to
the type selected for allocation.

**Diagnostic obligation:** required.

**Facets:** `unlimited-polymorphic-ordinary-type`, `coarray-potential-subobject-type-spec-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.2.3p4-p5;7.5.4.1 potential subobject components;8.5.6.1;9.7.1.1R929/C938.

### C943: Array allocation needs explicit shape/bounds or same-rank source

**Source:** 9.7.1.1 C943, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** If an allocate-object is an array, its allocation shall include either an
allocate-shape-spec-list or upper-bounds-expr, unless source-expr appears in the
ALLOCATE statement and has the same rank as the allocate-object. A scalar SOURCE= can
initialize elements only after a shape has otherwise been supplied.

**Diagnostic obligation:** required.

**Facets:** `shape-spec-list-for-array`, `upper-bounds-expr-for-array`, `same-rank-source-alternative`, `array-without-shape-or-same-rank-source-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.8.4 deferred shape;9.7.1.1R933/R935/R939/C946-C950;9.7.1.2p1-p2/p7-p8.

### C944: Scalar allocate objects do not have allocate-shape-spec lists

**Source:** 9.7.1.1 C944, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** If the allocate-object is scalar, an allocate-shape-spec-list shall not appear for that
object's allocation. Scalar allocation may still have type-spec, SOURCE, MOLD, STAT,
ERRMSG, and for scalar coarrays an allocate-coarray-spec when C945 requires it.

**Diagnostic obligation:** required.

**Facets:** `scalar-without-shape`, `scalar-shape-spec-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.8.1 rank;9.7.1.1R933/R935/C943/C946.

### C945: An allocate coarray spec appears exactly for coarray allocate objects

**Source:** 9.7.1.1 C945, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** An allocate-coarray-spec shall appear if and only if the allocate-object is a coarray.
It is required for coarray allocation and prohibited for noncoarray allocation.

**Diagnostic obligation:** required.

**Facets:** `coarray-requires-coarray-spec`, `noncoarray-prohibits-coarray-spec`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.6.1p1;8.5.6.2R810/C827;9.7.1.1R933/R940/C946;9.7.1.2p3-p5.

### C946: Shape spec count equals rank and coshape spec count equals corank minus one

**Source:** 9.7.1.1 C946, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** The number of allocate-shape-specs in an allocate-shape-spec-list shall equal the rank
of the allocate-object. The number of allocate-coshape-specs in an allocate-coarray-spec
shall be one less than the corank of the allocate-object; the final codimension is
represented by R940's asterisk.

**Diagnostic obligation:** required.

**Facets:** `shape-count-equals-rank`, `shape-count-mismatch-rejected`, `coshape-count-equals-corank-minus-one`, `coshape-count-mismatch-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.6.2p1-p5;8.5.8.1;9.7.1.1R933/R940-R941/C943-C945;9.7.1.2p1-p5.

### C947: Bounds-array expressions in the parenthesized allocation form have constant size equal to rank

**Source:** 9.7.1.1 C947, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** For an allocation using upper-bounds-expr and optionally lower-bounds-expr, if both
appear at least one of them shall be a rank-one array of constant size equal to the
allocate-object rank. If only upper-bounds-expr appears, it shall be a rank-one array of
constant size equal to that rank. Scalar broadcasting, when permitted, is execution
semantics in 9.7.1.2p2.

**Diagnostic obligation:** required.

**Facets:** `both-bounds-one-rank-array`, `upper-only-rank-array`, `wrong-size-or-rank-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.8.1 rank;10.1.12 constant expressions;9.7.1.1R933/R937/R939;9.7.1.2p2.

### C948: No allocation option appears more than once

**Source:** 9.7.1.1 C948, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** Within a given alloc-opt-list, no alloc-opt keyword shall appear more than once.
Distinct options may appear together subject to C949 and SOURCE/MOLD-specific
requirements.

**Diagnostic obligation:** required.

**Facets:** `distinct-options`, `duplicate-stat-rejected`, `duplicate-source-or-mold-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;9.7.1.1R930/C949-C954;9.7.4R946;9.7.5.

### C949: SOURCE expression and type-spec are mutually exclusive

**Source:** 9.7.1.1 C949, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** At most one of source-expr and type-spec shall appear in an ALLOCATE statement. Because
both SOURCE= and MOLD= contain a source-expr, either option is mutually exclusive with
the optional type-spec.

**Diagnostic obligation:** required.

**Facets:** `type-spec-without-source`, `source-without-type-spec`, `type-spec-and-source-rejected`, `type-spec-with-mold-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;9.7.1.1R929-R932/C937/C950-C954;9.7.1.2p8-p9.

### C950: Each allocate object is type compatible with source-expr and SOURCE rank is scalar or matching

**Source:** 9.7.1.1 C950, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** Each allocate-object shall be type compatible with source-expr. If the SOURCE= specifier
appears, source-expr shall be scalar or have the same rank as each allocate-object. Type
compatibility is 7.3.3; value copying and scalar expansion are execution semantics in
9.7.1.2p8.

**Diagnostic obligation:** required.

**Facets:** `type-compatible-source`, `scalar-source-for-array-with-shape`, `same-rank-source`, `incompatible-source-rejected`, `wrong-rank-source-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.3p1-p2;8.5.8.1 rank;9.7.1.1R932/C943/C951;9.7.1.2p6-p8.

### C951: Source expression kind type parameters match every allocate object

**Source:** 9.7.1.1 C951, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** If source-expr appears, the kind type parameters of each allocate-object shall have the
same values as the corresponding type parameters of source-expr. Length parameter
disagreement can be an execution error under 9.7.1.2p10; C951 is the kind-parameter
constraint.

**Diagnostic obligation:** required.

**Facets:** `matching-source-kind`, `mismatched-source-kind-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.2 type parameters;7.4.3.2;9.7.1.1R932/C950;9.7.1.2p10.

### C952: Coarray SOURCE declared type excludes C_PTR, C_FUNPTR, and TEAM_TYPE

**Source:** 9.7.1.1 C952, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** If any allocate-object is a coarray, the declared type of source-expr shall not be C_PTR
or C_FUNPTR from ISO_C_BINDING or TEAM_TYPE from ISO_FORTRAN_ENV. The rule concerns
declared type, not dynamic type; p4 separately restricts certain dynamic types for
SOURCE= with coarrays.

**Diagnostic obligation:** required.

**Facets:** `ordinary-source-for-coarray`, `special-declared-source-type-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;8.5.6.1C824;9.7.1.1R929-R932/C950-C951;16.10.2.34 TEAM_TYPE;18.3.2 C_PTR/C_FUNPTR.

### C953: Unlimited polymorphic SOURCE declared type excludes coarray potential subobject components

**Source:** 9.7.1.1 C953, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** If an allocate-object is unlimited polymorphic, the declared type of source-expr shall
not be a type that has a coarray potential subobject component. This is a declared-type
constraint; p5 also states a dynamic-type restriction for unlimited polymorphic
allocation from source-expr.

**Diagnostic obligation:** required.

**Facets:** `ordinary-declared-source-for-unlimited-polymorphic`, `declared-source-coarray-potential-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.2.3p4;7.5.4.1 potential subobjects;8.5.6.1;9.7.1.1R932/C950-C951/p5.

### C954: SOURCE excludes EVENT, LOCK, NOTIFY, coarray, and special potential subobjects

**Source:** 9.7.1.1 C954, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** If SOURCE= appears, the declared type of source-expr shall not be EVENT_TYPE, LOCK_TYPE,
or NOTIFY_TYPE from ISO_FORTRAN_ENV, and shall not have a potential subobject component
that is a coarray or of type EVENT_TYPE, LOCK_TYPE, or NOTIFY_TYPE. MOLD= is not SOURCE=
for this constraint.

**Diagnostic obligation:** required.

**Facets:** `ordinary-source-admitted`, `special-declared-type-rejected`, `coarray-potential-subobject-rejected`, `event-lock-notify-potential-subobject-rejected`, `mold-not-source-distinction`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.5.4.1 potential subobjects;8.5.6.1;9.7.1.1R930/C950-C953;16.10.2.10
EVENT_TYPE;16.10.2.19 LOCK_TYPE;16.10.2.22 NOTIFY_TYPE.

### C955: An allocate object is not coindexed

**Source:** 9.7.1.1 C955, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** An allocate-object shall not be a coindexed object. A pointer or allocatable component
of a coarray may be allocated by its own image when named without an image selector, but
a coindexed selection such as x[image]%component is not an allocate-object.

**Diagnostic obligation:** required.

**Facets:** `local-coarray-component-admitted`, `coindexed-object-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;9.4.2R912/C914-C916;9.4.3R914/C921;9.6R926-R928;9.7.1.1R934/C936;9.7.1.2p5.

### S9.7.1.1-002: ALLOCATE statement inquiry operands are independent of STAT, ERRMSG, and peer allocation state

**Source:** 9.7.1.1 p2, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** Within a single ALLOCATE statement, an allocate-object and any bound or type parameter
of an allocate-object shall not depend on the value of stat-variable, the value of
errmsg-variable, or on the value, bounds, length type parameters, allocation status, or
association status of any allocate-object in that same statement. This prevents
intra-statement circular dependencies; it does not create a static diagnostic for
execution-only allocation or association state in every case.

**Diagnostic obligation:** not-required.

**Facets:** `no-stat-dependence`, `no-errmsg-dependence`, `no-peer-allocation-status-dependence`, `independent-control`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;9.7.1.1R929-R941;9.7.4R946/p3;9.7.5;9.7.1.2p1-p3/p10;9.7.1.3p1-p6;8.5.14p2-p3;19.5.5p5.

### S9.7.1.1-003: SOURCE expression is not allocated within or dependent on allocate objects in the same statement

**Source:** 9.7.1.1 p3, J3/24-007, 18 December 2023, physical PDF161. **Class:** Restriction.

**Definition:** The source-expr of SOURCE= or MOLD= shall not be allocated within the ALLOCATE statement
in which it appears, and shall not depend on the value, bounds, deferred type
parameters, allocation status, or association status of any allocate-object in that
statement. This is a source-expression independence rule, separate from C950-C954
type/rank restrictions.

**Diagnostic obligation:** not-required.

**Facets:** `source-not-allocated-in-same-statement`, `source-no-value-or-bounds-dependence`, `source-no-status-dependence`, `independent-source-control`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;9.7.1.1R930/R932/C950-C954;9.7.1.2p6-p9;9.7.1.3p1-p6;8.5.14p2-p3;19.5.5p5.

### S9.7.1.1-004: Coarray SOURCE dynamic type excludes C_PTR, C_FUNPTR, EVENT, LOCK, NOTIFY, TEAM, and special subcomponents

**Source:** 9.7.1.1 p4, J3/24-007, 18 December 2023, physical PDF162. **Class:** Restriction.

**Definition:** If an ALLOCATE statement has a SOURCE= specifier and an allocate-object that is a
coarray, source-expr shall not have a dynamic type of C_PTR or C_FUNPTR from
ISO_C_BINDING, EVENT_TYPE, LOCK_TYPE, NOTIFY_TYPE, or TEAM_TYPE from ISO_FORTRAN_ENV,
nor have a subcomponent whose dynamic type is EVENT_TYPE, LOCK_TYPE, NOTIFY_TYPE, or
TEAM_TYPE. This is an execution-time dynamic-type restriction and is distinct from
C952's declared-type condition.

**Diagnostic obligation:** not-required.

**Facets:** `ordinary-dynamic-source-for-coarray`, `forbidden-dynamic-type-source`, `forbidden-dynamic-subcomponent`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.2.3p5;8.5.6.1;9.7.1.1C952-C954;9.7.1.2p4-p8;16.10.2.10 EVENT_TYPE;16.10.2.19
LOCK_TYPE;16.10.2.22 NOTIFY_TYPE;16.10.2.34 TEAM_TYPE;18.3.2 C_PTR/C_FUNPTR.

### S9.7.1.1-005: Allocation chooses dynamic type and type parameter values from type-spec, source-expr, or declared type

**Source:** 9.7.1.1 p5, J3/24-007, 18 December 2023, physical PDF162. **Class:** Effect.

**Definition:** When type-spec is specified, each allocate-object is allocated with the specified
dynamic type and type parameter values. When source-expr is specified, each
allocate-object is allocated with the dynamic type and type parameter values of
source-expr. Otherwise, each allocate-object is allocated with dynamic type the same as
its declared type. This records selection of dynamic type and type parameters, leaving
value initialization, bounds, and allocation status to 9.7.1.2 and 9.7.1.3.

**Diagnostic obligation:** not-required.

**Facets:** `type-spec-selects-dynamic-type`, `source-selects-dynamic-type`, `default-declared-dynamic-type`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.2.3p5;7.3.3p1-p2;7.5.7.1-.2;9.7.1.1C937-C951;9.7.1.2p1-p11;9.7.1.3p1-p6.

### S9.7.1.1-006: Unlimited polymorphic allocation from source excludes dynamic coarray potential subobject component

**Source:** 9.7.1.1 p5, J3/24-007, 18 December 2023, physical PDF162. **Class:** Restriction.

**Definition:** If an allocate-object is unlimited polymorphic, the dynamic type of source-expr shall
not have a coarray potential subobject component. This is the dynamic-type counterpart
to C953's declared-type condition and applies at execution when dynamic and declared
type can differ.

**Diagnostic obligation:** not-required.

**Facets:** `ordinary-dynamic-source-for-unlimited-polymorphic`, `dynamic-coarray-potential-subobject-rejected`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.3.2.3p4-p5;7.5.4.1 potential subobjects;8.5.6.1;9.7.1.1C953/p5;9.7.1.2p8.

### S9.7.1.1-007: Type parameter values in allocation are captured from asterisk or expression

**Source:** 9.7.1.1 p6, J3/24-007, 18 December 2023, physical PDF162. **Class:** Effect.

**Definition:** If a type-param-value in an ALLOCATE type-spec is an asterisk, it denotes the current
value of that assumed type parameter. If the type-param-value is an expression,
subsequent redefinition or undefinition of any entity in the expression does not affect
the allocated object's type parameter value.

**Diagnostic obligation:** not-required.

**Facets:** `asterisk-denotes-current-assumed-value`, `expression-value-is-frozen`.

**Oracle:** All facets are explicit PENDING source plans. Positive controls use independent literal
values, portable inquiry results, ALLOCATED/ASSOCIATED/STAT observations after checking
successful allocation, or gated coarray synchronization observations; they do not rely
on compiler agreement, addresses, TRANSFER, emitted code, undefined values, unallocated
storage, or allocation failure. Negative plans specify the single requirement to which
invalidity is attributable and a minimal repair that preserves every other stated
premise.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, invokes no compiler, records
no evidence link, approves no oracle, and claims no coverage. Numbered syntax rules and
constraints carry the processor detection duty from 4.2; unnumbered execution-time and
image-participation conditions are not promoted to invented static diagnostics. Coarray
observations require a separately gated multi-image capability.

**Dependencies:** 4.2;7.2 type parameters;7.4.4.2 character length;7.5.9 assumed type
parameters;9.7.1.1C939/p5;9.7.1.2p10;15.5.2.4.

<!-- END GENERATED 9.7.1.1 -->
