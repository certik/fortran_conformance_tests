# Fortran 2023 8.5.14: POINTER attribute

**Source independently reviewed in batch058.** Effective status is
`Registry.catalogue_review_state("8.5.14")`. Source review and fixture/evidence
approval are separate; every implementation facet remains pending.

The catalogue is `doc/catalogues/pointer_attribute_8_5_14.json`.
All seven original base units are on physical PDF page 131 of J3/24-007,
December 18, 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The section hash is
`2f469ec74612776e7000aee687198a1b8f3ed5c5275c6c2b4318134331ece3c4`.
The actual next heading is **8.5.15 PROTECTED attribute**. The unnumbered
declaration note is informative and supplies no initial association.

POINTER entity category, pointer association status, target definition and
target definability are different. A declaration does not establish a
target; association can be valid before a definable target has a value.
Data-pointer value reads require a defined target, whereas defining through
the pointer requires a definable target. Pointer-association operations and
permitted property inquiries are not silently treated as payload reads.

Zero-size arrays and zero-length strings are always defined under19.6.2.
That rule does not establish presence, allocation, pointer association or
permission for an unavailable inquiry. ASSOCIATED requires defined
association status; its optional-target empty-size behavior is not a
universal status oracle. LEN and RANK retain their own property contracts.

C854 concerns the pointer entity, not attributes of its target or parent.
C855 retains the **named procedure** subject: PROCEDURE declarations and
qualifying interface bodies can already specify EXTERNAL. A missing keyword,
a data-pointer function result, a procedure-pointer component or a link
failure is not an isolated C855 violation. C892 and reciprocal attribute
constraints retain their own owners and explicit overlap gates.

Deferred data-pointer parameters follow the corresponding target parameters
only under the stated association premise. Existing CHARACTER and PDT
pointer-assignment effects are candidates for qualified reuse, not new
executions. Procedure-pointer invocation separately needs a compatible live
target procedure; status inquiries and mere designator arguments do not
automatically invoke that target.

All future observations use valid states and independent literal values or
events, never undefined reads, guessed representations, addresses or an
affected descriptor. Role and origin contracts need actual source
adjudication; no positive-control opt-in, retagging or new connection is
activated by this source-only packet.

No fixtures, compiler invocations, profiles, case approvals, SourceUses entries,
links, baseline or shared-harness changes are added. Source and inventory
context reviews are separate and explicit.

<!-- BEGIN POINTER SAVE 8.5.14 8.5.16 FIXTURES -->
## POINTER/SAVE packet additions for POINTER

This packet adds bounded POINTER fixtures: line-anchored C854 ALLOCATABLE and
TARGET conflicts with one-property controls; a data-pointer run/effect program
for defined-target reference and definition through a previously undefined but
definable target; a CHARACTER deferred-length association program including an
undefined-payload LEN boundary; and a procedure-pointer status/invocation
program. Mutations redirect the association or target procedure so the guarded
feature-specific assertion fails without relying on addresses, undefined reads
or processor diagnostic wording.

Unimplemented facets remain pending for INTRINSIC/coarray conflicts, broader
role/source gates, PDT parameters, invalid dereference or invocation, lifetime
and argument-context source contrasts.
<!-- END POINTER SAVE 8.5.14 8.5.16 FIXTURES -->

<!-- BEGIN GENERATED 8.5.14 -->

### C854: A POINTER entity excludes three other attributes and coarray status

**Source:** 8.5.14 C854, physical PDF131, printed117, lines6-7 **Class:** Restriction.

**Definition:** An entity with POINTER has none of ALLOCATABLE, INTRINSIC or TARGET and is not a
coarray. These are properties of that entity, not prohibitions on a compatible allocated
target, an eligible intrinsic procedure target or pointer components inside other
objects.

**Diagnostic obligation:** required.

**Facets:** `allocatable-conflict`, `intrinsic-conflict-source`, `target-conflict-owner`, `coarray-conflict`, `distinct-target-controls`, `scalar-array-component-source`.

**Oracle:** Focused ordinary-entity incompatibilities with minimal valid repairs and explicit
canonical-overlap gates. Complete role and attribute premises precede diagnostic
attribution.

C854 pointer-entity conflict diagnostics: two compile/f2023 negatives place POINTER with
ALLOCATABLE or TARGET on the same ordinary scalar entity on line3. Each has a
one-property positive control deleting only the conflicting attribute and safely
nullifying/inquiring the pointer before exact output.

**Oracle limitation:** Required reporting does not prescribe fatal exit, English text or a printed code. No
duplicate canonical case, unsupported-feature/configuration credit, source echo,
unrelated type/rank error, ICE or source-span workaround. Merely listing a source
relation does not approve a connection.

C854 pointer-entity conflict diagnostic boundaries: only ALLOCATABLE and TARGET
conflicts on ordinary scalar entities are discharged. INTRINSIC and coarray conflicts,
component/parent roles and target-attribute controls remain pending. LFortran target
acceptance or ASR/internal reports are reported as compiler defects, not accepted
diagnostics.

**Dependencies:** C842/C855;8.5.18 C866;8.5.6.1 C823/C826;8.5.16p4;7.5.4.1 C750/C754;8.5.8.4
C834;10.2.2;15.4.3.6;15.5.2.8;4.2p2.

### C855: A named procedure pointer has the EXTERNAL attribute

**Source:** 8.5.14 C855, physical PDF131, line8 **Class:** Restriction.

**Definition:** A named procedure that has POINTER has EXTERNAL. The attribute can be conferred by the
actual declaration mechanism, not only by an explicit EXTERNAL keyword. The named
pointer entity is distinct from its target; a data-pointer result is not the function
procedure entity, and procedure-pointer components retain their own declaration rules.

**Diagnostic obligation:** required.

**Facets:** `pointer-statement-external`, `procedure-declaration-external`, `interface-body-external`, `named-role-and-negative-gate`.

**Oracle:** Complete declaration-mechanism admissions and a finite role/owner gate for any proposed
omission negative. No isolated violation is claimed merely from keyword absence.

**Oracle limitation:** No universal explicit-keyword requirement, target-procedure EXTERNAL requirement,
component grammar expansion, link-error oracle or diagnostic-policy shortcut. Precise
legitimate origins and case-specific causes are required without coalescing lines or
widening spans.

**Dependencies:** 8.5.9;8.6.12 R856/R857/C892;15.4.3.2p5;15.4.3.5/.6p1;15.6.2.2p3-p4;7.5.4.1
R741/R742;4.2p2.

### S8.5.14-001: Data-pointer value reference and definition require distinct valid target states

**Source:** 8.5.14 p2, physical PDF131, lines9-10 **Class:** Restriction.

**Definition:** A data-pointer value is referenced only while the pointer is associated with a defined
target object. Defining the value through a data pointer instead requires association
with a definable target. Association/status operations and permitted property inquiries
are distinct; association alone does not make the target's payload defined or definable.

**Diagnostic obligation:** not-required.

**Facets:** `defined-target-reference`, `undefined-target-definition-control`, `unassociated-source-contrasts`, `not-definable-target-source`, `zero-size-length-and-inquiry-boundary`.

**Oracle:** Meaningful, state-valid positive controls and explicit source/policy gates for
nonconforming contrasts. Literal data results are observed only after association and
the exact target-value/definability premises are established.

S8.5.14-001 data-pointer state fixtures: one complete run/effect program associates a
data pointer with a defined integer target t=17, checks ASSOCIATED(p,t), reads
literal17, defines through p and observes t=19. It then associates the same pointer with
a separate live but previously undefined definable target u, checks ASSOCIATED(p,u),
defines through p and observes u=23. Feature mutations redirect each pointer assignment
to an alternate defined target before the guarded assertion.

**Oracle limitation:** No blanket required prose diagnostic, undefined-value test, guessed default association,
absent/unallocated/dangling read, or hidden lifetime/SAVE repair. Existing
assignment/argument/component witnesses retain their original roles; no source-use
credit or positive-control opt-in is inferred.

S8.5.14-001 data-pointer state boundaries: no invalid unassociated dereference,
nondefinable target, zero-size target, aliasing, lifetime or diagnostic policy is
claimed. The undefined target's old value is never read before pointer definition.

**Dependencies:** 5.4.4/5.4.5/5.4.9;19.5.2.1-.5;19.6.2/19.6.7/19.6.8;16.1;16.9.20/16.9.122/16.9.171;8.5.10;8.5.15;15.5.2.4/.5/.8/.14;10.2.1.3/10.2.2.

### S8.5.14-002: Associated data pointers have their target's corresponding deferred type-parameter values

**Source:** 8.5.14 p3, physical PDF131, lines11-12 **Class:** Effect.

**Definition:** When a data pointer is associated, each deferred type-parameter value equals the
corresponding value of its target. This is a target relationship, not an assumption of
argument association, current target payload definition or arbitrary kind conversion.
Deferred result-type characteristics of a function or function procedure pointer are
distinct from the actual values of an associated data-pointer result.

**Diagnostic obligation:** not-required.

**Facets:** `character-parameter-reuse`, `pdt-parameter-reuse`, `associated-undefined-payload-boundary`, `inactive-nondeferred-procedure-boundaries`.

**Oracle:** Actual run/effect observations of associated data pointers with independent literal
target-parameter expectations, preferably through qualified canonical reuse. Target
liveness, type/kind/rank/parameter compatibility and specific inquiry conditions are
explicit prerequisites.

S8.5.14-002 deferred-parameter fixtures: one complete run/effect program associates a
deferred-length CHARACTER data pointer with length2 and length5 targets and checks LEN
directly on the pointer before literal payload checks. It separately associates the
pointer with an initially undefined length2 target, observes only LEN=2, then defines
the target through the pointer before reading raw='xy'. Feature mutations substitute the
length5 target for each selected association.

**Oracle limitation:** No compile-only substitute for required effects, fixed kind code, storage layout,
target-address or affected-descriptor self-oracle. A valid IN
pointer-dummy/nonpointer-actual route has separate rules and no fabricated effective
argument. No source/role/link approval is granted here.

S8.5.14-002 deferred-parameter boundaries: only default CHARACTER deferred length and
the undefined-payload LEN boundary are covered. PDT parameters, disassociated/inactive
inquiries and procedure-result boundaries remain pending; no address or descriptor
representation is asserted.

**Dependencies:** 7.2
C701/C702/p5-p7/note1;10.2.2.2/.3;19.5.2.1;16.1;16.9.122;15.5.2.4p5-p6/15.5.2.6/15.5.2.8;canonical
S7.2-004 pointer-assignment witnesses.

### S8.5.14-003: A procedure-pointer invocation requires association with a target procedure

**Source:** 8.5.14 p4, physical PDF131, line13 **Class:** Restriction.

**Definition:** A procedure pointer is referenced only when it is associated with a target procedure.
Under5.4.5, procedure reference means execution; a permitted status inquiry, association
operation or unevaluated procedure-designator actual is not automatically such an
invocation. Characteristic compatibility and target/host lifetime still apply.

**Diagnostic obligation:** not-required.

**Facets:** `associated-call-control`, `disassociated-call-source`, `status-and-argument-contexts`, `characteristics-lifetime-target-source`.

**Oracle:** Complete associated-call controls with independent known results and finite
source/policy gates for prohibited invocation states. Original callable witnesses keep
their owners, roles and qualified observations.

S8.5.14-003 procedure-pointer invocation fixture: one complete run/effect program
declares PROCEDURE(unary),POINTER fp, establishes defined disassociation by NULLIFY and
checks ASSOCIATED(fp)=false without invocation, then associates fp with add_eleven and
checks ASSOCIATED(fp) and fp(4)=15. Feature mutations pre-associate the status control
or redirect the call to add_twelve.

**Oracle limitation:** No all-name-use ban, forced fatal status, generic missing-symbol/link oracle,
undefined-call experiment or unqualified role transfer. No descriptor, C address, byte
representation or copy/allocator strategy is used as evidence.

S8.5.14-003 procedure-pointer boundaries: only status inquiry without invocation and a
compatible associated function call are covered. Disassociated calls, dummy-procedure
argument contexts, intrinsic targets and lifetime/source contrasts remain pending.

**Dependencies:** 5.4.5;C855;10.2.2.2
C1033/C1034;10.2.2.4;15.4.3.2/.6;15.5.2.10;16.8;16.9.20;19.5.2.2/19.5.2.5.

<!-- END GENERATED 8.5.14 -->
