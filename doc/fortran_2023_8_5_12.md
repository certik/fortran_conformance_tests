# Fortran 2023 8.5.12: OPTIONAL attribute

Effective source status is `Registry.catalogue_review_state("8.5.12")`,
not a cached claim derived from the raw review field. The original source
review is recorded in batch054. Source, fixture and inventory approvals
remain independent and content-bound; generation grants none.

The canonical catalogue is `doc/catalogues/optional_attribute_8_5_12.json`.
All three original base units are on physical PDF page 130 of J3/24-007,
December 18, 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The replayed section hash is
`14b534916a8b02e412a6073da565edd4c9b56baf4518760254d1e9673c7bd9cc`.
The scope ends before the actual 8.5.13 PARAMETER heading.

OPTIONAL is a dummy-argument attribute, including eligible nonpointer
dummy procedures as well as data objects and procedure pointers. INTENT's
different procedure-pointer restriction must not be imported.

Omission, keyword correspondence, forwarded absence and a conditional
argument's chosen `.NIL.` retain their separate argument rules. All
evaluation-independent consequent requirements and conditional-argument
constraints, including C1538/C1539, still apply. Presence does not authorize
an unavailable value or an absent-object operation.

The exact PRESENT result comes from 16.9.163 and 15.5.2.13, not a new
interpretation of the note. In particular, a present IN data-pointer dummy
can be pointer associated with a nonpointer actual without argument
association or an effective argument under 15.5.2.4. That terminology
boundary must not be turned into a false absence result.

<!-- BEGIN OPTIONAL ELIGIBILITY SUMMARY -->
## Bounded ordinary C851 eligibility family

Four compile/f2023 fixtures form two nondummy/valid-dummy pairs: an
ordinary INTEGER entity in an external subroutine and a nonpointer
dummy procedure with a complete abstract subroutine interface. Each
repair adds only the subject to its existing subroutine header.
No INTENT, POINTER, initialization, SAVE, call or runtime operation is
added. The two valid cases are compile/positive-controls requiring
objects; the two invalids are diagnostic-only evidence.

The exact calibrated nondummy-role causes are bound to actual staged
declaration lines3/10, whose only relevant attribute is OPTIONAL.
Only the measured Flang warning is allowed nonfatally. Neither fatal
status, a rule code nor standardized English is required. A bare
argument word, source echo, quoted example, foreign path, unsupported
procedure or Internal/ASR failure cannot supply the cause.

This generator manages only the three selected C851 pending entries
and its bounded oracle paragraphs. All unselected C851 facets, other
requirements, control designations and review records retain their
current independent state. It does not require future C851 cases to
belong to this family. No PRESENT, omission, SourceUses or runtime
effect credit, canonical link, approval or baseline update is added.
<!-- END OPTIONAL ELIGIBILITY SUMMARY -->

<!-- BEGIN GENERATED 8.5.12 -->

### C851: An OPTIONAL entity must be a dummy argument

**Source:** 8.5.12 C851, physical PDF130, printed116 **Class:** Restriction.

**Definition:** An entity with OPTIONAL must be a dummy argument. Eligible dummy data objects and dummy
procedures retain their other attribute/interface rules. Unlike INTENT's procedure
restriction, this does not require every optional dummy procedure to be a procedure
pointer.

**Diagnostic obligation:** required.

**Facets:** `data-dummy-admission`, `procedure-dummy-admission`, `nondummy-exclusion`, `entry-and-attribute-source-boundaries`.

**Oracle:** Complete eligible data/procedure admissions and minimally repaired nondummy
declarations, with source-specific role diagnostics.

C851 ordinary OPTIONAL eligibility family: four complete compile/f2023 cases form two
diagnostic/positive-control pairs. An external subroutine declares ordinary INTEGER
OPTIONAL subject, and a module-contained procedure declares ordinary nonpointer
PROCEDURE(signature) OPTIONAL callback with a prior abstract subroutine interface. Each
invalid omits its subject from the dummy list; the sole repair inserts that name in the
existing procedure header, preserving all other bytes. The actual staged declarations at
lines3 and10 contain OPTIONAL and no INTENT or VALUE, so the exact measured GNU
nondummy-role error and Flang full attribute/nondummy-role warning identify this
predicate. A report must match a complete extracted cause at its actual staged subject
declaration; generic argument words, echoes and foreign origins do not qualify. Only the
observed exact Flang warning is a nonfatal allowance. Both controls require successful
object compilation and retain positive-control evidence, not runtime effect.

**Oracle limitation:** Required reporting capability is not fatal exit, standardized wording or a printed code.
No broad INTENT-style procedure-pointer limitation is imported, and unrelated
attribute/interface failures cannot qualify.

C851 ordinary OPTIONAL eligibility limits: this family represents only
data-dummy-admission, procedure-dummy-admission and nondummy-exclusion. It is not an
exhaustive C851 case census. The callback repair remains a nonpointer dummy procedure,
without INTENT, POINTER, initialization or SAVE. There are no calls, allocation,
association, VALUE, C/coarray, payload or runtime operations. Entry/other-attribute
boundaries and all S8.5.12-001 plans retain their independently managed states. The
PRESENT note supplies no new case or source-use/runtime credit. Reporting capability
does not prescribe fatal status, English wording or a printed code; these finite exact
messages are calibrated qualification routes, not a universal vocabulary. No additional
source span is needed by the measured reports, and neither coalesced lines nor widened
spans are used. Unsupported procedures, Internal/ASR/crash/resource failures, quoted
examples, source echoes and wrong-file reports never count. Flang f2018 evidence is not
relabelled f2023. Generation preserves unselected pending/control and all review
records, without approving, renewing, retagging or updating a baseline.

**Dependencies:** 8.6.10/R853;15.4.2.2;15.4.3.6;15.5.2.10/.13;15.6.2.6;C845;4.2p2.

### S8.5.12-001: OPTIONAL permits a procedure reference without an effective argument for the dummy

**Source:** 8.5.12 p1, physical PDF130 **Class:** Restriction.

**Definition:** An OPTIONAL dummy need not have an effective argument in a procedure reference. This
admission is subject to exact argument correspondence, an explicit interface where
required,15.5.2.13 presence/absence and permitted-use rules, and the dummy's other
attributes. It is not a definition that presence, allocation, pointer association and
having an effective argument are equivalent.

**Diagnostic obligation:** not-required.

**Facets:** `omitted-data-actual`, `positional-keyword-omission`, `dummy-category-controls`, `forwarded-absence-source`, `presence-state-source`, `conditional-nil-source`, `present-without-effective-argument`.

**Oracle:** Meaningful omission/admission positive controls and finite canonical
correspondence/presence/source relations. Independently known outputs are observed only
after genuine presence and state guards; PRESENT effects retain their intrinsic owner.

**Oracle limitation:** No new mandatory rejection follows from this permission. An invalid absent-object use
has no prescribed value or trap. Existing effect witnesses are not cloned or retagged as
positive controls, and unsupported source/role mechanisms remain explicitly pending.

**Dependencies:** 15.4.2.2p1(4);15.5.1
C1530-C1532/R1526-R1528/C1540-C1545;15.5.2.1/.3/.4/.8/.10/.13;16.9.163.

<!-- END GENERATED 8.5.12 -->
