# Fortran 2023 12.6.4.5.1: General data transfer

The canonical catalogue is `doc/catalogues/data_transfer_general_12_6_4_5_1.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, note-unnumbered, p4, p5, p6, p7, p8, p9, p10. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.6.4.5.1 -->

### S12.6.4.5.1-001: Data transfer processes list and namelist items in the specified order

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Effect.

**Definition:** Data are transferred between the file and input/output list or namelist entities;
ordinary list items are processed in input/output list order, namelist input items in
input-record entity order, namelist output items in namelist-group-object-list order,
and effective items are derived as described in 12.6.3.

**Diagnostic obligation:** not-required.

**Facets:** `data-transferred-between-file-and-items`, `ordinary-list-items-processed-in-list-order`, `namelist-input-processed-in-input-record-order`, `namelist-output-processed-in-group-order`, `effective-items-derived-from-io-list-items`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-002: Entity-selection values for a list item are determined at the beginning of that item

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Effect.

**Definition:** All values needed to determine which entities are specified by an input/output list item
are determined at the beginning of processing that item.

**Diagnostic obligation:** not-required.

**Facets:** `list-item-entity-selection-values-captured-at-item-begin`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-003: A list item's values are transmitted before the succeeding list item

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Effect.

**Definition:** For all data transfer statements, all values are transmitted to or from entities
specified by a list item before any succeeding list item is processed.

**Diagnostic obligation:** not-required.

**Facets:** `list-item-values-transmitted-before-succeeding-item`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-004: Namelist input uses matching entities and the last repeated occurrence

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Effect.

**Definition:** For namelist input, all values after a name= part in the input records are transmitted
to the matching namelist entity before the succeeding input-record entity is processed,
and the last occurrence of an entity in the input record supplies that entity's value or
values.

**Diagnostic obligation:** not-required.

**Facets:** `namelist-values-transmitted-to-matching-entity`, `namelist-entity-complete-before-succeeding-entity`, `namelist-last-repeated-occurrence-wins`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-005: Pointer input/output items transfer through their associated targets

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Effect.

**Definition:** If the input/output item is a pointer, data are transferred between the file and the
associated target.

**Diagnostic obligation:** not-required.

**Facets:** `pointer-io-item-transfers-associated-target`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-006: Internal-file list items cannot be in or associated with the file

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Restriction.

**Definition:** If an internal file is specified, an input/output list item shall not be in the file or
associated with the file.

**Diagnostic obligation:** not-required.

**Facets:** `internal-file-list-item-not-in-file`, `internal-file-list-item-not-associated-with-file`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-007: Internal output cannot reference, define, or undefine the internal file while evaluating output items

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Restriction.

**Definition:** During output to an internal file, no part of that internal file shall be referenced,
defined, or become undefined as a result of evaluating any output list item.

**Diagnostic obligation:** not-required.

**Facets:** `internal-output-item-not-reference-file`, `internal-output-item-not-define-file`, `internal-output-item-not-undefine-file`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-008: Internal input cannot define or undefine the internal file through input items

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Restriction.

**Definition:** During input from an internal file, no part of that internal file shall be defined or
become undefined as a result of transferring a value to an input list item.

**Diagnostic obligation:** not-required.

**Facets:** `internal-input-transfer-not-define-file`, `internal-input-transfer-not-undefine-file`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-009: An I/O implied-DO variable is defined and its iteration count established at object-list processing

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Effect.

**Definition:** A DO variable becomes defined and its iteration count established at the beginning of
processing the io-implied-do-object-list of an io-implied-do.

**Diagnostic obligation:** not-required.

**Facets:** `io-implied-do-variable-defined-at-object-list-begin`, `io-implied-do-iteration-count-established-at-object-list-begin`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

### S12.6.4.5.1-010: Output transfers require every transferred entity to be defined

**Source:** S12.6.4.5.1, J3/24-007, 18 December 2023, physical PDF266. **Class:** Restriction.

**Definition:** On output, every entity whose value is to be transferred shall be defined.

**Diagnostic obligation:** not-required.

**Facets:** `output-transferred-entities-defined`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by opening or creating a known file, writing known records, initializing
variables to nonzero/nonblank sentinels, or using a distinct labelled control path) and
then observe only portable properties: successful IOSTAT zero, nonzero IOSTAT for a
specified error condition, exact internal records or values where the source fixes them,
named ISO_FORTRAN_ENV status values where the standard names them, or INQUIRE properties
fixed by the relevant rule. Processor-dependent unit numbers, filenames except names
created by the fixture, storage-unit encodings, buffering, asynchronous timing, IOMSG
text, and specific numeric status values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe that rejection portably; unnumbered program restrictions are
not promoted to required diagnostics. IOMSG wording, processor-dependent numeric status
values, asynchronous completion timing, and multiple-image or coarray-dependent behavior
are never single-image oracles; coarray facets remain out of scope for this suite's
fixtures.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
registered earlier clause 12 catalogues for file existence, access, file position,
internal files, connection, data-transfer statements, and control-list forms where
present. Unregistered dependencies explicitly retained: 12.6.2.2/12.6.3 sibling source
packets, later 12.6.4.8 defined I/O, clause 13 formatting, clause 15 defined I/O
procedure accessibility, clause 16 inquiry and ISO_FORTRAN_ENV intrinsics where future
fixtures need them, clause 14 image semantics only as out-of-scope context, and clauses
3, 5, 17, 18, and 19 where terminology, IEEE support, deleted features, or program
execution controls are reached.

<!-- END GENERATED 12.6.4.5.1 -->
