# Fortran 2023: 11.1.7.5 Additional semantics for DO CONCURRENT constructs

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.1.7.5 -->

### C1140: RETURN statements are prohibited within DO CONCURRENT constructs

**Source:** 11.1.7.5 C1140; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** A RETURN statement shall not appear within a DO CONCURRENT construct.

**Diagnostic obligation:** required.

**Facets:** `no-return-control`, `return-in-do-concurrent-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;11.2 RETURN statement NOT YET REGISTERED.

### C1141: Image control statements are prohibited within DO CONCURRENT constructs

**Source:** 11.1.7.5 C1141; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** An image control statement shall not appear within a DO CONCURRENT construct.

**Diagnostic obligation:** required.

**Facets:** `no-image-control-control`, `image-control-in-do-concurrent-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;11.6/11.7 image control statements and segments NOT YET
REGISTERED;single-image suite cannot observe multi-image synchronization.

### C1142: A branch within DO CONCURRENT must not target outside the construct

**Source:** 11.1.7.5 C1142; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** A branch within a DO CONCURRENT construct shall not have a branch target that is outside
the construct.

**Diagnostic obligation:** required.

**Facets:** `branch-inside-target-control`, `branch-out-of-do-concurrent-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;11.2 branch statements NOT YET REGISTERED.

### C1143: References to impure procedures are prohibited within DO CONCURRENT constructs

**Source:** 11.1.7.5 C1143; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** A reference to an impure procedure shall not appear within a DO CONCURRENT construct.

**Diagnostic obligation:** required.

**Facets:** `pure-procedure-reference-control`, `impure-procedure-reference-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;15 pure procedures and procedure reference semantics NOT YET
REGISTERED.

### C1144: Statements that might deallocate polymorphic entities are prohibited within DO CONCURRENT constructs

**Source:** 11.1.7.5 C1144; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** A statement that might result in the deallocation of a polymorphic entity shall not
appear within a DO CONCURRENT construct.

**Diagnostic obligation:** required.

**Facets:** `no-polymorphic-deallocation-control`, `polymorphic-deallocation-possible-statement-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;7 polymorphism partially registered;9.7 deallocation
registered;15 finalization and 19 dynamic type/definedness NOT YET REGISTERED.

### C1145: Selected IEEE_EXCEPTIONS procedure references are prohibited within DO CONCURRENT constructs

**Source:** 11.1.7.5 C1145; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** A reference to IEEE_GET_FLAG, IEEE_GET_HALTING_MODE, IEEE_GET_STATUS,
IEEE_SET_HALTING_MODE, IEEE_SET_MODES, or IEEE_SET_STATUS from intrinsic module
IEEE_EXCEPTIONS shall not appear within a DO CONCURRENT construct.

**Diagnostic obligation:** required.

**Facets:** `no-listed-ieee-exceptions-control`, `ieee-get-flag-rejected`, `ieee-get-halting-mode-rejected`, `ieee-get-status-rejected`, `ieee-set-halting-mode-rejected`, `ieee-set-modes-rejected`, `ieee-set-status-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;17 IEEE_EXCEPTIONS intrinsic module NOT YET REGISTERED.

### C1146: Selected IEEE_ARITHMETIC mode-setting procedure references are prohibited within DO CONCURRENT constructs

**Source:** 11.1.7.5 C1146; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** A reference to IEEE_SET_ROUNDING_MODE or IEEE_SET_UNDERFLOW_MODE from intrinsic module
IEEE_ARITHMETIC shall not appear within a DO CONCURRENT construct.

**Diagnostic obligation:** required.

**Facets:** `no-listed-ieee-arithmetic-control`, `ieee-set-rounding-mode-rejected`, `ieee-set-underflow-mode-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;17 IEEE_ARITHMETIC intrinsic module NOT YET REGISTERED.

### S11.1.7.5-001: Variables in a DO CONCURRENT construct have one of five locality classes, with construct entities shared only when saved

**Source:** 11.1.7.5 p1; J3/24-007 PDF p215. **Class:** Effect.

**Definition:** The locality of a variable that appears in a DO CONCURRENT construct is LOCAL,
LOCAL_INIT, REDUCE, SHARED, or unspecified. A construct or statement entity of a
construct or statement within the DO CONCURRENT construct has SHARED locality if it has
the SAVE attribute; without SAVE, it is a different entity in each iteration, similar to
LOCAL locality.

**Diagnostic obligation:** not-required.

**Facets:** `locality-classification-source-control`, `saved-construct-entity-is-shared`, `nonsaved-construct-entity-per-iteration`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 8.5.16 SAVE registered;19 construct entities and scoping NOT YET REGISTERED.

### S11.1.7.5-002: LOCAL and LOCAL_INIT variables are construct entities that hide the outside variable and inherit only specified attributes and bounds

**Source:** 11.1.7.5 p2; J3/24-007 PDF p215. **Class:** Effect.

**Definition:** A variable with LOCAL or LOCAL_INIT locality is a construct entity with the same type,
type parameters, and rank as the same-named variable in the innermost enclosing
executable construct or scoping unit; the outside variable is inaccessible by that name
within the construct. The construct entity has ASYNCHRONOUS, CONTIGUOUS, POINTER,
TARGET, or VOLATILE if and only if the outside variable has that attribute, does not
have BIND, INTENT, PROTECTED, SAVE, or VALUE, and, if not a pointer, has the same bounds
as the outside variable.

**Diagnostic obligation:** not-required.

**Facets:** `local-hides-outside-variable`, `local-preserves-allowed-attributes`, `local-excludes-disallowed-attributes`, `local-nonpointer-same-bounds`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** Clauses 8 attribute catalogues registered for listed attributes except
ASYNCHRONOUS/VOLATILE effects beyond declaration;19 scoping and construct entities NOT
YET REGISTERED.

### S11.1.7.5-003: LOCAL starts each iteration undefined, while LOCAL_INIT starts with the outside variable status and requires the outside variable to be defined when needed

**Source:** 11.1.7.5 p2; J3/24-007 PDF p215. **Class:** Effect.

**Definition:** At the beginning of each iteration, a LOCAL pointer has undefined pointer association
status and a nonpointer LOCAL variable is undefined except for default-initialized
subobjects; a LOCAL_INIT variable has the pointer association status and definition
status of the outside variable with that name; and the outside variable shall not be an
undefined pointer or a nonallocatable nonpointer variable that is undefined.

**Diagnostic obligation:** context-dependent.

**Facets:** `local-initial-undefined-source-control`, `local-assigned-before-use-control`, `local-init-copies-outside-definition-status`, `local-init-outside-defined-source-control`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 7.5 default initialization partially registered;19 definition and pointer association
status NOT YET REGISTERED.

### S11.1.7.5-004: LOCAL and LOCAL_INIT variables have pending-I/O and TARGET pointer lifetime restrictions within each iteration

**Source:** 11.1.7.5 p2; J3/24-007 PDF p215. **Class:** Restriction.

**Definition:** If a LOCAL or LOCAL_INIT variable becomes an affector of a pending input/output
operation, the operation shall have completed before the end of the iteration. If a
LOCAL or LOCAL_INIT variable has TARGET, a pointer associated with it during an
iteration becomes undefined when that iteration completes.

**Diagnostic obligation:** context-dependent.

**Facets:** `local-pending-io-completes-source-control`, `local-target-pointer-undefined-after-iteration-source-control`.

**Oracle:** This source rule is a program obligation or context-dependent restriction, not a
portable executable oracle in this packet. Pending evidence is
source-control/documentary unless a future reviewed fixture can observe only a defined
final state under all permitted execution orders.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 12 I/O pending operations NOT YET REGISTERED;19 pointer association/undefined status NOT
YET REGISTERED.

### S11.1.7.5-005: REDUCE variables are construct entities with outside-variable accessibility, status, and attribute restrictions

**Source:** 11.1.7.5 p3; J3/24-007 PDF p215. **Class:** Effect.

**Definition:** A REDUCE variable is a construct entity with the same type, type parameters, rank, and
bounds as the outside variable of the same name, and the outside variable is
inaccessible by that name within the construct. The outside variable shall not be an
unallocated allocatable variable or an unassociated pointer. The construct entity has
CONTIGUOUS if and only if the outside variable has it and does not have ALLOCATABLE,
BIND, INTENT, POINTER, PROTECTED, SAVE, TARGET, or VALUE.

**Diagnostic obligation:** context-dependent.

**Facets:** `reduce-hides-outside-variable`, `reduce-same-characteristics-and-bounds`, `reduce-outside-allocated-or-associated-control`, `reduce-attribute-rules-source-control`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 8 attributes and 9.7 allocation registered in part;19 pointer association and
allocatable status NOT YET REGISTERED.

### S11.1.7.5-006: REDUCE construct entities are initialized from Table 11.1 according to the reduction operation

**Source:** 11.1.7.5 p3 and Table 11.1; J3/24-007 PDF p215. **Class:** Effect.

**Definition:** Before DO CONCURRENT iterations begin, each REDUCE construct entity is assigned an
initial value corresponding to its reduction operation: + uses 0, * uses 1, .AND. uses
.TRUE., .OR. uses .FALSE., .EQV. uses .TRUE., .NEQV. uses .FALSE., IAND uses all bits
set, IEOR and IOR use 0, MAX uses the least representable value of the type and kind,
and MIN uses the largest representable value of the type and kind.

**Diagnostic obligation:** not-required.

**Facets:** `reduce-plus-initial-zero`, `reduce-times-initial-one`, `reduce-logical-initial-identities`, `reduce-bitwise-initial-identities`, `reduce-minmax-initial-extremes`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 10.1.5 intrinsic operations registered;16 kind models and numeric limits NOT YET
REGISTERED. Table 11.1 rows were checked row by row in the pinned PDF.

### S11.1.7.5-007: REDUCE variables may appear in the DO CONCURRENT block only in specified intrinsic-assignment reduction forms

**Source:** 11.1.7.5 p4; J3/24-007 PDF p215. **Class:** Restriction.

**Definition:** A REDUCE variable shall only appear within the DO CONCURRENT block in the designator of
a variable, as the object-name, or as the leftmost part-name of an array element or
section, in an intrinsic assignment statement of the listed binary-reduce-op or
function-reduction-name forms, and each occurrence of variable has the same form.

**Diagnostic obligation:** context-dependent.

**Facets:** `reduce-variable-allowed-binary-left-form-control`, `reduce-variable-allowed-binary-right-form-control`, `reduce-variable-allowed-function-form-control`, `reduce-variable-same-form-source-control`, `reduce-variable-disallowed-appearance-source-control`.

**Oracle:** This source rule is a program obligation or context-dependent restriction, not a
portable executable oracle in this packet. Pending evidence is
source-control/documentary unless a future reviewed fixture can observe only a defined
final state under all permitted execution orders.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 10.2.1.3 intrinsic assignment registered;10.1.5 intrinsic operations registered;15
function reference details NOT YET REGISTERED.

### S11.1.7.5-008: On DO CONCURRENT termination, each REDUCE outside variable is updated by combining it with iteration-completion values

**Source:** 11.1.7.5 p5; J3/24-007 PDF p216. **Class:** Effect.

**Definition:** On termination of a DO CONCURRENT construct, a REDUCE outside variable is updated by
combining it with the values the construct entity had at completion of each iteration,
using the reduce-operation.

**Diagnostic obligation:** not-required.

**Facets:** `reduce-final-integer-result`, `reduce-final-logical-result`, `reduce-outside-updated-only-at-termination`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 10.1.5 intrinsic operation semantics registered;real and complex reduction value oracles
are excluded unless exact representability and order-insensitivity are independently
established.

### S11.1.7.5-009: A processor may combine REDUCE values in any order

**Source:** 11.1.7.5 p5; J3/24-007 PDF p216. **Class:** Effect.

**Definition:** When updating a REDUCE outside variable, the processor may combine the values in any
order.

**Diagnostic obligation:** not-required.

**Facets:** `reduce-combination-order-unobservable-source-control`, `floating-reduce-order-sensitive-oracle-excluded`.

**Oracle:** This records processor or source latitude, not a required observable behavior. It
supports rejecting order-sensitive or latitude-sensitive oracles and cannot support a
test that requires the processor to exercise any particular order, grouping, temporary,
or implementation strategy.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 10.1.5.2.4 numeric approximation and mathematically equivalent rewrites registered;7.4.3
real/complex approximation registered.

### S11.1.7.5-010: SHARED variables refer to the outside variable and have cross-iteration definition, status-inquiry, and contiguous-dummy restrictions

**Source:** 11.1.7.5 p6; J3/24-007 PDF p216. **Class:** Restriction.

**Definition:** A SHARED variable appearance refers to the variable in the innermost enclosing
executable construct or scoping unit. If it is defined or becomes undefined during any
iteration, it shall not be referenced, defined, or become undefined during any other
iteration. If it is allocated, deallocated, nullified, or pointer-assigned during an
iteration, specified status, dynamic type, bounds, shape, or deferred type parameter
inquiries shall not occur in any other iteration. A noncontiguous SHARED array shall not
be supplied as an actual argument corresponding to a contiguous INTENT(INOUT) dummy
argument.

**Diagnostic obligation:** context-dependent.

**Facets:** `shared-refers-to-outside-final-state`, `shared-cross-iteration-definition-source-control`, `shared-cross-iteration-status-inquiry-source-control`, `shared-noncontiguous-contiguous-inout-source-control`.

**Oracle:** This source rule is a program obligation or context-dependent restriction, not a
portable executable oracle in this packet. Pending evidence is
source-control/documentary unless a future reviewed fixture can observe only a defined
final state under all permitted execution orders.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 8.5.7 CONTIGUOUS and 8.5.10 INTENT registered;12 procedures/dummy arguments and 15
procedure invocation NOT YET REGISTERED;19 status/dynamic type/definedness NOT YET
REGISTERED.

### S11.1.7.5-011: Unspecified-locality variables have cross-iteration definition, pointer, allocatable, and noncontiguous-dummy obligations

**Source:** 11.1.7.5 p7; J3/24-007 PDF p216. **Class:** Restriction.

**Definition:** For a variable with unspecified locality, references, definitions, undefinitions,
noncontiguous contiguous-INTENT(INOUT) actual arguments, pointer uses and
pointer-association changes, and allocatable allocation/deallocation/reference/status
inquiries are subject to the per-iteration and cross-iteration obligations listed in
11.1.7.5 p7; if it is defined or becomes undefined by more than one iteration it becomes
undefined when the loop terminates, and if a pointer association is changed in more than
one iteration its association status is undefined when the construct terminates.

**Diagnostic obligation:** context-dependent.

**Facets:** `unspecified-reference-source-control`, `unspecified-multidefine-undefined-after-source-control`, `unspecified-noncontiguous-dummy-source-control`, `unspecified-pointer-use-source-control`, `unspecified-pointer-multichange-undefined-after-source-control`, `unspecified-allocatable-multialloc-source-control`, `unspecified-allocatable-reference-source-control`.

**Oracle:** This source rule is a program obligation or context-dependent restriction, not a
portable executable oracle in this packet. Pending evidence is
source-control/documentary unless a future reviewed fixture can observe only a defined
final state under all permitted execution orders.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 8.5.7 CONTIGUOUS and 8.5.10 INTENT registered;9.7 allocation/deallocation registered;15
procedure invocation and 19 definedness/pointer/allocatable status NOT YET REGISTERED.

### S11.1.7.5-012: A DO CONCURRENT construct must not contain an input/output statement with an ADVANCE= specifier

**Source:** 11.1.7.5 p8; J3/24-007 PDF p216. **Class:** Restriction.

**Definition:** A DO CONCURRENT construct shall not contain an input/output statement that has an
ADVANCE= specifier.

**Diagnostic obligation:** context-dependent.

**Facets:** `no-advance-io-control`, `advance-io-in-do-concurrent-source-control`.

**Oracle:** This source rule is a program obligation or context-dependent restriction, not a
portable executable oracle in this packet. Pending evidence is
source-control/documentary unless a future reviewed fixture can observe only a defined
final state under all permitted execution orders.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 12 input/output statements and ADVANCE= specifier NOT YET REGISTERED.

### S11.1.7.5-013: File records or positions written in one DO CONCURRENT iteration must not be read or written in another iteration

**Source:** 11.1.7.5 p9; J3/24-007 PDF p216. **Class:** Restriction.

**Definition:** If data are written to a file record or position in one DO CONCURRENT iteration, that
record or position in that file shall not be read from or written to in a different
iteration.

**Diagnostic obligation:** context-dependent.

**Facets:** `file-record-cross-iteration-source-control`.

**Oracle:** This source rule is a program obligation or context-dependent restriction, not a
portable executable oracle in this packet. Pending evidence is
source-control/documentary unless a future reviewed fixture can observe only a defined
final state under all permitted execution orders.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 12 file and record I/O semantics NOT YET REGISTERED.

### S11.1.7.5-014: Sequential records written by different DO CONCURRENT iterations have processor-dependent ordering

**Source:** 11.1.7.5 p9; J3/24-007 PDF p216. **Class:** Effect.

**Definition:** If records are written to a file connected for sequential access by more than one DO
CONCURRENT iteration, the ordering of records written by different iterations is
processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `sequential-record-order-processor-dependent-source-control`.

**Oracle:** This records processor or source latitude, not a required observable behavior. It
supports rejecting order-sensitive or latitude-sensitive oracles and cannot support a
test that requires the processor to exercise any particular order, grouping, temporary,
or implementation strategy.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 12 sequential file I/O NOT YET REGISTERED.

<!-- END GENERATED 11.1.7.5 -->
