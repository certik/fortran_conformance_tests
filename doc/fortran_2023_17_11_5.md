# Fortran 2023 17.11.5: IEEE\_GET_FLAG

The canonical catalogue is `doc/catalogues/ieee_get_flag_17_11_5.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.5")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.5 -->

### S17.11.5-001: IEEE_GET_FLAG procedure identity

**Source:** S17.11.5 p1/p2; J3/24-007, 18 December 2023, physical PDF489. **Class:** Effect.

**Definition:** IEEE_GET_FLAG is the simple elemental subroutine whose description is get an exception
flag.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_GET_FLAG-get-exception-flag-description`, `IEEE_GET_FLAG-simple-elemental-subroutine-class`.

**Oracle:** Source registration only for IEEE_GET_FLAG. Future fixtures use guarded branch plans for
IEEE processor support: probe the relevant IEEE_SUPPORT_* inquiry first, assert the
stated effect only in the supported branch, and record an unsupported branch as skip
rather than pass. Positive observations initialize result variables or flags to opposite
sentinels, use exactly representable binary inputs such as 1.5, 2.0, -1.0, and -2.5, and
compare NaN, infinity, flag, rounding, halting, mode, and status effects only through
IEEE inquiry or get/set procedures.

**Oracle limitation:** Source registration only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json for IEEE_COPY_SIGN,
IEEE_FMA, IEEE_GET_FLAG, IEEE_GET_HALTING_MODE, IEEE_GET_MODES, IEEE_GET_ROUNDING_MODE,
IEEE_GET_STATUS, the in-scope IEEE_* derived types and named constants found only
tests/profiles/ieee_binary.f90 using ieee_support_datatype; no fixture.json binding or
procedure-name coverage exists for 17.11.3-17.11.9 in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, 15, and 16 where already
catalogued for source form, intrinsic types, declarations, expressions, execution,
program units, internal I/O observation, procedure calls, and intrinsic inquiry helpers.
Unregistered dependencies retained: clause 17 IEEE module definitions, IEEE_SUPPORT_*
inquiries, IEEE_SET_* companions, IEEE_VALUE and IEEE_IS_* classifiers, exception flag
and rounding mode constants, and neighbouring Clause 17 procedure catalogues owned by
sibling packets.

### S17.11.5-002: IEEE_GET_FLAG argument contract and flag value result

**Source:** S17.11.5 p3; J3/24-007, 18 December 2023, physical PDF489. **Class:** Effect.

**Definition:** FLAG shall be IEEE_FLAG_TYPE and specifies the exception flag; FLAG_VALUE shall be
logical INTENT(OUT) and receives true for a signaling IEEE_INVALID, IEEE_OVERFLOW,
IEEE_DIVIDE_BY_ZERO, IEEE_UNDERFLOW, or IEEE_INEXACT flag and false otherwise.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_GET_FLAG-FLAG-type`, `IEEE_GET_FLAG-FLAG_VALUE-logical-intent-out`, `IEEE_GET_FLAG-returns-signaling-state`.

**Oracle:** Source registration only for IEEE_GET_FLAG. Future fixtures use guarded branch plans for
IEEE processor support: probe the relevant IEEE_SUPPORT_* inquiry first, assert the
stated effect only in the supported branch, and record an unsupported branch as skip
rather than pass. Positive observations initialize result variables or flags to opposite
sentinels, use exactly representable binary inputs such as 1.5, 2.0, -1.0, and -2.5, and
compare NaN, infinity, flag, rounding, halting, mode, and status effects only through
IEEE inquiry or get/set procedures.

**Oracle limitation:** Source registration only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json for IEEE_COPY_SIGN,
IEEE_FMA, IEEE_GET_FLAG, IEEE_GET_HALTING_MODE, IEEE_GET_MODES, IEEE_GET_ROUNDING_MODE,
IEEE_GET_STATUS, the in-scope IEEE_* derived types and named constants found only
tests/profiles/ieee_binary.f90 using ieee_support_datatype; no fixture.json binding or
procedure-name coverage exists for 17.11.3-17.11.9 in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, 15, and 16 where already
catalogued for source form, intrinsic types, declarations, expressions, execution,
program units, internal I/O observation, procedure calls, and intrinsic inquiry helpers.
Unregistered dependencies retained: clause 17 IEEE module definitions, IEEE_SUPPORT_*
inquiries, IEEE_SET_* companions, IEEE_VALUE and IEEE_IS_* classifiers, exception flag
and rounding mode constants, and neighbouring Clause 17 procedure catalogues owned by
sibling packets.

<!-- END GENERATED 17.11.5 -->
