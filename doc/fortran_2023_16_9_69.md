# Fortran 2023 16.9.69: DATE_AND_TIME

The canonical catalogue is `doc/catalogues/date_and_time_16_9_69.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, note-unnumbered. Fine census units are recorded in the catalogue where mixed or dense source units are split.

<!-- BEGIN GENERATED 16.9.69 -->

### S16.9.69-001: DATE_AND_TIME provides date and time information

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Effect.

**Definition:** DATE_AND_TIME is the intrinsic for date and time information.

**Diagnostic obligation:** not-required.

**Facets:** `date-and-time-description`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-002: DATE_AND_TIME is a subroutine

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Effect.

**Definition:** DATE_AND_TIME is classified as a subroutine.

**Diagnostic obligation:** not-required.

**Facets:** `date-and-time-subroutine-class`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-003: DATE argument is a default character scalar INTENT(OUT) argument

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Restriction.

**Definition:** Optional DATE shall be a default character scalar and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `date-argument-default-character-scalar`, `date-intent-out`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-004: DATE receives YYYYMMDD digits or blanks

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Effect.

**Definition:** DATE is assigned YYYYMMDD decimal digits for Gregorian date information or all blanks if
no date is available.

**Diagnostic obligation:** not-required.

**Facets:** `date-yyyy-mm-dd-digits-or-blanks`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-005: TIME argument is a default character scalar INTENT(OUT) argument

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Restriction.

**Definition:** Optional TIME shall be a default character scalar and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `time-argument-default-character-scalar`, `time-intent-out`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-006: TIME receives hhmmss.sss digits or blanks

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Effect.

**Definition:** TIME is assigned hhmmss.sss with decimal digits except for the decimal point, or all
blanks if no clock is available.

**Diagnostic obligation:** not-required.

**Facets:** `time-hhmmss-sss-digits-dot-or-blanks`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-007: ZONE argument is a default character scalar INTENT(OUT) argument

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Restriction.

**Definition:** Optional ZONE shall be a default character scalar and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `zone-argument-default-character-scalar`, `zone-intent-out`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-008: ZONE receives a signed hhmm offset or blanks

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Effect.

**Definition:** ZONE is assigned +hhmm or -hhmm decimal offset characters, or all blanks if the
information is unavailable.

**Diagnostic obligation:** not-required.

**Facets:** `zone-signed-hhmm-digits-or-blanks`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-009: VALUES argument is a rank-one integer INTENT(OUT) array with sufficient range and size

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Restriction.

**Definition:** Optional VALUES shall be a rank-one integer array with decimal exponent range at least
four and size at least eight, and is an INTENT(OUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `values-rank-one-integer-range-size`, `values-intent-out`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-010: VALUES receives bounded date/time integers or -HUGE sentinels

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Effect.

**Definition:** The first eight VALUES elements receive
year/month/day/UTC-offset/hour/minute/second/millisecond values as specified, or
-HUGE(VALUES) when the corresponding information is unavailable.

**Diagnostic obligation:** not-required.

**Facets:** `values-date-elements-or-negative-huge`, `values-time-elements-or-negative-huge`, `values-zone-element-or-negative-huge`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.69-011: DATE_AND_TIME image availability and sharing are image-dependent processor latitude

**Source:** S16.9.69, J3/24-007, 18 December 2023, physical PDF402. **Class:** Effect.

**Definition:** Date, clock, and time zone information might be available on some images and not others;
if available on more than one image, whether images share the same information is
processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `date-time-availability-may-vary-by-image`, `date-time-multi-image-sharing-processor-dependent`.

**Oracle:** The p4 facets are image-dependent and are deliberately not converted into single-image
fixture obligations; p3 retains portable single-image form and range plans.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

<!-- END GENERATED 16.9.69 -->
