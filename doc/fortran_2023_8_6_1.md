# Fortran 2023 8.6.1: Accessibility statement

Source review is recorded in batch070. Effective review state comes from
`Registry.catalogue_review_state("8.6.1")`; current facet states remain in
the catalogue. Source classification does not approve fixtures or execution.
The canonical catalogue is `doc/catalogues/accessibility_statement_8_6_1.json`.

The ten original base units span physical PDF pages134-135, printed120-121,
of J3/24-007, December18,2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The complete section hash is
`7874021637b5cd9073fbb71aadae2cc42d3fbc6ed7b509744ffe1265fec28c2f`.
The next original heading is 8.6.2 ALLOCATABLE statement on PDF135.
The paragraph continuation and both separate notes are retained; neither
note is a complete fixture or a new normative requirement.

R830 distinguishes a bare default statement from a nonempty identifier
list, with an optional double colon inside that list-bearing form. R831
admits an eligible access-name or a canonical generic-spec, including
ordinary generic names, operators, assignment and all four defined-I/O
forms. Their actual interfaces and namespaces must be valid.

C873 counts omitted-list access statements within a module specification,
not PRIVATE keywords throughout a file. The private-components-stmt R745
and binding-private-stmt R747 are distinct productions with separate
owners. Type-name, component, binding, implementation-procedure and
constructor accessibility must not be collapsed; extension does not make
inherited private members accessible.

C874 retains the six named entity roles. A module-name list entry is an
import-default policy, not explicit accessibility of the module entity.
C875 requires that provider to be referenced by USE in the containing
scope. C876 counts its occurrences across access statements, including
one list, rather than counting USE statements or another module's policy.
Names and their resolved roles, not spellings or missing declarations,
determine the applicable condition.

Listed non-module identifiers receive explicit accessibility. Omitted-list
defaults apply to local declarations and applicable unlisted-provider
imports. An imported identifier that is also legitimately declared locally,
such as an extended generic, takes the local-declaration default. Only for
a nonlocal use-associated identifier whose every provider has a local
policy do all PRIVATE policies mean PRIVATE and any PUBLIC policy mean
PUBLIC. Missing provider policies do not satisfy that premise. Explicit
identifier access is not overwritten by a computed default, and no local
policy can import a provider's private identifier.

These statement/provenance interpretations supply the existing8.5.2
accessibility/default/use-access requirements; they do not replace those
owners or authorize a duplicate set of canonical executions. C815's
repeated explicit attribute, C1409's private USE identifier, C7107's
constructor access and the original type-bound effects retain their
current identities, input bytes, metadata and evidence roles.

Negative plans require complete ordered providers, real interfaces,
minimally repaired valid controls and exact causal reports at legitimate
source origins. Missing modules, unsupported features and nonspecific
PUBLIC/PRIVATE complaints are not proof. Prose defaults do not acquire a
universal reporting obligation; mandatory runtime effects are not replaced
with compile-only or no-op classifiers.

The source-only author packet added no fixtures, compiler observations,
profiles, role opt-ins, approvals, baseline updates, links or SourceUses,
and excluded its local index overlay. New p1 accounting genuinely staled
three existing binding-link reviews. Batch070 separately records source
approval, then explicit source-context renewals for those three connections,
and finally the inventory-context review. Their specifications and two
unique complete canonical programs are unchanged; the other19link reviews
remain exact. This adds no executions or passing linked aggregate and does
not relabel historical compiler observations.

<!-- BEGIN GENERATED 8.6.1 -->

### R830: An access statement has a qualified optional identifier list

**Source:** 8.6.1 R830, physical PDF134, printed120 **Class:** Syntax.

**Definition:** An access statement uses an access-spec and may have an access-id list. The double-colon
separator is optional within the list-bearing form; it is not a standalone alternative
when the list is absent. R807 owns PUBLIC/PRIVATE and R401 supplies the nonempty
comma-list convention. C873 and the other access-identifier constraints retain their own
premises.

**Diagnostic obligation:** required.

**Facets:** `listed-public-private-forms`, `omitted-list-forms`, `colon-without-list-exclusion`, `list-separator-source`, `mixed-list-syntax`.

**Oracle:** Finite complete grammar admissions and a minimally repaired incomplete-list contrast,
using native byte-preserving source contracts only in a later implementation phase.
PUBLIC/PRIVATE choice, identifier categories and default effects keep their defining
owners.

**Oracle limitation:** Required reporting capability does not prescribe fatal status, diagnostic English or a
printed code. A report must identify the actual malformed access statement at a
legitimate staged origin, not source echo, a quoted example, wrong-file recovery,
missing modules/interfaces or unsupported/Internal/ASR/verifier/crash/resource failures.
No observation is supplied.

**Dependencies:** 4.1.1;4.1.3 R401/R402;8.5.2 R807/C817;R831;C873-C876;14.2.1/.2;4.2p2.

### R831: An access identifier is a qualified name or generic specification

**Source:** 8.6.1 R831, physical PDF134, printed120 **Class:** Syntax.

**Definition:** An access-id is an access-name or a generic-spec. C874 qualifies the name branch;
R1508/R1509 define the generic-name, OPERATOR, ASSIGNMENT and defined-I/O alternatives.
A component designator, array element or type-bound binding name is not silently
substituted for a module-level identifier of the required category.

**Diagnostic obligation:** required.

**Facets:** `access-name-branch`, `generic-name-branch`, `operator-generic-branch`, `assignment-generic-branch`, `defined-io-generic-branch`.

**Oracle:** Complete contexts for both grammar alternatives and the finite canonical generic-spec
forms. Name roles and interfaces come from the original rules, not keyword scans or
compiler agreement. Actual generic visibility effects remain separate.

Attribute statements 8.6.a fixture implementation:
`attribute_statements_8_6_a_accessibility_resolution` covers `generic-name-branch`,
`operator-generic-branch`, `assignment-generic-branch` with 8 run-time assertion(s).
Runtime covered facets have named assertions and conforming feature mutations that
compile and fail at run time on both checked toolchains; diagnostic facets have a
one-property conforming control and a line-anchored excludes-only diagnostic oracle.
Oracles use exact INTEGER values, LOGICAL inquiry/PRESENT results, direct inquiries, and
same-source constant values only.

**Oracle limitation:** No arbitrary malformed keyword or wrong interface is invented as an isolated R831
negative. Generic syntax, definition, lookup and accessibility have different owners.
Type-bound generics are not direct USE generic identifiers under C1407/C1408, although a
separately declared ordinary interface may share their spelling. All implementation
facets remain pending.

Attribute statements 8.6.a fixture implementation: 3 facet(s) are fixture-backed here
and 2 remain pending. The fixtures do not claim coarray behavior, module-name access
policies rejected by the reference compiler, defined-I/O generics, diagnostic wording,
source-use inventory credit, or unselected grammar/source-gate facets.

**Dependencies:** 4.1.3 R402;C874;15.4.3.2 R1508/R1509/C1507-C1509;15.4.3.4.1-.3/.5;12.6.4.8.1/.2;14.2.2
C1407-C1410;19.3.1/.4.

### C873: Access statements belong in module specifications with at most one omitted-list statement

**Source:** 8.6.1 C873, physical PDF134, printed120 **Class:** Restriction.

**Definition:** An access-stmt occurs only in a module specification-part, which contains at most one
access-stmt without an access-id list. The limit is on this statement category, not all
PUBLIC/PRIVATE spellings or all statements in a file. Multiple lawful listed statements
remain subject to their separate identifier and attribute rules.

**Diagnostic obligation:** required.

**Facets:** `module-specification-context`, `nonmodule-context-source`, `one-no-list-default`, `listed-statements-not-defaults`, `component-binding-private-boundary`.

**Oracle:** A per-module statement-category and scope analysis, complete minimal statement-deletion
controls, and causal reports for the actual placement or no-list count defect.
Type-private grammar and ordinary access statements are not conflated.

Attribute statements 8.6.a fixture implementation:
`attribute_statements_8_6_a_c873_duplicate_omitted_private` covers `one-no-list-default`
with compile diagnostic/control. Runtime covered facets have named assertions and
conforming feature mutations that compile and fail at run time on both checked
toolchains; diagnostic facets have a one-property conforming control and a line-anchored
excludes-only diagnostic oracle. Oracles use exact INTEGER values, LOGICAL
inquiry/PRESENT results, direct inquiries, and same-source constant values only.

**Oracle limitation:** Numbered diagnostic capability is not mandatory rejection, exact English or a printed
C873. Bare PUBLIC/PRIVATE tokens, unrelated attributes, unsupported submodules,
unresolved providers, source echoes, Internal/ASR/verifier failures, crashes, generic
EOF and wrong-source recovery do not establish this condition.

Attribute statements 8.6.a fixture implementation: 1 facet(s) are fixture-backed here
and 4 remain pending. The fixtures do not claim coarray behavior, module-name access
policies rejected by the reference compiler, defined-I/O generics, diagnostic wording,
source-use inventory credit, or unselected grammar/source-gate facets.

**Dependencies:** 3.120;8.5.2 C817;8.5.1 C815;R830;7.5.2.1 R729/C743;7.5.4.8 R745/C771;7.5.5
R747/C772;14.2.1/.3 C1412;4.2p2.

### C874: Every access-name has one of six specified entity roles

**Source:** 8.6.1 C874, physical PDF134, printed120 **Class:** Restriction.

**Definition:** Each access-name names a module, variable, procedure, nonintrinsic type, named constant
or namelist group. The generic-spec alternative is separately defined by R831. The
parsed entity role, namespace and scope matter; word spelling alone does not identify an
intrinsic type, common block, component or procedure.

**Diagnostic obligation:** required.

**Facets:** `used-module-name`, `variable-name`, `procedure-name`, `nonintrinsic-type-name`, `named-constant-name`, `namelist-group-name`, `other-name-class-gates`.

**Oracle:** Finite complete contexts for the six literal source categories and explicitly held
namespace/classification contrasts. A source-qualified canonical role or grammar
relation precedes any negative; no invented third-party module or undeclared-name report
is evidence.

**Oracle limitation:** All seven facets are pending. C874's list is not replaced by a broad use-association
entity list or by generic-spec syntax. Existing type, constructor, component, binding
and C815 witnesses keep their owners and roles. No universal visibility effect is
claimed by mere name recognition.

**Dependencies:** R831;C875/C876;8.5.2;14.2.1p3/p4;14.2.2p2;15.4.3.2/.4;7.5.2.2/.10;7.6.1/.2;19.2;19.3.1-.4;4.2p2.

### C875: A named provider module requires a USE reference in the containing scope

**Source:** 8.6.1 C875, physical PDF134, printed120 **Class:** Restriction.

**Definition:** A module named in an access statement is referenced by a USE statement in the same
scoping unit as that access statement. A reference elsewhere in the source file or only
along an indirect import path is not this local USE. Module-name classification and the
normal acyclic/available-provider USE conditions remain necessary.

**Diagnostic obligation:** required.

**Facets:** `same-scope-use-required`, `only-renaming-references`, `nested-and-indirect-use-gates`.

**Oracle:** Complete module graphs with exactly controlled local USE presence, fixed entity roles
and a one-USE insertion repair. Every provider and relevant public interface exists;
absent external build artifacts cannot stand in for the specified relation.

**Oracle limitation:** Reporting capability is not a fatal status, exact code or generic module/PUBLIC/PRIVATE
wording. The entire module-name/access-statement/local-USE relation and legitimate
source origins must be calibrated later. Source ambiguity, unresolved interfaces,
unsupported facilities, echoes and compiler failures remain explicit noncredit gates.

**Dependencies:** 3.120;C874;14.2.1;14.2.2p1/p4-p9/R1409;19.2;19.3.1;19.5.1.3/.4;4.2p2.

### C876: Each provider module name occurs at most once in a module's access statements

**Source:** 8.6.1 C876, physical PDF134, printed120 **Class:** Restriction.

**Definition:** Within one receiving module, a module name occurs at most once across all access
statements. The quantifier includes repeated entries in one list and repetitions across
statements, regardless of equal or different access-specs. It is not a limit on the
number of USE statements or on a different receiving module's independent policy.

**Diagnostic obligation:** required.

**Facets:** `duplicate-module-in-one-list`, `repeated-module-across-statements`, `mixed-policy-repetition`, `scope-and-use-count-boundary`.

**Oracle:** One precisely resolved provider module, complete ordered sources and
single-occurrence-deletion controls. C815 remains the individual explicit-attribute
owner and C873 remains the omitted-list statement-count owner.

**Oracle limitation:** No compiler probe or diagnostic predicate is authored. Future evidence must preserve
complete case bytes, exact causes and live origins, including distinct legitimate
statement spans if required by the reporting processor. No source-line coalescing,
broadened span, forced fatal rejection or unsupported/ICE/recovery credit.

**Dependencies:** R830/R831;C874/C875;8.5.1 C815;14.2.2p1/p6-p9;19.2/19.3.1;4.2p2.

### S8.6.1-001: Listed non-module identifiers receive the statement's explicit accessibility

**Source:** 8.6.1 p1 first sentence, physical PDF134, printed120 **Class:** Effect.

**Definition:** An access statement with a list explicitly assigns its PUBLIC or PRIVATE accessibility
to each listed access-id that is not a module name. Module-name entries select import
defaults instead. Accessibility is via the relevant identifier, not an unconditional
entity-wide change across aliases, use paths, component names, bindings or a specific
implementation name.

**Diagnostic obligation:** not-required.

**Facets:** `listed-individual-identifiers`, `mixed-module-and-identifier-semantics`, `renamed-identifiers`, `generic-versus-specific-access`, `type-member-and-constructor-source`.

**Oracle:** The defining listed-statement interpretation is observed only through real complete
access/dispatch contexts or an independently approved canonical evidence relation.
S8.5.2 retains its declaration/default/use-access requirements; related canonical
observations are candidates, not duplicate local fixture slots.

**Oracle limitation:** No compile-only recognition or Python visibility classifier is a runtime effect. Private
use and constructor/member failures retain C1409, C7107 and Clause19 owners with actual
complete providers and precise source causes. No universal mandatory diagnostic follows
from this prose, and inaccessible data are not read to force a trap. Source, role and
link adjudications remain independent.

**Dependencies:** 8.5.2p1-p4/S8.5.2-001/-002/-003;8.5.1
C815;14.2.2p2/p7-p9/C1409;15.4.3.4;7.5.2.2/.4.8/.5/.7.2/.7.3/.10;19.3.1/.4;19.5.1.3/.4.

### S8.6.1-002: Statement defaults are selected by local declaration and the complete set of used providers

**Source:** 8.6.1 p1 default clauses and p2, physical PDF134-135, printed120-121 **Class:** Effect.

**Definition:** A no-list access statement specifies the module default for locally declared entity
identifiers and identifiers imported from a module whose name is absent from all local
access statements. PUBLIC selects a public default, PRIVATE a private default, and no
such statement leaves a public default. An identifier both imported and locally declared
follows the local-declaration default. For a use-associated identifier not locally
declared, when every module from which it is accessed is named in a local access
statement, all PRIVATE specifications give a private default and any PUBLIC
specification gives a public default. This computes defaults; it does not override an
explicit individual access specification or make a private provider identifier
importable.

**Diagnostic obligation:** not-required.

**Facets:** `local-default-forms`, `unlisted-provider-fallback`, `single-provider-policy`, `all-private-providers`, `any-public-provider`, `incomplete-provider-policies`, `imported-plus-local-generic`, `explicit-access-and-origin-boundaries`.

**Oracle:** A finite complete source-provenance and local-declaration matrix with real providers,
lawful generic merges, independent literal data/dispatch results and exact completion
events where runtime effects are observed. S8.5.2-002 consumes these statement-defined
defaults; overlapping source obligations must share independently reviewed canonical
evidence rather than copied fixtures or reciprocal owner rewrites.

**Oracle limitation:** Every facet is pending. This is not a compiler-consensus or text-classifier oracle.
Incomplete origin policies, a locally declared generic, two distinct ambiguous ultimate
variables, a private exporting identifier and a missing module are different conditions.
Pure default prose does not mandate rejection; any negative keeps its actual
numbered/name-scope basis, minimally repaired control and precise live-origin cause.
Required runtime semantics are not downgraded to compile-only for unsupported
module-name policies.

**Dependencies:** 8.5.2p3/p4/S8.5.2-002;R830/R831;C873-C876;14.2.2p1/p2/p4-p9/C1409;15.4.3.4.1/.5
C1514;19.2;19.3.1/.4;19.5.1.3/.4;7.5.4.8p1;7.5.5p8.

<!-- END GENERATED 8.6.1 -->
