# Fortran 2023: 10.1.2.4 Level-1 expressions

Catalogue: `doc/catalogues/level_1_expressions_10_1_2_4.json`. The original source-only registration supplied pending plans; the bounded batch291 fixtures below supply selected cases and mutation/diagnostic plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN EXPRESSIONS 10.1.1-10.1.2.4 FIXTURES -->
## Batch291 expression fixtures for 10.1.2.4

This section has generated batch291 fixtures for: S10.1.2.4-001, R1003, R1004, C1005. Runtime cases use exact integer, logical, character length/kind, rank, and shape oracles; diagnostic cases are line-anchored one-property malformed forms with conforming repairs documented in generator metadata. Feature mutations are permanent and checked on both the reference gfortran 16.1 and the frozen LFortran target. Facets not listed remain pending for the reasons recorded in each requirement.
<!-- END EXPRESSIONS 10.1.1-10.1.2.4 FIXTURES -->

<!-- BEGIN GENERATED 10.1.2.4 -->

### S10.1.2.4-001: Defined unary operators occupy the highest precedence layer

**Source:** 10.1.2.4 p1; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** Level-1 expressions are primaries optionally operated on by defined unary operators, and
defined unary operators have the highest operator precedence. This precedence statement
is structural; defined-operation applicability and interpretation remain dependencies.

**Diagnostic obligation:** not-required.

**Facets:** `defined-unary-before-intrinsic`, `defined-unary-on-parenthesized-expression`, `primary-without-defined-unary`.

**Oracle:** Future precedence controls must use user-defined functions with distinctive literal
results and must not claim coverage of generic resolution beyond 15.4.3.4.2 and 10.1.6.

Batch291 expressions runtime/diagnostic fixtures: one positive-control program defines
.u. as 10*x and observes .u.2*3=60, .u.(1+2)=30, and primary 6*2=12. The values
distinguish defined-unary grouping before lower intrinsic levels and the parenthesized
operand form.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch291 expressions fixture boundaries: Only the listed selected facets are represented
by batch291 fixtures. Existing source review state, evidence approval, rollout progress,
xfail policy, and unrelated facets are not changed. Remaining pending facets are left
for later packets where this generator does not provide a portable, both-toolchain
parent/control.

**Dependencies:** 10.1.2.2R1001;10.1.2.5R1005;10.1.3 Table 10.1;10.1.6;10.1.8;15.4.3.4.2.

### R1003: A level-1 expression is an optional defined unary operator followed by a primary

**Source:** 10.1.2.4 R1003; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The level-1-expr syntax is either a primary or a defined-unary-op immediately followed
by a primary. The operator is prefix and non-repeating in this grammar level unless
another primary form introduces parentheses or a lower expression.

**Diagnostic obligation:** required.

**Facets:** `primary-only`, `defined-unary-primary`, `two-unary-without-parentheses-rejected`.

**Oracle:** Negatives must use well-formed defined-unary-op spellings and valid interfaces so the
only defect is the absence of a primary after the first operator.

Batch291 expressions runtime/diagnostic fixtures: one positive control observes
primary-only and defined-unary-primary level-1 forms, and one diagnostic fixture uses
.u. .v. x with valid interfaces so the absence of a primary after the first operator is
isolated.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch291 expressions fixture boundaries: Only the listed selected facets are represented
by batch291 fixtures. Existing source review state, evidence approval, rollout progress,
xfail policy, and unrelated facets are not changed. Remaining pending facets are left
for later packets where this generator does not provide a portable, both-toolchain
parent/control.

**Dependencies:** 4.2;10.1.2.2R1001;10.1.2.4R1004/C1005;10.1.6;15.4.3.4.2.

### R1004: A defined unary operator is a dotted sequence of letters

**Source:** 10.1.2.4 R1004; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The defined-unary-op spelling is a period, one or more letters, and a period. Digits,
underscores, missing dots, and empty names are not part of this syntactic form.

**Diagnostic obligation:** required.

**Facets:** `single-letter`, `multiple-letters`, `missing-leading-dot-rejected`, `missing-trailing-dot-rejected`, `digit-in-name-rejected`.

**Oracle:** R1004 diagnostics are syntax diagnostics. Plans involving excessive length or
intrinsic/logical spellings are attributed to C1005 instead, not to this rule.

Batch291 expressions runtime/diagnostic fixtures: one positive control observes
single-letter .u. and letters-only .inverse. operators, and one diagnostic fixture uses
the dotted spelling .u1. so the digit-in-name syntax violation is isolated.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch291 expressions fixture boundaries: Only the listed selected facets are represented
by batch291 fixtures. Existing source review state, evidence approval, rollout progress,
xfail policy, and unrelated facets are not changed. Remaining pending facets are left
for later packets where this generator does not provide a portable, both-toolchain
parent/control.

**Dependencies:** 4.2;6.2.2 names and letters;6.2.4R609;10.1.2.4C1005;15.4.3.2;15.4.3.4.2.

### C1005: Defined unary operator names are short and not intrinsic or logical-literal spellings

**Source:** 10.1.2.4 C1005; J3/24-007 PDF p169. **Class:** Restriction.

**Definition:** A defined-unary-op shall not contain more than 63 letters and shall not be the same as
any intrinsic-operator or logical-literal-constant. This is a lexical/name restriction
independent of whether a matching interface exists.

**Diagnostic obligation:** required.

**Facets:** `sixty-three-letter-control`, `sixty-four-letter-rejected`, `intrinsic-operator-spelling-rejected`, `logical-true-spelling-rejected`, `logical-false-spelling-rejected`.

**Oracle:** Length and reserved-spelling negatives must retain R1004 dotted-letter syntax; malformed
dots or digits are not C1005 evidence.

Batch291 expressions runtime/diagnostic fixtures: one positive control uses a generated
63-letter defined unary operator. Four diagnostics isolate 64 letters, intrinsic .not.
spelling, and .true./.false. logical-literal spellings while keeping the dotted-letter
syntax otherwise intact.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch291 expressions fixture boundaries: Only the listed selected facets are represented
by batch291 fixtures. Existing source review state, evidence approval, rollout progress,
xfail policy, and unrelated facets are not changed. Remaining pending facets are left
for later packets where this generator does not provide a portable, both-toolchain
parent/control. The 64-letter and intrinsic-operator-spelling diagnostics are shipped as
genuine LFortran defects: gfortran diagnoses both isolated negatives, while the frozen
LFortran target accepts them.

**Dependencies:** 4.2;6.2.4R608-R609;7.4.5 logical literal
constants;10.1.2.4R1004;10.1.2.5R1008-R1010;10.1.2.6R1012;10.1.2.7R1014;10.1.2.8R1019-R1022;15.4.3.4.2.

<!-- END GENERATED 10.1.2.4 -->
