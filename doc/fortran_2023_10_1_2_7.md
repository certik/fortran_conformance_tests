# Fortran 2023: 10.1.2.7 Level-4 expressions

Catalogue: `doc/catalogues/level_4_expressions_10_1_2_7.json`. The original source-only registration supplied pending plans; the bounded batch295 fixtures below supply selected cases and mutation/diagnostic plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN EXPRESSIONS 10.1.2.5-10.1.2.7 FIXTURES -->
## Batch295 expression fixtures for 10.1.2.7

Generated batch295 fixtures cover selected facets for: S10.1.2.7-001, R1013, R1014. Runtime cases use exact integer, logical, and character LEN/value oracles; diagnostic cases are line-anchored one-property malformed forms with conforming repairs documented in generator metadata. Feature mutations, including concat operator replacements with defined .cat. controls, are permanent and checked on both the reference gfortran and frozen LFortran target. Facets not listed remain pending for the recorded reasons.
<!-- END EXPRESSIONS 10.1.2.5-10.1.2.7 FIXTURES -->

<!-- BEGIN GENERATED 10.1.2.7 -->

### S10.1.2.7-001: Level-4 expressions are the relational-operator layer over level-3 expressions

**Source:** 10.1.2.7 p1; J3/24-007 PDF p170. **Class:** Syntax.

**Definition:** Level-4 expressions are level-3 expressions optionally involving one relational
operator. The grammar admits a level-3 expression by itself or one relation between two
level-3 expressions; relation interpretation is separate.

**Diagnostic obligation:** not-required.

**Facets:** `level-3-base`, `single-relation-layer`, `no-unparenthesized-relation-chain`.

**Oracle:** Plans must distinguish grammar admission from relational truth rules and avoid chained
relations being rejected for type mismatch after an unintended parse.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: relational fixtures observe a level-3
expression without rel-op, one relation 2<3, and diagnostics for an unparenthesized
relation chain. Runtime operators use exact logical results; character equality checks
first inquire LEN on each operand to show blank-padding width.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 10.1.2.6R1011;10.1.3 Table 10.1;10.1.5.5;10.1.9.

### R1013: A level-4 expression is either level-3 or one relational operation between level-3 expressions

**Source:** 10.1.2.7 R1013; J3/24-007 PDF p170. **Class:** Syntax.

**Definition:** The level-4-expr syntax is a level-3-expr, optionally preceded by another level-3-expr
and rel-op. Thus an unparenthesized level-4 expression contains at most one relational
operator at this grammar level.

**Diagnostic obligation:** required.

**Facets:** `single-level-3`, `relational-operation`, `chained-relation-rejected`, `missing-right-level-3-rejected`.

**Oracle:** Chained-relation negatives must avoid relying on logical-integer comparison semantics;
the source must be rejected because the grammar cannot form a second rel-op at level 4.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: relational fixtures observe a single
level-3 expression and one relational operation; diagnostics plus compiled one-property
controls isolate a second unparenthesized rel-op and a missing right level-3 expression.
The chained-relation negative is a genuine frozen-LFortran defect case: gfortran
diagnoses it while the target accepts it as an extension.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property. The
chained-relation diagnostic is shipped as a genuine LFortran defect: gfortran diagnoses
the isolated R1013 violation, while the frozen target accepts it with an extension
warning.

**Dependencies:** 4.2;10.1.2.6R1011;10.1.2.7R1014;10.1.5.5.

### R1014: Relational operators have twelve spellings

**Source:** 10.1.2.7 R1014; J3/24-007 PDF pp170-171. **Class:** Syntax.

**Definition:** The rel-op alternatives are .EQ., .NE., .LT., .LE., .GT., .GE., ==, /=, <, <=, >, and
>=. The rule defines spellings only; paired interpretation and type admissibility are
owned by relational intrinsic operation rules.

**Diagnostic obligation:** required.

**Facets:** `dot-eq`, `dot-ne`, `dot-lt`, `dot-le`, `dot-gt`, `dot-ge`, `eqeq`, `slash-eq`, `lt`, `le`, `gt`, `ge`, `non-rel-op-rejected`.

**Oracle:** Future token controls should include both dotted and symbolic spellings but must cite
10.1.5.5 for truth values and operand type restrictions.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: one runtime fixture observes all twelve
rel-op spellings with exact logical assertions. .EQ. and == are both exercised on
character operands of lengths 1 and 2 after direct LEN checks, relying on blank padding;
ordering operators use integers to avoid character collating-order dependence.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property. The
non-rel-op-rejected facet remains pending: candidate malformed tokens were not
attributable solely to R1014.

**Dependencies:** 4.2;6.2.4R608;10.1.2.7R1013;10.1.5.5.

<!-- END GENERATED 10.1.2.7 -->
