# Fortran 2023 17.11.38: IEEE_SELECTED_REAL_KIND

The canonical catalogue is `doc/catalogues/ieee_selected_real_kind_17_11_38.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.38")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7, p8. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.38 -->

### S17.11.38-001: IEEE_SELECTED_REAL_KIND argument restrictions

**Source:** S17.11.38, J3/24-007, 18 December 2023, physical PDF502. **Class:** Restriction.

**Definition:** At least one argument shall be present; P, R, and RADIX shall be integer scalars.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SELECTED_REAL_KIND-at-least-one-argument`, `IEEE_SELECTED_REAL_KIND-P-integer-scalar`, `IEEE_SELECTED_REAL_KIND-R-integer-scalar`, `IEEE_SELECTED_REAL_KIND-RADIX-integer-scalar`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.38-002: IEEE_SELECTED_REAL_KIND returns a default integer scalar

**Source:** S17.11.38, J3/24-007, 18 December 2023, physical PDF502. **Class:** Effect.

**Definition:** The result of IEEE_SELECTED_REAL_KIND is a default integer scalar.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SELECTED_REAL_KIND-result-default-integer-scalar`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.38-003: IEEE_SELECTED_REAL_KIND absent P and R are zero

**Source:** S17.11.38, J3/24-007, 18 December 2023, physical PDF502. **Class:** Effect.

**Definition:** Absent P or R has the same effect as presence with value zero.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SELECTED_REAL_KIND-absent-P-equals-zero`, `IEEE_SELECTED_REAL_KIND-absent-R-equals-zero`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.38-004: IEEE_SELECTED_REAL_KIND positive result denotes an available IEEE format kind

**Source:** S17.11.38, J3/24-007, 18 December 2023, physical PDF502. **Class:** Effect.

**Definition:** For an available ISO/IEC/IEEE 60559 floating-point format meeting the requested
precision, range, and radix, the result is a kind type parameter value for such a
format.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SELECTED_REAL_KIND-available-kind-positive`, `IEEE_SELECTED_REAL_KIND-available-kind-meets-request`, `IEEE_SELECTED_REAL_KIND-positive-kind-supports-datatype`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.38-005: IEEE_SELECTED_REAL_KIND absent RADIX imposes no radix requirement

**Source:** S17.11.38, J3/24-007, 18 December 2023, physical PDF502. **Class:** Effect.

**Definition:** With absent RADIX, there is no requirement on the radix of the selected kind.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SELECTED_REAL_KIND-absent-RADIX-no-radix-oracle`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.38-006: IEEE_SELECTED_REAL_KIND failure codes describe unavailable criteria

**Source:** S17.11.38, J3/24-007, 18 December 2023, physical PDF502. **Class:** Effect.

**Definition:** Unavailable criteria return the specified negative codes -1 through -5 according to
which radix, precision, and range capabilities exist.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SELECTED_REAL_KIND-minus-1-precision-missing`, `IEEE_SELECTED_REAL_KIND-minus-2-range-missing`, `IEEE_SELECTED_REAL_KIND-minus-3-precision-and-range-missing`, `IEEE_SELECTED_REAL_KIND-minus-4-no-combined-kind`, `IEEE_SELECTED_REAL_KIND-minus-5-radix-missing`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.38-007: IEEE_SELECTED_REAL_KIND chooses the smallest precision and then smallest kind among ties

**Source:** S17.11.38, J3/24-007, 18 December 2023, physical PDF502. **Class:** Effect.

**Definition:** Among multiple kind values meeting the criteria, the returned value has the smallest
decimal precision, then the smallest kind value among equal-precision candidates.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SELECTED_REAL_KIND-smallest-precision-selection`, `IEEE_SELECTED_REAL_KIND-smallest-kind-tie-selection`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

<!-- END GENERATED 17.11.38 -->
