# Fortran 2023 16.9.30: ATOMIC CAS

The canonical catalogue is `doc/catalogues/atomic_cas_16_9_30.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4. Fine census units are recorded in the catalogue where the Arguments paragraph is split.

<!-- BEGIN GENERATED 16.9.30 -->

### S16.9.30-001: ATOMIC_CAS is described as Atomic compare and swap

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** Atomic compare and swap.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-description`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-002: ATOMIC_CAS is an atomic subroutine

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** The intrinsic is classified as an atomic subroutine.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-atomic-subroutine-class`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-003: ATOMIC_CAS ATOM is an atomic integer or logical scalar coarray or coindexed object

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Restriction.

**Definition:** ATOM is a scalar coarray or coindexed object of type integer kind ATOMIC_INT_KIND or
logical kind ATOMIC_LOGICAL_KIND and INTENT (INOUT).

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-atom-scalar-coarray-or-coindexed`, `atomic_cas-atom-integer-atomic-int-kind-or-logical-atomic-logical-kind`, `atomic_cas-atom-intent-inout`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-004: ATOMIC_CAS stores NEW when ATOM matches COMPARE

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** On successful execution, if integer ATOM is equal to COMPARE or logical ATOM is
equivalent to COMPARE, ATOM becomes defined with NEW.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-integer-match-stores-new`, `atomic_cas-logical-equivalent-match-stores-new`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-005: ATOMIC_CAS ATOM becomes undefined on error

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Undefined result.

**Definition:** If an error condition occurs, ATOM becomes undefined.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-atom-undefined-on-error`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-006: ATOMIC_CAS OLD matches ATOM and is an output argument

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Restriction.

**Definition:** OLD is scalar, has the same type and kind as ATOM, and is INTENT (OUT).

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-old-scalar-same-type-kind-as-atom`, `atomic_cas-old-intent-out`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-007: ATOMIC_CAS OLD receives the original ATOM value

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** On successful execution, OLD becomes defined with the value that ATOM had at the start
of the atomic operation.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-old-receives-original-atom`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-008: ATOMIC_CAS OLD becomes undefined on error

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Undefined result.

**Definition:** If an error condition occurs, OLD becomes undefined.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-old-undefined-on-error`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-009: ATOMIC_CAS COMPARE matches ATOM and is input-only

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Restriction.

**Definition:** COMPARE is scalar, has the same type and kind as ATOM, and is INTENT (IN).

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-compare-scalar-same-type-kind-as-atom`, `atomic_cas-compare-intent-in`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-010: ATOMIC_CAS NEW matches ATOM and is input-only

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Restriction.

**Definition:** NEW is scalar, has the same type and kind as ATOM, and is INTENT (IN).

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-new-scalar-same-type-kind-as-atom`, `atomic_cas-new-intent-in`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-011: ATOMIC_CAS STAT argument is a suitable integer scalar

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Restriction.

**Definition:** The optional STAT argument is a noncoindexed integer scalar with decimal exponent range
at least four and INTENT (OUT).

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-stat-noncoindexed-integer-scalar`, `atomic_cas-stat-decimal-exponent-range-at-least-four`, `atomic_cas-stat-intent-out`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-012: ATOMIC_CAS STAT is zero on successful execution

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** If STAT is present and no error condition occurs, it is assigned the value zero as
specified by 16.5 p4.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-stat-success-zero`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-013: ATOMIC_CAS STAT receives STAT_FAILED_IMAGE for a failed-image ATOM

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** If STAT is present and an error condition occurs because ATOM is on a failed image, STAT
is assigned STAT_FAILED_IMAGE from ISO_FORTRAN_ENV as specified by 16.5 p5.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-stat-failed-image-status`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-014: ATOMIC_CAS other atomic errors choose processor-dependent positive STAT values

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** For any other atomic error condition with STAT present, STAT is assigned a
processor-dependent positive value different from STAT_FAILED_IMAGE as specified by 16.5
p5.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-stat-other-error-positive-processor-dependent`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

### S16.9.30-015: ATOMIC_CAS without STAT initiates error termination for an atomic error

**Source:** S16.9.30, J3/24-007, 18 December 2023, physical PDF386. **Class:** Effect.

**Definition:** If STAT is absent and an error condition occurs, error termination is initiated as
specified by 16.5 p6.

**Diagnostic obligation:** not-required.

**Facets:** `atomic_cas-absent-stat-error-termination`.

**Oracle:** Plans are pending and source-derived. This packet intentionally has no executable
atomic-subroutine fixture plans: every conforming ATOM actual for these intrinsics is a
scalar coarray or coindexed object, and all coarray fixtures, including local
single-image coarrays, are out of scope by user decision. The catalogue therefore
records only source accounting for exact integer/logical effects and the 16.5 STAT
alternatives; it makes no fixture, compiler-execution, coverage, multi-image
ordering/progress, failed-image, processor-dependent error-value, or absent-STAT
termination claim.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray/coindexed-object observations, including local single-image coarrays, and
failed-image observations are out of scope by user decision. Unnumbered argument shalls
are not diagnostic facets; a future negative is allowed only if another numbered syntax
rule or constraint requires rejection and the fixture reaches that rule without
overlapping diagnostics. Undefinedness, IOMSG text, processor-dependent positive STAT
values, and error termination without a portable coarray error trigger are never used as
fixture oracles in this suite.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, 12, and 13 for source form, names,
expressions, execution constructs, and existing I/O/editing foundations. Unregistered
dependencies explicitly retained: clause 5 image/segment execution model, clause 16.5
atomic-subroutine semantics and STAT values, ISO_FORTRAN_ENV atomic kind and status
constants, and sibling clause 16 intrinsic definitions such as IAND, IOR, IEOR, and INT
where the result expression names them.

<!-- END GENERATED 16.9.30 -->
