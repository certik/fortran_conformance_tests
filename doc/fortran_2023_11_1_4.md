# Fortran 2023: 11.1.4 BLOCK construct

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN BLOCK BRANCH 11.1.4 11.2 FIXTURES -->
## BLOCK/branch executable fixture packet for 11.1.4

This packet adds generated f2023 fixtures for S11.1.4-001, C1107, C1108, C1110, S11.1.4-002, S11.1.4-003, S11.1.4-004. Runtime cases use integer sentinels and exact completion output. Diagnostic cases require a located error at the violating line and exclude unsupported-feature/internal-error routes. Pure processor latitude and facets whose single-image oracle is not isolated remain pending in the catalogue.
<!-- END BLOCK BRANCH 11.1.4 11.2 FIXTURES -->

<!-- BEGIN GENERATED 11.1.4 -->

### S11.1.4-001: BLOCK is an executable construct that can contain declarations

**Source:** 11.1.4 p1; J3/24-007 PDF p205. **Class:** Effect.

**Definition:** The BLOCK construct is classified as an executable construct and can contain
declarations. The allowed specification part forms, exclusions, scope, and execution
order are supplied by R1107-R1110, C1107-C1110, and p2-p4.

**Diagnostic obligation:** not-required.

**Facets:** `block-executable-construct-control`, `declarations-in-block-source`.

**Oracle:** This paragraph classifies BLOCK; it does not by itself permit excluded declaration forms
or define construct-entity scope.

S11.1.4-001 block-branch fixture family: one run/effect/f2023 program surrounds a
reached BLOCK with before/body/after integer events. The body contains a BLOCK-local
declaration and a separate assignment sentinel, so removing the declaration or changing
the body event is load-bearing and normal execution after END BLOCK is observed.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future BLOCK fixtures must distinguish BLOCK-local construct entities from host or
enclosing entities with non-vacuous initial values, keep declaration owners separate
from execution effects, and must not infer processor-dependent specification-expression
evaluation order, storage duration, addresses, or SAVE implementation strategy.
Diagnostics require a single attributed numbered constraint or explicit prohibiting
prose.

S11.1.4-001 block-branch fixture boundaries: only the listed facets are represented by
this generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 8.1 execution part/specification part registered;11.1.2 block execution in this
packet;C1107-C1110/p2 in this packet.

### R1107: BLOCK construct syntax has block statement, optional block specification part, block, and end block statement

**Source:** 11.1.4 R1107; J3/24-007 PDF p205. **Class:** Syntax.

**Definition:** A block-construct consists of a block-stmt, an optional block-specification-part, a
block, and an end-block-stmt.

**Diagnostic obligation:** required.

**Facets:** `block-without-specification-part`, `block-with-specification-part`, `block-body-form`.

**Oracle:** Syntax admission only; scope and execution effects are owned by p2-p4.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

**Dependencies:** R1108-R1110;11.1.1 R1101.

### R1108: BLOCK statement syntax permits an optional block construct name

**Source:** 11.1.4 R1108; J3/24-007 PDF p205. **Class:** Syntax.

**Definition:** A block-stmt is an optional block-construct-name followed by BLOCK.

**Diagnostic obligation:** required.

**Facets:** `unnamed-block-stmt`, `named-block-stmt`.

**Oracle:** Syntax admission only; scope and execution effects are owned by p2-p4.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

**Dependencies:** C1110;6.2.2 names registered.

### R1109: A block specification part contains USE statements, IMPORT statements, and declaration constructs in that order

**Source:** 11.1.4 R1109; J3/24-007 PDF p206. **Class:** Syntax.

**Definition:** The block-specification-part grammar is zero or more use-stmt entries, followed by zero
or more import-stmt entries, followed by zero or more declaration-construct entries.

**Diagnostic obligation:** required.

**Facets:** `declaration-only-block-spec`, `use-before-declaration-source`, `import-before-declaration-source`, `use-import-declaration-order`.

**Oracle:** Syntax admission only; scope and execution effects are owned by p2-p4.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

**Dependencies:** 8.8 IMPORT statement registered;14 USE association NOT YET REGISTERED;8 declaration
constructs registered.

### R1110: END BLOCK syntax permits an optional block construct name

**Source:** 11.1.4 R1110; J3/24-007 PDF p206. **Class:** Syntax.

**Definition:** An end-block-stmt is END BLOCK followed by an optional block-construct-name.

**Diagnostic obligation:** required.

**Facets:** `unnamed-end-block`, `named-end-block`.

**Oracle:** Syntax admission only; scope and execution effects are owned by p2-p4.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

**Dependencies:** C1110;R1107.

### C1107: BLOCK specification parts exclude COMMON, EQUIVALENCE, INTENT, NAMELIST, OPTIONAL, statement functions, and VALUE

**Source:** 11.1.4 C1107; J3/24-007 PDF p206. **Class:** Restriction.

**Definition:** A block-specification-part shall not contain a COMMON statement, EQUIVALENCE statement,
INTENT statement, NAMELIST statement, OPTIONAL statement, statement function, or VALUE
statement.

**Diagnostic obligation:** required.

**Facets:** `ordinary-declaration-control`, `common-in-block-rejected`, `equivalence-in-block-rejected`, `intent-in-block-rejected`, `namelist-in-block-rejected`, `optional-in-block-rejected`, `statement-function-in-block-rejected`, `value-in-block-rejected`.

**Oracle:** Each negative must keep the surrounding BLOCK syntax and declaration context valid; the
single differing property is the excluded statement/function form named by C1107.

C1107 block-branch fixture family: one ordinary-declaration run/positive-control BLOCK
uses only INTEGER declarations and no excluded specification form. Seven compile/f2023
negatives each differ from a real control by exactly one excluded COMMON, EQUIVALENCE,
INTENT, NAMELIST, OPTIONAL, statement-function, or VALUE form in the BLOCK specification
part; any error diagnostic at the violating line is sufficient, with unsupported/ICE
routes excluded.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

C1107 block-branch fixture boundaries: only the listed facets are represented by this
generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 8.6 COMMON/EQUIVALENCE/INTENT/NAMELIST/OPTIONAL/VALUE statements registered;statement
functions NOT YET REGISTERED.

### C1108: SAVE in BLOCK has a saved-entity list and does not specify a common block name

**Source:** 11.1.4 C1108; J3/24-007 PDF p206. **Class:** Restriction.

**Definition:** A SAVE statement in a BLOCK construct shall contain a saved-entity-list that does not
specify a common-block-name.

**Diagnostic obligation:** required.

**Facets:** `listed-save-entity-control`, `bare-save-in-block-rejected`, `common-block-save-in-block-rejected`.

**Oracle:** The invalid source must not also violate C1107 COMMON declaration exclusion;
common-block identity and SAVE syntax remain dependencies.

C1108 block-branch fixture family: a run/positive-control/f2023 BLOCK-local INTEGER with
SAVE :: kept is entered three times and increments 1/2/3 without a declaration
initializer; the conforming no-SAVE mutation reinitializes an unsaved local on each
entry and fails the persistence assertions. A separate compile negative names a common
block in the SAVE list while its control names an ordinary BLOCK-local entity.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

C1108 block-branch fixture boundaries: only the listed facets are represented by this
generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 8.6.14 SAVE statement registered;8.10.2 COMMON registered;C1107 in this packet.

### C1109: A BLOCK block does not begin with FORMAT or DATA

**Source:** 11.1.4 C1109; J3/24-007 PDF p206. **Class:** Restriction.

**Definition:** The block of a block-construct shall not begin with a FORMAT statement or a DATA
statement.

**Diagnostic obligation:** required.

**Facets:** `ordinary-first-executable-control`, `format-first-rejected`, `data-first-rejected`.

**Oracle:** The negative must distinguish the block body from the block-specification-part and keep
DATA/FORMAT syntax otherwise valid.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

**Dependencies:** 8.6.7 DATA statement registered;FORMAT statement NOT YET REGISTERED;11.1.1 R1101 in this
packet.

### C1110: BLOCK construct names are consistently present and equal

**Source:** 11.1.4 C1110; J3/24-007 PDF p206. **Class:** Restriction.

**Definition:** If the block-stmt specifies a block-construct-name, the corresponding end-block-stmt
shall specify the same name. If the block-stmt does not specify a name, the
corresponding end-block-stmt shall not specify a block-construct-name.

**Diagnostic obligation:** required.

**Facets:** `matching-block-construct-name-control`, `end-block-name-mismatch-rejected`, `unnamed-block-end-name-rejected`.

**Oracle:** The block specification part and body remain valid in each pair; only construct-name
presence/equality differs.

C1110 block-branch fixture family: one matching-name run/positive-control BLOCK proves a
named block with the same END BLOCK name is admitted. Two compile negatives vary only
the END BLOCK name: one mismatches a named opener and one supplies a name for an unnamed
BLOCK.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future syntax or constraint fixtures must isolate the cited numbered item with a
conforming positive control and a minimal repair when a diagnostic is expected. Required
diagnostics mean processor reporting capability under 4.2, not fixed wording, fatal
status, a printed rule number, or acceptance by another compiler.

C1110 block-branch fixture boundaries: only the listed facets are represented by this
generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** R1108;R1110;6.2.2 names registered.

### S11.1.4-002: BLOCK specifications declare BLOCK-scope construct entities except scoped ASYNCHRONOUS, IMPORT, and VOLATILE cases

**Source:** 11.1.4 p2; J3/24-007 PDF p206. **Class:** Effect.

**Definition:** Except for ASYNCHRONOUS, IMPORT, and VOLATILE statements, specifications in a BLOCK
construct declare construct entities whose scope is that of the BLOCK construct. If an
object name that is not a construct entity appears in ASYNCHRONOUS or VOLATILE in a
BLOCK construct, the object has that attribute within the construct even if it does not
outside.

**Diagnostic obligation:** not-required.

**Facets:** `block-local-construct-entity-scope`, `outer-homonym-unaffected`, `asynchronous-nonconstruct-entity-scoped-attribute`, `volatile-nonconstruct-entity-scoped-attribute`, `import-exception-source`.

**Oracle:** Scoping assertions must use discriminating values. This p2 owns BLOCK-local
construct-entity scope and the local ASYNCHRONOUS/VOLATILE attribute effect, but not
general host/use association or attribute operational semantics.

S11.1.4-002 block-branch fixture family: one run/effect/f2023 program initializes an
outer integer to101, declares a BLOCK-local homonym initialized to7, assigns/checks9
inside, and then observes the outer still101 after END BLOCK. Removing the local
declaration makes the same name resolve to the outer variable and fails both local-scope
and outer-homonym sentinels.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future BLOCK fixtures must distinguish BLOCK-local construct entities from host or
enclosing entities with non-vacuous initial values, keep declaration owners separate
from execution effects, and must not infer processor-dependent specification-expression
evaluation order, storage duration, addresses, or SAVE implementation strategy.
Diagnostics require a single attributed numbered constraint or explicit prohibiting
prose.

S11.1.4-002 block-branch fixture boundaries: only the listed facets are represented by
this generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 8.5.4/8.6.3 ASYNCHRONOUS registered;8.5.20/8.6.17 VOLATILE registered;8.8 IMPORT
registered;14 use association NOT YET REGISTERED;19.4 construct entities and 19.5 host
association NOT YET REGISTERED.

### S11.1.4-003: BLOCK execution evaluates specification expressions in processor-dependent order before executing the block

**Source:** 11.1.4 p3; J3/24-007 PDF p206. **Class:** Effect.

**Definition:** Executing a BLOCK construct evaluates the specification expressions in its specification
part in a processor-dependent order, followed by execution of its block. The order
latitude is a processor permission and cannot be tested by requiring a particular order.

**Diagnostic obligation:** not-required.

**Facets:** `specification-expressions-evaluated`, `processor-dependent-order-permission`, `block-after-specification-evaluation`.

**Oracle:** No side effects, function call counts, impure specification functions, or
ordering-sensitive expressions may be used to test the processor-dependent order.

S11.1.4-003 block-branch fixture family: one run/effect/f2023 program enters a BLOCK
whose specification part declares an automatic array with bound n+1, then the block
immediately consumes SIZE(local_values)==4 and writes the fourth element. Mutating the
bound or body value is conforming and fails the inquiry/value assertions without
observing expression order.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future BLOCK fixtures must distinguish BLOCK-local construct entities from host or
enclosing entities with non-vacuous initial values, keep declaration owners separate
from execution effects, and must not infer processor-dependent specification-expression
evaluation order, storage duration, addresses, or SAVE implementation strategy.
Diagnostics require a single attributed numbered constraint or explicit prohibiting
prose.

S11.1.4-003 block-branch fixture boundaries: only the listed facets are represented by
this generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 10.1.11 specification expressions registered;8.3 automatic data objects registered;16
SIZE/LBOUND/UBOUND NOT YET REGISTERED.

### S11.1.4-004: Branching to END BLOCK is permitted only from within the BLOCK construct

**Source:** 11.1.4 p4; J3/24-007 PDF p206. **Class:** Restriction.

**Definition:** It is permissible to branch to an end-block-stmt only from within its BLOCK construct; a
branch from outside the construct to that END BLOCK is not permitted.

**Diagnostic obligation:** context-dependent.

**Facets:** `internal-end-block-branch-control`, `outside-end-block-branch-rejected`.

**Oracle:** The negative isolates source/target location and keeps label, branch statement, BLOCK
syntax, and construct names valid.

S11.1.4-004 block-branch fixture family: one run/positive-control/f2023 program branches
from inside a BLOCK to a label on that same END BLOCK, skipping a poison assignment and
then checking the after-block value. The paired negative branches from outside to that
END BLOCK label; the label and BLOCK syntax are otherwise the same.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must use independently derived literal values, inquiry results, or
source relationships; must ensure discriminating non-vacuous observations for
association, scope, bounds, and control-flow claims; must not infer statement execution
counts, evaluation order beyond the cited rule, side effects, temporary storage, image
ordering, addresses, descriptor layout, or processor-dependent representations; and must
not rely on compiler agreement, message wording, emitted code, undefined values,
resource exhaustion, or a processor-dependent value.

S11.1.4-004 block-branch fixture boundaries: only the listed facets are represented by
this generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 8 labels registered;11.2 branch statements NOT YET REGISTERED;11.1.2.1 general block
control-flow in this packet.

<!-- END GENERATED 11.1.4 -->
