# Fortran 2023 16.9.95: GET TEAM

The canonical catalogue is `doc/catalogues/get_team_16_9_95.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.95")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, note-unnumbered. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.95 -->

### S16.9.95-001: GET_TEAM LEVEL argument is an ISO_FORTRAN_ENV team selector

**Source:** S16.9.95, J3/24-007, 18 December 2023, physical PDF415. **Class:** Restriction.

**Definition:** LEVEL, if present, shall be a scalar integer whose value is INITIAL_TEAM, PARENT_TEAM,
or CURRENT_TEAM from ISO_FORTRAN_ENV.

**Diagnostic obligation:** not-required.

**Facets:** `GET_TEAM-LEVEL-scalar-integer`, `GET_TEAM-LEVEL-named-constant-value`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree. GET_TEAM
result-value facets involving current, parent, or initial teams are OUT OF SCOPE for
this suite's single-image fixtures by user decision because they require image/team
semantics.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.95-002: GET_TEAM result is scalar TEAM_TYPE

**Source:** S16.9.95, J3/24-007, 18 December 2023, physical PDF415. **Class:** Effect.

**Definition:** The GET_TEAM result is a scalar of type TEAM_TYPE from ISO_FORTRAN_ENV.

**Diagnostic obligation:** not-required.

**Facets:** `GET_TEAM-result-scalar`, `GET_TEAM-result-team-type`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree. GET_TEAM
result-value facets involving current, parent, or initial teams are OUT OF SCOPE for
this suite's single-image fixtures by user decision because they require image/team
semantics.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.95-003: GET_TEAM identifies the current team for default, CURRENT_TEAM, and initial-team cases

**Source:** S16.9.95, J3/24-007, 18 December 2023, physical PDF415. **Class:** Effect.

**Definition:** The result identifies the current team with absent LEVEL, LEVEL equal to CURRENT_TEAM,
or current team equal to the initial team.

**Diagnostic obligation:** not-required.

**Facets:** `GET_TEAM-default-current-team`, `GET_TEAM-current-team-level-current-team`, `GET_TEAM-initial-current-team-current-result`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree. GET_TEAM
result-value facets involving current, parent, or initial teams are OUT OF SCOPE for
this suite's single-image fixtures by user decision because they require image/team
semantics.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.95-004: GET_TEAM identifies the parent team for PARENT_TEAM

**Source:** S16.9.95, J3/24-007, 18 December 2023, physical PDF415. **Class:** Effect.

**Definition:** The result identifies the parent team with LEVEL equal to PARENT_TEAM outside the
initial-team case.

**Diagnostic obligation:** not-required.

**Facets:** `GET_TEAM-parent-team-level-parent-team`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree. GET_TEAM
result-value facets involving current, parent, or initial teams are OUT OF SCOPE for
this suite's single-image fixtures by user decision because they require image/team
semantics.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

### S16.9.95-005: GET_TEAM identifies the initial team for INITIAL_TEAM

**Source:** S16.9.95, J3/24-007, 18 December 2023, physical PDF415. **Class:** Effect.

**Definition:** The result identifies the initial team with LEVEL equal to INITIAL_TEAM.

**Diagnostic obligation:** not-required.

**Facets:** `GET_TEAM-initial-team-level-initial-team`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, kind,
shape, status-class, and bit-pattern observations. Every positive plan establishes a
non-default sentinel state first, uses a feature mutation such as changing the intrinsic
name, argument presence, argument order, KIND, LEVEL, or input character, and avoids
exact comparison for processor-dependent command spelling, environment support, team
identity internals, explanatory messages, and approximation latitude. Unnumbered Clause
16 argument shall-restrictions are source-control restrictions only and are not promoted
to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent command-line spelling, environment case handling, trailing blank
significance, cross-image environment state, TEAM_TYPE representation, HYPOT
approximation details, ERRMSG text, and processor-specific status numbers beyond
standard sign or named values are not asserted. Grep of tests/ and
tests/fixtures/*/fixture.json for GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, and IACHAR found non-binding uses of
HUGE in clause 7/enumeration fixtures and IACHAR in clause 6/7/10 character fixtures,
and no fixture.json binding for these intrinsic names in this worktree. GET_TEAM
result-value facets involving current, parent, or initial teams are OUT OF SCOPE for
this suite's single-image fixtures by user decision because they require image/team
semantics.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact observation helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions, clause 16.5 status and ISO_FORTRAN_ENV
named constants, sibling 16.9 intrinsic procedures named by result formulas, clause 14
images and teams for image-dependent text, clause 17 IEEE support, clause 18 deleted
features, and clause 19 scope and association terminology.

<!-- END GENERATED 16.9.95 -->
