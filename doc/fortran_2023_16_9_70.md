# Fortran 2023 16.9.70: DBLE

The canonical catalogue is `doc/catalogues/dble_16_9_70.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or dense source units are split.

<!-- BEGIN GENERATED 16.9.70 -->

### S16.9.70-001: DBLE converts to double precision real

**Source:** S16.9.70, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** DBLE converts its argument to double precision real.

**Diagnostic obligation:** not-required.

**Facets:** `dble-conversion-description`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.70-002: DBLE is elemental

**Source:** S16.9.70, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** DBLE is classified as an elemental function.

**Diagnostic obligation:** not-required.

**Facets:** `dble-elemental-class`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.70-003: DBLE argument is integer, real, complex, or BOZ

**Source:** S16.9.70, J3/24-007, 18 December 2023, physical PDF403. **Class:** Restriction.

**Definition:** The argument A shall be of type integer, real, complex, or a boz-literal-constant.

**Diagnostic obligation:** not-required.

**Facets:** `dble-a-integer-real-complex-or-boz`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.70-004: DBLE result is double precision real

**Source:** S16.9.70, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** The DBLE result is double precision real.

**Diagnostic obligation:** not-required.

**Facets:** `dble-result-double-precision-real`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.70-005: DBLE result value is REAL(A,KIND(0.0D0))

**Source:** S16.9.70, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** DBLE(A) has the value REAL(A, KIND(0.0D0)).

**Diagnostic obligation:** not-required.

**Facets:** `dble-integer-real-conversion-value`, `dble-real-real-conversion-value`, `dble-complex-real-part-conversion-value`, `dble-boz-real-conversion-delegates-to-real`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. For BOZ, the portable oracle is relative: DBLE(b) and
REAL(b,KIND(0.0D0)) must have the same representation for the same actual BOZ literal b;
the absolute numeric interpretation of b is not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

<!-- END GENERATED 16.9.70 -->
