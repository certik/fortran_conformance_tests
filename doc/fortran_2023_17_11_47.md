# Fortran 2023 17.11.47: IEEE_SIGNALING_GT

The canonical catalogue is `doc/catalogues/ieee_signaling_gt_17_11_47.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.47")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where compound source units are split.

<!-- BEGIN GENERATED 17.11.47 -->

### S17.11.47-001: IEEE_SIGNALING_GT real arguments have matching kind

**Source:** S17.11.47, J3/24-007, 18 December 2023, physical PDF506. **Class:** Restriction.

**Definition:** A shall be real, and B shall have the same real kind type parameter as A for
IEEE_SIGNALING_GT.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SIGNALING_GT-A-real`, `IEEE_SIGNALING_GT-B-same-real-kind`.

**Oracle:** Source registration only. Future Clause 17 fixtures use branch plans with concrete
support inquiries: each restricted IEEE arithmetic call lists IEEE_SUPPORT_DATATYPE for
every actual operand, NaN construction lists IEEE_SUPPORT_DATATYPE(1.0) and
IEEE_SUPPORT_NAN(1.0), NaN observation lists IEEE_SUPPORT_DATATYPE(QNAN) and
IEEE_SUPPORT_DATATYPE(SNAN) for IEEE_CLASS and IEEE_IS_NAN, and each flag assertion
lists IEEE_SUPPORT_FLAG(flag,x). Flag rows reference named shared preamble
FLAG_ROW_SETUP: IEEE_SUPPORT_FLAG true, IEEE_SUPPORT_HALTING true ->
IEEE_SET_HALTING_MODE(flag,.FALSE.), IEEE_SUPPORT_HALTING false ->
IEEE_GET_HALTING_MODE(flag,h) and continue only for h .eqv. .FALSE., then clear flags.
The supported branch asserts only exact logical results, default-logical
characteristics, and flag states fixed by the source; the unsupported branch records a
skip and never a pass.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing binding for
IEEE_SIGNALING_GT, IEEE_SIGNALING_LE, IEEE_SIGNALING_LT, IEEE_SIGNALING_NE,
IEEE_SIGNBIT, IEEE_SUPPORT_DATATYPE, or S17.11.47 through S17.11.52. Unnumbered argument
and invocation restrictions are source-control restrictions only and are not promoted to
diagnostic facets. For signaling-comparison NaN rows the source requires IEEE_INVALID to
signal; it does not separately require the other four flags to remain quiet for NaN
operands. No set/restore round-trip facet occurs in 17.11.47 through 17.11.52;
SET_HALTING_MODE(flag,.FALSE.) is safety setup for exception-row observation, not
claimed as a round-trip result.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
logical-result, array, execution, and intrinsic-module foundations from clauses 6
through 16 where already catalogued. Unregistered dependencies retained: Clause 17
IEEE_ARITHMETIC and IEEE_EXCEPTIONS module availability, 17.9 IEEE support terminology,
sibling IEEE_VALUE, IEEE_SUPPORT_NAN, IEEE_SUPPORT_FLAG, IEEE_GET_FLAG, IEEE_SET_FLAG,
IEEE_IS_NEGATIVE, and named IEEE constants used by pending plans, plus clauses 18 and 19
where future fixtures need interoperability or scope terms.

### S17.11.47-002: IEEE_SIGNALING_GT invocation requires IEEE datatype support

**Source:** S17.11.47, J3/24-007, 18 December 2023, physical PDF506. **Class:** Restriction.

**Definition:** IEEE_SIGNALING_GT(A,B) shall not be invoked while IEEE_SUPPORT_DATATYPE(A) has the value
false.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SIGNALING_GT-requires-IEEE_SUPPORT_DATATYPE`.

**Oracle:** Source registration only. Future Clause 17 fixtures use branch plans with concrete
support inquiries: each restricted IEEE arithmetic call lists IEEE_SUPPORT_DATATYPE for
every actual operand, NaN construction lists IEEE_SUPPORT_DATATYPE(1.0) and
IEEE_SUPPORT_NAN(1.0), NaN observation lists IEEE_SUPPORT_DATATYPE(QNAN) and
IEEE_SUPPORT_DATATYPE(SNAN) for IEEE_CLASS and IEEE_IS_NAN, and each flag assertion
lists IEEE_SUPPORT_FLAG(flag,x). Flag rows reference named shared preamble
FLAG_ROW_SETUP: IEEE_SUPPORT_FLAG true, IEEE_SUPPORT_HALTING true ->
IEEE_SET_HALTING_MODE(flag,.FALSE.), IEEE_SUPPORT_HALTING false ->
IEEE_GET_HALTING_MODE(flag,h) and continue only for h .eqv. .FALSE., then clear flags.
The supported branch asserts only exact logical results, default-logical
characteristics, and flag states fixed by the source; the unsupported branch records a
skip and never a pass.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing binding for
IEEE_SIGNALING_GT, IEEE_SIGNALING_LE, IEEE_SIGNALING_LT, IEEE_SIGNALING_NE,
IEEE_SIGNBIT, IEEE_SUPPORT_DATATYPE, or S17.11.47 through S17.11.52. Unnumbered argument
and invocation restrictions are source-control restrictions only and are not promoted to
diagnostic facets. For signaling-comparison NaN rows the source requires IEEE_INVALID to
signal; it does not separately require the other four flags to remain quiet for NaN
operands. No set/restore round-trip facet occurs in 17.11.47 through 17.11.52;
SET_HALTING_MODE(flag,.FALSE.) is safety setup for exception-row observation, not
claimed as a round-trip result.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
logical-result, array, execution, and intrinsic-module foundations from clauses 6
through 16 where already catalogued. Unregistered dependencies retained: Clause 17
IEEE_ARITHMETIC and IEEE_EXCEPTIONS module availability, 17.9 IEEE support terminology,
sibling IEEE_VALUE, IEEE_SUPPORT_NAN, IEEE_SUPPORT_FLAG, IEEE_GET_FLAG, IEEE_SET_FLAG,
IEEE_IS_NEGATIVE, and named IEEE constants used by pending plans, plus clauses 18 and 19
where future fixtures need interoperability or scope terms.

### S17.11.47-003: IEEE_SIGNALING_GT returns default logical

**Source:** S17.11.47, J3/24-007, 18 December 2023, physical PDF506. **Class:** Effect.

**Definition:** The IEEE_SIGNALING_GT result has default logical kind for scalar real arguments.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SIGNALING_GT-result-default-logical`.

**Oracle:** Source registration only. Future Clause 17 fixtures use branch plans with concrete
support inquiries: each restricted IEEE arithmetic call lists IEEE_SUPPORT_DATATYPE for
every actual operand, NaN construction lists IEEE_SUPPORT_DATATYPE(1.0) and
IEEE_SUPPORT_NAN(1.0), NaN observation lists IEEE_SUPPORT_DATATYPE(QNAN) and
IEEE_SUPPORT_DATATYPE(SNAN) for IEEE_CLASS and IEEE_IS_NAN, and each flag assertion
lists IEEE_SUPPORT_FLAG(flag,x). Flag rows reference named shared preamble
FLAG_ROW_SETUP: IEEE_SUPPORT_FLAG true, IEEE_SUPPORT_HALTING true ->
IEEE_SET_HALTING_MODE(flag,.FALSE.), IEEE_SUPPORT_HALTING false ->
IEEE_GET_HALTING_MODE(flag,h) and continue only for h .eqv. .FALSE., then clear flags.
The supported branch asserts only exact logical results, default-logical
characteristics, and flag states fixed by the source; the unsupported branch records a
skip and never a pass.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing binding for
IEEE_SIGNALING_GT, IEEE_SIGNALING_LE, IEEE_SIGNALING_LT, IEEE_SIGNALING_NE,
IEEE_SIGNBIT, IEEE_SUPPORT_DATATYPE, or S17.11.47 through S17.11.52. Unnumbered argument
and invocation restrictions are source-control restrictions only and are not promoted to
diagnostic facets. For signaling-comparison NaN rows the source requires IEEE_INVALID to
signal; it does not separately require the other four flags to remain quiet for NaN
operands. No set/restore round-trip facet occurs in 17.11.47 through 17.11.52;
SET_HALTING_MODE(flag,.FALSE.) is safety setup for exception-row observation, not
claimed as a round-trip result.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
logical-result, array, execution, and intrinsic-module foundations from clauses 6
through 16 where already catalogued. Unregistered dependencies retained: Clause 17
IEEE_ARITHMETIC and IEEE_EXCEPTIONS module availability, 17.9 IEEE support terminology,
sibling IEEE_VALUE, IEEE_SUPPORT_NAN, IEEE_SUPPORT_FLAG, IEEE_GET_FLAG, IEEE_SET_FLAG,
IEEE_IS_NEGATIVE, and named IEEE constants used by pending plans, plus clauses 18 and 19
where future fixtures need interoperability or scope terms.

### S17.11.47-004: IEEE_SIGNALING_GT implements compareSignalingGreater for finite operands

**Source:** S17.11.47, J3/24-007, 18 December 2023, physical PDF506. **Class:** Effect.

**Definition:** For non-NaN operands, the result is true exactly for operands that compare greater than.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SIGNALING_GT-finite-greater-truth-table`, `IEEE_SIGNALING_GT-signed-zero-truth-table`, `IEEE_SIGNALING_GT-infinity-truth-table`.

**Oracle:** Source registration only. Future Clause 17 fixtures use branch plans with concrete
support inquiries: each restricted IEEE arithmetic call lists IEEE_SUPPORT_DATATYPE for
every actual operand, NaN construction lists IEEE_SUPPORT_DATATYPE(1.0) and
IEEE_SUPPORT_NAN(1.0), NaN observation lists IEEE_SUPPORT_DATATYPE(QNAN) and
IEEE_SUPPORT_DATATYPE(SNAN) for IEEE_CLASS and IEEE_IS_NAN, and each flag assertion
lists IEEE_SUPPORT_FLAG(flag,x). Flag rows reference named shared preamble
FLAG_ROW_SETUP: IEEE_SUPPORT_FLAG true, IEEE_SUPPORT_HALTING true ->
IEEE_SET_HALTING_MODE(flag,.FALSE.), IEEE_SUPPORT_HALTING false ->
IEEE_GET_HALTING_MODE(flag,h) and continue only for h .eqv. .FALSE., then clear flags.
The supported branch asserts only exact logical results, default-logical
characteristics, and flag states fixed by the source; the unsupported branch records a
skip and never a pass.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing binding for
IEEE_SIGNALING_GT, IEEE_SIGNALING_LE, IEEE_SIGNALING_LT, IEEE_SIGNALING_NE,
IEEE_SIGNBIT, IEEE_SUPPORT_DATATYPE, or S17.11.47 through S17.11.52. Unnumbered argument
and invocation restrictions are source-control restrictions only and are not promoted to
diagnostic facets. For signaling-comparison NaN rows the source requires IEEE_INVALID to
signal; it does not separately require the other four flags to remain quiet for NaN
operands. No set/restore round-trip facet occurs in 17.11.47 through 17.11.52;
SET_HALTING_MODE(flag,.FALSE.) is safety setup for exception-row observation, not
claimed as a round-trip result.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
logical-result, array, execution, and intrinsic-module foundations from clauses 6
through 16 where already catalogued. Unregistered dependencies retained: Clause 17
IEEE_ARITHMETIC and IEEE_EXCEPTIONS module availability, 17.9 IEEE support terminology,
sibling IEEE_VALUE, IEEE_SUPPORT_NAN, IEEE_SUPPORT_FLAG, IEEE_GET_FLAG, IEEE_SET_FLAG,
IEEE_IS_NEGATIVE, and named IEEE constants used by pending plans, plus clauses 18 and 19
where future fixtures need interoperability or scope terms.

### S17.11.47-005: IEEE_SIGNALING_GT returns .false. for NaN operands

**Source:** S17.11.47, J3/24-007, 18 December 2023, physical PDF506. **Class:** Effect.

**Definition:** For a NaN operand, the IEEE_SIGNALING_GT result is .false..

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SIGNALING_GT-nan-operand-result-table`.

**Oracle:** Source registration only. Future Clause 17 fixtures use branch plans with concrete
support inquiries: each restricted IEEE arithmetic call lists IEEE_SUPPORT_DATATYPE for
every actual operand, NaN construction lists IEEE_SUPPORT_DATATYPE(1.0) and
IEEE_SUPPORT_NAN(1.0), NaN observation lists IEEE_SUPPORT_DATATYPE(QNAN) and
IEEE_SUPPORT_DATATYPE(SNAN) for IEEE_CLASS and IEEE_IS_NAN, and each flag assertion
lists IEEE_SUPPORT_FLAG(flag,x). Flag rows reference named shared preamble
FLAG_ROW_SETUP: IEEE_SUPPORT_FLAG true, IEEE_SUPPORT_HALTING true ->
IEEE_SET_HALTING_MODE(flag,.FALSE.), IEEE_SUPPORT_HALTING false ->
IEEE_GET_HALTING_MODE(flag,h) and continue only for h .eqv. .FALSE., then clear flags.
The supported branch asserts only exact logical results, default-logical
characteristics, and flag states fixed by the source; the unsupported branch records a
skip and never a pass.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing binding for
IEEE_SIGNALING_GT, IEEE_SIGNALING_LE, IEEE_SIGNALING_LT, IEEE_SIGNALING_NE,
IEEE_SIGNBIT, IEEE_SUPPORT_DATATYPE, or S17.11.47 through S17.11.52. Unnumbered argument
and invocation restrictions are source-control restrictions only and are not promoted to
diagnostic facets. For signaling-comparison NaN rows the source requires IEEE_INVALID to
signal; it does not separately require the other four flags to remain quiet for NaN
operands. No set/restore round-trip facet occurs in 17.11.47 through 17.11.52;
SET_HALTING_MODE(flag,.FALSE.) is safety setup for exception-row observation, not
claimed as a round-trip result.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
logical-result, array, execution, and intrinsic-module foundations from clauses 6
through 16 where already catalogued. Unregistered dependencies retained: Clause 17
IEEE_ARITHMETIC and IEEE_EXCEPTIONS module availability, 17.9 IEEE support terminology,
sibling IEEE_VALUE, IEEE_SUPPORT_NAN, IEEE_SUPPORT_FLAG, IEEE_GET_FLAG, IEEE_SET_FLAG,
IEEE_IS_NEGATIVE, and named IEEE constants used by pending plans, plus clauses 18 and 19
where future fixtures need interoperability or scope terms.

### S17.11.47-006: IEEE_SIGNALING_GT signals IEEE_INVALID for NaN operands

**Source:** S17.11.47, J3/24-007, 18 December 2023, physical PDF506. **Class:** Effect.

**Definition:** A NaN operand to IEEE_SIGNALING_GT signals IEEE_INVALID.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SIGNALING_GT-nan-operand-invalid-flag-table`, `IEEE_SIGNALING_GT-nan-operand-other-flags-quiet`.

**Oracle:** Source registration only. Future Clause 17 fixtures use branch plans with concrete
support inquiries: each restricted IEEE arithmetic call lists IEEE_SUPPORT_DATATYPE for
every actual operand, NaN construction lists IEEE_SUPPORT_DATATYPE(1.0) and
IEEE_SUPPORT_NAN(1.0), NaN observation lists IEEE_SUPPORT_DATATYPE(QNAN) and
IEEE_SUPPORT_DATATYPE(SNAN) for IEEE_CLASS and IEEE_IS_NAN, and each flag assertion
lists IEEE_SUPPORT_FLAG(flag,x). Flag rows reference named shared preamble
FLAG_ROW_SETUP: IEEE_SUPPORT_FLAG true, IEEE_SUPPORT_HALTING true ->
IEEE_SET_HALTING_MODE(flag,.FALSE.), IEEE_SUPPORT_HALTING false ->
IEEE_GET_HALTING_MODE(flag,h) and continue only for h .eqv. .FALSE., then clear flags.
The supported branch asserts only exact logical results, default-logical
characteristics, and flag states fixed by the source; the unsupported branch records a
skip and never a pass.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing binding for
IEEE_SIGNALING_GT, IEEE_SIGNALING_LE, IEEE_SIGNALING_LT, IEEE_SIGNALING_NE,
IEEE_SIGNBIT, IEEE_SUPPORT_DATATYPE, or S17.11.47 through S17.11.52. Unnumbered argument
and invocation restrictions are source-control restrictions only and are not promoted to
diagnostic facets. For signaling-comparison NaN rows the source requires IEEE_INVALID to
signal; it does not separately require the other four flags to remain quiet for NaN
operands. No set/restore round-trip facet occurs in 17.11.47 through 17.11.52;
SET_HALTING_MODE(flag,.FALSE.) is safety setup for exception-row observation, not
claimed as a round-trip result.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
logical-result, array, execution, and intrinsic-module foundations from clauses 6
through 16 where already catalogued. Unregistered dependencies retained: Clause 17
IEEE_ARITHMETIC and IEEE_EXCEPTIONS module availability, 17.9 IEEE support terminology,
sibling IEEE_VALUE, IEEE_SUPPORT_NAN, IEEE_SUPPORT_FLAG, IEEE_GET_FLAG, IEEE_SET_FLAG,
IEEE_IS_NEGATIVE, and named IEEE constants used by pending plans, plus clauses 18 and 19
where future fixtures need interoperability or scope terms.

### S17.11.47-007: IEEE_SIGNALING_GT signals no exception for non-NaN operands

**Source:** S17.11.47, J3/24-007, 18 December 2023, physical PDF506. **Class:** Effect.

**Definition:** For finite, signed-zero, and infinity non-NaN operands to IEEE_SIGNALING_GT, no IEEE
exception is signaled.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_SIGNALING_GT-finite-operands-quiet-flags`, `IEEE_SIGNALING_GT-signed-zero-operands-quiet-flags`, `IEEE_SIGNALING_GT-infinity-operands-quiet-flags`.

**Oracle:** Source registration only. Future Clause 17 fixtures use branch plans with concrete
support inquiries: each restricted IEEE arithmetic call lists IEEE_SUPPORT_DATATYPE for
every actual operand, NaN construction lists IEEE_SUPPORT_DATATYPE(1.0) and
IEEE_SUPPORT_NAN(1.0), NaN observation lists IEEE_SUPPORT_DATATYPE(QNAN) and
IEEE_SUPPORT_DATATYPE(SNAN) for IEEE_CLASS and IEEE_IS_NAN, and each flag assertion
lists IEEE_SUPPORT_FLAG(flag,x). Flag rows reference named shared preamble
FLAG_ROW_SETUP: IEEE_SUPPORT_FLAG true, IEEE_SUPPORT_HALTING true ->
IEEE_SET_HALTING_MODE(flag,.FALSE.), IEEE_SUPPORT_HALTING false ->
IEEE_GET_HALTING_MODE(flag,h) and continue only for h .eqv. .FALSE., then clear flags.
The supported branch asserts only exact logical results, default-logical
characteristics, and flag states fixed by the source; the unsupported branch records a
skip and never a pass.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no existing binding for
IEEE_SIGNALING_GT, IEEE_SIGNALING_LE, IEEE_SIGNALING_LT, IEEE_SIGNALING_NE,
IEEE_SIGNBIT, IEEE_SUPPORT_DATATYPE, or S17.11.47 through S17.11.52. Unnumbered argument
and invocation restrictions are source-control restrictions only and are not promoted to
diagnostic facets. For signaling-comparison NaN rows the source requires IEEE_INVALID to
signal; it does not separately require the other four flags to remain quiet for NaN
operands. No set/restore round-trip facet occurs in 17.11.47 through 17.11.52;
SET_HALTING_MODE(flag,.FALSE.) is safety setup for exception-row observation, not
claimed as a round-trip result.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
logical-result, array, execution, and intrinsic-module foundations from clauses 6
through 16 where already catalogued. Unregistered dependencies retained: Clause 17
IEEE_ARITHMETIC and IEEE_EXCEPTIONS module availability, 17.9 IEEE support terminology,
sibling IEEE_VALUE, IEEE_SUPPORT_NAN, IEEE_SUPPORT_FLAG, IEEE_GET_FLAG, IEEE_SET_FLAG,
IEEE_IS_NEGATIVE, and named IEEE constants used by pending plans, plus clauses 18 and 19
where future fixtures need interoperability or scope terms.

<!-- END GENERATED 17.11.47 -->
