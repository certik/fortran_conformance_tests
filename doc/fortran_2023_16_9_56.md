# Fortran 2023 16.9.56: CO_MIN

The canonical catalogue is `doc/catalogues/co_min_16_9_56.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.56")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Five base units: p1-p5 on physical PDF396; p3 is split between restrictions, computed minimum effects, and RESULT_IMAGE assignment.

<!-- BEGIN GENERATED 16.9.56 -->

### S16.9.56-001: CO_MIN argument restrictions and intents

**Source:** S16.9.56, J3/24-007, 18 December 2023, physical PDF396. **Class:** Restriction.

**Definition:** A is integer, real, or character with corresponding shape, type, and type parameter
values and is not coindexed; RESULT_IMAGE, STAT, and ERRMSG have the stated scalar
forms, intents, and corresponding-reference restrictions.

**Diagnostic obligation:** not-required.

**Facets:** `A-integer-real-or-character`, `A-corresponding-same-shape-type-parameters`, `A-not-coindexed`, `A-intent-inout`, `RESULT_IMAGE-current-team-scalar-same-if-present`, `STAT-noncoindexed-integer-range-at-least-four`, `ERRMSG-noncoindexed-default-character-scalar`.

**Oracle:** Plans are pending and source-derived. Argument restrictions are recorded as
source-control obligations only; unnumbered argument restrictions carry no diagnostic
facet. Runtime behavior of CO_* collective intrinsics is image-dependent and OUT OF
SCOPE for this suite's single-image fixtures by user decision, so this packet
deliberately claims no single-image local-coarray execution plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. CO_* collective runtime effects, RESULT_IMAGE selection effects,
cross-image computed values, stopped/failed-image status behavior, STAT values, and
ERRMSG text are OUT OF SCOPE for this suite's single-image fixtures by user decision; no
local singleton execution is used as an oracle.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.56-002: CO_MIN computes scalar and elementwise minimum values

**Source:** S16.9.56, J3/24-007, 18 December 2023, physical PDF396. **Class:** Effect.

**Definition:** For scalar A the computed value equals the minimum of A in all corresponding references;
for array A each element equals the minimum of corresponding elements in all
corresponding references.

**Diagnostic obligation:** not-required.

**Facets:** `single-image-scalar-minimum-identity`, `single-image-array-minimum-identity`, `multi-image-minimum-out-of-scope`.

**Oracle:** Plans are pending and source-derived. Argument restrictions are recorded as
source-control obligations only; unnumbered argument restrictions carry no diagnostic
facet. Runtime behavior of CO_* collective intrinsics is image-dependent and OUT OF
SCOPE for this suite's single-image fixtures by user decision, so this packet
deliberately claims no single-image local-coarray execution plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. CO_* collective runtime effects, RESULT_IMAGE selection effects,
cross-image computed values, stopped/failed-image status behavior, STAT values, and
ERRMSG text are OUT OF SCOPE for this suite's single-image fixtures by user decision; no
local singleton execution is used as an oracle.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.56-003: CO_MIN assigns the computed value on selected images and otherwise leaves A undefined

**Source:** S16.9.56, J3/24-007, 18 December 2023, physical PDF396. **Class:** Effect.

**Definition:** The computed value is assigned to A if no error occurs and RESULT_IMAGE is absent or
identifies the executing image; otherwise A becomes undefined.

**Diagnostic obligation:** not-required.

**Facets:** `assigned-when-RESULT_IMAGE-absent`, `assigned-on-selected-RESULT_IMAGE`, `non-result-image-A-undefined-out-of-scope`.

**Oracle:** Plans are pending and source-derived. Argument restrictions are recorded as
source-control obligations only; unnumbered argument restrictions carry no diagnostic
facet. Runtime behavior of CO_* collective intrinsics is image-dependent and OUT OF
SCOPE for this suite's single-image fixtures by user decision, so this packet
deliberately claims no single-image local-coarray execution plan.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. CO_* collective runtime effects, RESULT_IMAGE selection effects,
cross-image computed values, stopped/failed-image status behavior, STAT values, and
ERRMSG text are OUT OF SCOPE for this suite's single-image fixtures by user decision; no
local singleton execution is used as an oracle.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

<!-- END GENERATED 16.9.56 -->
