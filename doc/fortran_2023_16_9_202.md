# Fortran 2023 16.9.202: SYSTEM_CLOCK

The canonical catalogue is `doc/catalogues/system_clock_16_9_202.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.202")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7, p8, p9. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.202 -->

### S16.9.202-001: SYSTEM_CLOCK argument restrictions

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Restriction.

**Definition:** COUNT, COUNT_RATE, and COUNT_MAX have the stated scalar type, kind/range, and
INTENT(OUT) restrictions, and all integer arguments in one reference shall have the same
kind type parameter.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-COUNT-integer-scalar-range`, `SYSTEM_CLOCK-COUNT_RATE-integer-or-real-scalar`, `SYSTEM_CLOCK-COUNT_MAX-integer-scalar-range`, `SYSTEM_CLOCK-arguments-intent-out`, `SYSTEM_CLOCK-all-integer-arguments-same-kind`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-002: SYSTEM_CLOCK COUNT assignment range and clock absence value

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** COUNT is assigned either -HUGE(COUNT) with no clock or a clock value in the range 0
through COUNT_MAX.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-COUNT-assigned`, `SYSTEM_CLOCK-COUNT-no-clock-negative-huge`, `SYSTEM_CLOCK-COUNT-clock-range`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-003: SYSTEM_CLOCK COUNT clock value is processor dependent

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** With a clock present, COUNT is a processor-dependent value based on a processor clock.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-COUNT-processor-dependent-value`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-004: SYSTEM_CLOCK COUNT progression and reset have no finite portable oracle

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** The processor-dependent COUNT value increments by one for each clock count until
COUNT_MAX and resets to zero at the next count.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-COUNT-increments-by-one`, `SYSTEM_CLOCK-COUNT-resets-after-COUNT_MAX`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-005: SYSTEM_CLOCK COUNT_RATE assignment and no-clock zero

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** COUNT_RATE is assigned a clock count rate or zero with no clock.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-COUNT_RATE-assigned`, `SYSTEM_CLOCK-COUNT_RATE-no-clock-zero`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-006: SYSTEM_CLOCK COUNT_RATE approximation latitude

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** With a clock present, COUNT_RATE is a processor-dependent approximation to processor
clock counts per second.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-COUNT_RATE-approximation-latitude`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-007: SYSTEM_CLOCK COUNT_MAX assignment

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** COUNT_MAX is assigned the maximum COUNT value, or zero with no clock.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-COUNT_MAX-assigned`, `SYSTEM_CLOCK-COUNT_MAX-no-clock-zero`, `SYSTEM_CLOCK-COUNT_MAX-bounds-COUNT`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-008: SYSTEM_CLOCK clock availability and sharing are processor dependent

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** Whether an image has no clock, has one or more clocks of its own, or shares a clock with
another image is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-clock-availability-processor-dependent`, `SYSTEM_CLOCK-clock-sharing-processor-dependent`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-009: SYSTEM_CLOCK argument characteristics select among multiple clocks

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** With more than one available clock, the types and kinds of SYSTEM_CLOCK arguments
determine the accessed clock.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-clock-selection-by-argument-characteristics`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-010: SYSTEM_CLOCK clock-selection documentation recommendation

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** The processor should document the relationship between clock selection and argument
characteristics.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-clock-selection-documentation-recommended`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-011: SYSTEM_CLOCK timing calculations should use consistent argument types and kinds

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** Different invocations of SYSTEM_CLOCK should use the same argument types and kinds so
timing calculations use the same clock.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-consistent-argument-characteristics-recommended`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.202-012: SYSTEM_CLOCK large integer argument range recommendation

**Source:** S16.9.202, J3/24-007, 18 December 2023, physical PDF463-464. **Class:** Effect.

**Definition:** References to SYSTEM_CLOCK are recommended to use integer arguments with decimal
exponent range at least 18.

**Diagnostic obligation:** not-required.

**Facets:** `SYSTEM_CLOCK-integer-range-at-least-18-recommended`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. SYSTEM_CLOCK clock availability, exact count values, clock
sharing, clock selection, progression, reset timing, and COUNT_RATE approximations are
processor-dependent; portable plans assert only assignment effects, kind restrictions,
and bounded disjunctions.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

<!-- END GENERATED 16.9.202 -->
