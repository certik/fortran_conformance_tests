# Fortran 2023 16.9.175: RESHAPE

The canonical catalogue is `doc/catalogues/reshape_intrinsic_16_9_175.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.175")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.175 -->

### S16.9.175-001: RESHAPE argument restrictions

**Source:** S16.9.175, J3/24-007, 18 December 2023, physical PDF451. **Class:** Restriction.

**Definition:** SOURCE shall be an array, source size shall be sufficient with PAD absent or size zero,
SHAPE shall be a rank-one integer array of positive size less than 16 with no negative
element, PAD shall match SOURCE type and type parameters, and ORDER shall be an integer
array with SHAPE's shape and a permutation of 1 through n.

**Diagnostic obligation:** not-required.

**Facets:** `RESHAPE-SOURCE-array`, `RESHAPE-SOURCE-size-sufficient-without-PAD`, `RESHAPE-SHAPE-rank-one-integer-positive-size-less-than-16`, `RESHAPE-SHAPE-elements-nonnegative`, `RESHAPE-PAD-same-type-parameters`, `RESHAPE-ORDER-integer-same-shape-permutation`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. The 16.9.175 p6 example paragraph was visually verified because its
typeset matrices have known census hash drift; the registry census hash is retained and
the paragraph is informative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.175-002: RESHAPE absent ORDER defaults to identity order

**Source:** S16.9.175, J3/24-007, 18 December 2023, physical PDF451. **Class:** Effect.

**Definition:** ORDER absence is equivalent to ORDER value (1,2,...,n).

**Diagnostic obligation:** not-required.

**Facets:** `RESHAPE-ORDER-absent-identity-default`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. The 16.9.175 p6 example paragraph was visually verified because its
typeset matrices have known census hash drift; the registry census hash is retained and
the paragraph is informative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.175-003: RESHAPE result characteristics

**Source:** S16.9.175, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** The RESHAPE result has shape SHAPE and the same type and type parameters as SOURCE.

**Diagnostic obligation:** not-required.

**Facets:** `RESHAPE-result-shape-is-SHAPE`, `RESHAPE-result-type-parameters-match-SOURCE`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. The 16.9.175 p6 example paragraph was visually verified because its
typeset matrices have known census hash drift; the registry census hash is retained and
the paragraph is informative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.175-004: RESHAPE consumes SOURCE elements in array element order

**Source:** S16.9.175, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** Result elements in the selected subscript order begin with SOURCE elements in normal
array element order.

**Diagnostic obligation:** not-required.

**Facets:** `RESHAPE-source-elements-array-element-order`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. The 16.9.175 p6 example paragraph was visually verified because its
typeset matrices have known census hash drift; the registry census hash is retained and
the paragraph is informative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.175-005: RESHAPE pads by cycling PAD elements

**Source:** S16.9.175, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** After SOURCE elements are exhausted, RESHAPE uses PAD elements in array element order
and repeats additional copies of PAD as necessary.

**Diagnostic obligation:** not-required.

**Facets:** `RESHAPE-uses-PAD-after-SOURCE`, `RESHAPE-repeats-PAD-as-needed`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. The 16.9.175 p6 example paragraph was visually verified because its
typeset matrices have known census hash drift; the registry census hash is retained and
the paragraph is informative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.175-006: RESHAPE fills in permuted subscript order

**Source:** S16.9.175, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** The result elements are assigned in permuted subscript order ORDER(1),...,ORDER(n).

**Diagnostic obligation:** not-required.

**Facets:** `RESHAPE-ORDER-controls-subscript-fill-order`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. The 16.9.175 p6 example paragraph was visually verified because its
typeset matrices have known census hash drift; the registry census hash is retained and
the paragraph is informative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

<!-- END GENERATED 16.9.175 -->
