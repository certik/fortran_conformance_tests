# Fortran 2023 12.5.2: Connection modes - I/O units and connection fixtures

The canonical catalogue is `doc/catalogues/connection_modes_12_5_2.json`. This author packet adds finite runtime and diagnostic fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 7 facets in 5 program(s); 7 of 9 facets are represented and 2 remain pending.

<!-- BEGIN GENERATED 12.5.2 -->

### S12.5.2-001: Formatted connections have the listed changeable modes

**Source:** S12.5.2, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** A connection for formatted input/output has the listed changeable modes: blank
interpretation, delimiter, sign, leading zero, decimal edit, input/output rounding, pad,
and scale factor.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-connection-listed-changeable-modes`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.2-002: Unformatted connections have no changeable modes

**Source:** S12.5.2, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** A connection for unformatted input/output has no changeable modes.

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-connection-no-changeable-modes`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.2-003: Connection mode values are established when the connection is initiated

**Source:** S12.5.2, J3/24-007, 18 December 2023, physical PDF249. **Class:** Effect.

**Definition:** Mode values are established when the connection is initiated: by explicit or implicit
OPEN specifiers for an OPEN-initiated connection, or by the values implied by an initial
OPEN without corresponding keywords for internal or preconnected files.

**Diagnostic obligation:** not-required.

**Facets:** `open-established-mode-values`, `internal-preconnected-initial-mode-values`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 2 valid run-phase program(s) and 0
diagnostic program(s) cover `internal-preconnected-initial-mode-values`,
`open-established-mode-values`. Runtime oracles use scratch or run-directory files,
controlled stdin/stdout/stderr, INQUIRE properties, exact integer/character/logical
values, and IOSTAT zero only. No processor-dependent ordinary unit number, physical
device, IOMSG text, or numeric nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.2-004: The scale factor is not specified in OPEN and is implicitly zero

**Source:** S12.5.2, J3/24-007, 18 December 2023, physical PDF250. **Class:** Effect.

**Definition:** The scale factor cannot be explicitly specified in an OPEN statement and is implicitly
zero.

**Diagnostic obligation:** not-required.

**Facets:** `open-scale-factor-implicit-zero`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `open-scale-factor-implicit-zero`. Runtime oracles use
scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE properties,
exact integer/character/logical values, and IOSTAT zero only. No processor-dependent
ordinary unit number, physical device, IOMSG text, or numeric nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.2-005: A subsequent OPEN can change external-file connection modes

**Source:** S12.5.2, J3/24-007, 18 December 2023, physical PDF250. **Class:** Effect.

**Definition:** The modes of a connection to an external file can be changed by a subsequent OPEN
statement that modifies the connection.

**Diagnostic obligation:** not-required.

**Facets:** `subsequent-open-can-change-external-modes`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `subsequent-open-can-change-external-modes`. Runtime oracles
use scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE properties,
exact integer/character/logical values, and IOSTAT zero only. No processor-dependent
ordinary unit number, physical device, IOMSG text, or numeric nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.5.2-006: Data transfer specifiers and edit descriptors can temporarily change modes and reset afterward

**Source:** S12.5.2, J3/24-007, 18 December 2023, physical PDF250. **Class:** Effect.

**Definition:** A connection mode can be temporarily changed by a corresponding data transfer keyword or
edit descriptor. Keyword specifiers take effect at statement execution, edit descriptors
when encountered in format processing, and modes reset to their prior values when the
data transfer statement terminates.

**Diagnostic obligation:** not-required.

**Facets:** `data-transfer-keyword-temporary-mode`, `edit-descriptor-temporary-mode`, `mode-reset-after-data-transfer`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

I/O units-connection fixture implementation: 1 valid run-phase program(s) and 0
diagnostic program(s) cover `data-transfer-keyword-temporary-mode`,
`edit-descriptor-temporary-mode`, `mode-reset-after-data-transfer`. Runtime oracles use
scratch or run-directory files, controlled stdin/stdout/stderr, INQUIRE properties,
exact integer/character/logical values, and IOSTAT zero only. No processor-dependent
ordinary unit number, physical device, IOMSG text, or numeric nonzero status is
asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.5.2 -->

## I/O units and connection fixture derivations

### `open_and_subsequent_blank_modes` / `S12.5.2-003`

**Facets:** `open-established-mode-values`.

12.5.2 p2 and p4 establish BLANK='NULL' at OPEN and then change it by a subsequent OPEN; 13.8.7 makes '1 2' read as 12 vs 102.

Feature mutations: `initial-open-blank-zero`, `subsequent-open-blank-null`, `subsequent-inquire-before-open`.

### `implicit_zero_scale_factor` / `S12.5.2-004`

**Facets:** `open-scale-factor-implicit-zero`.

12.5.2 p2-p3 gives internal files the initial OPEN-implied modes and makes scale factor implicitly zero; exact F5.1 output of whole-number reals observes zero scale.

Feature mutations: `external-scale-factor-one`, `internal-scale-factor-one`.

### `temporary_blank_modes` / `S12.5.2-006`

**Facets:** `data-transfer-keyword-temporary-mode`, `edit-descriptor-temporary-mode`, `mode-reset-after-data-transfer`.

12.5.2 p5 says data-transfer keywords and edit descriptors temporarily change modes and then reset; BLANK ZERO/NULL effects distinguish 102 from 12.

Feature mutations: `keyword-blank-null`, `reset-read-keeps-zero`, `edit-bz-changed-to-bn`, `edit-bn-changed-to-bz`.

### `internal_initial_zero_scale` / `S12.5.2-003`

**Facets:** `internal-preconnected-initial-mode-values`.

12.5.2 p2 gives an internal file the modes implied by an initial OPEN without keywords; p3 makes its scale factor zero, observed by exact F5.1 output.

Feature mutations: `internal-initial-scale-one`.

### `subsequent_open_blank_change` / `S12.5.2-005`

**Facets:** `subsequent-open-can-change-external-modes`.

12.5.2 p4 permits a subsequent OPEN to change an external connection mode; BLANK changes to ZERO and makes '1 2' read as 102.

Feature mutations: `subsequent-open-keeps-null`, `subsequent-open-after-read`.

