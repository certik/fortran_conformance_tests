# Fortran 2023 8.5.17: RANK clause

Effective source status is `Registry.catalogue_review_state("8.5.17")`,
not a cached claim from the raw review field. Source classification,
fixture approval and compiler observations remain separate.

The canonical catalogue is `doc/catalogues/rank_clause_8_5_17.json`.
The six original units span physical PDF pages132-133 of J3/24-007,
December18,2023,688pages,SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The next heading is8.5.18 TARGET attribute. RANK is a clause, not TARGET
or an independently invented attribute name.

The clause supplies DIMENSION, whose actual8.5.8.1 definition includes
scalar entities. Its scalar INTEGER constant expression can contain a
qualified property inquiry on a variable; unused data need not become a
constant merely because the rank property is constant. The grammar,
nonnegative supported range and C864 role/attribute condition remain
separate. Rank zero does not waive C864.

Positive-rank POINTER/ALLOCATABLE entities are deferred-shape arrays, not
automatically allocated or associated objects. Other positive-rank entities
are ordinary assumed-shape dummies with unit lower bounds. Do not apply
those bounds to the deferred-shape branch or turn an empty-dimension
inquiry convention into proof of hidden declared bounds.

The NOTE is a declaration fragment. Its nonpointer nonallocatable X3 and
X4 need dummy-data-object contexts; pasting them into an ordinary main
program would not make valid controls. The processor-limit endpoint also
needs independent qualification, rather than accepting the result of the
same clause under test as its own oracle.

The source-only packet introduces pending facet plans, not execution
evidence. Existing assumed-rank runtime programs do not declare entities
with a RANK clause and receive no automatic reuse credit. Current facet
states are recorded in the catalogue; this source registration itself
creates no cases, compiler runs, profiles, reviews, baseline changes or
source-use records.

<!-- BEGIN GENERATED 8.5.17 -->

### R829: A RANK clause encloses a scalar integer constant expression

**Source:** 8.5.17 R829, physical PDF132, with the DIMENSION route in p1 and8.2 R802 **Class:** Syntax.

**Definition:** The clause consists of RANK and one parenthesized scalar INTEGER constant expression.
Eligible property inquiries can be constant even when their data-object arguments are
variables. The expression category, nonnegative supported value and declared-entity role
remain separate conditions.

**Diagnostic obligation:** required.

**Facets:** `literal-form`, `named-constant-form`, `specification-inquiry-form`, `expression-category-source-relations`, `parenthesis-grammar-exclusion`.

**Oracle:** Finite syntax controls and minimal repairs, with independently sourced
expression-category relationships. All new facets are pending and no existing RANK
inquiry case is retagged.

**Oracle limitation:** The grammar is not an oracle for processor rank limits, allocation, pointer association,
assumed-shape bounds or runtime value. Reporting capability does not prescribe fatal
exit, English wording or a printed rule code; unsupported-feature, ICE, echo and
wrong-origin reports do not qualify.

**Dependencies:** 8.2 R802;8.5.8.1;C863/C864;10.1.12 R1032/C1014 andp1(6);C401;16.9.171;4.2p2.

### C863: The RANK-clause value lies between zero and the supported maximum

**Source:** 8.5.17 C863, physical PDF132 **Class:** Restriction.

**Definition:** The constant INTEGER value in the clause is nonnegative and does not exceed the
processor's supported maximum array rank. Other standard rank/corank and declaration
restrictions still apply; an extension or a guessed supported limit is not a portable
boundary oracle.

**Diagnostic obligation:** required.

**Facets:** `negative-exclusion`, `zero-admission`, `ordinary-positive-admission`, `supported-maximum-boundary`, `above-supported-maximum-exclusion`.

**Oracle:** An unconditional negative-value contrast and processor-qualified finite endpoint
contrasts, each with valid entity-role controls and exact report origins.

**Oracle limitation:** No maximum-rank profile is presently supplied. Keep this gate pending rather than
guessing a limit, deriving it from the case under test or making recommended
documentation a new mandatory requirement.

**Dependencies:** R829;C864;5.4.6p1;8.5.8.1note1;4.2p2(1)/p7;10.1.12.

### C864: A RANK-clause entity has an eligible dummy role or storage attribute

**Source:** 8.5.17 C864, physical PDF132 **Class:** Restriction.

**Definition:** An entity declared using the clause is a dummy data object or has ALLOCATABLE or
POINTER. The condition includes rank-zero declarations. It does not override other rules
about which entities and declaration forms can have these attributes.

**Diagnostic obligation:** required.

**Facets:** `ordinary-dummy-admission`, `allocatable-admission`, `pointer-admission`, `ordinary-local-positive-exclusion`, `ordinary-local-zero-exclusion`, `nondata-role-source-boundary`.

**Oracle:** Complete role/attribute declarations with minimal ordinary-local repairs; all case plans
remain pending. Declaration admission is distinct from rank, shape, allocation,
association and runtime effect.

**Oracle limitation:** The five NOTE declarations are not a self-contained valid program. Do not infer role
from a name, skip C864 at rank zero, manufacture unsupported feature diagnostics or
promote compile controls into effects.

**Dependencies:** 8.2 R801-R803;8.5.3;8.5.8.1/.3/.4;8.5.14;15.4.2.1/.2;4.2p2.

### S8.5.17-001: An eligible RANK-clause declaration has its specified rank

**Source:** 8.5.17 p2 first sentence and zero-rank branch, physical PDF132 **Class:** Effect.

**Definition:** The eligible entity's rank equals the constant value specified by the clause; value zero
denotes a scalar. This is not the dynamic assumed-rank category, a zero-sized
positive-rank array, or a claim about available shape and data.

**Diagnostic obligation:** not-required.

**Facets:** `zero-rank-effect`, `positive-rank-effects`, `named-constant-rank-effect`, `specification-inquiry-rank-effect`.

**Oracle:** Independent literal ranks on actual objects, with complete interfaces, required states
and counted execution; whole-program wrong-oracle sensitivity is required before an
implementation is approved.

**Oracle limitation:** No programs or oracles are implemented by this source packet. Rank inquiry does not
authorize unavailable payload/shape/bounds access. Existing lexical, C834, assumed-rank
and finalization witnesses retain their original owners and roles.

**Dependencies:** R829;C863/C864;3.114/3.119;5.4.6;8.5.8.1/.3/.4;10.1.12;15.4.2.1/.2;16.1;16.9.171.

### S8.5.17-002: The positive-rank ordinary dummy branch has unit lower bounds

**Source:** 8.5.17 p2 final branch, physical PDF132 **Class:** Effect.

**Definition:** For positive rank without POINTER or ALLOCATABLE, the RANK clause specifies an
assumed-shape array with lower bound one in every dimension. C864 requires the ordinary
dummy-data-object role. Deferred-shape declarations, scalar rank zero and
effective-argument extents have their own conditions.

**Diagnostic obligation:** not-required.

**Facets:** `rank-one-unit-lower-bound`, `all-dimension-unit-lower-bounds`, `empty-inquiry-source-boundary`.

**Oracle:** Direct whole-dummy lower-bound observations with independent literals in the
positive-extent cases; separately qualified source relationships for empty dimensions.

**Oracle limitation:** This packet supplies no execution. Unit lower bounds do not extend to the positive-rank
POINTER/ALLOCATABLE branch, and scalar rank zero is not an assumed-shape array. Do not
infer shape, extent, physical descriptor layout or allocation from the declaration.

**Dependencies:** C864;8.5.8.3p1-p4;8.5.8.4;15.4.2.2;15.5.2.4/.5;16.9.119.

<!-- END GENERATED 8.5.17 -->
