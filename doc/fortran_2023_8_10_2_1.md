# Fortran 2023 8.10.2.1: COMMON statement: General

The canonical catalogue is `doc/catalogues/common_statement_8_10_2_1.json`. Effective review state comes
from `Registry.catalogue_review_state("8.10.2.1")`; source accounting
does not approve cases, canonical relationships or an execution inventory.
The original source-only registration introduced56 pending facets and no cases.
Current facet accounting is authoritative in the catalogue; source, case and
execution-inventory reviews remain separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`. The original section hash is
`1e6bacc34b79459e4d801f6beb605ea68db66c60425c15fb286b48eefae1f5ee`. Physical page numbers are 1-based;
physical148/149 correspond to printed134/135.

The original heading is **8.10.2.1 General**, within 8.10.2 COMMON
statement. The empty parent heading receives no invented requirement. The
thirteen original units are p1, p2, R876, R877, C8121-C8125 and p3-p6; the next
heading is 8.10.2.2 on physical148. All are accounted, including the permission
to repeat block names and the separate ordered continuation rule.

R876 has an optional first slash/name group, a required first object list and
optional subsequent slash/name/list groups. The comma between groups is
optional; commas within an object list come from R401. A missing first name
or an empty slash pair denotes blank common, not a missing object list.
There is no double-colon form. The complete502-production scan finds no
explicit common-block-name head, but **variable-name is explicitly R903**,
with C903 supplying the variable role; R402 does not replace it.

R877 parentheses introduce a declarator, not an element selection. In
particular, `COMMON /b/ a(1)` can declare a whole one-element array; it is
not automatically an invalid array-element spelling. C8121 requires an
explicit-shape-spec list, not the distinct vector-bounds production R818.
The DIMENSION definition in p5 retains its canonical8.5.8 owners and C815.

C8123's ten exclusions retain their actual roles. Its ultimate-component
test follows3.30.5/7.5.1p3 and stops at pointer or allocatable components;
it is not arbitrary pointer reachability. Enumeration type7.6.2 is not
enum type7.6.1. C8124 requires BIND or SEQUENCE on the **type**, together
with no default initialization; it does not give BIND to the object or block.
Even one directly or recursively default-initialized component matters.
A pointer component initialized to NULL also supplies default initialization.

C8125 bans a USE-associated **object** in the list, not a COMMON-block
homonym or access to a module's already declared COMMON member. A local
COMMON variable-name hides a host homonym under19.5.1.4p2(7). Names,
global block identities and separately scoped member entities must not be
conflated. COMMON is excluded from BLOCK by C1107, and its specification
position is governed by R504/R513 rather than a table-only inference.

p1/p2/p3/p5 are definitions, not new no-op runtime effects. The two local
supplementary effects cover actual ordered-list continuation and the
numeric/character sequence-component interpretation through future
defined-value observations. Other derived types and BIND layouts do not
acquire numeric or character sequence status by resembling one processor's
memory layout. COMMON's obsolescence is distinct from invalid-source
diagnosis; neither is a compiler-fixing task.

All pending plans require independent source/fixture/relationship review.
No compiler was invoked for the original source-only registration. No F2018 observation
is promoted to F2023 qualification, and no model, kind, C companion,
launcher or processor-header prerequisite is inferred from missing data.
Existing real/BOZ, SAVE, TARGET, BIND and initialization owners remain
unchanged. The whole-source census and universal execution obligation
are not closed by these catalogues.

<!-- BEGIN COMMON LIST EFFECTS -->
## Four bounded ordered-list runtime fixtures

Four complete run/effect/f2023 programs represent only the four S8.10.2.1-001
facets. Their drivers declare no COMMON. Ordinary external seed, writer and
reader procedures have matching complete default-INTEGER storage sequences.
Named blocks have SAVE /block/ in every declaring procedure. The blank case
has no SAVE, DATA or declaration initialization; its nonpointer objects retain
defined values across returns under8.10.2.5/19.6.6.

A separately called and counted seed defines all observed positions to
-101/-102/-103/-104 before the writer. Seeding survives every mutation.
The writer uses repeated /packet/ groups in one statement, repeated named
statements, omitted-name plus // blank continuation, or four interleaved
/left_block/ and /right_block/ statements. Readers check each actual COMMON
coordinate against independent11/22/33/44 literals: one four-element tuple
or separate left11/22 and right33/44 tuples, never a commutative sum.

Control arguments are distinct initialized driver scalars, never COMMON
members. Seed/writer/reader entries, four writes/observations and normal
returns are guarded; nine counted caller checks precede exact externally
checked completion, empty runtime stderr and exit0. A missing writer or
no-op body is caught before the reader. Individual omitted writes leave
defined sentinels, so value-guard failures do not read undefined storage.

Full-parent sensitivity covers every assertion, wrong order/block values,
individual writes/observations, body/call/return-count omissions, caller
checks and wrong or absent completion. Completion mismatches can correctly
fail with exit0. Failed or stale parents, incomplete traces, internal/resource
failures and preempting guards never qualify. F2018 remains supplementary.

The generator removes the four selected pending entries and replaces the two
inherited sentences that said no program or mutation existed. All other
requirement fields, including the original source plans of the113 unrepresented
facets, remain exact. No source/case/inventory approval, link, SourceUse,
baseline update or byte-layout/representation credit is supplied.
<!-- END COMMON LIST EFFECTS -->

<!-- BEGIN GENERATED 8.10.2.1 -->

### R876: COMMON specifies nonempty object lists in named or blank blocks

**Source:** 8.10.2.1 R876, J3/24-007, 18 December 2023, physical PDF148. **Class:** Syntax.

**Definition:** COMMON is followed by a nonempty common-block-object list, optionally preceded by a
slash pair containing an optional block name. Further slash/name/list groups may follow,
with or without a separating comma. Every group has an object list; R401 supplies that
list's mandatory intra-list separators.

**Diagnostic obligation:** required.

**Facets:** `named-first-block`, `implicit-blank-first-block`, `explicit-blank-block`, `optional-intergroup-comma`, `mandatory-intralist-separator`, `missing-object-list`, `delimiter-and-double-colon-exclusions`, `name-identity-source`, `ordering-scope-and-obsolescence-source`.

**Oracle:** Complete source-qualified compile admissions and minimally repaired, otherwise eligible
contrasts. Resolve names, roles, scope and all overlapping constraints before qualifying
an actual diagnostic.

**Oracle limitation:** The numbered reporting obligation is not a universal fatal-exit requirement, a
requirement to print a rule number, or a prescribed English message. A future exact
report needs its actual source/control context and staged origin. Unsupported syntax or
modes, unrelated earlier errors, recovery alone, internal/resource failures and source
echoes do not establish this cause. An obsolescence report is not a report of the
particular forbidden form. Compile admission is a positive control, not execution. All
proposed evidence remains PENDING. No case, direct/linked facet, SourceUse relationship,
inventory renewal, source approval, baseline change or universal conformance credit is
created. Each reference/target/configuration is separate; f2018 success cannot
corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE, coarray and
companion/CFI-header gates remain with their actual owners. Missing or unsupported
prerequisite evidence cannot be treated as success.

**Dependencies:** 4.1.1-.3;4.2p1/p2;5.1R504/R507/R508/R513;6.2.2;R877/C8121-C8125/p3/p4;9.2R903/C903;11.1.4C1107;19.2;19.3.1/.2;19.5.1.4p2;B.3.11.

### R877: A COMMON object is a variable name with an optional array declarator

**Source:** 8.10.2.1 R877, J3/24-007, 18 December 2023, physical PDF148. **Class:** Syntax.

**Definition:** The object form is a variable-name, optionally followed by parenthesized array-spec. The
explicit R903/C903 production determines the name and role. The optional parentheses
specify the variable's DIMENSION attribute, subject to C8121 and C815; they do not
select a subobject.

**Diagnostic obligation:** required.

**Facets:** `scalar-variable`, `inline-array-declarator`, `separately-shaped-array`, `declarator-not-element`, `component-not-name`, `variable-role-canonical-source`.

**Oracle:** Complete source-qualified compile admissions and minimally repaired, otherwise eligible
contrasts. Resolve names, roles, scope and all overlapping constraints before qualifying
an actual diagnostic.

**Oracle limitation:** The numbered reporting obligation is not a universal fatal-exit requirement, a
requirement to print a rule number, or a prescribed English message. A future exact
report needs its actual source/control context and staged origin. Unsupported syntax or
modes, unrelated earlier errors, recovery alone, internal/resource failures and source
echoes do not establish this cause. An obsolescence report is not a report of the
particular forbidden form. Compile admission is a positive control, not execution. All
proposed evidence remains PENDING. No case, direct/linked facet, SourceUse relationship,
inventory renewal, source approval, baseline change or universal conformance credit is
created. Each reference/target/configuration is separate; f2018 success cannot
corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE, coarray and
companion/CFI-header gates remain with their actual owners. Missing or unsupported
prerequisite evidence cannot be treated as success.

**Dependencies:** 4.1.3p2;6.2.2R603/C601;8.5.1C815;8.5.8.1/.2;R876/C8121-C8125/p5;9.2R903/C903;19.5.1.4p2(7).

### C8121: An inline COMMON array specification is an explicit-shape list

**Source:** 8.10.2.1 C8121, J3/24-007, 18 December 2023, physical PDF148. **Class:** Restriction.

**Definition:** Whenever R877 contains an array-spec, it must be an explicit-shape-spec-list. The
distinct explicit-shape-bounds-spec alternative is not licensed merely because it
describes explicit shape. Eligibility, constant/specification expressions and single
attribute specification remain independent.

**Diagnostic obligation:** required.

**Facets:** `constant-list-admission`, `vector-bounds-exclusion`, `other-array-spec-alternatives`, `shape-and-typing-canonical-source`.

**Oracle:** Complete source-qualified compile admissions and minimally repaired, otherwise eligible
contrasts. Resolve names, roles, scope and all overlapping constraints before qualifying
an actual diagnostic.

**Oracle limitation:** The numbered reporting obligation is not a universal fatal-exit requirement, a
requirement to print a rule number, or a prescribed English message. A future exact
report needs its actual source/control context and staged origin. Unsupported syntax or
modes, unrelated earlier errors, recovery alone, internal/resource failures and source
echoes do not establish this cause. An obsolescence report is not a report of the
particular forbidden form. Compile admission is a positive control, not execution. All
proposed evidence remains PENDING. No case, direct/linked facet, SourceUse relationship,
inventory renewal, source approval, baseline change or universal conformance credit is
created. Each reference/target/configuration is separate; f2018 success cannot
corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE, coarray and
companion/CFI-header gates remain with their actual owners. Missing or unsupported
prerequisite evidence cannot be treated as success.

**Dependencies:** 4.1.3R401;8.3;8.5.1C815;8.5.8.1R814;8.5.8.2R815-R819/C831-C833;R877/C8123/p5;10.1.11/.12.

### C8122: A variable occurs in only one COMMON object-list position per scope

**Source:** 8.10.2.1 C8122, J3/24-007, 18 December 2023, physical PDF148. **Class:** Restriction.

**Definition:** A given variable-name may appear only once across all COMMON object lists in one scoping
unit. The count does not reset at a new block, a new statement or a blank/named
boundary. Distinct scoping units and distinct local entities are not a file-wide
duplicate count.

**Diagnostic obligation:** required.

**Facets:** `repeat-in-one-list`, `repeat-across-statements`, `different-block-names`, `named-and-blank-lists`, `distinct-entities-and-scopes`.

**Oracle:** Complete source-qualified compile admissions and minimally repaired, otherwise eligible
contrasts. Resolve names, roles, scope and all overlapping constraints before qualifying
an actual diagnostic.

**Oracle limitation:** The numbered reporting obligation is not a universal fatal-exit requirement, a
requirement to print a rule number, or a prescribed English message. A future exact
report needs its actual source/control context and staged origin. Unsupported syntax or
modes, unrelated earlier errors, recovery alone, internal/resource failures and source
echoes do not establish this cause. An obsolescence report is not a report of the
particular forbidden form. Compile admission is a positive control, not execution. All
proposed evidence remains PENDING. No case, direct/linked facet, SourceUse relationship,
inventory renewal, source approval, baseline change or universal conformance credit is
created. Each reference/target/configuration is separate; f2018 success cannot
corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE, coarray and
companion/CFI-header gates remain with their actual owners. Missing or unsupported
prerequisite evidence cannot be treated as success.

**Dependencies:** 3.120;R876/R877/p4;8.10.2.2;19.1-.3;19.5.1.4p2(7).

### C8123: COMMON objects exclude ten specified entity and storage roles

**Source:** 8.10.2.1 C8123, J3/24-007, 18 December 2023, physical PDF148. **Class:** Restriction.

**Definition:** A COMMON object is excluded if it is a dummy argument, function result, allocatable
variable, derived-type object with an allocatable ultimate component, enumeration-type
object, procedure pointer, automatic data object, variable with BIND, unlimited
polymorphic pointer, or coarray. The list does not prohibit every data pointer, every
derived type or an enum-type object, and BIND on a common block is not BIND on its
member.

**Diagnostic obligation:** required.

**Facets:** `dummy-argument`, `function-result`, `allocatable-variable`, `allocatable-ultimate-component`, `recursive-ultimate-component-boundary`, `enumeration-type`, `procedure-pointer`, `automatic-data-object`, `bind-variable`, `unlimited-polymorphic-pointer`, `coarray`, `ordinary-data-pointer-admission`, `enum-and-block-bind-boundaries`.

**Oracle:** Complete source-qualified compile admissions and minimally repaired, otherwise eligible
contrasts. Resolve names, roles, scope and all overlapping constraints before qualifying
an actual diagnostic.

**Oracle limitation:** The numbered reporting obligation is not a universal fatal-exit requirement, a
requirement to print a rule number, or a prescribed English message. A future exact
report needs its actual source/control context and staged origin. Unsupported syntax or
modes, unrelated earlier errors, recovery alone, internal/resource failures and source
echoes do not establish this cause. An obsolescence report is not a report of the
particular forbidden form. Compile admission is a positive control, not execution. All
proposed evidence remains PENDING. No case, direct/linked facet, SourceUse relationship,
inventory renewal, source approval, baseline change or universal conformance credit is
created. Each reference/target/configuration is separate; f2018 success cannot
corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE, coarray and
companion/CFI-header gates remain with their actual owners. Missing or unsupported
prerequisite evidence cannot be treated as success.

**Dependencies:** 3.30.5;7.5.1p3/p6;7.5.2.3C745;7.6.1/.2;8.3;8.5.3/.5/.14;C8124;9.2R903/C903;15.4.3.2/.6;18.2.2;18.9.1;19.5.2.

### C8124: A derived COMMON object's type has BIND or SEQUENCE and no default initialization

**Source:** 8.10.2.1 C8124, J3/24-007, 18 December 2023, physical PDF148. **Class:** Restriction.

**Definition:** For a derived-type COMMON object, the type must have BIND or SEQUENCE and must have no
default initialization. These are conjunctive conditions; they concern the type, not
BIND on the variable or block. A type has default initialization when a direct component
has component-initialization, including recursive nonpointer/nonallocatable components
and initialized pointers.

**Diagnostic obligation:** required.

**Facets:** `sequence-type-admission`, `bind-type-admission`, `missing-type-attribute`, `nonpointer-default-initialization`, `pointer-default-initialization`, `nested-default-initialization`, `type-object-and-initialization-boundaries`.

**Oracle:** Complete source-qualified compile admissions and minimally repaired, otherwise eligible
contrasts. Resolve names, roles, scope and all overlapping constraints before qualifying
an actual diagnostic.

**Oracle limitation:** The numbered reporting obligation is not a universal fatal-exit requirement, a
requirement to print a rule number, or a prescribed English message. A future exact
report needs its actual source/control context and staged origin. Unsupported syntax or
modes, unrelated earlier errors, recovery alone, internal/resource failures and source
echoes do not establish this cause. An obsolescence report is not a report of the
particular forbidden form. Compile admission is a positive control, not execution. All
proposed evidence remains PENDING. No case, direct/linked facet, SourceUse relationship,
inventory renewal, source approval, baseline change or universal conformance credit is
created. Each reference/target/configuration is separate; f2018 success cannot
corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE, coarray and
companion/CFI-header gates remain with their actual owners. Missing or unsupported
prerequisite evidence cannot be treated as success.

**Dependencies:** 3.30.1;7.5.1p4/p6/p7;7.5.2.1/.3/.4;7.5.4.1/.6p8;8.4;C8123;18.2.2;18.3.4C1801-C1806.

### C8125: A COMMON object name is not made accessible by USE association

**Source:** 8.10.2.1 C8125, J3/24-007, 18 December 2023, physical PDF148. **Class:** Restriction.

**Definition:** A variable-name in a common-block-object cannot be made accessible by use association.
Renaming does not remove that association. This is not a ban on using a module's COMMON
members without redeclaring membership, nor a ban on a legal block-name homonym. A
COMMON object-name has a different host shadowing rule from USE association.

**Diagnostic obligation:** required.

**Facets:** `direct-use-associated-object`, `renamed-use-associated-object`, `module-common-member-access`, `block-name-homonym`, `host-shadowing-source`.

**Oracle:** Complete source-qualified compile admissions and minimally repaired, otherwise eligible
contrasts. Resolve names, roles, scope and all overlapping constraints before qualifying
an actual diagnostic.

**Oracle limitation:** The numbered reporting obligation is not a universal fatal-exit requirement, a
requirement to print a rule number, or a prescribed English message. A future exact
report needs its actual source/control context and staged origin. Unsupported syntax or
modes, unrelated earlier errors, recovery alone, internal/resource failures and source
echoes do not establish this cause. An obsolescence report is not a report of the
particular forbidden form. Compile admission is a positive control, not execution. All
proposed evidence remains PENDING. No case, direct/linked facet, SourceUse relationship,
inventory renewal, source approval, baseline change or universal conformance credit is
created. Each reference/target/configuration is separate; f2018 success cannot
corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE, coarray and
companion/CFI-header gates remain with their actual owners. Missing or unsupported
prerequisite evidence cannot be treated as success.

**Dependencies:** R877;8.7;14.2.2p2/p7/p9/note4;19.3.1/.2;19.5.1.3/.4p2(7).

### S8.10.2.1-001: Successive lists continue the same block within one scope

**Source:** 8.10.2.1 p4, J3/24-007, 18 December 2023, physical PDF148. **Class:** Effect.

**Definition:** Successive object lists for the same named block, or for blank common, are concatenated
in one scoping unit. Repeating a block name does not reset its list. Differently scoped
declarations describe associated storage views, not further text appended to this
scope's object list.

**Diagnostic obligation:** not-required.

**Facets:** `named-one-statement`, `named-multiple-statements`, `blank-spelling-continuation`, `interleaved-distinct-blocks`.

**Oracle:** Defined default-INTEGER value propagation across complete, matching COMMON views. Use
independent literal tuple oracles, not addresses, emitted assembly or a same-processor
serialization round trip. Every future effect requires a complete driver and all
declaring/defining procedures, actual successful compile/link/run traces in the required
f2023 configuration, independent literal observations and executed-call/completion
checks. For named COMMON initial families, put consistent SAVE /block/ in every
nonexempt declaring procedure and match the complete storage sequences; avoid relying on
an accidentally active scope or implicit SAVE. Seed every observed storage unit with a
known sentinel before the operation under test, so omitted-write sensitivity never reads
undefined data. Keep actual arguments distinct from COMMON storage to avoid
dummy-aliasing violations. Wrong-order/value/omission/omitted-call mutations must fail
the whole counted parent, not a detached expression. Four complete programs and their
whole-program mutations now implement exactly the four ordered-list facets named above;
every other plan under this requirement remains unimplemented.

**Oracle limitation:** The permission to repeat block names is not a separate effect, and this does not permit
a variable to recur. Same-name declarations in another scope are not appended to this
list. Only the four named ordered-list facets are represented by cases; every other
proposed effect under this requirement remains PENDING. Those four cases create no
linked facet, SourceUse relationship, inventory renewal, source approval, baseline
change or universal conformance credit. Each reference/target/configuration is separate;
f2018 success cannot corroborate an f2023 expectation. Numerical-kind, real/BOZ/IEEE,
coarray and companion/CFI-header gates remain with their actual owners. Missing or
unsupported prerequisite evidence cannot be treated as success.

**Dependencies:** R876/C8122/p3;8.10.2.2/.3/.4/.5;8.5.16;9.2p2;15.5.2.14;19.5.3;19.6.5(12).

### S8.10.2.1-002: Qualifying sequence objects act as enumerated COMMON components

**Source:** 8.10.2.1 p6, J3/24-007, 18 December 2023, physical PDF148. **Class:** Effect.

**Definition:** Numeric sequence and character sequence objects in COMMON are interpreted as though
their individual components occurred in the object list. Their exact classes come
from7.5.2.3 and recursively exclude pointer/allocatable components. Ordinary derived
types, mixed sequence types and BIND layouts are not silently treated as these two
classes.

**Diagnostic obligation:** not-required.

**Facets:** `numeric-sequence-integer-components`, `nested-numeric-sequence`, `character-sequence-components`.

**Oracle:** Independent literal component/position observations in complete programs. Every future
effect requires a complete driver and all declaring/defining procedures, actual
successful compile/link/run traces in the required f2023 configuration, independent
literal observations and executed-call/completion checks. For named COMMON initial
families, put consistent SAVE /block/ in every nonexempt declaring procedure and match
the complete storage sequences; avoid relying on an accidentally active scope or
implicit SAVE. Seed every observed storage unit with a known sentinel before the
operation under test, so omitted-write sensitivity never reads undefined data. Keep
actual arguments distinct from COMMON storage to avoid dummy-aliasing violations.
Wrong-order/value/omission/omitted-call mutations must fail the whole counted parent,
not a detached expression. No such program or mutation has been implemented here.

**Oracle limitation:** Cross-type numeric association is not numeric conversion and does not make every
associated object defined. The first programs deliberately use only same-kind INTEGER or
default CHARACTER storage; other numerical/BOZ/model premises remain separately gated.
No physical address or TRANSFER oracle. All proposed evidence remains PENDING. No case,
direct/linked facet, SourceUse relationship, inventory renewal, source approval,
baseline change or universal conformance credit is created. Each
reference/target/configuration is separate; f2018 success cannot corroborate an f2023
expectation. Numerical-kind, real/BOZ/IEEE, coarray and companion/CFI-header gates
remain with their actual owners. Missing or unsupported prerequisite evidence cannot be
treated as success.

**Dependencies:** 3.30.5;7.5.1;7.5.2.3/.4;C8123/C8124;8.10.2.2-.5;9.5.3.3;19.5.3.2p2(3)/(5)/(7);19.5.3.4p7;19.6.1;19.6.5(12)/(15).

<!-- END GENERATED 8.10.2.1 -->
