# Fortran 2023 12.3.4.4: Positioning after data transfer - I/O positioning/internal fixtures

The canonical catalogue is `doc/catalogues/positioning_after_data_transfer_12_3_4_4.json`. This author packet adds finite runtime fixtures; fixture approval remains separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 3 facets in 2 rule-bound manifest(s); 3 of 11 facets are represented and 8 remain pending.

<!-- BEGIN GENERATED 12.3.4.4 -->

### S12.3.4.4-001: Post-transfer error conditions make file position indeterminate

**Source:** S12.3.4.4, J3/24-007, 18 December 2023, physical PDF247. **Class:** Effect.

**Definition:** If an error condition occurs during data transfer, the position of the file is
indeterminate.

**Diagnostic obligation:** not-required.

**Facets:** `posttransfer-error-position-indeterminate`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.4-002: Reading an endfile record positions after the endfile record absent error

**Source:** S12.3.4.4, J3/24-007, 18 December 2023, physical PDF247. **Class:** Effect.

**Definition:** If no error occurs but an end-of-file condition occurs as a result of reading an endfile
record, the file is positioned after the endfile record.

**Diagnostic obligation:** not-required.

**Facets:** `read-endfile-record-positions-after-endfile`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.4-003: Unformatted stream transfer does not change position absent error and output can extend terminal point

**Source:** S12.3.4.4, J3/24-007, 18 December 2023, physical PDF247. **Class:** Effect.

**Definition:** For unformatted stream input/output with no error, the file position is not changed; for
unformatted stream output, a file position beyond the previous terminal point sets the
terminal point to that file position.

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-stream-position-unchanged-after-transfer`, `unformatted-stream-output-extends-terminal-point`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.4-003 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`unformatted-stream-output-extends-terminal-point`,
`unformatted-stream-position-unchanged-after-transfer`. These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.4-003 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.4-004: Formatted stream input at end-of-file does not change position absent error

**Source:** S12.3.4.4, J3/24-007, 18 December 2023, physical PDF247. **Class:** Effect.

**Definition:** For formatted stream input, if an end-of-file condition occurs, the file position is not
changed.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-eof-position-unchanged`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.4-005: Nonadvancing transfer post-positioning depends on EOR, EOF, and error conditions

**Source:** S12.3.4.4, J3/24-007, 18 December 2023, physical PDF247. **Class:** Effect.

**Definition:** For nonadvancing input, an end-of-record condition without error or end-of-file
positions after the record just read; absent error, end-of-file, and end-of-record,
nonadvancing input leaves position unchanged; absent error, nonadvancing output leaves
position unchanged.

**Diagnostic obligation:** not-required.

**Facets:** `nonadvancing-input-eor-positions-after-record`, `nonadvancing-input-no-condition-position-unchanged`, `nonadvancing-output-no-error-position-unchanged`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.4-005 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`nonadvancing-input-eor-positions-after-record`. These fixtures are finite single-image
positive runtime observations over scratch or local external files and character
internal files. They assert only exact integers, logical inequalities, exact characters
with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants, IOLENGTH=/POS=/SIZE=
values or inequalities derived from the executing processor, and FILE_STORAGE_SIZE
positivity. They do not assert physical byte encodings, record-marker layout, IOMSG
text, processor-specific numeric error statuses, unsupported deferred-length internal
WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.4-005 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.4-006: All other data transfer cases position after the record and make it preceding

**Source:** S12.3.4.4, J3/24-007, 18 December 2023, physical PDF247. **Class:** Effect.

**Definition:** In all other cases after data transfer, the file is positioned after the record just
read or written and that record becomes the preceding record.

**Diagnostic obligation:** not-required.

**Facets:** `other-transfer-positions-after-record`, `other-transfer-record-becomes-preceding`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.3.4.4-006 I/O positioning/internal runtime fixtures: valid run-phase fixtures cover
`other-transfer-positions-after-record`, `other-transfer-record-becomes-preceding`.
These fixtures are finite single-image positive runtime observations over scratch or
local external files and character internal files. They assert only exact integers,
logical inequalities, exact characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR
named constants, IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing
processor, and FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings,
record-marker layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.3.4.4-006 I/O positioning/internal fixture boundaries: These fixtures are finite
single-image positive runtime observations over scratch or local external files and
character internal files. They assert only exact integers, logical inequalities, exact
characters with LEN checks, IOSTAT zero, IOSTAT_END/EOR named constants,
IOLENGTH=/POS=/SIZE= values or inequalities derived from the executing processor, and
FILE_STORAGE_SIZE positivity. They do not assert physical byte encodings, record-marker
layout, IOMSG text, processor-specific numeric error statuses, unsupported
deferred-length internal WRITE behavior, or diagnostics for unnumbered restrictions.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

### S12.3.4.4-007: Formatted stream output sets terminal point after highest transferred position absent error

**Source:** S12.3.4.4, J3/24-007, 18 December 2023, physical PDF247. **Class:** Effect.

**Definition:** For a formatted stream output statement with no error, the terminal point is set to the
next position after the highest-numbered position to which a datum was transferred by
the statement.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-stream-output-terminal-after-highest-transferred-position`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection modes,
connection/specifier, data-transfer, file-position, internal-file, and input/output-item
rules; clause 13 formatting; clause 15 defined I/O; clause 16 inquiry intrinsics and
ISO_FORTRAN_ENV constants where future fixtures need them; clauses 3, 5, 14, 17, 18, and
19 where terminology, constants, images, IEEE support, deleted features, or
scope/lifetime controls are reached.

<!-- END GENERATED 12.3.4.4 -->

<!-- BEGIN IO POSITIONING INTERNAL FIXTURES -->
## I/O positioning, file-storage-unit, and internal-file fixtures

This packet adds 12 runtime programs and 12 rule-bound manifests representing 21 selected facets in 12.3.4.2, 12.3.4.3, 12.3.4.4, 12.3.5, and 12.4. The programs use scratch or local files deleted by the program and character internal files. READ targets use nondefault sentinels and pre-READ guards; stream positions are derived with IOLENGTH=, POS=, and SIZE= inquiries; character equality checks first compare LEN. Deferred-length allocatable internal WRITE facets remain pending because the reference compiler does not validate them.
<!-- END IO POSITIONING INTERNAL FIXTURES -->

### `nonadvancing_input_eor_next` / `S12.3.4.4-005`

**Facets:** `nonadvancing-input-eor-positions-after-record`.

12.3.4.4 p4 positions after the record when nonadvancing input reaches EOR; the next sequential read therefore obtains the following distinctive record.

Feature/sentinel mutations: `not-wide-enough-for-eor`, `next-record-changed`, `sentinel-wide-eor-remove`, `sentinel-wide-eor-expected`, `sentinel-wide-eor-default`, `sentinel-next-after-eor-remove`, `sentinel-next-after-eor-expected`, `sentinel-next-after-eor-default`.

### `unformatted_stream_terminal_size` / `S12.3.4.4-003`

**Facets:** `unformatted-stream-position-unchanged-after-transfer`, `unformatted-stream-output-extends-terminal-point`.

12.3.4.4 p2 leaves an empty unformatted stream output at the POS= position and extends the terminal point after a later positioned datum; 12.3.5 p1 makes SIZE= report file-size units.

Feature/sentinel mutations: `empty-output-other-pos`, `terminal-write-other-value`, `sentinel-terminal-readback-remove`, `sentinel-terminal-readback-expected`, `sentinel-terminal-readback-default`.
