# Fortran 2023 16.9.208: THIS_IMAGE

The canonical catalogue is `doc/catalogues/this_image_16_9_208.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.208")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.208 -->

### S16.9.208-001: THIS_IMAGE COARRAY, DIM, and TEAM restrictions

**Source:** S16.9.208, J3/24-007, 18 December 2023, physical PDF465. **Class:** Restriction.

**Definition:** COARRAY shall be a coarray of any type with allocation and rightmost-part-ref
restrictions; DIM shall be an integer scalar in the range one through the corank of
COARRAY; TEAM shall be scalar TEAM_TYPE identifying the current or an ancestor team, and
COARRAY shall be established in that team.

**Diagnostic obligation:** not-required.

**Facets:** `THIS_IMAGE-COARRAY-coarray-any-type`, `THIS_IMAGE-allocatable-COARRAY-allocated`, `THIS_IMAGE-rightmost-part-ref-nonzero-corank`, `THIS_IMAGE-DIM-integer-scalar-valid-codimension`, `THIS_IMAGE-TEAM-current-or-ancestor`, `THIS_IMAGE-COARRAY-established-in-TEAM`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only exact properties fixed by 16.9:
type, kind, rank, shape, allocation bounds, integer counts, token positions, sequence
contents, model-inquiry relations, or bit-context observations. Processor-dependent
approximations, processor-dependent model values, and image/team-dependent behavior are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json for TANH, TANPI,
TEAM_NUMBER, THIS_IMAGE, TINY, TOKENIZE, TRAILZ, and S16.9.205 through S16.9.211 found
no fixture-manifest binding for these sections in this worktree. Existing clause 10
source files mention TEAM_NUMBER and THIS_IMAGE, and one Python test helper excludes
THIS_IMAGE text; those incidental uses are recorded as existing coverage and are not
rebound by this source packet. Every image, team, coarray, DIM, and cosubscript runtime
observation, including single-image THIS_IMAGE() results, is OUT OF SCOPE for this
single-image suite by user decision; these entries record source facts only and no
diagnostic facets.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, kind, declaration,
expression, assignment, array, character, allocation, execution, and I/O foundations
from clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 5 image and team execution terminology, clause 7 type and kind rules, clause 10
expression and intrinsic-assignment semantics, clause 16.3 integer bit model, clause
16.4 real model parameters, ISO_FORTRAN_ENV TEAM_TYPE, sibling 16.9 intrinsics named by
pending plans such as BIT_SIZE, BTEST, EXPONENT, IBSET, KIND, LEN, MAXEXPONENT,
MINEXPONENT, RADIX, RANK, SELECTED_*_KIND, and SIZE, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, module, deleted-feature, or scope terms.

### S16.9.208-002: THIS_IMAGE result characteristics

**Source:** S16.9.208, J3/24-007, 18 December 2023, physical PDF466. **Class:** Effect.

**Definition:** The THIS_IMAGE result is default integer; without COARRAY or with DIM it is scalar, and
with COARRAY and no DIM it is rank one with size equal to the corank of COARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `THIS_IMAGE-no-COARRAY-default-integer-scalar`, `THIS_IMAGE-DIM-present-scalar`, `THIS_IMAGE-COARRAY-no-DIM-rank-one-size-corank`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only exact properties fixed by 16.9:
type, kind, rank, shape, allocation bounds, integer counts, token positions, sequence
contents, model-inquiry relations, or bit-context observations. Processor-dependent
approximations, processor-dependent model values, and image/team-dependent behavior are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json for TANH, TANPI,
TEAM_NUMBER, THIS_IMAGE, TINY, TOKENIZE, TRAILZ, and S16.9.205 through S16.9.211 found
no fixture-manifest binding for these sections in this worktree. Existing clause 10
source files mention TEAM_NUMBER and THIS_IMAGE, and one Python test helper excludes
THIS_IMAGE text; those incidental uses are recorded as existing coverage and are not
rebound by this source packet. Every image, team, coarray, DIM, and cosubscript runtime
observation, including single-image THIS_IMAGE() results, is OUT OF SCOPE for this
single-image suite by user decision; these entries record source facts only and no
diagnostic facets.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, kind, declaration,
expression, assignment, array, character, allocation, execution, and I/O foundations
from clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 5 image and team execution terminology, clause 7 type and kind rules, clause 10
expression and intrinsic-assignment semantics, clause 16.3 integer bit model, clause
16.4 real model parameters, ISO_FORTRAN_ENV TEAM_TYPE, sibling 16.9 intrinsics named by
pending plans such as BIT_SIZE, BTEST, EXPONENT, IBSET, KIND, LEN, MAXEXPONENT,
MINEXPONENT, RADIX, RANK, SELECTED_*_KIND, and SIZE, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, module, deleted-feature, or scope terms.

### S16.9.208-003: THIS_IMAGE result values

**Source:** S16.9.208, J3/24-007, 18 December 2023, physical PDF466. **Class:** Effect.

**Definition:** THIS_IMAGE returns the invoking image index for the specified or current team, or
cosubscript values for a COARRAY and optional DIM.

**Diagnostic obligation:** not-required.

**Facets:** `THIS_IMAGE-current-single-image-index-one`, `THIS_IMAGE-TEAM-image-index-out-of-scope`, `THIS_IMAGE-COARRAY-cosubscript-vector-out-of-scope`, `THIS_IMAGE-COARRAY-DIM-cosubscript-out-of-scope`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only exact properties fixed by 16.9:
type, kind, rank, shape, allocation bounds, integer counts, token positions, sequence
contents, model-inquiry relations, or bit-context observations. Processor-dependent
approximations, processor-dependent model values, and image/team-dependent behavior are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json for TANH, TANPI,
TEAM_NUMBER, THIS_IMAGE, TINY, TOKENIZE, TRAILZ, and S16.9.205 through S16.9.211 found
no fixture-manifest binding for these sections in this worktree. Existing clause 10
source files mention TEAM_NUMBER and THIS_IMAGE, and one Python test helper excludes
THIS_IMAGE text; those incidental uses are recorded as existing coverage and are not
rebound by this source packet. Every image, team, coarray, DIM, and cosubscript runtime
observation, including single-image THIS_IMAGE() results, is OUT OF SCOPE for this
single-image suite by user decision; these entries record source facts only and no
diagnostic facets.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, kind, declaration,
expression, assignment, array, character, allocation, execution, and I/O foundations
from clauses 6 through 13 where already catalogued. Unregistered dependencies retained:
clause 5 image and team execution terminology, clause 7 type and kind rules, clause 10
expression and intrinsic-assignment semantics, clause 16.3 integer bit model, clause
16.4 real model parameters, ISO_FORTRAN_ENV TEAM_TYPE, sibling 16.9 intrinsics named by
pending plans such as BIT_SIZE, BTEST, EXPONENT, IBSET, KIND, LEN, MAXEXPONENT,
MINEXPONENT, RADIX, RANK, SELECTED_*_KIND, and SIZE, and clauses 14 through 19 where
future fixtures need program, procedure, IEEE, module, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.208 -->
