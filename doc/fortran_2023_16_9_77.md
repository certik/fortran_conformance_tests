# Fortran 2023 16.9.77: EOSHIFT

The canonical catalogue is `doc/catalogues/eoshift_16_9_77.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.77")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, table16.4, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN INTRINSICS 16.9 H FIXTURES -->
## Intrinsics 16.9 H runtime observations

The `intrinsics_16_9_h` generator supplies 6 requirement bindings covering 16 portable facets in this section. Oracles use direct result inquiries, same-model DIGITS relationships, exact integer/logical/character/small-real results, and DSHIFT BTEST observations. DPROD processor-dependent approximation latitude and SHOULD guidance remain pending.
<!-- END INTRINSICS 16.9 H FIXTURES -->

<!-- BEGIN GENERATED 16.9.77 -->

### S16.9.77-001: EOSHIFT ARRAY, SHIFT, BOUNDARY, and DIM argument restrictions

**Source:** S16.9.77, J3/24-007, 18 December 2023, physical PDF405. **Class:** Restriction.

**Definition:** ARRAY shall be an array of any type; SHIFT shall be integer and scalar for rank-one
ARRAY or scalar/rank n-1 with the required shape otherwise; BOUNDARY, when present,
shall have ARRAY type and type parameters and scalar or the required rank n-1 shape; DIM
shall be an integer scalar in range 1 through rank(ARRAY).

**Diagnostic obligation:** not-required.

**Facets:** `EOSHIFT-ARRAY-array-any-type`, `EOSHIFT-SHIFT-integer-scalar-or-conforming-shape`, `EOSHIFT-BOUNDARY-same-type-params-scalar-or-conforming-shape`, `EOSHIFT-DIM-integer-scalar-valid-range`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.77-001 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.77-001 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.77-002: EOSHIFT BOUNDARY may be absent for Table 16.4 intrinsic types

**Source:** S16.9.77, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** BOUNDARY is permitted to be absent for the ARRAY types listed in Table 16.4.

**Diagnostic obligation:** not-required.

**Facets:** `EOSHIFT-BOUNDARY-absent-table-types-permitted`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.77-002 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.77-002 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.77-003: EOSHIFT BOUNDARY absence is limited to Table 16.4 types

**Source:** S16.9.77, J3/24-007, 18 December 2023, physical PDF405. **Class:** Restriction.

**Definition:** BOUNDARY is permitted to be absent only for the ARRAY types in Table 16.4.

**Diagnostic obligation:** not-required.

**Facets:** `EOSHIFT-BOUNDARY-absence-only-table-types`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.77-003 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.77-003 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.77-004: EOSHIFT absent BOUNDARY uses the Table 16.4 default converted to ARRAY kind or length

**Source:** S16.9.77, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** When BOUNDARY is absent for a Table 16.4 type, it is as if present with the listed
scalar value, converted if necessary to ARRAY kind type parameter or character length.

**Diagnostic obligation:** not-required.

**Facets:** `EOSHIFT-default-boundary-integer-zero`, `EOSHIFT-default-boundary-real-zero`, `EOSHIFT-default-boundary-complex-zero`, `EOSHIFT-default-boundary-logical-false`, `EOSHIFT-default-boundary-character-blanks`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.77-004 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.77-004 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.77-005: EOSHIFT absent DIM is as DIM=1

**Source:** S16.9.77, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** If DIM is absent, EOSHIFT behaves as if DIM were present with value 1.

**Diagnostic obligation:** not-required.

**Facets:** `EOSHIFT-DIM-absent-defaults-to-one`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.77-005 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.77-005 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.77-006: EOSHIFT result has ARRAY type, type parameters, and shape

**Source:** S16.9.77, J3/24-007, 18 December 2023, physical PDF406. **Class:** Effect.

**Definition:** The EOSHIFT result has the type, type parameters, and shape of ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `EOSHIFT-result-type-params-shape-of-array`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.77-006 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.77-006 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.77-007: EOSHIFT maps elements by SHIFT and supplies BOUNDARY outside bounds

**Source:** S16.9.77, J3/24-007, 18 December 2023, physical PDF406. **Class:** Effect.

**Definition:** Element (s1,...,sn) of EOSHIFT has ARRAY at index s_DIM+SHIFT (or the corresponding
SHIFT element) when that subscript is within bounds, and otherwise has the scalar or
indexed BOUNDARY value.

**Diagnostic obligation:** not-required.

**Facets:** `EOSHIFT-positive-shift-element-mapping`, `EOSHIFT-negative-shift-element-mapping`, `EOSHIFT-array-valued-shift-indexing`, `EOSHIFT-boundary-used-outside-bounds`.

**Oracle:** Original source registration only; selected facets now have bounded runtime fixtures and
mutation plans. Future fixtures use exact integer, logical, character, shape, kind, and
bit-pattern observations, and use real values only where the selected operation is exact
or where the standard defines an inquiry-model value. Every positive plan establishes a
non-default state first, uses a conforming companion where a zero or .FALSE. result
could be vacuous, and includes a feature mutation such as changing the intrinsic name,
argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

S16.9.77-007 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Processor-dependent
approximations are never given exact-value expected results; exact plans are limited to
integer/logical/character/shape/kind/bit effects and exactly representable real
inquiry-model properties. Grep of tests/ and tests/fixtures/*/fixture.json for DIM,
DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, and EPSILON found no existing fixture
binding in this worktree.

S16.9.77-007 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

<!-- END GENERATED 16.9.77 -->
