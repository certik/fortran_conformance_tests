# Fortran 2023 12.6.2.9: ID specifier

The canonical catalogue is `doc/catalogues/id_specifier_12_6_2_9.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.6.2.9 -->

### S12.6.2.9-001: Successful asynchronous ID transfer defines the ID variable with a processor-determined value

**Source:** S12.6.2.9, J3/24-007, 18 December 2023, physical PDF261. **Class:** Effect.

**Definition:** Successful execution of an asynchronous data transfer statement containing ID= causes
the ID variable to become defined with a processor-determined value.

**Diagnostic obligation:** not-required.

**Facets:** `successful-id-transfer-defines-id-variable`, `id-value-processor-determined`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required INQUIRE tokens, or required
diagnosis of a numbered syntax rule or constraint. Negative/source-control plans change
one property from the conforming control and do not require a particular diagnostic
message, severity, exit status, or printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording, processor-dependent numeric status values, external-file byte encodings,
buffering, timing, and actual overlap of asynchronous operations are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, earlier clause 12 connection/access
catalogues, and existing clause 13 format/editing catalogues for source form, names,
labels, expressions, variables, units, access methods, internal files, formatting, and
existing mode descriptors where used by pending controls. Unregistered dependencies
explicitly retained: later 12.6 input/output-item rules; 12.7-12.11 transfer, status,
INQUIRE, WAIT, and asynchronous semantics; clause 15 defined I/O; clause 16 named I/O
status constants if future fixtures use them; clause 19 scope and lifetime controls.

### S12.6.2.9-002: A zero ID value denotes completed transfer

**Source:** S12.6.2.9, J3/24-007, 18 December 2023, physical PDF261. **Class:** Effect.

**Definition:** If the processor-determined ID value is zero, the data transfer operation has been
completed.

**Diagnostic obligation:** not-required.

**Facets:** `zero-id-means-transfer-completed`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required INQUIRE tokens, or required
diagnosis of a numbered syntax rule or constraint. Negative/source-control plans change
one property from the conforming control and do not require a particular diagnostic
message, severity, exit status, or printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording, processor-dependent numeric status values, external-file byte encodings,
buffering, timing, and actual overlap of asynchronous operations are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, earlier clause 12 connection/access
catalogues, and existing clause 13 format/editing catalogues for source form, names,
labels, expressions, variables, units, access methods, internal files, formatting, and
existing mode descriptors where used by pending controls. Unregistered dependencies
explicitly retained: later 12.6 input/output-item rules; 12.7-12.11 transfer, status,
INQUIRE, WAIT, and asynchronous semantics; clause 15 defined I/O; clause 16 named I/O
status constants if future fixtures use them; clause 19 scope and lifetime controls.

### S12.6.2.9-003: A nonzero ID value identifies a pending data transfer operation

**Source:** S12.6.2.9, J3/24-007, 18 December 2023, physical PDF261. **Class:** Effect.

**Definition:** A nonzero ID value is the identifier of the data transfer operation.

**Diagnostic obligation:** not-required.

**Facets:** `nonzero-id-identifies-pending-transfer`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required INQUIRE tokens, or required
diagnosis of a numbered syntax rule or constraint. Negative/source-control plans change
one property from the conforming control and do not require a particular diagnostic
message, severity, exit status, or printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording, processor-dependent numeric status values, external-file byte encodings,
buffering, timing, and actual overlap of asynchronous operations are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, earlier clause 12 connection/access
catalogues, and existing clause 13 format/editing catalogues for source form, names,
labels, expressions, variables, units, access methods, internal files, formatting, and
existing mode descriptors where used by pending controls. Unregistered dependencies
explicitly retained: later 12.6 input/output-item rules; 12.7-12.11 transfer, status,
INQUIRE, WAIT, and asynchronous semantics; clause 15 defined I/O; clause 16 named I/O
status constants if future fixtures use them; clause 19 scope and lifetime controls.

### S12.6.2.9-004: Nonzero IDs are unique among pending operations for the unit

**Source:** S12.6.2.9, J3/24-007, 18 December 2023, physical PDF261. **Class:** Effect.

**Definition:** A nonzero identifier is different from the identifier of any other pending data transfer
operation for the same unit.

**Diagnostic obligation:** not-required.

**Facets:** `pending-id-unique-for-unit`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required INQUIRE tokens, or required
diagnosis of a numbered syntax rule or constraint. Negative/source-control plans change
one property from the conforming control and do not require a particular diagnostic
message, severity, exit status, or printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording, processor-dependent numeric status values, external-file byte encodings,
buffering, timing, and actual overlap of asynchronous operations are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, earlier clause 12 connection/access
catalogues, and existing clause 13 format/editing catalogues for source form, names,
labels, expressions, variables, units, access methods, internal files, formatting, and
existing mode descriptors where used by pending controls. Unregistered dependencies
explicitly retained: later 12.6 input/output-item rules; 12.7-12.11 transfer, status,
INQUIRE, WAIT, and asynchronous semantics; clause 15 defined I/O; clause 16 named I/O
status constants if future fixtures use them; clause 19 scope and lifetime controls.

### S12.6.2.9-005: ID values can identify operations in subsequent WAIT or INQUIRE statements

**Source:** S12.6.2.9, J3/24-007, 18 December 2023, physical PDF261. **Class:** Effect.

**Definition:** The ID value can be used in a subsequent WAIT or INQUIRE statement to identify the
particular data transfer operation.

**Diagnostic obligation:** not-required.

**Facets:** `id-usable-in-subsequent-wait`, `id-usable-in-subsequent-inquire`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required INQUIRE tokens, or required
diagnosis of a numbered syntax rule or constraint. Negative/source-control plans change
one property from the conforming control and do not require a particular diagnostic
message, severity, exit status, or printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording, processor-dependent numeric status values, external-file byte encodings,
buffering, timing, and actual overlap of asynchronous operations are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, earlier clause 12 connection/access
catalogues, and existing clause 13 format/editing catalogues for source form, names,
labels, expressions, variables, units, access methods, internal files, formatting, and
existing mode descriptors where used by pending controls. Unregistered dependencies
explicitly retained: later 12.6 input/output-item rules; 12.7-12.11 transfer, status,
INQUIRE, WAIT, and asynchronous semantics; clause 15 defined I/O; clause 16 named I/O
status constants if future fixtures use them; clause 19 scope and lifetime controls.

### S12.6.2.9-006: ID variable becomes undefined if an error occurs during the ID transfer

**Source:** S12.6.2.9, J3/24-007, 18 December 2023, physical PDF261. **Class:** Effect.

**Definition:** If an error condition occurs during execution of a data transfer statement containing
ID=, the ID variable becomes undefined.

**Diagnostic obligation:** not-required.

**Facets:** `id-variable-undefined-on-transfer-error`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required INQUIRE tokens, or required
diagnosis of a numbered syntax rule or constraint. Negative/source-control plans change
one property from the conforming control and do not require a particular diagnostic
message, severity, exit status, or printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording, processor-dependent numeric status values, external-file byte encodings,
buffering, timing, and actual overlap of asynchronous operations are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, earlier clause 12 connection/access
catalogues, and existing clause 13 format/editing catalogues for source form, names,
labels, expressions, variables, units, access methods, internal files, formatting, and
existing mode descriptors where used by pending controls. Unregistered dependencies
explicitly retained: later 12.6 input/output-item rules; 12.7-12.11 transfer, status,
INQUIRE, WAIT, and asynchronous semantics; clause 15 defined I/O; clause 16 named I/O
status constants if future fixtures use them; clause 19 scope and lifetime controls.

### S12.6.2.9-007: Child data transfer statements do not specify ID

**Source:** S12.6.2.9, J3/24-007, 18 December 2023, physical PDF261. **Class:** Restriction.

**Definition:** A child data transfer statement shall not specify the ID= specifier.

**Diagnostic obligation:** not-required.

**Facets:** `child-data-transfer-id-source-control`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required INQUIRE tokens, or required
diagnosis of a numbered syntax rule or constraint. Negative/source-control plans change
one property from the conforming control and do not require a particular diagnostic
message, severity, exit status, or printed rule number.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording, processor-dependent numeric status values, external-file byte encodings,
buffering, timing, and actual overlap of asynchronous operations are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, 11, earlier clause 12 connection/access
catalogues, and existing clause 13 format/editing catalogues for source form, names,
labels, expressions, variables, units, access methods, internal files, formatting, and
existing mode descriptors where used by pending controls. Unregistered dependencies
explicitly retained: later 12.6 input/output-item rules; 12.7-12.11 transfer, status,
INQUIRE, WAIT, and asynchronous semantics; clause 15 defined I/O; clause 16 named I/O
status constants if future fixtures use them; clause 19 scope and lifetime controls.

<!-- END GENERATED 12.6.2.9 -->
