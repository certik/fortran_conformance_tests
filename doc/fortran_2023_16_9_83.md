# Fortran 2023 16.9.83: EXECUTE_COMMAND_LINE

The canonical catalogue is `doc/catalogues/execute_command_line_16_9_83.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

Mixed splits: p3 separates each argument group and p4 separates conditional synchronous support from asynchronous permission. Command interpretation, exit status, positive error status, CMDMSG text, support, and timing are processor dependent.

<!-- BEGIN GENERATED 16.9.83 -->

### S16.9.83-001: EXECUTE_COMMAND_LINE executes a command line

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** EXECUTE_COMMAND_LINE is the intrinsic subroutine for executing a command line.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-description`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-002: EXECUTE_COMMAND_LINE is a subroutine

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** EXECUTE_COMMAND_LINE is a subroutine.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-subroutine-class`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-003: COMMAND argument restrictions for EXECUTE_COMMAND_LINE

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Restriction.

**Definition:** COMMAND shall be a default character scalar INTENT(IN) argument.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-command-default-character`, `execute-command-line-command-scalar`, `execute-command-line-command-intent-in`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-004: COMMAND value is the command line to execute

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** The value of COMMAND is the command line to be executed.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-command-value`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-005: COMMAND interpretation is processor dependent

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** The interpretation of COMMAND is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-command-interpretation-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-006: WAIT argument restrictions for EXECUTE_COMMAND_LINE

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Restriction.

**Definition:** Optional WAIT shall be a logical scalar INTENT(IN) argument.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-wait-logical-scalar`, `execute-command-line-wait-intent-in`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-007: WAIT false selects asynchronous execution only when supported

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If WAIT is present false and the processor supports asynchronous execution, the command
is executed asynchronously; otherwise it is executed synchronously.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-wait-false-async-if-supported`, `execute-command-line-wait-otherwise-sync`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-008: EXITSTAT argument restrictions for EXECUTE_COMMAND_LINE

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Restriction.

**Definition:** Optional EXITSTAT shall be an integer scalar with decimal exponent range at least nine
and is INTENT(INOUT).

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-exitstat-integer-scalar`, `execute-command-line-exitstat-range`, `execute-command-line-exitstat-intent-inout`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-009: Synchronous EXECUTE_COMMAND_LINE assigns EXITSTAT

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If the command is executed synchronously, EXITSTAT is assigned a value.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-exitstat-assigned-on-sync`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-010: Nonsynchronous EXECUTE_COMMAND_LINE leaves EXITSTAT unchanged

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If the command is not executed synchronously, EXITSTAT is unchanged.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-exitstat-unchanged-otherwise`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-011: CMDSTAT argument restrictions for EXECUTE_COMMAND_LINE

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Restriction.

**Definition:** Optional CMDSTAT shall be an integer scalar with decimal exponent range at least four
and is INTENT(OUT).

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdstat-integer-scalar`, `execute-command-line-cmdstat-range`, `execute-command-line-cmdstat-intent-out`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-012: CMDSTAT has exact values for unsupported, asynchronous fallback, and success conditions

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** CMDSTAT is assigned -1 if command-line execution is unsupported, -2 if asynchronous
execution is requested but unsupported, and zero otherwise.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdstat-minus-one-unsupported`, `execute-command-line-cmdstat-minus-two-async-unsupported`, `execute-command-line-cmdstat-zero-otherwise`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-013: CMDSTAT is positive when an EXECUTE_COMMAND_LINE error condition occurs

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If an error condition occurs, CMDSTAT is assigned a positive value.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdstat-positive-error`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-014: CMDMSG argument restrictions for EXECUTE_COMMAND_LINE

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Restriction.

**Definition:** Optional CMDMSG shall be a default character scalar INTENT(INOUT) argument.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdmsg-default-character`, `execute-command-line-cmdmsg-scalar`, `execute-command-line-cmdmsg-intent-inout`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-015: CMDMSG is assigned when an EXECUTE_COMMAND_LINE error condition occurs

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If an error condition occurs, CMDMSG is assigned an explanatory message.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdmsg-error-assigned`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-016: CMDMSG is unchanged when no error occurs

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If no error condition occurs, CMDMSG is unchanged.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdmsg-unchanged-otherwise`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-017: Processors with command-line execution support synchronous execution

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If a processor supports command-line execution, it shall support synchronous execution
of the command line.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-sync-required-if-supported`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-018: Processors may support asynchronous command-line execution

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** A processor may support asynchronous execution of the command line.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-async-permitted`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-019: EXECUTE_COMMAND_LINE return timing depends on synchronous execution

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** For synchronous execution EXECUTE_COMMAND_LINE returns after the command line completes;
otherwise it returns without waiting.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-sync-returns-after-completion`, `execute-command-line-async-returns-without-waiting`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-020: Absent CMDSTAT initiates error termination for reportable conditions

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** If a condition would assign a nonzero CMDSTAT value but CMDSTAT is absent, error
termination is initiated.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-absent-cmdstat-error-termination`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-021: Synchronous EXECUTE_COMMAND_LINE exit status value is processor dependent

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** The EXITSTAT value assigned for synchronous command execution is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-exitstat-value-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-022: CMDSTAT positive error value is processor dependent

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** The positive CMDSTAT value assigned for an EXECUTE_COMMAND_LINE error condition is
processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdstat-positive-value-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

### S16.9.83-023: CMDMSG error explanatory message text is processor dependent

**Source:** S16.9.83, J3/24-007, 18 December 2023, physical PDF408. **Class:** Effect.

**Definition:** The explanatory message assigned to CMDMSG for an EXECUTE_COMMAND_LINE error condition
is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-cmdmsg-error-text-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Future tests must not assume support for
command-line execution, a portable command language, a portable no-op command,
asynchronous support, elapsed-time behavior, command output, processor-dependent exit
status values, or CMDMSG wording. Exact CMDSTAT tokens may be asserted only in a profile
that first establishes the corresponding condition without relying on
processor-dependent command interpretation.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in 16.9 intrinsic descriptions are program restrictions but
are not numbered constraints and therefore carry no diagnostic facet in this packet.
Future negative tests need a separately owned numbered rule or syntax rule, a required
rejection, and a single-image portable observation.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, and 11 for source form, intrinsic
types, declarations, expressions, execution constructs, and existing source foundations.
Unregistered dependencies explicitly retained: neighbouring Clause 16 intrinsic
definitions and models, clause 14 image/coarray/event semantics where reached, clause 17
IEEE support where IEEE infinities or NaNs are used, clause 18 processor-dependency
terminology, and clause 19 scope/lifetime rules for future executable fixtures.

<!-- END GENERATED 16.9.83 -->
