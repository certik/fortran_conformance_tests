# Fortran 2023 16.9.41: BESSEL_JN

The canonical catalogue is `doc/catalogues/bessel_jn_16_9_41.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.41 -->

### S16.9.41-001: BESSEL_JN identifies Bessel functions of the first kind

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Effect.

**Definition:** BESSEL_JN is the intrinsic for Bessel functions of the first kind.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-first-kind-family`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.41-002: BESSEL_JN has elemental two-argument and transformational three-argument forms

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Effect.

**Definition:** BESSEL_JN(N,X) is elemental and BESSEL_JN(N1,N2,X) is transformational.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-two-argument-elemental-class`, `bessel-jn-three-argument-transformational-class`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.41-003: BESSEL_JN arguments have integer-order and real-X restrictions

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Restriction.

**Definition:** N shall be of type integer and nonnegative; N1 and N2 shall be integer scalars with
nonnegative values; X shall be real; for the transformational form X shall be scalar.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-n-integer-nonnegative`, `bessel-jn-n1-integer-scalar-nonnegative`, `bessel-jn-n2-integer-scalar-nonnegative`, `bessel-jn-x-real`, `bessel-jn-transformational-x-scalar`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.41-004: BESSEL_JN result characteristics match X and define the transformational extent

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Effect.

**Definition:** The result has the same type and kind as X; BESSEL_JN(N1,N2,X) returns a rank-one array
with extent MAX(N2 - N1 + 1, 0).

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-result-same-type-kind-as-x`, `bessel-jn-transformational-rank-one`, `bessel-jn-transformational-extent-max`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.41-005: BESSEL_JN(N,X) result approximates the order-N first-kind Bessel value

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Effect.

**Definition:** The two-argument result is an approximation to the Bessel function of the first kind and
order N of X.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-case-i-order-n-approximation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.41-006: BESSEL_JN(N,X) approximation is processor dependent

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Effect.

**Definition:** The approximation chosen for the two-argument BESSEL_JN result is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-case-i-processor-dependent-approximation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.41-007: BESSEL_JN(N1,N2,X) elements approximate successive first-kind Bessel orders

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Effect.

**Definition:** Element i of the three-argument result is an approximation to the Bessel function of the
first kind and order N1 + i - 1 of X.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-case-ii-successive-order-approximations`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.41-008: BESSEL_JN(N1,N2,X) element approximations are processor dependent

**Source:** S16.9.41, J3/24-007, 18 December 2023, physical PDF390. **Class:** Effect.

**Definition:** The approximation chosen for each three-argument BESSEL_JN result element is processor
dependent.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-jn-case-ii-processor-dependent-approximations`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

<!-- END GENERATED 16.9.41 -->
