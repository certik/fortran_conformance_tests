# Fortran 2023 12.5.6.2: OPEN statement syntax - OPEN fixtures

The canonical catalogue is `doc/catalogues/open_statement_syntax_12_5_6_2.json`. This author packet adds finite runtime and diagnostic fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 40 facets in 26 program(s); 32 of 37 facets are represented and 5 remain pending.

<!-- BEGIN GENERATED 12.5.6.2 -->

### R1204: OPEN statement syntax uses a connect-spec list

**Source:** 12.5.6.2 R1204; J3/24-007, 18 December 2023, physical PDF251. **Class:** Syntax.

**Definition:** The open-stmt syntax is OPEN followed by a parenthesized connect-spec-list.

**Diagnostic obligation:** required.

**Facets:** `open-parenthesized-connect-spec-list`, `open-missing-parenthesis-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 2 valid run-phase program(s) and 1 diagnostic
program(s) cover `open-missing-parenthesis-rejected`,
`open-parenthesized-connect-spec-list`. Runtime oracles use controlled stdout/stderr,
per-run relative files, INQUIRE properties, exact integer/character/logical values, and
only zero/nonzero IOSTAT distinctions. Diagnostics are line-anchored for numbered syntax
or constraints and have one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### R1205: OPEN connect-spec alternatives are the listed specifiers

**Source:** 12.5.6.2 R1205; J3/24-007, 18 December 2023, physical PDF252. **Class:** Syntax.

**Definition:** A connect-spec is one of the listed OPEN specifiers: unit, ACCESS, ACTION, ASYNCHRONOUS,
BLANK, DECIMAL, DELIM, ENCODING, ERR, FILE, FORM, IOMSG, IOSTAT, LEADING_ZERO, NEWUNIT,
PAD, POSITION, RECL, ROUND, SIGN, or STATUS.

**Diagnostic obligation:** required.

**Facets:** `connect-spec-unit`, `connect-spec-access`, `connect-spec-action`, `connect-spec-asynchronous`, `connect-spec-blank`, `connect-spec-decimal`, `connect-spec-delim`, `connect-spec-encoding`, `connect-spec-err`, `connect-spec-file`, `connect-spec-form`, `connect-spec-iomsg`, `connect-spec-iostat`, `connect-spec-leading-zero`, `connect-spec-newunit`, `connect-spec-pad`, `connect-spec-position`, `connect-spec-recl`, `connect-spec-round`, `connect-spec-sign`, `connect-spec-status`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 6 valid run-phase program(s) and 0 diagnostic
program(s) cover `connect-spec-access`, `connect-spec-action`, `connect-spec-blank`,
`connect-spec-decimal`, `connect-spec-delim`, `connect-spec-encoding`,
`connect-spec-err`, `connect-spec-file`, `connect-spec-form`, `connect-spec-iostat`,
`connect-spec-newunit`, `connect-spec-pad`, `connect-spec-position`,
`connect-spec-recl`, `connect-spec-round`, `connect-spec-sign`, `connect-spec-status`,
`connect-spec-unit`. Runtime oracles use controlled stdout/stderr, per-run relative
files, INQUIRE properties, exact integer/character/logical values, and only zero/nonzero
IOSTAT distinctions. Diagnostics are line-anchored for numbered syntax or constraints
and have one-property controls. No IOMSG text, processor-dependent unit number, physical
filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### R1206: FILE= uses a scalar default character expression

**Source:** 12.5.6.2 R1206; J3/24-007, 18 December 2023, physical PDF252. **Class:** Syntax.

**Definition:** A file-name-expr is a scalar default character expression.

**Diagnostic obligation:** required.

**Facets:** `file-name-expr-scalar-default-character`, `noncharacter-file-name-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 2 valid run-phase program(s) and 1 diagnostic
program(s) cover `file-name-expr-scalar-default-character`,
`noncharacter-file-name-rejected`. Runtime oracles use controlled stdout/stderr, per-run
relative files, INQUIRE properties, exact integer/character/logical values, and only
zero/nonzero IOSTAT distinctions. Diagnostics are line-anchored for numbered syntax or
constraints and have one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### R1207: IOMSG= uses a scalar default character variable

**Source:** 12.5.6.2 R1207; J3/24-007, 18 December 2023, physical PDF252. **Class:** Syntax.

**Definition:** An iomsg-variable is a scalar default character variable.

**Diagnostic obligation:** required.

**Facets:** `iomsg-variable-scalar-default-character`, `iomsg-nonvariable-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 1 diagnostic
program(s) cover `iomsg-nonvariable-rejected`. Runtime oracles use controlled
stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### C1203: No OPEN specifier appears more than once

**Source:** 12.5.6.2 C1203; J3/24-007, 18 December 2023, physical PDF252. **Class:** Restriction.

**Definition:** No specifier shall appear more than once in a given connect-spec-list.

**Diagnostic obligation:** required.

**Facets:** `duplicate-open-specifier-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 1 diagnostic
program(s) cover `duplicate-open-specifier-rejected`. Runtime oracles use controlled
stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### C1204: OPEN without NEWUNIT= specifies a file-unit-number in the required position

**Source:** 12.5.6.2 C1204; J3/24-007, 18 December 2023, physical PDF252. **Class:** Restriction.

**Definition:** If NEWUNIT= does not appear, a file-unit-number shall be specified; if UNIT= is omitted,
the file-unit-number shall be first in the connect-spec-list.

**Diagnostic obligation:** required.

**Facets:** `unit-required-without-newunit`, `bare-unit-first`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 2 valid run-phase program(s) and 2 diagnostic
program(s) cover `bare-unit-first`, `unit-required-without-newunit`. Runtime oracles use
controlled stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### C1205: OPEN with NEWUNIT= shall not include a file-unit-number

**Source:** 12.5.6.2 C1205; J3/24-007, 18 December 2023, physical PDF252. **Class:** Restriction.

**Definition:** If NEWUNIT= appears, a file-unit-number shall not appear.

**Diagnostic obligation:** required.

**Facets:** `newunit-excludes-file-unit-number`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 1 diagnostic
program(s) cover `newunit-excludes-file-unit-number`. Runtime oracles use controlled
stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### C1206: ERR= label is a branch target in the same inclusive scope

**Source:** 12.5.6.2 C1206; J3/24-007, 18 December 2023, physical PDF252. **Class:** Restriction.

**Definition:** The label used in ERR= shall be the statement label of a branch target statement in the
same inclusive scope as the OPEN statement.

**Diagnostic obligation:** required.

**Facets:** `err-label-branch-target-same-scope`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 1 diagnostic
program(s) cover `err-label-branch-target-same-scope`. Runtime oracles use controlled
stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.2-001: OPEN character specifier values ignore trailing blanks and case

**Source:** S12.5.6.2, J3/24-007, 18 December 2023, physical PDF252. **Class:** Effect.

**Definition:** Some OPEN specifiers have limited character values; trailing blanks are ignored, values
are interpreted without regard to case, and some specifiers have defaults when omitted.

**Diagnostic obligation:** not-required.

**Facets:** `limited-character-values`, `trailing-blanks-ignored`, `case-insensitive-values`, `omitted-specifier-defaults-exist`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 0 diagnostic
program(s) cover `case-insensitive-values`, `limited-character-values`,
`trailing-blanks-ignored`. Runtime oracles use controlled stdout/stderr, per-run
relative files, INQUIRE properties, exact integer/character/logical values, and only
zero/nonzero IOSTAT distinctions. Diagnostics are line-anchored for numbered syntax or
constraints and have one-property controls. No IOMSG text, processor-dependent unit
number, physical filename, or specific nonzero status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.2-002: OPEN IOSTAT, ERR, and IOMSG specifiers are described by 12.11

**Source:** S12.5.6.2, J3/24-007, 18 December 2023, physical PDF252. **Class:** Effect.

**Definition:** The IOSTAT=, ERR=, and IOMSG= specifiers for OPEN are described in 12.11.

**Diagnostic obligation:** not-required.

**Facets:** `open-error-specifiers-defer-to-12-11`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

I/O OPEN fixture implementation: 1 valid run-phase program(s) and 0 diagnostic
program(s) cover `open-error-specifiers-defer-to-12-11`. Runtime oracles use controlled
stdout/stderr, per-run relative files, INQUIRE properties, exact
integer/character/logical values, and only zero/nonzero IOSTAT distinctions. Diagnostics
are line-anchored for numbered syntax or constraints and have one-property controls. No
IOMSG text, processor-dependent unit number, physical filename, or specific nonzero
status is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself is a numbered constraint or syntax rule that requires
rejection and a future fixture can observe the rejection portably. Program restrictions
expressed only as prose shall-statements are not diagnostic facets here. IOMSG wording
and processor-dependent numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.5.6.2 -->

## I/O OPEN fixture derivations

### `open_parenthesized_control` / `R1204`

**Facets:** `open-parenthesized-connect-spec-list`.

R1204 defines OPEN(connect-spec-list); a parenthesized NEWUNIT/STATUS list opens a scratch unit.

Feature mutations: `remove-open-statement`.

### `connect_spec_modes` / `R1205`

**Facets:** `connect-spec-unit`, `connect-spec-access`, `connect-spec-action`, `connect-spec-blank`, `connect-spec-decimal`, `connect-spec-delim`, `connect-spec-encoding`, `connect-spec-file`, `connect-spec-form`, `connect-spec-iostat`, `connect-spec-pad`, `connect-spec-round`, `connect-spec-sign`.

R1205 lists OPEN connect-spec alternatives; UNIT, FILE, IOSTAT and listed character mode specifiers are observed by successful OPEN and INQUIRE tokens fixed by those specifiers.

Feature mutations: `unit-spec-other-unit`, `file-spec-other-name`, `access-spec-stream`, `action-spec-read`, `blank-spec-null`, `decimal-spec-point`, `delim-spec-apostrophe`, `encoding-spec-utf8`, `form-spec-unformatted`, `iostat-spec-omitted`, `pad-spec-yes`, `round-spec-down`, `sign-spec-suppress`.

### `connect_spec_newunit` / `R1205`

**Facets:** `connect-spec-newunit`.

R1205 includes NEWUNIT=scalar-int-variable; OPEN assigns a valid negative unit to that variable.

Feature mutations: `newunit-spec-other-variable`.

### `connect_spec_recl_direct` / `R1205`

**Facets:** `connect-spec-recl`.

R1205 includes RECL=scalar-int-expr; direct formatted OPEN reports the specified exact RECL value.

Feature mutations: `recl-spec-changed`.

### `connect_spec_position_append` / `R1205`

**Facets:** `connect-spec-position`.

R1205 includes POSITION=; POSITION='APPEND' makes the subsequent write follow the two existing records.

Feature mutations: `position-spec-rewind`.

### `connect_spec_status_replace` / `R1205`

**Facets:** `connect-spec-status`.

R1205 includes STATUS=; STATUS='REPLACE' deletes any existing file and creates an empty replacement, so an immediate read has nonzero IOSTAT.

Feature mutations: `status-spec-old`.

### `connect_spec_err_branch` / `R1205`

**Facets:** `connect-spec-err`.

R1205 includes ERR=label; an OLD nonexistent file error transfers control to the branch target.

Feature mutations: `err-spec-existing-file-no-branch`.

### `file_name_expr_variable` / `R1206`

**Facets:** `file-name-expr-scalar-default-character`.

R1206 defines FILE= as a scalar default character expression; a padded character variable names the file.

Feature mutations: `file-name-expr-other-variable`.

### `limited_values_case_trailing` / `S12.5.6.2-001`

**Facets:** `limited-character-values`, `trailing-blanks-ignored`, `case-insensitive-values`.

12.5.6.2 p1 makes limited character values case-insensitive and ignores trailing blanks; INQUIRE reports the fixed BLANK, DECIMAL, and DELIM tokens.

Feature mutations: `limited-value-blank-null`, `trailing-value-decimal-point`, `case-value-delim-apostrophe`.

### `open_error_specifiers_defer` / `S12.5.6.2-002`

**Facets:** `open-error-specifiers-defer-to-12-11`.

12.5.6.2 p2 defers IOSTAT and ERR to 12.11; for OLD on a nonexistent file the portable oracle is only nonzero IOSTAT and control transfer to the branch label.

Feature mutations: `error-specifiers-existing-file`.

### `missing_parenthesis` / `R1204`

**Facets:** `open-missing-parenthesis-rejected`.

R1204 requires OPEN followed by a parenthesized connect-spec-list; the control adds only the parentheses.

Conforming control: `R1204_valid__io_open_12_5_6_missing_parenthesis_control`.

### `missing_parenthesis_control` / `R1204`

**Facets:** `open-missing-parenthesis-rejected`.

One-property control for R1204 missing-parenthesis: the OPEN statement differs only by the required parentheses.

Feature mutations: .

### `noncharacter_file_name` / `R1206`

**Facets:** `noncharacter-file-name-rejected`.

R1206 requires FILE= to be a scalar default character expression; the control changes only FILE= to a string.

Conforming control: `R1206_valid__io_open_12_5_6_noncharacter_file_name_control`.

### `noncharacter_file_name_control` / `R1206`

**Facets:** `noncharacter-file-name-rejected`.

One-property control for R1206 noncharacter FILE=: the FILE= value is a default character expression.

Feature mutations: .

### `iomsg_nonvariable` / `R1207`

**Facets:** `iomsg-nonvariable-rejected`.

R1207 requires IOMSG= to be a scalar default character variable; the control uses a character variable.

Conforming control: `R1207_valid__io_open_12_5_6_iomsg_nonvariable_control`.

### `iomsg_nonvariable_control` / `R1207`

**Facets:** `iomsg-nonvariable-rejected`.

One-property control for R1207 IOMSG=: the specifier designates a scalar default character variable.

Feature mutations: .

### `duplicate_unit_specifier` / `C1203`

**Facets:** `duplicate-open-specifier-rejected`.

C1203 prohibits duplicate specifiers in one connect-spec-list; the control removes the second UNIT=.

Conforming control: `C1203_valid__io_open_12_5_6_duplicate_unit_specifier_control`.

### `duplicate_unit_specifier_control` / `C1203`

**Facets:** `duplicate-open-specifier-rejected`.

One-property control for C1203 duplicate specifier: the second UNIT= specifier is absent.

Feature mutations: .

### `unit_required_without_newunit` / `C1204`

**Facets:** `unit-required-without-newunit`.

C1204 requires a file-unit-number when NEWUNIT= is absent; the control adds UNIT=10.

Conforming control: `C1204_valid__io_open_12_5_6_unit_required_without_newunit_control`.

### `unit_required_without_newunit_control` / `C1204`

**Facets:** `unit-required-without-newunit`.

One-property control for C1204 missing file-unit-number: UNIT=10 is supplied.

Feature mutations: .

### `bare_unit_not_first` / `C1204`

**Facets:** `bare-unit-first`.

C1204 requires a bare file-unit-number without UNIT= to be first in the connect-spec-list; the control moves it first.

Conforming control: `C1204_valid__io_open_12_5_6_bare_unit_first_control`.

### `bare_unit_first_control` / `C1204`

**Facets:** `bare-unit-first`.

One-property control for C1204 bare-unit placement: the bare file-unit-number is first.

Feature mutations: .

### `newunit_excludes_unit` / `C1205`

**Facets:** `newunit-excludes-file-unit-number`.

C1205 prohibits a file-unit-number when NEWUNIT= appears; the control removes the bare unit.

Conforming control: `C1205_valid__io_open_12_5_6_newunit_excludes_unit_control`.

### `newunit_excludes_unit_control` / `C1205`

**Facets:** `newunit-excludes-file-unit-number`.

One-property control for C1205 NEWUNIT/file-unit-number exclusion: the bare unit is absent.

Feature mutations: .

### `err_label_undefined` / `C1206`

**Facets:** `err-label-branch-target-same-scope`.

C1206 requires ERR= to name a branch target in the same inclusive scope; the control defines label 99.

Conforming control: `C1206_valid__io_open_12_5_6_err_label_undefined_control`.

### `err_label_undefined_control` / `C1206`

**Facets:** `err-label-branch-target-same-scope`.

One-property control for C1206 ERR= label: label 99 is a branch target in the same inclusive scope.

Feature mutations: .

