# Fortran 2023: 10.1.2.6 Level-3 expressions

Catalogue: `doc/catalogues/level_3_expressions_10_1_2_6.json`. The original source-only registration supplied pending plans; the bounded batch295 fixtures below supply selected cases and mutation/diagnostic plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN EXPRESSIONS 10.1.2.5-10.1.2.7 FIXTURES -->
## Batch295 expression fixtures for 10.1.2.6

Generated batch295 fixtures cover selected facets for: S10.1.2.6-001, R1011, R1012. Runtime cases use exact integer, logical, and character LEN/value oracles; diagnostic cases are line-anchored one-property malformed forms with conforming repairs documented in generator metadata. Feature mutations, including concat operator replacements with defined .cat. controls, are permanent and checked on both the reference gfortran and frozen LFortran target. Facets not listed remain pending for the recorded reasons.
<!-- END EXPRESSIONS 10.1.2.5-10.1.2.7 FIXTURES -->

<!-- BEGIN GENERATED 10.1.2.6 -->

### S10.1.2.6-001: Level-3 expressions are the character concatenation layer over level-2 expressions

**Source:** 10.1.2.6 p1; J3/24-007 PDF p170. **Class:** Syntax.

**Definition:** Level-3 expressions are level-2 expressions optionally involving concat-op. This places
concatenation above relational and logical operations but below numeric level-2 syntax
in the grammar ladder.

**Diagnostic obligation:** not-required.

**Facets:** `level-2-base`, `concatenation-layer`, `left-concatenation-chain`.

**Oracle:** Intrinsic character controls use fixed character literals and length checks only for
admission and final value, not grouping. Grouping-sensitive future controls may instead
use a non-associative defined extension of // on a derived type, with independently
computed component-value oracles and valid interfaces under 10.1.6 and 15.4.3.4.2.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: character fixtures observe a level-2
character expression by direct LEN/value checks, intrinsic concatenation 'ab'//'cd' with
LEN 4 and value 'abcd', and a non-associative defined extension of // where an
unparenthesized three-operand chain produces the left-grouped value.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 10.1.2.5R1007;10.1.3 Table 10.1;10.1.5.3;10.1.6;10.1.9;10.2.1.3;15.4.3.4.2.

### R1011: A level-3 expression is a left-recursive chain of level-2 expressions joined by concat-op

**Source:** 10.1.2.6 R1011; J3/24-007 PDF p170. **Class:** Syntax.

**Definition:** The level-3-expr syntax is either a level-2-expr or a level-3-expr followed by concat-op
and level-2-expr, making unparenthesized concatenation left-recursive in the grammar.

**Diagnostic obligation:** required.

**Facets:** `single-level-2`, `single-concat`, `left-recursive-concat`, `missing-right-level-2-rejected`.

**Oracle:** Missing-operand negatives must keep concat-op valid and operands otherwise declared;
type mismatch belongs to 10.1.5.3, not R1011.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: character fixtures observe a single
level-2 expression, a single intrinsic concat, and a left-recursive defined-// chain; a
diagnostic fixture and compiled one-property control isolate a missing level-2
expression after a valid '//'.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 4.2;10.1.2.5R1007;10.1.2.6R1012;10.1.5.3;10.1.6;15.4.3.4.2.

### R1012: The concatenation operator token is double slash

**Source:** 10.1.2.6 R1012; J3/24-007 PDF p170. **Class:** Syntax.

**Definition:** The concat-op token is '//'. It is the token used by R1011 and does not define character
operand validity by itself.

**Diagnostic obligation:** required.

**Facets:** `double-slash-token`, `single-slash-not-concat`.

**Oracle:** The negative must force the concat-op slot with character operands so a single slash is
not merely parsed as division in a numeric expression.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: the intrinsic concatenation fixture
observes the '//' token with direct LEN('ab'//'cd') and value checks; a feature mutation
replaces the construct with another conforming concatenation of different length/value.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property. The
single-slash-not-concat facet remains pending because single slash is a valid mult-op
token before character operand validity is considered.

**Dependencies:** 4.2;6.2.4R608;10.1.2.6R1011;10.1.5.3.

<!-- END GENERATED 10.1.2.6 -->
