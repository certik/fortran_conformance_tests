# Fortran 2023 16.9.152: NINT

The canonical catalogue is `doc/catalogues/nint_intrinsic_16_9_152.json`. Effective review state is computed by the registry; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.152 -->

### S16.9.152-001: NINT argument restrictions

**Source:** S16.9.152, J3/24-007, 18 December 2023, physical PDF440. **Class:** Restriction.

**Definition:** A shall be real; optional KIND shall be a scalar integer constant expression.

**Diagnostic obligation:** not-required.

**Facets:** `NINT-A-real`, `NINT-KIND-scalar-integer-constant-expression`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.152-002: NINT result is integer with KIND selection

**Source:** S16.9.152, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** The NINT result is integer; with KIND present its kind type parameter is the value of
KIND, and with KIND absent it is default integer.

**Diagnostic obligation:** not-required.

**Facets:** `NINT-result-integer`, `NINT-result-kind-from-KIND`, `NINT-result-default-kind-absent`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.152-003: NINT result selects nearest integer with greater-magnitude tie break

**Source:** S16.9.152, J3/24-007, 18 December 2023, physical PDF440. **Class:** Effect.

**Definition:** The NINT result is the integer nearest A, with ties resolved to the integer of greater
magnitude.

**Diagnostic obligation:** not-required.

**Facets:** `NINT-nearest-integer`, `NINT-halfway-greater-magnitude`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.152 -->
