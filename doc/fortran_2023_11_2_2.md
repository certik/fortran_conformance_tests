# Fortran 2023: 11.2.2 GO TO statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN BLOCK BRANCH 11.1.4 11.2 FIXTURES -->
## BLOCK/branch executable fixture packet for 11.2.2

This packet adds generated f2023 fixtures for R1159, C1174, S11.2.2-001. Runtime cases use integer sentinels and exact completion output. Diagnostic cases require a located error at the violating line and exclude unsupported-feature/internal-error routes. Pure processor latitude and facets whose single-image oracle is not isolated remain pending in the catalogue.
<!-- END BLOCK BRANCH 11.1.4 11.2 FIXTURES -->

<!-- BEGIN GENERATED 11.2.2 -->

### R1159: GO TO statement syntax names a target label

**Source:** 11.2.2 R1159; J3/24-007 PDF p227. **Class:** Syntax.

**Definition:** A goto-stmt is the keywords GO TO followed by a label.

**Diagnostic obligation:** required.

**Facets:** `go-to-label-form`, `label-token-required`.

**Oracle:** Syntax admission only; target validity and execution transfer are owned by C1174 and p1.

R1159 block-branch fixture family: a run/positive-control/f2023 GO TO has the keyword
pair followed by a valid numeric label and reaches that target; the label-token negative
removes only the label token from the same complete source shape.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must isolate the cited source unit, use a conforming positive control
when a diagnostic is expected, and observe control transfer by which statement executes
next rather than by assumed branch implementation, compiler wording, process status, or
agreement with another processor.

R1159 block-branch fixture boundaries: only the listed facets are represented by this
generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 6.4 labels registered;11.2.1 branch target concept and C1174 in this packet.

### C1174: A GO TO label designates a branch target statement in the same inclusive scope

**Source:** 11.2.2 C1174; J3/24-007 PDF p228. **Class:** Restriction.

**Definition:** The label in a GO TO statement shall be the statement label of a branch target statement
that appears in the same inclusive scope as the goto-stmt.

**Diagnostic obligation:** required.

**Facets:** `go-to-valid-target-control`, `go-to-nonbranch-target-rejected`, `go-to-target-outside-inclusive-scope-rejected`.

**Oracle:** Each negative is attributed solely to C1174: target statement kind or
same-inclusive-scope relation. The control must satisfy R1159 and execute neither
unrelated construct exits nor alternate-return/I/O branches.

C1174 block-branch fixture family: a run/positive-control/f2023 GO TO targets a labeled
CONTINUE action statement in the same inclusive scope. The nonbranch-target negative
changes only the target statement to FORMAT with the same label.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must isolate the cited source unit, use a conforming positive control
when a diagnostic is expected, and observe control transfer by which statement executes
next rather than by assumed branch implementation, compiler wording, process status, or
agreement with another processor.

C1174 block-branch fixture boundaries: only the listed facets are represented by this
generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** 11.2.1 branch target list;6.4 labels registered;scope details may depend on 19
association and unregistered procedure/block owners for some target kinds.

### S11.2.2-001: Executing GO TO branches to the statement identified by its label

**Source:** 11.2.2 p1; J3/24-007 PDF p228. **Class:** Effect.

**Definition:** Execution of a GO TO statement causes a branch to the branch target statement identified
by the label.

**Diagnostic obligation:** not-required.

**Facets:** `go-to-transfers-to-labeled-target`, `go-to-skips-fallthrough`.

**Oracle:** The observable effect is which statement executed next; target legality remains C1174.

S11.2.2-001 block-branch fixture family: one run/effect/f2023 GO TO program sets a
pre-branch sentinel, skips a poison fallthrough assignment, and executes the labeled
target as the next effective statement before a common continuation. Retargeting the GO
TO to a different valid label or removing the target update fails distinct assertions.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Branching effects must be observed with distinguishable sentinels for the statement
reached next; tests must not inspect implementation jump mechanics or attribute a
branch-target violation to more than one rule.

S11.2.2-001 block-branch fixture boundaries: only the listed facets are represented by
this generator. Other pending facets in the same requirement, including
ASYNCHRONOUS/VOLATILE/IMPORT BLOCK exceptions, processor-dependent BLOCK
specification-expression order, unsaved local undefined-on-exit behavior, USE/IMPORT
ordering, bare SAVE in BLOCK, FORMAT/DATA first statements, complete branch-cause
inventories, complete branch-target kind lists, and same-inclusive-scope negatives not
isolated here remain pending. Diagnostics require a located error, not a fixed wording,
rule number, fatal status, or LFortran agreement.

**Dependencies:** C1174 in this section;11.2.1 branch concept;Clause 10 assignment/expression effects
registered.

<!-- END GENERATED 11.2.2 -->
