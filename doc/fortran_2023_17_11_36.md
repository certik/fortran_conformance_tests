# Fortran 2023 17.11.36: IEEE_RINT

The canonical catalogue is `doc/catalogues/ieee_rint_17_11_36.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.36")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.36 -->

### S17.11.36-001: IEEE_RINT argument restrictions

**Source:** S17.11.36, J3/24-007, 18 December 2023, physical PDF501. **Class:** Restriction.

**Definition:** X shall be real and optional ROUND shall be of type IEEE_ROUND_TYPE.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_RINT-X-real`, `IEEE_RINT-ROUND-ieee-round-type`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.36-002: IEEE_RINT invocations require supported datatype

**Source:** S17.11.36, J3/24-007, 18 December 2023, physical PDF501. **Class:** Restriction.

**Definition:** IEEE_RINT shall not be invoked unless IEEE_SUPPORT_DATATYPE(X) is true.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_RINT-X-support-guard`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.36-003: IEEE_RINT result characteristics match X

**Source:** S17.11.36, J3/24-007, 18 December 2023, physical PDF501. **Class:** Effect.

**Definition:** The IEEE_RINT result has the same type, kind, and rank as X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_RINT-result-same-as-X`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.36-004: IEEE_RINT ROUND argument selects integral rounding mode

**Source:** S17.11.36, J3/24-007, 18 December 2023, physical PDF501. **Class:** Effect.

**Definition:** With ROUND present, IEEE_RINT rounds X to an integer according to the mode specified by
ROUND.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_RINT-ROUND-present-directed-modes`, `IEEE_RINT-ROUND-present-nearest-ties-to-even`, `IEEE_RINT-ROUND-present-nearest-no-inexact`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.36-005: IEEE_RINT without ROUND uses the current rounding mode

**Source:** S17.11.36, J3/24-007, 18 December 2023, physical PDF501. **Class:** Effect.

**Definition:** Without ROUND, IEEE_RINT returns roundToIntegralExact using the current rounding mode.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_RINT-ROUND-absent-current-mode`, `IEEE_RINT-current-nearest-ties-to-even`, `IEEE_RINT-current-nearest-inexact-signals-for-nonintegral`, `IEEE_RINT-current-nearest-inexact-quiet-for-integral`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

### S17.11.36-006: IEEE_RINT zero result keeps the sign of X

**Source:** S17.11.36, J3/24-007, 18 December 2023, physical PDF501. **Class:** Effect.

**Definition:** A zero IEEE_RINT result has the sign of X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_RINT-zero-result-sign-of-X`.

**Oracle:** Source registration only. Future Clause 17 fixtures use guarded branch plans: first
probe the named IEEE_SUPPORT_DATATYPE, IEEE_SUPPORT_FLAG(flag,X), IEEE_SUPPORT_HALTING,
IEEE_SUPPORT_ROUNDING, IEEE_SUPPORT_INF, IEEE_SUPPORT_NAN, and subnormal-support
inquiries, exercise only the supported branch, and treat the unsupported branch as a
skip. Flag plans use named shared preamble FLAG-HALTING-OFF(flag,X): after
IEEE_SUPPORT_FLAG(flag,X) succeeds, probe IEEE_SUPPORT_HALTING(flag); the
halting-supported branch calls IEEE_SET_HALTING_MODE(flag,.FALSE.); the
no-halting-support branch calls IEEE_GET_HALTING_MODE(flag,h) under its own guard and
proceeds only for h .FALSE., with h .TRUE. skipping that row. Exception rows then clear
all supported flags before the operation and assert every text-required quiet flag under
its own IEEE_SUPPORT_FLAG(flag,X) probe. Plans invoke restricted IEEE observer
procedures only after their own support probes. Exact assertions use representable
binary inputs, named IEEE constants, quiet and signaling NaN operands where applicable,
MINEXPONENT/DIGITS subnormal constructions, kind/rank/type checks, and IEEE_IS_NEGATIVE,
IEEE_CLASS, or IEEE_IS_NAN observers rather than processor-dependent encodings.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Case-insensitive grep of tests/ and tests/fixtures/*/fixture.json found no existing hits
for IEEE_REAL, IEEE_REM, IEEE_RINT, IEEE_SCALB, IEEE_SELECTED_REAL_KIND, or
IEEE_SET_FLAG in this worktree; those procedure names remain unbound here. The existing
tests/profiles/ieee_binary.f90 profile can guard future binary32/binary64 IEEE
arithmetic cases.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 where already
catalogued for source form, intrinsic types and kinds, declarations, expressions,
arrays, execution, I/O observation helpers, and intrinsic inquiry functions.
Unregistered dependencies retained: surrounding Clause 17 IEEE module procedures, named
IEEE constants and derived types, ISO/IEC 60559 operation terminology, and later Clause
17 support inquiries named by guarded plans.

<!-- END GENERATED 17.11.36 -->
