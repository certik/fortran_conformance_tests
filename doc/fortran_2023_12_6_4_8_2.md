# Fortran 2023 12.6.4.8.2: Characteristics of defined input/output procedures

The canonical catalogue is `doc/catalogues/defined_input_output_procedure_characteristics_12_6_4_8_2.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, R1221, C1236, C1237, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.6.4.8.2 -->

### S12.6.4.8.2-001: Defined input/output has four direction-and-form characteristic sets for each derived type and kind-parameter set

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Effect.

**Definition:** For a particular derived type and a particular set of kind type parameter values, there
are four possible sets of characteristics for defined input/output procedures: formatted
input, formatted output, unformatted input, and unformatted output.

**Diagnostic obligation:** not-required.

**Facets:** `formatted-input-characteristic-set`, `formatted-output-characteristic-set`, `unformatted-input-characteristic-set`, `unformatted-output-characteristic-set`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-002: A program need not supply all four defined input/output procedures

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Effect.

**Definition:** The program is permitted to supply fewer than all four defined input/output procedures
for a derived type and kind-parameter set.

**Diagnostic obligation:** not-required.

**Facets:** `partial-defined-io-procedure-set-permitted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-003: Defined input/output procedures are specified by interface blocks or generic bindings

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Effect.

**Definition:** Procedures are specified for derived-type input/output by interface blocks or by generic
bindings with a defined-io-generic-spec.

**Diagnostic obligation:** not-required.

**Facets:** `defined-io-interface-block-specification`, `defined-io-generic-binding-specification`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-004: Defined-io generic specs map to the four direction-and-form cases

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Effect.

**Definition:** READ(FORMATTED), READ(UNFORMATTED), WRITE(FORMATTED), and WRITE(UNFORMATTED) are the
defined-io-generic-specs for formatted input, unformatted input, formatted output, and
unformatted output respectively.

**Diagnostic obligation:** not-required.

**Facets:** `read-formatted-maps-formatted-input`, `read-unformatted-maps-unformatted-input`, `write-formatted-maps-formatted-output`, `write-unformatted-maps-unformatted-output`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### R1221: dtv-type-spec syntax admits TYPE or CLASS of a derived-type specifier

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Syntax.

**Definition:** A dtv-type-spec is TYPE(derived-type-spec) or CLASS(derived-type-spec).

**Diagnostic obligation:** required.

**Facets:** `type-dtv-type-spec-form`, `class-dtv-type-spec-form`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### C1236: Extensible dtv types use CLASS and nonextensible dtv types use TYPE

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Restriction.

**Definition:** If derived-type-spec specifies an extensible type, the CLASS keyword shall be used;
otherwise, the TYPE keyword shall be used.

**Diagnostic obligation:** required.

**Facets:** `extensible-dtv-uses-class`, `nonextensible-dtv-uses-type`, `extensible-type-keyword-rejected`, `nonextensible-class-keyword-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### C1237: All length type parameters of the dtv derived-type specifier are assumed

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Restriction.

**Definition:** All length type parameters of derived-type-spec in a dtv-type-spec shall be assumed.

**Diagnostic obligation:** required.

**Facets:** `dtv-length-parameters-assumed`, `explicit-dtv-length-parameter-rejected`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-005: READ(FORMATTED) procedures have the formatted input dummy characteristics

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Restriction.

**Definition:** A READ(FORMATTED) defined input/output procedure shall have the same characteristics as
the formatted input interface: dtv is a dtv-type-spec dummy with INTENT(INOUT), unit is
INTEGER INTENT(IN), iotype is CHARACTER(LEN=*) INTENT(IN), v_list is INTEGER INTENT(IN)
rank-one array, iostat is INTEGER INTENT(OUT), and iomsg is CHARACTER(LEN=*)
INTENT(INOUT).

**Diagnostic obligation:** not-required.

**Facets:** `formatted-read-dtv-inout`, `formatted-read-unit-integer-in`, `formatted-read-iotype-character-in`, `formatted-read-v-list-integer-array-in`, `formatted-read-iostat-integer-out`, `formatted-read-iomsg-character-inout`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-006: READ(UNFORMATTED) procedures have the unformatted input dummy characteristics

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Restriction.

**Definition:** A READ(UNFORMATTED) defined input/output procedure shall have the same characteristics
as the unformatted input interface: dtv is a dtv-type-spec dummy with INTENT(INOUT),
unit is INTEGER INTENT(IN), iostat is INTEGER INTENT(OUT), and iomsg is CHARACTER(LEN=*)
INTENT(INOUT).

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-read-dtv-inout`, `unformatted-read-unit-integer-in`, `unformatted-read-iostat-integer-out`, `unformatted-read-iomsg-character-inout`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-007: WRITE(FORMATTED) procedures have the formatted output dummy characteristics

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Restriction.

**Definition:** A WRITE(FORMATTED) defined input/output procedure shall have the same characteristics as
the formatted output interface: dtv is a dtv-type-spec dummy with INTENT(IN), unit is
INTEGER INTENT(IN), iotype is CHARACTER(LEN=*) INTENT(IN), v_list is INTEGER INTENT(IN)
rank-one array, iostat is INTEGER INTENT(OUT), and iomsg is CHARACTER(LEN=*)
INTENT(INOUT).

**Diagnostic obligation:** not-required.

**Facets:** `formatted-write-dtv-in`, `formatted-write-unit-integer-in`, `formatted-write-iotype-character-in`, `formatted-write-v-list-integer-array-in`, `formatted-write-iostat-integer-out`, `formatted-write-iomsg-character-inout`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-008: WRITE(UNFORMATTED) procedures have the unformatted output dummy characteristics

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Restriction.

**Definition:** A WRITE(UNFORMATTED) defined input/output procedure shall have the same characteristics
as the unformatted output interface: dtv is a dtv-type-spec dummy with INTENT(IN), unit
is INTEGER INTENT(IN), iostat is INTEGER INTENT(OUT), and iomsg is CHARACTER(LEN=*)
INTENT(INOUT).

**Diagnostic obligation:** not-required.

**Facets:** `unformatted-write-dtv-in`, `unformatted-write-unit-integer-in`, `unformatted-write-iostat-integer-out`, `unformatted-write-iomsg-character-inout`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.2-009: Specific procedure names and dummy argument names in the model interfaces are arbitrary

**Source:** S12.6.4.8.2, J3/24-007, 18 December 2023, physical PDF268. **Class:** Effect.

**Definition:** The actual specific procedure names shown in the model interfaces are not significant,
and the dummy argument names used in the discussion are arbitrary.

**Diagnostic obligation:** not-required.

**Facets:** `specific-procedure-names-arbitrary`, `defined-io-dummy-names-arbitrary`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

<!-- END GENERATED 12.6.4.8.2 -->
