# Fortran 2023: 7.5.4.6 Default initialization for components

Reviewed catalogue and fixtures: `doc/catalogues/derived_types_7_5_4_6.json`.
The checksum-pinned J3/24-007, dated 18 December 2023, supplies
the original source on PDF 96-98. Independent source-plan review preceded
127 authored cases: 48 runtime witnesses and 79 compile cases.
They directly represent 91 facets; 25 canonical-use, event and classification
facets remain pending. Current compiler observations are separate from
individual fixture approval and whole-standard completion. Independent
adjudications are documented in `doc/source_audits/batch_012.json`.

Source-defined status, declaration admission, runtime effects and canonical
use graphs remain separate. Initializers never justify reading undefined
values or querying undefined pointer association, and a prose restriction
does not automatically create a standard-required diagnostic. All future
negative cases need cause-specific contracts and minimal valid repairs.

Every null initializer uses `NULL()` without arguments under R806/C813.
The valid prior-KIND-bound control remains even though GNU rejects it;
blanket parameterized-component rejection is not credited as a C767
nonconstant-bound diagnostic. Likewise, complaints that pointer target
values must be compile-time constants do not establish C769/C770 causes.
Reference failures do not turn the external-function or compatible
polymorphic-target controls into invalid programs.

<!-- BEGIN GENERATED 7.5.4.6 -->

### R743: Component initialization selects a constant value, a null initializer or an initial data target

**Source:** 7.5.4.6, R743, R743.constant-expression-alternative, R743.null-initialization-alternative, R743.initial-data-target-alternative; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Syntax.

**Definition:** A component-initialization is an equals sign followed by a constant expression, or an
arrow followed by null-init or initial-data-target. These alternatives do not grant
ordinary expression or pointer-assignment syntax indiscriminately: the component
attributes and descendants retain their separate restrictions. A procedure component
uses proc-decl and its own procedure-pointer initializer.

**Diagnostic obligation:** required.

**Facets:** `scalar-constant-value`, `array-constant-value`, `null-initializer`, `initial-data-target`, `missing-value-operand`, `missing-pointer-operand`, `initializer-descendant-use-graph`.

**Oracle:** Compile admission and minimal syntax repairs are separate from the p3-p6 runtime
effects. Use only exact small integer values and independently justified shape/target
premises.

**Oracle limitation:** C813 requires NULL() with no arguments for null-init; general NULL(MOLD=...) permissions
do not extend that use. An arbitrary function call is not thereby a constant expression
or an initial data target. Do not claim conversion, allocation or association effects
from parser acceptance. Every planned negative needs a source-minimal conforming repair
and a subject-specific primary diagnostic at its actual point or independently justified
bounded relation. Required reporting need not be fatal. Unsupported facilities,
unrelated recovery, crashes, verifier/resource failures and timeouts do not establish
the intended condition. Native diagnostic routes are limited to their observed or
explicitly prospective source-specific conditions. These definitions do not supply
fixture approval.

**Dependencies:** R737 and C766-C770, PDF93-96; R744 here; R806/C813, PDF118, requiring NULL() with no
arguments; 10.1.12, PDF187-188; 15.4.3.6/R1515/R1517/R1518, PDF333-334; 4.2 p2(3),
PDF46.

### R744: An initial data target is a designator

**Source:** 7.5.4.6, R744; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Syntax.

**Definition:** The initial-data-target syntax is a designator, not an arbitrary data expression or
procedure invocation. C770 further limits the object and designator selectors; C769
separately tests compatibility with the actual component.

**Diagnostic obligation:** required.

**Facets:** `named-variable-designator`, `array-element-designator`, `array-section-designator`, `substring-designator`, `arithmetic-expression-excluded`, `canonical-designator-graph`.

**Oracle:** Keep all object/selector/compatibility premises constant in the grammar contrast. Target
values are read only in separate effects after their definition is established.

**Oracle limitation:** A scalar designator versus an array section needs the actual resulting rank, not the
base object rank. A target expression cannot be rescued by a general pointer-assignment
permission. Every planned negative needs a source-minimal conforming repair and a
subject-specific primary diagnostic at its actual point or independently justified
bounded relation. Required reporting need not be fatal. Unsupported facilities,
unrelated recovery, crashes, verifier/resource failures and timeouts do not establish
the intended condition. Native diagnostic routes are limited to their observed or
explicitly prospective source-specific conditions. These definitions do not supply
fixture approval.

**Dependencies:** R743/C769/C770 here; 9.4.2 and9.5, PDF151-157; 8.5.16 and TARGET, PDF132-134.

### C766: Component initialization requires the double-colon separator

**Source:** 7.5.4.6, C766, C766.initialization-present, C766.double-colon-before-declaration-list; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Restriction.

**Definition:** When a data component declaration contains component-initialization, a double-colon
separator must precede its component-decl-list. The condition concerns the list
separator, not an array bound, substring colon or procedure-component declaration.

**Diagnostic obligation:** required.

**Facets:** `initialized-with-separator`, `uninitialized-without-separator`, `missing-separator`.

**Oracle:** The missing-separator contrast changes one declaration token sequence and preserves a
complete valid type body. It is not an EOF or name-lookup test.

**Oracle limitation:** Required-report evidence must identify the initialized component declaration separator.
Every planned negative needs a source-minimal conforming repair and a subject-specific
primary diagnostic at its actual point or independently justified bounded relation.
Required reporting need not be fatal. Unsupported facilities, unrelated recovery,
crashes, verifier/resource failures and timeouts do not establish the intended
condition. Native diagnostic routes are limited to their observed or explicitly
prospective source-specific conditions. These definitions do not supply fixture
approval.

**Dependencies:** R737/R743 and4.2 p2(3).

### C767: Initialized components have only constant or deferred type parameters and bounds

**Source:** 7.5.4.6, C767, C767.initialization-present, C767.every-type-parameter, C767.every-array-bound, C767.colon-or-constant-expression; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Restriction.

**Definition:** If component-initialization is present, every type parameter and every array bound of
that component must be a colon or a constant expression. Both the condition and the
universal every-parameter/every-bound qualification matter. A valid specification
expression is not necessarily a constant expression.

**Diagnostic obligation:** required.

**Facets:** `constant-parameters-and-bounds`, `deferred-pointer-parameters`, `deferred-pointer-shape`, `prior-kind-constant-primary`, `nonconstant-length`, `nonconstant-lower-bound`, `nonconstant-upper-bound`, `parameter-and-bound-use-graph`.

**Oracle:** Use legal parameter declarations in R726 order and separate KIND constant primaries from
LEN specification primaries. Repairs preserve the same uninitialized component rather
than changing its dynamic specification into a different test.

**Oracle limitation:** All independent component declaration restrictions remain in force. Colon is not
permission to initialize an ALLOCATABLE component with = or =>. Every planned negative
needs a source-minimal conforming repair and a subject-specific primary diagnostic at
its actual point or independently justified bounded relation. Required reporting need
not be fatal. Unsupported facilities, unrelated recovery, crashes, verifier/resource
failures and timeouts do not establish the intended condition. Native diagnostic routes
are limited to their observed or explicitly prospective source-specific conditions.
These definitions do not supply fixture approval.

**Dependencies:** 7.5.3.1 p5, PDF92; R737 and its constraints, PDF93-94; R743/C768 here;10.1.12,
PDF187-188.

### C768: Initializer punctuation restricts the component attributes

**Source:** 7.5.4.6, C768, C768.arrow-needs-pointer, C768.equals-excludes-pointer, C768.equals-excludes-allocatable; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Restriction.

**Definition:** An arrow in component-initialization requires POINTER in the component attributes. An
equals initializer permits neither POINTER nor ALLOCATABLE there. A data target and
NULL() share the arrow attribute condition, while allocatable initial status does not
come from an explicit component initializer.

**Diagnostic obligation:** required.

**Facets:** `pointer-null-arrow`, `pointer-target-arrow`, `ordinary-equals`, `null-arrow-without-pointer`, `target-arrow-without-pointer`, `equals-with-pointer`, `equals-with-allocatable`.

**Oracle:** Each attribute contrast has one forbidden attribute relationship; do not combine POINTER
and ALLOCATABLE into a separate mutually-exclusive-attribute error.

**Oracle limitation:** A report about an unsupported target facility or another type definition is not a report
of the specified component attribute relationship. Every planned negative needs a
source-minimal conforming repair and a subject-specific primary diagnostic at its actual
point or independently justified bounded relation. Required reporting need not be fatal.
Unsupported facilities, unrelated recovery, crashes, verifier/resource failures and
timeouts do not establish the intended condition. Native diagnostic routes are limited
to their observed or explicitly prospective source-specific conditions. These
definitions do not supply fixture approval.

**Dependencies:** R737/R738/R743 here and canonical POINTER/ALLOCATABLE attributes in8.5.

### C769: An initialized data pointer component is compatible with its target

**Source:** 7.5.4.6, C769, C769.initial-data-target-condition, C769.named-component-compatibility; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Restriction.

**Definition:** When an initial-data-target occurs in a component declaration, the named component must
be data-pointer-initialization compatible with that target. Paragraph2 defines the
complete conjunction of type compatibility, equal rank, equal nondeferred type
parameters and the conditional contiguity requirement.

**Diagnostic obligation:** required.

**Facets:** `same-type-rank-parameters`, `polymorphic-compatible-extension`, `type-mismatch`, `rank-mismatch`, `character-length-mismatch`, `nondeferred-pdt-parameter-mismatch`, `contiguous-target`, `noncontiguous-target`, `compatibility-definition-link`.

**Oracle:** Targets satisfy C770 before compatibility is varied. Character lengths and finite array
ranks/extents distinguish properties independently of numerical representation.

**Oracle limitation:** Type compatibility is directional for polymorphic entities. Do not test a scalar
CONTIGUOUS pointer, an undefined association status or an unsupported kind as a
surrogate. Every planned negative needs a source-minimal conforming repair and a
subject-specific primary diagnostic at its actual point or independently justified
bounded relation. Required reporting need not be fatal. Unsupported facilities,
unrelated recovery, crashes, verifier/resource failures and timeouts do not establish
the intended condition. Native diagnostic routes are limited to their observed or
explicitly prospective source-specific conditions. These definitions do not supply
fixture approval.

**Dependencies:** 7.5.4.6 p2;7.3.3, PDF79;8.5.7, PDF123; C770; 7.5.3.1 and7.5.9 for PDT values; 4.2 p2(3).

### C770: An initial data target has permanent object and constant-selector premises

**Source:** 7.5.4.6, C770, C770.nonallocatable-variable, C770.noncoindexed-variable, C770.target-attribute, C770.save-attribute, C770.no-vector-subscript, C770.constant-subscripts, C770.constant-section-subscripts, C770.constant-substring-start, C770.constant-substring-end; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Restriction.

**Definition:** An initial-data-target designator must designate a nonallocatable, noncoindexed variable
with TARGET and SAVE and no vector subscript. Each subscript, section subscript and
substring endpoint in the designator must be a constant expression. An initializer on a
local variable can imply SAVE, so omission of the SAVE spelling alone does not establish
the missing attribute.

**Diagnostic obligation:** required.

**Facets:** `saved-target-name`, `constant-array-element`, `constant-section-triplet`, `constant-substring`, `missing-target`, `missing-save`, `allocatable-target`, `coindexed-target`, `vector-subscript`, `variable-element-subscript`, `variable-triplet-start`, `variable-triplet-end`, `variable-triplet-stride`, `variable-substring-start`, `variable-substring-end`, `nested-designator-use-graph`.

**Oracle:** Invalid and repaired sources are compile-only unless all read targets are separately
defined. Missing-SAVE controls deliberately avoid implicit saving and never inspect
undefined target values. Every varying selector is isolated from rank and parameter
compatibility through an appropriate pointer declaration.

**Oracle limitation:** Allocatable status, coindexing, vector shape and constancy are distinct reasons. Do not
let another selector or an earlier unsupported coarray facility supply the report. Every
planned negative needs a source-minimal conforming repair and a subject-specific primary
diagnostic at its actual point or independently justified bounded relation. Required
reporting need not be fatal. Unsupported facilities, unrelated recovery, crashes,
verifier/resource failures and timeouts do not establish the intended condition. Native
diagnostic routes are limited to their observed or explicitly prospective
source-specific conditions. These definitions do not supply fixture approval.

**Dependencies:** R744/C769 and p2 here;8.5.16 p4, PDF132; TARGET attribute in8.5.18, PDF133; 9.4-9.5,
PDF151-158;10.1.12, PDF187-188;19.5.2, PDF556-557.

### S7.5.4.6-001: Allocatable components begin with unallocated status

**Source:** 7.5.4.6, p1.allocatable-initial-status; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** Allocatable components are initialized to unallocated status. This is an initialization
property, not a claim that an allocated component remains unallocated after subsequent
allocation, construction or assignment. It does not require a component-initialization
specifier and must be distinguished from the p8 definition of a type with default
initialization.

**Diagnostic obligation:** not-required.

**Facets:** `declared-object-status`, `allocated-container-status`, `nested-container-status`, `allocation-event-use-graph`.

**Oracle:** Only allocation-status inquiries are permitted before explicit component allocation. A
later allocation and defined payload can provide a separate guard/control.

**Oracle limitation:** Do not infer a component value, shape or dynamic type from unallocated status. Automatic
unallocation is not an invented explicit =NULL() component initializer.

**Dependencies:** 7.5.4.6 p1/p8;9.7 and allocation-status rules;7.5.10 p3/p6-p8, PDF107-108;19.5/19.6.

### S7.5.4.6-002: Data-pointer-initialization compatibility is a qualified conjunction

**Source:** 7.5.4.6, p2, p2.compatibility-term, p2.type-compatible, p2.same-rank, p2.nondeferred-parameter-values, p2.contiguous-pointer-condition, p2.contiguous-target; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** The named compatibility relation holds when the pointer is type compatible with the
target, ranks agree, every nondeferred pointer parameter agrees with the corresponding
target parameter, and a CONTIGUOUS pointer has a contiguous target. The definition
applies to a pointer variable or component; it is not general intrinsic assignment
compatibility and does not perform a value conversion.

**Diagnostic obligation:** not-required.

**Facets:** `component-compatibility-link`, `pointer-variable-use-graph`, `deferred-parameter-condition`, `directional-type-compatibility`.

**Oracle:** This definition is tested through reviewed users of the relation rather than a duplicate
compatibility simulator or unrestricted compile-success proxy.

**Oracle limitation:** All four conditions must remain visible. The relation says nothing about reading
undefined targets or calling a procedure pointer.

**Dependencies:** C769;7.3.3, PDF79;8.2 pointer initialization;8.5.7, PDF123; 9.4.5 parameter inquiries
and10.2.2 pointer assignment.

### S7.5.4.6-003: Pointer component default disassociation

**Source:** 7.5.4.6, p3, p3.null-initializer-condition, p3.initial-disassociation, p3.later-disassociation-events; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** When null-init on a pointer component is specified, the component is initially
disassociated, or becomes disassociated at the events specified by 19.5.2.4(5). The
event alternatives retain their nonpointer/nonallocatable/unsaved and no-SOURCE
conditions; they are not an instruction to reset association after every assignment or
reference.

**Diagnostic obligation:** not-required.

**Facets:** `initial-status`, `local-entry`, `intent-out-entry`, `block-entry`, `allocation-without-source`, `canonical-event-use-graph`, `procedure-null-status`.

**Oracle:** Use ASSOCIATED only after the declared initializer or exact default-initialization event
establishes defined association status. Non-null targets outlive every use; target
values and called procedures have separately established semantics.

**Oracle limitation:** Never query a dangling or never-initialized pointer. A TARGET attribute does not imply a
defined data value. Procedure-component PASS/NOPASS, target class and interface
compatibility retain their canonical restrictions. A type-only admission is not a
runtime association effect.

**Dependencies:** 7.5.4.6 p3;19.5.2.4(5), PDF556-557;19.5.2.2/19.5.2.5; 15.4.3.6/R1517/R1518/C1519/p7-p10,
PDF333-334 for procedure targets.

### S7.5.4.6-004: Pointer component default data-target association

**Source:** 7.5.4.6, p4, p4.data-target-initializer-condition, p4.initial-target-association, p4.later-target-association-events; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** When initial-data-target on a data pointer component is specified, the component is
initially associated with that target, or becomes associated with that target at the
events specified by 19.5.2.3(5). The event alternatives retain their
nonpointer/nonallocatable/unsaved and no-SOURCE conditions; they are not an instruction
to reset association after every assignment or reference.

**Diagnostic obligation:** not-required.

**Facets:** `initial-status`, `local-entry`, `intent-out-entry`, `block-entry`, `allocation-without-source`, `canonical-event-use-graph`, `defined-target-alias`.

**Oracle:** Use ASSOCIATED only after the declared initializer or exact default-initialization event
establishes defined association status. Non-null targets outlive every use; target
values and called procedures have separately established semantics.

**Oracle limitation:** Never query a dangling or never-initialized pointer. A TARGET attribute does not imply a
defined data value. Procedure-component PASS/NOPASS, target class and interface
compatibility retain their canonical restrictions. A type-only admission is not a
runtime association effect.

**Dependencies:** 7.5.4.6 p4;19.5.2.3(5), PDF556-557;19.5.2.2/19.5.2.5; 15.4.3.6/R1517/R1518/C1519/p7-p10,
PDF333-334 for procedure targets.

### S7.5.4.6-005: Pointer component default procedure-target association

**Source:** 7.5.4.6, p5, p5.procedure-target-initializer-condition, p5.initial-procedure-association, p5.later-procedure-association-events; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** When initial-proc-target in a procedure pointer component proc-decl is specified, the
component is initially associated with that procedure, or becomes associated with that
procedure at the events specified by 19.5.2.3(5). The event alternatives retain their
nonpointer/nonallocatable/unsaved and no-SOURCE conditions; they are not an instruction
to reset association after every assignment or reference.

**Diagnostic obligation:** not-required.

**Facets:** `initial-status`, `local-entry`, `intent-out-entry`, `block-entry`, `allocation-without-source`, `canonical-event-use-graph`, `module-target-call`, `external-target-call`.

**Oracle:** Use ASSOCIATED only after the declared initializer or exact default-initialization event
establishes defined association status. Non-null targets outlive every use; target
values and called procedures have separately established semantics.

**Oracle limitation:** Never query a dangling or never-initialized pointer. A TARGET attribute does not imply a
defined data value. Procedure-component PASS/NOPASS, target class and interface
compatibility retain their canonical restrictions. A type-only admission is not a
runtime association effect.

**Dependencies:** 7.5.4.6 p5;19.5.2.3(5), PDF556-557;19.5.2.2/19.5.2.5; 15.4.3.6/R1517/R1518/C1519/p7-p10,
PDF333-334 for procedure targets.

### S7.5.4.6-006: Value defaults establish the expression value with intrinsic-assignment conversion

**Source:** 7.5.4.6, p6.constant-expression-condition, p6.initial-and-event-definition, p6.value-from-expression, p6.conversion-if-necessary, p6.type-agreement, p6.parameter-agreement, p6.shape-agreement; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** A nonpointer component with a constant-expression initializer is initially defined or
becomes defined at the canonical definition events. Its value is obtained from that
expression and, when needed, converted under intrinsic assignment to agree with the
component type, type parameters and shape. C768 independently excludes ALLOCATABLE from
this explicit value-initializer form.

**Diagnostic obligation:** not-required.

**Facets:** `scalar-integer-value`, `integer-kind-conversion`, `character-length-conversion`, `scalar-to-array-expansion`, `array-value-and-shape`, `local-entry-value`, `intent-out-value`, `block-entry-value`, `allocation-without-source-value`, `definition-event-use-graph`.

**Oracle:** Use exact small integers and default-character equality. Do not infer arbitrary real
rounding, physical representation or conversion correctness from repeated literal
comparisons. Read only initialized/otherwise defined subcomponents.

**Oracle limitation:** Partial defaults do not define every sibling. Array scalar expansion and actual
array-expression conformance are distinct. The default value is not necessarily
reapplied during intrinsic assignment or SOURCE= allocation.

**Dependencies:** 7.5.4.6 p6;10.2.1.2-.3, PDF189-193;19.6.1/19.6.3, PDF562;19.6.5, PDF563-565;C767/C768.

### S7.5.4.6-007: An enclosing component value default overrides nested defaults

**Source:** 7.5.4.6, p6.nested-default-overridden, p6.only-overriding-initialization; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** When an initialized component has a derived type with its own component defaults, the
outer constant-expression initialization overrides the nested defaults. The semantics
are as though only the overriding initialization were specified, not a later
reapplication of the inner defaults over the supplied value.

**Diagnostic obligation:** not-required.

**Facets:** `nested-value-override`, `nested-pointer-status-override`, `multiple-nesting-levels`, `override-event-use-graph`.

**Oracle:** Construct valid constant derived values with complete required components. Choose simple
nonfinalizable types so no unreviewed finalization count enters the oracle.

**Oracle limitation:** The clause is not permission to use an invalid constant constructor or inaccessible
component. Pointer initial target/lifetime restrictions remain necessary.

**Dependencies:** 7.5.4.6 p6/p7;7.5.10, PDF106-109;10.1.12, PDF187-188;19.6.3/19.6.5.

### S7.5.4.6-008: Explicit type-declaration initialization overrides component defaults

**Source:** 7.5.4.6, p6.explicit-overrides-default; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** Explicit initialization of an object in a type declaration statement overrides its
default initialization. This precedence concerns an explicit initializer, not an
ordinary executable assignment or a claim that default initialization implicitly
supplies SAVE.

**Diagnostic obligation:** not-required.

**Facets:** `explicit-scalar-object`, `explicit-array-object`, `explicit-nested-object`, `explicit-initialization-use-graph`.

**Oracle:** Compare separate explicitly initialized and only-default-initialized objects. All
referenced constant components are defined and accessible.

**Oracle limitation:** No claim is made that an arbitrary executable expression is a legal declaration
initializer. Explicit initialization may imply SAVE under its own rule.

**Dependencies:** 7.5.4.6 p6;8.2, PDF117-119;7.5.10, PDF106-109;10.1.12.

### S7.5.4.6-009: Default initialization does not itself imply SAVE

**Source:** 7.5.4.6, p6.no-implied-save; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** Default initialization alone does not give an object the SAVE attribute. An object can
separately be saved by explicit declaration, containing scope or other canonical rules.
Initialization on entry and retention of prior association, allocation or value state
must not be conflated.

**Diagnostic obligation:** not-required.

**Facets:** `unsaved-local-reentry`, `explicit-save-control`, `save-source-use-graph`.

**Oracle:** Observe state only while defined in each invocation. Use counters or distinct arguments
outside the tested local variable to distinguish invocations.

**Oracle limitation:** No storage address, stack layout or attempted read after lifetime termination is part of
the oracle. The rule does not prohibit an independent SAVE attribute.

**Dependencies:** 7.5.4.6 p6;8.5.16, PDF132;19.6.5(23), PDF564;19.6.6(3), PDF565.

### S7.5.4.6-010: Default-initialized subcomponents exclude enclosing initialized subobjects

**Source:** 7.5.4.6, p7, p7.containing-type-specifies-default, p7.no-default-initialized-containing-object, p7.no-explicitly-initialized-containing-object; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** A subcomponent is default-initialized when its containing object type specifies default
initialization for that component and the subcomponent is not a subobject of an object
that is itself default-initialized or explicitly initialized. These containing-object
exclusions prevent nested defaults from independently overriding an enclosing supplied
initialization.

**Diagnostic obligation:** not-required.

**Facets:** `ordinary-nested-default`, `default-initialized-container-exclusion`, `explicitly-initialized-container-exclusion`, `subobject-and-event-use-graph`.

**Oracle:** Use complete nesting and source-use relations, not an implementation model that
initializes every reachable memory location.

**Oracle limitation:** A dereferenced pointer target is not a subobject of its pointer-containing base object
merely because the pointer component is a subobject for status purposes.

**Dependencies:** 7.5.4.6 p6/p7;9.4.2 p5/note1, PDF152;19.5.2.3-.4 and19.6.3/19.6.5.

### S7.5.4.6-011: Type and object default-initialization classification use direct components

**Source:** 7.5.4.6, p8, p8.type-direct-component-condition, p8.object-type-condition; J3/24-007, 18 December 2023, PDF 96-98. **Class:** Effect.

**Definition:** A type has default initialization if component-initialization is specified for any
direct component of the type. An object has default initialization when its type has it.
Under3.30.1, direct components include components reached recursively through nonpointer
nonallocatable components; the term is not limited to the outermost declaration list and
does not traverse pointer targets or allocatable component objects. A pointer component
can still have its own initializer. This classification is separate from the initially
unallocated status of allocatable components.

**Diagnostic obligation:** not-required.

**Facets:** `direct-component-classification`, `object-type-classification`, `nested-direct-component-classification`, `pointer-and-allocatable-recursion-boundary`, `inherited-and-recursive-use-graph`.

**Oracle:** This is a source-level classification with explicitly pending consumer evidence, not a
parser-only proof or a new language inquiry.

**Oracle limitation:** The p1 allocation-status property does not automatically imply that the p8 syntactic
condition was met. Complete direct/inherited/recursive use accounting needs its own
reviewed graph.

**Dependencies:** 7.5.4.6 p1/p7/p8;3.30.1 and3.30.4, PDF22;7.5.7.2, PDF104-105;19.5/19.6.

<!-- END GENERATED 7.5.4.6 -->
