# Fortran 2023 13.11.3.2: Namelist input processing

The canonical catalogue is `doc/catalogues/namelist_input_processing_13_11_3_2.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`. Source-read receipt: `$SESS/receipts-batch139-source.json`.

Base units read and accounted: p1, p2, note-unnumbered, p3, p4, p5. No fine census units occur in the pinned inventory for this section.

Output-latitude note: list-directed and namelist output field widths, separator spelling/counts, record lengths, and record breaks are processor dependent unless a requirement below names exact characters.

<!-- BEGIN GENERATED 13.11.3.2 -->

### S13.11.3.2-001: Namelist name-value subsequences are processed serially and leave untouched objects unchanged

**Source:** S13.11.3.2, J3/24-007, 18 December 2023, physical PDF312. **Class:** Effect.

**Definition:** Name-value subsequences are evaluated serially left to right; a designator may appear in
more than one subsequence; objects not subobjects of any designator remain unchanged.

**Diagnostic obligation:** not-required.

**Facets:** `serial-left-to-right`, `repeated-designator-last-effect`, `unmentioned-object-unchanged`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.11.3.2-002: Aggregate namelist designators expand to effective items with bounded following values

**Source:** S13.11.3.2, J3/24-007, 18 December 2023, physical PDF313. **Class:** Restriction.

**Definition:** Array or derived-type designators expand to scalar effective items as formatted I/O
lists do; values after equals shall not exceed the number of effective items but may be
fewer, in which case nulls are appended; unacceptable forms cause an error condition.

**Diagnostic obligation:** not-required.

**Facets:** `aggregate-expands-effective-items`, `value-count-not-exceed-items`, `fewer-values-null-appended`, `unacceptable-value-error`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.11.3.2-003: Slash terminates namelist input and null-supplies remaining current-object items

**Source:** S13.11.3.2, J3/24-007, 18 December 2023, physical PDF313. **Class:** Effect.

**Definition:** A slash value separator terminates namelist input after previous value; additional items
in the current namelist group object are treated as supplied null values.

**Diagnostic obligation:** not-required.

**Facets:** `namelist-slash-terminates`, `namelist-slash-null-supplies-current-object`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.11.3.2-004: Namelist input reads successive records until slash and ignores record remainder

**Source:** S13.11.3.2, J3/24-007, 18 December 2023, physical PDF313. **Class:** Effect.

**Definition:** Successive namelist records are read until slash; the remainder of the slash record is
ignored.

**Diagnostic obligation:** not-required.

**Facets:** `successive-records-until-slash`, `slash-record-remainder-ignored`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.11.3.2-005: Namelist comments may follow separators except slash or start a record

**Source:** S13.11.3.2, J3/24-007, 18 December 2023, physical PDF313. **Class:** Effect.

**Definition:** A namelist comment may appear after any value separator except terminating slash, and
may start in the first nonblank position outside character literals.

**Diagnostic obligation:** not-required.

**Facets:** `comment-after-nonslash-separator`, `comment-first-nonblank-position`, `no-comment-after-slash`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

<!-- END GENERATED 13.11.3.2 -->
