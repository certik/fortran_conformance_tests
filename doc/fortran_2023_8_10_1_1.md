# Fortran 2023 8.10.1.1: EQUIVALENCE statement - general

This is an **UNAPPROVED source-only packet**. Its canonical record is
`doc/catalogues/equivalence_statement_8_10_1_1.json`; every new facet is
pending. No fixture, compiler observation, review approval or evidence
connection is supplied.

Authority: J3/24-007, December 18, 2023, 688 physical pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
This complete clause starts on physical146 and ends at the actual8.10.1.2
heading on147, including the note. Its17 original units have84 fine
subdivisions and101 accounting rows, with15 requirements and95 pending
facets. Fine units inherit original parent hashes; they are not extra
independently extracted source. The empty8.10 and8.10.1 parents gain no
fake units or requirements.

## Grammar, entities and reporting

R873 requires a nonempty comma-separated set list without double colons.
R874 encloses at least two object occurrences. R875 uses **explicit
R903/C903** for variable names, confirmed by the complete502-head scan,
not assumed R402. R917/C924 and R908-R910 govern its element and substring
alternatives. Syntax negatives have complete conforming controls and
source-minimal repairs.

C8110 tests the **base object**, including for an element that itself
lacks POINTER or ALLOCATABLE. C8111 independently limits part-refs.
TARGET and PROTECTED have explicit subobject-inheritance rules.
Enumeration type is not enum type. SEQUENCE classification is recursive;
other sequence types require actual type identity. Controls avoid the
pre-existing unresolved private-component identity question.

A member can trigger a condition for the **entire set**. Default LOGICAL
belongs to C8114's listed family, while nondefault COMPLEX follows C8117.
The latter requires same type and KIND, not a universal equal-LEN rule.
Optional kinds must first be supported and distinguished by equality,
not by numeric ordering of kind codes. Unavailable kinds and unsupported
facilities cannot count as success.

Numbered reporting capability does not require fatal rejection, fixed
English, a nonzero status or printed rule identifiers. Some family and
constant-role negatives have genuinely coupled predicates. The plans
record those causes instead of inventing unique diagnosis or reusable
evidence edges.

## Source ownership and state

P1 is a normative statement/association definition, not another no-op S.
P2's property plans retain8.10.1.2-.4 and Clause19 prerequisites.
Cross-type association is not conversion, mathematical equivalence or
permission to read undefined aliases. No byte, address, TRANSFER,
truth-bit, padding-layout or compiler-consensus oracle is used.

Controls use actual local entities and prior explicit declarations where
useful. The later-declaration plan stays inside the specification part;
it does not import DATA/PARAMETER-specific confirmation restrictions.
Constant property inquiries have their own prior-property rule under
10.1.12p2. Actual use in a specification expression also activates
10.1.11p6's prior-typing and implicit-type-confirmation conditions; a
whole-name EQUIVALENCE occurrence alone is not that use.
EQUIVALENCE can establish local identity under19.5.1.4p2(10);
USE-associated object names remain excluded. BLOCK placement and COMMON
extension restrictions keep their canonical owners.

The note is informative.19.5.3.4p10 retains single explicit initialization
and compatible default-initialization conditions. Obsolescence under
4.4.3/B.3.11 is not deletion or a wrong-value oracle; no warning-as-error
policy is added.

## Preservation and gates

The exact author base is
`ddb095217a4984f70cb65287b03e1fd890225fb6`. No directly scoped existing
case or structured requirement was found there. The packet does not
change any old case, review, catalogue, profile, baseline or inventory.
The local five-entry index overlay is excluded from its commit and
preserves the evidence, execution-inventory and source-use pointers.

All130 facets across the five clauses remain pending. Future syntax and
constraint controls, runtime parents, diagnostic policies and canonical
source/interface relationships need adjudication. Current same-numbered
diagnostic/control links do not authorize arbitrary S-runtime or
positive-only reuse. No cloned program or fictional inquiry fills that
gap. Independent source review is required before registration.
Historical full059 and selected072 observations are not observations of
this source context.

<!-- BEGIN EQUIVALENCE STATEMENT FIXTURES -->
## Finite EQUIVALENCE runtime fixture family

This packet adds eleven single-image runtime fixtures for exactly seventeen selected
facets of 8.10.1.1. The programs use only same-kind INTEGER/COMPLEX,
default CHARACTER, and numeric-SEQUENCE value observations. Each fixture starts from non-default
sentinels, checks the pre-state, performs the aliasing write, checks the
derived alias value, and has conforming removal/offset mutants that fail on
both the frozen LFortran target and the gfortran reference.

No differing-type value reinterpretation, processor-dependent truth bits,
padding layout, TRANSFER image, diagnostic text, COMMON effect, or review
state is asserted. All unselected facets remain pending in the catalogue.
<!-- END EQUIVALENCE STATEMENT FIXTURES -->

<!-- BEGIN GENERATED 8.10.1.1 -->

### R873: EQUIVALENCE requires a nonempty comma-separated set list

**Source:** 8.10.1.1 R873, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Syntax.

**Definition:** The keyword is followed directly by one or more equivalence sets. R401 supplies commas
between sets. There is no double-colon form or optional set-list branch. Every set
separately satisfies R874 and the object constraints.

**Diagnostic obligation:** required.

**Facets:** `one-set`, `multiple-sets`, `missing-set-list`, `no-double-colon`, `between-set-comma`, `trailing-set-comma`, `scope-order-canonical-plan`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;11.1.4C1107;19.5.1.4p2(10);8.10.2.2/8.10.3.

### R874: Each set encloses at least two object occurrences

**Source:** 8.10.1.1 R874, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Syntax.

**Definition:** A set encloses a first object, a required comma and a nonempty R401 list of further
objects. The syntactic minimum is therefore two members. Parentheses delimit each set,
not a separate outer statement wrapper.

**Diagnostic obligation:** required.

**Facets:** `two-object-set`, `three-object-set`, `singleton-set`, `empty-tail`, `set-parentheses`, `object-list-separators`, `object-identity-gate`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11.

### R875: Objects are variable names, array elements or substrings

**Source:** 8.10.1.1 R875, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Syntax.

**Definition:** R903/C903 governs the explicit variable-name alternative, R917/C924 array elements, and
R908-R910/C908 substrings. Whole arrays can enter through their variable names.
Arbitrary expressions and array sections are not extra alternatives. Base-object,
part-reference, constant-index and nonzero-substring restrictions remain separate.

**Diagnostic obligation:** required.

**Facets:** `scalar-variable-name`, `whole-array-name`, `array-element`, `scalar-substring`, `array-element-substring`, `array-section-not-element`, `arithmetic-expression-not-object`, `literal-not-variable`, `component-and-constant-owner-plan`, `earlier-later-declarations`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;9.4.1R908-R910/C908;9.4.2R911/R912/C913;9.5.3.1R917/C924/p2;10.1.11p6-p7/10.1.12p2.

### C8110: Excluded categories are predicates on the base object

**Source:** 8.10.1.1 C8110, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** A designator is ineligible when its base is a dummy, function result, pointer,
allocatable variable, coarray, automatic object, named constant, BIND variable or member
of a BIND common block. Also excluded are nonsequence derived objects, enumeration-type
objects and derived objects with any pointer or allocatable ultimate component. These
are alternatives, not a conjunction. An element's own attributes do not replace its
base's role. Enumeration type and enum type are distinct.

**Diagnostic obligation:** required.

**Facets:** `dummy-base`, `function-result-base`, `pointer-base`, `pointer-array-base`, `allocatable-base`, `allocatable-array-base`, `pointer-ultimate-component`, `allocatable-ultimate-component`, `nonsequence-derived-base`, `enumeration-type-base`, `automatic-array-base`, `automatic-character-base`, `coarray-base`, `bind-variable-base`, `bind-common-member-base`, `named-constant-base`, `eligible-sequence-boundary`, `enum-and-private-identity-gates`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;7.3.2.1C708;7.5.1p3/7.5.2.3C745/7.5.2.4;7.6.1-.2;8.3/8.4/8.5.3/8.5.5;8.5.6.1C826/8.5.6.3R811/C828;8.6.4R835/R836;9.4.2p3;9.5.3.1note2;10.1.11p1/p2(2)/p6-p7;15.6.2.2-.3;18.2.2p3/18.3.1/18.3.5.

### C8111: An object designator has at most one part-ref

**Source:** 8.10.1.1 C8111, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** A component-reference chain is not allowed. This restriction is independent of whether
the final form parses as an array element or substring, and whether its whole base is an
eligible sequence type.

**Diagnostic obligation:** required.

**Facets:** `one-part-element`, `component-array-element`, `component-substring`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;7.5.2.3;9.4.1R909;9.4.2R911/R912/p3;9.5.3.1C924;19.5.3.4p7.

### C8112: TARGET is excluded including inherited subobject TARGET

**Source:** 8.10.1.1 C8112, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** An equivalence object cannot have TARGET.8.5.18p2 supplies this attribute to nonpointer
subobjects of a TARGET object; the general fact that array elements do not inherit every
array attribute is no exception.

**Diagnostic obligation:** required.

**Facets:** `target-scalar`, `target-array-element`, `target-substring`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;8.5.18C865/C866/p2;9.5.3.1note2.

### C8113: Indices and present substring endpoints are INTEGER constant expressions

**Source:** 8.10.1.1 C8113, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** Every subscript and each expression present in a substring range must have INTEGER type
and meet 10.1.12. Constant expressions are not limited to literals, and omitted
endpoints are not absent required expressions. Array rank, valid bounds and nonzero
substring length remain independent conditions.

**Diagnostic obligation:** required.

**Facets:** `nonconstant-subscript`, `nonconstant-substring-start`, `nonconstant-substring-end`, `named-constant-arithmetic`, `fixed-property-inquiry`, `omitted-endpoints`, `expression-category-and-user-function`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;9.4.1R910/p2-p3;9.5.3.1R919/p2;10.1.12R1032/C1014/p1-p3;16.9.122.

### C8114: A listed default numeric-family member constrains the whole set

**Source:** 8.10.1.1 C8114, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** Any default INTEGER, default REAL, double precision REAL, default COMPLEX, default
LOGICAL or numeric SEQUENCE member requires every member to belong to that type/kind
family. Heterogeneous members inside the family are permitted; one common intrinsic type
or interchangeable values are not required or implied.

**Diagnostic obligation:** required.

**Facets:** `default-integer-trigger`, `default-real-trigger`, `double-precision-trigger`, `default-complex-trigger`, `default-logical-trigger`, `numeric-sequence-trigger`, `outside-family-repair`, `set-wide-not-first-only`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;7.4.3.1-.3/7.4.5;7.5.2.3p1;19.5.3.2p2/19.5.3.4p7;19.6.5p1(12)-(15)/19.6.6p1(1).

### C8115: Default CHARACTER-family membership applies to every set member

**Source:** 8.10.1.1 C8115, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** A default CHARACTER or character-SEQUENCE member requires all members to belong to those
categories. Equal lengths and one derived-type identity are not additional conditions
here. Eligibility, state and alignment restrictions still apply.

**Diagnostic obligation:** required.

**Facets:** `default-character-pair`, `character-sequence-pair`, `different-character-sequence-types`, `foreign-family-repair`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;7.4.4.2;7.5.2.3p1;8.10.1.3;19.5.3.2p2(3)/(5);19.5.3.4p7.

### C8116: Other sequence types require the same derived type

**Source:** 8.10.1.1 C8116, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** If a member has SEQUENCE but is neither numeric SEQUENCE nor character SEQUENCE, all
members must have that same derived type. All three premises matter. Similar component
lists or storage sizes do not replace type identity under7.5.2.4.

**Diagnostic obligation:** required.

**Facets:** `mixed-sequence-same-type`, `different-sequence-type`, `every-member-same-type`, `identity-and-partial-association-gate`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;7.5.2.3C745/p1;7.5.2.4p2;19.5.3.4p7.

### C8117: Remaining intrinsic kinds require matching type and KIND

**Source:** 8.10.1.1 C8117, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** For an intrinsic member outside the six listed default/double-precision categories, all
members must have the same intrinsic type and KIND. A shared kind code does not equate
different types. This is not a blanket equal-LEN rule; storage-unit and
partial-association restrictions are also necessary.

**Diagnostic obligation:** required.

**Facets:** `nondefault-integer-same-kind`, `nondefault-complex-same-kind`, `other-real-logical-kinds`, `nondefault-character-equal-length`, `different-kind-repair`, `same-code-different-type-gate`, `nondefault-character-overlap-gate`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;7.2;7.4.3.1-.3/7.4.4.2/7.4.5;16.9.118/16.9.181;18.2.2p6;19.5.3.2p2(4)/(6);19.5.3.4p7.

### C8118: A PROTECTED member requires every member to be PROTECTED

**Source:** 8.10.1.1 C8118, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** Any PROTECTED equivalence object requires PROTECTED on all members. Unlike SAVE
propagation, this is a condition to satisfy, not an attribute automatically conferred by
EQUIVALENCE. Module scope, subobject inheritance, USE and COMMON restrictions retain
their own owners.

**Diagnostic obligation:** required.

**Facets:** `all-protected-module-objects`, `missing-protected-member`, `three-member-protected-trigger`, `protected-subobject`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;8.5.15C856-C860/p3.

### C8119: A USE-associated object name is excluded

**Source:** 8.10.1.1 C8119, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** The object's name must not be made accessible through USE. Renaming an imported object
does not make it local. Importing a type or kind for a separate local declaration is
different. EQUIVALENCE's name-locality effect for host entities is resolved
under19.5.1.4.

**Diagnostic obligation:** required.

**Facets:** `imported-variable`, `renamed-import`, `imported-type-local-objects`, `host-locality-source`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;8.5.2;14.2.2R1409/R1411/p1-p2/p7-p9;19.5.1.3;19.5.1.4p2(10);7.5.2.4.

### C8120: An equivalence substring must be nonzero length

**Source:** 8.10.1.1 C8120, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Restriction.

**Definition:** The substring alternative cannot have zero length. Zero-sized storage sequences in
general are not banned: a whole zero-length CHARACTER variable or zero-length CHARACTER
array element is not a substring occurrence. General endpoint defaults and bounds stay
with9.4.1.

**Diagnostic obligation:** required.

**Facets:** `reversed-substring-range`, `omitted-start-zero-end`, `one-character-substring`, `zero-length-whole-object-boundary`.

**Oracle:** Bounded complete-program-unit admission and diagnostic/control plans under a qualified
F2023 profile. Resolve actual objects, declarations, types, kinds, ranks and statement
positions before the contrast. Positive compile admission is not a runtime association
or value observation.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** 4.2 requires capability to detect and report numbered violations, not fatal rejection, a
nonzero status, fixed English or a printed rule number. Each future negative needs its
stated complete conforming repair and a grammar/entity-specific cause. Unsupported
facilities, arbitrary keyword hits, unrelated scope/type errors, source echoes, crashes
and resource failures are not corroboration. Stricter rejection policies and diagnostic
matchers require explicit qualification. No case or evidence connection is implemented
here.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;7.4.4.2p5;9.4.1p2-p3;19.6.2.

### S8.10.1.1-001: Sharing storage does not convert values or transfer array properties

**Source:** 8.10.1.1 p2, J3/24-007, 18 December 2023, physical PDF146-147 **Class:** Effect.

**Definition:** Different types or type parameters do not trigger conversion or guarantee mathematical
equality. The associated objects retain their declarations: a scalar does not acquire
array properties, nor does an array become scalar. Value access still needs the
definition-state rules of Clause19.

**Diagnostic obligation:** not-required.

**Facets:** `no-conversion-source`, `no-mathematical-equality-source`, `scalar-array-properties`, `character-length-properties`, `declared-type-kind-source`.

**Oracle:** Future complete run/effect parents must observe actual associated objects against
independent small INTEGER or default CHARACTER literals. Every referenced subobject must
already be defined by justified events. Count reached observations, calls and normal
returns where applicable; require exact completion stdout, empty runtime stderr and
normal exit. Wrong-expectation and observation-omission sensitivity must preserve
required definitions and associations, and follow a fresh passing complete parent.

Finite EQUIVALENCE runtime fixture family: eleven complete single-image programs observe
storage association caused by EQUIVALENCE using only same-type/same-kind default
INTEGER, default CHARACTER, numeric-sequence, and COMPLEX(KIND(0.0D0)) objects. Each
program initializes non-default sentinels, verifies the positive pre-state, performs the
defining write through the associated object, checks the independently derived alias
value, and prints one exact completion line with empty runtime stderr. Array-element and
substring cases use in-bounds constant offsets; mutations delete the EQUIVALENCE
statement or change the object/offset while remaining conforming, so survivors would
show that the selected rule facet was not load-bearing.

**Oracle limitation:** All plans are pending, not compiler observations or fixture approvals. A source summary,
compile-only or no-op success, implementation byte/address layout, TRANSFER,
undefined-alias read, arbitrary kind ordering or compiler consensus is not an oracle.
Canonical state/type/lifetime premises and any future cross-owner source/interface
relationship need explicit adjudication. Existing same-numbered diagnostic/control links
do not implement arbitrary S-runtime or positive-only reuse; neither cloned programs nor
fictional passing inquiries close that gap.

Finite EQUIVALENCE runtime fixture boundaries: only the selected positive/runtime facets
are discharged. No cross-type bit-pattern interpretation, mathematical conversion,
LOGICAL truth representation, padding layout, TRANSFER result, diagnostic vocabulary,
COMMON interaction, use-associated name, TARGET/PROTECTED exclusion,
pointer/allocatable/dummy/function-result exclusion, coarray, BIND, zero-length
substring, unsupported kind fallback, suite-wide count, review-state claim, or
negative-control adjudication is made. Differing intrinsic-type admissions under C8114
are not value-tested here; all value effects are same-kind numeric sequence or character
storage effects derived from 8.10.1.1 and 19.5.3.

**Dependencies:** 4.1.3R401/p2;4.2;5.1R504/R507/R508/R513;5.3.1-.2;8.1/8.2/8.7;8.10.1.1-.5;9.2R903/C903/p2;19.5.3.1-.4;19.6.1-.6;4.4.3/B.3.11;8.10.1.3/.4;10.2.1.2-.3;16.9.118/16.9.122/16.9.171/16.9.194;19.6.5p1(12)-(15)/19.6.6p1(1).

<!-- END GENERATED 8.10.1.1 -->
