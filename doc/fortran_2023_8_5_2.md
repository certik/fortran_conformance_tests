# Fortran 2023 8.5.2: Accessibility attribute

**Accessibility fixtures.** Fourteen finite plans are fixture-backed here and twenty-one remain pending.
Current content-bound review states are independent of this representation.
`doc/catalogues/accessibility_attribute_8_5_2.json` supplies source/duty
classifications, reciprocal fine accounting and complete per-facet plans.
This view does not grant or withdraw source, fixture, link or inventory
adjudication. It adds fixture-backed accessibility execution for the named facets, but no SourceUses instance.

Source: original J3/24-007, December 18, 2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
Physical PDF120, printed106, starts at the actual 8.5.2 heading and stops
before 8.5.3. The verified section hash is
`e4683cd9a55f0f5de538ecd135e6bd320f25f99a9db59b012edad27ddea94147`.
The original NOTE1 and NOTE2 retain their separate identities.

| Original units | PDF120 lines | Treatment |
| --- | --- | --- |
| `p1` | 3 | Per-identifier accessibility definition, not a no-op effect |
| `R807`, `C817` | 4-6 | PUBLIC/PRIVATE syntax and actual module specification context |
| `p2` | 7-10 | Three declaration subjects; qualified separate access-statement permission |
| `p3`, `p4` | 11-16 | Qualified defaults and necessary PUBLIC condition for USE |
| `note1`, `note2` | After 16 | Re-export explanation and three-name REAL declaration; no new duties or initial-value claims |

Eight base units have thirty-three fine subdivisions and forty-one
accounting rows. Five requirements have thirty-five facets: fourteen represented and twenty-one pending.
The three local S IDs describe actual declaration/default/use semantics;
their metadata is not a claim of current execution or proof from keyword
recognition.

| Qualification | Required source distinction |
| --- | --- |
| Per identifier | A public ultimate entity can have differently accessible local aliases. COMMON names, member variables, type names, components, bindings and implementation names are distinct subjects/namespaces. |
| Declaration context | Type-declaration access covers all listed names; derived-type access covers its name; enumeration-type access also supplies enumerator defaults. Named ENUM has no analogous access-spec in its definition. |
| Defaults | No-list PRIVATE/PUBLIC, individual identifier access, module-name import policies and type component/binding PRIVATE statements are not interchangeable. |
| Import policies | Preserve8.6.1's every-origin premise, any-PUBLIC/all-PRIVATE rule when applicable, unlisted-origin default route and locally declared identifier exception. A module-name entry is not a simple accessibility assignment to that module entity. |
| Member access | A public type does not make all components/bindings public. A private implementation name does not make a public binding unusable. Public members are not thereby direct USE names. |
| USE versus host | C1409 and Clause19 support actual mandatory forbidden-USE reporting. Private names can still be available through qualified host/descendant access, public aliases or other permitted routes. IMPORT and shadowing remain relevant. |
| Evidence | Declaration occurrence/admission is not a runtime effect or a private-name trap. SourceUses is integrated but empty, and never grants facet-completion credit by itself. |

Existing C1409, C7107, S7.5.5-007 and S7.5.2.2-001 cases keep their original
IDs, inputs, phases and roles. The C1409 type-name repair is already a
run/positive-control and stays one. No constructor, binding, enum or
PUBLIC/PRIVATE wrapper program is cloned.

Six existing canonical links explicitly cite the original 8.5.2 base
paragraphs. New accounting and source-review preconditions can stale those
content-bound receipts without changing their Fortran members or case
fingerprints. Their actual current/stale states come from native evidence
validation and may change after independent source and link adjudication.
Generation does not renew records, drop anchors or bypass validation.
The source-accounting impact is distinct from C815 metadata migration and
whole-inventory context; neither authorship nor representation grants approval.

The generated definitions follow. Per-facet documentary, diagnostic/control
and canonical-witness plans remain in the catalogue; represented facets name their fixture assertions below.

<!-- BEGIN GENERATED 8.5.2 -->

### R807: PUBLIC and PRIVATE access-spec alternatives

**Source:** 8.5.2 R807, physical PDF120, printed106, lines4-5 **Class:** Syntax.

**Definition:** The access-spec category has the PUBLIC and PRIVATE alternatives. Consumers determine
the affected identifier(s), scope and default/override meaning. An identical keyword
used by a distinct private-components-stmt or binding-private-stmt is not automatically
an occurrence of this nonterminal.

**Diagnostic obligation:** required.

**Facets:** `public-form`, `private-form`, `canonical-occurrences-source-use`.

**Oracle:** Qualified syntax admissions and source-occurrence resolution. No arbitrary third
keyword, malformed wrapper or private-value trap is invented as an isolated R807
negative.

Attributes 8.5.1/8.5.2 fixture implementation:
`attributes_8_5_1_8_5_4_r807_access_spec_forms` covers `public-form`, `private-form`
with 4 run-time assertion(s). Each assertion has at least one conforming feature
mutation that compiles and fails at run time on both checked toolchains. Oracles use
only exact INTEGER values, LOGICAL results of direct inquiry expressions, and same-named
sentinel resolution; no processor-dependent diagnostic text, file-system effect or
timing behavior is asserted.

**Oracle limitation:** All facets are pending. The integrated SourceUses facility enforces coverage_credit=none
and targets that remain pending; its empty registry proves no occurrences or completion.

Attributes 8.5.1/8.5.2 fixture implementation: 2 facet(s) are fixture-backed here and 1
remain pending. The fixtures do not grant source-use inventory credit, do not cover
enumeration accessibility, C817 invalid nonmodule contexts, vector-bound C816 negatives,
ASYNCHRONOUS pending-I/O obligations, or associated component/binding owner rules beyond
the named facets.

**Dependencies:** C817;8.2 R802;7.5.2.1 R728;7.5.4.1;7.5.4.8 R745;7.5.5 R752;7.6.1 R760;7.6.2 R767;8.6.1
R830;15.4.3.6 R1514.

### C817: An access-spec requires a module specification-part

**Source:** 8.5.2 C817, physical PDF120, printed106, line6 **Class:** Restriction.

**Definition:** An access-spec can occur only in a module specification-part. The actual syntactic
context matters: a module's type definitions can contain their eligible
component/binding access-specs, while a contained procedure or a submodule is not
thereby the module specification-part. Separate rules govern the PRIVATE forms that are
not access-spec and the permitted host access to ancestor-private entities.

**Diagnostic obligation:** required.

**Facets:** `module-admission`, `main-program`, `external-subprogram`, `contained-procedure`, `submodule`, `local-type-component`, `interface-context-source-use`.

**Oracle:** Five focused context report/control families, eligible module admissions and a held
interface-context source plan. Controls retain the same entity and declaration except
for the selected access-spec. Reporting capability is not mandatory fatal rejection.

Attributes 8.5.1/8.5.2 fixture implementation:
`attributes_8_5_1_8_5_4_c817_module_admission` covers `module-admission` with 2 run-time
assertion(s). Each assertion has at least one conforming feature mutation that compiles
and fails at run time on both checked toolchains. Oracles use only exact INTEGER values,
LOGICAL results of direct inquiry expressions, and same-named sentinel resolution; no
processor-dependent diagnostic text, file-system effect or timing behavior is asserted.

**Oracle limitation:** No fixture exists. Do not count absence of a module artifact, unsupported
scope/facility, wrong entity attribute, generic source echo or recovery as this context
violation. C873, C771, C772 and C7114 retain their own overlapping or distinct forms
rather than being renamed C817.

Attributes 8.5.1/8.5.2 fixture implementation: 1 facet(s) are fixture-backed here and 6
remain pending. The fixtures do not grant source-use inventory credit, do not cover
enumeration accessibility, C817 invalid nonmodule contexts, vector-bound C816 negatives,
ASYNCHRONOUS pending-I/O obligations, or associated component/binding owner rules beyond
the named facets.

**Dependencies:** 3.120;4.2p2(3);8.2;8.6.1 C873;7.5.2.1;7.5.4.1/.8;7.5.5;7.6.2
C7114;14.2.1/.3,C1412;15.4.3.2;15.6.2.5;8.8/19.5.1.4.

### S8.5.2-001: Declaration access-specs have specific identifier subjects

**Source:** 8.5.2 p2, physical PDF120, printed106, lines7-10 **Class:** Effect.

**Definition:** An access-spec in a type declaration applies to every entity name declared by that
statement. In a derived-type-stmt it sets the derived type name's accessibility. In an
enumeration-type-stmt it sets the enumeration type name's accessibility and the default
for its enumerators. These are declarative interpretations of distinct subjects, not a
propagation of type-name accessibility to every component, binding or implementation
name.

**Diagnostic obligation:** not-required.

**Facets:** `declaration-list-names`, `derived-type-name`, `enumeration-type-and-defaults`, `enumerator-confirmation-override`, `separate-type-name-access`, `component-binding-owner-source-use`.

**Oracle:** A finite declaration-subject and canonical-witness plan. The effect category records
declarative semantics; it is not a claim that recognizing PUBLIC/PRIVATE executes an
observable effect. Any forbidden-use reporter must separately meet its actual canonical
scope relation and control premises.

Attributes 8.5.1/8.5.2 fixture implementation:
`attributes_8_5_1_8_5_4_s852_declaration_subjects` covers `declaration-list-names`,
`derived-type-name` with 4 run-time assertion(s). Each assertion has at least one
conforming feature mutation that compiles and fails at run time on both checked
toolchains. Oracles use only exact INTEGER values, LOGICAL results of direct inquiry
expressions, and same-named sentinel resolution; no processor-dependent diagnostic text,
file-system effect or timing behavior is asserted.

**Oracle limitation:** All six facets remain pending. SourceUses may document occurrences only and must keep
its target facet pending even after review; the current registry is empty. No
compile-only placeholder is labelled a runtime effect. Closure needs independently
justified canonical case evidence or another explicitly approved evidence mechanism, not
a word scan or cloned programs.

Attributes 8.5.1/8.5.2 fixture implementation: 2 facet(s) are fixture-backed here and 4
remain pending. The fixtures do not grant source-use inventory credit, do not cover
enumeration accessibility, C817 invalid nonmodule contexts, vector-bound C816 negatives,
ASYNCHRONOUS pending-I/O obligations, or associated component/binding owner rules beyond
the named facets.

**Dependencies:** 8.2 R803/C809;R807/C817;7.5.2.2p1;7.5.4.8;7.5.5p8/p9;7.6.2p2;8.6.1;14.2.2;19.3.4;
existing canonical C1409 and S7.5.2.2-001 inputs.

### S8.5.2-002: Unspecified identifier accessibility follows its qualified default

**Source:** 8.5.2 p3 and p4 first sentence, physical PDF120, printed106, lines11-14 **Class:** Effect.

**Definition:** An identifier in the module setting whose accessibility is not explicit takes its
default. The ordinary module default is PUBLIC unless changed by the relevant PRIVATE
statement. The8.6.1 cross-reference is essential: a no-list statement sets the default
for locally declared identifiers and applicable imports, while access statements naming
used modules can determine import-specific defaults. Explicit identifier access, local
declaration and the complete set of import origins retain their separate conditions.

**Diagnostic obligation:** not-required.

**Facets:** `implicit-public-local`, `no-list-public`, `no-list-private-local`, `explicit-identifier-access`, `use-associated-local-default`, `single-origin-module-policy`, `all-origin-policies`, `incomplete-origin-policies`, `imported-and-local-generic`, `other-private-namespaces`.

**Oracle:** An explicit finite default-resolution plan grounded in8.5.2 and8.6.1, with separate
identifier, scope, origin and declaration predicates. Compile admission or declaration
occurrence does not itself establish the effect or a prohibited-use report. Existing
canonical runtime/diagnostic witnesses retain their owners.

Attributes 8.5.1/8.5.2 fixture implementation:
`attributes_8_5_1_8_5_4_s852_defaults_and_imports` covers `implicit-public-local`,
`no-list-public`, `no-list-private-local`, `explicit-identifier-access`,
`use-associated-local-default` with 6 run-time assertion(s). Each assertion has at least
one conforming feature mutation that compiles and fails at run time on both checked
toolchains. Oracles use only exact INTEGER values, LOGICAL results of direct inquiry
expressions, and same-named sentinel resolution; no processor-dependent diagnostic text,
file-system effect or timing behavior is asserted.

**Oracle limitation:** All ten facets remain pending; no SourceUses inventory or canonical link is added. Even
a future reviewed source-use inventory has no facet-completion credit. Generic merges,
origin aggregation and declaration-role cases need independent source/evidence
adjudication. No default binding/component inheritance from a PUBLIC type,
compiler-consensus oracle, fake runtime program or silent closure.

Attributes 8.5.1/8.5.2 fixture implementation: 5 facet(s) are fixture-backed here and 5
remain pending. The fixtures do not grant source-use inventory credit, do not cover
enumeration accessibility, C817 invalid nonmodule contexts, vector-bound C816 negatives,
ASYNCHRONOUS pending-I/O obligations, or associated component/binding owner rules beyond
the named facets.

**Dependencies:** 8.6.1
R830/R831,C873-C876,p1/p2;14.2.2p2,p4-p9;15.4.3.4.1/.5;7.5.4.8p1;7.5.5p8;7.6.1/.2;19.3.1
and19.5.1.3/.4.

### S8.5.2-003: Use association requires PUBLIC in the exporting module

**Source:** 8.5.2 p4 final sentence, physical PDF120, printed106, lines15-16 **Class:** Restriction.

**Definition:** An identifier can be accessed from a module by use association only if it is PUBLIC in
that module. This is necessary, not sufficient to bypass the USE statement's other
entity, ONLY, renaming or generic restrictions. Private module identifiers may still
have legitimate host/descendant, argument-associated, public-alias/binding or separate
global access paths; the rule is not an undefined-value trap or a prohibition of every
way to reach an entity.

**Diagnostic obligation:** required.

**Facets:** `public-use-admission-source-use`, `private-data-name`, `private-type-name-canonical`, `private-procedure-name`, `private-generic-identifier`, `private-reexport`, `renamed-identifiers`, `host-and-descendant-access`, `member-and-binding-boundary`.

**Oracle:** The actual mandatory basis for an explicit forbidden USE name/generic/operator is C1409
with4.2p2(3), and19.5.1.3's scope rules referring to14.2.2 with4.2p2(6). Complete
submitted source and case-specific causal reports are required. This does not impose
mandatory reporting on arbitrary prose defaults or every failed access-like expression.

Attributes 8.5.1/8.5.2 fixture implementation:
`attributes_8_5_1_8_5_4_s852_private_use_resolution` covers `private-data-name`,
`private-procedure-name`, `private-generic-identifier`, `private-reexport` with 4
run-time assertion(s). Each assertion has at least one conforming feature mutation that
compiles and fails at run time on both checked toolchains. Oracles use only exact
INTEGER values, LOGICAL results of direct inquiry expressions, and same-named sentinel
resolution; no processor-dependent diagnostic text, file-system effect or timing
behavior is asserted.

**Oracle limitation:** All nine facets are pending. No new policy, fixture, graph, SourceUses entry or link
receipt is authored. A future report must bind the real private/unavailable exporting
identifier and source location; bare PUBLIC/PRIVATE words, echoes, unrelated
typing/shape/constructor errors, missing modules, unsupported facilities,
Internal/crash/resource failures and wrong-source recovery cannot count.
Ordinary0/nonzero reporting capability is distinct from fatal rejection. The existing
run repair and runtime witnesses must not be downgraded.

Attributes 8.5.1/8.5.2 fixture implementation: 4 facet(s) are fixture-backed here and 5
remain pending. The fixtures do not grant source-use inventory credit, do not cover
enumeration accessibility, C817 invalid nonmodule contexts, vector-bound C816 negatives,
ASYNCHRONOUS pending-I/O obligations, or associated component/binding owner rules beyond
the named facets.

**Dependencies:** 4.2p2(3)/(6);14.2.2p1/p2/p4-p9,R1409,C1407-C1410;19.5.1.3;8.6.1;8.8;14.2.3
C1412;19.5.1.2/.4;19.3.4p5/p6;7.5.2.2/7.5.4.8/7.5.5;15.4.3.4.

<!-- END GENERATED 8.5.2 -->
