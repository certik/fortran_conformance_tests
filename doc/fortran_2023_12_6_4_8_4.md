# Fortran 2023 12.6.4.8.4: Resolution of defined input/output procedures

The canonical catalogue is `doc/catalogues/defined_input_output_resolution_12_6_4_8_4.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.6.4.8.4 -->

### S12.6.4.8.4-001: Suitable generic interfaces match direction/form and dtv argument compatibility

**Source:** S12.6.4.8.4, J3/24-007, 18 December 2023, physical PDF274. **Class:** Effect.

**Definition:** A suitable generic interface for defined input/output has a defined-io-generic-spec
appropriate to the read/write direction and formatted/unformatted form of the data
transfer, and has a specific interface whose dtv argument is compatible with the
effective item according to argument association rules.

**Diagnostic obligation:** not-required.

**Facets:** `suitable-interface-matches-direction-and-form`, `suitable-interface-dtv-compatible-with-effective-item`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.4-002: Defined I/O occurs when permitted circumstances and a suitable procedure are both present

**Source:** S12.6.4.8.4, J3/24-007, 18 December 2023, physical PDF274. **Class:** Effect.

**Definition:** When a derived-type effective item is encountered during data transfer, defined
input/output occurs if the circumstances permit defined input/output and a suitable
defined input/output procedure is available.

**Diagnostic obligation:** not-required.

**Facets:** `defined-io-occurs-with-permitted-circumstance`, `defined-io-occurs-with-suitable-procedure`, `list-directed-namelist-unformatted-circumstances-permit-defined-io`, `dt-edit-descriptor-circumstance-permits-defined-io`, `type-bound-procedure-can-be-suitable`, `accessible-interface-can-be-suitable`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.4-003: Type-bound defined I/O resolution selects the dynamic-type corresponding binding

**Source:** S12.6.4.8.4, J3/24-007, 18 December 2023, physical PDF274. **Class:** Effect.

**Definition:** If a suitable generic type-bound procedure is available from the declared type, the
procedure reference is determined as for explicit type-bound procedure references: the
binding with the appropriate specific interface is located in the declared type and the
corresponding binding in the dynamic type is selected.

**Diagnostic obligation:** not-required.

**Facets:** `type-bound-defined-io-locates-declared-type-binding`, `type-bound-defined-io-selects-dynamic-type-binding`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

### S12.6.4.8.4-004: Accessible interface-block defined I/O resolves to the identified specific procedure

**Source:** S12.6.4.8.4, J3/24-007, 18 December 2023, physical PDF274. **Class:** Effect.

**Definition:** If no suitable generic type-bound procedure is available and a suitable generic
interface is accessible, the reference is to the procedure identified by the appropriate
specific interface in the interface block.

**Diagnostic obligation:** not-required.

**Facets:** `interface-block-defined-io-selects-appropriate-specific`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or using a compile-only conforming control) and then observe only portable
properties: exact internal records, successful IOSTAT zero, nonzero IOSTAT or named
status values where the standard names them, required dummy characteristics through a
conforming interface, or required diagnosis of a numbered syntax rule or constraint.
Negative plans change one source property from a conforming control and do not require a
particular diagnostic message, severity, exit status, printed rule number, IOMSG
wording, or processor-dependent numeric status value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IOMSG wording and processor-dependent numeric status values are never an oracle. A
diagnostic facet is only a future source-control plan when the cited numbered rule alone
is violated and the invalid program reaches that rule; unnumbered program restrictions
are not assumed to be diagnosed.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and existing clause 12/13
catalogues for source form, derived types, generic bindings, expressions, variables,
execution constructs, data-transfer statement forms, control information, file
positioning, and edit descriptors. Unregistered dependencies explicitly retained: later
12.6 effective-item and data-transfer termination rules; 12.7-12.11 connection,
transfer, status, and error semantics; clause 15 interface, generic, type-bound, and
argument association rules; clause 16 ISO_FORTRAN_ENV named constants; clause 19 scope
and lifetime controls where active parent data transfer statements are reached.

<!-- END GENERATED 12.6.4.8.4 -->
