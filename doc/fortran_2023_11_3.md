# Fortran 2023: 11.3 CONTINUE statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.3 -->

### S11.3-001: Executing CONTINUE has no effect

**Source:** 11.3 p1; J3/24-007 PDF p228. **Class:** Effect.

**Definition:** Execution of a CONTINUE statement has no effect.

**Diagnostic obligation:** not-required.

**Facets:** `continue-preserves-sequence`, `continue-as-computed-goto-fallthrough-model`.

**Oracle:** The observable property is that execution proceeds to the following statement with no
independent state change caused by CONTINUE.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must isolate the cited source unit, use a conforming positive control
when a diagnostic is expected, and observe control transfer by which statement executes
next rather than by assumed branch implementation, compiler wording, process status, or
agreement with another processor.

**Dependencies:** 11.2.3 computed GO TO uses CONTINUE as its out-of-range model;Clause 10
assignment/expression effects registered.

### R1161: CONTINUE statement syntax is the CONTINUE keyword

**Source:** 11.3 R1161; J3/24-007 PDF p228. **Class:** Syntax.

**Definition:** A continue-stmt is CONTINUE.

**Diagnostic obligation:** required.

**Facets:** `continue-stmt-form`.

**Oracle:** Syntax admission only; p1 owns the no-effect execution semantics.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures must isolate the cited source unit, use a conforming positive control
when a diagnostic is expected, and observe control transfer by which statement executes
next rather than by assumed branch implementation, compiler wording, process status, or
agreement with another processor.

**Dependencies:** 11.1.1 blocks and 8.1 execution-part context registered.

<!-- END GENERATED 11.3 -->
