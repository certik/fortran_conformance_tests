# Fortran 2023 16.7: Standard generic intrinsic procedures

The canonical catalogue is `doc/catalogues/standard_generic_intrinsic_procedures_16_7.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, note1, note2, p2, table16.1, p3, p4. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.7 -->

### S16.7-001: Standard intrinsic keyword names are the shown argument names

**Source:** S16.7, J3/24-007, 18 December 2023, physical PDF369. **Class:** Restriction.

**Definition:** For all standard intrinsic procedures, the shown argument names shall be used for
argument keywords if keyword form is used.

**Diagnostic obligation:** not-required.

**Facets:** `standard-intrinsic-keywords-use-shown-argument-names`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.7-002: Table 16.1 class codes define generic intrinsic classes

**Source:** S16.7, J3/24-007, 18 December 2023, physical PDF369. **Class:** Effect.

**Definition:** Table 16.1 class codes mean A atomic subroutine, C collective subroutine, E elemental
function, ES simple elemental subroutine, I inquiry function, PS simple subroutine when
FROM is not a coarray, S impure subroutine, SS simple subroutine, and T transformational
function.

**Diagnostic obligation:** not-required.

**Facets:** `class-code-a-atomic`, `class-code-c-collective`, `class-code-e-elemental`, `class-code-es-simple-elemental-subroutine`, `class-code-i-inquiry`, `class-code-ps-conditional-simple-subroutine`, `class-code-s-impure-subroutine`, `class-code-ss-simple-subroutine`, `class-code-t-transformational`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.7-003: Table 16.1 lists standard generic intrinsic names, arguments, classes, and descriptions

**Source:** S16.7, J3/24-007, 18 December 2023, physical PDF369. **Class:** Effect.

**Definition:** Table 16.1 is the standard generic intrinsic procedure summary, listing shown argument
forms, class codes, and short descriptions for: ABS, ACHAR, ACOS, ACOSD, ACOSH, ACOSPI,
ADJUSTL, ADJUSTR, AIMAG, AINT, ALL, ALLOCATED, ANINT, ANY, ASIN, ASIND, ASINH, ASINPI,
ASSOCIATED, ATAN, ATAN2, ATAN2D, ATAN2PI, ATAND, ATANH, ATANPI, ATOMIC_ADD, ATOMIC_AND,
ATOMIC_CAS, ATOMIC_DEFINE, ATOMIC_FETCH_ADD, ATOMIC_FETCH_AND, ATOMIC_FETCH_OR,
ATOMIC_FETCH_XOR, ATOMIC_OR, ATOMIC_REF, ATOMIC_XOR, BESSEL_J0, BESSEL_J1, BESSEL_JN,
BESSEL_Y0, BESSEL_Y1, BESSEL_YN, BGE, BGT, BIT_SIZE, BLE, BLT, BTEST, CEILING, CHAR,
CMPLX, CO_BROADCAST, CO_MAX, CO_MIN, CO_REDUCE, CO_SUM, COMMAND_ARGUMENT_COUNT, CONJG,
COS, COSD, COSH, COSHAPE, COSPI, COUNT, CPU_TIME, CSHIFT, DATE_AND_TIME, DBLE, DIGITS,
DIM, DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT, EPSILON, ERF, ERFC, ERFC_SCALED,
EVENT_QUERY, EXECUTE_COMMAND_LINE, EXP, EXPONENT, EXTENDS_TYPE_OF, FAILED_IMAGES,
FINDLOC, FLOOR, FRACTION, GAMMA, GET_COMMAND, GET_COMMAND_ARGUMENT,
GET_ENVIRONMENT_VARIABLE, GET_TEAM, HUGE, HYPOT, IACHAR, IALL, IAND, IANY, IBCLR, IBITS,
IBSET, ICHAR, IEOR, IMAGE_INDEX, IMAGE_STATUS, INDEX, INT, IOR, IPARITY, ISHFT, ISHFTC,
IS_CONTIGUOUS, IS_IOSTAT_END, IS_IOSTAT_EOR, KIND, LBOUND, LCOBOUND, LEADZ, LEN,
LEN_TRIM, LGE, LGT, LLE, LLT, LOG, LOG_GAMMA, LOG10, LOGICAL, MASKL, MASKR, MATMUL, MAX,
MAXEXPONENT, MAXLOC, MAXVAL, MERGE, MERGE_BITS, MIN, MINEXPONENT, MINLOC, MINVAL, MOD,
MODULO, MOVE_ALLOC, MVBITS, NEAREST, NEW_LINE, NEXT, NINT, NORM2, NOT, NULL, NUM_IMAGES,
OUT_OF_RANGE, PACK, PARITY, POPCNT, POPPAR, PRECISION, PRESENT, PREVIOUS, PRODUCT,
RADIX, RANDOM_INIT, RANDOM_NUMBER, RANDOM_SEED, RANGE, RANK, REAL, REDUCE, REPEAT,
RESHAPE, RRSPACING, SAME_TYPE_AS, SCALE, SCAN, SELECTED_CHAR_KIND, SELECTED_INT_KIND,
SELECTED_LOGICAL_KIND, SELECTED_REAL_KIND, SET_EXPONENT, SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, SIND, SINH, SINPI, SIZE, SPACING, SPLIT, SPREAD, SQRT, STOPPED_IMAGES,
STORAGE_SIZE, SUM, SYSTEM_CLOCK, TAN, TAND, TANH, TANPI, TEAM_NUMBER, THIS_IMAGE, TINY,
TOKENIZE, TRAILZ, TRANSFER, TRANSPOSE, TRIM, UBOUND, UCOBOUND, UNPACK, VERIFY.

**Diagnostic obligation:** not-required.

**Facets:** `standard-generic-intrinsic-procedure-summary`, `standard-generic-intrinsic-argument-forms`, `standard-generic-intrinsic-classifications`, `standard-generic-intrinsic-short-descriptions`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.7-004: EXECUTE_COMMAND_LINE off image 1 of the initial team has processor-dependent effect

**Source:** S16.7, J3/24-007, 18 December 2023, physical PDF374. **Class:** Effect.

**Definition:** The effect of calling EXECUTE_COMMAND_LINE on any image other than image 1 in the
initial team is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `execute-command-line-noninitial-image-effect-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.7-005: Other standard intrinsics in unordered segments are limited only by argument-use rules

**Source:** S16.7, J3/24-007, 18 December 2023, physical PDF374. **Class:** Effect.

**Definition:** Use of all other standard intrinsic procedures in unordered segments is subject only to
their argument use following 11.7.2.

**Diagnostic obligation:** not-required.

**Facets:** `other-standard-intrinsics-unordered-segments-only-argument-use-rules`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

<!-- END GENERATED 16.7 -->
