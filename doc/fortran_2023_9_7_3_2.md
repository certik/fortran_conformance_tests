# Fortran 2023: 9.7.3.2 Deallocation of allocatable variables

Source-only draft catalogue: `doc/catalogues/deallocation_of_allocatable_variables_9_7_3_2.json`.
No Fortran test program, compiler invocation, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 9.7.3.2 -->

### S9.7.3.2-001: Deallocating an unallocated allocatable variable is a DEALLOCATE error condition

**Source:** 9.7.3.2 p1, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** Executing a DEALLOCATE statement for an allocatable variable that is unallocated causes
an error condition in that DEALLOCATE statement. The STAT value and post-error status
are owned by 9.7.4; this requirement does not invent a static diagnostic duty.

**Diagnostic obligation:** not-required.

**Facets:** `unallocated-allocatable-deallocate-error`, `allocated-control-no-error`.

**Positive-control facets:** `allocated-control-no-error`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

S9.7.3.2-001 runtime fixtures: unallocated-allocatable-deallocate-error first allocates
INTEGER x(-3:-1), assigns nonzero sentinels [37,41,43], and checks
ALLOCATED/LBOUND/UBOUND/value before explicit DEALLOCATE with STAT=0. It then executes a
second DEALLOCATE on the now-unallocated variable and observes only the portable 9.7.4
consequence: STAT is a positive value distinct from STAT_STOPPED_IMAGE and
STAT_FAILED_IMAGE, and ALLOCATED(x) remains false; it then reallocates x(5:7) with fresh
nonzero values. The allocated-control positive fixture allocates x(-9:-7), proves
status/bounds/values, DEALLOCATEs with STAT=0, observes only ALLOCATED=.FALSE., then
reallocates x(4:6) with fresh values.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

S9.7.3.2-001 fixture boundaries: the error fixture covers the deterministic
unallocated-object error only, not processor-dependent numeric STAT codes, ERRMSG
contents, resource failure, pointer deallocation, coarray image errors, or any value
formerly stored in deallocated storage. The allocated-control facet is marked
positive-control evidence and does not claim additional ALLOCATE execution semantics
beyond establishing the required pre-deallocation state.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-002: Deallocating an allocatable TARGET makes associated pointer statuses undefined

**Source:** 9.7.3.2 p1, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** When an allocatable variable with the TARGET attribute is deallocated, the pointer
association status of every pointer associated with it becomes undefined. Because
undefined pointer association status is not a portable executable oracle, future tests
must avoid querying or dereferencing those pointers after the deallocation.

**Diagnostic obligation:** not-required.

**Facets:** `target-deallocation-undefines-associated-pointers`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-003: Allocatable variables associated with dummy arguments or associate names shall not be deallocated

**Source:** 9.7.3.2 p1, J3/24-007, 18 December 2023, physical PDF165. **Class:** Restriction.

**Definition:** An allocatable variable shall not be deallocated if the variable itself or any subobject
of it is argument associated with a dummy argument or construct associated with an
associate name. This is a program restriction; static reporting is not mandated for
every possible execution path.

**Diagnostic obligation:** not-required.

**Facets:** `argument-associated-allocatable-not-deallocated`, `construct-associated-allocatable-not-deallocated`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-004: Unsaved allocatable procedure locals are deallocated on RETURN or END except function results

**Source:** 9.7.3.2 p2, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** When procedure execution terminates by RETURN or END, an unsaved allocatable local
variable retains its allocation and definition status if it is a function result or a
subobject of a function result. Otherwise, an unsaved allocatable local variable that is
allocated is deallocated.

**Diagnostic obligation:** not-required.

**Facets:** `function-result-retains-status`, `ordinary-unsaved-local-deallocated`, `saved-local-control`.

**Positive-control facets:** `saved-local-control`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

S9.7.3.2-004 runtime fixtures: function-result-retains-status uses an allocatable
INTEGER function result allocated as r(-5:-3), proves its allocation/bounds/nonzero
values inside the function before END, and has the caller observe the retained copied
values [97,101,103]. ordinary-unsaved-local-deallocated calls a procedure twice; the
first call proves an unsaved local allocatable scratch(-4:-2) is allocated and defined,
and the second call observes at procedure entry that scratch is unallocated before
allocating fresh scratch(6:8). saved-local-control is positive-control evidence: a SAVE
allocatable retained(-8:-6) is allocated and defined on the first call and is still
allocated with the same bounds/values on the second call before explicit cleanup
deallocation.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

S9.7.3.2-004 fixture boundaries: the ordinary-local fixture observes automatic
deallocation only through the next invocation's allocation status and fresh
reallocation; it never references an out-of-scope local or deallocated storage. The
function-result fixture observes the result value after normal result delivery, not a
local variable after it has ceased to exist. The SAVE case is positive-control evidence
for the exception and uses explicit cleanup only to leave no retained allocation.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-005: Unsaved allocated allocatable BLOCK locals are deallocated when the BLOCK terminates

**Source:** 9.7.3.2 p3, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** When a BLOCK construct terminates, any unsaved allocated allocatable local variable of
that construct is deallocated.

**Diagnostic obligation:** not-required.

**Facets:** `block-local-automatic-deallocation`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

S9.7.3.2-005 runtime fixture: a loop executes the same BLOCK construct twice. The first
execution proves a BLOCK-local unsaved allocatable local(-6:-4) is allocated and filled
with [157,163,167]. After END BLOCK, the second execution observes only that the new
BLOCK-local local is unallocated on entry, then reallocates local(7:9) with fresh
nonzero values.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

S9.7.3.2-005 fixture boundaries: the fixture does not reference the BLOCK-local entity
after its construct terminates; the deallocation effect is observed only by re-entering
the construct and checking the new local allocation status before fresh allocation. It
does not cover SAVE BLOCK locals, finalization, coarrays, or abnormal control flow.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-006: Allocatable function results referenced in executable constructs are deallocated after the innermost construct

**Source:** 9.7.3.2 p4, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** If an executable construct executes a reference to a function whose result is
allocatable or has an allocatable subobject, an allocatable result and any allocated
allocatable subobject of the result is deallocated after execution of the innermost
executable construct containing the reference.

**Diagnostic obligation:** not-required.

**Facets:** `allocatable-result-after-innermost-construct`, `allocatable-subobject-after-innermost-construct`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-007: Allocatable function results referenced in specification parts are deallocated before executable constructs

**Source:** 9.7.3.2 p5, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** If a function whose result is allocatable or has an allocatable subobject is referenced
in the specification part of a scoping unit and that reference is executed, an
allocatable result and any allocated allocatable subobject of the result is deallocated
before execution of the executable constructs of the scoping unit.

**Diagnostic obligation:** not-required.

**Facets:** `specification-part-result-deallocated-before-execution`, `specification-part-subobject-deallocated-before-execution`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-008: INTENT(OUT) invocation deallocates corresponding allocated allocatable actuals and subobjects

**Source:** 9.7.3.2 p6, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** On procedure invocation, any allocated allocatable object that is an actual argument
corresponding to an INTENT(OUT) allocatable dummy argument is deallocated, and any
allocated allocatable object that is a subobject of an actual argument corresponding to
an INTENT(OUT) dummy argument is deallocated. The same deallocation-on-entry rule is
stated for C invoking Fortran through a C descriptor for an allocated allocatable
variable, and for Fortran invoking a C function through an interface with an INTENT(OUT)
allocatable dummy before C execution begins.

**Diagnostic obligation:** not-required.

**Facets:** `intent-out-allocatable-actual-deallocated`, `intent-out-actual-subobject-deallocated`, `c-calls-fortran-descriptor-deallocated`, `fortran-calls-c-actual-deallocated-before-c`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

S9.7.3.2-008 Fortran INTENT(OUT) runtime fixtures:
intent-out-allocatable-actual-deallocated allocates actual(-4:-2), proves
status/bounds/values, invokes an INTENT(OUT) allocatable dummy, and the callee observes
ALLOCATED(dummy)=.FALSE. before allocating dummy(8:10) with fresh sentinels observed by
the caller. intent-out-actual-subobject-deallocated allocates actual%part(-7:-5), proves
its status/bounds/tag/values, invokes a derived-type INTENT(OUT) dummy, and the callee
observes the component unallocated before allocating h%part(3:5) and assigning fresh
tag/value sentinels.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

S9.7.3.2-008 fixture boundaries: this packet covers only Fortran-calls-Fortran
INTENT(OUT) deallocation of an allocatable actual and of an allocatable component
subobject. It does not cover the two C descriptor facets, CFI descriptor state,
interoperability launch profiles, or any stale pre-call storage after invocation.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20);18.3.7p2/p4/p6;18.5.3p1-p5.

### S9.7.3.2-009: Intrinsic assignment deallocates noncoarray allocated allocatable subobjects before assignment

**Source:** 9.7.3.2 p7, J3/24-007, 18 December 2023, physical PDF165. **Class:** Effect.

**Definition:** When an intrinsic assignment statement is executed, any noncoarray allocated allocatable
subobject of the variable is deallocated before the assignment takes place. Coarray
subobjects are excluded by this paragraph and remain subject to coarray synchronization
rules where deallocation occurs.

**Diagnostic obligation:** not-required.

**Facets:** `intrinsic-assignment-noncoarray-subobject-before-assignment`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

S9.7.3.2-009 runtime fixture: a derived-type variable lhs has noncoarray allocatable
component lhs%part(-6:-4) allocated with nonzero values and tag 269 before intrinsic
assignment from rhs whose corresponding component is unallocated and tag is 283. After
lhs = rhs, the oracle observes only lhs%tag=283 and ALLOCATED(lhs%part)=.FALSE.; the old
component values are not read.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

S9.7.3.2-009 fixture boundaries: the fixture covers intrinsic assignment where the
right-hand allocatable component is unallocated, making the required pre-assignment
deallocation observable as post-assignment unallocated status. It does not cover
assignment from an allocated component, coarray components, defined assignment,
finalization ordering, or deallocated old component contents.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-010: Deallocating a derived-type variable deallocates allocated allocatable subobjects unless an error path leaves it processor dependent

**Source:** 9.7.3.2 p8, J3/24-007, 18 December 2023, physical PDF166. **Class:** Effect.

**Definition:** When a variable of derived type is deallocated, any allocated allocatable subobject is
deallocated. If an error condition occurs during deallocation, whether an allocated
allocatable subobject is deallocated is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `derived-deallocation-deallocates-allocatable-subobject`, `error-condition-subobject-status-processor-dependent`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

S9.7.3.2-010 runtime fixture: derived-deallocation-deallocates-allocatable-subobject
allocates a derived-type allocatable parent item, allocates its finalizable allocatable
component item%part(-11:-9), and proves parent status, component status, nonunit bounds,
tag, and nonzero component tokens [367,373,379] before DEALLOCATE(item) with STAT=0.
After deallocation it observes only ALLOCATED(item)=.FALSE. and an independent finalizer
log [3,367,373,379] from the component elements; the deallocated parent and component
are not referenced.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

S9.7.3.2-010 fixture boundaries: the derived-subobject fixture covers the successful
DEALLOCATE path for a noncoarray derived-type allocatable with one allocatable component
whose element finalizer records independent tokens before storage ceases to exist. It
does not cover processor-dependent error-condition subobject status, final subroutine
ordering for the parent object, coarray components, or contents of the deallocated
object after DEALLOCATE.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-011: Final subroutines run before automatic deallocation of allocatable components of finalizable objects

**Source:** 9.7.3.2 p9, J3/24-007, 18 December 2023, physical PDF166. **Class:** Effect.

**Definition:** If an allocatable component is a subobject of a finalizable object, any final subroutine
for that object is executed before the component is automatically deallocated. Final
subroutine selection and finalization events are owned by 7.5.6.

**Diagnostic obligation:** not-required.

**Facets:** `finalization-before-component-automatic-deallocation`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-012: Coarray deallocation synchronizes active images and succeeds only on all active images

**Source:** 9.7.3.2 p10, J3/24-007, 18 December 2023, physical PDF166. **Class:** Effect.

**Definition:** Executing a statement that deallocates a coarray or an object with a coarray potential
subobject component includes implicit synchronization of all active images in the
current team. A stopped or failed image in the current team causes an error condition.
If no other error occurs, active images are delayed after the statement until all other
active images in the current team have executed the same statement the same number of
times in that team; segments before the statement on an active image precede segments
after the statement on another active image; and a coarray shall not become deallocated
on an image unless it is successfully deallocated on all active images in the team.

**Diagnostic obligation:** not-required.

**Facets:** `implicit-synchronization`, `coarray-potential-subobject-component-trigger`, `stopped-or-failed-image-error-condition`, `same-statement-same-count-delay`, `segment-ordering`, `all-active-images-success-before-deallocated`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-013: Coarray dummy allocate-objects require corresponding coarray ultimate arguments

**Source:** 9.7.3.2 p11, J3/24-007, 18 December 2023, physical PDF166. **Class:** Restriction.

**Definition:** If an allocate-object in a DEALLOCATE statement is a coarray dummy argument, the
ultimate arguments on those images shall be corresponding coarrays as defined by
15.5.2.4. This is an image/argument correspondence restriction and does not by itself
impose a static diagnostic duty.

**Diagnostic obligation:** not-required.

**Facets:** `corresponding-coarray-ultimate-arguments`, `noncorresponding-coarray-ultimate-arguments-invalid`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

### S9.7.3.2-014: Automatic deallocation has the effect of DEALLOCATE without dealloc options

**Source:** 9.7.3.2 p12, J3/24-007, 18 December 2023, physical PDF166. **Class:** Effect.

**Definition:** Automatic deallocation has the same effect as a DEALLOCATE statement without a
dealloc-opt-list. STAT= and ERRMSG= are therefore not present for automatic deallocation
unless another owning construct provides its own status mechanism.

**Diagnostic obligation:** not-required.

**Facets:** `automatic-deallocation-same-effect`, `automatic-deallocation-no-dealloc-options`.

**Oracle:** All facets are pending source plans. Positive plans use independent literal values,
permitted allocation or pointer association inquiry results, STAT/ERRMSG observations
only through their owning subclauses, finalizer call sentinels, or reached-control-flow
sentinels that first establish the required preconditions. Negative plans use an
otherwise conforming source and a minimal repair, with invalidity attributed to the
named requirement rather than to declaration syntax, ALLOCATE ownership, NULLIFY
ownership, STAT= or ERRMSG= semantics, undefined data, address arithmetic, TRANSFER,
emitted code, compiler agreement, resource exhaustion, or unsupported launch mode.

S9.7.3.2-014 runtime fixture: automatic-deallocation-same-effect compares an explicit
DEALLOCATE without dealloc-opt-list and a procedure-exit automatic deallocation using
only permitted status and fresh reallocation observations. The explicit half allocates
explicit(-10:-8), proves status/bounds/values, DEALLOCATEs without options, observes
ALLOCATED=.FALSE., then reallocates explicit(4:6). The automatic half calls a procedure
twice; the first call proves auto(-5:-3) allocated and defined, while the second call
observes auto unallocated on entry and reallocates auto(10:12).

**Oracle limitation:** This is a source-only catalogue. It creates no Fortran test program, invokes no
compiler, records no evidence link, approves no oracle or fixture, and claims no
coverage. Numbered syntax rules and constraints carry the processor detection/reporting
duty from 4.2; unnumbered prose effects, execution-time allocation status, pointer
association status, STAT values, error conditions, image participation,
processor-dependent outcomes, and coarray synchronization requirements are not promoted
to mandatory static diagnostics. Future executable plans must not read deallocated
allocatable storage, dereference deallocated pointer targets, read undefined pointer or
C_PTR values, use address arithmetic, or rely on deliberate resource exhaustion.
Multi-image execution, stopped-image handling, failed-image handling, and teams are
separately gated capabilities in this suite.

S9.7.3.2-014 fixture boundaries: only the status/reallocation effects common to explicit
DEALLOCATE without options and automatic procedure-exit deallocation are covered. No
hidden STAT= or ERRMSG= is invented for the automatic event;
automatic-deallocation-no-dealloc-options remains pending as a source classification
facet.

**Dependencies:** 4.2;7.5.6.1p1-p2;7.5.6.2p1-p2;7.5.6.3p1-p8;8.5.3p1;8.5.6.1p1/C822-C826;8.5.6.2p1-p5/C827;8.5.6.3p1-p4;8.5.10
INTENT
attribute;9.7.1.2p5;9.7.1.3p1-p6;9.7.1.4p1-p2;9.7.3.1R944-R945/C957/p1-p4;9.7.4R946/p1-p6;9.7.5p1-p2;10.2.1.3;11.7.2p1-p4;15.5.2.4;16.9.13
ALLOCATED;16.10.2.28 STAT_FAILED_IMAGE;16.10.2.31
STAT_STOPPED_IMAGE;19.5.1;19.5.2.1-19.5.2.5;19.6.5(16)-(17);19.6.6(10),(15),(20).

<!-- END GENERATED 9.7.3.2 -->
