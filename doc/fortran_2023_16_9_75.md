# Fortran 2023 16.9.75: DSHIFTL

The canonical catalogue is `doc/catalogues/dshiftl_16_9_75.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.75")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.75 -->

### S16.9.75-001: DSHIFTL argument restrictions

**Source:** S16.9.75, J3/24-007, 18 December 2023, physical PDF404. **Class:** Restriction.

**Definition:** I and J shall be integer or BOZ literal constants with matching integer kinds when both
are integer, shall not both be BOZ literals, and SHIFT shall be an integer in the
permitted nonnegative BIT_SIZE range for DSHIFTL.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTL-I-integer-or-boz`, `DSHIFTL-J-integer-or-boz`, `DSHIFTL-integer-kinds-match`, `DSHIFTL-not-both-boz`, `DSHIFTL-SHIFT-integer-nonnegative-in-range`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only where the selected
operation is exact or where the standard defines an inquiry-model value. Every positive
plan establishes a non-default state first, uses a conforming companion where a zero or
.FALSE. result could be vacuous, and includes a feature mutation such as changing the
intrinsic name, argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are never given exact-value expected results; exact
plans are limited to integer/logical/character/shape/kind/bit effects and exactly
representable real inquiry-model properties. Grep of tests/ and
tests/fixtures/*/fixture.json for DIM, DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT,
and EPSILON found no existing fixture binding in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.75-002: DSHIFTL result characteristics follow the integer operand

**Source:** S16.9.75, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** The result of DSHIFTL has the same type and kind as I when I is integer, otherwise the
same type and kind as J.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTL-result-kind-from-I-or-J`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only where the selected
operation is exact or where the standard defines an inquiry-model value. Every positive
plan establishes a non-default state first, uses a conforming companion where a zero or
.FALSE. result could be vacuous, and includes a feature mutation such as changing the
intrinsic name, argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are never given exact-value expected results; exact
plans are limited to integer/logical/character/shape/kind/bit effects and exactly
representable real inquiry-model properties. Grep of tests/ and
tests/fixtures/*/fixture.json for DIM, DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT,
and EPSILON found no existing fixture binding in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.75-003: DSHIFTL converts a BOZ operand as INT to the other integer kind

**Source:** S16.9.75, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** If either I or J is a BOZ literal constant, DSHIFTL first converts it as if by INT to
integer with the kind type parameter of the other operand.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTL-boz-converted-as-int-to-other-kind`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only where the selected
operation is exact or where the standard defines an inquiry-model value. Every positive
plan establishes a non-default state first, uses a conforming companion where a zero or
.FALSE. result could be vacuous, and includes a feature mutation such as changing the
intrinsic name, argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are never given exact-value expected results; exact
plans are limited to integer/logical/character/shape/kind/bit effects and exactly
representable real inquiry-model properties. Grep of tests/ and
tests/fixtures/*/fixture.json for DIM, DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT,
and EPSILON found no existing fixture binding in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

### S16.9.75-004: DSHIFTL result bit composition and intrinsic equivalence

**Source:** S16.9.75, J3/24-007, 18 December 2023, physical PDF405. **Class:** Effect.

**Definition:** The DSHIFTL result is formed from shifted bits of I and J and is equal to
IOR(SHIFTL(I,SHIFT), SHIFTR(J,BIT_SIZE(J)-SHIFT)); the integer-as-bits model is the one
in 16.3.

**Diagnostic obligation:** not-required.

**Facets:** `DSHIFTL-shifted-bits-from-operands`, `DSHIFTL-ior-shift-equivalence`, `DSHIFTL-shift-zero-and-full-width-edges`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, character, shape,
kind, and bit-pattern observations, and use real values only where the selected
operation is exact or where the standard defines an inquiry-model value. Every positive
plan establishes a non-default state first, uses a conforming companion where a zero or
.FALSE. result could be vacuous, and includes a feature mutation such as changing the
intrinsic name, argument order, KIND, DIM, SHIFT, or BOUNDARY value. Unnumbered argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Processor-dependent approximations are never given exact-value expected results; exact
plans are limited to integer/logical/character/shape/kind/bit effects and exactly
representable real inquiry-model properties. Grep of tests/ and
tests/fixtures/*/fixture.json for DIM, DOT_PRODUCT, DPROD, DSHIFTL, DSHIFTR, EOSHIFT,
and EPSILON found no existing fixture binding in this worktree.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, internal I/O, and exact formatting helpers. Unregistered dependencies
retained: clause 16.3 and 16.4 model definitions for bit and real-model terminology;
sibling 16.9 intrinsic procedures named by result formulas; clauses 3, 5, 14, 15, 17,
18, and 19 where future fixtures need terms, constants, programs, procedure interfaces,
IEEE support, deleted features, or scope rules.

<!-- END GENERATED 16.9.75 -->
