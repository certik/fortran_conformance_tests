# Fortran 2023 16.9.107: IMAGE INDEX

The canonical catalogue is `doc/catalogues/image_index_16_9_107.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6. No fine source splits were introduced.

Clause 16 note: Description and Class paragraphs are definitions, unnumbered argument restrictions carry no diagnostic facet, and exact bit results are planned only in bit context. Coarray/image-dependent facets are marked OUT OF SCOPE by user decision.

<!-- BEGIN GENERATED 16.9.107 -->

### S16.9.107-001: IMAGE_INDEX argument and team restrictions

**Source:** S16.9.107, J3/24-007, 18 December 2023, physical PDF420. **Class:** Restriction.

**Definition:** COARRAY shall be a coarray with the rightmost part-ref having nonzero corank for
multipart designators; TEAM, TEAM_NUMBER, and default team selection govern where the
cosubscripts are established; SUB shall be a rank-one integer array of size equal to the
COARRAY corank; TEAM shall be scalar TEAM_TYPE identifying the current or an ancestor
team; TEAM_NUMBER shall be an integer scalar identifying the initial or a sibling team.

**Diagnostic obligation:** not-required.

**Facets:** `IMAGE_INDEX-coarray-argument`, `IMAGE_INDEX-rightmost-part-ref-nonzero-corank`, `IMAGE_INDEX-sub-rank-one-size-corank`, `IMAGE_INDEX-team-type-current-or-ancestor`, `IMAGE_INDEX-team-number-initial-or-sibling`, `IMAGE_INDEX-team-establishment`.

**Oracle:** Source registration only. Coarray/image-dependent runtime fixtures are intentionally not
planned for this suite: coarray, local-coarray, team, failed-image, and stopped-image
observations are OUT OF SCOPE by user decision. The only retained executable
IMAGE_STATUS plans use the current single image and observe default-integer kind or the
zero otherwise status fixed by the Result Value text. No diagnostic, multi-image
ordering, team-establishment, failed-image, or stopped-image coverage claim is created.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IMAGE_INDEX requires a coarray argument and team/cosubscript context, so every positive
runtime observation is OUT OF SCOPE by user decision, including local single-image
coarrays. IMAGE_STATUS facets concerning failed or stopped images are also OUT OF SCOPE;
the catalogue keeps only the single-image current-image zero-status and default-integer
characteristic plans that do not require coarrays. Unnumbered argument shalls are not
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kind parameters, declarations, arrays, expressions,
execution, internal I/O, and exact checking helpers. Unregistered dependencies retained:
clause 5 image and team execution model, clause 16.2 reduction and mask/DIM rules,
clause 16.3 bit model and BOZ conversion rules, ISO_FORTRAN_ENV named constants and
TEAM_TYPE, sibling 16.9 intrinsic definitions named by result formulas, and clauses 14,
15, 17, 18, and 19 for future module, interface, IEEE, deleted-feature, and scope
interactions.

### S16.9.107-002: IMAGE_INDEX result is a default integer scalar

**Source:** S16.9.107, J3/24-007, 18 December 2023, physical PDF420. **Class:** Effect.

**Definition:** The result characteristics are default integer scalar.

**Diagnostic obligation:** not-required.

**Facets:** `IMAGE_INDEX-default-integer-scalar-result`.

**Oracle:** Source registration only. Coarray/image-dependent runtime fixtures are intentionally not
planned for this suite: coarray, local-coarray, team, failed-image, and stopped-image
observations are OUT OF SCOPE by user decision. The only retained executable
IMAGE_STATUS plans use the current single image and observe default-integer kind or the
zero otherwise status fixed by the Result Value text. No diagnostic, multi-image
ordering, team-establishment, failed-image, or stopped-image coverage claim is created.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IMAGE_INDEX requires a coarray argument and team/cosubscript context, so every positive
runtime observation is OUT OF SCOPE by user decision, including local single-image
coarrays. IMAGE_STATUS facets concerning failed or stopped images are also OUT OF SCOPE;
the catalogue keeps only the single-image current-image zero-status and default-integer
characteristic plans that do not require coarrays. Unnumbered argument shalls are not
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kind parameters, declarations, arrays, expressions,
execution, internal I/O, and exact checking helpers. Unregistered dependencies retained:
clause 5 image and team execution model, clause 16.2 reduction and mask/DIM rules,
clause 16.3 bit model and BOZ conversion rules, ISO_FORTRAN_ENV named constants and
TEAM_TYPE, sibling 16.9 intrinsic definitions named by result formulas, and clauses 14,
15, 17, 18, and 19 for future module, interface, IEEE, deleted-feature, and scope
interactions.

### S16.9.107-003: IMAGE_INDEX maps valid cosubscripts to an image index and invalid cosubscripts to zero

**Source:** S16.9.107, J3/24-007, 18 December 2023, physical PDF420. **Class:** Effect.

**Definition:** Valid cosubscripts for the specified team produce the corresponding image index in that
team; otherwise the result is zero.

**Diagnostic obligation:** not-required.

**Facets:** `IMAGE_INDEX-valid-cosubscripts-image-index`, `IMAGE_INDEX-invalid-cosubscripts-zero`.

**Oracle:** Source registration only. Coarray/image-dependent runtime fixtures are intentionally not
planned for this suite: coarray, local-coarray, team, failed-image, and stopped-image
observations are OUT OF SCOPE by user decision. The only retained executable
IMAGE_STATUS plans use the current single image and observe default-integer kind or the
zero otherwise status fixed by the Result Value text. No diagnostic, multi-image
ordering, team-establishment, failed-image, or stopped-image coverage claim is created.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
IMAGE_INDEX requires a coarray argument and team/cosubscript context, so every positive
runtime observation is OUT OF SCOPE by user decision, including local single-image
coarrays. IMAGE_STATUS facets concerning failed or stopped images are also OUT OF SCOPE;
the catalogue keeps only the single-image current-image zero-status and default-integer
characteristic plans that do not require coarrays. Unnumbered argument shalls are not
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kind parameters, declarations, arrays, expressions,
execution, internal I/O, and exact checking helpers. Unregistered dependencies retained:
clause 5 image and team execution model, clause 16.2 reduction and mask/DIM rules,
clause 16.3 bit model and BOZ conversion rules, ISO_FORTRAN_ENV named constants and
TEAM_TYPE, sibling 16.9 intrinsic definitions named by result formulas, and clauses 14,
15, 17, 18, and 19 for future module, interface, IEEE, deleted-feature, and scope
interactions.

<!-- END GENERATED 16.9.107 -->
