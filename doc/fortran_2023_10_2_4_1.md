# Fortran 2023: 10.2.4.1 Form of the FORALL Construct

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 10.2.4.1 -->

### S10.2.4.1-001: FORALL constructs control assignment, WHERE, and nested FORALL bodies with one concurrent header and optional scalar mask

**Source:** 10.2.4.1 p1; J3/24-007 PDF p200. **Class:** Effect.

**Definition:** A FORALL construct allows multiple assignments, masked array assignments, nested FORALL
constructs, and FORALL statements to be controlled by a single concurrent-control-list
and scalar-mask-expr.

**Diagnostic obligation:** not-required.

**Facets:** `construct-controls-multiple-body-constructs`, `construct-uses-single-header-and-mask`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.1 expression syntax registered;10.2.1.3 assignment effects registered;10.2.2 pointer
assignment registered;10.2.3 WHERE registered;10.2.4.2 execution in this packet.

### R1051: A FORALL construct comprises a construct statement, zero or more body constructs, and an END FORALL statement

**Source:** 10.2.4.1 R1051; J3/24-007 PDF p200. **Class:** Syntax.

**Definition:** The forall-construct production is a forall-construct-stmt followed by zero or more
forall-body-construct occurrences and an end-forall-stmt.

**Diagnostic obligation:** not-required.

**Facets:** `empty-body-form`, `multi-body-form`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 11 construct execution general rules NOT YET REGISTERED.

### R1052: A FORALL construct statement is an optional construct name, FORALL, and a concurrent header

**Source:** 10.2.4.1 R1052; J3/24-007 PDF p200. **Class:** Syntax.

**Definition:** The forall-construct-stmt production admits an optional forall-construct-name followed
by FORALL and a concurrent-header.

**Diagnostic obligation:** not-required.

**Facets:** `unnamed-construct-stmt`, `named-construct-stmt`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 8 names registered;11.1.7 concurrent-header NOT YET REGISTERED;19.4 construct
names/index names NOT YET REGISTERED.

### R1053: FORALL body constructs are assignments, WHERE forms, nested FORALL constructs, or FORALL statements

**Source:** 10.2.4.1 R1053; J3/24-007 PDF p200. **Class:** Syntax.

**Definition:** A forall-body-construct is a forall-assignment-stmt, where-stmt, where-construct,
forall-construct, or forall-stmt.

**Diagnostic obligation:** not-required.

**Facets:** `assignment-body`, `where-stmt-body`, `where-construct-body`, `nested-forall-construct-body`, `nested-forall-stmt-body`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.1 assignment registered;10.2.2 pointer assignment registered;10.2.3 WHERE
registered;10.2.4.3 FORALL statement in this packet.

### R1054: A FORALL assignment statement is an intrinsic/defined assignment or pointer assignment statement

**Source:** 10.2.4.1 R1054; J3/24-007 PDF p200. **Class:** Syntax.

**Definition:** A forall-assignment-stmt is either an assignment-stmt or a pointer-assignment-stmt.

**Diagnostic obligation:** not-required.

**Facets:** `assignment-stmt-alternative`, `pointer-assignment-stmt-alternative`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 10.2.1 assignment registered;10.2.2 pointer assignment registered.

### R1055: An END FORALL statement has an optional construct name

**Source:** 10.2.4.1 R1055; J3/24-007 PDF p200. **Class:** Syntax.

**Definition:** The end-forall-stmt production is END FORALL with an optional forall-construct-name.

**Diagnostic obligation:** not-required.

**Facets:** `unnamed-end-forall`, `named-end-forall`.

**Oracle:** Positive source plans establish syntax, classification, final values, or scope without
relying on order, side effects, temporary storage, or compiler agreement. Assignment
conversion, element correspondence, character padding/truncation, and pointer-assignment
effects are credited to their registered owner clauses.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 8 names registered;19.4 construct names NOT YET REGISTERED.

### C1038: FORALL construct names on the start and END statements must match when present

**Source:** 10.2.4.1 C1038; J3/24-007 PDF p200. **Class:** Restriction.

**Definition:** If the forall-construct-stmt has a forall-construct-name, the end-forall-stmt shall have
the same forall-construct-name. If the end-forall-stmt has a forall-construct-name, the
forall-construct-stmt shall have the same forall-construct-name.

**Diagnostic obligation:** required.

**Facets:** `start-name-matched-by-end-name`, `end-name-matched-by-start-name`, `mismatched-names-rejected`, `end-name-without-start-name-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under the source
unit containing the prohibiting shall/shall-not text.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;8 names registered;19.4 construct names NOT YET REGISTERED.

### C1039: A FORALL body construct must not define an index name of the enclosing FORALL construct

**Source:** 10.2.4.1 C1039; J3/24-007 PDF p200. **Class:** Restriction.

**Definition:** A statement in a forall-body-construct shall not define an index-name of the
forall-construct.

**Diagnostic obligation:** required.

**Facets:** `body-does-not-define-index`, `body-index-definition-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under the source
unit containing the prohibiting shall/shall-not text.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;9.2 variable/definition dependencies registered;19.4
index-name scope NOT YET REGISTERED.

### C1040: Procedures referenced from a FORALL body construct must be pure

**Source:** 10.2.4.1 C1040; J3/24-007 PDF p200. **Class:** Restriction.

**Definition:** Any procedure referenced in a forall-body-construct, including one referenced by a
defined operation, assignment, or finalization, shall be a pure procedure.

**Diagnostic obligation:** required.

**Facets:** `pure-function-reference-allowed`, `impure-function-reference-rejected`, `impure-defined-operation-rejected`, `impure-defined-assignment-rejected`, `impure-finalization-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under the source
unit containing the prohibiting shall/shall-not text.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;10.1.6 defined operations registered;10.2.1.4 defined
assignment registered;15 pure procedures/finalization NOT YET REGISTERED.

### C1041: A FORALL body construct must not be a branch target

**Source:** 10.2.4.1 C1041; J3/24-007 PDF p200. **Class:** Restriction.

**Definition:** A forall-body-construct shall not be a branch target.

**Diagnostic obligation:** required.

**Facets:** `no-branch-target-control`, `branch-to-body-construct-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under the source
unit containing the prohibiting shall/shall-not text.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;11 branch and construct control rules NOT YET REGISTERED.

### S10.2.4.1-002: FORALL index-name scope and attributes are delegated to 19.4

**Source:** 10.2.4.1 p2; J3/24-007 PDF p200. **Class:** Effect.

**Definition:** The scope and attributes of an index-name in a concurrent-header in a FORALL construct
or statement are described in 19.4.

**Diagnostic obligation:** not-required.

**Facets:** `construct-index-scope-dependency`, `statement-index-scope-dependency`.

**Oracle:** No diagnostic or executable oracle is claimed here; this unit is a dependency pointer to
unregistered 19.4.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future FORALL fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect final values and scoping directly; must preload
untouched or inactive objects with distinguishable sentinels; must not compute masking
or selection oracles with MERGE, PACK, COUNT, or an equivalent; must not infer index
processing order, assignment order, association order, function call counts, side
effects, processor temporaries, simultaneity, or temporary storage; and must not rely on
compiler agreement, diagnostic wording, emitted code, undefined values, resource
exhaustion, or processor-dependent kind availability.

**Dependencies:** 19.4 scope and attributes of index names NOT YET REGISTERED;10.2.4.3 p3 in this packet.

<!-- END GENERATED 10.2.4.1 -->
