# Fortran 2023 8.6.13: PROTECTED statement

Effective source status is `Registry.catalogue_review_state("8.6.13")`.
Source classification, case approval and compiler observations are separate.
The source packet is registered after independent review and PPSR-001
closeout in batch076. All ten local facets remain pending, not executed.

The canonical record is `doc/catalogues/protected_statement_8_6_13.json`.
R858 and p1 are on physical PDF140 of J3/24-007, December18,2023,688pages,
SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The following heading is8.6.14 SAVE statement on the same page.

The optional double colon does not make the name list optional. R401
supplies the nonempty list; the complete502-head check found no explicit
entity-name production, leaving R402 for name syntax. A valid subject is a
variable or procedure pointer under C857, not any arbitrary named entity.
No inline shape, component designator or procedure invocation is part of
R858. Grammar controls require an actual module specification part and one
explicit PROTECTED specification, not duplicated type/procedure attributes.

P1 is a normative route to8.5.15, not a new S effect. C856-C860 retain
module-only specification, entity/common restrictions and the distinct
use-associated value-definition and pointer-association contexts. Defining
an already protected ancestor entity in a descendant is different from
giving a new submodule entity the attribute. PROTECTED does not mean PRIVATE,
immutable target storage, synchronization, initialized pointer status or
extended target lifetime. Subobject inheritance and qualified loss-of-target
exceptions remain canonical8.5.15p2/p3 relationships.

Registration supplies source and inventory-context reviews, not fixture,
profile, link or SourceUse approvals or compiler observations. Current facet
states remain in the catalogue.

<!-- BEGIN BATCH320 ATTRIBUTE STATEMENTS 8.6.13 -->
Batch320 PROTECTED statement fixtures retain omitted-list and declarator-suffix diagnostic/control pairs. Frozen LFortran rejects the conforming PROTECTED controls, so those reference-backed failures are reported rather than weakened.
<!-- END BATCH320 ATTRIBUTE STATEMENTS 8.6.13 -->

<!-- BEGIN GENERATED 8.6.13 -->

### R858: A PROTECTED statement requires a list of eligible entity names

**Source:** 8.6.13 R858, J3/24-007, 18 December 2023, physical PDF140 **Class:** Syntax.

**Definition:** PROTECTED is followed by an optional double colon and a nonempty comma-separated
entity-name list. R401 and the assumed R402 supply the list and names. A name may
identify an eligible variable or procedure pointer, but the syntax contains no
array/coarray declarator, component reference or invocation. The statement must obey
module-specification, eligibility, common-block and single-attribute constraints; the
operative protection rules are those of8.5.15.

**Diagnostic obligation:** required.

**Facets:** `double-colon-form`, `no-double-colon-form`, `multiple-module-data`, `mixed-variable-procedure-pointer`, `missing-list-exclusion`, `name-not-declarator`, `eligibility-and-scope-source`, `single-specification-source`, `protected-use-context-source`, `subobject-and-pointer-state-source`.

**Oracle:** Complete standalone-statement compile admissions and source-isolated name/list repairs
in a legitimate module specification part. Attribute restrictions and effects retain
their canonical owners; a property declaration does not itself execute a protected
operation.

Batch320 R858 attribute-statement fixtures: two diagnostic pairs isolate an omitted
PROTECTED entity list and an array declarator suffix, each repaired by adding or
retaining the same eligible module entity. No syntax-form admission facet is claimed
without a malformed-statement control.

**Oracle limitation:** Required syntax reporting does not prescribe fatal rejection, particular English or a
printed R858. Wrong scope/entity, duplicate attributes, COMMON membership, missing
procedure interfaces, unsupported PROTECTED, quoted/context-only records, wrong origins
and native failures are not independent grammar witnesses. No association, target-value,
subobject, access-control or runtime-effect approval is created.

Batch320 R858 limits: only the omitted-list and declarator-suffix diagnostics are
covered. Optional-colon forms, multi-name/mixed lists, source relationships to
C857-C860, single-specification overlaps, use-context links, and pointer/subobject state
remain pending. Frozen LFortran rejects the paired conforming PROTECTED controls as
unsupported; those reference-valid controls are retained.

**Dependencies:** 4.1.3R401/R402/p2;4.2p2;8.5.1C815;8.5.2;8.5.14C855;8.5.15C856-C860/p1-p3;14.2;15.4.3.2p5;15.4.3.6p1;19.5.1;19.5.2;19.6.7;19.6.8.

<!-- END GENERATED 8.6.13 -->
