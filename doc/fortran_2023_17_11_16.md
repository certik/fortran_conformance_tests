# Fortran 2023 17.11.16: IEEE_LOGB

The canonical catalogue is `doc/catalogues/ieee_logb_17_11_16.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.16")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.16 -->

### S17.11.16-001: IEEE_LOGB description defines the exponent operation

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Effect.

**Definition:** IEEE_LOGB is the IEEE exponent function.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-operation-description`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-002: IEEE_LOGB is an elemental function

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Effect.

**Definition:** IEEE_LOGB has the elemental function class.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-elemental-function-class`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-003: IEEE_LOGB argument X is real

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Restriction.

**Definition:** The X argument of IEEE_LOGB shall be of type real.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-X-real`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-004: IEEE_LOGB requires supported datatype

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Restriction.

**Definition:** IEEE_LOGB shall not be invoked unless IEEE_SUPPORT_DATATYPE(X) has the value true.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-requires-supported-datatype`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-005: IEEE_LOGB result has the same characteristics as X

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Effect.

**Definition:** The result of IEEE_LOGB has the same characteristics as X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-result-same-characteristics-as-X`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-006: IEEE_LOGB finite nonzero result is the unbiased exponent

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Effect.

**Definition:** For finite nonzero X that is neither infinity nor NaN, IEEE_LOGB(X) has the value of the
unbiased exponent of X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-finite-nonzero-unbiased-exponent`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-007: IEEE_LOGB zero result and divide-by-zero flag

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Effect.

**Definition:** For X equal to zero, IEEE_LOGB returns negative infinity on processors supporting
infinity for X and -HUGE(X) otherwise; IEEE_DIVIDE_BY_ZERO signals.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-zero-negative-infinity-when-supported`, `IEEE_LOGB-zero-negative-huge-without-infinity`, `IEEE_LOGB-zero-signals-divide-by-zero`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-008: IEEE_LOGB infinity result is positive infinity

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Effect.

**Definition:** For supported infinity input X, IEEE_LOGB(X) has the value positive infinity.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-infinity-result-positive-infinity`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

### S17.11.16-009: IEEE_LOGB NaN result is NaN

**Source:** S17.11.16, J3/24-007, 18 December 2023, physical PDF493. **Class:** Effect.

**Definition:** For supported NaN input X, IEEE_LOGB(X) has a NaN result.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_LOGB-nan-result-nan`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the IEEE_ARITHMETIC procedure. Guarded plans first
probe the relevant IEEE_SUPPORT_* inquiry or fixture capability profile, execute the
exact-result assertion only on the supported branch actually observed, and record an
unsupported branch as a skip rather than a pass. Unnumbered argument and invocation
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for IEEE_LOGB, IEEE_MAX, IEEE_MAX_MAG,
IEEE_MAX_NUM, IEEE_MAX_NUM_MAG, IEEE_MIN, and sections 17.11.16 through 17.11.21 found
no existing fixture metadata or Fortran calls binding these source facets. NaN payload
identity, result-operand identity for equal same-sign operands, and signaling-NaN
trapping mechanics remain processor dependent or non-portably observable; exact class,
sign, value, and flag effects named by the source are retained as guarded pending plans.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, and intrinsic-procedure foundations from clauses 6 through
16 where already catalogued. Unregistered dependencies retained: clause 3 terminology;
7.4 real type and kind foundations; 10.1 expression rules; 15 procedure and
dummy-argument association rules; 16.4 numeric model terminology; 17.2 through 17.10
IEEE modules, derived types, named constants, flags, modes, classes, and support
inquiries; neighbouring 17.11 procedures used by delegated result definitions; and
clauses 18 through 19 where future fixtures need interoperability, deleted-feature, or
scope terms.

<!-- END GENERATED 17.11.16 -->
