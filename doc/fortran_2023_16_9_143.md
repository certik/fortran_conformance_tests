# Fortran 2023 16.9.143: MINLOC

The canonical catalogue is `doc/catalogues/minloc_16_9_143.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.143")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6, p7, p8. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.143 -->

### S16.9.143-001: MINLOC argument restrictions

**Source:** S16.9.143, J3/24-007, 18 December 2023, physical PDF435. **Class:** Restriction.

**Definition:** ARRAY shall be an integer, real, or character array; DIM shall be an in-range integer
scalar; MASK shall be logical and conformable; KIND shall be a scalar integer constant
expression; BACK shall be a logical scalar.

**Diagnostic obligation:** not-required.

**Facets:** `MINLOC-ARRAY-integer-real-or-character-array`, `MINLOC-DIM-integer-scalar-in-range`, `MINLOC-MASK-logical-conformable`, `MINLOC-KIND-scalar-integer-constant-expression`, `MINLOC-BACK-logical-scalar`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINLOC occurrence and no
S16.9.143 binding. Reader extraction for p8 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.143-002: MINLOC result kind, rank, and shape follow KIND and DIM

**Source:** S16.9.143, J3/24-007, 18 December 2023, physical PDF435. **Class:** Effect.

**Definition:** MINLOC returns integer of the KIND value or default integer; without DIM it has rank one
and size equal to ARRAY rank, and with DIM it has rank n-1 and ARRAY shape with the DIM
extent removed.

**Diagnostic obligation:** not-required.

**Facets:** `MINLOC-result-kind-from-KIND`, `MINLOC-result-default-integer-without-KIND`, `MINLOC-dim-absent-rank-one-size-rank`, `MINLOC-dim-present-rank-and-shape`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINLOC occurrence and no
S16.9.143 binding. Reader extraction for p8 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.143-003: MINLOC without DIM locates eligible minima and returns zeros for empty selections

**Source:** S16.9.143, J3/24-007, 18 December 2023, physical PDF435. **Class:** Effect.

**Definition:** Without DIM, MINLOC returns subscripts of an ARRAY element equal to the eligible
minimum, with zero subscripts for zero-size ARRAY or no true MASK elements.

**Diagnostic obligation:** not-required.

**Facets:** `MINLOC-no-DIM-unmasked-minimum-subscripts`, `MINLOC-no-DIM-subscript-ranges`, `MINLOC-zero-size-array-returns-zeros`, `MINLOC-mask-selects-true-elements`, `MINLOC-all-false-mask-returns-zeros`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINLOC occurrence and no
S16.9.143 binding. Reader extraction for p8 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.143-004: MINLOC with DIM reduces each selected section

**Source:** S16.9.143, J3/24-007, 18 December 2023, physical PDF435. **Class:** Effect.

**Definition:** With DIM, rank-one ARRAY produces the first element of the corresponding no-DIM MINLOC
result, and higher-rank ARRAY applies MINLOC to each selected rank-one section.

**Diagnostic obligation:** not-required.

**Facets:** `MINLOC-DIM-rank-one-scalar-location`, `MINLOC-DIM-higher-rank-section-locations`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINLOC occurrence and no
S16.9.143 binding. Reader extraction for p8 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.143-005: MINLOC tie handling follows first element order unless BACK is true

**Source:** S16.9.143, J3/24-007, 18 December 2023, physical PDF435. **Class:** Effect.

**Definition:** A unique minimum returns its subscripts; tied minima return the first in array element
order unless BACK is true, and BACK true returns the last in array element order.

**Diagnostic obligation:** not-required.

**Facets:** `MINLOC-unique-minimum-returns-that-location`, `MINLOC-tie-first-with-BACK-absent-or-false`, `MINLOC-tie-last-with-BACK-true`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINLOC occurrence and no
S16.9.143 binding. Reader extraction for p8 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.143-006: MINLOC character arrays use intrinsic relational operator ordering

**Source:** S16.9.143, J3/24-007, 18 December 2023, physical PDF435. **Class:** Effect.

**Definition:** For character ARRAY, MINLOC selects according to intrinsic relational operators and the
character collating sequence for the argument kind.

**Diagnostic obligation:** not-required.

**Facets:** `MINLOC-character-selected-by-relational-operators`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINLOC occurrence and no
S16.9.143 binding. Reader extraction for p8 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.143 -->
