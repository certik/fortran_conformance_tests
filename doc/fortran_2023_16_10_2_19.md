# Fortran 2023 16.10.2.19: LOCK_TYPE



The canonical catalogue is `doc/catalogues/lock_type_16_10_2_19.json`. This source packet records source accounting and pending plans only, not fixture approval.



Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.



Base units read and accounted: p1, p2, C1607, C1608, C1609, C1610, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.



Catalogue source review: draft.



<!-- BEGIN GENERATED 16.10.2.19 -->

### S16.10.2.19-001: LOCK_TYPE has the specified opaque derived-type properties

**Source:** S16.10.2.19, J3/24-007, 18 December 2023, physical PDF475. **Class:** Effect.

**Definition:** LOCK_TYPE is a derived type with private nonallocatable nonpointer components, no type
parameters, extensibility, and default initialization for all components.

**Diagnostic obligation:** not-required.

**Facets:** `LOCK_TYPE-opaque-derived-type-properties`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, kind, shape,
standard I/O status, and preconnected-unit observations. Processor-dependent unit
numbers, kind ordering, storage-unit sizes, image failure capability, lock state from
other images, team identity, IOMSG text, and processor-dependent STAT or IOSTAT numeric
values are not hard-coded. Positive plans initialize observed variables to distinguished
sentinels, exercise the named ISO_FORTRAN_ENV entity with concrete values, and name a
feature mutation such as replacing the named constant, changing the requested bit-size
table, or substituting a different preconnected unit.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray, image-control, failed-image, lock, notify, team, and atomic observations,
including local single-image coarrays, are out of scope by user decision. Clause 16
unnumbered argument and module-entity restrictions are not promoted to diagnostic
facets; numbered constraints remain source-registered without fixture binding in this
packet. Grep coverage: LOCK_TYPE appears in five tests/ files and three fixture
manifests for other clauses; no fixture manifest binds a 16.10.2.19 facet or
C1607-C1610.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, data transfer, IOSTAT handling, preconnection, and exact formatting helpers.
Unregistered dependencies retained: clause 5 image and team execution model; clause 11
image-control, lock, notify, and team constructs; clause 16.5 atomic subroutines;
sibling ISO_FORTRAN_ENV sections 16.10-16.10.2.17; clause 17 IEEE support; and clause 19
storage association and storage-unit terminology.

### S16.10.2.19-002: LOCK_TYPE scalar variables are lock variables with locked and unlocked states

**Source:** S16.10.2.19, J3/24-007, 18 December 2023, physical PDF475. **Class:** Effect.

**Definition:** A scalar LOCK_TYPE variable is a lock variable; default construction represents the
unlocked state, all other values represent locked state, and LOCK and UNLOCK statements
change the value.

**Diagnostic obligation:** not-required.

**Facets:** `LOCK_TYPE-lock-variable-state-model`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, kind, shape,
standard I/O status, and preconnected-unit observations. Processor-dependent unit
numbers, kind ordering, storage-unit sizes, image failure capability, lock state from
other images, team identity, IOMSG text, and processor-dependent STAT or IOSTAT numeric
values are not hard-coded. Positive plans initialize observed variables to distinguished
sentinels, exercise the named ISO_FORTRAN_ENV entity with concrete values, and name a
feature mutation such as replacing the named constant, changing the requested bit-size
table, or substituting a different preconnected unit.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray, image-control, failed-image, lock, notify, team, and atomic observations,
including local single-image coarrays, are out of scope by user decision. Clause 16
unnumbered argument and module-entity restrictions are not promoted to diagnostic
facets; numbered constraints remain source-registered without fixture binding in this
packet. Grep coverage: LOCK_TYPE appears in five tests/ files and three fixture
manifests for other clauses; no fixture manifest binds a 16.10.2.19 facet or
C1607-C1610.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, data transfer, IOSTAT handling, preconnection, and exact formatting helpers.
Unregistered dependencies retained: clause 5 image and team execution model; clause 11
image-control, lock, notify, and team constructs; clause 16.5 atomic subroutines;
sibling ISO_FORTRAN_ENV sections 16.10-16.10.2.17; clause 17 IEEE support; and clause 19
storage association and storage-unit terminology.

### C1607: LOCK_TYPE named entities and components have required object categories

**Source:** S16.10.2.19, J3/24-007, 18 December 2023, physical PDF475. **Class:** Restriction.

**Definition:** A named entity with declared type LOCK_TYPE, or with a noncoarray potential subobject
component of declared type LOCK_TYPE, shall be a variable; such a component shall be a
data component.

**Diagnostic obligation:** required.

**Facets:** `C1607-lock-type-variable-and-data-component`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, kind, shape,
standard I/O status, and preconnected-unit observations. Processor-dependent unit
numbers, kind ordering, storage-unit sizes, image failure capability, lock state from
other images, team identity, IOMSG text, and processor-dependent STAT or IOSTAT numeric
values are not hard-coded. Positive plans initialize observed variables to distinguished
sentinels, exercise the named ISO_FORTRAN_ENV entity with concrete values, and name a
feature mutation such as replacing the named constant, changing the requested bit-size
table, or substituting a different preconnected unit.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray, image-control, failed-image, lock, notify, team, and atomic observations,
including local single-image coarrays, are out of scope by user decision. Clause 16
unnumbered argument and module-entity restrictions are not promoted to diagnostic
facets; numbered constraints remain source-registered without fixture binding in this
packet. Grep coverage: LOCK_TYPE appears in five tests/ files and three fixture
manifests for other clauses; no fixture manifest binds a 16.10.2.19 facet or
C1607-C1610.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, data transfer, IOSTAT handling, preconnection, and exact formatting helpers.
Unregistered dependencies retained: clause 5 image and team execution model; clause 11
image-control, lock, notify, and team constructs; clause 16.5 atomic subroutines;
sibling ISO_FORTRAN_ENV sections 16.10-16.10.2.17; clause 17 IEEE support; and clause 19
storage association and storage-unit terminology.

### C1608: LOCK_TYPE named variables and containing variables are coarrays

**Source:** S16.10.2.19, J3/24-007, 18 December 2023, physical PDF475. **Class:** Restriction.

**Definition:** A named variable with declared type LOCK_TYPE, or with a noncoarray potential subobject
component of type LOCK_TYPE, shall be a coarray.

**Diagnostic obligation:** required.

**Facets:** `C1608-lock-type-coarray-requirement`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, kind, shape,
standard I/O status, and preconnected-unit observations. Processor-dependent unit
numbers, kind ordering, storage-unit sizes, image failure capability, lock state from
other images, team identity, IOMSG text, and processor-dependent STAT or IOSTAT numeric
values are not hard-coded. Positive plans initialize observed variables to distinguished
sentinels, exercise the named ISO_FORTRAN_ENV entity with concrete values, and name a
feature mutation such as replacing the named constant, changing the requested bit-size
table, or substituting a different preconnected unit.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray, image-control, failed-image, lock, notify, team, and atomic observations,
including local single-image coarrays, are out of scope by user decision. Clause 16
unnumbered argument and module-entity restrictions are not promoted to diagnostic
facets; numbered constraints remain source-registered without fixture binding in this
packet. Grep coverage: LOCK_TYPE appears in five tests/ files and three fixture
manifests for other clauses; no fixture manifest binds a 16.10.2.19 facet or
C1607-C1610.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, data transfer, IOSTAT handling, preconnection, and exact formatting helpers.
Unregistered dependencies retained: clause 5 image and team execution model; clause 11
image-control, lock, notify, and team constructs; clause 16.5 atomic subroutines;
sibling ISO_FORTRAN_ENV sections 16.10-16.10.2.17; clause 17 IEEE support; and clause 19
storage association and storage-unit terminology.

### C1609: Lock variables appear in only the permitted definition contexts

**Source:** S16.10.2.19, J3/24-007, 18 December 2023, physical PDF475. **Class:** Restriction.

**Definition:** A lock variable shall not appear in a variable definition context except as the
lock-variable in LOCK or UNLOCK, as an allocate-object, or as an explicit-interface
INTENT(INOUT) actual argument.

**Diagnostic obligation:** required.

**Facets:** `C1609-lock-variable-definition-contexts`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, kind, shape,
standard I/O status, and preconnected-unit observations. Processor-dependent unit
numbers, kind ordering, storage-unit sizes, image failure capability, lock state from
other images, team identity, IOMSG text, and processor-dependent STAT or IOSTAT numeric
values are not hard-coded. Positive plans initialize observed variables to distinguished
sentinels, exercise the named ISO_FORTRAN_ENV entity with concrete values, and name a
feature mutation such as replacing the named constant, changing the requested bit-size
table, or substituting a different preconnected unit.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray, image-control, failed-image, lock, notify, team, and atomic observations,
including local single-image coarrays, are out of scope by user decision. Clause 16
unnumbered argument and module-entity restrictions are not promoted to diagnostic
facets; numbered constraints remain source-registered without fixture binding in this
packet. Grep coverage: LOCK_TYPE appears in five tests/ files and three fixture
manifests for other clauses; no fixture manifest binds a 16.10.2.19 facet or
C1607-C1610.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, data transfer, IOSTAT handling, preconnection, and exact formatting helpers.
Unregistered dependencies retained: clause 5 image and team execution model; clause 11
image-control, lock, notify, and team constructs; clause 16.5 atomic subroutines;
sibling ISO_FORTRAN_ENV sections 16.10-16.10.2.17; clause 17 IEEE support; and clause 19
storage association and storage-unit terminology.

### C1610: Variables with LOCK_TYPE subobjects appear in only the permitted definition contexts

**Source:** S16.10.2.19, J3/24-007, 18 December 2023, physical PDF475. **Class:** Restriction.

**Definition:** A variable with a LOCK_TYPE subobject shall not appear in a variable definition context
except as an allocate-object or as an explicit-interface INTENT(INOUT) actual argument.

**Diagnostic obligation:** required.

**Facets:** `C1610-lock-subobject-definition-contexts`.

**Oracle:** Source registration only. Future fixtures use exact integer, logical, kind, shape,
standard I/O status, and preconnected-unit observations. Processor-dependent unit
numbers, kind ordering, storage-unit sizes, image failure capability, lock state from
other images, team identity, IOMSG text, and processor-dependent STAT or IOSTAT numeric
values are not hard-coded. Positive plans initialize observed variables to distinguished
sentinels, exercise the named ISO_FORTRAN_ENV entity with concrete values, and name a
feature mutation such as replacing the named constant, changing the requested bit-size
table, or substituting a different preconnected unit.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Coarray, image-control, failed-image, lock, notify, team, and atomic observations,
including local single-image coarrays, are out of scope by user decision. Clause 16
unnumbered argument and module-entity restrictions are not promoted to diagnostic
facets; numbered constraints remain source-registered without fixture binding in this
packet. Grep coverage: LOCK_TYPE appears in five tests/ files and three fixture
manifests for other clauses; no fixture manifest binds a 16.10.2.19 facet or
C1607-C1610.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays,
execution, data transfer, IOSTAT handling, preconnection, and exact formatting helpers.
Unregistered dependencies retained: clause 5 image and team execution model; clause 11
image-control, lock, notify, and team constructs; clause 16.5 atomic subroutines;
sibling ISO_FORTRAN_ENV sections 16.10-16.10.2.17; clause 17 IEEE support; and clause 19
storage association and storage-unit terminology.

<!-- END GENERATED 16.10.2.19 -->

