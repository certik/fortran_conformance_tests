# Fortran 2023 16.9.144: MINVAL

The canonical catalogue is `doc/catalogues/minval_16_9_144.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.144")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.144 -->

### S16.9.144-001: MINVAL argument restrictions

**Source:** S16.9.144, J3/24-007, 18 December 2023, physical PDF436. **Class:** Restriction.

**Definition:** ARRAY shall be an integer, real, or character array; DIM shall be an in-range integer
scalar; MASK shall be logical and conformable.

**Diagnostic obligation:** not-required.

**Facets:** `MINVAL-ARRAY-integer-real-or-character-array`, `MINVAL-DIM-integer-scalar-in-range`, `MINVAL-MASK-logical-conformable`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINVAL occurrence and no
S16.9.144 binding. Reader extraction for p7 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.144-002: MINVAL result type, parameters, rank, and shape follow ARRAY and DIM

**Source:** S16.9.144, J3/24-007, 18 December 2023, physical PDF436. **Class:** Effect.

**Definition:** MINVAL has the same type and type parameters as ARRAY; without DIM it is scalar, and
with DIM it has rank n-1 and ARRAY shape with the DIM extent removed.

**Diagnostic obligation:** not-required.

**Facets:** `MINVAL-result-same-type-and-parameters`, `MINVAL-dim-absent-scalar`, `MINVAL-dim-present-rank-and-shape`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINVAL occurrence and no
S16.9.144 binding. Reader extraction for p7 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.144-003: MINVAL without DIM returns nonempty minimum values

**Source:** S16.9.144, J3/24-007, 18 December 2023, physical PDF436. **Class:** Effect.

**Definition:** For nonzero-size ARRAY without DIM, MINVAL returns the minimum value of all elements of
ARRAY.

**Diagnostic obligation:** not-required.

**Facets:** `MINVAL-integer-nonempty-minimum`, `MINVAL-real-nonempty-minimum`, `MINVAL-character-nonempty-minimum-by-relational-operators`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINVAL occurrence and no
S16.9.144 binding. Reader extraction for p7 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.144-004: MINVAL zero-size ARRAY returns type-specific largest values

**Source:** S16.9.144, J3/24-007, 18 December 2023, physical PDF436. **Class:** Effect.

**Definition:** For zero-size integer or real ARRAY, MINVAL returns the positive number of largest
magnitude supported for that type and kind; for zero-size character ARRAY, it returns a
string of LEN(ARRAY) copies of CHAR(n-1,KIND(ARRAY)).

**Diagnostic obligation:** not-required.

**Facets:** `MINVAL-zero-size-integer-HUGE`, `MINVAL-zero-size-real-HUGE`, `MINVAL-zero-size-character-last-collating-character`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINVAL occurrence and no
S16.9.144 binding. Reader extraction for p7 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.144-005: MINVAL MASK and DIM forms reduce selected elements and sections

**Source:** S16.9.144, J3/24-007, 18 December 2023, physical PDF436. **Class:** Effect.

**Definition:** MINVAL with MASK equals MINVAL(PACK(ARRAY,MASK)); with DIM it reduces rank-one ARRAY
directly or applies MINVAL to each selected rank-one section.

**Diagnostic obligation:** not-required.

**Facets:** `MINVAL-mask-equals-packed-minimum`, `MINVAL-mask-all-false-uses-zero-size-result`, `MINVAL-DIM-rank-one-scalar-value`, `MINVAL-DIM-higher-rank-section-values`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINVAL occurrence and no
S16.9.144 binding. Reader extraction for p7 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.144-006: MINVAL character values use intrinsic relational operator ordering

**Source:** S16.9.144, J3/24-007, 18 December 2023, physical PDF436. **Class:** Effect.

**Definition:** For character ARRAY, MINVAL selects the value that would be selected by intrinsic
relational operators and the character collating sequence for the argument kind.

**Diagnostic obligation:** not-required.

**Facets:** `MINVAL-character-selected-by-relational-operators`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MINVAL occurrence and no
S16.9.144 binding. Reader extraction for p7 has the known census-hash-drift math-glyph
issue; the registry census hash is retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.144 -->
