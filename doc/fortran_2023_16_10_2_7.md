# Fortran 2023 16.10.2.7: COMPILER_VERSION ( )

The canonical catalogue is `doc/catalogues/compiler_version_16_10_2_7.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.10.2.7 -->

### S16.10.2.7-001: COMPILER_VERSION returns a default character scalar

**Source:** S16.10.2.7 p4 result characteristics; J3/24-007, 18 December 2023, physical PDF472-473. **Class:** Effect.

**Definition:** COMPILER_VERSION has a default character scalar result.

**Diagnostic obligation:** not-required.

**Facets:** `compiler_version-default-character-scalar-result`.

**Oracle:** Plans are pending and source-derived. Positive plans observe only portable single-image
properties fixed by 16.10.2: named-constant type/kind/rank, exact relational properties
among ISO_FORTRAN_ENV constants and intrinsic inquiries, successful use of preconnected
units, or exact IOSTAT named-constant assignment. Processor-dependent numeric values,
module extensions, compiler option/version text, team identity, atomic/event behavior,
multi-image preconnection, and message text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. Existing tests
and profiles mention ISO_FORTRAN_ENV, CHARACTER_KINDS, INTEGER_KINDS, COMPILER_VERSION
text in a test name, and unrelated INT8-style identifiers; this packet records that
coverage without rebinding it. The 16.10.2.7 census text was visually checked against
the rendered PDF page for the known PyMuPDF extraction drift; the catalogue keeps the
pinned census hash and treats the Example and NOTE as nonnormative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and 12 for source form,
declarations, expressions, execution, program units, intrinsic assignment, and I/O
harnesses. Unregistered dependencies explicitly retained: remaining 16.10.2
ISO_FORTRAN_ENV entities, 16.9 inquiry procedures such as BIT_SIZE, KIND, RANK, SIZE,
LBOUND, and STORAGE_SIZE, clause 17 atomic/IEEE terms, clause 18 interoperability
context, and clause 19 storage units and image/team terminology.

### S16.10.2.7-002: COMPILER_VERSION result length is processor dependent

**Source:** S16.10.2.7 p4 result length; J3/24-007, 18 December 2023, physical PDF472-473. **Class:** Effect.

**Definition:** The length of the COMPILER_VERSION default character scalar result is processor
dependent.

**Diagnostic obligation:** not-required.

**Facets:** `compiler_version-result-length-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans observe only portable single-image
properties fixed by 16.10.2: named-constant type/kind/rank, exact relational properties
among ISO_FORTRAN_ENV constants and intrinsic inquiries, successful use of preconnected
units, or exact IOSTAT named-constant assignment. Processor-dependent numeric values,
module extensions, compiler option/version text, team identity, atomic/event behavior,
multi-image preconnection, and message text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. Existing tests
and profiles mention ISO_FORTRAN_ENV, CHARACTER_KINDS, INTEGER_KINDS, COMPILER_VERSION
text in a test name, and unrelated INT8-style identifiers; this packet records that
coverage without rebinding it. The 16.10.2.7 census text was visually checked against
the rendered PDF page for the known PyMuPDF extraction drift; the catalogue keeps the
pinned census hash and treats the Example and NOTE as nonnormative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and 12 for source form,
declarations, expressions, execution, program units, intrinsic assignment, and I/O
harnesses. Unregistered dependencies explicitly retained: remaining 16.10.2
ISO_FORTRAN_ENV entities, 16.9 inquiry procedures such as BIT_SIZE, KIND, RANK, SIZE,
LBOUND, and STORAGE_SIZE, clause 17 atomic/IEEE terms, clause 18 interoperability
context, and clause 19 storage units and image/team terminology.

### S16.10.2.7-003: COMPILER_VERSION result text is processor dependent

**Source:** S16.10.2.7 p5 result value; J3/24-007, 18 December 2023, physical PDF472-473. **Class:** Effect.

**Definition:** The value of COMPILER_VERSION() is a processor-dependent string that identifies the name
and version of the program translation phase of the processor.

**Diagnostic obligation:** not-required.

**Facets:** `compiler_version-result-text-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans observe only portable single-image
properties fixed by 16.10.2: named-constant type/kind/rank, exact relational properties
among ISO_FORTRAN_ENV constants and intrinsic inquiries, successful use of preconnected
units, or exact IOSTAT named-constant assignment. Processor-dependent numeric values,
module extensions, compiler option/version text, team identity, atomic/event behavior,
multi-image preconnection, and message text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. Existing tests
and profiles mention ISO_FORTRAN_ENV, CHARACTER_KINDS, INTEGER_KINDS, COMPILER_VERSION
text in a test name, and unrelated INT8-style identifiers; this packet records that
coverage without rebinding it. The 16.10.2.7 census text was visually checked against
the rendered PDF page for the known PyMuPDF extraction drift; the catalogue keeps the
pinned census hash and treats the Example and NOTE as nonnormative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and 12 for source form,
declarations, expressions, execution, program units, intrinsic assignment, and I/O
harnesses. Unregistered dependencies explicitly retained: remaining 16.10.2
ISO_FORTRAN_ENV entities, 16.9 inquiry procedures such as BIT_SIZE, KIND, RANK, SIZE,
LBOUND, and STORAGE_SIZE, clause 17 atomic/IEEE terms, clause 18 interoperability
context, and clause 19 storage units and image/team terminology.

### S16.10.2.7-004: COMPILER_VERSION result should include relevant diagnostic information

**Source:** S16.10.2.7 p5 recommendation; J3/24-007, 18 December 2023, physical PDF472-473. **Class:** Effect.

**Definition:** The result value should include relevant information that could be useful for diagnosing
problems at a later date.

**Diagnostic obligation:** not-required.

**Facets:** `compiler_version-relevant-diagnostic-info-recommendation`.

**Oracle:** Plans are pending and source-derived. Positive plans observe only portable single-image
properties fixed by 16.10.2: named-constant type/kind/rank, exact relational properties
among ISO_FORTRAN_ENV constants and intrinsic inquiries, successful use of preconnected
units, or exact IOSTAT named-constant assignment. Processor-dependent numeric values,
module extensions, compiler option/version text, team identity, atomic/event behavior,
multi-image preconnection, and message text are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. Existing tests
and profiles mention ISO_FORTRAN_ENV, CHARACTER_KINDS, INTEGER_KINDS, COMPILER_VERSION
text in a test name, and unrelated INT8-style identifiers; this packet records that
coverage without rebinding it. The 16.10.2.7 census text was visually checked against
the rendered PDF page for the known PyMuPDF extraction drift; the catalogue keeps the
pinned census hash and treats the Example and NOTE as nonnormative.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, and 12 for source form,
declarations, expressions, execution, program units, intrinsic assignment, and I/O
harnesses. Unregistered dependencies explicitly retained: remaining 16.10.2
ISO_FORTRAN_ENV entities, 16.9 inquiry procedures such as BIT_SIZE, KIND, RANK, SIZE,
LBOUND, and STORAGE_SIZE, clause 17 atomic/IEEE terms, clause 18 interoperability
context, and clause 19 storage units and image/team terminology.

<!-- END GENERATED 16.10.2.7 -->
