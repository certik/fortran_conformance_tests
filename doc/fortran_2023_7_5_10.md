# Fortran 2023: 7.5.10 Construction of derived-type values

**Catalogue source review: reviewed.** Case and evidence adjudications are separate content-bound records.

The bounded declaration corpus has **28 new compile-only cases**: **13 diagnostics** and **15 positive controls**. Together with the **four unchanged C7107 cases**, **54 of98 facets are represented; 44 remain pending**.

## Bounded source and implementation gate

Authority: J3/24-007, **18 December 2023**, **688 physical PDF pages**, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The source checkpoint is `8ba944c4fffb6ea56a4594e75b3d8bff9d6abcda`.
Independent `constructors-source-review.json` has SHA-256
`132824f1265b76f2c836b99bb649379d88da7023eb709d2eca7ba1b12a09ee04`.
The exact compile-only scope map has SHA-256
`4cc8d934e25a610115683da1a5f815780ba6b1a888f09963e94424f4935c80d3`.
Original PDF106-109 and the directed abstract/PDT/component/inheritance,
pointer-assignment, NULL, accessibility and reporting dependencies were read.
Neither a compiler outcome nor another language's convention defines an oracle.

The source identity/accounting is unchanged:28 base units,114 fine units and142
accounting rows. On PDF107, the retained `note4` census slice completes NOTE2's
embedded reference to7.5.7.2 NOTE4; it is not a fresh note heading. `note2.2`
continues NOTE2 on PDF108. `note4.2` is the genuine NOTE4 there. All these
identities, source qualifiers and dispositions remain intact, ending before7.5.11.

## Concrete declaration and reporting premises

* Empty constructors have actual empty/defaulted types, required parentheses and
  no same-name generic. There is no empty-value runtime assertion.
* The abstract negative has no deferred binding and uses an explicit CLASS(base),
  INTENT(IN) observer. Its only repair removes ABSTRACT and its comma; no illegal
  TYPE(base) variable or dummy introduces a different abstract-type condition.
* Duplicate-component sources supply every other required component. Distinct
  components may have equal literals; both sources retain that valid context.
  Ancestor overlap instead uses parent x11/y13 set by independent named writes,
  supplies required local z19, and removes only the overlapping inherited x.
* Missing ordinary/pointer components are actually nondefaulted. Contextual
  NULL() supplies the missing pointer without a target/bounds read. Keyword-order
  repairs leave every component once and preserve the admitted positional prefix.
* The user PDT tag k=2 is specified only in the first parameter list. The extra
  second-list k is deleted; no intrinsic hardware kind or empty first parameter
  list is introduced.
* Data/procedure-pointer category repairs change only the receiving keyword.
  Both slots really default to bare NULL(). A complete explicit interface and
  NOPASS match the eligible module subroutine; the data TARGET is defined/live.
* Rank repairs use a true TARGET element or change to the correctly ranked slot.
  Both slots have actual defaults. Same-rank variables/sections, contextual NULL
  and NULL(MOLD=typed_pointer) appear in the shared sources. Molds are explicitly
  disassociated, but no pointer is dereferenced. General NULL construction is
  not the R806/C813 no-argument null-init context.
* Every new case is compile-phase. Thirteen source-minimal repairs also supply
  the corresponding admissions, including additional legal forms in the same
  source, rather than cloned third programs. S7.5.10-006 is a positive control for
  a restriction, using prior definition and constant declaration initialization.
* Numbered reporting capability is not fatal rejection or mandatory printed
  codes. Normal-status error reports may qualify; warnings/portability require
  exact family/severity permissions, not blanket warning acceptance. Cause,
  subject/property, input file and source point must all match.
* The TBFR-001/002 and DCFR-001 lessons are retained: no bare punctuation/attribute
  predicates, unsupported wrappers, wrong-role/property/source echoes, generic
  recovery, Internal/verifier/crash, timeout or resource failure can supply a
  different diagnostic. No unsupported facility is turned into a skip or success.

### Native cause qualification

The named repeated-keyword report is accepted only for the actual repeated-keyword
C7102 form, not its positional/keyword counterpart. For C7103, a constructor
conflict naming x is bound to the source where the only earlier specification is
its ancestor parent; a bare duplicate-component claim is not substituted.
C7105 can use the specific constructor-value-missing-name report at its unique
post-keyword positional entry, but not the companion missing-right recovery or
an ordinary-call argument-list error. C7106 does not accept the erroneous claim
that the user type parameter k is a component initialized twice.

Bare terminal rank messages are not adopted: the contains-only predicate could
otherwise match rank10 as rank1. The complete parenthesized GNU rank pair and
punctuated case-specific forms retain numeric boundaries. This limitation does
not justify a shared harness change or a weaker rank oracle. Original failures
and the exact current-fingerprint refresh selection are preserved separately.

## Historical C7107 representation and integration boundary

The four existing C7107 manifests, provider/client bytes, IDs, facets and
compile/effect versus compile/positive-control roles are unchanged. At the
author base98c6db06c759d46fff2c142a2614a402309ee6a9, registering the detailed
C7107 requirement deliberately changes their four requirement-bound fingerprints
and three dependent link fingerprints. The historical two represented facets
remain represented; authorship is not current approval. Independent decisions
and explicit source/case/link/inventory renewals are recorded in
`doc/source_audits/batch_023.json`, not performed by this generator.

Original and any refreshed observations retain their actual source_root,
compiler identities, standards, inputs and commands. Exact current-row selection
is recorded in `constructor-declarations-handoff.json`, never as an invented
combined run. Frozen LF411, GNU f2023 and actual Flang f2018 are not relabelled.
The other1869 base case fingerprints/inputs and all protected shared files stay
unchanged. The local index overlay is excluded from the owned commit.

Selected runtime constructor effects are now implemented by the generated fixtures below. No new
source-contrast, profile, C companion, coarray or diagnostic policy is implemented here. Integration retains the then-current nine
link declarations and explicitly renews only the three C7107-dependent receipts;
the separate five binding-reuse connections are not overwritten by the older
author context. Unselected original plans below stay pending in full.

## Preserved source accounting

* `p1`: **structural** - Original census p1 at PDF106; retain the exact base identity/hash and separately classify its fine source content.
* `p1.derived-definition`: **definition** - S7.5.10-001
* `p1.implicit-constructor`: **definition** - S7.5.10-001
* `p1.scalar-values`: **definition** - S7.5.10-001
* `p1.corresponding-type`: **definition** - S7.5.10-001
* `p1.derived-type-specifier-parameters`: **definition** - S7.5.10-001
* `R756`: **requirements** - R756
* `R756.derived-type-spec`: **requirements** - R756
* `R756.component-parentheses`: **requirements** - R756
* `R756.empty-list`: **requirements** - R756
* `R756.nonempty-component-list`: **requirements** - R756
* `R757`: **requirements** - R757
* `R757.optional-keyword`: **requirements** - R757
* `R757.equals-with-keyword`: **requirements** - R757
* `R757.component-data-source`: **requirements** - R757
* `R758`: **requirements** - R758
* `R758.expression`: **requirements** - R758
* `R758.data-target`: **requirements** - R758
* `R758.procedure-target`: **requirements** - R758
* `C7101`: **requirements** - C7101
* `C7101.constructor-derived-type-spec`: **requirements** - C7101
* `C7101.abstract-exclusion`: **requirements** - C7101
* `C7102`: **requirements** - C7102
* `C7102.component-subject`: **requirements** - C7102
* `C7102.at-most-one-spec`: **requirements** - C7102
* `C7103`: **requirements** - C7103
* `C7103.ancestor-spec-condition`: **requirements** - C7103
* `C7103.inheritance-associated-subcomponent`: **requirements** - C7103
* `C7103.other-spec-exclusion`: **requirements** - C7103
* `C7104`: **requirements** - C7104
* `C7104.nonallocatable-subject`: **requirements** - C7104
* `C7104.required-spec`: **requirements** - C7104
* `C7104.default-initialization-exception`: **permission** - C7104
* `C7104.ancestor-coverage-exception`: **permission** - C7104
* `C7105`: **requirements** - C7105
* `C7105.keyword-omission-condition`: **requirements** - C7105
* `C7105.all-preceding-keywords-omitted`: **requirements** - C7105
* `C7106`: **requirements** - C7106
* `C7106.each-keyword`: **requirements** - C7106
* `C7106.component-name-of-this-type`: **requirements** - C7106
* `C7107`: **requirements** - C7107
* `C7107.type-name-access`: **requirements** - C7107
* `C7107.component-spec-present`: **requirements** - C7107
* `C7107.each-specified-component`: **requirements** - C7107
* `C7107.containing-scope`: **requirements** - C7107
* `C7107.default-private-origin`: **requirements** - C7107
* `C7107.explicit-private-origin`: **requirements** - C7107
* `C7108`: **requirements** - C7108
* `C7108.same-type-and-generic-name`: **requirements** - C7108
* `C7108.resolvable-generic-reference`: **requirements** - C7108
* `C7108.constructor-interpretation-excluded`: **requirements** - C7108
* `C7109`: **requirements** - C7109
* `C7109.data-target-data-pointer`: **requirements** - C7109
* `C7109.procedure-target-procedure-pointer`: **requirements** - C7109
* `C7110`: **requirements** - C7110
* `C7110.data-target-rank`: **requirements** - C7110
* `C7110.corresponding-component-rank`: **requirements** - C7110
* `note1`: **structural** - Original census note1 at PDF107; retain the exact base identity/hash and separately classify its fine source content.
* `note1.generic-function-precedence`: **informative** - This note explains C7108's generic-function-first interpretation; it is not a separate mandatory diagnostic.
* `note1.constructor-fallback`: **informative** - This note explains C7108's generic-function-first interpretation; it is not a separate mandatory diagnostic.
* `p2`: **structural** - Original census p2 at PDF107; retain the exact base identity/hash and separately classify its fine source content.
* `p2.no-keyword`: **requirements** - S7.5.10-002
* `p2.positional-component-order`: **requirements** - S7.5.10-002
* `p2.keyword-present`: **requirements** - S7.5.10-002
* `p2.named-component`: **requirements** - S7.5.10-002
* `p2.nonpointer-condition`: **requirements** - S7.5.10-003
* `p2.declared-type-conformance`: **requirements** - S7.5.10-003
* `p2.parameter-conformance`: **requirements** - S7.5.10-003
* `p2.intrinsic-conversion-condition`: **requirements** - S7.5.10-003
* `p2.component-type-parameter-result`: **requirements** - S7.5.10-003
* `p2.ordinary-component-condition`: **requirements** - S7.5.10-004
* `p2.shape-conformance`: **requirements** - S7.5.10-004
* `p3`: **structural** - Original census p3 at PDF107; retain the exact base identity/hash and separately classify its fine source content.
* `p3.defaulted-component-source-absent`: **requirements** - S7.5.10-005
* `p3.apply-default`: **requirements** - S7.5.10-005
* `p3.allocatable-source-absent`: **requirements** - S7.5.10-005
* `p3.unallocated-status`: **requirements** - S7.5.10-005
* `note2`: **structural** - Original census note2 at PDF107; retain the exact base identity/hash and separately classify its fine source content.
* `note2.parent-excluded-from-order`: **informative** - This is the opening of one NOTE2 box: parent components are excluded from positional order, and the example refers to types in 7.5.7.2 NOTE4.
* `note2.parent-keyword-only`: **informative** - This is the opening of one NOTE2 box: parent components are excluded from positional order, and the example refers to types in 7.5.7.2 NOTE4.
* `note2.examples-introduction`: **informative** - This is the opening of one NOTE2 box: parent components are excluded from positional order, and the example refers to types in 7.5.7.2 NOTE4.
* `note4`: **structural** - Original census note4 at PDF107; retain the exact base identity/hash and separately classify its fine source content. This is an extraction slice inside NOTE2 beginning with an embedded reference to 7.5.7.2 NOTE4, not a new NOTE4 heading. The PDF107 page image was checked.
* `note4.embedded-note-reference`: **structural** - The census label note4 on PDF107 begins the wrapped words 'NOTE 4:' completing the preceding 7.5.7.2 cross-reference inside NOTE2. It is not a standalone NOTE4 or normative requirement. Keep its original hash/identity.
* `note4.same-note2-value-setup`: **informative** - The remainder of this census slice is NOTE2's POINT/PV setup, still inside the same note box. Its positional initializer is illustrative, not independent fixture setup.
* `note4.incomplete-context`: **informative** - The ellipsis ends the first page of NOTE2; no missing program context is silently invented.
* `note2.2`: **structural** - Original census note2.2 at PDF108; retain the exact base identity/hash and separately classify its fine source content.
* `note2.2.note2-continuation`: **informative** - PDF108 NOTE2 continuation contrasts parent-keyword values and flat leaf values, including accessible parent values whose type has private components. The original census identity remains separate.
* `note2.2.parent-constructor-keyword`: **informative** - PDF108 NOTE2 continuation contrasts parent-keyword values and flat leaf values, including accessible parent values whose type has private components. The original census identity remains separate.
* `note2.2.existing-parent-value-private-leaves`: **informative** - PDF108 NOTE2 continuation contrasts parent-keyword values and flat leaf values, including accessible parent values whose type has private components. The original census identity remains separate.
* `note2.2.flat-accessible-leaves`: **informative** - PDF108 NOTE2 continuation contrasts parent-keyword values and flat leaf values, including accessible parent values whose type has private components. The original census identity remains separate.
* `p4`: **structural** - Original census p4 at PDF108; retain the exact base identity/hash and separately classify its fine source content.
* `p4.referenced-type`: **requirements** - S7.5.10-006
* `p4.prior-definition`: **requirements** - S7.5.10-006
* `p5`: **structural** - Original census p5 at PDF108; retain the exact base identity/hash and separately classify its fine source content.
* `p5.pointer-component`: **requirements** - S7.5.10-007
* `p5.allowable-data-target`: **requirements** - S7.5.10-007
* `p5.allowable-procedure-target`: **requirements** - S7.5.10-007
* `p5.pointer-source-condition`: **requirements** - S7.5.10-007
* `p5.copy-source-association`: **requirements** - S7.5.10-007
* `p5.nonpointer-source-condition`: **requirements** - S7.5.10-007
* `p5.associate-with-source`: **requirements** - S7.5.10-007
* `note3`: **structural** - Original census note3 at PDF108; retain the exact base identity/hash and separately classify its fine source content.
* `note3.text-array-target`: **informative** - The example's TEXT is a rank-one CHARACTER TARGET array, not a character scalar or opaque representation.
* `note3.reference-type-roles`: **informative** - The referenced REFERENCE type has four nonpointer components, a defaulted procedure pointer PRINT and data pointer SYNOPSIS.
* `note3.synopsis-keyword`: **informative** - SYNOPSIS= skips the fifth procedure-pointer slot; an unkeyworded data target there would have the wrong pointer category.
* `note3.skipped-defaulted-procedure-pointer`: **informative** - PRINT's actual default permits its omission; an arbitrary nondefaulted pointer could not be omitted.
* `note3.illustrative-association`: **informative** - The BIBLIO assignment illustrates association but also contains outer assignment; no constructor-only observation or runtime approval is inferred.
* `p6`: **structural** - Original census p6 at PDF108; retain the exact base identity/hash and separately classify its fine source content.
* `p6.allocatable-component`: **requirements** - S7.5.10-008
* `p6.null-without-arguments-alternative`: **requirements** - S7.5.10-008
* `p6.allocatable-entity-same-rank`: **requirements** - S7.5.10-008
* `p6.other-entity-same-rank`: **requirements** - S7.5.10-008
* `p6.intrinsic-null-reference`: **requirements** - S7.5.10-008
* `p6.null-unallocated-result`: **requirements** - S7.5.10-008
* `p7`: **structural** - Original census p7 at PDF108; retain the exact base identity/hash and separately classify its fine source content.
* `p7.allocatable-component-and-source`: **requirements** - S7.5.10-009
* `p7.source-allocation-status`: **requirements** - S7.5.10-009
* `p7.allocated-source-condition`: **requirements** - S7.5.10-009
* `p7.same-bounds`: **requirements** - S7.5.10-009
* `p7.deferred-length-condition`: **requirements** - S7.5.10-009
* `p7.source-length-value`: **requirements** - S7.5.10-009
* `p7.polymorphic-component`: **requirements** - S7.5.10-009
* `p7.same-dynamic-type-and-value`: **requirements** - S7.5.10-009
* `p7.nonpolymorphic-component`: **requirements** - S7.5.10-009
* `p7.converted-declared-type-value`: **requirements** - S7.5.10-009
* `p8`: **structural** - Original census p8 at PDF108; retain the exact base identity/hash and separately classify its fine source content.
* `p8.allocatable-component-nonallocatable-source`: **requirements** - S7.5.10-009
* `p8.allocated-result`: **requirements** - S7.5.10-009
* `p8.expression-bounds`: **requirements** - S7.5.10-009
* `p8.deferred-length-condition`: **requirements** - S7.5.10-009
* `p8.expression-length-value`: **requirements** - S7.5.10-009
* `p8.polymorphic-component`: **requirements** - S7.5.10-009
* `p8.same-dynamic-type-and-value`: **requirements** - S7.5.10-009
* `p8.nonpolymorphic-component`: **requirements** - S7.5.10-009
* `p8.converted-declared-type-value`: **requirements** - S7.5.10-009
* `note4.2`: **structural** - Original census note4.2 at PDF108; retain the exact base identity/hash and separately classify its fine source content. This is the genuine NOTE4 on PDF108, not a second normative NOTE4 requirement.
* `note4.2.genuine-note4-person-value`: **informative** - This is the actual NOTE4 heading on PDF108, indexed note4.2 because note4 was already consumed by the embedded cross-reference. PERSON's scalar example is informative; do not renumber/delete either census unit.
* `note4.2.keyword-order-variant`: **informative** - The same PERSON values are shown with reordered component keywords, not reordered declaration/parameter lists.
* `note5`: **structural** - Original census note5 at PDF109; retain the exact base identity/hash and separately classify its fine source content.
* `note5.parameter-list`: **informative** - GENERAL_POINT(dim=3)([1.,2.,3.]) illustrates separate PDT parameter/component lists and an array-valued component in a scalar constructed value; it supplies no representation or floating approximation oracle.
* `note5.component-list`: **informative** - GENERAL_POINT(dim=3)([1.,2.,3.]) illustrates separate PDT parameter/component lists and an array-valued component in a scalar constructed value; it supplies no representation or floating approximation oracle.
* `note5.array-valued-component`: **informative** - GENERAL_POINT(dim=3)([1.,2.,3.]) illustrates separate PDT parameter/component lists and an array-valued component in a scalar constructed value; it supplies no representation or floating approximation oracle.

## Definitions

<!-- BEGIN STRUCTURE CONSTRUCTOR EFFECT FIXTURES -->
## Structure constructor runtime observations

Fifteen complete run/effect/f2023 programs discharge selected 7.5.10 effect facets. Every program uses a scalar typed observer, two positional identity components (101 and 103), and an omitted default-initialized component with sentinel -9051 before checking the facet-specific component by name. No fixture uses TRANSFER, LOC, C_LOC, EQUIVALENCE, COMMON, storage offsets or address comparison.

Mutation metadata covers every oracle literal/expression/completion, every facet input value, and three feature-level mutations in each program: default-initialization change, component-definition reorder, and positional component-value swap. A reverse default-initialization mutation pairs the changed default with the changed oracle to prove the -9051 sentinel is load-bearing. Character cases assert LEN explicitly and use nonblank three-character values.

The remaining source-use, diagnostic, polymorphic, procedure-pointer, expression-bound, zero-extent and assignment-boundary plans stay pending.
<!-- END STRUCTURE CONSTRUCTOR EFFECT FIXTURES -->

<!-- BEGIN STRUCTURE CONSTRUCTOR 7.5.10 B FIXTURES -->
## Structure constructor batch317 observations

Eight run/effect programs and one diagnostic/control pair discharge additional 7.5.10 facets for PDT KIND parameters, character conversion, omitted private/defaulted components, procedure-pointer targets, allocatable same-rank sources, allocatable source state/value rules, same-named generic precedence and a binding-name keyword negative. Runtime fixtures include feature-level mutations for every claimed facet, checked in compiler-specific scratch subdirectories.
<!-- END STRUCTURE CONSTRUCTOR 7.5.10 B FIXTURES -->

<!-- BEGIN GENERATED 7.5.10 -->

### R756: A structure constructor has a type specifier and a separate component list

**Source:** 7.5.10 R756; J3/24-007, 18 December 2023, physical PDF106, printed92, line32. **Class:** Syntax.

**Definition:** A structure-constructor is derived-type-spec followed by parentheses enclosing an
optional component-spec-list. Even an empty component list retains those parentheses.
For a PDT the type-parameter parentheses belong to derived-type-spec and precede the
separate component-list parentheses. Empty syntax does not waive C7104 or accessibility,
and a bare-type-name form that resolves as a same-named generic function has C7108's
interpretation instead.

**Diagnostic obligation:** required.

**Facets:** `nonempty-form-source-use`, `empty-component-list`, `separate-parameter-and-component-lists`, `required-parentheses`.

**Oracle:** Finite form plans separate empty/nonempty lists and PDT parameters from component
values; the parameter/payload observable is independently fixed at (2,17). Existing
basic forms remain pending reuse.

Compile-only declaration packet: 1 cases represent 1 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** A component array does not make the constructor an array of structures. An elemental
same-named user function is a different interpretation, not an exception that
elementally applies R756. 4.2 p2(3) requires capability to detect/report numbered
syntax/constraint violations, not fatal rejection, a particular severity, exact
diagnostic prose or printed rule codes. Later contracts must identify this source
role/property and the failing file/span/step. Unrelated punctuation/name recovery,
unsupported facilities, crashes, internal/verifier failures, timeouts and resource
errors are not corroboration.

**Dependencies:** R754/R755/C795-C7100 (PDF106); R401 (PDF45); p1 and C7104/C7108 (PDF106-108);
7.2/7.5.3.1-.2 and R916/C923 (PDF76/91-92/153).

### R757: Each component specification optionally names its component before an equals sign

**Source:** 7.5.10 R757; physical PDF107, printed93, line1. **Class:** Syntax.

**Definition:** A component-spec is a component-data-source, optionally preceded by keyword=. The
keyword does not name a type parameter or procedure dummy merely because the same outer
syntax resembles a function reference. C7105 controls ordering and C7106 controls the
keyword's role.

**Diagnostic obligation:** required.

**Facets:** `positional-and-keyword-source-use`, `keyword-equals-required`, `named-component-observable`.

**Oracle:** The keyword grammar has a one-token repair; component mapping is observed at its p2
owner.

**Oracle limitation:** Ordinary procedure keywords and PDT type-parameter keywords have separate defining
rules. Do not rename an existing primary case or credit a generic 'expected name' as
this keyword-role cause. 4.2 p2(3) requires capability to detect/report numbered
syntax/constraint violations, not fatal rejection, a particular severity, exact
diagnostic prose or printed rule codes. Later contracts must identify this source
role/property and the failing file/span/step. Unrelated punctuation/name recovery,
unsupported facilities, crashes, internal/verifier failures, timeouts and resource
errors are not corroboration.

**Dependencies:** C7105/C7106 and p2 (PDF107); R755 (PDF106); 15.5.2.1 and C1532 (PDF335/337-338).

### R758: Component data sources include expressions and the two pointer-target categories

**Source:** 7.5.10 R758; physical PDF107, printed93, lines2-4. **Class:** Syntax.

**Definition:** The alternatives are expr, data-target and proc-target. They are not freely
interchangeable for every component: C7109/p5 distinguish data and procedure pointers,
C7110 constrains data-target rank, p2 governs ordinary values and p6-p8 govern
allocatable sources.

**Diagnostic obligation:** required.

**Facets:** `expression-source-use`, `data-target-source-use`, `procedure-target-source-use`.

**Oracle:** An explicit three-way source-use union, with each alternative's component and interface
conditions, remains pending rather than being declared covered by one successful
constructor.

**Oracle limitation:** This union does not authorize arbitrary expressions as pointer targets, wrong target
categories, or an unsupported numbered-target/family/use-graph evidence mechanism. 4.2
p2(3) requires capability to detect/report numbered syntax/constraint violations, not
fatal rejection, a particular severity, exact diagnostic prose or printed rule codes.
Later contracts must identify this source role/property and the failing file/span/step.
Unrelated punctuation/name recovery, unsupported facilities, crashes, internal/verifier
failures, timeouts and resource errors are not corroboration.

**Dependencies:** C7109/C7110/p2/p5-p8 (PDF107-108); R1038/R1041/C1028-C1034 and 10.2.2.3-.4 (PDF194-196).

### C7101: The actual structure-constructor type is not abstract

**Source:** 7.5.10 C7101 (R756); physical PDF107, printed93, line5. **Class:** Restriction.

**Definition:** The derived-type-spec of R756 cannot specify an abstract type. This concerns the actual
structure-constructor interpretation; a resolvable same-named generic function is
governed by C7108 and does not become an abstract structure constructor merely by
spelling.

**Diagnostic obligation:** required.

**Facets:** `abstract-constructor`, `concrete-constructor-admission`, `generic-name-boundary-source-use`.

**Oracle:** One source-minimal ABSTRACT deletion admits the same constructor context without
introducing an independently invalid abstract object declaration.

Compile-only declaration packet: 2 cases represent 2 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** C703/C706 and abstract allocation constraints are not interchangeable C7101 reports. A
missing implementation or an unrecognized generic name is not an abstract-constructor
cause. 4.2 p2(3) requires capability to detect/report numbered syntax/constraint
violations, not fatal rejection, a particular severity, exact diagnostic prose or
printed rule codes. Later contracts must identify this source role/property and the
failing file/span/step. Unrelated punctuation/name recovery, unsupported facilities,
crashes, internal/verifier failures, timeouts and resource errors are not corroboration.

**Dependencies:** 7.5.7.1/C738/C739 (PDF88/104); C703/C706/C708 (PDF77); C7108 (PDF107); 15.5.5.2
(PDF350-351).

### C7102: A component receives at most one constructor component specification

**Source:** 7.5.10 C7102 (R756); physical PDF107, printed93, line6. **Class:** Restriction.

**Definition:** At most one component-spec is supplied for a particular component, whether it is reached
positionally or by keyword. This is distinct from specifying an ancestor component and
one of its inheritance-associated subcomponents, which C7103 addresses explicitly.

**Diagnostic obligation:** required.

**Facets:** `repeated-keyword`, `positional-keyword-repeat`, `different-components-admission`.

**Oracle:** Both prospective negatives provide every other required component, and each deletion
repairs only component multiplicity. The positive role is admission, not a runtime or
diagnostic claim.

Compile-only declaration packet: 4 cases represent 3 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** Do not accidentally leave an unsupplied nondefaulted component and then count its C7104
report as duplicate-component corroboration. 4.2 p2(3) requires capability to
detect/report numbered syntax/constraint violations, not fatal rejection, a particular
severity, exact diagnostic prose or printed rule codes. Later contracts must identify
this source role/property and the failing file/span/step. Unrelated punctuation/name
recovery, unsupported facilities, crashes, internal/verifier failures, timeouts and
resource errors are not corroboration.

**Dependencies:** R757/C7104-C7106 and p2 (PDF107); component order (PDF98).

### C7103: An ancestor component specification excludes specifications for its associated subcomponents

**Source:** 7.5.10 C7103 (R756); physical PDF107, printed93, lines7-8. **Class:** Restriction.

**Definition:** When a component-spec is supplied for an ancestor component, no component-spec is
supplied for a component inheritance associated with a subcomponent of that ancestor.
The rule covers direct and recursively inherited overlap, not unrelated child
components.

**Diagnostic obligation:** required.

**Facets:** `parent-and-inherited-member`, `recursive-ancestor-overlap`, `disjoint-ancestor-and-child`.

**Oracle:** The finite direct and recursive plans isolate inheritance-associated overlap with
coherent repairs and independently initialized ancestor values.

Compile-only declaration packet: 2 cases represent 2 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** C7102 component identity and C7103 ancestor/member overlap are not conflated. Parent
components are not positional entries; abstract or private whole-parent designators need
their own conditions. 4.2 p2(3) requires capability to detect/report numbered
syntax/constraint violations, not fatal rejection, a particular severity, exact
diagnostic prose or printed rule codes. Later contracts must identify this source
role/property and the failing file/span/step. Unrelated punctuation/name recovery,
unsupported facilities, crashes, internal/verifier failures, timeouts and resource
errors are not corroboration.

**Dependencies:** 7.5.7.2 p2 (PDF104), 19.5.4 (PDF561); C7104/C7107 and NOTE2 continuation (PDF107-108);
C911 (PDF152).

### C7104: Nonallocatable components require sources unless defaulted or covered by an ancestor source

**Source:** 7.5.10 C7104 (R756); physical PDF107, printed93, lines9-11. **Class:** Restriction.

**Definition:** A nonallocatable component needs a component-spec unless it has default initialization
or is inheritance associated with a subcomponent of another specified component.
Pointers are nonallocatable and therefore also need a source unless the exception holds.
Allocatable omission is handled by p3 rather than by pretending a missing value is
defined.

**Diagnostic obligation:** required.

**Facets:** `missing-ordinary-component`, `missing-pointer-component`, `defaulted-omission-source-use`, `ancestor-covered-omission-source-use`, `allocatable-omission-source-use`.

**Oracle:** The two future missing-source repairs are bounded and conforming. Default, ancestor and
allocatable omission branches remain separate pending source-use/effect plans.

Compile-only declaration packet: 4 cases represent 2 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** A POINTER component without initialization is not implicitly disassociated merely
because the constructor omits it. Defaults must actually exist; do not infer
opaque/private defaults. 4.2 p2(3) requires capability to detect/report numbered
syntax/constraint violations, not fatal rejection, a particular severity, exact
diagnostic prose or printed rule codes. Later contracts must identify this source
role/property and the failing file/span/step. Unrelated punctuation/name recovery,
unsupported facilities, crashes, internal/verifier failures, timeouts and resource
errors are not corroboration.

**Dependencies:** p3/p5/p6 (PDF107-108); 7.5.4.6 (PDF96-98); NULL/Table16.5 (PDF441-442); 19.5.2.2
(PDF556).

### C7105: Positional specifications precede keyword specifications

**Source:** 7.5.10 C7105 (R757); physical PDF107, printed93, lines12-13. **Class:** Restriction.

**Definition:** A component-spec can omit keyword= only if every preceding component-spec also omitted
it. Thus positional entries form a prefix; once a keyword is used, subsequent entries
have keywords. The component-list ordering rule is distinct from R755's type-parameter
list.

**Diagnostic obligation:** required.

**Facets:** `positional-after-keyword`, `positional-prefix-admission`, `all-keyword-reordering-source-use`.

**Oracle:** One keyword insertion repairs the ordering violation; admitted mixed/all-keyword forms
keep each component unique and defined.

Compile-only declaration packet: 2 cases represent 2 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** Do not repair by changing a type-parameter list or by altering the component declaration
order. 4.2 p2(3) requires capability to detect/report numbered syntax/constraint
violations, not fatal rejection, a particular severity, exact diagnostic prose or
printed rule codes. Later contracts must identify this source role/property and the
failing file/span/step. Unrelated punctuation/name recovery, unsupported facilities,
crashes, internal/verifier failures, timeouts and resource errors are not corroboration.

**Dependencies:** R757/C7102/C7106/p2 (PDF107); R755/C798 (PDF106); R401 (PDF45).

### C7106: A constructor keyword names a component of the selected type

**Source:** 7.5.10 C7106 (R757); physical PDF107, printed93, line14. **Class:** Restriction.

**Definition:** Each component keyword denotes a component of the type, including an applicable parent
or inherited component, subject to access and overlap constraints. A type-parameter
name, binding name or unrelated identifier is not a component merely because it is
accessible.

**Diagnostic obligation:** required.

**Facets:** `unknown-component-keyword`, `parameter-is-not-component`, `binding-is-not-component`.

**Oracle:** Keyword repairs operate only in the component list and keep required payload data
present. They do not confuse type parameters, procedure dummies or type-bound bindings
with components.

Compile-only declaration packet: 4 cases represent 2 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

C7106 structure-constructor binding keyword diagnostic (batch317): one invalid compile
fixture supplies get=19 where get is a type-bound binding, not a component; the paired
positive control deletes only that component-spec and keeps payload present.

**Oracle limitation:** If the same syntax resolves as a generic call, C7108 applies first. A vague 'cannot
resolve' diagnosis does not identify this constructor keyword constraint. 4.2 p2(3)
requires capability to detect/report numbered syntax/constraint violations, not fatal
rejection, a particular severity, exact diagnostic prose or printed rule codes. Later
contracts must identify this source role/property and the failing file/span/step.
Unrelated punctuation/name recovery, unsupported facilities, crashes, internal/verifier
failures, timeouts and resource errors are not corroboration.

C7106 structure-constructor binding keyword diagnostic boundary (batch317): the
diagnostic is line-anchored and excludes unsupported/internal failures, but no exact
wording or fatal status is required.

**Dependencies:** R755 and C7108 (PDF106-107); 19.3.1/.4 (PDF549-550); 7.5.7.2 (PDF104).

### C7107: The type name and each explicitly specified component are accessible in the constructor scope

**Source:** 7.5.10 C7107 (R756); physical PDF107, printed93, lines15-16. **Class:** Restriction.

**Definition:** The type name and all components for which component-spec appears must be accessible in
the constructor's containing scoping unit. The condition does not demand accessibility
of every private component if it is legitimately omitted/defaulted or supplied within an
accessible ancestor component value. Type-name accessibility, component accessibility,
binding accessibility and the provider procedure's name remain different predicates.

**Diagnostic obligation:** required.

**Facets:** `private-component-default`, `private-component-explicit`, `type-name-access-source-use`, `omitted-private-default`, `omitted-private-allocatable`, `accessible-parent-value-private-leaves`, `scope-and-use-graph`.

**Oracle:** Two facets are already represented, not pending: private-component-default by the
existing C7107 default-private diagnostic/control pair, and private-component-explicit
by the existing explicit-private pair. All four are compile cases: invalid/effect versus
valid/positive-control. The default repair removes only the provider's bare PRIVATE; the
explicit repair changes only the hidden component's PRIVATE to PUBLIC. The client
constructor bytes are identical in all four. Their source/input roles remain valid
historical representation, while the newly detailed requirement intentionally makes
their recorded requirement-bound approvals stale pending coordinator adjudication.

C7107 structure-constructor private omission fixtures (batch317): one client-scope
run/effect program constructs public types while omitting private defaulted and private
allocatable components; owner-scope observers check the hidden default value 17 and
unallocated hidden store.

**Oracle limitation:** The four existing IDs, paths, Fortran/manifest bytes, roles, phases and exact facet IDs
are immutable. Do not mark them newly pending, reassign their primary or treat legal
owner calls as proof of inaccessible client names. A private leaf contained in a
supplied public parent value is not itself an explicitly supplied component keyword. 4.2
p2(3) requires capability to detect/report numbered syntax/constraint violations, not
fatal rejection, a particular severity, exact diagnostic prose or printed rule codes.
Later contracts must identify this source role/property and the failing file/span/step.
Unrelated punctuation/name recovery, unsupported facilities, crashes, internal/verifier
failures, timeouts and resource errors are not corroboration.

C7107 structure-constructor private omission boundaries (batch317): only legal omission
of private defaulted/allocatable components is represented; type-name access, parent
private leaves and broader use-graph evidence remain pending.

**Dependencies:** 7.5.2.2/7.5.4.8 (PDF89/99); 8.5.2 (PDF120); 19.3.4 p1/p5/p6 (PDF550); 4.2 p2(3)/(6)
(PDF46); C7104 and NOTE2 continuation (PDF107-108); doc/source_audits/batch_012.json and
component_scope_duty_001.json (base prior-art records, not new approvals).

### C7108: A resolvable same-named generic function takes precedence over constructor interpretation

**Source:** 7.5.10 C7108 (R756); physical PDF107, printed93, lines17-19. **Class:** Restriction.

**Definition:** When derived-type-spec consists of a type name, without an explicit type-parameter
specification list, and that name is also a generic name, an actual-argument list
resolvable as a generic function reference is not interpreted as a structure-constructor
component list. Resolution is under 15.5.5.2 with valid generic declarations. This is
interpretation precedence, not a ban on a valid overlap expression or a license for
ambiguous specifics.

**Diagnostic obligation:** required.

**Facets:** `resolvable-function-precedence`, `keyword-constructor-fallback`, `type-constructor-fallback`, `generic-ambiguity-owner`, `elemental-and-name-association-source-use`.

**Oracle:** The two interpretations produce independently predicted payloads29 versus11. A valid
generic overlap has no manufactured negative twin; the function result is set without
another same-name constructor.

C7108 structure-constructor generic precedence fixtures (batch317): one run/effect
program contrasts record(11) resolving to a same-named generic function with payload 29,
record(payload=11) falling back to the constructor, and token(21) falling back when the
same-named generic accepts only CHARACTER.

**Oracle limitation:** Do not use compiler acceptance or a generic 'cannot resolve' error as the oracle. No
callback requires private representation, undefined input, physical kind numbers or
finalization effects. 4.2 p2(3) requires capability to detect/report numbered
syntax/constraint violations, not fatal rejection, a particular severity, exact
diagnostic prose or printed rule codes. Later contracts must identify this source
role/property and the failing file/span/step. Unrelated punctuation/name recovery,
unsupported facilities, crashes, internal/verifier failures, timeouts and resource
errors are not corroboration.

C7108 structure-constructor generic precedence boundaries (batch317): only unambiguous
same-named generic function precedence and two constructor fallbacks are represented;
generic ambiguity and elemental/name-association source-use remain pending.

**Dependencies:** 15.4.3.4.1 p4, C1514 and 15.5.5.1-.4 (PDF329/331-332/350-352); 19.3.1 (PDF549); p1 and
NOTE1 (PDF106-107); 15.5.3 (PDF349).

### C7109: Data and procedure targets correspond to the matching pointer-component category

**Source:** 7.5.10 C7109 (R758); physical PDF107, printed93, lines20-21. **Class:** Restriction.

**Definition:** A data-target corresponds to a data pointer component, and a proc-target corresponds to
a procedure pointer component. Such targets additionally satisfy p5's pointer-assignment
eligibility and interface conditions; type-bound procedures are not procedure-pointer
components.

**Diagnostic obligation:** required.

**Facets:** `procedure-target-in-data-slot`, `data-target-in-procedure-slot`, `target-union-source-use`.

**Oracle:** Two single-keyword repairs move an eligible source to the correct pointer category while
defaults keep the other required pointer source satisfied. No target is dereferenced or
called in the negative.

Compile-only declaration packet: 4 cases represent 2 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** Do not remove a required component accidentally while changing categories. A missing
explicit interface or an invalid NOPASS/PASS declaration is a different source cause.
4.2 p2(3) requires capability to detect/report numbered syntax/constraint violations,
not fatal rejection, a particular severity, exact diagnostic prose or printed rule
codes. Later contracts must identify this source role/property and the failing
file/span/step. Unrelated punctuation/name recovery, unsupported facilities, crashes,
internal/verifier failures, timeouts and resource errors are not corroboration.

**Dependencies:** R741/R742/C760-C765 (PDF94-96); p5 (PDF108); R1038/R1041/C1028-C1034 and 10.2.2.3-.4
(PDF194-196); NULL (PDF441-442).

### C7110: A data target has the rank of its pointer component

**Source:** 7.5.10 C7110 (R758); physical PDF107, printed93, line22. **Class:** Restriction.

**Definition:** A data-target supplied for a component has the same rank as that component. This
pointer-target condition does not use ordinary scalar-to-array value expansion, and a
structure constructor contains no pointer bounds-remapping list that could waive it.
Type, kind, target and contiguity conditions still apply separately.

**Diagnostic obligation:** required.

**Facets:** `array-target-to-scalar-pointer`, `scalar-target-to-array-pointer`, `same-rank-and-null-context`.

**Oracle:** Rank repairs leave target eligibility intact and do not use bounds remapping or an
unallocated pointer inquiry. Compile-only positives do not establish association/value
effects.

Compile-only declaration packet: 4 cases represent 3 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** C1022 and the pointer-assignment target rules remain canonical dependencies; do not
credit an ordinary scalar assignment or a scalar-expanded ordinary component as
pointer-rank evidence. 4.2 p2(3) requires capability to detect/report numbered
syntax/constraint violations, not fatal rejection, a particular severity, exact
diagnostic prose or printed rule codes. Later contracts must identify this source
role/property and the failing file/span/step. Unrelated punctuation/name recovery,
unsupported facilities, crashes, internal/verifier failures, timeouts and resource
errors are not corroboration.

**Dependencies:** R1034/C1022/C1028 and 10.2.2.3 (PDF193-196); C7109/p5 (PDF107-108); 3.32 (PDF23);
NULL/Table16.5 (PDF441-442).

### S7.5.10-001: The implicit structure constructor forms a scalar value with the specified type parameters

**Source:** 7.5.10 p1; physical PDF106, printed92, lines29-31. **Class:** Effect.

**Definition:** A derived-type definition implicitly supplies a corresponding structure constructor for
scalar values of that type. The derived-type-spec supplies the constructed type and type
parameters. Components can themselves be arrays without making the constructor an
elemental array-of-structures operation. Generic functions with the same name have their
own reference/rank semantics under C7108.

**Diagnostic obligation:** not-required.

**Facets:** `scalar-value-source-use`, `parameter-specifier-source-use`, `array-component-not-elemental-constructor`.

**Oracle:** The finite scalar/parameter/type distinction is tested through explicit member values
and source-valid observer interfaces, not by treating compiler acceptance as a value
oracle. The companion document's P1-P8 premises apply: independently named setup,
defined read values, live targets/host instances, source-valid
type/kind/rank/interfaces, sequential state guards, and constructor observation without
a confounding outer derived assignment.

S7.5.10-001 structure-constructor scalar value fixtures: one complete run/effect/f2023
program passes a scalar structure constructor with an ordinary rank-one INTEGER
component to a typed scalar observer. The component source is a separately assigned
array with hand literals 11, 13 and 17, and the observer checks exactly one scalar
record and each named component element. This observes that the constructor produces a
scalar value containing an array component, not an array of three records or an
elemental same-named factory. A load-bearing default component -9051, two positional
identity components 101 and 103, and mutation probes for default changes, component
reordering and swapped positional values accompany the facet-specific source-value
probe.

S7.5.10-001 structure-constructor PDT parameter fixture (batch317): one run/effect
program constructs packet(k=kind(0.0))(payload=4.0) with separate PDT parameter and
component lists, observes KIND(payload)=KIND(0.0) and exact value 4.0 through a typed
observer.

**Oracle limitation:** Existing S-owned reuse is expressible only with an explicit finite reviewed pattern;
this packet installs no links. Rank/parameter declarations and generic-function
precedence remain separate.

S7.5.10-001 structure-constructor scalar value boundaries: only
array-component-not-elemental-constructor is represented. Scalar-value and
parameter-specifier source-use remain pending because this packet does not install
finite reuse links or a PDT parameter test.

S7.5.10-001 structure-constructor PDT parameter boundary (batch317): only a KIND PDT
parameter is represented; LEN parameters and source-use graph reuse remain pending.

**Dependencies:** R756/C7108 (PDF106-107); 7.2/7.5.3.2/7.5.9 (PDF76/92/106); 15.5.5.2 (PDF350-351); 19.6.1
(PDF562).

### S7.5.10-002: Positional sources follow component order and keywords select named components

**Source:** 7.5.10 p2, first two sentences; physical PDF107, printed93, lines23-25. **Class:** Effect.

**Definition:** Without a component keyword the source corresponds to the component in component order.
With a keyword it corresponds to that named component. Component order excludes parent
components, although inherited nonparent components participate; parent values therefore
need component keywords. Keyword order need not be declaration order once C7105 holds.

**Diagnostic obligation:** not-required.

**Facets:** `positional-order-source-use`, `inherited-order-source-use`, `parent-keyword-source-use`, `keyword-order-independent`.

**Oracle:** Existing positional/ancestor cases retain ownership. The new reversed-keyword plan
checks (left,right)=(11,13), with no I/O layout or constructor-to-constructor
cancellation. The companion document's P1-P8 premises apply: independently named setup,
defined read values, live targets/host instances, source-valid
type/kind/rank/interfaces, sequential state guards, and constructor observation without
a confounding outer derived assignment.

S7.5.10-002 structure-constructor component mapping fixtures: one complete
run/effect/f2023 program constructs a record with right=13 before left=11 after two
positional controls. The observer checks left and right by name against distinct
literals, plus the default and positional controls, so declaration-order, source-order
and set-only implementations are distinguished.

**Oracle limitation:** Do not count a formatted-output case initialized by the same positional constructor as
independent constructor-order evidence. Ancestor/member overlap and missing fields still
meet C7103/C7104.

S7.5.10-002 structure-constructor component mapping boundaries: only
keyword-order-independent is represented. Existing positional, inherited and
parent-keyword cases remain at their original owners until a source-use graph is
reviewed.

**Dependencies:** 7.5.4.7 (PDF98); 7.5.7.2 (PDF104); C7102-C7107 and NOTE2 (PDF107-108);
doc/source_audits/batch_012.json CIFR-001 prior-art correction.

### S7.5.10-003: Ordinary component sources obey intrinsic type/parameter conformance and conversion

**Source:** 7.5.10 p2, nonpointer type/parameter and intrinsic-value clauses; physical PDF107, printed93, lines25-28. **Class:** Effect.

**Definition:** For a nonpointer component, source declared type and type parameters conform as for
intrinsic assignment, including directional polymorphic compatibility and applicable
corresponding KIND/LEN conditions. Intrinsic values are converted if necessary to the
component's type and parameters. Availability of defined assignment is not permission to
coerce an otherwise nonconforming component source, and constructor evaluation is not an
outer derived-type assignment statement.

**Diagnostic obligation:** not-required.

**Facets:** `numeric-conversion`, `character-length-conversion`, `declared-type-parameter-conformance-source-use`, `defined-assignment-not-coercion`, `no-defined-assignment-during-construction`, `outer-assignment-owner-source-use`.

**Oracle:** The planned value observations are integer4, padded/truncated character values, and
nested17 with zero defined-assignment callbacks during direct construction observation.
Input setup uses named component assignments; the surrounding observation does not
perform a confounding derived assignment. The companion document's P1-P8 premises apply:
independently named setup, defined read values, live targets/host instances,
source-valid type/kind/rank/interfaces, sequential state guards, and constructor
observation without a confounding outer derived assignment.

S7.5.10-003 structure-constructor ordinary conversion fixture: one complete
run/effect/f2023 program supplies a separately defined default REAL source 4.0 to an
INTEGER scalar component and observes integer value 4 through a typed observer. The
oracle is the hand application of p2's intrinsic-assignment conversion rule, not a
representation, kind code or compiler consensus.

S7.5.10-003 structure-constructor character/assignment fixtures (batch317): one
run/effect program checks CHARACTER padding/truncation with LEN guards and exact
nonblank values, then directly observes outer(src) while a matching defined assignment
counter remains zero and the nested component value stays 17.

**Oracle limitation:** Only type/parameter conformance and intrinsic conversion are imported here; this is not
blanket permission for arbitrary defined-assignment coercion or an assumption about
primitive representations. Unnumbered mismatch contrasts do not acquire mandatory
reporting merely from these examples.

S7.5.10-003 structure-constructor ordinary conversion fixture: only numeric-conversion
is represented. Character padding/truncation, type-parameter conformance, defined
assignment contrasts and outer-assignment ownership remain pending. The real source is
the exact small model value 4.0 solely to exercise intrinsic conversion to INTEGER 4.

S7.5.10-003 structure-constructor ordinary conversion boundaries: only
numeric-conversion is represented. Character padding/truncation, type-parameter
conformance, defined assignment contrasts and outer-assignment ownership remain pending.
The real source is the exact small model value 4.0 solely to exercise intrinsic
conversion to INTEGER 4.

S7.5.10-003 structure-constructor character/assignment boundaries (batch317): only
default CHARACTER padding/truncation and absence of a defined-assignment callback during
direct construction are represented; mismatch diagnostics and assignment-owner reuse
remain pending.

**Dependencies:** 10.2.1.2/Table10.8 and 10.2.1.3/Table10.9/p15 (PDF189-192); 10.2.1.4-.5 (PDF192-193);
7.3.3 (PDF79); 15.5.2.4/.5/.3 and 7.5.6 (PDF339-341/349/102-103); 19.6.5 (PDF563).

### S7.5.10-004: Ordinary array component values conform without using pointer rank rules

**Source:** 7.5.10 p2, final sentence; physical PDF107, printed93, lines28-29. **Class:** Effect.

**Definition:** For a nonpointer nonallocatable component, the expression shape conforms with the
component shape under ordinary value assignment/conformability. A scalar value can
supply an ordinary array component by scalar expansion; array sources must conform
rather than merely have the same total element count. This is distinct from C7110's
same-rank data targets and p6's same-rank allocatable component sources.

**Diagnostic obligation:** not-required.

**Facets:** `scalar-to-ordinary-array`, `conforming-array-value`, `nonconforming-array-source-contrast`, `scalar-component-and-elemental-boundary-source-use`.

**Oracle:** Ordinary-array observations distinguish scalar expansion from array-shape agreement and
from pointer/allocatable rank requirements. Array input values are independently
initialized and checked, not produced by the same constructor under test. The companion
document's P1-P8 premises apply: independently named setup, defined read values, live
targets/host instances, source-valid type/kind/rank/interfaces, sequential state guards,
and constructor observation without a confounding outer derived assignment.

S7.5.10-004 structure-constructor ordinary array fixtures: two complete run/effect/f2023
programs cover ordinary nonpointer nonallocatable array components. The scalar-expansion
case supplies a separately assigned scalar 17 to INTEGER values(-1:1) and checks bounds
-1:1 plus all three elements. The conforming-array case supplies a separately populated
rank-one source 11,13,17 and checks the component's declared bounds and each element by
name, proving shape-conforming value assignment rather than storage remapping or
pointer-rank behavior.

**Oracle limitation:** Conformability is not storage-sequence equivalence or same-size rank remapping. Any fine
boundary contrast must keep the intended component category fixed and all source
elements defined.

S7.5.10-004 structure-constructor ordinary array boundaries: only
scalar-to-ordinary-array and conforming-array-value are represented. Nonconforming
contrasts and elemental/generic source-use boundaries remain pending. The fixtures do
not claim pointer rank, allocatable rank, storage sequence or same-size rank remapping.

**Dependencies:** 3.32 (PDF23); 10.2.1.2 p1(2)-(3)/10.2.1.3 p5-p6 (PDF189-190); C7110/p6 (PDF107-108);
R740/C754 and 7.5.4.2 (PDF93-95); LBOUND/UBOUND (PDF424-425/469).

### S7.5.10-005: Omitted defaulted components use their defaults and omitted allocatables are unallocated

**Source:** 7.5.10 p3; physical PDF107, printed93, lines30-32. **Class:** Effect.

**Definition:** When a component with default initialization has no corresponding component-data-source,
its default initialization applies. When an allocatable component has no corresponding
source, it is unallocated. This is not implicit zeroing of all ordinary components or
disassociation of an uninitialized omitted pointer; C7104's required-source rules
remain.

**Diagnostic obligation:** not-required.

**Facets:** `omitted-value-default`, `pointer-default-source-use`, `omitted-allocatable-status`, `private-omission-source-use`, `default-versus-explicit-setup-owner`.

**Oracle:** Finite direct observations are default/override values (11,13)/(11,29) and unallocated
scalar/array states. Existing initialization events and private supplied-component
contrasts retain their owners. The companion document's P1-P8 premises apply:
independently named setup, defined read values, live targets/host instances,
source-valid type/kind/rank/interfaces, sequential state guards, and constructor
observation without a confounding outer derived assignment.

S7.5.10-005 structure-constructor omitted component fixtures: two complete
run/effect/f2023 programs observe omitted components. The default case constructs one
value with both defaulted integer components omitted and a second with right=29,
requiring left/right pairs 11/13 and 11/29. The allocatable case omits scalar and
rank-one allocatable components and observes only their allocation status as
unallocated, never bounds or values.

S7.5.10-005 structure-constructor omitted default/private fixtures (batch317): one
run/effect program omits a default-initialized data pointer and private
default/allocatable components; the owner observer checks disassociation, hidden value
17, unallocated private storage and public tag 29.

**Oracle limitation:** Omission does not make every subcomponent value defined. Only permitted state queries
are used for unallocated components; any missing nondefaulted nonallocatable component
is separately invalid.

S7.5.10-005 structure-constructor omitted component boundaries: only
omitted-value-default and omitted-allocatable-status are represented. Pointer defaults,
private omission and default-vs-explicit source-use remain pending. Unallocated
components are queried only for allocation status.

S7.5.10-005 structure-constructor omitted default/private boundaries (batch317): only
omitted pointer default status and private omission effects are represented;
default-vs-explicit owner reuse remains pending.

**Dependencies:** C7104/C7107 (PDF107); 7.5.4.6 (PDF96-98); 19.6.1/.3/.5 (PDF562-563); 16.9.13 ALLOCATED
(PDF379-380); 19.5.2.2/.4 (PDF556-557).

### S7.5.10-006: A structure constructor is not referenced before its type definition

**Source:** 7.5.10 p4; physical PDF108, printed94, line1. **Class:** Restriction.

**Definition:** A structure constructor does not appear before the referenced type is defined. Before
diagnosing an apparent early name(...) form, distinguish the constructor interpretation
from a generic or ordinary function reference and keep the separate type-name
accessibility and declaration-order constraints.

**Diagnostic obligation:** not-required.

**Facets:** `prior-definition-admission`, `early-use-canonical-source-contrast`.

**Oracle:** The bounded prior-definition control and explicitly coupled early-use repair avoid
pretending that function lookup, type-name access and constructor-before-definition are
one diagnostic.

Compile-only declaration packet: 1 cases represent 1 selected facets. Invalid cases
require located, subject/role/property-specific reporting; normal zero/nonzero compiler
statuses do not replace that obligation. Repairs and valid admissions are positive
controls. No runtime effect, new policy or unregistered source-use credit is inferred.
All other original pending plans and qualifications are retained.

**Oracle limitation:** This unnumbered restriction does not itself mandate reporting under 4.2 p2(3). An
optional processor rejection policy would need separate authorization; none is
introduced here.

**Dependencies:** 7.3.2.2 and C795 (PDF78/106); C7108 and 15.5.5.1-.4 (PDF107/350-352); R726 and source
ordering (PDF88); 4.1.2/4.2 (PDF45-46).

### S7.5.10-007: Pointer components acquire allowable data or procedure target associations

**Source:** 7.5.10 p5; physical PDF108, printed94, lines2-5. **Class:** Effect.

**Definition:** A pointer component source is an allowable data-target or proc-target for that pointer
under pointer assignment. A pointer source supplies its association; a nonpointer source
becomes the associated target. This preserves data/procedure category,
type/kind/rank/parameter/interface and CONTIGUOUS conditions, source pointer status and
actual target or internal-procedure host lifetime. Constructor targets are not default
initial-data-target declarations with an automatic SAVE requirement.

**Diagnostic obligation:** not-required.

**Facets:** `live-data-target-alias`, `pointer-source-association-and-bounds`, `known-disassociated-pointer`, `contextual-and-mold-null`, `procedure-target-call`, `canonical-target-repair-graph`, `interface-and-contiguity-source-use`, `lifetime-and-snapshot-source-use`.

**Oracle:** The finite effects observe known association/disassociation, explicit bounds5:7, defined
target values and a procedure result8. Association identity uses only canonical
inquiries on valid, nonzero/live entities, never raw addresses or private
representation. The companion document's P1-P8 premises apply: independently named
setup, defined read values, live targets/host instances, source-valid
type/kind/rank/interfaces, sequential state guards, and constructor observation without
a confounding outer derived assignment.

S7.5.10-007 structure-constructor pointer component fixtures: four complete
run/effect/f2023 programs observe pointer component association. A live local TARGET
datum is associated only during the observer call and yields value 17. A rank-one source
pointer with remapped bounds 5:7 supplies copied association, bounds and values
11,13,17. A known disassociated source pointer yields a disassociated component. NULL()
and NULL(MOLD=typed_pointer) produce disassociated compatible pointer components. All
use ASSOCIATED/LBOUND/UBOUND/value inquiries on defined valid entities only, never
addresses, TRANSFER, LOC or storage offsets.

S7.5.10-007 structure-constructor procedure pointer fixture (batch317): one run/effect
program supplies an eligible module function as a NOPASS procedure-pointer component,
guards ASSOCIATED, then calls the component with argument 3 and requires exact result 8.

**Oracle limitation:** NULL([MOLD]) may supply context-dependent pointer characteristics; only default
null-init has its separate restricted grammar. Pointer bounds-remapping syntax is not
part of a constructor. No target/host survives by assumption after RETURN, DEALLOCATE or
block exit.

S7.5.10-007 structure-constructor pointer component boundaries: only
live-data-target-alias, pointer-source-association-and-bounds,
known-disassociated-pointer and contextual-and-mold-null are represented.
Procedure-target calls and canonical target/interface/lifetime source-use graphs remain
pending. No dangling target or undefined pointer status is read.

S7.5.10-007 structure-constructor procedure pointer boundary (batch317): only a live
module procedure target with matching explicit interface is represented; target repair,
contiguity/interface and lifetime graphs remain pending.

**Dependencies:** 10.2.2.2-.4 (PDF193-196); C7109/C7110 (PDF107); 7.5.4.4/.5 (PDF95-96); 8.5.7/.14/.18
(PDF123-124/131/133); NULL and ASSOCIATED (PDF441-442/382-383); 19.5.2.1-.7
(PDF556-558); 15.5.2.4/.5 (PDF339-341).

### S7.5.10-008: Allocatable component sources have rank-qualified alternatives and an intrinsic-NULL exception

**Source:** 7.5.10 p6; physical PDF108, printed94, lines6-9. **Class:** Effect.

**Definition:** An allocatable component's expression is a reference to intrinsic NULL with no
arguments, an allocatable entity of the same rank, or an expression evaluating to an
entity of the same rank. A reference to intrinsic NULL gives an unallocated component.
Keep these alternatives and NULL's context/characteristics rules together: the first
alternative does not license a blanket ban on all constructor NULL(MOLD=...) forms. The
NULL result rule is applied before the general nonallocatable-expression branch.

**Diagnostic obligation:** not-required.

**Facets:** `no-mold-null-unallocated`, `typed-mold-null-unallocated`, `same-rank-allocatable-source-use`, `same-rank-other-source-use`, `rank-mismatch-source-contrast`, `null-context-and-identity-source-use`.

**Oracle:** The direct finite NULL cases require unallocated state only. The remaining source
alternatives route to the p7/p8 matrix with explicit rank/state prerequisites. The
companion document's P1-P8 premises apply: independently named setup, defined read
values, live targets/host instances, source-valid type/kind/rank/interfaces, sequential
state guards, and constructor observation without a confounding outer derived
assignment.

S7.5.10-008 structure-constructor allocatable NULL fixtures: two complete
run/effect/f2023 programs observe p6's intrinsic NULL branch for allocatable components.
The no-MOLD case supplies NULL() for scalar and rank-one allocatable components; the
typed-MOLD case uses NULL(MOLD=mold) with a declared unallocated same-rank allocatable
mold. The only observed component state is ALLOCATED false, with no bounds, length or
value inquiry.

S7.5.10-008 structure-constructor same-rank source fixtures (batch317): one run/effect
program supplies a same-rank allocated allocatable array source and a same-rank ordinary
array source to allocatable components, checking allocated status, representative bounds
and values for each branch.

**Oracle limitation:** Do not send NULL(MOLD=wrong-rank-entity) through a rank-matching branch or treat an
ordinary disassociated pointer as an unallocated allocatable-source entity. Intrinsic
NULL permits MOLD states that ASSOCIATED/LBOUND do not permit querying.

S7.5.10-008 structure-constructor allocatable NULL boundaries: only
no-mold-null-unallocated and typed-mold-null-unallocated are represented. Same-rank
source-use, rank mismatch contrasts and NULL identity/context source-use remain pending.

S7.5.10-008 structure-constructor same-rank source boundaries (batch317): only same-rank
allocated allocatable and ordinary-array source admissions are represented; unallocated
source status, rank-mismatch and NULL identity/context source-use remain pending.

**Dependencies:** 16.9.155 p3-p7/Table16.5 (PDF441-442); 8.5.3 note (PDF120); 9.7.1.3 (PDF163); 16.9.13
(PDF379-380); p2/p7/p8 (PDF107-108); C7108 (PDF107).

### S7.5.10-009: Allocatable component state, bounds, parameters and values follow the source category

**Source:** 7.5.10 p7-p8; physical PDF108, printed94, lines10-19. **Class:** Effect.

**Definition:** For an allocatable-entity source, the component has that entity's allocation status.
When allocated, it has the same bounds and takes deferred length parameters from the
source; a polymorphic component has the same dynamic type and value, otherwise it has
the value converted if necessary to its declared type. For an ordinary nonallocatable
expression source (after p6's NULL exception), the component is allocated, has the
expression's bounds, takes deferred lengths from it, and uses the same
polymorphic/nonpolymorphic value alternatives. State must be established before bounds,
deferred parameters or component values are inspected.

**Diagnostic obligation:** not-required.

**Facets:** `unallocated-source-status`, `allocated-source-bounds-values`, `allocated-source-deferred-length`, `allocated-polymorphic-source`, `allocated-source-to-nonpolymorphic-component`, `nonallocatable-whole-array-source`, `array-expression-bounds`, `ordinary-scalar-conversion`, `ordinary-character-deferred-length`, `ordinary-polymorphic-value`, `zero-extent-and-result-expression-source-use`, `independent-value-and-assignment-boundary-source-use`.

**Oracle:** A single bounded source-category matrix A is planned, using three-element arrays,
length3 character data, exact small scalar values and ordinary base/child types. State
gates precede all dependent properties and observation uses direct typed calls rather
than a second assignment. The companion document's P1-P8 premises apply: independently
named setup, defined read values, live targets/host instances, source-valid
type/kind/rank/interfaces, sequential state guards, and constructor observation without
a confounding outer derived assignment.

S7.5.10-009 structure-constructor allocatable character fixtures: two complete
run/effect/f2023 programs observe deferred-length allocatable CHARACTER components. The
allocated-entity source case allocates CHARACTER(LEN=3) source and assigns 'QRS'; the
ordinary-expression case uses a nonallocatable CHARACTER(LEN=3) variable 'LMN'. In both,
the constructed component is first required allocated, then LEN 3 and the exact three
nonblank characters are checked.

S7.5.10-009 structure-constructor allocatable source matrix fixtures (batch317): one
run/effect program checks p7 allocated-source bounds/values, p8 whole-array
bounds/values and p8 scalar REAL-to-INTEGER allocation/conversion after allocation
guards.

**Oracle limitation:** Unallocated is a valid specified state, not a defined payload. Plain expressions and
function results are not automatically allocatable entities; whole-array and expression
bounds differ. No finalization ordering, copy layout, hidden opaque component, or
unsupported-language fallback is used to claim a result. Allocation resource failure
never corroborates an expected state.

S7.5.10-009 structure-constructor allocatable character boundaries: only
allocated-source-deferred-length and ordinary-character-deferred-length are represented.
Allocation status matrices for arrays, polymorphism, nonpolymorphic conversion,
expression bounds, zero extent and assignment-boundary source-use remain pending.
Character comparisons assert LEN explicitly and use nonblank three-character values.

S7.5.10-009 structure-constructor allocatable source matrix boundaries (batch317): only
allocated integer array source, nonallocatable whole-array source and scalar conversion
are represented; unallocated, expression-bound, polymorphic, zero-extent and
assignment-boundary cases remain pending.

**Dependencies:** 7.5.8 (PDF106); 7.3.2.3/7.3.3 (PDF79); 8.5.3 (PDF120); 9.5.2/9.7.1.2-.3
(PDF154/162-163); 10.2.1.2/.3 and assignment p15 (PDF189-192); 15.5.2.4/.5/.3
(PDF339-341/349); ALLOCATED/LBOUND/UBOUND/SIZE (PDF379-380/424-425/459/469); SELECT TYPE
(PDF225-226).

<!-- END GENERATED 7.5.10 -->

## Exact compile-only cases and repairs

### `R756_valid__constructor_decl_empty_components`

**Primary:** R756; **facets:** `empty-component-list`; **phase/evidence:** compile / positive-control.

Two nonabstract ordinary types: genuinely empty and actually defaulted. Both component parentheses remain. No same-name generic or runtime-value oracle.

### `C7101_invalid__constructor_decl_abstract`

**Primary:** C7101; **facets:** `abstract-constructor`; **phase/evidence:** compile / effect.

Only ABSTRACT and its comma are removed. The type has no deferred bindings, and the explicit CLASS(base),IN observer stays legal; no TYPE(base) variable/dummy introduces C706.

Control: `C7101_valid__constructor_decl_abstract_repair`. The sole source repair in `C7101.f90` is `, abstract` -> ``; diagnostic point is line15.

### `C7101_valid__constructor_decl_abstract_repair`

**Primary:** C7101; **facets:** `abstract-constructor`, `concrete-constructor-admission`; **phase/evidence:** compile / positive-control.

Only ABSTRACT and its comma are removed. The type has no deferred bindings, and the explicit CLASS(base),IN observer stays legal; no TYPE(base) variable/dummy introduces C706.

### `C7102_invalid__constructor_decl_repeated_keyword`

**Primary:** C7102; **facets:** `repeated-keyword`; **phase/evidence:** compile / effect.

Every required component is supplied; delete only the repeated left specification. Both sources also contain the legal equal-valued distinct-component admission, so the repair is the admission witness without a cloned third program.

Control: `C7102_valid__constructor_decl_repeated_keyword_repair`. The sole source repair in `C7102.f90` is `left=13,` -> ``; diagnostic point is line13.

### `C7102_valid__constructor_decl_repeated_keyword_repair`

**Primary:** C7102; **facets:** `repeated-keyword`, `different-components-admission`; **phase/evidence:** compile / positive-control.

Every required component is supplied; delete only the repeated left specification. Both sources also contain the legal equal-valued distinct-component admission, so the repair is the admission witness without a cloned third program.

### `C7102_invalid__constructor_decl_positional_repeat`

**Primary:** C7102; **facets:** `positional-keyword-repeat`; **phase/evidence:** compile / effect.

Every required component is supplied; delete only the repeated left specification. Both sources also contain the legal equal-valued distinct-component admission, so the repair is the admission witness without a cloned third program.

Control: `C7102_valid__constructor_decl_positional_repeat_repair`. The sole source repair in `C7102.f90` is `left=13,` -> ``; diagnostic point is line13.

### `C7102_valid__constructor_decl_positional_repeat_repair`

**Primary:** C7102; **facets:** `positional-keyword-repeat`, `different-components-admission`; **phase/evidence:** compile / positive-control.

Every required component is supplied; delete only the repeated left specification. Both sources also contain the legal equal-valued distinct-component admission, so the repair is the admission witness without a cloned third program.

### `C7103_invalid__constructor_decl_ancestor_overlap`

**Primary:** C7103; **facets:** `parent-and-inherited-member`; **phase/evidence:** compile / effect.

Named primitive parent setup gives x11/y13. Delete only inherited x from the constructor; the supplied nonabstract parent still covers x/y and required local z remains19. No positional parent constructor, inaccessible component or duplicate component identity supplies the cause.

Control: `C7103_valid__constructor_decl_ancestor_overlap_repair`. The sole source repair in `C7103.f90` is `x=17,` -> ``; diagnostic point is line18.

### `C7103_valid__constructor_decl_ancestor_overlap_repair`

**Primary:** C7103; **facets:** `parent-and-inherited-member`, `disjoint-ancestor-and-child`; **phase/evidence:** compile / positive-control.

Named primitive parent setup gives x11/y13. Delete only inherited x from the constructor; the supplied nonabstract parent still covers x/y and required local z remains19. No positional parent constructor, inaccessible component or duplicate component identity supplies the cause.

### `C7104_invalid__constructor_decl_missing_ordinary`

**Primary:** C7104; **facets:** `missing-ordinary-component`; **phase/evidence:** compile / effect.

Only required right is absent; adding right=13 leaves type, access and left unchanged.

Control: `C7104_valid__constructor_decl_missing_ordinary_repair`. The sole source repair in `C7104.f90` is `pair(left=11)` -> `pair(left=11,right=13)`; diagnostic point is line12.

### `C7104_valid__constructor_decl_missing_ordinary_repair`

**Primary:** C7104; **facets:** `missing-ordinary-component`; **phase/evidence:** compile / positive-control.

Only required right is absent; adding right=13 leaves type, access and left unchanged.

### `C7104_invalid__constructor_decl_missing_pointer`

**Primary:** C7104; **facets:** `missing-pointer-component`; **phase/evidence:** compile / effect.

The nonallocatable pointer p has no default. Contextual NULL supplies it in the repair; no target, pointer value or bounds are read.

Control: `C7104_valid__constructor_decl_missing_pointer_repair`. The sole source repair in `C7104.f90` is `holder()` -> `holder(p=null())`; diagnostic point is line11.

### `C7104_valid__constructor_decl_missing_pointer_repair`

**Primary:** C7104; **facets:** `missing-pointer-component`; **phase/evidence:** compile / positive-control.

The nonallocatable pointer p has no default. Contextual NULL supplies it in the repair; no target, pointer value or bounds are read.

### `C7105_invalid__constructor_decl_keyword_order`

**Primary:** C7105; **facets:** `positional-after-keyword`; **phase/evidence:** compile / effect.

Insert only the later right keyword; the two components are otherwise supplied once. Both sources retain the distinct legal positional-prefix form, so the repair also witnesses that admission. An ordinary-call, duplicate or missing-component cause cannot qualify.

Control: `C7105_valid__constructor_decl_keyword_order_repair`. The sole source repair in `C7105.f90` is `pair(left=11,13)` -> `pair(left=11,right=13)`; diagnostic point is line13.

### `C7105_valid__constructor_decl_keyword_order_repair`

**Primary:** C7105; **facets:** `positional-after-keyword`, `positional-prefix-admission`; **phase/evidence:** compile / positive-control.

Insert only the later right keyword; the two components are otherwise supplied once. Both sources retain the distinct legal positional-prefix form, so the repair also witnesses that admission. An ordinary-call, duplicate or missing-component cause cannot qualify.

### `C7106_invalid__constructor_decl_unknown_keyword`

**Primary:** C7106; **facets:** `unknown-component-keyword`; **phase/evidence:** compile / effect.

Delete only noncomponent keyword extra; required payload17 stays present. No competing generic.

Control: `C7106_valid__constructor_decl_unknown_keyword_repair`. The sole source repair in `C7106.f90` is `,extra=19` -> ``; diagnostic point is line11.

### `C7106_valid__constructor_decl_unknown_keyword_repair`

**Primary:** C7106; **facets:** `unknown-component-keyword`; **phase/evidence:** compile / positive-control.

Delete only noncomponent keyword extra; required payload17 stays present. No competing generic.

### `C7106_invalid__constructor_decl_parameter_keyword`

**Primary:** C7106; **facets:** `parameter-is-not-component`; **phase/evidence:** compile / effect.

k=2 belongs to the complete first PDT list. Delete only second-list k=3; k is a user type parameter, never an intrinsic representation selector, and payload remains17.

Control: `C7106_valid__constructor_decl_parameter_keyword_repair`. The sole source repair in `C7106.f90` is `,k=3` -> ``; diagnostic point is line12.

### `C7106_valid__constructor_decl_parameter_keyword_repair`

**Primary:** C7106; **facets:** `parameter-is-not-component`; **phase/evidence:** compile / positive-control.

k=2 belongs to the complete first PDT list. Delete only second-list k=3; k is a user type parameter, never an intrinsic representation selector, and payload remains17.

### `C7109_invalid__constructor_decl_procedure_in_data`

**Primary:** C7109; **facets:** `procedure-target-in-data-slot`; **phase/evidence:** compile / effect.

Only the receiving keyword changes. Both pointer defaults are bare NULL(). The module procedure has a complete matching zero-argument explicit interface and the component has NOPASS; datum is a defined live scalar INTEGER TARGET. No target is dereferenced or called.

Control: `C7109_valid__constructor_decl_procedure_in_data_repair`. The sole source repair in `C7109.f90` is `holder(p=worker)` -> `holder(action=worker)`; diagnostic point is line21.

### `C7109_valid__constructor_decl_procedure_in_data_repair`

**Primary:** C7109; **facets:** `procedure-target-in-data-slot`; **phase/evidence:** compile / positive-control.

Only the receiving keyword changes. Both pointer defaults are bare NULL(). The module procedure has a complete matching zero-argument explicit interface and the component has NOPASS; datum is a defined live scalar INTEGER TARGET. No target is dereferenced or called.

### `C7109_invalid__constructor_decl_data_in_procedure`

**Primary:** C7109; **facets:** `data-target-in-procedure-slot`; **phase/evidence:** compile / effect.

Only the receiving keyword changes. Both pointer defaults are bare NULL(). The module procedure has a complete matching zero-argument explicit interface and the component has NOPASS; datum is a defined live scalar INTEGER TARGET. No target is dereferenced or called.

Control: `C7109_valid__constructor_decl_data_in_procedure_repair`. The sole source repair in `C7109.f90` is `holder(action=datum)` -> `holder(p=datum)`; diagnostic point is line21.

### `C7109_valid__constructor_decl_data_in_procedure_repair`

**Primary:** C7109; **facets:** `data-target-in-procedure-slot`; **phase/evidence:** compile / positive-control.

Only the receiving keyword changes. Both pointer defaults are bare NULL(). The module procedure has a complete matching zero-argument explicit interface and the component has NOPASS; datum is a defined live scalar INTEGER TARGET. No target is dereferenced or called.

### `C7110_invalid__constructor_decl_array_to_scalar`

**Primary:** C7110; **facets:** `array-target-to-scalar-pointer`; **phase/evidence:** compile / effect.

Repair only the TARGET element selection or receiving keyword. Both omitted pointer slots are genuinely defaulted. Shared valid contexts cover scalar/array variables, a true TARGET element and same-rank section, contextual NULL and typed NULL(MOLD=...) with declared disassociated pointer molds. No broadcasting, remapping, target or bounds inquiry is used.

Control: `C7110_valid__constructor_decl_array_to_scalar_repair`. The sole source repair in `C7110.f90` is `holder(one=vec)` -> `holder(one=vec(1))`; diagnostic point is line22.

### `C7110_valid__constructor_decl_array_to_scalar_repair`

**Primary:** C7110; **facets:** `array-target-to-scalar-pointer`, `same-rank-and-null-context`; **phase/evidence:** compile / positive-control.

Repair only the TARGET element selection or receiving keyword. Both omitted pointer slots are genuinely defaulted. Shared valid contexts cover scalar/array variables, a true TARGET element and same-rank section, contextual NULL and typed NULL(MOLD=...) with declared disassociated pointer molds. No broadcasting, remapping, target or bounds inquiry is used.

### `C7110_invalid__constructor_decl_scalar_to_array`

**Primary:** C7110; **facets:** `scalar-target-to-array-pointer`; **phase/evidence:** compile / effect.

Repair only the TARGET element selection or receiving keyword. Both omitted pointer slots are genuinely defaulted. Shared valid contexts cover scalar/array variables, a true TARGET element and same-rank section, contextual NULL and typed NULL(MOLD=...) with declared disassociated pointer molds. No broadcasting, remapping, target or bounds inquiry is used.

Control: `C7110_valid__constructor_decl_scalar_to_array_repair`. The sole source repair in `C7110.f90` is `holder(many=scalar_target)` -> `holder(one=scalar_target)`; diagnostic point is line22.

### `C7110_valid__constructor_decl_scalar_to_array_repair`

**Primary:** C7110; **facets:** `scalar-target-to-array-pointer`, `same-rank-and-null-context`; **phase/evidence:** compile / positive-control.

Repair only the TARGET element selection or receiving keyword. Both omitted pointer slots are genuinely defaulted. Shared valid contexts cover scalar/array variables, a true TARGET element and same-rank section, contextual NULL and typed NULL(MOLD=...) with declared disassociated pointer molds. No broadcasting, remapping, target or bounds inquiry is used.

### `S7_5_10_006_valid__constructor_decl_prior_definition`

**Primary:** S7.5.10-006; **facets:** `prior-definition-admission`; **phase/evidence:** compile / positive-control.

The accessible complete type precedes a valid initialized PARAMETER declaration. This is a genuine compile-only positive control for an unnumbered restriction, not an effect or mandatory diagnostic policy.

## Complete finite pending plans

The following original plan text is retained verbatim as plan metadata. Authorship or a prerequisite observation does not implement a graph or policy.

### Pending R756

* **`nonempty-form-source-use`** - PENDING — Source-use: the existing C7107 repaired constructors and S7.5.4.7 positional/parent-keyword runtime cases already contain this form. Their actual primary IDs and roles stay unchanged. A numbered R756 target is not supported by the current finite reuse mechanism; do not clone a program just to clear it.
* **`separate-parameter-and-component-lists`** - PENDING — Runtime plan: packet(k) has a user KIND tag k and ordinary INTEGER payload. Inspect packet(k=2)(payload=17) directly through a typed non-VALUE dummy and require k=2,payload=17. k never selects a machine representation. The two named lists have distinct roles.
* **`required-parentheses`** - PENDING — Source contrast at R756: remove the final ')' from a constructor in an otherwise complete assignment/call, then restore exactly that token. Any later diagnosis must identify the unterminated constructor component list at that source span, not generic EOF or missing END recovery.

### Pending R757

* **`positional-and-keyword-source-use`** - PENDING — Source-use: reuse the independently inspected positional-constructor owner S7.5.4.7-001 and the existing C7107 named-component controls only for their exact syntax contexts. Numbered-target reuse is unimplemented; no duplicate positive program is supplied.
* **`keyword-equals-required`** - PENDING — Source contrast: ordinary pair has components left/right, and pair(left 11,right=13) lacks '=' only after left. Repair to left=11. The later source-located report must identify missing keyword association in the constructor, not an unknown dummy or generic procedure ambiguity.
* **`named-component-observable`** - PENDING — Runtime plan at p2's keyword owner S7.5.10-002: inspect pair(right=13,left=11) against independent left=11/right=13 expectations. This pending edge does not create a second execution owned by R757.

### Pending R758

* **`expression-source-use`** - PENDING — Source-use: ordinary and allocatable value plans are owned by p2/p6-p8. A scalar literal is an expression source for an ordinary scalar, not a data-pointer target merely because it has the right type.
* **`data-target-source-use`** - PENDING — Source-use: canonical R1038/C1028/C1029 and p5 require a valid TARGET/POINTER designator or direct data-pointer function reference, excluding vector-subscript/coindexed targets. Target eligibility remains at its defining rule; the constructor's rank requirement remains C7110.
* **`procedure-target-source-use`** - PENDING — Source-use: R1041/C1032-C1034 and 10.2.2.4 supply eligible procedure names/references/components and interface/host-instance conditions. The finite local module-procedure pointer call is planned at S7.5.10-007, not duplicated as a wrapper program.

### Pending C7101

* **`generic-name-boundary-source-use`** - PENDING — Source-use: if a function with the abstract type's name is independently resolvable, apply C7108/15.5.5.2 before interpreting the expression as R756. Do not label every such name(...) form a C7101 negative; a concrete function-result type and valid interface are prerequisites.

### Pending C7103

* **`recursive-ancestor-overlap`** - PENDING — Source contrast: a root/middle/leaf chain supplies root=root_value and an explicitly named leaf-inherited root member plus every remaining required middle/leaf component. Remove only the overlapping member specification. Before authoring, enumerate the actual inheritance associations; source-name similarity alone is not an overlap oracle.

### Pending C7104

* **`defaulted-omission-source-use`** - PENDING — Source-use: p3's defaulted-component plans and the existing 7.5.4.6 default/override cases provide scoped contexts. Preserve those owners; constructor omission and object-entry default events are not automatically the same evidence.
* **`ancestor-covered-omission-source-use`** - PENDING — Source-use: C7103's repaired ancestor-plus-child construction and existing S7.5.4.7 parent-keyword case cover ancestor contexts. Do not add individual inherited member specifications merely to satisfy this rule when the ancestor source already supplies them.
* **`allocatable-omission-source-use`** - PENDING — Source-use: omission leaves an allocatable component unallocated under p3. Only ALLOCATED is observed before a state guard; absence of an allocatable source is not a missing required nonallocatable component.

### Pending C7105

* **`all-keyword-reordering-source-use`** - PENDING — Source-use: S7.5.10-002 checks right=13,left=11 against named member values. Legal all-keyword reordering does not mean positional entries can follow them.

### Pending C7107

* **`type-name-access-source-use`** - PENDING — Source-use: C795 and 7.5.2.2/8.5.2 govern the accessible derived-type name. A proposed private type-name constructor negative must avoid an independently invalid TYPE declaration; otherwise keep its canonical name-access pair rather than counting generic unknown-name recovery here.
* **`accessible-parent-value-private-leaves`** - PENDING — Runtime plan: public parent type has private x/y initialized to11/13 by an owner setter. Public child constructor receives parent=parent_value and z=19 through an accessible public parent component. Observe private leaves only in owner procedures, and z by its public name. No explicit private x/y specs appear and no positional parent setup can cancel an order error.
* **`scope-and-use-graph`** - PENDING — Source-use: preserve module/descendant and constructor access owners and all three existing C7107-dependent links. New defined-source material intentionally stales four case fingerprints and three link fingerprints. This packet records exact deltas only; the inputs, role/phase/facet metadata and review/evidence registries are not rewritten, rerun or approved.

### Pending C7108

* **`generic-ambiguity-owner`** - PENDING — Source-use: C1514 and other canonical generic/interface restrictions own nondistinguishable specifics; 15.4.3.4.1 requires same-type-name generics to contain functions. Any prospective ambiguity negative needs its own valid minimal T/K/R/name repair and role-specific cause there, not a new invalid-overlap C7108 program.
* **`elemental-and-name-association-source-use`** - PENDING — Source-use: retain the bare-type-name antecedent of C7108; an explicit PDT type-parameter list followed by the separate component list is not simply the same one-list name(...) ambiguity. 15.5.5.2's nonelemental/elemental, local/USE/host and intrinsic alternatives retain their owners. An elemental user function can produce an array of records, but does not make R756 elemental. Renaming and generic association need exact canonical bindings.

### Pending C7109

* **`target-union-source-use`** - PENDING — Source-use: R1038/R1041 and 10.2.2.4 own target spelling, explicit-interface, purity/simplicity and host-instance conditions. The constructor wrapper does not reassign those canonical owners or duplicate existing assignment programs.

### Pending S7.5.10-001

* **`scalar-value-source-use`** - PENDING — Source-use: the actual scalar constructors in existing S7.5.4.7 positional/ancestor cases are candidates for a reviewed S-owned runtime connection. Preserve their exact member observations and ownership; no empty-success program or duplicate constructor is supplied here.

### Pending S7.5.10-002

* **`positional-order-source-use`** - PENDING — Source-use: preserve S7_5_4_7_001_valid__initialization_positions, including its zed/alpha/middle declaration-list order and independent literal result checks2/3/5. This is an eligible future finite runtime-pattern connection, not a second program or automatic link approval.
* **`inherited-order-source-use`** - PENDING — Source-use: preserve S7_5_4_7_002_valid__initialization_flat, __initialization_three_generations and __initialization_inherited_only. Their owner establishes the inherited prefix/local suffix; a family union is not implemented by naming several cases.
* **`parent-keyword-source-use`** - PENDING — Source-use: preserve S7_5_4_7_002_valid__initialization_parent_keyword at its current owner. For any new ancestor-value observation use an independently named/set parent object so two positional constructors cannot cancel an ordering defect.

### Pending S7.5.10-003

* **`declared-type-parameter-conformance-source-use`** - PENDING — Source-use: Table10.8 and 10.2.1.2's relevant type/parameter branches retain their owners, including matching derived KIND values and fixed versus deferred LEN. A future mismatch pair changes one supplied source/type-parameter premise and names that cause; no new mandatory prose-diagnostic policy is asserted.
* **`defined-assignment-not-coercion`** - PENDING — Source contrast only: even when an INTEGER-to-inner defined assignment exists, outer(17) is not thereby a valid source for a TYPE(inner) component. Repair the source to a separately declared inner value whose members were set by name. Distinguish this type-conformance failure from generic constructor/function resolution; no rejection fixture/policy is authorized solely by this prose.
* **`outer-assignment-owner-source-use`** - PENDING — Source-use: existing S10_2_1_3_025_valid and S10_2_1_3_031_valid intentionally exercise callback-producing derived assignment. Preserve those programs and their owners. A constructor assigned into another object can be followed by those assignment effects; do not misattribute their callbacks or changed bounds to construction.

### Pending S7.5.10-004

* **`nonconforming-array-source-contrast`** - PENDING — Source contrast: a four-element source is supplied to a fixed three-element component; replace only the component source expression by its defined first-three-element section. Require the ordinary component value's shape mismatch if an optional diagnostic contract is later authorized; do not claim C7110 pointer-rank reporting.
* **`scalar-component-and-elemental-boundary-source-use`** - PENDING — Source-use: constructor scalar-value semantics and ordinary assignment's array/scalar direction restrictions must be preserved. A whole array source is not a way to elementally construct an array of records with scalar components; a same-named elemental factory is governed by C7108/15.5.5.2. No vague generic-resolution negative is invented.

### Pending S7.5.10-005

* **`default-versus-explicit-setup-owner`** - PENDING — Source-use: inspect existing 7.5.4.6 enclosing-default and explicit-type-declaration initializer cases for exact eligible finite reuse. Entry/ALLOCATE/default events are not automatically constructor effects; no new event or lifetime evidence is implied.

### Pending S7.5.10-006

* **`early-use-canonical-source-contrast`** - PENDING — Source contrast: an early TYPE(record) declaration initialized by record(...) also has a not-yet-defined type-name premise. Moving the complete record definition before that declaration is a coherent repair, but does not isolate p4 from the canonical type/declaration rules. Keep the graph pending until a role-specific owner is selected; no generic 'cannot resolve' or compulsory p4 diagnostic is supplied.

### Pending S7.5.10-007

* **`canonical-target-repair-graph`** - PENDING — Source-use: C1028/C1029/C1032-C1034 own missing TARGET, vector-subscript/coindexed data targets and invalid procedure categories. Candidate repairs respectively add TARGET to the same live variable, replace a vector selection with the intended ordinary section, use the local noncoindexed object, or substitute a known eligible procedure. Each future pair must isolate that actual owner, not borrow C7109's category or C7110's rank cause.
* **`interface-and-contiguity-source-use`** - PENDING — Source-use: 10.2.2.4 retains explicit-interface, function/subroutine, result/dummy characteristics and permitted PURE/SIMPLE/elemental-intrinsic qualifications; 10.2.2.3 p6 retains contiguous target for a CONTIGUOUS pointer. A strided target contrast is repaired by removing only the component's CONTIGUOUS attribute or selecting a known contiguous target. These unnumbered compatibility conditions do not become a new mandatory diagnostic here.
* **`lifetime-and-snapshot-source-use`** - PENDING — Source-use: source-pointer reassociation and target lifetime are distinct. 19.5.2.5 and 15.6 host-instance rules govern dangling data/procedure targets. Retain existing S10_2_1_3_024_valid at the assignment owner; it does not prove constructor behavior without a separately reviewed connection.

### Pending S7.5.10-008

* **`rank-mismatch-source-contrast`** - PENDING — Source contrast: an INTEGER scalar value is supplied to an allocatable INTEGER array component, unlike the ordinary-array expansion case. Repair only the source to an independently defined same-rank array (or a context-valid NULL() if testing absence). A later optional diagnosis must identify allocatable-constructor rank, not C7110 data-pointer rank; no policy is adopted here.
* **`null-context-and-identity-source-use`** - PENDING — Source-use: 16.9.155 requires MOLD when contextual parameters are assumed, or generic/assumed-rank argument resolution needs it. A same-named ordinary function is not intrinsic NULL, and a disassociated pointer variable is not automatically the constructor NULL exception. Keep these exact canonical identity/context conditions pending rather than inventing allocation or pointer-value assertions.

### Pending S7.5.10-009

* **`unallocated-source-status`** - PENDING — Runtime in finite matrix A: pass a constructor containing a declared unallocated INTEGER allocatable array source to an IN observer. Require ALLOCATED(component)=false only. Do not allocate it to enable a value/shape assertion or read nonexistent bounds/deferred parameters.
* **`allocated-polymorphic-source`** - PENDING — Runtime in A: CLASS(base),ALLOCATABLE source is explicitly allocated as ordinary nonabstract child and initialized by named base/child fields11/13 inside a valid SELECT TYPE guard. A CLASS(base),ALLOCATABLE component must be allocated with child dynamic type and the same values. No unallocated type/value inquiry or private representation read.
* **`allocated-source-to-nonpolymorphic-component`** - PENDING — Runtime in A: reuse a CLASS(base),ALLOCATABLE source with child dynamic type but a TYPE(base),ALLOCATABLE component. Declared types/parameters satisfy p2; after allocated-state guard, observe the base value11, not an implicit allowance to feed arbitrary TYPE(child) declared sources into a TYPE(base) component. The fixed component dynamic type and value conversion retain their canonical conditions.
* **`array-expression-bounds`** - PENDING — Runtime in A: parenthesizing that array or using an ordinary section gives a non-whole-array expression whose LBOUND/UBOUND semantics are1/3 for this nonzero rank-one example. Constructed component must be allocated with1:3 and values11/13/17. Never reuse the original variable's -2 lower bound for a different expression category.
* **`ordinary-polymorphic-value`** - PENDING — Runtime in A: a defined nonallocatable TYPE(child) object supplies a CLASS(base),ALLOCATABLE component under directional type compatibility. Guard allocation, then SELECT TYPE and inspect the initialized child/base members. A nonpolymorphic component variant must instead satisfy declared-type conformance, for example through a CLASS(base) dummy with a live child effective argument.
* **`zero-extent-and-result-expression-source-use`** - PENDING — Source-use: an allocated zero-sized source remains allocated; SIZE is0 and LBOUND/UBOUND use their canonical zero-extent results, not arbitrary stored allocation bounds. A function's allocatable result reference is not automatically an allocatable-entity source by 8.5.3's note; establish its expression bounds/lifetime before extending matrix A. No unallocated function-result value is read.
* **`independent-value-and-assignment-boundary-source-use`** - PENDING — Source-use: preserve S10_2_1_3_030_valid and related assignment cases as assignment owners. Any new mutation-based independence check must isolate the constructed value from later assignment and argument aliasing, preserve target lifetimes and avoid representation/address guesses. No existing case is renamed or cloned to fill this constructor graph.

## Reproduction and separate gates

`python3 -B tools/generate_constructor_declaration_fixtures.py --check` checks the exact owned fixture bytes, facet partition and phase-aware rendered view. The focused regressions exercise minimal repairs, independently derived source faults, reporting capability at normal statuses and strict cause/nonfatal/failure guards. No check or processor observation grants a case, catalogue or link approval. The independent fixture/cause and current-observation decisions are in `doc/source_audits/batch_023.json`; all17valid controls compile with GNU f2023, and three negatives retain explicit source-only adjudication without weakening their source or reporting expectations.
