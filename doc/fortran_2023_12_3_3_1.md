# Fortran 2023 12.3.3.1: File access methods - access-position fixtures

The canonical catalogue is `doc/catalogues/file_access_methods_12_3_3_1.json`. This author packet adds finite scratch-file runtime fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 4 facets in 2 program(s); 4 of 7 facets are represented and 3 remain pending.

<!-- BEGIN GENERATED 12.3.3.1 -->

### S12.3.3.1-001: External-file access methods are sequential, direct, and stream

**Source:** S12.3.3.1, J3/24-007, 18 December 2023, physical PDF244. **Class:** Effect.

**Definition:** There are three methods of accessing external-file data: sequential, direct, and stream.

**Diagnostic obligation:** not-required.

**Facets:** `sequential-access-method`, `direct-access-method`, `stream-access-method`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`direct-access-method`, `sequential-access-method`, `stream-access-method`. They create
the external files in the run directory with STATUS='SCRATCH' and also close with
STATUS='DELETE'. Oracles are limited to exact integer/character values, character
lengths, IOSTAT zero, IOSTAT_END, and POS= inequalities or values fixed by this source.
No file name, external representation, processor-specific IOSTAT number, IOMSG text,
formatted-stream byte spacing, or unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.1-002: Allowed access methods can vary by file and processor

**Source:** S12.3.3.1, J3/24-007, 18 December 2023, physical PDF244. **Class:** Effect.

**Definition:** Some files may allow more than one access method and other files may be restricted to
one access method.

**Diagnostic obligation:** not-required.

**Facets:** `multiple-access-methods-permitted`, `single-access-method-restriction-permitted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.1-003: A file access method is determined at connection or preconnected-file creation

**Source:** S12.3.3.1, J3/24-007, 18 December 2023, physical PDF244. **Class:** Effect.

**Definition:** The method of accessing a file is determined when the file is connected to a unit or
when the file is created if it is preconnected.

**Diagnostic obligation:** not-required.

**Facets:** `access-method-determined-at-connection`, `preconnected-access-method-determined-at-creation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`access-method-determined-at-connection`. They create the external files in the run
directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited
to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.3.3.1 -->

## Access-position fixture derivations

### `three_methods` / `S12.3.3.1-001`

**Facets:** `sequential-access-method`, `direct-access-method`, `stream-access-method`.

12.3.3.1 p1 defines sequential, direct and stream methods; the fixture uses one scratch external file connection of each access method and observes data by that method.

Feature mutations: `sequential-backspace-not-rewind`, `direct-read-other-rec`, `stream-read-other-pos`, `pre-read-1-value-remove-sentinel-init`, `pre-read-1-value-expected-sentinel-init`, `pre-read-1-value-default-sentinel-init`, `pre-read-2-value-remove-sentinel-init`, `pre-read-2-value-expected-sentinel-init`, `pre-read-2-value-default-sentinel-init`, `pre-read-3-ch-remove-sentinel-init`, `pre-read-3-ch-expected-sentinel-init`, `pre-read-3-ch-default-sentinel-init`.

### `access_method_connection` / `S12.3.3.1-003`

**Facets:** `access-method-determined-at-connection`.

12.3.3.1 p2 says access method is determined when the file is connected; the same run-directory file name is connected sequential, direct, and stream, with INQUIRE(ACCESS=) and method-specific read-back each time.

Feature mutations: `connection-method-changed`, `pre-read-1-value-remove-sentinel-init`, `pre-read-1-value-expected-sentinel-init`, `pre-read-1-value-default-sentinel-init`, `pre-read-2-value-remove-sentinel-init`, `pre-read-2-value-expected-sentinel-init`, `pre-read-2-value-default-sentinel-init`, `pre-read-3-ch-remove-sentinel-init`, `pre-read-3-ch-expected-sentinel-init`, `pre-read-3-ch-default-sentinel-init`.

