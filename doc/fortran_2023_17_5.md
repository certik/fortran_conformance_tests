# Fortran 2023 17.5: Underflow mode

The canonical catalogue is `doc/catalogues/ieee_underflow_mode_17_5.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN IEEE 17.4-17.9 FIXTURES -->
## Executable IEEE 17.4-17.9 fixtures

The fixtures cover supported gradual/abrupt underflow effects, support inquiry, GET/SET round trips, and procedure-entry preservation. Every claimed facet has a distinct assertion and feature mutation; optional support is required by profiles that exit 77 before the fixture is counted.
<!-- END IEEE 17.4-17.9 FIXTURES -->

<!-- BEGIN GENERATED 17.5 -->

### S17.5-001: Processors may support gradual or abrupt underflow mode control with performance latitude

**Source:** S17.5 p1; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** Some processors allow execution-time control over gradual underflow, producing subnormal
numbers, versus abrupt underflow, producing zero; abrupt mode may improve performance.

**Diagnostic obligation:** not-required.

**Facets:** `underflow-mode-control-processor-dependent`, `gradual-underflow-produces-subnormal`, `abrupt-underflow-produces-zero`, `abrupt-underflow-performance-latitude`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.5-001 underflow-mode effect fixture: with support for datatype, subnormal values,
and underflow control required by profile, TINY(X)/2.0 through volatile variables
classifies as IEEE_POSITIVE_DENORMAL in gradual mode and equals positive zero in abrupt
mode; timing/performance latitude is not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.5-002: IEEE underflow mode procedures inquire, set, and restore supported modes

**Source:** S17.5 p2.control-procedures, p2.initial-latitude, p2.procedure-restore; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** IEEE_SET_UNDERFLOW_MODE controls underflow mode, IEEE_GET_UNDERFLOW_MODE gets it,
IEEE_SUPPORT_UNDERFLOW_CONTROL inquires support, the initial mode is processor
dependent, and procedures other than IEEE_SET_UNDERFLOW_MODE or IEEE_SET_STATUS preserve
and restore the mode.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-support-underflow-control-inquires-facility`, `ieee-get-underflow-mode-retrieves-current-mode`, `ieee-set-underflow-mode-alters-current-mode`, `initial-underflow-mode-processor-dependent`, `underflow-mode-unchanged-on-procedure-entry`, `underflow-mode-restored-on-procedure-return`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.5-002 underflow-control fixture: IEEE_SUPPORT_UNDERFLOW_CONTROL(0.0) is asserted on
the supported profile, GET returns the mode set by SET for .TRUE. and .FALSE. sentinels,
SET to the opposite of a saved mode is observed before restoration, and a helper's entry
GET sees the caller's .TRUE. mode.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.5-003: Underflow mode affects only types whose support inquiry is true

**Source:** S17.5 p3; J3/24-007, 18 December 2023, physical PDF483. **Class:** Effect.

**Definition:** Underflow mode affects only floating-point calculations of a type corresponding to an X
for which IEEE_SUPPORT_UNDERFLOW_CONTROL returns true.

**Diagnostic obligation:** not-required.

**Facets:** `underflow-mode-effects-guarded-by-support-underflow-control-true`, `underflow-mode-no-effect-assertion-for-support-false-types`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.5-003 guarded-underflow fixture: the fixture contains an explicit
IEEE_SUPPORT_UNDERFLOW_CONTROL(0.0) true assertion before setting gradual and abrupt
modes and observing TINY(X)/2.0 effects; no false-support type effect is claimed.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.4-17.9 fixture boundaries: this packet uses IEEE_ARITHMETIC only. Optional IEEE
facilities are gated by tests/profiles that require the supported branch before the
fixture runs; no unsupported-success branch is counted as coverage. Processor-dependent
initial modes, IEEE_OTHER arithmetic effects, decimal-real behavior absent from the
host, halting occurrence timing, performance latitude, status-object internals, and
reference-compiler failures of procedure-return mode restoration remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

<!-- END GENERATED 17.5 -->
