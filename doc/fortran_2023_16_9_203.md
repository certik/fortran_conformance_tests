# Fortran 2023 16.9.203: TAN

The canonical catalogue is `doc/catalogues/tan_intrinsic_16_9_203.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.203")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.203 -->

### S16.9.203-001: TAN argument restriction

**Source:** S16.9.203, J3/24-007, 18 December 2023, physical PDF464. **Class:** Restriction.

**Definition:** X shall be of type real or complex.

**Diagnostic obligation:** not-required.

**Facets:** `TAN-X-real-or-complex`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. TAN result values are processor-dependent approximations; portable
executable plans assert only type/kind characteristics and record radian interpretation
as source semantics without exact trigonometric values.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.203-002: TAN result characteristics

**Source:** S16.9.203, J3/24-007, 18 December 2023, physical PDF464. **Class:** Effect.

**Definition:** The TAN result has the same type and kind type parameter as X.

**Diagnostic obligation:** not-required.

**Facets:** `TAN-result-same-type-kind-as-X`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. TAN result values are processor-dependent approximations; portable
executable plans assert only type/kind characteristics and record radian interpretation
as source semantics without exact trigonometric values.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.203-003: TAN result is processor-dependent approximation to tan

**Source:** S16.9.203, J3/24-007, 18 December 2023, physical PDF464. **Class:** Effect.

**Definition:** The TAN result value is a processor-dependent approximation to tan(X).

**Diagnostic obligation:** not-required.

**Facets:** `TAN-processor-dependent-approximation-latitude`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. TAN result values are processor-dependent approximations; portable
executable plans assert only type/kind characteristics and record radian interpretation
as source semantics without exact trigonometric values.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

### S16.9.203-004: TAN real and complex arguments use radians for angle interpretation

**Source:** S16.9.203, J3/24-007, 18 December 2023, physical PDF464. **Class:** Effect.

**Definition:** Real X is regarded as a value in radians; for complex X, the real part is regarded as a
value in radians.

**Diagnostic obligation:** not-required.

**Facets:** `TAN-real-X-radians`, `TAN-complex-real-part-radians`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only properties fixed by 16.9: exact
kind, rank, shape, bounds, integer/logical values, interval membership, equality between
source expressions, or named constants. Processor-dependent approximations, clock
values, storage sizes, and image-dependent behavior are separated from exact
obligations. Unnumbered argument shall-restrictions are source-control restrictions only
and are not diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for STOPPED_IMAGES, STORAGE_SIZE, SUM,
SYSTEM_CLOCK, TAN, and TAND found no existing 16.9 fixture binding in this worktree;
incidental helper or unrelated source mentions of STORAGE_SIZE, SUM, and SYSTEM_CLOCK
are not rebound here. TAN result values are processor-dependent approximations; portable
executable plans assert only type/kind characteristics and record radian interpretation
as source semantics without exact trigonometric values.

**Dependencies:** Registered dependencies: existing source-form, intrinsic type, declaration, expression,
assignment, execution, array, and I/O foundations from clauses 6 through 13.
Unregistered dependencies retained: clause 5 image and team model for image inquiries;
clauses 16.3 and 16.4 for integer, bit, and numeric model terminology; ISO_FORTRAN_ENV
named constants and TEAM_TYPE; sibling 16.9 intrinsics named by pending plans such as
KIND, RANK, SHAPE, SIZE, HUGE, and SELECTED_INT_KIND; clauses 14, 15, 17, 18, and 19 for
future module, procedure, IEEE, deleted-feature, and scope interactions.

<!-- END GENERATED 16.9.203 -->
