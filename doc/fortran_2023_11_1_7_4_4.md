# Fortran 2023: 11.1.7.4.4 CYCLE statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.1.7.4.4 -->

### S11.1.7.4.4-001: A CYCLE statement belonging to a DO construct can curtail the current loop iteration

**Source:** 11.1.7.4.4 p1; J3/24-007 PDF p213. **Class:** Effect.

**Definition:** Execution of a loop iteration can be curtailed by executing a CYCLE statement that
belongs to the construct.

**Diagnostic obligation:** not-required.

**Facets:** `cycle-curtails-current-iteration`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** R1135/C1138/C1139 and p2-p5 in this packet;11.2 branch concepts NOT YET REGISTERED.

### R1135: A CYCLE statement has an optional DO construct name

**Source:** 11.1.7.4.4 R1135; J3/24-007 PDF p213. **Class:** Syntax.

**Definition:** A cycle-stmt is CYCLE with an optional do-construct-name.

**Diagnostic obligation:** required.

**Facets:** `cycle-stmt-without-name`, `cycle-stmt-with-name`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 11.1.7.2 DO construct names owned by parallel packet.

### C1138: A CYCLE statement name must designate an enclosing DO construct, and an unnamed CYCLE must be within a DO construct

**Source:** 11.1.7.4.4 C1138; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** If a do-construct-name appears on a CYCLE statement, the CYCLE statement shall be within
that do-construct; otherwise, it shall be within at least one do-construct.

**Diagnostic obligation:** required.

**Facets:** `named-cycle-target-control`, `named-cycle-outside-target-rejected`, `unnamed-cycle-in-do-control`, `unnamed-cycle-outside-do-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;11.1.7.2 construct names owned by parallel packet.

### C1139: A CYCLE statement in CHANGE TEAM, CRITICAL, or DO CONCURRENT must not belong to an outer construct

**Source:** 11.1.7.4.4 C1139; J3/24-007 PDF p214. **Class:** Restriction.

**Definition:** A cycle-stmt shall not appear within a CHANGE TEAM, CRITICAL, or DO CONCURRENT construct
if it belongs to an outer construct.

**Diagnostic obligation:** required.

**Facets:** `inner-cycle-control`, `outer-cycle-through-change-team-rejected`, `outer-cycle-through-critical-rejected`, `outer-cycle-through-do-concurrent-rejected`.

**Oracle:** Negative plans are paired with a positive control that differs in one property and is
conforming for the cited rule. Diagnostic expectations are filed only under numbered
constraints or source units that themselves contain the prohibiting words, and future
fixtures must not rely on diagnostic wording or recovery behavior.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 4.2 diagnostics registered;11.1.5 CHANGE TEAM and 11.1.6 CRITICAL registered;11.1.7.2 DO
names owned by parallel packet.

### S11.1.7.4.4-002: CYCLE statement belonging is determined by its name or by the innermost enclosing DO construct

**Source:** 11.1.7.4.4 p2; J3/24-007 PDF p214. **Class:** Effect.

**Definition:** A CYCLE statement belongs to a particular DO construct: if it contains a DO construct
name, it belongs to that DO construct; otherwise, it belongs to the innermost DO
construct in which it appears.

**Diagnostic obligation:** not-required.

**Facets:** `named-cycle-belongs-to-named-do`, `unnamed-cycle-belongs-to-innermost-do`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 11.1.7.2 construct names owned by parallel packet.

### S11.1.7.4.4-003: A CYCLE in a non-DO-CONCURRENT DO construct progresses immediately to execution-cycle step three

**Source:** 11.1.7.4.4 p3; J3/24-007 PDF p214. **Class:** Effect.

**Definition:** Execution of a CYCLE statement that belongs to a DO construct that is not DO CONCURRENT
causes immediate progression to step (3) of the execution cycle of the DO construct to
which it belongs.

**Diagnostic obligation:** not-required.

**Facets:** `nonconcurrent-cycle-skips-rest-of-body`, `nonconcurrent-cycle-still-increments-do-variable`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 11.1.7.4.3 p1 execution cycle registered in this packet.

### S11.1.7.4.4-004: A CYCLE in a DO CONCURRENT construct completes that iteration

**Source:** 11.1.7.4.4 p4; J3/24-007 PDF p214. **Class:** Effect.

**Definition:** Execution of a CYCLE statement that belongs to a DO CONCURRENT construct completes
execution of that iteration of the construct.

**Diagnostic obligation:** not-required.

**Facets:** `do-concurrent-cycle-completes-current-iteration`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 11.1.7.4.3 p3 DO CONCURRENT iterations registered in this packet.

### S11.1.7.4.4-005: A transfer of control to the end-do has the same effect as a CYCLE belonging to that DO construct

**Source:** 11.1.7.4.4 p5; J3/24-007 PDF p214. **Class:** Effect.

**Definition:** In a DO construct, a transfer of control to the end-do has the same effect as execution
of a CYCLE statement belonging to that construct.

**Diagnostic obligation:** not-required.

**Facets:** `branch-to-end-do-same-effect-as-cycle`.

**Oracle:** Positive source plans establish source form, final values, scope, locality, or status
without relying on order, side effects, temporary storage, or compiler agreement.
Assignment effects, expression evaluation latitude, intrinsic operation semantics,
pointer and allocatable status, I/O semantics, pure-procedure classification, IEEE
modules, and branch-statement forms are credited to their registered owner clauses where
applicable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future DO and DO CONCURRENT fixtures must use independently derived literal, inquiry, or
source-relation oracles; must inspect only final values and defined statuses that the
standard makes observable; must preload untouched objects with sentinels distinct from
every value the program can write; must not infer iteration order, expression evaluation
order, side-effect counts, procedure call counts, simultaneity, temporary storage,
scheduler choices, or processor-dependent ordering; must not read a variable whose
value, pointer association status, or allocation status is undefined; and must not rely
on compiler agreement, diagnostic wording, resource exhaustion, unsupported multi-image
execution, or processor-dependent kind availability.

**Dependencies:** 11.2 branch statements and branch target semantics NOT YET REGISTERED.

<!-- END GENERATED 11.1.7.4.4 -->
