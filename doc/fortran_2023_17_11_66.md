# Fortran 2023 17.11.66: IEEE_VALUE

The canonical catalogue is `doc/catalogues/ieee_value_17_11_66.json`. Effective review state comes from `Registry.catalogue_review_state("17.11.66")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted are listed in the catalogue. Fine census units are recorded there where mixed or compound source units are split.

<!-- BEGIN GENERATED 17.11.66 -->

### S17.11.66-001: IEEE_VALUE argument type restrictions

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Restriction.

**Definition:** X shall be of type real, and CLASS shall be of type IEEE_CLASS_TYPE.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-X-real`, `IEEE_VALUE-CLASS-IEEE_CLASS_TYPE`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-002: IEEE_VALUE permitted CLASS values

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Effect.

**Definition:** CLASS is permitted to be the listed IEEE_CLASS_TYPE constants, with NaN, infinity, and
subnormal class values admitted under their corresponding IEEE_SUPPORT_* inquiries.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-quiet-NaN-class-admitted`, `IEEE_VALUE-signaling-NaN-class-admitted`, `IEEE_VALUE-infinity-classes-admitted`, `IEEE_VALUE-subnormal-classes-admitted`, `IEEE_VALUE-normal-and-zero-classes-admitted`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-003: IEEE_VALUE invocation requires datatype support

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Restriction.

**Definition:** IEEE_VALUE(X,CLASS) shall not be invoked unless IEEE_SUPPORT_DATATYPE(X) is true.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-datatype-support-probe`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-004: IEEE_VALUE result characteristics

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Effect.

**Definition:** The result has the same characteristics as X.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-result-same-kind-as-X`, `IEEE_VALUE-result-shape-follows-X`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-005: IEEE_VALUE result class matches CLASS

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Effect.

**Definition:** The IEEE_VALUE result is an IEEE value in the class specified by CLASS, subject to the
signaling-NaN latitude stated separately.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-normal-classes-result`, `IEEE_VALUE-zero-classes-result`, `IEEE_VALUE-infinity-classes-result`, `IEEE_VALUE-quiet-NaN-class-result`, `IEEE_VALUE-subnormal-classes-result`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-006: IEEE_VALUE processor-dependent representative values

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Effect.

**Definition:** In most IEEE_VALUE cases, the particular representative value within the requested class
is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-representative-value-processor-dependent`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-007: IEEE_VALUE repeated invocations are stable

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Effect.

**Definition:** For any particular X kind type parameter and CLASS value, the IEEE_VALUE result value
shall not vary between invocations.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-comparable-results-stable`, `IEEE_VALUE-NaN-payload-stability-no-oracle`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-008: IEEE_VALUE signaling-NaN exception and quieting latitude

**Source:** S17.11.66, J3/24-007, 18 December 2023, physical PDF513. **Class:** Effect.

**Definition:** For a signaling NaN returned by IEEE_VALUE, whether invalid is raised and whether the
signaling NaN is converted into a quiet NaN are processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-signaling-NaN-invalid-latitude`, `IEEE_VALUE-signaling-NaN-quieting-latitude`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

### S17.11.66-009: IEEE_VALUE excludes IEEE_OTHER_VALUE as a CLASS argument

**Source:** S17.11.66 p3, J3/24-007, 18 December 2023, physical PDF513. **Class:** Restriction.

**Definition:** IEEE_OTHER_VALUE is not among the CLASS values permitted for IEEE_VALUE.

**Diagnostic obligation:** not-required.

**Facets:** `IEEE_VALUE-CLASS-not-IEEE_OTHER_VALUE`.

**Oracle:** Source registration only. Future Clause 17 fixtures use explicit probe-and-branch plans
keyed to the relevant IEEE_SUPPORT_* inquiry and initialize observed variables to
distinguished sentinels. Unsupported branches record skips rather than passes. Argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. Observer calls to restricted IEEE procedures include concrete
IEEE_SUPPORT_DATATYPE(actual) probes. Processor-dependent IEEE support, NaN payloads,
signaling-NaN behavior, exact processor-chosen representative values, and
radix-dependent exact numeric expectations are separated from portable class, kind,
rank, shape, logical, flag, and round-trip observations; any future radix-dependent
exact-value plan must spell out RADIX(x)==2 or use a radix-free value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found zero test-file and zero
fixture-manifest matches for each scoped Clause 17 name: IEEE_SUPPORT_UNDERFLOW_CONTROL,
IEEE_UNORDERED, IEEE_VALUE, IEEE_CLASS_TYPE, IEEE_STATUS_TYPE, IEEE_EXCEPTIONS,
IEEE_FEATURES, IEEE_OVERFLOW, IEEE_GET_FLAG, IEEE_SET_FLAG, IEEE_GET_STATUS,
IEEE_SET_STATUS, IEEE_SET_HALTING_MODE, IEEE_USUAL, IEEE_INVALID_FLAG,
IEEE_SIGNALING_NAN, IEEE_QUIET_NAN, IEEE_NEGATIVE_INF, IEEE_POSITIVE_INF,
IEEE_NEGATIVE_SUBNORMAL, IEEE_POSITIVE_SUBNORMAL, IEEE_NEGATIVE_NORMAL,
IEEE_NEGATIVE_ZERO, IEEE_POSITIVE_ZERO, and IEEE_POSITIVE_NORMAL. Those absent matches
leave no existing fixture binding to migrate or rebind.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic types and kinds, declarations, expressions, assignment, arrays, execution, and
I/O scaffolding. Unregistered dependencies retained: clause 16 intrinsic helper
procedures such as KIND, RANK, SHAPE, MERGE, and SELECTED_REAL_KIND; sibling Clause 17
IEEE modules, derived types, constants, support inquiries, exception-flag procedures,
rounding and underflow mode procedures, and IEEE_CLASS observer semantics; clauses 14,
15, 18, and 19 where future fixtures need program, procedure, interoperability, or
storage terms.

<!-- END GENERATED 17.11.66 -->
