# Fortran 2023 16.9.65: COSPI

The canonical catalogue is `doc/catalogues/cospi_16_9_65.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or dense source units are split.

<!-- BEGIN INTRINSICS 16.9.F FIXTURES -->

## Intrinsics 16.9.F runtime observations

This batch adds 3 generated f2023 runtime fixtures for 16.9.65, covering 4 portable pending facets with exact integer, logical, kind, shape/rank, or exactly representable zero-value oracles. Feature mutations are conforming source substitutions that must compile and fail at run time on both toolchains. Approximate COS/COSD/COSH/COSPI values and nonzero command-argument support remain pending where the standard provides no portable oracle.
<!-- END INTRINSICS 16.9.F FIXTURES -->

<!-- BEGIN GENERATED 16.9.65 -->

### S16.9.65-001: COSPI is the circular cosine intrinsic

**Source:** S16.9.65, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** COSPI is the circular cosine function.

**Diagnostic obligation:** not-required.

**Facets:** `cospi-circular-cosine-description`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.65-002: COSPI is elemental

**Source:** S16.9.65, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** COSPI is classified as an elemental function.

**Diagnostic obligation:** not-required.

**Facets:** `cospi-elemental-class`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.65-002 intrinsics_16_9_f fixture: COSPI elemental class is observed by applying
COSPI to a rank-one real array and directly inquiring the COSPI expression extent and
shape, without comparing approximated values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.65-002 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.65-003: COSPI argument X is real

**Source:** S16.9.65, J3/24-007, 18 December 2023, physical PDF400. **Class:** Restriction.

**Definition:** The argument X shall be of type real.

**Diagnostic obligation:** not-required.

**Facets:** `cospi-x-real`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.65-003 intrinsics_16_9_f fixture: COSPI X-real positive control calls COSPI with a
selected-real-kind scalar and observes only the direct result kind, making the real
argument feature load-bearing without any exact value oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.65-003 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.65-004: COSPI result characteristics match X

**Source:** S16.9.65, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** The COSPI result has the same type, kind, and shape characteristics as X under elemental
application.

**Diagnostic obligation:** not-required.

**Facets:** `cospi-result-same-kind-as-x`, `cospi-result-elemental-shape`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.65-004 intrinsics_16_9_f fixture: COSPI result characteristics are checked
directly with KIND(COSPI(scalar_x)) and SHAPE(COSPI(vector_x)) inquiries; p5
approximation values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

S16.9.65-004 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.65-005: COSPI interprets X in half-revolutions

**Source:** S16.9.65, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** COSPI's required mathematical reference is the cosine of X regarded as a value in
half-revolutions, approximately equal to COS(X times pi).

**Diagnostic obligation:** not-required.

**Facets:** `cospi-half-revolution-argument`.

**Oracle:** The half-revolution meaning is source-accounted separately from the approximation
latitude. This packet records no exact numeric result plan for COSPI.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.65-006: COSPI approximation is processor dependent

**Source:** S16.9.65, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** The COSPI numeric value is a processor-dependent approximation to the mathematical
cosine in half-revolutions.

**Diagnostic obligation:** not-required.

**Facets:** `cospi-processor-dependent-approximation`.

**Oracle:** No portable oracle for exact COSPI values is available from p5 because the approximation
is processor dependent and no error bound is specified.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

<!-- END GENERATED 16.9.65 -->
