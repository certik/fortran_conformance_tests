# Fortran 2023 16.9.196: SPLIT

The canonical catalogue is `doc/catalogues/split_intrinsic_16_9_196.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.196")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.196 -->

### S16.9.196-001: SPLIT description defines the intrinsic operation

**Source:** S16.9.196, J3/24-007, 18 December 2023, physical PDF460. **Class:** Effect.

**Definition:** SPLIT parses a string into tokens, one at a time.

**Diagnostic obligation:** not-required.

**Facets:** `SPLIT-operation-description`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, position, sign, or
bounded-relation observations. Processor-dependent approximations and
processor-dependent real models are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SINH, SINPI, SIZE, SPACING, SPLIT,
SPREAD, and SQRT found no fixture metadata binding these 16.9.192-16.9.198 section
facets. SINH, SINPI, and SQRT had no matches; SIZE has many unrelated inquiry uses plus
assumed-size fixture metadata; SPACING matched only format-spacing text; SPLIT matched
Python/token wording plus one fixed-source token facet; SPREAD matched generator guard
text and legacy rationale text.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and I/O foundations from clauses 6 through 13
where already catalogued. Unregistered dependencies retained: clause 3 terminology; 7.4
type, kind, and character ordering foundations; 9.5 array, pointer, allocation, and
assumed-rank rules; 10.1 expression and intrinsic operation rules; 15 procedure and
dummy-argument association rules; 16.3 integer and bit context where helper intrinsics
are used; 16.4 real model terminology; neighbouring 16.9 intrinsics such as PRODUCT,
SHAPE, TINY, IEEE helpers from clause 17, and clauses 14 through 19 where future
fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.196-002: SPLIT is a simple subroutine

**Source:** S16.9.196, J3/24-007, 18 December 2023, physical PDF460. **Class:** Effect.

**Definition:** SPLIT has the simple subroutine class.

**Diagnostic obligation:** not-required.

**Facets:** `SPLIT-simple-subroutine-class`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, position, sign, or
bounded-relation observations. Processor-dependent approximations and
processor-dependent real models are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SINH, SINPI, SIZE, SPACING, SPLIT,
SPREAD, and SQRT found no fixture metadata binding these 16.9.192-16.9.198 section
facets. SINH, SINPI, and SQRT had no matches; SIZE has many unrelated inquiry uses plus
assumed-size fixture metadata; SPACING matched only format-spacing text; SPLIT matched
Python/token wording plus one fixed-source token facet; SPREAD matched generator guard
text and legacy rationale text.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and I/O foundations from clauses 6 through 13
where already catalogued. Unregistered dependencies retained: clause 3 terminology; 7.4
type, kind, and character ordering foundations; 9.5 array, pointer, allocation, and
assumed-rank rules; 10.1 expression and intrinsic operation rules; 15 procedure and
dummy-argument association rules; 16.3 integer and bit context where helper intrinsics
are used; 16.4 real model terminology; neighbouring 16.9 intrinsics such as PRODUCT,
SHAPE, TINY, IEEE helpers from clause 17, and clauses 14 through 19 where future
fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.196-003: SPLIT argument restrictions and intents

**Source:** S16.9.196, J3/24-007, 18 December 2023, physical PDF460. **Class:** Restriction.

**Definition:** STRING and SET shall be scalar character of the same kind, POS shall be integer scalar
with the specified range and INTENT(INOUT), and BACK shall be optional logical scalar.

**Diagnostic obligation:** not-required.

**Facets:** `SPLIT-STRING-character-scalar`, `SPLIT-SET-character-scalar-same-kind`, `SPLIT-POS-integer-scalar-in-range`, `SPLIT-BACK-logical-scalar`, `SPLIT-argument-intents`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, position, sign, or
bounded-relation observations. Processor-dependent approximations and
processor-dependent real models are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SINH, SINPI, SIZE, SPACING, SPLIT,
SPREAD, and SQRT found no fixture metadata binding these 16.9.192-16.9.198 section
facets. SINH, SINPI, and SQRT had no matches; SIZE has many unrelated inquiry uses plus
assumed-size fixture metadata; SPACING matched only format-spacing text; SPLIT matched
Python/token wording plus one fixed-source token facet; SPREAD matched generator guard
text and legacy rationale text.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and I/O foundations from clauses 6 through 13
where already catalogued. Unregistered dependencies retained: clause 3 terminology; 7.4
type, kind, and character ordering foundations; 9.5 array, pointer, allocation, and
assumed-rank rules; 10.1 expression and intrinsic operation rules; 15 procedure and
dummy-argument association rules; 16.3 integer and bit context where helper intrinsics
are used; 16.4 real model terminology; neighbouring 16.9 intrinsics such as PRODUCT,
SHAPE, TINY, IEEE helpers from clause 17, and clauses 14 through 19 where future
fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.196-004: SPLIT SET characters delimit possibly zero-length tokens

**Source:** S16.9.196, J3/24-007, 18 December 2023, physical PDF460. **Class:** Effect.

**Definition:** Each character in SET is a token delimiter; delimiter-separated sequences, including
leading, trailing, and consecutive delimiters, define tokens with zero or more
characters.

**Diagnostic obligation:** not-required.

**Facets:** `SPLIT-SET-characters-are-delimiters`, `SPLIT-zero-length-leading-token`, `SPLIT-zero-length-consecutive-token`, `SPLIT-zero-length-trailing-token`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, position, sign, or
bounded-relation observations. Processor-dependent approximations and
processor-dependent real models are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SINH, SINPI, SIZE, SPACING, SPLIT,
SPREAD, and SQRT found no fixture metadata binding these 16.9.192-16.9.198 section
facets. SINH, SINPI, and SQRT had no matches; SIZE has many unrelated inquiry uses plus
assumed-size fixture metadata; SPACING matched only format-spacing text; SPLIT matched
Python/token wording plus one fixed-source token facet; SPREAD matched generator guard
text and legacy rationale text.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and I/O foundations from clauses 6 through 13
where already catalogued. Unregistered dependencies retained: clause 3 terminology; 7.4
type, kind, and character ordering foundations; 9.5 array, pointer, allocation, and
assumed-rank rules; 10.1 expression and intrinsic operation rules; 15 procedure and
dummy-argument association rules; 16.3 integer and bit context where helper intrinsics
are used; 16.4 real model terminology; neighbouring 16.9 intrinsics such as PRODUCT,
SHAPE, TINY, IEEE helpers from clause 17, and clauses 14 through 19 where future
fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.196-005: SPLIT forward scan assigns POS to the next delimiter or LEN plus one

**Source:** S16.9.196, J3/24-007, 18 December 2023, physical PDF460. **Class:** Effect.

**Definition:** With BACK absent or false, SPLIT assigns POS to the leftmost token delimiter after the
incoming POS, or to LEN(STRING)+1 with no later delimiter, identifying token start and
end positions.

**Diagnostic obligation:** not-required.

**Facets:** `SPLIT-forward-next-delimiter`, `SPLIT-forward-no-delimiter-LEN-plus-one`, `SPLIT-forward-token-bounds`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, position, sign, or
bounded-relation observations. Processor-dependent approximations and
processor-dependent real models are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SINH, SINPI, SIZE, SPACING, SPLIT,
SPREAD, and SQRT found no fixture metadata binding these 16.9.192-16.9.198 section
facets. SINH, SINPI, and SQRT had no matches; SIZE has many unrelated inquiry uses plus
assumed-size fixture metadata; SPACING matched only format-spacing text; SPLIT matched
Python/token wording plus one fixed-source token facet; SPREAD matched generator guard
text and legacy rationale text.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and I/O foundations from clauses 6 through 13
where already catalogued. Unregistered dependencies retained: clause 3 terminology; 7.4
type, kind, and character ordering foundations; 9.5 array, pointer, allocation, and
assumed-rank rules; 10.1 expression and intrinsic operation rules; 15 procedure and
dummy-argument association rules; 16.3 integer and bit context where helper intrinsics
are used; 16.4 real model terminology; neighbouring 16.9 intrinsics such as PRODUCT,
SHAPE, TINY, IEEE helpers from clause 17, and clauses 14 through 19 where future
fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.196-006: SPLIT backward scan assigns POS to the previous delimiter or zero

**Source:** S16.9.196, J3/24-007, 18 December 2023, physical PDF460. **Class:** Effect.

**Definition:** With BACK true, SPLIT assigns POS to the rightmost token delimiter before the incoming
POS, or to zero with no earlier delimiter, identifying token end and start positions.

**Diagnostic obligation:** not-required.

**Facets:** `SPLIT-backward-previous-delimiter`, `SPLIT-backward-no-delimiter-zero`, `SPLIT-backward-token-bounds`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, position, sign, or
bounded-relation observations. Processor-dependent approximations and
processor-dependent real models are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SINH, SINPI, SIZE, SPACING, SPLIT,
SPREAD, and SQRT found no fixture metadata binding these 16.9.192-16.9.198 section
facets. SINH, SINPI, and SQRT had no matches; SIZE has many unrelated inquiry uses plus
assumed-size fixture metadata; SPACING matched only format-spacing text; SPLIT matched
Python/token wording plus one fixed-source token facet; SPREAD matched generator guard
text and legacy rationale text.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and I/O foundations from clauses 6 through 13
where already catalogued. Unregistered dependencies retained: clause 3 terminology; 7.4
type, kind, and character ordering foundations; 9.5 array, pointer, allocation, and
assumed-rank rules; 10.1 expression and intrinsic operation rules; 15 procedure and
dummy-argument association rules; 16.3 integer and bit context where helper intrinsics
are used; 16.4 real model terminology; neighbouring 16.9 intrinsics such as PRODUCT,
SHAPE, TINY, IEEE helpers from clause 17, and clauses 14 through 19 where future
fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.196-007: SPLIT can identify partial tokens from an interior POS

**Source:** S16.9.196, J3/24-007, 18 December 2023, physical PDF460. **Class:** Effect.

**Definition:** Starting SPLIT at a nondelimiter character inside STRING identifies a partial token
rather than a complete delimiter-bounded token.

**Diagnostic obligation:** not-required.

**Facets:** `SPLIT-partial-token-from-interior-POS`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
or opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character, shape, position, sign, or
bounded-relation observations. Processor-dependent approximations and
processor-dependent real models are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SINH, SINPI, SIZE, SPACING, SPLIT,
SPREAD, and SQRT found no fixture metadata binding these 16.9.192-16.9.198 section
facets. SINH, SINPI, and SQRT had no matches; SIZE has many unrelated inquiry uses plus
assumed-size fixture metadata; SPACING matched only format-spacing text; SPLIT matched
Python/token wording plus one fixed-source token facet; SPREAD matched generator guard
text and legacy rationale text.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and I/O foundations from clauses 6 through 13
where already catalogued. Unregistered dependencies retained: clause 3 terminology; 7.4
type, kind, and character ordering foundations; 9.5 array, pointer, allocation, and
assumed-rank rules; 10.1 expression and intrinsic operation rules; 15 procedure and
dummy-argument association rules; 16.3 integer and bit context where helper intrinsics
are used; 16.4 real model terminology; neighbouring 16.9 intrinsics such as PRODUCT,
SHAPE, TINY, IEEE helpers from clause 17, and clauses 14 through 19 where future
fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.196 -->
