# Fortran 2023 16.9.53: CMPLX

The canonical catalogue is `doc/catalogues/cmplx_16_9_53.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.53")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Seven base units: p1-p4 on physical PDF394 and p5-p7 on physical PDF395; p6 is split between exact construction obligations and REAL-conversion latitude.

<!-- BEGIN GENERATED 16.9.53 -->

### S16.9.53-001: CMPLX(X[,KIND]) accepts complex X and optional scalar integer constant KIND

**Source:** S16.9.53, J3/24-007, 18 December 2023, physical PDF394. **Class:** Restriction.

**Definition:** For the one-argument form with optional KIND, X shall be complex and KIND shall be a
scalar integer constant expression if present.

**Diagnostic obligation:** not-required.

**Facets:** `complex-form-X-complex`, `complex-form-KIND-scalar-integer-constant-expression`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.53-002: CMPLX(X[,Y,KIND]) accepts integer, real, or BOZ X and optional compatible Y and KIND

**Source:** S16.9.53, J3/24-007, 18 December 2023, physical PDF394. **Class:** Restriction.

**Definition:** For the two-value form, X shall be integer, real, or a BOZ literal; Y, if present, shall
be integer, real, or a BOZ literal; KIND, if present, shall be a scalar integer constant
expression.

**Diagnostic obligation:** not-required.

**Facets:** `real-form-X-integer-real-or-BOZ`, `real-form-Y-integer-real-or-BOZ`, `real-form-KIND-scalar-integer-constant-expression`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles. For CMPLX BOZ arguments, this packet treats actual nonzero
BOZ literals as observable argument admissions only: a future source/control can show
that CMPLX accepts a BOZ literal in X or Y and still produces a complex result of the
selected kind. That type/kind observation would also hold if the BOZ bits were ignored,
so it is not claimed as evidence of BOZ-to-real component value conversion. The
component value has no portable oracle because 16.9.172 p5 case (iii), with 16.3.3
padding or truncation, makes interpretation of the BOZ bit sequence processor dependent.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.53-003: CMPLX returns a complex result of the selected or default real kind

**Source:** S16.9.53, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** The result is complex; present KIND selects the kind type parameter and absent KIND
selects default real kind.

**Diagnostic obligation:** not-required.

**Facets:** `result-complex-type`, `present-KIND-selects-complex-kind`, `absent-KIND-default-real-kind`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.53-004: CMPLX defaulting and component construction define exact results for exact inputs

**Source:** S16.9.53, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** Absent Y is treated as zero, absent KIND is treated as KIND(0.0), complex X is
decomposed into REAL and AIMAG, and CMPLX(X,Y,KIND) has real and imaginary parts
REAL(X,KIND) and REAL(Y,KIND).

**Diagnostic obligation:** not-required.

**Facets:** `absent-Y-supplies-zero-imaginary-part`, `absent-KIND-supplies-default-real-kind`, `complex-X-decomposes-to-parts`, `components-come-from-REAL-conversions`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles. For CMPLX BOZ arguments, this packet treats actual nonzero
BOZ literals as observable argument admissions only: a future source/control can show
that CMPLX accepts a BOZ literal in X or Y and still produces a complex result of the
selected kind. That type/kind observation would also hold if the BOZ bits were ignored,
so it is not claimed as evidence of BOZ-to-real component value conversion. The
component value has no portable oracle because 16.9.172 p5 case (iii), with 16.3.3
padding or truncation, makes interpretation of the BOZ bit sequence processor dependent.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

### S16.9.53-005: CMPLX component conversions inherit REAL approximation latitude for inexact conversions

**Source:** S16.9.53, J3/24-007, 18 December 2023, physical PDF394. **Class:** Effect.

**Definition:** Where the REAL conversions named by the result-value rule approximate an inexact or
kind-changing value, the exact rounded component value is not fixed by this CMPLX unit.

**Diagnostic obligation:** not-required.

**Facets:** `inexact-REAL-conversion-not-exact-oracle`.

**Oracle:** Plans are pending and source-derived. Exact result-value plans use inputs whose required
results are not affected by rounding or processor-dependent representation; restrictions
on arguments are source-control plans unless a later numbered rule supplies a required
diagnostic.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument 'shall' restrictions on intrinsic procedure references are program restrictions
and are not promoted to diagnostic facets without a numbered constraint or other
detection requirement. Positive pending plans use exact intrinsic results, non-default
sentinels where variables are observed, and feature mutations such as changing the
intrinsic name, swapping argument order, omitting KIND, or changing RESULT_IMAGE where
applicable. Processor-dependent approximations, processor-dependent collating
characters, multi-image collective behavior, error status values, and IOMSG text are not
asserted as exact oracles. For CMPLX BOZ arguments, this packet treats actual nonzero
BOZ literals as observable argument admissions only: a future source/control can show
that CMPLX accepts a BOZ literal in X or Y and still produces a complex result of the
selected kind. That type/kind observation would also hold if the BOZ bits were ignored,
so it is not claimed as evidence of BOZ-to-real component value conversion. The
component value has no portable oracle because 16.9.172 p5 case (iii), with 16.3.3
padding or truncation, makes interpretation of the BOZ bit sequence processor dependent.

**Dependencies:** Registered dependencies cited, not restated: clauses 6, 8, 9, 10, 11, and registered
12-13 where existing source form, declarations, expressions, execution constructs,
internal I/O, and exact character/numeric observations are used by future fixtures.
Unregistered dependencies explicitly retained: earlier and later 16.9 intrinsic
procedure semantics, 16.6 collective STAT/ERRMSG semantics, clause 15 procedure
interface rules for CO_REDUCE operation arguments, clause 14 teams/images for collective
execution, clause 17 IEEE behavior where real exceptional values could otherwise be
reached, and clauses 3, 5, 18, and 19 for terminology, lexical representation, deleted
features, and scope/lifetime controls.

<!-- END GENERATED 16.9.53 -->
