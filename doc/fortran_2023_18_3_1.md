# Fortran 2023 18.3.1: Interoperability of intrinsic types

The canonical catalogue is `doc/catalogues/interoperability_intrinsic_types_18_3_1.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, table18.2, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 18.3.1 -->

### S18.3.1-001: Intrinsic Fortran type parameters interoperate with the C types named in Table 18.2

**Source:** S18.3.1, J3/24-007, 18 December 2023, physical PDF525. **Class:** Effect.

**Definition:** A Fortran intrinsic type with a particular kind type parameter, and character length one
for character, is interoperable with a listed C type exactly on the corresponding Table
18.2 row; such a combination is also interoperable with compatible unqualified C types.

**Diagnostic obligation:** not-required.

**Facets:** `table-row-type-kind-correspondence`, `character-length-one-correspondence`, `unqualified-compatible-c-type-correspondence`.

**Oracle:** Source registration only. Positive plans establish load-bearing values before the
observation, usually with distinct sentinels or C-written data, then assert only
portable properties: successful compilation and linking, exact integer/logical/character
values, exactly representable real values, required rejection of numbered constraints,
or source-use restrictions fixed by the standard. Mixed Fortran/C plans build a Fortran
source with a small C companion. They do not assert kind numbers, addresses, structure
padding, diagnostic wording, C implementation details beyond the declared companion
objects, or support for a C type whose ISO_C_BINDING named constant is negative.

**Oracle limitation:** This packet creates no Fortran fixture, compiler invocation, execution evidence,
evidence link, fixture approval, oracle approval, or coverage claim. Unnumbered program
restrictions are retained as source-control plans and are not promoted to required
diagnostic facets. Coarray-dependent observations are out of scope by user decision.
NOTES and examples are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 10, 11, 12, 13, 15, and 16 for source form,
types, attributes, expressions, execution, I/O, procedure calls, and ISO_C_BINDING named
constants and procedures. Unregistered or separately catalogued dependencies explicitly
retained: C companion semantics from ISO/IEC 9899:2018, BIND(C) data-entity rules, ENUM
definitions, argument association, 18.3.7 procedure interoperability, 18.10
ISO_C_BINDING procedures, and later C descriptor rules.

### S18.3.1-002: ISO_C_BINDING named-constant signs determine intrinsic interoperable combinations

**Source:** S18.3.1, J3/24-007, 18 December 2023, physical PDF525. **Class:** Effect.

**Definition:** The second Table 18.2 column names ISO_C_BINDING constants; a negative value means no
Fortran type and type-parameter combination interoperates with that C type, while
positive values identify usable Fortran kind type parameter values.

**Diagnostic obligation:** not-required.

**Facets:** `iso-c-binding-kind-constant-source`, `negative-kind-means-no-fortran-combination`, `positive-kind-identifies-usable-kind`.

**Oracle:** Source registration only. Positive plans establish load-bearing values before the
observation, usually with distinct sentinels or C-written data, then assert only
portable properties: successful compilation and linking, exact integer/logical/character
values, exactly representable real values, required rejection of numbered constraints,
or source-use restrictions fixed by the standard. Mixed Fortran/C plans build a Fortran
source with a small C companion. They do not assert kind numbers, addresses, structure
padding, diagnostic wording, C implementation details beyond the declared companion
objects, or support for a C type whose ISO_C_BINDING named constant is negative.

**Oracle limitation:** This packet creates no Fortran fixture, compiler invocation, execution evidence,
evidence link, fixture approval, oracle approval, or coverage claim. Unnumbered program
restrictions are retained as source-control plans and are not promoted to required
diagnostic facets. Coarray-dependent observations are out of scope by user decision.
NOTES and examples are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 10, 11, 12, 13, 15, and 16 for source form,
types, attributes, expressions, execution, I/O, procedure calls, and ISO_C_BINDING named
constants and procedures. Unregistered or separately catalogued dependencies explicitly
retained: C companion semantics from ISO/IEC 9899:2018, BIND(C) data-entity rules, ENUM
definitions, argument association, 18.3.7 procedure interoperability, 18.10
ISO_C_BINDING procedures, and later C descriptor rules.

### S18.3.1-003: Support for Table 18.2 C rows and exact kind values is processor latitude

**Source:** S18.3.1, J3/24-007, 18 December 2023, physical PDF525. **Class:** Effect.

**Definition:** A processor can expose a negative ISO_C_BINDING named constant for a Table 18.2 C row,
meaning that no interoperable Fortran combination exists for that C type; positive kind
values are usable through the named constants without fixing their exact integer values.

**Diagnostic obligation:** not-required.

**Facets:** `c-row-support-processor-latitude`, `kind-value-exact-number-processor-latitude`.

**Oracle:** Source registration only. Positive plans establish load-bearing values before the
observation, usually with distinct sentinels or C-written data, then assert only
portable properties: successful compilation and linking, exact integer/logical/character
values, exactly representable real values, required rejection of numbered constraints,
or source-use restrictions fixed by the standard. Mixed Fortran/C plans build a Fortran
source with a small C companion. They do not assert kind numbers, addresses, structure
padding, diagnostic wording, C implementation details beyond the declared companion
objects, or support for a C type whose ISO_C_BINDING named constant is negative.

**Oracle limitation:** This packet creates no Fortran fixture, compiler invocation, execution evidence,
evidence link, fixture approval, oracle approval, or coverage claim. Unnumbered program
restrictions are retained as source-control plans and are not promoted to required
diagnostic facets. Coarray-dependent observations are out of scope by user decision.
NOTES and examples are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 10, 11, 12, 13, 15, and 16 for source form,
types, attributes, expressions, execution, I/O, procedure calls, and ISO_C_BINDING named
constants and procedures. Unregistered or separately catalogued dependencies explicitly
retained: C companion semantics from ISO/IEC 9899:2018, BIND(C) data-entity rules, ENUM
definitions, argument association, 18.3.7 procedure interoperability, 18.10
ISO_C_BINDING procedures, and later C descriptor rules.

### S18.3.1-004: Intrinsic type and type-parameter combinations inherit interoperability from C-type interoperability

**Source:** S18.3.1, J3/24-007, 18 December 2023, physical PDF525. **Class:** Effect.

**Definition:** A combination of intrinsic type and type parameters is interoperable exactly by virtue
of being interoperable with a C type; the C types named in Table 18.2 are the referenced
ISO C types.

**Diagnostic obligation:** not-required.

**Facets:** `intrinsic-combination-interoperable-via-c-type`, `c-type-definitions-referenced`.

**Oracle:** Source registration only. Positive plans establish load-bearing values before the
observation, usually with distinct sentinels or C-written data, then assert only
portable properties: successful compilation and linking, exact integer/logical/character
values, exactly representable real values, required rejection of numbered constraints,
or source-use restrictions fixed by the standard. Mixed Fortran/C plans build a Fortran
source with a small C companion. They do not assert kind numbers, addresses, structure
padding, diagnostic wording, C implementation details beyond the declared companion
objects, or support for a C type whose ISO_C_BINDING named constant is negative.

**Oracle limitation:** This packet creates no Fortran fixture, compiler invocation, execution evidence,
evidence link, fixture approval, oracle approval, or coverage claim. Unnumbered program
restrictions are retained as source-control plans and are not promoted to required
diagnostic facets. Coarray-dependent observations are out of scope by user decision.
NOTES and examples are informative only.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 10, 11, 12, 13, 15, and 16 for source form,
types, attributes, expressions, execution, I/O, procedure calls, and ISO_C_BINDING named
constants and procedures. Unregistered or separately catalogued dependencies explicitly
retained: C companion semantics from ISO/IEC 9899:2018, BIND(C) data-entity rules, ENUM
definitions, argument association, 18.3.7 procedure interoperability, 18.10
ISO_C_BINDING procedures, and later C descriptor rules.

<!-- END GENERATED 18.3.1 -->
