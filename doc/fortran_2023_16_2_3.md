# Fortran 2023 16.2.3: Mask arguments

The canonical catalogue is `doc/catalogues/mask_arguments_16_2_3.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.2.3 -->

### S16.2.3-001: MASK arguments select elements operated on by array intrinsics

**Source:** S16.2.3, J3/24-007, 18 December 2023, physical PDF365. **Class:** Effect.

**Definition:** Some array intrinsics have an optional logical MASK argument used to select elements of
one or more arguments to be operated on.

**Diagnostic obligation:** not-required.

**Facets:** `mask-argument-logical-selector`, `mask-selects-operated-elements`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.2.3-002: Unselected MASK elements need not be defined

**Source:** S16.2.3, J3/24-007, 18 December 2023, physical PDF365. **Class:** Effect.

**Definition:** Any element not selected by MASK need not be defined when the function is invoked.

**Diagnostic obligation:** not-required.

**Facets:** `unselected-mask-elements-need-not-be-defined`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

### S16.2.3-003: MASK affects only function value, not prior array-expression evaluation

**Source:** S16.2.3, J3/24-007, 18 December 2023, physical PDF365. **Class:** Effect.

**Definition:** MASK affects only the function value and does not affect evaluation, before invocation,
of arguments that are array expressions.

**Diagnostic obligation:** not-required.

**Facets:** `mask-affects-only-function-value`, `mask-does-not-suppress-prior-array-expression-evaluation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default or
load-bearing state first and observe only portable properties fixed by this unit or the
cited 16.9 specification: exact integer, logical, character, bit-sequence, STAT,
kind/model-parameter, class, or procedure-use behavior. Diagnostic plans are
source-control plans; numbered constraints with required diagnostic obligations state
rejection as their separate portable oracle. Processor-dependent representations,
command/environment/time state, failed/stopped-image availability, approximation
quality, ordering latitude, progress timing, and explanatory message text are not
asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive each pending plan from the pinned source before binding
it.

**Dependencies:** Registered dependencies: clauses 5, 7, 8, 10, 11, and 15 for images/teams, data objects,
type/kind/shape, expressions, execution ordering, image-control contexts, and procedure
argument association. Unregistered dependencies explicitly retained: individual 16.9
intrinsic specifications, ISO_FORTRAN_ENV named constants, and intrinsic-module
semantics where a future fixture reaches them.

<!-- END GENERATED 16.2.3 -->
