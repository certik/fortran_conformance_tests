# Fortran 2023 17.2: Derived types and constants

The canonical catalogue is `doc/catalogues/ieee_types_constants_17_2.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN IEEE 17.1-17.3 FIXTURES -->
## Executable IEEE 17.1-17.3 fixtures

This packet covers IEEE_ARITHMETIC class/round constants and comparison operators, plus IEEE_EXCEPTIONS public flag arrays and modes/status types through the IEEE_ARITHMETIC reexport path. IEEE_FEATURES facets remain pending. No fixture contains an unsupported-success oracle; all discharged facets have load-bearing feature mutations.
<!-- END IEEE 17.1-17.3 FIXTURES -->

<!-- BEGIN GENERATED 17.2 -->

### S17.2-001: IEEE module derived types have private nonallocatable nonpointer direct components

**Source:** S17.2 p1; J3/24-007, 18 December 2023, physical PDF480. **Class:** Effect.

**Definition:** The IEEE_EXCEPTIONS, IEEE_ARITHMETIC, and IEEE_FEATURES modules define derived types
whose components are all private and whose direct components are not allocatable or
pointers.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-derived-types-components-private`, `ieee-derived-types-no-direct-allocatable-components`, `ieee-derived-types-no-direct-pointer-components`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.2-002: IEEE_EXCEPTIONS defines flag, modes, status types and exception constants

**Source:** S17.2 p2; J3/24-007, 18 December 2023, physical PDF480. **Class:** Effect.

**Definition:** IEEE_EXCEPTIONS defines IEEE_FLAG_TYPE, IEEE_MODES_TYPE, IEEE_STATUS_TYPE, scalar
exception constants, and the IEEE_USUAL and IEEE_ALL arrays with the listed elements.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-flag-type-values-are-named-exception-constants`, `ieee-usual-array-has-overflow-divide-invalid`, `ieee-all-array-extends-usual-underflow-inexact`, `ieee-modes-status-types-defined`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.2-002 IEEE exception type fixture: with the ieee-flags-all profile requiring all
five default-real flags to be supported and nonhalting, the program imports
IEEE_ARITHMETIC only, declares IEEE_MODES_TYPE and IEEE_STATUS_TYPE dummies, and uses
IEEE_SET_FLAG/IEEE_GET_FLAG to prove the scalar flag constants, IEEE_USUAL ordering, and
IEEE_ALL extension.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.2 fixture boundaries: this packet uses IEEE_ARITHMETIC only. IEEE_FEATURES_TYPE
and IEEE_FEATURES constants remain pending because 17.1 p1 makes IEEE_FEATURES provision
and contents processor dependent. Exception flag type, array, modes, and status facets
are gated by profiles that require the needed IEEE_SUPPORT_FLAG branches before the case
runs.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.2-003: IEEE_ARITHMETIC defines IEEE_CLASS_TYPE class constants and denormal aliases

**Source:** S17.2 p3.class-constants; J3/24-007, 18 December 2023, physical PDF480. **Class:** Effect.

**Definition:** IEEE_CLASS_TYPE identifies floating-point classes with the listed named constants;
IEEE_NEGATIVE_DENORMAL and IEEE_POSITIVE_DENORMAL have the same values as the
corresponding SUBNORMAL constants.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-class-type-listed-class-constants`, `ieee-denormal-class-aliases-equal-subnormal-classes`, `ieee-class-type-values-comparable`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.2-003 IEEE class constant fixture: the program imports the listed class constants
from IEEE_ARITHMETIC and OPERATOR(==)/OPERATOR(/=), assigns them to IEEE_CLASS_TYPE
objects, checks selected distinct named values, checks DENORMAL aliases equal the
corresponding SUBNORMAL constants, and checks same-value/different-value comparisons.
This is intentionally IEEE_ARITHMETIC-only: S17.1 p1 makes module provision processor
dependent, but if IEEE_ARITHMETIC is provided it defines these S17.2 p3 public
constants; the frozen target provides IEEE_ARITHMETIC but omits IEEE_NEGATIVE_SUBNORMAL.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.2 fixture boundaries: this packet uses IEEE_ARITHMETIC only. IEEE_FEATURES_TYPE
and IEEE_FEATURES constants remain pending because 17.1 p1 makes IEEE_FEATURES provision
and contents processor dependent. Exception flag type, array, modes, and status facets
are gated by profiles that require the needed IEEE_SUPPORT_FLAG branches before the case
runs.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.2-004: IEEE_ARITHMETIC defines rounding constants and comparison operators for IEEE types

**Source:** S17.2 p3.round-constants, p3.comparison-operators; J3/24-007, 18 December 2023, physical PDF480. **Class:** Effect.

**Definition:** IEEE_ROUND_TYPE identifies the listed rounding modes, and simple elemental == and /=
compare values of IEEE_CLASS_TYPE and IEEE_ROUND_TYPE for sameness or difference.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-round-type-listed-rounding-constants`, `ieee-other-rounding-constant-defined`, `ieee-types-equality-operator-same-values`, `ieee-types-inequality-operator-different-values`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

S17.2-004 IEEE round constant/operator fixture: the program imports IEEE_NEAREST,
IEEE_TO_ZERO, IEEE_UP, IEEE_DOWN, IEEE_AWAY, IEEE_OTHER, and comparison operators from
IEEE_ARITHMETIC. It assigns the listed values to IEEE_ROUND_TYPE objects, checks
adjacent distinctness, checks IEEE_OTHER is a defined same-value constant, and uses
elemental == and /= on rank-one round-type arrays. The frozen target provides
IEEE_ARITHMETIC but omits IEEE_AWAY.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

IEEE 17.2 fixture boundaries: this packet uses IEEE_ARITHMETIC only. IEEE_FEATURES_TYPE
and IEEE_FEATURES constants remain pending because 17.1 p1 makes IEEE_FEATURES provision
and contents processor dependent. Exception flag type, array, modes, and status facets
are gated by profiles that require the needed IEEE_SUPPORT_FLAG branches before the case
runs.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

### S17.2-005: IEEE_FEATURES defines feature type and listed feature constants

**Source:** S17.2 p4; J3/24-007, 18 December 2023, physical PDF480. **Class:** Effect.

**Definition:** IEEE_FEATURES_TYPE expresses the need for particular 60559 features, and its only
possible values are the listed named constants of IEEE_FEATURES.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-features-type-listed-feature-constants`, `ieee-features-type-values-assignable`.

**Oracle:** Plans are pending and source-derived. IEEE effects are tested only under explicit
capability probes; the TRUE branch asserts the concrete flag, mode, type, constant, or
operation effect, and the FALSE branch is a skip rather than a pass. Named
FLAG-ROW-PREAMBLE(flag,x): under IEEE_SUPPORT_FLAG(flag,x), if
IEEE_SUPPORT_HALTING(flag) then CALL IEEE_SET_HALTING_MODE(flag,.FALSE.); else CALL
IEEE_GET_HALTING_MODE(flag,h) and proceed only if h is .FALSE. (otherwise skip); CALL
IEEE_SET_FLAG(IEEE_ALL,.FALSE.). Positive plans establish a non-default state first by
setting distinguished sentinel values and apply FLAG-ROW-PREAMBLE before any exception
operation or flag-state assertion unless the row explicitly says the preamble is not
applied because it would destroy the initial-state oracle.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Fixture authors must re-derive every pending plan from the pinned source. IEEE module
availability, feature-set inclusion, exact supported real kinds, initial modes, halting
mechanics, NaN payloads, the identity of a NaN result not fixed by the text,
signaling-NaN behavior outside a stated rule, and operations that may also signal are
treated as processor latitude.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 13, and 16 for source form,
type declarations, expressions, execution constructs, scoping units, I/O and format
contexts, intrinsic procedures, and intrinsic module USE mechanics. Unregistered
dependencies explicitly retained: later Clause 17 inquiry and mode procedures, IEEE
value constructors/classification procedures, exact operation descriptions, and feature
profiles such as tests/profiles/ieee_binary.f90.

<!-- END GENERATED 17.2 -->
