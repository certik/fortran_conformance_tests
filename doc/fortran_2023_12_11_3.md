# Fortran 2023 12.11.3: End-of-file condition and END= specifier

The canonical catalogue is `doc/catalogues/end_of_file_and_end_specifier_12_11_3.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.11.3 -->

### S12.11.3-001: Unhandled end-of-file initiates error termination

**Source:** S12.11.3, J3/24-007, 18 December 2023, physical PDF286. **Class:** Effect.

**Definition:** An EOF condition in an I/O statement containing neither END= nor IOSTAT= initiates error
termination.

**Diagnostic obligation:** not-required.

**Facets:** `eof-without-end-or-iostat-terminates`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, named intrinsic status values where the standard names them, branch-target
execution, or unchanged sentinel values where the source requires no definition.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific nonzero IOSTAT numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording, processor-dependent
numeric status values, undefined values, indeterminate file positions, and recursive I/O
program restrictions without a required diagnostic are never treated as exact oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
earlier clause 12 input/output concepts, file, unit, connection, positioning, and
data-transfer source already registered in this branch. Unregistered dependencies
explicitly retained: 12.5.6.x connection correction work; later clause 12 and clause 13
formatting details where pending plans use formatted input; clause 15 defined
input/output and child data transfer; clause 16 ISO_FORTRAN_ENV named constants and
inquiry intrinsics; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants,
images, IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.11.3-002: Handled end-of-file terminates input processing and defines status specifiers

**Source:** S12.11.3, J3/24-007, 18 December 2023, physical PDF286. **Class:** Effect.

**Definition:** When EOF occurs with END= or IOSTAT= present and no error occurs, input-list processing
terminates and IOSTAT= and IOMSG= specifiers that appear are defined as specified by
their owning rules.

**Diagnostic obligation:** not-required.

**Facets:** `handled-eof-input-processing-terminates`, `handled-eof-iostat-defined`, `handled-eof-iomsg-defined`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, named intrinsic status values where the standard names them, branch-target
execution, or unchanged sentinel values where the source requires no definition.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific nonzero IOSTAT numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording, processor-dependent
numeric status values, undefined values, indeterminate file positions, and recursive I/O
program restrictions without a required diagnostic are never treated as exact oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
earlier clause 12 input/output concepts, file, unit, connection, positioning, and
data-transfer source already registered in this branch. Unregistered dependencies
explicitly retained: 12.5.6.x connection correction work; later clause 12 and clause 13
formatting details where pending plans use formatted input; clause 15 defined
input/output and child data transfer; clause 16 ISO_FORTRAN_ENV named constants and
inquiry intrinsics; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants,
images, IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.11.3-003: Handled end-of-file makes transfer do-variables and input items undefined

**Source:** S12.11.3, J3/24-007, 18 December 2023, physical PDF286. **Class:** Effect.

**Definition:** For a data transfer statement or wait-operation EOF, do-variables in the initiating
statement become undefined; for input-initiated EOF, all effective input items or
namelist objects in the initiating statement become undefined.

**Diagnostic obligation:** not-required.

**Facets:** `handled-eof-do-variables-undefined`, `handled-eof-input-items-undefined`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, named intrinsic status values where the standard names them, branch-target
execution, or unchanged sentinel values where the source requires no definition.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific nonzero IOSTAT numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording, processor-dependent
numeric status values, undefined values, indeterminate file positions, and recursive I/O
program restrictions without a required diagnostic are never treated as exact oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
earlier clause 12 input/output concepts, file, unit, connection, positioning, and
data-transfer source already registered in this branch. Unregistered dependencies
explicitly retained: 12.5.6.x connection correction work; later clause 12 and clause 13
formatting details where pending plans use formatted input; clause 15 defined
input/output and child data transfer; clause 16 ISO_FORTRAN_ENV named constants and
inquiry intrinsics; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants,
images, IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.11.3-004: EOF on an external record file positions the file after the endfile record

**Source:** S12.11.3, J3/24-007, 18 December 2023, physical PDF286. **Class:** Effect.

**Definition:** If the input statement specifies an external record file, handled EOF positions the file
after the endfile record.

**Diagnostic obligation:** not-required.

**Facets:** `handled-eof-external-record-position-after-endfile`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, named intrinsic status values where the standard names them, branch-target
execution, or unchanged sentinel values where the source requires no definition.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific nonzero IOSTAT numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording, processor-dependent
numeric status values, undefined values, indeterminate file positions, and recursive I/O
program restrictions without a required diagnostic are never treated as exact oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
earlier clause 12 input/output concepts, file, unit, connection, positioning, and
data-transfer source already registered in this branch. Unregistered dependencies
explicitly retained: 12.5.6.x connection correction work; later clause 12 and clause 13
formatting details where pending plans use formatted input; clause 15 defined
input/output and child data transfer; clause 16 ISO_FORTRAN_ENV named constants and
inquiry intrinsics; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants,
images, IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.11.3-005: END= branches on handled end-of-file

**Source:** S12.11.3, J3/24-007, 18 December 2023, physical PDF286. **Class:** Effect.

**Definition:** If an END= specifier appears for handled EOF, execution branches to the statement with
the specified label.

**Diagnostic obligation:** not-required.

**Facets:** `handled-eof-end-branch`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, named intrinsic status values where the standard names them, branch-target
execution, or unchanged sentinel values where the source requires no definition.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific nonzero IOSTAT numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording, processor-dependent
numeric status values, undefined values, indeterminate file positions, and recursive I/O
program restrictions without a required diagnostic are never treated as exact oracles.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations;
earlier clause 12 input/output concepts, file, unit, connection, positioning, and
data-transfer source already registered in this branch. Unregistered dependencies
explicitly retained: 12.5.6.x connection correction work; later clause 12 and clause 13
formatting details where pending plans use formatted input; clause 15 defined
input/output and child data transfer; clause 16 ISO_FORTRAN_ENV named constants and
inquiry intrinsics; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants,
images, IEEE support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.11.3 -->
