# Fortran 2023 12.3.3.3: Direct access - access-position fixtures

The canonical catalogue is `doc/catalogues/direct_access_12_3_3_3.json`. This author packet adds finite scratch-file runtime fixtures without fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

**Catalogue source review: reviewed.** This topic represents 7 facets in 3 program(s); 7 of 16 facets are represented and 9 remain pending.

<!-- BEGIN GENERATED 12.3.3.3 -->

### S12.3.3.3-001: Direct access accesses external record-file records in arbitrary order

**Source:** S12.3.3.3, J3/24-007, 18 December 2023, physical PDF244. **Class:** Effect.

**Definition:** Direct access is a method of accessing records of an external record file in arbitrary
order.

**Diagnostic obligation:** not-required.

**Facets:** `direct-access-arbitrary-record-order`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`direct-access-arbitrary-record-order`. They create the external files in the run
directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited
to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.3-002: Direct records are identified by immutable positive record numbers and ordered by those numbers

**Source:** S12.3.3.3, J3/24-007, 18 December 2023, physical PDF244. **Class:** Effect.

**Definition:** Each direct-access record is uniquely identified by a positive integer record number
specified when written; once established, the record number cannot be changed, and
record order is record-number order.

**Diagnostic obligation:** not-required.

**Facets:** `direct-record-positive-number`, `direct-record-number-specified-on-write`, `direct-record-number-immutable`, `direct-record-order-number-order`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`direct-record-number-immutable`, `direct-record-number-specified-on-write`,
`direct-record-positive-number`. They create the external files in the run directory
with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited to exact
integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.3-003: Direct files have uniform record form and constrained endfile treatment

**Source:** S12.3.3.3, J3/24-007, 18 December 2023, physical PDF244. **Class:** Restriction.

**Definition:** Direct-file records are either all formatted or all unformatted; if sequential access is
also allowed, any endfile record is not part of the file while connected for direct
access; if sequential access is not allowed, the file shall not contain an endfile
record.

**Diagnostic obligation:** not-required.

**Facets:** `direct-records-all-formatted-or-all-unformatted`, `direct-endfile-ignored-when-sequential-also-allowed`, `direct-endfile-prohibited-without-sequential-access`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.3-004: Direct-file records are read or written only by direct access data transfer statements

**Source:** S12.3.3.3, J3/24-007, 18 December 2023, physical PDF244. **Class:** Restriction.

**Definition:** The records of a direct file shall be read or written only by direct access data
transfer statements.

**Diagnostic obligation:** not-required.

**Facets:** `direct-records-direct-transfer-only`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.3-005: Direct-file records all have the same length

**Source:** S12.3.3.3, J3/24-007, 18 December 2023, physical PDF244. **Class:** Effect.

**Definition:** All records of a direct-access file have the same length.

**Diagnostic obligation:** not-required.

**Facets:** `direct-records-same-length`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.3-006: Direct records may be read or written out of record-number order subject to write/read permission conditions

**Source:** S12.3.3.3, J3/24-007, 18 December 2023, physical PDF244. **Class:** Effect.

**Definition:** Direct records need not be read or written in record-number order; any record may be
written while connected; any written record may be read while connected if READ is
permitted for the connection.

**Diagnostic obligation:** not-required.

**Facets:** `direct-out-of-order-write-permitted`, `direct-gap-write-permitted`, `direct-written-record-read-permitted`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

Access-position fixture implementation: 1 valid run-phase scratch-file program(s) cover
`direct-gap-write-permitted`, `direct-out-of-order-write-permitted`,
`direct-written-record-read-permitted`. They create the external files in the run
directory with STATUS='SCRATCH' and also close with STATUS='DELETE'. Oracles are limited
to exact integer/character values, character lengths, IOSTAT zero, IOSTAT_END, and POS=
inequalities or values fixed by this source. No file name, external representation,
processor-specific IOSTAT number, IOMSG text, formatted-stream byte spacing, or
unsupported cross-access-method property is asserted.

**Oracle limitation:** Runtime fixture coverage is finite and does not imply fixture review. This packet
creates no Fortran fixture, compiler invocation, execution evidence, evidence link,
fixture approval, oracle approval, or coverage claim. Negative plans are source-control
plans unless this unit itself requires rejection and a future fixture can observe the
rejection portably. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.3.3.3-007: Direct files shall not use list-directed, namelist, or nonadvancing data transfer

**Source:** S12.3.3.3, J3/24-007, 18 December 2023, physical PDF244. **Class:** Restriction.

**Definition:** Direct-file records shall not be read or written using list-directed formatting,
namelist formatting, or a nonadvancing data transfer statement.

**Diagnostic obligation:** not-required.

**Facets:** `direct-list-directed-prohibited`, `direct-namelist-prohibited`, `direct-nonadvancing-prohibited`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.3.3.3 -->

## Access-position fixture derivations

### `direct_arbitrary_order` / `S12.3.3.3-001`

**Facets:** `direct-access-arbitrary-record-order`.

12.3.3.3 p1 defines direct access as arbitrary-order record access; records written 2 then 1 are read back by REC=1 and REC=2.

Feature mutations: `read-one-as-two`, `pre-read-1-left-remove-sentinel-init`, `pre-read-1-left-expected-sentinel-init`, `pre-read-1-left-default-sentinel-init`, `pre-read-2-right-remove-sentinel-init`, `pre-read-2-right-expected-sentinel-init`, `pre-read-2-right-default-sentinel-init`.

### `direct_record_numbers` / `S12.3.3.3-002`

**Facets:** `direct-record-positive-number`, `direct-record-number-specified-on-write`, `direct-record-number-immutable`.

12.3.3.3 p2 identifies each direct record by a positive REC= value fixed when written; rewriting REC=2 changes that record's value without renumbering it.

Feature mutations: `positive-read-other-written-record`, `specified-read-other-written-record`, `rewrite-other-record`, `pre-read-1-value-remove-sentinel-init`, `pre-read-1-value-expected-sentinel-init`, `pre-read-1-value-default-sentinel-init`, `pre-read-2-value-remove-sentinel-init`, `pre-read-2-value-expected-sentinel-init`, `pre-read-2-value-default-sentinel-init`, `pre-read-3-value-remove-sentinel-init`, `pre-read-3-value-expected-sentinel-init`, `pre-read-3-value-default-sentinel-init`.

### `direct_out_of_order_gap` / `S12.3.3.3-006`

**Facets:** `direct-out-of-order-write-permitted`, `direct-gap-write-permitted`, `direct-written-record-read-permitted`.

12.3.3.3 p2 permits direct writes and reads out of record-number order, including writing REC=3 before lower records; written records are read by REC=.

Feature mutations: `out-of-order-read-higher`, `gap-read-lower`, `written-read-lower`, `pre-read-1-value-remove-sentinel-init`, `pre-read-1-value-expected-sentinel-init`, `pre-read-1-value-default-sentinel-init`, `pre-read-2-value-remove-sentinel-init`, `pre-read-2-value-expected-sentinel-init`, `pre-read-2-value-default-sentinel-init`, `pre-read-3-value-remove-sentinel-init`, `pre-read-3-value-expected-sentinel-init`, `pre-read-3-value-default-sentinel-init`.

