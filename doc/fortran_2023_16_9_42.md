# Fortran 2023 16.9.42: BESSEL_Y0

The canonical catalogue is `doc/catalogues/bessel_y0_16_9_42.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 16.9.42 -->

### S16.9.42-001: BESSEL_Y0 identifies the Bessel function of the second kind, order zero

**Source:** S16.9.42, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** BESSEL_Y0 is the intrinsic for the Bessel function of the second kind and order zero.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-y0-second-kind-order-zero`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.42-002: BESSEL_Y0 is elemental

**Source:** S16.9.42, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** BESSEL_Y0 is an elemental function.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-y0-elemental-class`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.42-003: BESSEL_Y0 argument X shall be real and positive

**Source:** S16.9.42, J3/24-007, 18 December 2023, physical PDF391. **Class:** Restriction.

**Definition:** Argument X shall be of type real. Its value shall be greater than zero.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-y0-x-real`, `bessel-y0-x-positive`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.42-004: BESSEL_Y0 result characteristics match X

**Source:** S16.9.42, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** The result has the same type and kind as X.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-y0-result-same-as-x`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.42-005: BESSEL_Y0 result is an approximation to the order-zero second-kind Bessel value

**Source:** S16.9.42, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** The result value is an approximation to the Bessel function of the second kind and order
zero of X.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-y0-result-second-kind-order-zero-approximation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

### S16.9.42-006: BESSEL_Y0 approximation is processor dependent

**Source:** S16.9.42, J3/24-007, 18 December 2023, physical PDF391. **Class:** Effect.

**Definition:** The approximation chosen for the BESSEL_Y0 result is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `bessel-y0-processor-dependent-approximation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish non-default state first
with explicitly initialized real scalars or arrays, then observe only portable
properties: accepted intrinsic references, type and kind equality, rank and extent fixed
by result characteristics, and successful execution where an exact characteristic is
tested. Processor-dependent Bessel approximations, numerical tolerances, rounding
behavior, IEEE exception behavior, and approximate example values are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in intrinsic descriptions are unnumbered program
restrictions, so they are not diagnostic facets here. Approximate Bessel result values
have no portable finite oracle unless a later rule supplies an exact bound or value.

**Dependencies:** Registered dependencies: clauses 7, 8, 9, 10, 11, 12, and 13 where existing catalogues
cover declarations, types and kinds, expressions, arrays, execution, input/output, and
formatting used by future fixtures. Unregistered dependencies explicitly retained:
clause 15 procedure characteristics for elemental and transformational intrinsic
behavior; earlier clause 16 intrinsic-procedure general rules, numeric model
terminology, and any processor approximation requirements outside these sections;
clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images, IEEE
support, interoperability, or scope/lifetime controls are reached.

<!-- END GENERATED 16.9.42 -->
