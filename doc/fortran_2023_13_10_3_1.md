# Fortran 2023 13.10.3.1: List-directed input forms

The canonical catalogue is `doc/catalogues/list_directed_input_forms_13_10_3_1.json`. This source packet records source accounting. Selected list-directed input fixtures now supply executable standard-oracle observations and mutation plans; fixture approval remains separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`. Source-read receipt: `$SESS/receipts-batch139-source.json`.

Base units read and accounted: p1, p2, note1, p3, p4, p5, p6, p7, p8, p9, note2. No fine census units occur in the pinned inventory for this section.

Output-latitude note: list-directed and namelist output field widths, separator spelling/counts, record lengths, and record breaks are processor dependent unless a requirement below names exact characters.

<!-- BEGIN LIST-DIRECTED INPUT FIXTURES -->
## List-directed input runtime observations

Eighteen complete internal READ fixtures cover selected values, separators, input forms, null values and slash termination from 13.10.2, 13.10.3.1 and 13.10.3.2. Every receiving variable is initialized to a distinctive nonzero sentinel before READ; character fixtures assert LEN explicitly before equal-length value comparisons. The generator permanently records oracle, input-record, feature-level, READ-list and reverse sentinel mutations. These cases make no claim about list-directed output form.
<!-- END LIST-DIRECTED INPUT FIXTURES -->

<!-- BEGIN GENERATED 13.10.3.1 -->

### S13.10.3.1-001: List-directed input uses acceptable type forms without blank-as-zero semantics

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF308. **Class:** Restriction.

**Definition:** Type-appropriate edit-descriptor input forms are accepted except where modified;
unacceptable forms cause an error condition; blanks are never zeros and embedded blanks
are forbidden in constants except in character and specified complex constants.

**Diagnostic obligation:** not-required.

**Facets:** `edit-compatible-forms`, `unacceptable-form-error`, `blanks-not-zeros`, `embedded-blanks-limited`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

S13.10.3.1-001 list-directed type-form runtime controls: two complete positive-control
programs use exact type-appropriate forms. The edit-compatible fixture reads '42' into
an INTEGER and '.TRUE.' into a LOGICAL in separate internal READ controls, requiring 42
and true. The blanks-not-zeros fixture reads '1 2' into two integers and requires 1 then
2, which would fail if the blank were treated as a zero inside one integer constant.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

S13.10.3.1-001 list-directed type-form fixture boundary controls: These fixtures are
bounded internal list-directed input observations only. They do not assert list-directed
output spelling, field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error
IOSTAT mechanics, processor diagnostics, nondefault character kinds, enum input,
real/complex approximations, defined I/O, files, external units, coarrays, or universal
compiler conformance. Remaining facets stay pending with their source-authored plans.

S13.10.3.1-001 list-directed type-form runtime controls: These fixtures are bounded
internal list-directed input observations only. They do not assert list-directed output
spelling, field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error IOSTAT
mechanics, processor diagnostics, nondefault character kinds, enum input, real/complex
approximations, defined I/O, files, external units, coarrays, or universal compiler
conformance. Remaining facets stay pending with their source-authored plans.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-002: Repeated character input chooses undelimited character c for character effective items

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF308. **Class:** Effect.

**Definition:** For r*c, c is an undelimited character constant when the first corresponding effective
item is default, ASCII, or ISO 10646 character and the character after r* is nonblank
and not a quote; otherwise c is a literal constant.

**Diagnostic obligation:** not-required.

**Facets:** `repeat-undelimited-character`, `repeat-literal-otherwise`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

S13.10.3.1-002 r*c character-vs-literal runtime fixtures: two complete run/effect
programs use r*c records. '2*ab' is read into CHARACTER(LEN=2) array elements and
requires both exact values 'ab' with LEN 2 checks; '5,2*7,19' is read into four integers
and requires [5,7,7,19], proving that for integer effective items c is interpreted as a
literal constant rather than as character text. Distinct surrounding values make
repeat-count and ordering mutations fail.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

S13.10.3.1-002 r*c character-vs-literal fixture boundary fixtures: These fixtures are
bounded internal list-directed input observations only. They do not assert list-directed
output spelling, field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error
IOSTAT mechanics, processor diagnostics, nondefault character kinds, enum input,
real/complex approximations, defined I/O, files, external units, coarrays, or universal
compiler conformance. Remaining facets stay pending with their source-authored plans.

S13.10.3.1-002 r*c character-vs-literal fixture boundaries: These fixtures are bounded
internal list-directed input observations only. They do not assert list-directed output
spelling, field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error IOSTAT
mechanics, processor diagnostics, nondefault character kinds, enum input, real/complex
approximations, defined I/O, files, external units, coarrays, or universal compiler
conformance. Remaining facets stay pending with their source-authored plans.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-003: Integer and enum list-directed input is interpreted as suitable I editing

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF309. **Class:** Effect.

**Definition:** An integer or enum effective item receives a value as if an Iw edit descriptor with a
suitable width were used.

**Diagnostic obligation:** not-required.

**Facets:** `integer-i-editing`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-004: Real list-directed input is a numeric input field with implicit zero fractional digits

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF309. **Class:** Effect.

**Definition:** A real effective item uses a numeric input field suitable for F editing and assumes no
fractional digits unless a decimal symbol appears.

**Diagnostic obligation:** not-required.

**Facets:** `real-decimal-field`, `real-no-decimal-fraction-digits`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-005: Complex list-directed input is a parenthesized ordered pair of numeric fields

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF309. **Class:** Effect.

**Definition:** A complex effective item is read as a parenthesized real part and imaginary part
separated by comma in POINT mode or semicolon in COMMA mode; blanks and record ends may
surround the numeric fields, and a record end may occur after the real part or before
the imaginary part.

**Diagnostic obligation:** not-required.

**Facets:** `complex-point-comma`, `complex-comma-mode-semicolon`, `complex-real-imaginary-order`, `complex-record-end-allowances`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-006: Logical list-directed input excludes value separators from optional L-editing characters

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF309. **Class:** Restriction.

**Definition:** For logical effective items, optional L-editing characters shall not include value
separators.

**Diagnostic obligation:** not-required.

**Facets:** `logical-separator-excluded`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-007: Character list-directed input permits delimited continuation and preserves listed characters

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF309. **Class:** Effect.

**Definition:** A character effective item is read from a possibly delimited sequence of rep-chars of
the effective kind; delimited sequences may continue across records, but not between
doubled delimiters; record ends do not add characters; blank, comma, semicolon, and
slash may appear in default/ASCII/ISO 10646 sequences.

**Diagnostic obligation:** not-required.

**Facets:** `character-delimited-sequence`, `character-continuation-no-record-blank`, `doubled-delimiter-not-split`, `separator-characters-in-delimited-character`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

S13.10.3.1-007 delimited character runtime fixture: one complete run/effect program
reads record "'A,B/;'" into CHARACTER(LEN=5), explicitly checks LEN==5, and compares
against the equal-length literal 'A,B/;'. Thus comma, slash and semicolon are data
characters inside the delimited character sequence, not value separators.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

S13.10.3.1-007 delimited character fixture boundary fixture: These fixtures are bounded
internal list-directed input observations only. They do not assert list-directed output
spelling, field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error IOSTAT
mechanics, processor diagnostics, nondefault character kinds, enum input, real/complex
approximations, defined I/O, files, external units, coarrays, or universal compiler
conformance. Remaining facets stay pending with their source-authored plans.

S13.10.3.1-007 delimited character fixture boundaries: These fixtures are bounded
internal list-directed input observations only. They do not assert list-directed output
spelling, field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error IOSTAT
mechanics, processor diagnostics, nondefault character kinds, enum input, real/complex
approximations, defined I/O, files, external units, coarrays, or universal compiler
conformance. Remaining facets stay pending with their source-authored plans.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-008: Default-family character input may be undelimited only under listed conditions

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF309. **Class:** Effect.

**Definition:** For default, ASCII, or ISO 10646 character items meeting all listed conditions,
delimiters are not required; an undelimited sequence terminates at the first applicable
blank, comma/semicolon, slash, or record end, and embedded quotes are not doubled.

**Diagnostic obligation:** not-required.

**Facets:** `undelimited-character-admitted`, `undelimited-termination`, `undelimited-quotes-not-doubled`, `undelimited-repeat-prefix-excluded`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

### S13.10.3.1-009: Character list-directed input truncates or blank-pads to the effective item length

**Source:** S13.10.3.1, J3/24-007, 18 December 2023, physical PDF309. **Class:** Effect.

**Definition:** If the effective character length is at most the sequence length the leftmost len
characters are transmitted; otherwise the sequence fills the leftmost w characters and
the rest are blanks.

**Diagnostic obligation:** not-required.

**Facets:** `character-truncation`, `character-blank-padding`.

**Oracle:** Plans are pending and source-derived. Positive plans use internal READ or internal WRITE
where possible; input plans name exact record text and resulting values, and output
plans name exact characters when the source fixes them. Processor-dependent
list-directed or namelist output spelling, field widths, separator choice, record
length, and record breaks are explicitly left unasserted. No fixture, compiler
invocation, execution evidence, evidence link, fixture approval, oracle approval, or
coverage claim is created by this packet.

S13.10.3.1-009 character padding runtime fixture: one complete run/effect program reads
record "'AB'" into CHARACTER(LEN=4), explicitly checks LEN==4, and compares against the
equal-length literal 'AB  ', deriving the two trailing blanks from len-w = 2.

**Oracle limitation:** Source accounting only. Unnumbered prose restrictions are not promoted to
numbered-constraint diagnostics. A future diagnostic facet is allowed only if a later
owner identifies a numbered syntax rule or constraint, a required rejection, and a
single-image portable observation. Clause 12 I/O statement mechanics remain unregistered
and are treated as dependencies, not as evidence owned by this packet.

S13.10.3.1-009 character padding fixture boundary fixture: These fixtures are bounded
internal list-directed input observations only. They do not assert list-directed output
spelling, field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error IOSTAT
mechanics, processor diagnostics, nondefault character kinds, enum input, real/complex
approximations, defined I/O, files, external units, coarrays, or universal compiler
conformance. Remaining facets stay pending with their source-authored plans.

S13.10.3.1-009 character padding fixture boundaries: These fixtures are bounded internal
list-directed input observations only. They do not assert list-directed output spelling,
field widths, record wrapping, DECIMAL='COMMA', DELIM=, input-error IOSTAT mechanics,
processor diagnostics, nondefault character kinds, enum input, real/complex
approximations, defined I/O, files, external units, coarrays, or universal compiler
conformance. Remaining facets stay pending with their source-authored plans.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
designators, character assignment, arrays, and execution constructs; 13.1-13.4 for
format concepts already registered by batch136. Unregistered dependencies explicitly
retained: 13.5-13.9 parallel sibling material for decimal/delim modes and edit
descriptors; clause 12 for data transfer, internal files, effective items, defined I/O,
DELIM=, DECIMAL=, NML=, stream and record structure; clause 15 for defined I/O; clause
16 if future fixtures use intrinsics; clauses 3, 5, 14, 17, and 19 where terminology,
kind, processor-dependency, coarray/image, or scope rules are reached.

<!-- END GENERATED 13.10.3.1 -->
