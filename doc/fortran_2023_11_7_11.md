# Fortran 2023: 11.7.11 STAT= and ERRMSG= specifiers in image control statements

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.7.11 -->

### S11.7.11-001: STAT= and ERRMSG= variables in image control statements are independent of each other and image-control variables

**Source:** 11.7.11 p1; J3/24-007 PDF p239. **Class:** Restriction.

**Definition:** In an image control statement, STAT= variables shall not depend on ERRMSG=, event, lock,
team, or ACQUIRED_LOCK= variables; ERRMSG= variables shall not depend on STAT=, event,
lock, team, or ACQUIRED_LOCK= variables.

**Diagnostic obligation:** not-required.

**Facets:** `sync-stat-image-control-independence-source-control`, `stat-errmsg-dependence-source-control`, `stat-event-variable-dependence-source-control`, `stat-lock-variable-dependence-source-control`, `stat-team-variable-dependence-source-control`, `stat-acquired-lock-dependence-source-control`, `errmsg-stat-dependence-source-control`, `errmsg-event-variable-dependence-source-control`, `errmsg-lock-variable-dependence-source-control`, `errmsg-team-variable-dependence-source-control`, `errmsg-acquired-lock-dependence-source-control`.

**Oracle:** The p1 compound shall sentences decompose into ten dependency prohibitions, all recorded
as source-control/program-obligation facets only. No conforming processor diagnostic is
required, and no rejection facet is claimed.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.7-11.7.10 in this packet;19 association/dependence terminology NOT YET REGISTERED.

### S11.7.11-002: STAT= is zero after successful image control execution

**Source:** 11.7.11 p2; J3/24-007 PDF p239. **Class:** Effect.

**Definition:** If STAT= appears in sync-stat in an image control statement, the stat-variable is
assigned zero when execution succeeds.

**Diagnostic obligation:** not-required.

**Facets:** `stat-zero-on-success-source-control`.

**Oracle:** The value zero is portable on success, but this packet supplies no execution fixture and
no synchronization oracle.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** All image control statement forms in 11.7;11.1.6 p4 delegation discharged here.

### S11.7.11-003: EVENT WAIT and SYNC MEMORY nonfailed errors assign processor-dependent positive STAT= distinct from failed/stopped constants

**Source:** 11.7.11 p3; J3/24-007 PDF p239. **Class:** Effect.

**Definition:** For EVENT WAIT or SYNC MEMORY with STAT=, an error condition assigns a
processor-dependent positive value distinct from STAT_FAILED_IMAGE and
STAT_STOPPED_IMAGE.

**Diagnostic obligation:** context-dependent.

**Facets:** `event-wait-sync-memory-other-error-stat-positive`, `event-wait-sync-memory-other-error-not-failed-stopped`.

**Oracle:** The standard names the class, not the integer value. Resource exhaustion, invalid
images, and communication failures are not manufactured as portable errors.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.8 EVENT WAIT in this packet;11.7.5 SYNC MEMORY UNREGISTERED (parallel);16
STAT_FAILED_IMAGE/STAT_STOPPED_IMAGE NOT YET REGISTERED.

### S11.7.11-004: Image control statements define their involved images

**Source:** 11.7.11 p4; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** The involved images are the current team for END TEAM, FORM TEAM, and SYNC ALL; the
specified team for CHANGE TEAM and SYNC TEAM; the specified images plus the executing
image for SYNC IMAGES; and the event-variable image plus the executing image for EVENT
POST.

**Diagnostic obligation:** not-required.

**Facets:** `current-team-involved-images-definition`, `specified-team-involved-images-definition`, `sync-images-involved-images-definition`, `event-post-involved-images-definition`.

**Oracle:** This paragraph defines the image set used by p5 status rules. It is not itself a
multi-image executable oracle.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.1.5 CHANGE TEAM registered;11.7.3/11.7.4/11.7.6 UNREGISTERED (parallel);11.7.7/11.7.9
in this packet.

### S11.7.11-005: STAT= for team, event, and sync statements names stopped/failed-image values and processor-dependent other errors

**Source:** 11.7.11 p5; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** For CHANGE TEAM, END TEAM, EVENT POST, FORM TEAM, SYNC ALL, SYNC IMAGES, or SYNC TEAM
with STAT=, a stopped involved image assigns STAT_STOPPED_IMAGE; otherwise a failed
involved image with no other error performs the intended action on active images
involved and assigns STAT_FAILED_IMAGE; otherwise any other error assigns a
processor-dependent positive value distinct from both.

**Diagnostic obligation:** context-dependent.

**Facets:** `stopped-image-stat-source-control`, `failed-image-stat-source-control`, `failed-image-active-intended-action-source-control`, `other-error-stat-positive-not-stopped-failed`.

**Oracle:** Stopped/failed image values are named but not portably inducible here. The failed-image
branch also has a separate active-image intended-action obligation, recorded only as
source-control. Other errors are processor-dependent positive values with only the
stated distinctions portable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.1.5 CHANGE TEAM registered;11.7.3/11.7.4/11.7.6 UNREGISTERED (parallel);11.7.7/11.7.9
in this packet;16 STAT constants NOT YET REGISTERED.

### S11.7.11-006: Stopped-image STAT= for sync statements has SYNC MEMORY effect except for STAT=

**Source:** 11.7.11 p6; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** For SYNC ALL, SYNC IMAGES, or SYNC TEAM with STAT= and STAT_STOPPED_IMAGE, the effect is
the same as executing SYNC MEMORY except for defining STAT=.

**Diagnostic obligation:** not-required.

**Facets:** `stopped-sync-memory-effect-source-control`.

**Oracle:** This stopped-image effect is not executable in the current suite because stopped images
cannot be portably manufactured.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.5 SYNC MEMORY and 11.7.3/11.7.4/11.7.6 sync statements UNREGISTERED (parallel);16
STAT_STOPPED_IMAGE NOT YET REGISTERED.

### S11.7.11-007: LOCK STAT= names failed-image, already-locked, and unlocked-by-failed-image statuses

**Source:** 11.7.11 p7; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** For LOCK with STAT=, if the lock-variable image has failed STAT_FAILED_IMAGE is
assigned; otherwise if the lock is locked by the executing image STAT_LOCKED is
assigned; otherwise if it is unlocked because the locking image failed
STAT_UNLOCKED_FAILED_IMAGE is assigned.

**Diagnostic obligation:** context-dependent.

**Facets:** `lock-stat-failed-image-source-control`, `lock-stat-locked-by-self`, `lock-stat-unlocked-failed-image-source-control`.

**Oracle:** STAT_LOCKED is a named local error value. The failed-image statuses are named but
unimplementable as executable oracles here.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.10 p7 in this packet;16 STAT_FAILED_IMAGE/STAT_LOCKED/STAT_UNLOCKED_FAILED_IMAGE
and LOCK_TYPE NOT YET REGISTERED.

### S11.7.11-008: UNLOCK STAT= names failed-image, unlocked, and locked-by-other-image statuses

**Source:** 11.7.11 p8; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** For UNLOCK with STAT=, if the lock-variable image has failed STAT_FAILED_IMAGE is
assigned; otherwise if the lock is unlocked STAT_UNLOCKED is assigned; otherwise if it
is locked by a different image STAT_LOCKED_OTHER_IMAGE is assigned.

**Diagnostic obligation:** context-dependent.

**Facets:** `unlock-stat-failed-image-source-control`, `unlock-stat-unlocked`, `unlock-stat-locked-other-image-source-control`.

**Oracle:** STAT_UNLOCKED is a named local error value. The failed/other-image statuses require
unportable image states.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.10 p7 in this packet;16 STAT_FAILED_IMAGE/STAT_UNLOCKED/STAT_LOCKED_OTHER_IMAGE and
LOCK_TYPE NOT YET REGISTERED.

### S11.7.11-009: LOCK/UNLOCK other errors assign processor-dependent positive STAT= distinct from lock status constants

**Source:** 11.7.11 p9; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** For LOCK or UNLOCK with STAT=, any other error assigns a processor-dependent positive
value distinct from STAT_LOCKED, STAT_LOCKED_OTHER_IMAGE, STAT_UNLOCKED, and
STAT_UNLOCKED_FAILED_IMAGE.

**Diagnostic obligation:** context-dependent.

**Facets:** `lock-unlock-other-error-positive-distinct`.

**Oracle:** The integer value for other errors is processor dependent. The only portable assertions
are positivity and distinction from the named lock status constants.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.10 in this packet;16 STAT lock constants NOT YET REGISTERED.

### S11.7.11-010: CRITICAL STAT= reports failed previous image or processor-dependent other errors

**Source:** 11.7.11 p10; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** For CRITICAL with STAT=, if the previous image to enter failed while executing it,
STAT_FAILED_IMAGE is assigned and execution of the construct continues normally; any
other error assigns a processor-dependent positive value other than STAT_FAILED_IMAGE.

**Diagnostic obligation:** context-dependent.

**Facets:** `critical-previous-failed-stat-source-control`, `critical-continues-normally-after-previous-failure-source-control`, `critical-other-error-positive-not-failed`.

**Oracle:** The failed-previous-image branch has two obligations: assign STAT_FAILED_IMAGE and
continue CRITICAL execution normally. Both are source-control here because the
failed-image premise cannot be portably induced. Other errors are processor dependent
except for positivity and distinction from STAT_FAILED_IMAGE.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.1.6 CRITICAL registered;16 STAT_FAILED_IMAGE NOT YET REGISTERED.

### S11.7.11-011: Image control errors without STAT= initiate error termination

**Source:** 11.7.11 p11; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** If an image control statement has an error condition and no STAT= specifier in
sync-stat, error termination is initiated.

**Diagnostic obligation:** context-dependent.

**Facets:** `error-without-stat-termination-source-control`.

**Oracle:** The rule owns the no-STAT error-termination route, but this packet does not manufacture
nonportable error conditions.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7 image control statements;5.3.6 termination NOT YET REGISTERED.

### S11.7.11-012: ERRMSG= is assigned an explanatory message on error and is unchanged on success

**Source:** 11.7.11 p12; J3/24-007 PDF p240. **Class:** Effect.

**Definition:** If ERRMSG= appears and an error condition occurs, the errmsg-variable is assigned an
explanatory message as if by intrinsic assignment; if no such condition occurs, its
definition status and value are unchanged.

**Diagnostic obligation:** context-dependent.

**Facets:** `errmsg-error-message-source-control`, `errmsg-success-unchanged-source-control`.

**Oracle:** ERRMSG text content is unspecified. On error, only assignment occurrence is specified;
on success, unchanged status/value may be locally observable with a carefully
length-pinned character variable after dependencies are registered.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 10.2 intrinsic assignment registered;character comparison blank-padding precedent from
batch096;11.7 image control statements.

### S11.7.11-013: Image-control error condition sets are processor dependent

**Source:** 11.7.11 p13; J3/24-007 PDF p241. **Class:** Effect.

**Definition:** The set of error conditions that can occur in an image control statement is processor
dependent.

**Diagnostic obligation:** not-required.

**Facets:** `image-control-error-set-processor-dependent`.

**Oracle:** This is explicit processor latitude; it cannot support a diagnostic or executable oracle
for a particular error set.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** All 11.7 image control statements;16 STAT constants NOT YET REGISTERED.

<!-- END GENERATED 11.7.11 -->
