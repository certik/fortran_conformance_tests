# Fortran 2023 18.5.5.8: CFI_select_part

The canonical catalogue is `doc/catalogues/cfi_select_part_18_5_5_8.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted are listed in the catalogue. Fine census units are recorded there for mixed or compound source units.

<!-- BEGIN GENERATED 18.5.5.8 -->

### S18.5.5.8-001: CFI_select_part has the ISO_Fortran_binding.h part-selection interface

**Source:** S18.5.5.8, J3/24-007, 18 December 2023, physical PDF541. **Class:** Effect.

**Definition:** CFI_select_part has the interface int CFI_select_part(CFI_cdesc_t *result, const
CFI_cdesc_t *source, size_t displacement, size_t elem_len) and updates a descriptor for
a section whose elements are parts of source elements.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_select_part-interface`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.8-002: CFI_select_part actual arguments must satisfy result, source, displacement, alignment, and element-length restrictions

**Source:** S18.5.5.8, J3/24-007, 18 December 2023, physical PDF541. **Class:** Restriction.

**Definition:** The result descriptor rank, attribute, and type, source descriptor state, displacement
bounds and alignment, and character element length shall satisfy the stated
restrictions.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_select_part-result-descriptor-restrictions`, `CFI_select_part-source-descriptor-restrictions`, `CFI_select_part-displacement-bounds-and-alignment`, `CFI_select_part-character-elem-len`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.8-003: Successful CFI_select_part describes component, substring, or complex-part arrays

**Source:** S18.5.5.8, J3/24-007, 18 December 2023, physical PDF541. **Class:** Effect.

**Definition:** Successful CFI_select_part updates result base_addr, dim, and elem_len to describe an
array section whose element is the selected component, substring, or real or imaginary
part of the corresponding source element.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_select_part-updates-base-dim-elem-len`, `CFI_select_part-component-substring-complex-part`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.8-004: Detected CFI_select_part errors preserve the result descriptor

**Source:** S18.5.5.8, J3/24-007, 18 December 2023, physical PDF541. **Class:** Effect.

**Definition:** After a detected CFI_select_part error, the result descriptor is not modified.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_select_part-detected-error-preserves-result`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.8-005: CFI_select_part returns an error indicator

**Source:** S18.5.5.8, J3/24-007, 18 December 2023, physical PDF541. **Class:** Effect.

**Definition:** The result of CFI_select_part is an error indicator.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_select_part-result-error-indicator`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

<!-- END GENERATED 18.5.5.8 -->
