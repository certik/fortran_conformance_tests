# Fortran 2023 16.9.66: COUNT

The canonical catalogue is `doc/catalogues/count_16_9_66.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or dense source units are split.

<!-- BEGIN INTRINSICS 16.9.F FIXTURES -->

## Intrinsics 16.9.F runtime observations

This batch adds 5 generated f2023 runtime fixtures for 16.9.66, covering 14 portable pending facets with exact integer, logical, kind, shape/rank, or exactly representable zero-value oracles. Feature mutations are conforming source substitutions that must compile and fail at run time on both toolchains. Approximate COS/COSD/COSH/COSPI values and nonzero command-argument support remain pending where the standard provides no portable oracle.
<!-- END INTRINSICS 16.9.F FIXTURES -->

<!-- BEGIN GENERATED 16.9.66 -->

### S16.9.66-001: COUNT reduces a logical array by counting true values

**Source:** S16.9.66, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** COUNT reduces an array by counting elements whose logical value is true.

**Diagnostic obligation:** not-required.

**Facets:** `count-true-value-reduction-description`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.66-001 intrinsics_16_9_f fixture: The COUNT description is bound through an exact
true-value reduction: COUNT([T,F,T]) is required to be 2, not the array size.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only. Reader note: 16.9.66#p6 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; it was visually verified against physical PDF400, the census hash is
retained, and no requirement or oracle is derived from that example.

S16.9.66-001 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.66-002: COUNT is transformational

**Source:** S16.9.66, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** COUNT is classified as a transformational function.

**Diagnostic obligation:** not-required.

**Facets:** `count-transformational-class`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.66-002 intrinsics_16_9_f fixture: The transformational class is observed through
DIM rank reduction: COUNT on a rank-two mask with DIM has rank one while COUNT without
DIM is scalar.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only. Reader note: 16.9.66#p6 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; it was visually verified against physical PDF400, the census hash is
retained, and no requirement or oracle is derived from that example.

S16.9.66-002 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.66-003: COUNT arguments satisfy MASK, DIM, and KIND restrictions

**Source:** S16.9.66, J3/24-007, 18 December 2023, physical PDF400. **Class:** Restriction.

**Definition:** MASK shall be a logical array; optional DIM shall be an integer scalar in range 1
through the rank n of MASK and its corresponding actual argument shall not be an
optional dummy argument, disassociated pointer, or unallocated allocatable; optional
KIND shall be a scalar integer constant expression.

**Diagnostic obligation:** not-required.

**Facets:** `count-mask-logical-array`, `count-dim-integer-scalar-in-range`, `count-dim-actual-present-associated-allocated`, `count-kind-scalar-integer-constant`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.66-003 intrinsics_16_9_f fixture: COUNT argument controls use logical masks,
scalar DIM variables in range including an associated pointer actual, and a scalar
integer constant KIND; exact counts and direct KIND inquiry make each argument feature
load-bearing.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only. Reader note: 16.9.66#p6 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; it was visually verified against physical PDF400, the census hash is
retained, and no requirement or oracle is derived from that example.

S16.9.66-003 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.66-004: COUNT result has specified integer kind, rank, and shape

**Source:** S16.9.66, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** The COUNT result is integer; KIND selects the kind type parameter when present,
otherwise default integer is used; the result is scalar if DIM is absent or n equals
one, otherwise rank n-1 with the DIM extent removed from MASK's shape.

**Diagnostic obligation:** not-required.

**Facets:** `count-result-integer`, `count-result-kind-selected`, `count-result-kind-default`, `count-result-scalar-without-dim-or-rank-one`, `count-result-rank-and-shape-with-dim`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.66-004 intrinsics_16_9_f fixture: COUNT result characteristics are observed with
integer assignment, direct KIND(COUNT(...)) inquiries for selected and default kind,
direct RANK(COUNT(...)) for scalar results, and direct rank/size plus exact DIM values
for rank-reduced results.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only. Reader note: 16.9.66#p6 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; it was visually verified against physical PDF400, the census hash is
retained, and no requirement or oracle is derived from that example.

S16.9.66-004 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.66-005: COUNT result values equal true-element counts

**Source:** S16.9.66, J3/24-007, 18 December 2023, physical PDF400. **Class:** Effect.

**Definition:** With DIM absent or rank-one MASK, the value is the number of true elements or zero for
size zero; with DIM present and rank greater than one, each result element is the number
of true elements along the selected dimension section.

**Diagnostic obligation:** not-required.

**Facets:** `count-rank-one-true-elements`, `count-size-zero-mask-zero`, `count-dim-present-section-counts`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.66-005 intrinsics_16_9_f fixture: COUNT value cases use exact logical masks:
rank-one COUNT has two true elements, a zero-size mask counts zero with a nonzero
companion guard, and DIM section counts are [1,2,0] and [2,1].

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Argument shall-restrictions in these intrinsic descriptions are program restrictions and
carry no diagnostic facet because no numbered rule in this range requires detection.
Example paragraphs and notes are informative only. Reader note: 16.9.66#p6 is an
informative typeset-matrix example affected by the known census-hash-drift-math-glyphs
reader issue; it was visually verified against physical PDF400, the census hash is
retained, and no requirement or oracle is derived from that example.

S16.9.66-005 intrinsics_16_9_f boundaries: This packet supplies only the listed
single-image f2023 positive-control or effect facets. It does not assert diagnostics for
unnumbered Clause 16 restrictions, approximate transcendental values,
processor-dependent command-argument support or command-name concepts, source-review
renewal, evidence-link approval, or unrelated pending facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

<!-- END GENERATED 16.9.66 -->
