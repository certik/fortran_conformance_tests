# Fortran 2023 16.9.71: DIGITS

The canonical catalogue is `doc/catalogues/digits_16_9_71.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or dense source units are split.

<!-- BEGIN INTRINSICS 16.9 H FIXTURES -->
## Intrinsics 16.9 H runtime observations

The `intrinsics_16_9_h` generator supplies 6 requirement bindings covering 8 portable facets in this section. Oracles use direct result inquiries, same-model DIGITS relationships, exact integer/logical/character/small-real results, and DSHIFT BTEST observations. DPROD processor-dependent approximation latitude and SHOULD guidance remain pending.
<!-- END INTRINSICS 16.9 H FIXTURES -->

<!-- BEGIN GENERATED 16.9.71 -->

### S16.9.71-001: DIGITS queries significant digits in the numeric model

**Source:** S16.9.71, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** DIGITS returns significant digits in the numeric model.

**Diagnostic obligation:** not-required.

**Facets:** `digits-significant-digits-description`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.71-001 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Argument shall-restrictions in
these intrinsic descriptions are program restrictions and carry no diagnostic facet
because no numbered rule in this range requires detection. Example paragraphs and notes
are informative only.

S16.9.71-001 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.71-002: DIGITS is an inquiry function

**Source:** S16.9.71, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** DIGITS is classified as an inquiry function.

**Diagnostic obligation:** not-required.

**Facets:** `digits-inquiry-class`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.71-002 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Argument shall-restrictions in
these intrinsic descriptions are program restrictions and carry no diagnostic facet
because no numbered rule in this range requires detection. Example paragraphs and notes
are informative only.

S16.9.71-002 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.71-003: DIGITS argument X is integer or real

**Source:** S16.9.71, J3/24-007, 18 December 2023, physical PDF403. **Class:** Restriction.

**Definition:** X shall be of type integer or real.

**Diagnostic obligation:** not-required.

**Facets:** `digits-x-integer-or-real`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.71-003 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Argument shall-restrictions in
these intrinsic descriptions are program restrictions and carry no diagnostic facet
because no numbered rule in this range requires detection. Example paragraphs and notes
are informative only.

S16.9.71-003 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.71-006: DIGITS permits scalar or array X

**Source:** S16.9.71, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** The X argument may be a scalar or an array.

**Diagnostic obligation:** not-required.

**Facets:** `digits-x-scalar-or-array`.

**Oracle:** Pending plan is source-derived from the admission that X may be scalar or array; it
observes only the scalar default-integer inquiry result required for the same type and
kind.

S16.9.71-006 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Argument shall-restrictions in
these intrinsic descriptions are program restrictions and carry no diagnostic facet
because no numbered rule in this range requires detection. Example paragraphs and notes
are informative only.

S16.9.71-006 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.71-004: DIGITS result is a default integer scalar

**Source:** S16.9.71, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** The DIGITS result is a default integer scalar.

**Diagnostic obligation:** not-required.

**Facets:** `digits-result-default-integer-scalar`.

**Oracle:** Pending plans are source-derived and preserve the distinction between exact required
effects and processor latitude. Future executable fixtures must use non-default
sentinels where state is observed and must mutate the intrinsic, argument order, DIM,
KIND, or companion expression so the feature is load-bearing.

S16.9.71-004 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Argument shall-restrictions in
these intrinsic descriptions are program restrictions and carry no diagnostic facet
because no numbered rule in this range requires detection. Example paragraphs and notes
are informative only.

S16.9.71-004 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

### S16.9.71-005: DIGITS result value is q for integer X and p for real X

**Source:** S16.9.71, J3/24-007, 18 December 2023, physical PDF403. **Class:** Effect.

**Definition:** The DIGITS result has value q for integer X and p for real X, where q and p are defined
in 16.4 for the model representing numbers of the same type and kind as X.

**Diagnostic obligation:** not-required.

**Facets:** `digits-integer-model-q`, `digits-real-model-p`, `digits-same-kind-same-model-value`.

**Oracle:** DIGITS returns exact model parameters for the executing processor, but most numeric
values are processor dependent across kinds. Pending plans therefore assert model
consistency and source-mandated kind/rank behavior, not a universal precision for every
processor.

S16.9.71-005 intrinsics_16_9_h runtime fixture: Generated positive runtime fixtures
check only source-stated exact properties: direct kind/len/shape inquiries on intrinsic
expressions, same-model DIGITS relationships without profile-specific q or p literals,
exact integer/logical/character/small-real values, and DSHIFT bit positions through
BTEST.

**Oracle limitation:** Original source accounting created no Fortran fixture, compiler invocation, execution
evidence, evidence link, fixture approval, oracle approval, or coverage claim; this
intrinsics_16_9_h generator supplies selected runtime fixtures and mutation plans
without granting those approvals or universal coverage. Argument shall-restrictions in
these intrinsic descriptions are program restrictions and carry no diagnostic facet
because no numbered rule in this range requires detection. Example paragraphs and notes
are informative only.

S16.9.71-005 intrinsics_16_9_h fixture boundaries: Only the listed portable facets are
discharged. Unnumbered restrictions are represented by conforming positive controls, not
diagnostic claims; processor-dependent DPROD approximation latitude and nonbinding
SHOULD guidance remain pending.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where future fixtures
use source form, expressions, declarations, arrays, I/O, or exact internal checks.
Unregistered dependencies retained: 16.4 numeric models and later clause 16 intrinsic
descriptions such as COS, REAL, KIND, HUGE, RADIX, RANGE, and BIT_SIZE where a pending
plan cross-checks model or conversion properties.

<!-- END GENERATED 16.9.71 -->
