# Fortran 2023: 7.5.5 Type-bound procedures — phase-2 implementation

**Catalogue source review: reviewed.** Case and evidence adjudications are maintained in their separate content-bound records.

The finite packet has **151 cases**: **57 diagnostic inputs**, **85 compile-only positive controls/admissions**, and **9 runtime effect cases**. Of **156 facets**, **90 are directly represented** and **66 remain PENDING**. Representation is not a claim of compiler success, independent review or source closure.

## Source, interpretation and observation boundaries

Source authority is J3/24-007, **18 December 2023**, **688 PDF pages**, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The section starts at physical PDF99 (printed85), continues through PDF100-101
and NOTE3's continuation on PDF102, and ends before the actual 7.5.6 heading.
The 39 base units, 145 fine units, 33 requirement IDs, definitions, classifications
and reciprocal accounting remain those of the source packet.

The author source commit `50be5268940cc2a56523922c331a89bc72fae16c` and coordinator
corrections `6d988a3a6da8f26ca9d589f48a8eea2aaf4e5d1a` and
`e8eb0932bb80d01192e9fac13ff0e171556104c5` are preserved. The coordinator's
original-source review is retained in the session artifact
`type-bound-coordinator-source-review.json`; the integrated source, fixture and
correction decisions are recorded in `doc/source_audits/batch_013.json`.
Eligibility is not fixture approval. This generator neither self-approves
sources/cases nor changes baselines.

### P1-P6 retained premises

* **P1 — live type/rank/state.** Runtime objects are ordinary scalar noncoarray,
  nonpointer and nonallocatable. No abstract object is instantiated. Deferred
  interfaces are implemented by concrete children before invocation; CLASS(base)
  helpers receive live concrete child actuals. There are no unallocated receiver,
  dangling-pointer or whole abstract-parent references.
* **P2 — independent defined inputs/results.** Named setup routines or explicit
  component assignments define every read payload. No structure-constructor
  ordering/default initialization is used to make an oracle cancel its own
  setup error. Calls are evaluated separately; every runtime result is compared
  with an independent integer literal, not only with a second binding's result.
  Results and markers are defined and stay in scope; no evaluation-order or
  short-circuit assumption is used.
* **P3 — actual interfaces and PASS.** Concrete targets are eligible accessible
  module procedures or explicitly interfaced externals. Abstract interface bodies
  import required already-defined types. Default/bare PASS selects the first
  dummy; named PASS selects an existing eligible dummy; NOPASS has no implicit
  object argument. C765's scalar/nonpointer/nonallocatable/same-declared-type,
  assumed-length, polymorphic-iff-extensible and no-VALUE conditions remain.
  Nonpassed actual type/kind/rank and keyword correspondence also remain valid.
* **P4 — inheritance and generic resolution.** Overriding fixtures preserve
  passed-object name/position, all other dummy/result characteristics, PURE/SIMPLE/
  ELEMENTAL relations and public accessibility except the tested C789/C790
  attribute. Named generics use distinguishable nonpassed default INTEGER/REAL
  dummies; no physical kind numbers or dynamic-only overload lookup. Inherited
  private bindings remain inherited, not public and not replaced by a homonym.
* **P5 — private contexts and evidence roles.** Private representations are
  accessed only in the defining module/descendants. Public callers receive values
  through public bindings on accessible objects, even when the type name or
  implementation is private. Named outside-call contrasts have actual PRIVATE
  attribute or bare binding PRIVATE repairs. Internal wrappers are allowed-use
  controls, not effects proving outside inaccessibility. The descendant control
  has a real ancestor separate-module-procedure declaration.
* **P6 — I/O/final/representation limits.** C779's six nameless-generic pairs
  preserve valid operator/assignment/four defined-I/O interfaces while changing
  only NOPASS. Those I/O procedures have correct scalar CLASS dtv, default-kind
  arguments, iotype/v_list only in formatted forms, and the required intents.
  Their bodies assign iostat=0 and do not change iomsg or execute I/O.
  No I/O transfer, finalization, representation or undefined-pointer inquiry is
  implemented for a canonical wrapper graph.

## Reporting, repair and review limits

Numbered grammar/constraint reporting uses 4.2 p2(3).
Named private-binding access under S7.5.5-007 uses **19.3.4 p5 and 4.2 p2(6)**.
That duty is not weakened by the separate old component metadata correction.
Reporting capability is not a requirement for fatal rejection, printed rule
codes or exact prose. These compile manifests expect `diagnose`, allowing a
proper located nonfatal report. No new LFortran-only policy is introduced.

Every negative has one exact source-minimal conforming repair. Diagnostics
bind the intended failing build step, file/span and cause; a provider failure
cannot satisfy an intended client diagnostic. Wrong-role/context messages,
source echo, unsupported language facilities, crashes, verifier errors, timeouts
and resource failures do not corroborate the target. A correct source fixture
can retain a failing compiler observation or an uncorroborated diagnostic.
Expectations are not derived from compiler acceptance, and reference disagreement
does not authorize weakening the source or changing ownership.

The frozen target is `files/toolchains/lfortran-411-a0afaa840b-20260914/bin/lfortran`.
Bounded observations use GNU16.1 in f2023 and Flang22 with the recorded f2018
fallback; no fallback observation is relabelled f2023. Original and refreshed
reports, hashes and per-case current-fingerprint selection are retained in
the phase-2 handoff. They are not a fictitious combined compiler run or approval.

## Existing ownership and pending mechanisms

The author packet preserved its 1395 base execution IDs, paths and fingerprints.
Integration separately retains the current main bindings, including the three
metadata-only component-scope-duty corrections recorded in
`doc/source_audits/component_scope_duty_001.json`. In particular preserve
`S7_5_2_2_001_valid__component_private`,
`__binding_private`, `__private_private` and `__public_public` rather than
copying them under S7.5.5. Private-default/explicit-private S7.5.5-005 consumers
remain pending connections to the genuine S7.5.5-007 outside-call contrasts.
The current link schema supports explicit finite S-owned diagnostic/control
pairs and singleton runtime-effect or positive-control witnesses. These
consumers still need independently adjudicated, registered connections; the
available mechanism alone clears no pending facet. Arbitrary numbered wrappers
and general source-use graphs remain outside those finite patterns. No new
links or duplicate programs are installed by this generator.

C784's INTEGER-only binding interface necessarily also violates C765 while
implicit PASS remains. Its NOPASS repair removes passed-object status; adding
a defining-type witness would instead defeat C784's antecedent. This is not
perfect single-clause isolation and is distinct from the avoidable C765-primary
overlap corrected in the separate data-component packet.

C780/C781/C782 canonical interface/wrapper graphs, C783 mixed-access attribution,
conditional operator-result and dtv-type isolation, nameless privacy causality
and unrepresented source-use graphs remain pending. Finalization and extension
source work and the separate A-component fixture review are not altered.
The real-BOZ needs-oracle member and 7.5.2.4 private-identity question remain
out of scope. This is not whole-suite or whole-standard completion.

## Definitions

<!-- BEGIN GENERATED 7.5.5 -->

### R746: A binding part starts with CONTAINS and optionally its own PRIVATE statement

**Source:** 7.5.5 R746; J3/24-007, 18 December 2023, physical PDF 99, printed 85. **Class:** Syntax.

**Definition:** The type-bound-procedure part consists of CONTAINS, optionally one binding-private-stmt,
then zero or more type-bound-proc-bindings. The PRIVATE statement, if present, precedes
every binding. Zero bindings is permitted, including after a permitted binding PRIVATE.
This is not the component part or the module's CONTAINS. The surrounding derived-type
grammar still applies; SEQUENCE and BIND types cannot acquire this part.

**Diagnostic obligation:** required.

**Facets:** `empty-binding-part-source-use`, `private-only-part`, `private-before-bindings`, `single-private-slot`, `binding-union-source-use`.

**Oracle:** The private-only compile admission and two single-purpose repairs isolate the optional
slot. Existing R726's empty-part admission and the R748 member union remain graph
proposals, not new executions.

Phase 2 implementation: 5 uniquely named cases represent 3 facets: 2 diagnostic inputs,
3 compile-only positive controls/admissions and 0 runtime effect cases. 2
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** An empty permitted binding part has no call effect to assert. A SEQUENCE/BIND negative
belongs to C745/C1804, and component-versus-binding ordering remains R726's owner.
Reporting capability follows 4.2 p2(3), not compulsory fatal rejection, severity, a
printed rule code, or rejection in every mode. A later negative contract must identify
the failing compile step, source span and binding-specific cause. Wrong-role/context
recovery, unsupported facilities, crashes, ASR/verifier failures, timeouts, resource
errors and unrelated reports are not corroboration.

**Dependencies:** R726/R729 and C745, PDF 88-89; C1804, PDF 526; R748/R747/C772, PDF 99-100; R401 and
4.1.1, PDF 44-45; 4.2 p2(3), PDF 46.

### R747: The binding-private statement is the bare PRIVATE keyword

**Source:** 7.5.5 R747; J3/24-007, 18 December 2023, physical PDF 99, printed 85. **Class:** Syntax.

**Definition:** A binding-private-stmt is PRIVATE alone. It has no access-name list, double colon,
PUBLIC alternative or attribute-list role. Individual binding accessibility is instead
supplied by access-spec in R749/R751, and the enclosing slot is R746.

**Diagnostic obligation:** required.

**Facets:** `bare-keyword-source-use`, `no-access-list`, `no-public-statement`.

**Oracle:** Two token/statement deletions repair the inadmissible statement forms; the ordinary
bare-keyword admission is reusable only through a future reviewed graph.

Phase 2 implementation: 4 uniquely named cases represent 2 facets: 2 diagnostic inputs,
2 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** R807's PUBLIC/PRIVATE access-spec is not R747. PRIVATE in the component part is R745 and
a module access statement is R830; none receives new local coverage. Reporting
capability follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule
code, or rejection in every mode. A later negative contract must identify the failing
compile step, source span and binding-specific cause. Wrong-role/context recovery,
unsupported facilities, crashes, ASR/verifier failures, timeouts, resource errors and
unrelated reports are not corroboration.

**Dependencies:** R746 and 7.5.5 p8, PDF 99/101; R745, PDF 99; R807/C817, PDF 120; R830/C873, PDF 134.

### C772: Binding PRIVATE is restricted to a module specification-part type definition

**Source:** 7.5.5 C772; J3/24-007, 18 December 2023, physical PDF 99, printed 85. **Class:** Restriction.

**Definition:** A binding-private-stmt is permitted only when its derived-type definition is within the
specification part of a module. Being somewhere inside a module file, in a module
procedure's local specification part, or in a submodule specification part does not
satisfy the stated module-specification-part condition.

**Diagnostic obligation:** required.

**Facets:** `module-specification-admission`, `main-program-local-type`, `module-procedure-local-type`, `submodule-local-type`.

**Oracle:** Three otherwise empty type definitions differ only in their genuine enclosing scope;
each invalid source has a one-statement conforming repair.

Phase 2 implementation: 7 uniquely named cases represent 4 facets: 3 diagnostic inputs,
4 compile-only positive controls/admissions and 0 runtime effect cases. 0
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Descendant access to an existing private binding under p9 is different from permission
to introduce a binding-private-stmt. No new C771 private-component fixture is
authorized. Reporting capability follows 4.2 p2(3), not compulsory fatal rejection,
severity, a printed rule code, or rejection in every mode. A later negative contract
must identify the failing compile step, source span and binding-specific cause.
Wrong-role/context recovery, unsupported facilities, crashes, ASR/verifier failures,
timeouts, resource errors and unrelated reports are not corroboration.

**Dependencies:** R746/R747, PDF 99; R1404/R1407 and 14.2.3/R1416/C1412, PDF 318/321; C771 and R745, PDF
99.

### R748: A binding entry is specific, generic or final

**Source:** 7.5.5 R748; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Syntax.

**Definition:** The three alternatives of type-bound-proc-binding are type-bound-procedure-stmt,
type-bound-generic-stmt and final-procedure-stmt. A final subroutine is a binding, but
not a named specific binding or a procedure-pointer component.

**Diagnostic obligation:** required.

**Facets:** `specific-member-source-use`, `generic-member-source-use`, `final-member-source-use`.

**Oracle:** Three explicit dependency edges form a finite grammar union. No standalone wrapper
program or finalization counter is supplied.

**Oracle limitation:** Final accessibility, call triggers, inheritance and ordering are defined by 7.5.6, not
by this grammar alternative. The union has zero direct or linked evidence here.
Reporting capability follows 4.2 p2(3), not compulsory fatal rejection, severity, a
printed rule code, or rejection in every mode. A later negative contract must identify
the failing compile step, source span and binding-specific cause. Wrong-role/context
recovery, unsupported facilities, crashes, ASR/verifier failures, timeouts, resource
errors and unrelated reports are not corroboration.

**Dependencies:** R749/R751, PDF 100; R753/C791-C794 and 7.5.6.1 notes, PDF 102; 7.5.6.2-.4, PDF 102-103;
p4-p5, PDF 101.

### R749: Specific binding declarations have concrete and named-interface forms

**Source:** 7.5.5 R749; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Syntax.

**Definition:** The concrete form admits PROCEDURE followed by a nonempty type-bound-proc-decl-list,
optionally with ::, or with a comma-led binding-attr-list and ::. The other form is
PROCEDURE(interface-name), binding-attr-list :: binding-name-list: its interface name,
attribute list, separator and nonempty name list are required. C788 couples this form to
DEFERRED. interface-name is R1516, not a declaration-type-spec: C1515 permits an
abstract interface or a procedure having an explicit interface, adds prior-declaration
conditions for procedure-declaration-stmt names and a Table 16.2 condition for
intrinsics; C1516 excludes intrinsic-type keywords. Those conditions apply at R1516's
canonical owner.

**Diagnostic obligation:** required.

**Facets:** `bare-concrete-source-use`, `colon-concrete`, `attributed-declaration-list`, `named-interface-form`, `attribute-colons-required`, `nonempty-binding-list`, `interface-name-canonical-source-use`.

**Oracle:** Three finite compile admissions separate optional punctuation, concrete declarator lists
and the mandatory named-interface list. The missing-separator/list repairs are
grammatical; canonical PASS and interface-name checks remain reuse plans.

Phase 2 implementation: 9 uniquely named cases represent 5 facets: 3 diagnostic inputs,
6 compile-only positive controls/admissions and 0 runtime effect cases. 2
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** The named-interface form does not require an ABSTRACT INTERFACE exclusively: an eligible
explicit procedure interface also satisfies R1516. Do not apply C1517, attached to
R1512, merely because R1516 was imported. Reporting capability follows 4.2 p2(3), not
compulsory fatal rejection, severity, a printed rule code, or rejection in every mode. A
later negative contract must identify the failing compile step, source span and
binding-specific cause. Wrong-role/context recovery, unsupported facilities, crashes,
ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** R401/R402 and 4.1.2 p2, PDF 45; R750/R752/C773-C790, PDF 100-101; R1516/C1515/C1516, PDF
333; 15.4.2.1, PDF 325; 16.8/Table 16.2 including continuation, PDF 374-375; C738, PDF
88; C765, PDF 96.

### R750: A concrete declarator names its binding and optionally a procedure target

**Source:** 7.5.5 R750; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Syntax.

**Definition:** A type-bound-proc-decl is a binding-name optionally followed by => procedure-name. The
declaration creates the binding name; the target is a procedure name, not an invocation,
procedure-pointer assignment or data component. Omitting the association uses the
same-name interpretation of p1. Target presence triggers C773 and target eligibility is
C774.

**Diagnostic obligation:** required.

**Facets:** `explicit-target`, `omitted-target-source-use`, `target-is-name-not-call`.

**Oracle:** One explicit-alias admission and one parentheses deletion distinguish a target
identifier from a call. The omitted-target effect stays at p1.

Phase 2 implementation: 3 uniquely named cases represent 2 facets: 1 diagnostic inputs,
2 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** The => token here does not give the binding pointer association status. No ASSOCIATED
query or storage assertion about a binding is meaningful. Reporting capability follows
4.2 p2(3), not compulsory fatal rejection, severity, a printed rule code, or rejection
in every mode. A later negative contract must identify the failing compile step, source
span and binding-specific cause. Wrong-role/context recovery, unsupported facilities,
crashes, ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** R749/C773/C774 and p1, PDF 100; R402, PDF 45; procedure pointer component R741/R742, PDF
94; 15.5.1, PDF 335.

### C773: An explicit bound-procedure target requires the double colon

**Source:** 7.5.5 C773; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** If => procedure-name occurs in a type-bound-proc-decl, :: shall occur in its type-bound
procedure statement. The condition is target presence, even without binding attributes;
the attributed form independently requires :: under R749.

**Diagnostic obligation:** required.

**Facets:** `target-without-attributes`, `target-with-colons`, `no-target-source-use`.

**Oracle:** The one-token separator repair deliberately has no attribute list, so it tests the
explicit-target condition rather than R749's independent attribute punctuation.

Phase 2 implementation: 2 uniquely named cases represent 2 facets: 1 diagnostic inputs,
1 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** A NOPASS attribute would make the same missing separator independently illegal under
R749 and obscure this chosen boundary. Reporting capability follows 4.2 p2(3), not
compulsory fatal rejection, severity, a printed rule code, or rejection in every mode. A
later negative contract must identify the failing compile step, source span and
binding-specific cause. Wrong-role/context recovery, unsupported facilities, crashes,
ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** R749/R750/C774 and p1, PDF 100; 7.5.4.5 p3/C765, PDF 96.

### C774: A concrete binding targets an accessible module procedure or an explicitly interfaced external

**Source:** 7.5.5 C774; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** The procedure-name is the name of an accessible module procedure or an external
procedure having an explicit interface. An internal procedure's explicit interface does
not turn it into either allowed category. A qualifying specific module procedure can
also be supplied by an intrinsic module. Procedure pointers, dummy procedures and
generic-only names do not substitute for a qualifying concrete procedure. The same
eligibility applies to the implicit same-name target of p1.

**Diagnostic obligation:** required.

**Facets:** `local-module-target`, `use-renamed-module-target`, `explicit-external-target`, `implicit-external-interface`, `internal-target-category`, `inaccessible-module-target`, `other-target-categories-source-use`.

**Oracle:** Three category/access admissions and three minimal repairs isolate external-interface,
internal-target and module-name accessibility causes. None relies on linking an absent
procedure.

Phase 2 implementation: 9 uniquely named cases represent 6 facets: 3 diagnostic inputs,
6 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** C1515's interface-name domain is broader than C774's concrete-target domain. Eligibility
of an interface name does not make that name an eligible concrete target. Reporting
capability follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule
code, or rejection in every mode. A later negative contract must identify the failing
compile step, source span and binding-specific cause. Wrong-role/context recovery,
unsupported facilities, crashes, ASR/verifier failures, timeouts, resource errors and
unrelated reports are not corroboration.

**Dependencies:** 15.2.2.2-.4, PDF 323-324; 15.4.2.1-.2 and 15.4.3.2 p5/p7, PDF 325-327; 14.2.1 p3 and
14.2.2, PDF 318-320; R1516/C1515, PDF 333; 8.5.2, PDF 120; p1/p6/p9, PDF 100-101.

### C775: Concrete binding declaration names are unique within the derived-type definition

**Source:** 7.5.5 C775; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** A binding-name in a type-bound-proc-decl cannot be the same as another binding-name
within that derived-type definition. This is a declaration-name restriction, not a ban
on the ordinary later use of that name in a GENERIC member list or a call. It does not
prohibit distinct aliases for one target or the permitted overriding declaration in an
extension. Pure named-interface-list duplicates and collisions with components/type
parameters or generic binding names also need Clause 19's canonical local-identifier
analysis; they are not silently widened into this declarator's wording.

**Diagnostic obligation:** required.

**Facets:** `same-statement-duplicate`, `separate-statement-duplicate`, `distinct-aliases-one-target`, `scope-collision-source-use`.

**Oracle:** The finite pairs change only declaration identity or remove its repetition. A positive
same-target/different-binding admission separates names from implementation identity.

Phase 2 implementation: 5 uniquely named cases represent 3 facets: 2 diagnostic inputs,
3 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** C778 concerns membership under the same generic identifier; treating every GENERIC
reference as a second C775 declaration would reject conforming examples. Reporting
capability follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule
code, or rejection in every mode. A later negative contract must identify the failing
compile step, source span and binding-specific cause. Wrong-role/context recovery,
unsupported facilities, crashes, ASR/verifier failures, timeouts, resource errors and
unrelated reports are not corroboration.

**Dependencies:** R749/R750 and C778, PDF 100; note1/p5, PDF 101; 19.3.1 p1(2)/p3 and 19.3.4, PDF 549-550;
7.5.7.3 p1, PDF 105.

### R751: A GENERIC binding associates a generic specification with specific binding names

**Source:** 7.5.5 R751; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Syntax.

**Definition:** The statement is GENERIC, optionally comma access-spec, followed by :: generic-spec => a
nonempty binding-name-list. R1508 supplies generic-name, OPERATOR(defined-operator),
ASSIGNMENT(=), or R1509's READ/WRITE crossed with FORMATTED/UNFORMATTED: seven families
in total. RHS names identify specific bindings of the type, not arbitrary procedure
names. Optional accessibility, member uniqueness and interfaces still satisfy C776-C782
and the canonical generic-distinguishability rules.

**Diagnostic obligation:** required.

**Facets:** `named-generic-admission`, `explicit-access-admission`, `missing-association-arrow`, `nonempty-member-list`, `seven-family-union-source-use`.

**Oracle:** Named generic admissions and two token/list repairs are bounded grammar plans. The
seven-family union is an explicit pending graph rather than seven fictional passing
inquiries.

Phase 2 implementation: 6 uniquely named cases represent 4 facets: 2 diagnostic inputs,
4 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** R1510 is an ordinary GENERIC statement listing specific procedures, not this type-bound
R751 statement listing binding names. Reporting capability follows 4.2 p2(3), not
compulsory fatal rejection, severity, a printed rule code, or rejection in every mode. A
later negative contract must identify the failing compile step, source span and
binding-specific cause. Wrong-role/context recovery, unsupported facilities, crashes,
ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** R1508/R1509, PDF 326; R1510, PDF 328; C776-C782, PDF 100; C1511-C1514 and p4 effective
positions, PDF 331-332; 4.1.3/R401, PDF 45.

### C776: Repeated generic specifications have consistent binding accessibility

**Source:** 7.5.5 C776; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** Within a module specification part, every type-bound-generic-stmt with the same
generic-spec in the same derived type specifies the same accessibility, whether explicit
or inherited from that type definition's binding default. The comparison is between
generic statements, not between their specific members' accessibility, implementation
procedure names, component defaults or different types.

**Diagnostic obligation:** required.

**Facets:** `explicit-access-conflict`, `implicit-private-conflict`, `implicit-public-agreement`, `same-identifier-and-type-boundaries`, `nongeneric-name-family-source-use`.

**Oracle:** Two one-attribute repairs and two positive boundary admissions distinguish effective
generic access from member or module defaults.

Phase 2 implementation: 6 uniquely named cases represent 4 facets: 2 diagnostic inputs,
4 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** C817 separately limits explicit access-spec scope. C776 is not a blanket ban on a public
generic naming private specifics. Reporting capability follows 4.2 p2(3), not compulsory
fatal rejection, severity, a printed rule code, or rejection in every mode. A later
negative contract must identify the failing compile step, source span and
binding-specific cause. Wrong-role/context recovery, unsupported facilities, crashes,
ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** p8-p9, PDF 101; R807/C817 and 8.6.1, PDF 120/134; R1508/R1509, PDF 326; C1511-C1514, PDF
331-332; 7.5.7.2 p3, PDF 104.

### C777: Every generic member names a specific binding of this type

**Source:** 7.5.5 C777; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** Each binding-name in a type-bound GENERIC binding-name-list is a specific binding of the
type. Such a binding can be declared locally, later in the binding part, or inherited
subject to accessibility and repeated-membership restrictions. A procedure name without
that binding, a generic binding, a component or an anonymous final binding does not
supply the required specific binding.

**Diagnostic obligation:** required.

**Facets:** `local-specific-member`, `forward-specific-member`, `inherited-specific-new-generic`, `procedure-name-not-binding`, `generic-is-not-specific`, `component-is-not-specific`.

**Oracle:** Local, forward and inherited admissions are separate finite contexts; three one-name
repairs retain an otherwise complete binding part.

Phase 2 implementation: 9 uniquely named cases represent 6 facets: 3 diagnostic inputs,
6 compile-only positive controls/admissions and 0 runtime effect cases. 0
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Inherited private bindings remain inaccessible in an unrelated extension scope. Their
mere inheritance is not permission to reference their names there. Reporting capability
follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule code, or
rejection in every mode. A later negative contract must identify the failing compile
step, source span and binding-specific cause. Wrong-role/context recovery, unsupported
facilities, crashes, ASR/verifier failures, timeouts, resource errors and unrelated
reports are not corroboration.

**Dependencies:** R751/C778, PDF 100; p4-p5, PDF 101; 7.5.7.2 p1/NOTE 1, PDF 104; 19.3.4, PDF 550.

### C778: A generic binding does not re-add an inherited or previously listed specific

**Source:** 7.5.5 C778; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** A binding-name in a type-bound GENERIC statement cannot specify a specific binding
already inherited or specified previously for that same generic identifier in the
derived-type definition. The restriction is repeated membership of one generic, not a
prohibition on every inherited specific or reuse of a specific under a different
generic. An override corresponds to the inherited binding; it does not justify re-adding
the same generic member.

**Diagnostic obligation:** required.

**Facets:** `repeated-local-membership`, `same-list-membership`, `inherited-generic-membership`, `same-specific-different-generics`, `override-membership-source-use`.

**Oracle:** Three member-deletion repairs distinguish declaration duplication from local/inherited
generic membership. No generic ambiguity is deliberately introduced.

Phase 2 implementation: 7 uniquely named cases represent 4 facets: 3 diagnostic inputs,
4 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** The inherited-generic control must still satisfy C1514 (or the corresponding
nameless-generic distinguishability constraint); an ambiguity diagnosis is not this
rule's cause. Reporting capability follows 4.2 p2(3), not compulsory fatal rejection,
severity, a printed rule code, or rejection in every mode. A later negative contract
must identify the failing compile step, source span and binding-specific cause.
Wrong-role/context recovery, unsupported facilities, crashes, ASR/verifier failures,
timeouts, resource errors and unrelated reports are not corroboration.

**Dependencies:** C777, PDF 100; p7, PDF 101; 7.5.7.2 p3 and 7.5.7.3 p3, PDF 104-105; C1511-C1514, PDF
331-332.

### C779: Every specific of a non-name generic has a passed-object dummy

**Source:** 7.5.5 C779; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** If generic-spec is not generic-name, every specific binding has a passed-object dummy
argument. This includes OPERATOR, ASSIGNMENT and all four defined-I/O generic
specifications. Named generics are outside this requirement and can use NOPASS. Merely
having a dummy of the type does not establish passed-object status when NOPASS was
specified.

**Diagnostic obligation:** required.

**Facets:** `operator-nopass`, `assignment-nopass`, `four-io-nopass`, `named-generic-nopass`, `canonical-passed-object-source-use`.

**Oracle:** Six finite negative/control members retain otherwise valid operator, assignment and I/O
interfaces; the named-generic boundary is conforming. The only intended mutation is
passed-object status.

Phase 2 implementation: 13 uniquely named cases represent 4 facets: 6 diagnostic inputs,
7 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Do not repair an I/O binding by making dtv the wrong type, or use an operator with a
VALUE passed dummy: either would introduce C765/C782 confounders. Reporting capability
follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule code, or
rejection in every mode. A later negative contract must identify the failing compile
step, source span and binding-specific cause. Wrong-role/context recovery, unsupported
facilities, crashes, ASR/verifier failures, timeouts, resource errors and unrelated
reports are not corroboration.

**Dependencies:** 7.5.4.5/C765, PDF 96; R1508/R1509, PDF 326; 15.4.3.4.2/.3, PDF 329-331; 12.6.4.8.2, PDF
268-270.

### C780: Operator binding interfaces import the defined-operation contract

**Source:** 7.5.5 C780; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** For OPERATOR(defined-operator), every specific interface meets 15.4.3.4.2: a function
with one dummy for prefix unary notation or two for infix binary notation; nonoptional
dummy data objects having INTENT(IN) or VALUE; no assumed-character-length result. For
an intrinsic operator, arity agrees with intrinsic use and dummy type, kind or rank
differs from the intrinsic operation, treating CLASS(*) as not differing in type or
kind. C779/C765 additionally require an eligible passed object, which cannot have VALUE.
Operand correspondence, generic operation extension and equivalent relational spellings
retain their defining scopes rather than becoming new C780 programs.

**Diagnostic obligation:** required.

**Facets:** `function-category-source-use`, `unary-binary-arity-source-use`, `dummy-data-nonoptional-source-use`, `intent-or-value-source-use`, `result-length-source-use`, `intrinsic-operator-boundary-source-use`, `operand-and-generic-effects-source-use`.

**Oracle:** This is an explicit finite interface/use graph, with canonical prospective causes and
repairs. Zero C780 programs, links or passing effects are asserted. The inherited
numbered constraint supplies reporting capability for the imported interface condition
without transferring canonical ownership.

**Oracle limitation:** Current links cannot target numbered C780, cannot reuse a positive alone, and cannot use
a supplementary-runtime/S-owned source as the canonical numbered pair. Result-length
isolation and all mechanism work remain gates. Reporting capability follows 4.2 p2(3),
not compulsory fatal rejection, severity, a printed rule code, or rejection in every
mode. A later negative contract must identify the failing compile step, source span and
binding-specific cause. Wrong-role/context recovery, unsupported facilities, crashes,
ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** 15.4.3.4.2 p1-p3 including continuation note, PDF 329-330; C779, PDF 100; C765, PDF 96;
15.5.1/.6, PDF 335/352; C1511 and TKR definitions, PDF 331.

### C781: Assignment binding interfaces import the defined-assignment contract

**Source:** 7.5.5 C781; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** For ASSIGNMENT(=), every specific interface meets 15.4.3.4.3: a subroutine with exactly
two nonoptional dummy data objects; first INTENT(OUT) or INTENT(INOUT), second
INTENT(IN) or VALUE. In addition, the second is an array of different rank from the
first, or their declared types/kinds do not conform to Table 10.8, or the first is
derived type. The call associates the left side with the first dummy and the
parenthesized right side with the second, under normal argument restrictions. Generic
assignment extends or redefines assignment. These canonical provisions are not a new
local assignment catalogue.

**Diagnostic obligation:** required.

**Facets:** `subroutine-and-arity-source-use`, `nonoptional-data-source-use`, `lhs-intent-source-use`, `rhs-intent-value-source-use`, `rank-type-kind-disjunction-source-use`, `parenthesized-rhs-and-effect-source-use`.

**Oracle:** A finite canonical graph records the exact category, arity, attribute, compatibility and
actual-argument conditions. The scalar value-7 effect is a planned canonical call
observation, not an executed or linked oracle here.

**Oracle limitation:** First-dummy INTENT(OUT) would trigger definition/finalization premises; the initial
planned effect instead uses INOUT. A second pointer/allocatable dummy cannot be invoked
by defined assignment merely because its interface is declarable. Reporting capability
follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule code, or
rejection in every mode. A later negative contract must identify the failing compile
step, source span and binding-specific cause. Wrong-role/context recovery, unsupported
facilities, crashes, ASR/verifier failures, timeouts, resource errors and unrelated
reports are not corroboration.

**Dependencies:** 15.4.3.4.3 and NOTE 2 continuation, PDF 330-331; 10.2.1.4-.5, PDF 192-193; C779/C765,
PDF 100/96; 15.5.2.1/.2/.4/.5, PDF 337-340; 19.6.5/19.6.6, PDF 563-566.

### C782: Defined-I/O bindings use the four canonical interfaces and the defining dtv type

**Source:** 7.5.5 C782; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** For a defined-io-generic-spec, every binding interface follows 12.6.4.8, and dtv has the
type being defined. In the order shown by 12.6.4.8.2, formatted forms have dtv, unit,
iotype, v_list, iostat, iomsg; unformatted forms omit iotype and v_list. READ has dtv
INTENT(INOUT); WRITE has dtv INTENT(IN). unit is default INTEGER INTENT(IN); iotype is
assumed-length default CHARACTER INTENT(IN); v_list is a rank-one assumed-shape default
INTEGER INTENT(IN); iostat is default INTEGER INTENT(OUT); iomsg is assumed-length
default CHARACTER INTENT(INOUT). Other characteristics agree with these interfaces.
Dummy/procedure names are arbitrary. R1221/C1236/C1237 require CLASS for an extensible
dtv type, TYPE otherwise, and assumed length parameters. Type-bound use further
satisfies C779/C765.

**Diagnostic obligation:** required.

**Facets:** `read-formatted-source-use`, `write-formatted-source-use`, `read-unformatted-source-use`, `write-unformatted-source-use`, `io-characteristics-repair-source-use`, `dtv-defining-type-isolation`, `io-execution-resolution-source-use`.

**Oracle:** Four compile-admission graph nodes cover the complete interface family. The two
single-characteristic repairs remain canonical source-use plans. A narrowly specified
formatted-output effect is conditional on the separate execution source and fixture
review; it is not current evidence.

**Oracle limitation:** Do not mistake C1236/C1237, C765, C779 or defined-I/O execution restrictions for a
uniquely isolated C782 dtv-type diagnostic. The broader evidence schema needed for this
numbered wrapper is coordinator-owned and unavailable. Reporting capability follows 4.2
p2(3), not compulsory fatal rejection, severity, a printed rule code, or rejection in
every mode. A later negative contract must identify the failing compile step, source
span and binding-specific cause. Wrong-role/context recovery, unsupported facilities,
crashes, ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** 12.6.4.8.1-.4, PDF 268-274; R1221/C1236/C1237, PDF 269; R1509, PDF 326; C1513, PDF 331;
C765/C779, PDF 96/100; 12.6.4.5 p9, PDF 268; 19.6.5(3)/19.6.6(5), PDF 563/565.

### R752: Binding attributes include accessibility, deferred/override control and PASS selection

**Source:** 7.5.5 R752; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Syntax.

**Definition:** A binding-attr is access-spec, DEFERRED, NON_OVERRIDABLE, NOPASS, or PASS with an
optional parenthesized arg-name. R807 supplies PUBLIC or PRIVATE. PASS alone does not
require parentheses and PASS() is not the optional named form. Attribute membership does
not authorize repetitions or incompatible combinations. Omission of both PASS and NOPASS
defaults to PASS of the first dummy under 7.5.4.5, not NOPASS.

**Diagnostic obligation:** required.

**Facets:** `access-union-source-use`, `deferred-union-source-use`, `non-overridable-union-source-use`, `nopass-and-pass-source-use`, `empty-pass-parentheses`, `pointer-is-not-binding-attribute`.

**Oracle:** The union includes both access alternatives and both PASS spellings, but does not
synthesize separate duplicate controls. Two grammar repairs isolate empty PASS
parentheses and a component-only attribute.

Phase 2 implementation: 4 uniquely named cases represent 2 facets: 2 diagnostic inputs,
2 compile-only positive controls/admissions and 0 runtime effect cases. 4
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** R742's POINTER attribute belongs to procedure-pointer components; its absence from R752
is deliberate. The rule supplies no storage, pointer-state or dispatch-implementation
oracle. Reporting capability follows 4.2 p2(3), not compulsory fatal rejection,
severity, a printed rule code, or rejection in every mode. A later negative contract
must identify the failing compile step, source span and binding-specific cause.
Wrong-role/context recovery, unsupported facilities, crashes, ASR/verifier failures,
timeouts, resource errors and unrelated reports are not corroboration.

**Dependencies:** R807/C817, PDF 120; R742/C761, PDF 94; 7.5.4.5 p2-p4/C765, PDF 96; C783-C790, PDF
100-101.

### C783: A binding attribute is not repeated within one attribute list

**Source:** 7.5.5 C783; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** The same binding-attr shall not occur more than once in a binding-attr-list. The subject
is one binding declaration's attributes, not repeated statements with distinct bindings
or the attributes of the implementation procedure. Identical PUBLIC, PRIVATE, DEFERRED,
NON_OVERRIDABLE, NOPASS and PASS repetitions have unambiguous finite repairs.
Differently spelled access alternatives are retained as an explicit source-attribution
question, not silently equated with one of these identical-token negatives.

**Diagnostic obligation:** required.

**Facets:** `duplicate-access`, `duplicate-deferred`, `duplicate-non-overridable`, `duplicate-nopass`, `duplicate-pass`, `access-alternative-identity-source-question`.

**Oracle:** Seven identical-attribute negative/control members cover PUBLIC, PRIVATE, DEFERRED,
NON_OVERRIDABLE, NOPASS, PASS and PASS(self). Mixed-access attribution remains a visibly
unresolved fine source unit.

Phase 2 implementation: 14 uniquely named cases represent 5 facets: 7 diagnostic inputs,
7 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Five grammar attribute families yield seven planned pairs because access and PASS each
have two explicit spellings here. No mixed-access case is counted. Facets, prospective
members and source units have separate denominators. Reporting capability follows 4.2
p2(3), not compulsory fatal rejection, severity, a printed rule code, or rejection in
every mode. A later negative contract must identify the failing compile step, source
span and binding-specific cause. Wrong-role/context recovery, unsupported facilities,
crashes, ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** R752/C786/C787/C788, PDF 100-101; R807 and C817, PDF 120; C815, PDF 119; C765, PDF 96.

### C784: An interface with no dummy of the defining type requires NOPASS

**Source:** 7.5.5 C784; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** If the binding interface has no dummy argument of the type being defined, NOPASS shall
appear. This includes no-dummy interfaces. Having such a dummy is not itself a
requirement to use PASS: NOPASS is still permitted for a named specific/generic binding.
A nonfirst dummy of the type does not move default PASS from the first position;
explicit PASS(name) and C765 decide eligibility. CLASS(*) is not the same declared type
as the defining type.

**Diagnostic obligation:** required.

**Facets:** `no-dummy`, `only-unrelated-dummy`, `typed-dummy-with-nopass`, `nonfirst-typed-dummy`, `default-pass-eligibility-source-use`.

**Oracle:** Two NOPASS insertions repair interfaces without a dummy of the defining type. The
INTEGER-only interface necessarily also fails C765 while implicit PASS remains; NOPASS
removes passed-object status and repairs both conditions. Adding a separate
defining-type dummy here would defeat C784's antecedent, so this is not advertised as
perfect single-clause isolation. The two admissions distinguish permission to use NOPASS
from explicit selection of a nonfirst dummy.

Phase 2 implementation: 6 uniquely named cases represent 4 facets: 2 diagnostic inputs,
4 compile-only positive controls/admissions and 0 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** The first negative may also be worded by a processor as a missing passed dummy. Later
contracts must retain the interface/no-dummy cause rather than accept any complaint
containing PASS. Reporting capability follows 4.2 p2(3), not compulsory fatal rejection,
severity, a printed rule code, or rejection in every mode. A later negative contract
must identify the failing compile step, source span and binding-specific cause.
Wrong-role/context recovery, unsupported facilities, crashes, ASR/verifier failures,
timeouts, resource errors and unrelated reports are not corroboration.

**Dependencies:** 7.5.4.5 p2-p4/C765, PDF 96; 7.3.2.3 p4, PDF 79; C774/C779/C785, PDF 100.

### C785: A named PASS selector denotes a dummy in the binding interface

**Source:** 7.5.5 C785; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** If PASS(arg-name) occurs, the binding interface has a dummy argument with that name. The
name comes from the interface selected by p6, not from the binding's own name or a
component name. Existence does not by itself establish the selected dummy's type, rank
or attributes; C765 supplies those requirements.

**Diagnostic obligation:** required.

**Facets:** `nonexistent-dummy-name`, `first-dummy-name`, `nonfirst-dummy-name-source-use`, `named-interface-dummy-source-use`.

**Oracle:** One identifier repair and one compile admission establish name existence. The value-73
receiver-selection effect remains canonically owned outside this constraint.

Phase 2 implementation: 3 uniquely named cases represent 2 facets: 1 diagnostic inputs,
2 compile-only positive controls/admissions and 0 runtime effect cases. 2
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Do not accept a wrong type/rank/POINTER/ALLOCATABLE/VALUE report as proof of misspelled
PASS selection. The selected self in the negative/repair is otherwise C765-conforming.
Reporting capability follows 4.2 p2(3), not compulsory fatal rejection, severity, a
printed rule code, or rejection in every mode. A later negative contract must identify
the failing compile step, source span and binding-specific cause. Wrong-role/context
recovery, unsupported facilities, crashes, ASR/verifier failures, timeouts, resource
errors and unrelated reports are not corroboration.

**Dependencies:** p6, PDF 101; 7.5.4.5 p4/C765, PDF 96; 15.4.1, PDF 325; R1516, PDF 333; 15.5.2.1-.2, PDF
337-338.

### C786: PASS and NOPASS cannot coexist in one binding attribute list

**Source:** 7.5.5 C786; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** PASS, whether bare or named, and NOPASS do not both appear in the same
binding-attr-list. Different bindings may independently select PASS or NOPASS. The
default PASS interpretation of an omitted attribute is not a second explicit attribute
in a list that contains NOPASS.

**Diagnostic obligation:** required.

**Facets:** `bare-pass-conflict`, `named-pass-conflict`, `distinct-binding-choices`.

**Oracle:** The two repairs preserve opposite valid choices, and the admission checks list-locality
rather than an across-type ban.

Phase 2 implementation: 5 uniquely named cases represent 3 facets: 2 diagnostic inputs,
3 compile-only positive controls/admissions and 0 runtime effect cases. 0
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** A named NOPASS specific with a defining-type dummy is valid; no operator/assignment/I/O
generic is attached to the repaired NOPASS declaration. Reporting capability follows 4.2
p2(3), not compulsory fatal rejection, severity, a printed rule code, or rejection in
every mode. A later negative contract must identify the failing compile step, source
span and binding-specific cause. Wrong-role/context recovery, unsupported facilities,
crashes, ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** R752/C784/C785 and C779, PDF 100; 7.5.4.5 p2-p4, PDF 96.

### C787: NON_OVERRIDABLE and DEFERRED cannot coexist in one binding attribute list

**Source:** 7.5.5 C787; J3/24-007, 18 December 2023, physical PDF 100, printed 86. **Class:** Restriction.

**Definition:** NON_OVERRIDABLE and DEFERRED are mutually excluded within a binding-attr-list. An
ABSTRACT type can nevertheless have a separate concrete NON_OVERRIDABLE binding and a
separate DEFERRED binding. The exclusion is neither an all-bindings property of the type
nor a reason to remove DEFERRED while leaving the named-interface form.

**Diagnostic obligation:** required.

**Facets:** `incompatible-attributes`, `separate-bindings`.

**Oracle:** One attribute deletion is the minimal repair; a separate-binding admission establishes
the same-list boundary.

Phase 2 implementation: 3 uniquely named cases represent 2 facets: 1 diagnostic inputs,
2 compile-only positive controls/admissions and 0 runtime effect cases. 0
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Deleting only DEFERRED from PROCEDURE(iface) would violate C788 and is not a conforming
repair. Reporting capability follows 4.2 p2(3), not compulsory fatal rejection,
severity, a printed rule code, or rejection in every mode. A later negative contract
must identify the failing compile step, source span and binding-specific cause.
Wrong-role/context recovery, unsupported facilities, crashes, ASR/verifier failures,
timeouts, resource errors and unrelated reports are not corroboration.

**Dependencies:** C788 and p2, PDF 101; C738, PDF 88; R749/R752, PDF 100.

### C788: DEFERRED occurs if and only if the declaration has an interface name

**Source:** 7.5.5 C788; J3/24-007, 18 December 2023, physical PDF 101, printed 87. **Class:** Restriction.

**Definition:** DEFERRED is present exactly when interface-name is present in the specific binding
statement. Both implications matter. Without interface-name, the declaration names a
concrete procedure target explicitly or by p1. With interface-name, the binding is
deferred and its declaration has no concrete target association. Abstract-type and
override constraints continue to apply independently.

**Diagnostic obligation:** required.

**Facets:** `interface-without-deferred`, `deferred-without-interface`, `both-present-admission`, `both-absent-admission`.

**Oracle:** Two single-attribute repairs and both conforming truth-table rows retain all
enclosing-type and interface premises.

Phase 2 implementation: 4 uniquely named cases represent 4 facets: 2 diagnostic inputs,
2 compile-only positive controls/admissions and 0 runtime effect cases. 0
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Do not call iface as if it were necessarily an implementation, infer an object of
abstract dynamic type, or clear this iff relation from only one direction. Reporting
capability follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule
code, or rejection in every mode. A later negative contract must identify the failing
compile step, source span and binding-specific cause. Wrong-role/context recovery,
unsupported facilities, crashes, ASR/verifier failures, timeouts, resource errors and
unrelated reports are not corroboration.

**Dependencies:** R749/R750/C774/C787, PDF 100; p1/p2/p6, PDF 100-101; C738, PDF 88; R1516/C1515, PDF 333;
7.5.7.1, PDF 104.

### C789: A deferred override can replace only a deferred binding

**Source:** 7.5.5 C789; J3/24-007, 18 December 2023, physical PDF 101, printed 87. **Class:** Restriction.

**Definition:** An overriding binding may have DEFERRED only if the overridden binding is deferred.
Overriding here requires the same binding name as an accessible parent binding. The
implication is one-way: a concrete binding may override a deferred one, and a deferred
binding may override a deferred one if the type is abstract. All overriding-interface
conditions of 7.5.7.3 also hold.

**Diagnostic obligation:** required.

**Facets:** `nondeferred-to-deferred`, `deferred-to-deferred`, `concrete-implementation-source-use`, `private-homonym-source-use`.

**Oracle:** The single-statement deletion removes only the invalid overriding declaration. The other
facets are an abstract declaration admission or explicit canonical reuse plans.

Phase 2 implementation: 3 uniquely named cases represent 2 facets: 1 diagnostic inputs,
2 compile-only positive controls/admissions and 0 runtime effect cases. 2
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** The negative child stays ABSTRACT to avoid C738/p2. Do not deliberately change purity,
simplicity, elemental status, dummy count/names/characteristics, result characteristics
or public access merely to elicit an override error. Reporting capability follows 4.2
p2(3), not compulsory fatal rejection, severity, a printed rule code, or rejection in
every mode. A later negative contract must identify the failing compile step, source
span and binding-specific cause. Wrong-role/context recovery, unsupported facilities,
crashes, ASR/verifier failures, timeouts, resource errors and unrelated reports are not
corroboration.

**Dependencies:** 7.5.7.2 p1/NOTE 1 and 7.5.7.3 p1-p3 (all nine interface conditions), PDF 104-105; C738,
PDF 88; p2, PDF 101; 15.3, PDF 324.

### C790: An inherited NON_OVERRIDABLE binding is not overridden

**Source:** 7.5.5 C790; J3/24-007, 18 December 2023, physical PDF 101, printed 87. **Class:** Restriction.

**Definition:** A binding does not override an inherited binding having NON_OVERRIDABLE. Inheritance
retains that attribute. An override depends on accessibility and binding-name
correspondence, not on changing the implementation procedure's name. The rule is a
language restriction; it does not require a particular vtable layout, static machine
call, optimization or lowering strategy.

**Diagnostic obligation:** required.

**Facets:** `forbidden-accessible-override`, `inherited-nonoverridable-admission`, `different-binding-name-admission`, `inaccessible-private-boundary-source-use`, `dispatch-source-use`.

**Oracle:** One attribute deletion permits the otherwise valid override. Two admissions establish
inheritance and distinct-name boundaries; runtime selection stays canonically owned at
15.5.6.

Phase 2 implementation: 4 uniquely named cases represent 3 facets: 1 diagnostic inputs,
3 compile-only positive controls/admissions and 0 runtime effect cases. 2
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** NON_OVERRIDABLE does not change the declared-type generic-interface selection rule, or
exempt PASS/NOPASS and overriding-characteristic constraints. Reporting capability
follows 4.2 p2(3), not compulsory fatal rejection, severity, a printed rule code, or
rejection in every mode. A later negative contract must identify the failing compile
step, source span and binding-specific cause. Wrong-role/context recovery, unsupported
facilities, crashes, ASR/verifier failures, timeouts, resource errors and unrelated
reports are not corroboration.

**Dependencies:** 7.5.7.2-.3, PDF 104-105; 7.3.2.3 p5, PDF 79; 15.5.6 p1-p3, PDF 352; C787, PDF 100.

### S7.5.5-001: Omitting a concrete target uses the binding name as the procedure name

**Source:** 7.5.5 p1; physical PDF 100, printed 86, lines 12-13. **Class:** Effect.

**Definition:** When => procedure-name is absent from a type-bound-proc-decl, the interpretation is as
if that target association were present with a procedure name equal to the binding name.
The target must still meet C774 and the interface and passed-object conditions are
unchanged. This does not mean that a private binding name becomes an accessible
stand-alone procedure name in a client.

**Diagnostic obligation:** not-required.

**Facets:** `omitted-explicit-same-name`, `renamed-accessible-target`, `target-eligibility-source-use`.

**Oracle:** Named setup assigns first/second payloads 17/29. Omitted and explicit bindings are each
checked against independent expected results 17/59, not just each other; the second
function computes 2*29+1. A separate renamed NOPASS target has expected result22 and
preserves payload11. Every receiver/result is defined and live.

Phase 2 implementation: 2 uniquely named cases represent 2 facets: 0 diagnostic inputs,
0 compile-only positive controls/admissions and 2 runtime effect cases. 1
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** The no-attribute form uses default PASS; a zero-dummy target requires explicit NOPASS.
Distinct explicit aliases avoid C775, and no type identity or private representation
inquiry is used.

**Dependencies:** R749/R750/C774, PDF 100; 7.5.4.5 p2-p4/C765, PDF 96; 14.2.2, PDF 318-320;
15.5.1/.2/.3/.6, PDF 335/337-340/349/352; 19.6.5, PDF 563.

### S7.5.5-002: The local DEFERRED restriction reuses the canonical ABSTRACT requirement

**Source:** 7.5.5 p2, final sentence; physical PDF 101, printed 87, lines 8-9; canonical overlap C738 at PDF 88. **Class:** Restriction.

**Definition:** The DEFERRED keyword appears only in an abstract type definition. This local statement
overlaps C738's canonical condition for a type containing or inheriting a deferred
binding. The source-use plan reuses C738's local-deferred diagnostic and repair rather
than creating a second rejection program. Abstractness alone does not supply a concrete
implementation and does not authorize an abstract dynamic object or a call to a deferred
binding.

**Diagnostic obligation:** required.

**Facets:** `local-abstract-condition-source-use`, `inherited-deferred-boundary-source-use`.

**Oracle:** The inspected base C738 local pair is a concrete pending candidate: diagnostic
capability versus a one-attribute compile repair, never fatal rejection or an
instantiated abstract object.

**Oracle limitation:** The current link schema could represent the local same-C738 pair to this supplementary
target, but source/case/link review and coordinator registration are still gates.
Inherited source ownership remains C738; no new diagnostic policy is imposed.

**Dependencies:** C738 and R728, PDF 88; 7.5.7.1 p4/NOTE 2 and 7.5.7.2, PDF 104; C788, PDF 101; 4.2 p2(3),
PDF 46.

### S7.5.5-003: A specific binding's interface comes from its target or interface name

**Source:** 7.5.5 p6; physical PDF 101, printed 87, lines 18-19. **Class:** Effect.

**Definition:** The interface of a specific type-bound procedure is the interface of the procedure
selected by procedure-name, or the interface selected by interface-name. Names of dummy
arguments, characteristics and the permitted reference forms come from that interface,
not from the spelling of the binding alias. The declaration's interface and generic
resolution by declared type are distinct from procedure selection by dynamic type, whose
owner is 15.5.6.

**Diagnostic obligation:** not-required.

**Facets:** `concrete-interface-call`, `deferred-interface-call`, `interface-name-domain-source-use`, `declared-versus-dynamic-source-use`, `call-characteristics-source-use`.

**Oracle:** Two runtime plans return the exact integer 20 from fully defined data through concrete
and deferred-declaration interfaces. Names, receiver position and result characteristics
are explicit. Runtime premises P1-P6 in the companion document apply: live scalar
ordinary objects, explicitly defined integer payloads/results, source-valid explicit
interfaces and argument correspondence, no pointer queries, no private representation
access.

Phase 2 implementation: 2 uniquely named cases represent 2 facets: 0 diagnostic inputs,
0 compile-only positive controls/admissions and 2 runtime effect cases. 3
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Override validity also preserves pure/simple implications, elemental status, dummy
count/names/characteristics, corresponding passed-dummy name/position,
subroutine/function result category, and public accessibility. No signature mismatch is
excused by dynamic dispatch.

**Dependencies:** 15.4.1-.2 and 15.3, PDF 324-325; R1516/C1515/C1516, PDF 333; C765, PDF 96; 7.5.7.3, PDF
105; 7.3.2.3 p5, PDF 79; 15.5.1/.2/.3/.6, PDF 335/337-340/349/352; IMPORT, PDF 143-144.

### S7.5.5-004: Additional GENERIC statements extend the same interface within one type

**Source:** 7.5.5 p7; physical PDF 101, printed 87, lines 20-21. **Class:** Effect.

**Definition:** Several GENERIC statements in one derived-type definition may have the same
generic-spec, and each additional statement extends that generic interface. The specific
member sets accumulate rather than the last statement replacing earlier ones.
Accessibility consistency, no repeated members and generic distinguishability remain
necessary. Extension of a generic inherited from a parent has its own rule in 7.5.7.2
p3.

**Diagnostic obligation:** not-required.

**Facets:** `two-statement-name-union`, `declaration-order-independence`, `nameless-generic-union-source-use`, `inherited-generic-extension-source-use`.

**Oracle:** Both independent declaration-order variants use named payload setup. Integer/real
generic branches must yield 11/22 with payload5 and 13/24 with payload7, using distinct
nonpassed argument types and separate call evaluation. This tests cumulative membership
and receiver use without real arithmetic or constructor-order cancellation.

Phase 2 implementation: 2 uniquely named cases represent 2 facets: 0 diagnostic inputs,
0 compile-only positive controls/admissions and 2 runtime effect cases. 2
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** A same-signature second specific would test generic ambiguity rather than accumulation;
a repeated first member would test C778. No such confounder is admitted.

**Dependencies:** R751/C776-C778, PDF 100; C1511-C1514, PDF 331-332; 7.5.7.2 p3, PDF 104; 15.5.6, PDF 352;
C765 and default PASS, PDF 96.

### S7.5.5-005: Binding accessibility defaults belong to the declaring type definition

**Source:** 7.5.5 p8; physical PDF 101, printed 87, lines 22-24. **Class:** Effect.

**Definition:** The default accessibility of a type's type-bound procedures is private when that
definition contains a binding-private-stmt and public otherwise. An explicit access-spec
on the particular binding overrides this default; absent it, the default of the
definition where the binding was declared applies. Component-part PRIVATE, module
default PRIVATE and type-name accessibility are not substitutes for binding PRIVATE.
Inherited bindings retain original attributes rather than being reset by a child's
binding default.

**Diagnostic obligation:** not-required.

**Facets:** `default-public-binding`, `default-private-binding`, `explicit-public-over-private`, `explicit-private-over-public`, `inherited-default-origin`, `generic-default-source-use`.

**Oracle:** The new explicit-public-over-private runtime observes an allowed client call and integer
result 17. Existing default-public and legal owner paths retain their original S7.5.2.2
ownership. Private-default and explicit-private effects require the actual outside-scope
diagnostic/public-repair relations at p9, not a wrapper call that would also pass when
PRIVATE is ignored. Those S-owned connections and broader inherited/generic source-use
graphs remain pending. Runtime premises P1-P6 still apply.

Phase 2 implementation: 1 uniquely named cases represent 1 facets: 0 diagnostic inputs,
0 compile-only positive controls/admissions and 1 runtime effect cases. 5
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Only legal calls are made here. A private component statement before CONTAINS and
binding PRIVATE after it have independent subjects, as NOTE 2 explains.

**Dependencies:** p9/NOTE 2, PDF 101; 7.5.2.2 and 7.5.4.8, PDF 89/99; 8.5.2/8.6.1, PDF 120/134; 7.5.7.2
p1, PDF 104; 19.3.4/19.5.1.4, PDF 550/553-554.

### S7.5.5-006: A public binding is accessible through an accessible object

**Source:** 7.5.5 p9, first sentence; physical PDF 101, printed 87, line 25. **Class:** Effect.

**Definition:** A public type-bound procedure is accessible via any accessible object of its type. The
type name and implementation procedure name need not themselves be accessible to the
caller. A public generic can provide access to a private specific without making that
private specific binding name directly usable. 'Accessible object' does not waive
reference validity, receiver lifetime or argument/interface restrictions.

**Diagnostic obligation:** not-required.

**Facets:** `public-type-public-object`, `private-type-public-object`, `private-implementation-name`, `public-generic-private-specific`, `nameless-binding-access-source-use`.

**Oracle:** Two new legal-call plans expose exactly 17 through accessible interfaces: a deliberately
inaccessible type name with private components and a public generic naming a private
specific. The uncomplicated public-call and private-implementation-name observations
already exist under S7.5.2.2-001 and remain pending reuse edges, not duplicated
programs. Runtime premises P1-P6 apply.

Phase 2 implementation: 2 uniquely named cases represent 2 facets: 0 diagnostic inputs,
0 compile-only positive controls/admissions and 2 runtime effect cases. 3
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** No client uses a structure constructor for the private type or reads its private fields.
No allocation, pointer association, TYPE IS or SAME_TYPE_AS inquiry is needed.

**Dependencies:** 7.5.2.2 p1-p2, PDF 89; p5/p8, PDF 101; 19.3.4 p3-p6, PDF 550; 14.2.2/C1407-C1409, PDF
318-320; 15.5.1 p2 and 15.5.6, PDF 335/352.

### S7.5.5-007: Private bindings are accessible only in their defining module and descendants

**Source:** 7.5.5 p9, second sentence; physical PDF 101, printed 87, lines 25-26. **Class:** Restriction.

**Definition:** A private type-bound procedure is accessible only within the module containing its type
definition and within that module's descendants. Ordinary client modules and separately
defined extension types do not become descendants by USE association or EXTENDS.
Inaccessible inherited bindings retain their identity and attributes. Clause 19
separately prohibits a component/binding name appearing where inaccessible, and 4.2
requires reporting capability for that name-scope violation.

**Diagnostic obligation:** required.

**Facets:** `defining-module-control`, `descendant-control`, `unrelated-client-call`, `unrelated-module-call`, `private-generic-name-call`, `inherited-private-call`, `nameless-private-source-use`.

**Oracle:** One descendant compile control, existing owner-call reuse and five finite
diagnostic/control pairs are planned: two explicit/default-private client variants and
the unrelated-module, private-generic and inherited-private cases. Each repair changes
only the source of the tested private accessibility. Clause 19.3.4 p5 and 4.2 p2(6)
supply capability to report named private-binding violations; actual diagnostic wording
remains a later contract review.

Phase 2 implementation: 11 uniquely named cases represent 5 facets: 5 diagnostic inputs,
6 compile-only positive controls/admissions and 0 runtime effect cases. 2
source-use/conditional facets remain pending. Authorship and observations do not
establish adjudication; approval status is held in separate content-bound review
records. A control remains distinct from a runtime effect.

**Oracle limitation:** Fatal rejection and mandatory printed source-rule codes are not required. Treat
unsupported submodules, unavailable facilities, crashes, wrong context, generic recovery
or resource errors as noncorroborating. No external private component/type-name access
is introduced merely to provoke a failure.

**Dependencies:** 19.3.4 p3-p6, PDF 550; 4.2 p2(6)/(10), PDF 46; 14.2.2-.3/R1416/C1412, PDF 318-321;
19.5.1.4 and IMPORT defaults, PDF 553-554/144; 7.5.7.2 NOTE 1 and 7.5.7.3 p1, PDF
104-105.

<!-- END GENERATED 7.5.5 -->

## Implemented finite case census

| Primary owner | Cases | Diagnostic | Compile control | Runtime | Represented facets |
| --- | ---: | ---: | ---: | ---: | --- |
| R746 | 5 | 2 | 3 | 0 | `private-only-part`, `private-before-bindings`, `single-private-slot` |
| R747 | 4 | 2 | 2 | 0 | `no-access-list`, `no-public-statement` |
| C772 | 7 | 3 | 4 | 0 | `module-specification-admission`, `main-program-local-type`, `module-procedure-local-type`, `submodule-local-type` |
| R749 | 9 | 3 | 6 | 0 | `colon-concrete`, `attributed-declaration-list`, `named-interface-form`, `attribute-colons-required`, `nonempty-binding-list` |
| R750 | 3 | 1 | 2 | 0 | `explicit-target`, `target-is-name-not-call` |
| C773 | 2 | 1 | 1 | 0 | `target-without-attributes`, `target-with-colons` |
| C774 | 9 | 3 | 6 | 0 | `local-module-target`, `use-renamed-module-target`, `explicit-external-target`, `implicit-external-interface`, `internal-target-category`, `inaccessible-module-target` |
| C775 | 5 | 2 | 3 | 0 | `same-statement-duplicate`, `separate-statement-duplicate`, `distinct-aliases-one-target` |
| R751 | 6 | 2 | 4 | 0 | `named-generic-admission`, `explicit-access-admission`, `missing-association-arrow`, `nonempty-member-list` |
| C776 | 6 | 2 | 4 | 0 | `explicit-access-conflict`, `implicit-private-conflict`, `implicit-public-agreement`, `same-identifier-and-type-boundaries` |
| C777 | 9 | 3 | 6 | 0 | `local-specific-member`, `forward-specific-member`, `inherited-specific-new-generic`, `procedure-name-not-binding`, `generic-is-not-specific`, `component-is-not-specific` |
| C778 | 7 | 3 | 4 | 0 | `repeated-local-membership`, `same-list-membership`, `inherited-generic-membership`, `same-specific-different-generics` |
| C779 | 13 | 6 | 7 | 0 | `operator-nopass`, `assignment-nopass`, `four-io-nopass`, `named-generic-nopass` |
| R752 | 4 | 2 | 2 | 0 | `empty-pass-parentheses`, `pointer-is-not-binding-attribute` |
| C783 | 14 | 7 | 7 | 0 | `duplicate-access`, `duplicate-deferred`, `duplicate-non-overridable`, `duplicate-nopass`, `duplicate-pass` |
| C784 | 6 | 2 | 4 | 0 | `no-dummy`, `only-unrelated-dummy`, `typed-dummy-with-nopass`, `nonfirst-typed-dummy` |
| C785 | 3 | 1 | 2 | 0 | `nonexistent-dummy-name`, `first-dummy-name` |
| C786 | 5 | 2 | 3 | 0 | `bare-pass-conflict`, `named-pass-conflict`, `distinct-binding-choices` |
| C787 | 3 | 1 | 2 | 0 | `incompatible-attributes`, `separate-bindings` |
| C788 | 4 | 2 | 2 | 0 | `interface-without-deferred`, `deferred-without-interface`, `both-present-admission`, `both-absent-admission` |
| C789 | 3 | 1 | 2 | 0 | `nondeferred-to-deferred`, `deferred-to-deferred` |
| C790 | 4 | 1 | 3 | 0 | `forbidden-accessible-override`, `inherited-nonoverridable-admission`, `different-binding-name-admission` |
| S7.5.5-001 | 2 | 0 | 0 | 2 | `omitted-explicit-same-name`, `renamed-accessible-target` |
| S7.5.5-003 | 2 | 0 | 0 | 2 | `concrete-interface-call`, `deferred-interface-call` |
| S7.5.5-004 | 2 | 0 | 0 | 2 | `two-statement-name-union`, `declaration-order-independence` |
| S7.5.5-005 | 1 | 0 | 0 | 1 | `explicit-public-over-private` |
| S7.5.5-006 | 2 | 0 | 0 | 2 | `private-type-public-object`, `public-generic-private-specific` |
| S7.5.5-007 | 11 | 5 | 6 | 0 | `descendant-control`, `unrelated-client-call`, `unrelated-module-call`, `private-generic-name-call`, `inherited-private-call` |

## Complete finite pending plans

This appendix is generated from the canonical JSON `pending` maps, not a stale copy of the original all-pending source packet. Every row below remains **PENDING**; no compiler observation supplies a missing evidence mechanism.

### Pending R746

* **`empty-binding-part-source-use`** — PENDING — Source-use: preserve R726_valid__empty_binding_part, a compile-only admission of a CONTAINS-only ordinary type. Proposed R746 credit needs a positive-only numbered-wrapper mechanism, which is unavailable; do not duplicate or relabel that case.
* **`binding-union-source-use`** — PENDING — Source-use: finite union of R748's specific, generic and final alternatives, with its R749/R751/R753 dependencies. Grammar permits their interleaving after PRIVATE; individual member admission is not yet union evidence and no numbered-wrapper link is currently representable.

### Pending R747

* **`bare-keyword-source-use`** — PENDING — Source-use: use R746's private-only admission after independent fixture review; this remains a positive-only numbered-target graph proposal, not a supported evidence link or a second program.

### Pending R748

* **`specific-member-source-use`** — PENDING — Source-use: R749 concrete and deferred declaration admissions, preserving C774/C784/C788 and C738. Keep the union edge pending; member cases are not duplicated under R748.
* **`generic-member-source-use`** — PENDING — Source-use: R751's seven generic-spec families with C776-C782 and canonical generic distinguishability. Require reviewed member admissions and conditions; no wrapper-union mechanism currently accepts this numbered target.
* **`final-member-source-use`** — PENDING — Source-use: canonical R753/C791-C794 compile admission of FINAL naming a module subroutine with one scalar TYPE(t), nonoptional noncoarray nonpointer nonallocatable dummy, no OUT or VALUE, and assumed lengths if present. No finalization is invoked or inferred; owner remains 7.5.6.

### Pending R749

* **`bare-concrete-source-use`** — PENDING — Source-use: an omitted target without attributes necessarily defaults to PASS; reuse the canonical first-dummy admission/effect under 7.5.4.5 and S7.5.5-001, not an invalid zero-dummy bare declaration or a new duplicate runtime.
* **`interface-name-canonical-source-use`** — PENDING — Source-use: R1516/C1515/C1516, including abstract versus explicit-procedure alternatives, prior declaration when applicable, Table 16.2 intrinsic eligibility, and forbidden intrinsic-type keywords. Future canonical negatives must repair only the name/declaration premise; do not import R1512-only constraints or give their diagnostics to R749.

### Pending R750

* **`omitted-target-source-use`** — PENDING — Source-use: S7.5.5-001's same-name comparison, with default PASS supplied by 7.5.4.5 when no NOPASS is present. No positive-only or supplementary-runtime reuse mechanism currently gives this numbered wrapper credit.

### Pending C773

* **`no-target-source-use`** — PENDING — Source-use: R749's no-attribute bare form and S7.5.5-001 establish that omitted targets need no separator. Keep this boundary edge pending, not a second copy of their program.

### Pending C774

* **`other-target-categories-source-use`** — PENDING — Source-use: classify intrinsic, generic, dummy and procedure-pointer targets at their defining procedure/name owners before any further negative. No unsupported facility, undefined pointer target or arbitrary nonprocedure spelling is a C774 oracle.

### Pending C775

* **`scope-collision-source-use`** — PENDING — Source-use: Clause 19 class-(2) collisions, named-interface-list declarations, note1's generic/specific distinction and overriding in 7.5.7.3 retain their own scopes. Future canonical name-conflict negatives must rename just the colliding declaration and preserve every reference; no local duplicate credit yet.

### Pending R751

* **`seven-family-union-source-use`** — PENDING — Source-use: exactly generic-name, OPERATOR, ASSIGNMENT, READ(FORMATTED), READ(UNFORMATTED), WRITE(FORMATTED), WRITE(UNFORMATTED). Reuse their R1508/R1509/C779-C782 canonical admissions only after review; no arbitrary numbered-wrapper or positive-only union is currently supported.

### Pending C776

* **`nongeneric-name-family-source-use`** — PENDING — Source-use: repeat the same access comparison for operator/assignment/I/O generic identifiers only after canonical distinguishability is established. Multiple I/O specifics of one type may require genuinely distinct KIND parameters, never arbitrary intrinsic kind-code ordering. No additional negative is authorized by this graph alone.

### Pending C778

* **`override-membership-source-use`** — PENDING — Source-use: 7.5.7.2 p3 and 7.5.7.3 correspondence govern a child overriding a generic's inherited specific. Preserve that canonical binding identity before any duplicate-membership negative; a changed implementation alone is not a new generic member.

### Pending C779

* **`canonical-passed-object-source-use`** — PENDING — Source-use: 7.5.4.5 p1-p4/C765 supplies default/explicit PASS selection and all dummy eligibility. Keep its tests owned there; C779 only adds the non-name-generic requirement.

### Pending C780

* **`function-category-source-use`** — PENDING — Source-use at 15.4.3.4.2 p1: future canonical negative replaces the function by a subroutine with the same dummies; repair restores function category plus its defined scalar result. A report must identify an operator specific that is not a function. No numbered-wrapper link exists.
* **`unary-binary-arity-source-use`** — PENDING — Source-use at the same owner: admit one- and two-dummy functions; prospective three-dummy negative is repaired by removing only the extra unused nonoptional dummy/declaration. Keep a valid first passed object. Require operator arity, not argument association recovery.
* **`dummy-data-nonoptional-source-use`** — PENDING — Source-use: separately replace a nonpassed dummy data object by a dummy procedure, or add OPTIONAL to the nonpassed data dummy of a binary function. Repairs restore the data declaration or remove OPTIONAL. Keep the passed-object constraints intact and diagnose the operator dummy's actual role.
* **`intent-or-value-source-use`** — PENDING — Source-use: distinguish INTENT(IN) from permitted VALUE on a nonpassed dummy. A canonical negative using neither is repaired by adding INTENT(IN). Do not offer VALUE as the repair for the passed object; C765 forbids it.
* **`result-length-source-use`** — PENDING — Source-use: the assumed-character-length result exclusion remains at 15.4.3.4.2. Before a negative is authorized, isolate it from the separate module/external function-result restrictions; a valid fixed-length function admission alone is not proof of reporting the excluded case.
* **`intrinsic-operator-boundary-source-use`** — PENDING — Source-use: finite unary and binary intrinsic-operator extensions need matching intrinsic arity and a genuine T/K/R distinction; CLASS(*) supplies no type/kind distinction. Canonical controls must use symbolic KIND values only if supported, or a derived-type distinction; no arbitrary kind ordering.
* **`operand-and-generic-effects-source-use`** — PENDING — Source-use at 15.4.3.4.2 p2-p3 and 15.5.6: unary operand maps to the sole dummy, binary left/right to first/second; extending one relational spelling extends its equivalent spelling. A later runtime must check separately assigned exact integer/logical results from fully defined operands, not just successful operator parsing.

### Pending C781

* **`subroutine-and-arity-source-use`** — PENDING — Source-use at 15.4.3.4.3: extra unused third dummy is repaired by removing it/declaration. For the separate category negative, an otherwise valid INTEGER function f(lhs,rhs) assigns lhs%value=rhs and result f=0; repair changes its opening/ending to SUBROUTINE and removes only the result assignment. Keep both dummy declarations and the LHS update. Require assignment-specific category/arity, never general CALL mismatch.
* **`nonoptional-data-source-use`** — PENDING — Source-use: independently add OPTIONAL to the nonpassed RHS dummy, or substitute a dummy procedure; remove OPTIONAL or restore the ordinary RHS data declaration. Both repairs retain the valid passed-object LHS and two-dummy contract.
* **`lhs-intent-source-use`** — PENDING — Source-use: choose first CLASS(t) dummy and otherwise valid scalar INTEGER RHS. A missing LHS intent is repaired by adding INTENT(INOUT). Canonical cause must be defined-assignment LHS intent, not an attempt to read an undefined INTENT(OUT) payload.
* **`rhs-intent-value-source-use`** — PENDING — Source-use: INTENT(IN) and VALUE alternatives belong at the canonical assignment owner. Remove the RHS's required intent/value then repair by adding INTENT(IN); never assume VALUE is legal for whichever dummy is the passed object.
* **`rank-type-kind-disjunction-source-use`** — PENDING — Source-use: preserve all three disjuncts rather than requiring all of them. Bound type LHS supplies the derived-type alternative in the initial admission; tests of the other branches remain canonical assignment work with exact rank and supported kind premises.
* **`parenthesized-rhs-and-effect-source-use`** — PENDING — Source-use: later runtime may assign scalar integer 7 to a live t LHS through a subroutine that writes lhs%value=rhs. The public result query must yield 7 after return; use INOUT and no finalizable/pointer/allocatable components. Parenthesized RHS has no POINTER/ALLOCATABLE/TARGET attribute. Own the effect at 10.2.1.4-.5 and 15.4.3.4.3, not a duplicate C781 program.

### Pending C782

* **`read-formatted-source-use`** — PENDING — Source-use: canonical compile admission of the six-argument READ(FORMATTED) interface with CLASS(t), INOUT dtv first and all remaining characteristics as defined above. Use default kinds, arbitrary valid dummy names and explicit PASS of the local dtv name or default PASS. No transfer result is claimed.
* **`write-formatted-source-use`** — PENDING — Source-use: canonical six-argument WRITE(FORMATTED) admission differs in dtv INTENT(IN), not in iomsg intent or array rank. No change to iomsg on successful return and iostat=0 belong to execution contracts, not an interface-only compile observation.
* **`read-unformatted-source-use`** — PENDING — Source-use: canonical four-argument READ(UNFORMATTED) admission retains dtv INOUT, unit IN, iostat OUT and iomsg INOUT; it has neither iotype nor v_list. Do not require supplying the other three I/O directions/forms.
* **`write-unformatted-source-use`** — PENDING — Source-use: canonical four-argument WRITE(UNFORMATTED) admission has dtv IN and the same three remaining roles. File representation and processor-specific unit numbers are not admissible interface oracles.
* **`io-characteristics-repair-source-use`** — PENDING — Source-use: prospective canonical negative changes only INTEGER, INTENT(OUT) iostat to INTENT(IN); repair restores OUT. Separately change v_list to scalar in a formatted interface and repair to (:). Bodies do not write the malformed dummy. Require the named I/O role/characteristic, not an unrelated assignment or rank error.
* **`dtv-defining-type-isolation`** — PENDING — Source-use, conditional: in a valid type-bound I/O interface dtv is the only derived-type dummy and must be the passed object. Giving it another type also violates C765; adding NOPASS additionally violates C779. No isolated C782 wrong-dtv negative is authorized. A later reviewer must distinguish these causes or retain a canonical C765 link proposal without false C782 credit.
* **`io-execution-resolution-source-use`** — PENDING — Source-use: 12.6.4.8.3-.4 owns invocation, selected interface from declared type followed by dynamic binding, unit/iotype/v_list, iostat/iomsg and I/O-state restrictions. Future effect: initialized scalar payload 17 written with DT into a sufficiently long internal character record; child writes I2, iostat=0, leaves iomsg unchanged; assert '17' plus defined blank padding only after successful output. No external-unit introspection or unformatted-byte oracle.

### Pending R752

* **`access-union-source-use`** — PENDING — Source-use: finite PUBLIC/PRIVATE access-spec alternatives at R807, C817 and p8-p9. Preserve actual module declaration scope; positive-only numbered-wrapper reuse is not currently representable.
* **`deferred-union-source-use`** — PENDING — Source-use: C788's iff relation and C738's abstract-type condition must accompany the DEFERRED token; never compile an isolated keyword in a nonabstract concrete binding as a valid grammar control.
* **`non-overridable-union-source-use`** — PENDING — Source-use: a concrete NON_OVERRIDABLE declaration under C787/C790, not an assertion about static dispatch machinery. Reuse remains pending.
* **`nopass-and-pass-source-use`** — PENDING — Source-use: NOPASS, bare PASS and PASS(arg-name) under C784-C786 and canonical 7.5.4.5 p2-p4/C765. Preserve the default-PASS case even though there is no PASS token.

### Pending C783

* **`access-alternative-identity-source-question`** — PENDING — Source-use, unresolved attribution: determine from R752/R807, C783, C815 and accessibility semantics whether PUBLIC, PRIVATE in one binding list is reported specifically as repetition of the same binding-attr or another incompatible-access relationship. A candidate repair deletes one access attribute, but no dedicated negative or required C783 cause is authorized until this fine-unit review.

### Pending C784

* **`default-pass-eligibility-source-use`** — PENDING — Source-use: missing/invalid first passed dummy when a later defining-type dummy exists is a 7.5.4.5/C765 issue, not proof that C784's antecedent held. Preserve that canonical negative and repair scope.

### Pending C785

* **`nonfirst-dummy-name-source-use`** — PENDING — Source-use: canonical 7.5.4.5 p4/15.5.2.2 runtime selects the second dummy self while reduced-list positional/keyword arguments fill the first. With defined receiver payload 7 and other integer 3, return 73 from 10*self%value+other; do not claim this effect by compile admission.
* **`named-interface-dummy-source-use`** — PENDING — Source-use: S7.5.5-003 and R1516 establish the DEFERRED interface's dummy names. A future misspelling repair must change only PASS's name and keep ABSTRACT/DEFERRED/imports valid; no concrete procedure body is required for the declaration.

### Pending C789

* **`concrete-implementation-source-use`** — PENDING — Source-use: preserve C738_valid__concrete_override and the canonical 7.5.7.3 interface/correspondence rules. Concrete implementation of every inherited deferred binding can permit a nonabstract child; no duplicate case or positive-only link is created.
* **`private-homonym-source-use`** — PENDING — Source-use: an inaccessible private parent binding is inherited but is not overridden by an unrelated child-scope homonym. Before any boundary fixture, retain C738's inherited deferred obligation and original accessibility; same spelling is not enough to trigger C789.

### Pending C790

* **`inaccessible-private-boundary-source-use`** — PENDING — Source-use: 7.5.7.3 p1 requires an accessible parent binding for override. A new same-spelled child binding in an unrelated module does not override an inaccessible private parent binding; retain both identities and do not relabel this as a C790 rejection.
* **`dispatch-source-use`** — PENDING — Source-use at 15.5.6/7.5.7.2: a live concrete child passed to CLASS(parent) has child dynamic type; an ordinary overridden specific may return 22 rather than parent implementation 11, whereas an inherited NON_OVERRIDABLE implementation remains the parent's procedure. Observe results only, not static versus indirect generated instructions.

### Pending S7.5.5-001

* **`target-eligibility-source-use`** — PENDING — Source-use: C774's target category/access and explicit-external-interface conditions also apply to the inferred name. Do not duplicate those diagnostic/control pairs under this effect requirement; any proposed supplementary link needs one same-numbered-rule pair and independent source review.

### Pending S7.5.5-002

* **`local-abstract-condition-source-use`** — PENDING — Source-use: candidate same-rule pair is C738_invalid__local_deferred and C738_valid__local_deferred_repair at tests/fixtures/derived_definition_c738_{invalid__local_deferred,valid__local_deferred_repair}/fixture.json. Actual source differs only by ABSTRACT in the TYPE header. The negative must report a nonabstract type containing a deferred binding; the repair is compile-only. No link is installed or approved here.
* **`inherited-deferred-boundary-source-use`** — PENDING — Source-use: inherited obligations remain C738/7.5.7.2, including inaccessible inherited deferred bindings. Preserve existing C738 inherited/private-inherited case IDs and paths; do not relabel their compile controls as local p2 runtime effects or add them to a single unsupported multi-pair link.

### Pending S7.5.5-003

* **`interface-name-domain-source-use`** — PENDING — Source-use: R1516/C1515/C1516 admits abstract interfaces and eligible explicit procedure interfaces, with declaration-order and intrinsic-table conditions. An ABSTRACT-INTERFACE-only assumption is forbidden; those canonical name-domain controls stay pending rather than being copied here.
* **`declared-versus-dynamic-source-use`** — PENDING — Source-use at 15.5.6: specific names resolve in dynamic type; generic name selects a specific from the declared type's generic interface, then resolves the corresponding name in dynamic type. A child-only overload cannot be used through a parent-declared generic merely because the dynamic object is the child. Require separate canonical admission/dispatch evidence, not a fictional SAME_TYPE_AS or static-code-generation oracle.
* **`call-characteristics-source-use`** — PENDING — Source-use: keyword presence, function/subroutine form, type/kind/rank and argument restrictions remain C1523/C1525/C1532 and 15.5.2. A prospective wrong-keyword negative is repaired by changing only that keyword to scale; its canonical cause is the interface dummy name, not a local p6 rejection policy.

### Pending S7.5.5-004

* **`nameless-generic-union-source-use`** — PENDING — Source-use: OPERATOR, ASSIGNMENT and all four I/O forms must accumulate consistently with C776/C778/C1511-C1513. Their member-distinguishability prerequisites, including kind-parameter distinctions if necessary for I/O, are canonical gates; the two named-generic calls do not close this union.
* **`inherited-generic-extension-source-use`** — PENDING — Source-use: 7.5.7.2 p3 and 15.5.6 govern declared-type inherited generic membership and dynamic corresponding-specific dispatch. Preserve C778's no-re-addition condition and source ownership; this graph is not another local p7 program.

### Pending S7.5.5-005

* **`default-public-binding`** — PENDING — Source-use: preserve S7_5_2_2_001_valid__component_private, which already has module default PRIVATE, component PRIVATE, unspecified getter/setter binding access, private implementation names and explicit public type/object exports. Its separate client calls the default-public bindings and observes defined values 13/17/19. Finite S-owned runtime reuse is available, but this connection still needs independent semantic adjudication and registration. Keep its S7.5.2.2-001 ownership rather than author a second copy under p8.
* **`default-private-binding`** — PENDING — Source-use: S7_5_5_007_invalid__tbp_default_private_client and its repair provide the actual named outside-call contrast, changing only the bare binding PRIVATE default. The available finite S-owned pair pattern still requires independent semantic adjudication and registration for this consumer. A defining-module wrapper call is an allowed-use control, not proof of the private default; preserve S7_5_2_2_001_valid__binding_private rather than duplicate it or promote it to an inaccessibility effect.
* **`explicit-private-over-public`** — PENDING — Source-use: preserve S7_5_5_007_invalid__tbp_explicit_private_client and its PRIVATE-to-PUBLIC repair. This actual outside-call pair distinguishes explicit private access from the otherwise public default. An internal wrapper returning17 would work even if PRIVATE were ignored and cannot prove the override. Finite S-owned pair reuse is supported, but this consumer remains pending independent semantic adjudication and registration; no duplicate effect is introduced.
* **`inherited-default-origin`** — PENDING — Source-use at 7.5.7.2 p1: a future canonical runtime inherits a PUBLIC parent binding into a child whose own binding part says PRIVATE; the inherited call must still return initialized payload 17. The original attribute, not the child's default, controls it. Retain inheritance ownership and pending S-runtime reuse, not a duplicate p8 program.
* **`generic-default-source-use`** — PENDING — Source-use: C776's effective access comparisons and S7.5.5-006's public generic/private specific plan supply generic defaults. Preserve named versus nameless scope differences and explicit access; no unimplemented positive-only runtime linkage is asserted.

### Pending S7.5.5-006

* **`public-type-public-object`** — PENDING — Source-use: preserve S7_5_2_2_001_valid__public_public, whose public type/object and explicitly PUBLIC bindings already let the client define the public payload, call get/set and observe13/17/19. Its module default PRIVATE does not weaken these explicit publicity premises. A finite S-owned runtime connection is now structurally supported, but independent semantic adjudication and registration remain pending. A new public-call program would duplicate the existing behavior.
* **`private-implementation-name`** — PENDING — Source-use: S7_5_2_2_001_valid__component_private has private implementation procedure names, callable default-public bindings and defined client observations. Preserve its actual S7.5.2.2-001 primary owner. The finite S-owned runtime pattern does not automatically adjudicate this semantic connection; registration and independent review remain pending, with no new duplicate program or claim of direct private-name rejection.
* **`nameless-binding-access-source-use`** — PENDING — Source-use: public operator/assignment/I/O bindings are available in their canonical expression/assignment/transfer contexts via accessible objects. They have no binding name under p5 and are not imported individually in USE ONLY (C1407/C1408). Keep those owner-specific invocation graphs pending.

### Pending S7.5.5-007

* **`defining-module-control`** — PENDING — Source-use: preserve S7_5_2_2_001_valid__binding_private and S7_5_2_2_001_valid__private_private, which invoke private bindings legally inside their defining module through public wrappers. These are allowed owner-use contexts, not proof of outside inaccessibility. Finite S-owned reuse is available, but the actual bounded connection and its control meaning still require independent review and registration. No duplicate program or automatic privacy-effect credit is supplied.
* **`nameless-private-source-use`** — PENDING — Source-use: private operators, assignment and I/O bindings have no binding names; canonical availability/resolution rules must establish any proposed negative and its reporting basis. A generic 'operator not defined' or missing I/O implementation report alone cannot establish this access cause. No such negative is authorized yet.

## Reproduction and gates

`python3 -B tools/generate_type_bound_fixtures.py --check` checks exact generated bytes and both document views. `tests/test_type_bound_fixtures.py` checks the case/facet/pending census, phases, unique repairs, source premises and diagnostic guards. The author worktree's validation-only index overlay is not merged wholesale. Coordinator index integration and source/case/evidence adjudications are separate from code generation and never inferred from compiler observations.

Consult current content-bound fixture and diagnostic-cause adjudications before integration. Retained observation handoffs and per-case current-fingerprint indexes record reference failures and source-valid controls without inventing a combined run. The original `type-bound-phase2-handoff.json` and subsequent correction handoffs remain separate provenance records; this view does not confer approval.
