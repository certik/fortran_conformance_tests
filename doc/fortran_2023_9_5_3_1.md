# Fortran 2023 9.5.3.1: Array elements and array sections — Syntax

The canonical catalogue is `doc/catalogues/array_element_section_syntax_9_5_3_1.json`. Effective review state comes from `Registry.catalogue_review_state`; this source-only packet does not approve cases, canonical relationships or an execution inventory.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Twenty-two base units: R917, C924, R918, C925, C926, R919, R920, C927, R921-R925, C928-C931, p1, p2, and NOTE 1 through NOTE 3. Fifty-seven fine units record grammar alternatives, constraints, prose effects, bounds restrictions, and informative examples.

Selected array element and section syntax facets now have executable positive-control fixtures; unsupported @ multiple-subscript forms, diagnostics, assumed-size omission constraints, and remaining source plans stay pending.

<!-- BEGIN ARRAY SECTION SYNTAX FIXTURES -->
## Array element and section syntax fixture observations

Eleven complete run/f2023 fixtures cover twenty selected facets of 9.5.3.1. They use nondefault lower bounds, distinct integer values, and distinct fixed-length character values so wrong bound, stride, vector order, subscript-order, rank, or substring-length behavior changes a checked result. Character assertions check LEN before equality. No numeric formatted-output oracle is used.

The generator records conforming feature mutations for every fixture, including removing the tested subscript list, section triplet stride, vector subscript, or substring range; changing stride sign; omitting or supplying a triplet bound; and swapping subscript/vector order. Multiple-subscript @ forms, multiple-subscript-triplets, assumed-size omission constraints, negatives, zero stride, nonfinal component sections, and complex part designators remain pending.
<!-- END ARRAY SECTION SYNTAX FIXTURES -->

<!-- BEGIN GENERATED 9.5.3.1 -->

### R917: An array element is a data reference

**Source:** 9.5.3.1 R917, J3/24-007, 18 December 2023, physical PDF154. **Class:** Syntax.

**Definition:** The grammar for array-element is exactly data-ref. C924 supplies the additional
rank-zero and final-subscript-list restrictions; 9.4.2 owns data-ref and part-ref
formation.

**Diagnostic obligation:** required.

**Facets:** `data-ref-array-element-form`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program selects
box%c(k) from a scalar derived-type data-ref with a nondefault component lower bound and
distinct values, then places that scalar element in a one-element constructor. The
observer requires size one and value 202; removing the final subscript contributes the
whole component array and changing the subscript selects 303.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.1.3;4.2;9.1R901;9.4.2R911-R912/C913/C919/p2-p3;9.5.3.1C924/p1-p2.

### C924: Every part reference in an array element has rank zero and the last part reference has a subscript list

**Source:** 9.5.3.1 C924, J3/24-007, 18 December 2023, physical PDF154. **Class:** Restriction.

**Definition:** For an R917 array-element, every part-ref in the data-ref shall have rank zero, and the
last part-ref shall contain a subscript list. This excludes array sections and whole
arrays from the array-element category while allowing component chains whose parent
parts are scalar or are reduced to scalar elements.

**Diagnostic obligation:** required.

**Facets:** `all-part-refs-rank-zero`, `last-part-ref-has-subscript-list`, `nonzero-rank-part-ref-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program observes
box%child%c(j) through a scalar parent chain and a one-element constructor. The
rightmost part-ref has the only subscript list and every part-ref is rank zero; removing
that list contributes the rank-one component array and selecting c(1) yields 803.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.2;7.5;9.4.2R911-R912/C913/C919/p2-p3;9.5.2;9.5.3.1R917/R921-R925.

### R918: An array section is a data reference with optional substring range or a complex part designator

**Source:** 9.5.3.1 R918, J3/24-007, 18 December 2023, physical PDF154. **Class:** Syntax.

**Definition:** The grammar for array-section admits a data-ref, optionally followed by a parenthesized
substring-range, or a complex-part-designator. C925 and C926 supply rank and character
restrictions; 9.4.4 owns complex-part-designator details.

**Diagnostic obligation:** required.

**Facets:** `data-ref-section-form`, `data-ref-with-substring-range-form`, `complex-part-designator-section-form`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program observes an
integer data-ref section a(-1:3:2) with shape three and values [22,44,66], and a
character array-section substring words(:)(2:4) with length three and values
['bcd','wxy','mno']. Removing the stride or substring range changes rank-one extent,
values, or character length.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.1.3;4.2;9.4.1R908-R910/C908;9.4.2R911-R912/C913/C919/p2-p3;9.4.4R915/C922/p1;9.5.3.1C925-C926/p1;9.5.3.4.

### C925: An array section has exactly one nonzero-rank source and must be justified by section subscripts, another nonzero-rank part, or an array complex part

**Source:** 9.5.3.1 C925, J3/24-007, 18 December 2023, physical PDF154. **Class:** Restriction.

**Definition:** For an R918 array-section, exactly one part-ref shall have nonzero rank. In addition,
either the final part-ref has a section-subscript-list with nonzero rank, another
part-ref has nonzero rank, or the complex-part-designator is an array. This catalogue
records the complex-part wording as source-sensitive: the complex-part alternative is
admitted through 9.4.4, while data-ref rank and part-ref rank remain owned by 9.4.2.

**Diagnostic obligation:** required.

**Facets:** `exactly-one-nonzero-rank-part-ref`, `final-section-subscript-list-nonzero-rank`, `nonfinal-part-ref-nonzero-rank`, `complex-part-designator-array`, `zero-rank-section-rejected`, `multiple-nonzero-rank-parts-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program observes
a(-1:3:2), whose final part-ref section-subscript-list has nonzero rank and is the only
nonzero-rank part-ref. The observer checks rank one, size three, and values [22,44,66];
replacing the section by a one-element constructor or reversing the stride changes the
observed size, value, or order.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.2;7.5;9.4.2C913/C919/p2-p3;9.4.4R915/C922/p1;9.5.3.1R918/R921-R925;9.5.3.4.

### C926: A substring range in an array section requires a character data reference

**Source:** 9.5.3.1 C926, J3/24-007, 18 December 2023, physical PDF154. **Class:** Restriction.

**Definition:** If a substring-range appears in the data-ref form of an R918 array-section, the data-ref
shall be of type character. This constraint does not define substring bounds or
character length; 9.4.1 owns substring-range formation and character substring
semantics.

**Diagnostic obligation:** required.

**Facets:** `character-data-ref-with-substring-range`, `noncharacter-data-ref-with-substring-range-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program applies
words(:)(2:4) only to a CHARACTER data-ref. The observer checks element length before
equality and then the three substrings ['bcd','wxy','mno']; removing the substring range
leaves length five and omitting the lower section bound drops an element.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.2;7.4.4 character type;9.4.1R908-R910/C908/p2-p3;9.4.2;9.5.3.1R918/p1.

### R919: A subscript is a scalar integer expression

**Source:** 9.5.3.1 R919, J3/24-007, 18 December 2023, physical PDF154. **Class:** Syntax.

**Definition:** The grammar for subscript is scalar-int-expr. The value bounds of a subscript in an
array element are specified separately by 9.5.3.1 p2, and section-subscript rank effects
are specified by 9.4.2 and 9.5.3.4.

**Diagnostic obligation:** required.

**Facets:** `literal-scalar-int-subscript`, `variable-scalar-int-subscript`, `noninteger-or-nonscalar-subscript-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.1.3;4.2;7.4.1 integer type;10.1 expressions;9.5.3.1p2;9.5.3.4.

### R920: A multiple subscript is an at sign followed by an integer expression

**Source:** 9.5.3.1 R920, J3/24-007, 18 December 2023, physical PDF154. **Class:** Syntax.

**Definition:** The grammar for multiple-subscript is @ followed by int-expr. C927 requires that
expression to be a rank-one array, and 9.5.3.2 owns the expansion into consecutive
dimensions.

**Diagnostic obligation:** required.

**Facets:** `at-token-present`, `int-expr-after-at`, `missing-at-token-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.1.3;4.2;7.4.1;9.4.2C913/p2;9.5.3.1C927/R921;9.5.3.2.

### C927: The integer expression in a multiple subscript is rank one array-valued

**Source:** 9.5.3.1 C927, J3/24-007, 18 December 2023, physical PDF154. **Class:** Restriction.

**Definition:** The int-expr in a multiple-subscript shall be an array of rank one. Scalar expressions
and arrays of rank other than one do not satisfy the multiple-subscript constraint.

**Diagnostic obligation:** required.

**Facets:** `rank-one-array-int-expr`, `scalar-int-expr-rejected`, `rank-two-int-expr-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.2;5.4.6 rank;7.4.1;10.1;9.5.3.1R920;9.5.3.2p1.

### R921: A section subscript is one of five subscript forms

**Source:** 9.5.3.1 R921, J3/24-007, 18 December 2023, physical PDF154. **Class:** Syntax.

**Definition:** The grammar for section-subscript admits exactly: subscript, multiple-subscript,
subscript-triplet, multiple-subscript-triplet, or vector-subscript. Rank contribution
and sequence semantics are owned by 9.4.2 p2, 9.5.3.2, and 9.5.3.4.

**Diagnostic obligation:** required.

**Facets:** `subscript-alternative`, `multiple-subscript-alternative`, `subscript-triplet-alternative`, `multiple-subscript-triplet-alternative`, `vector-subscript-alternative`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program exercises a
scalar subscript alternative m(0,:), a subscript-triplet alternative a(-1:3:2), and a
vector-subscript alternative a([3,-2,1]). Distinct coordinate-coded values make swapping
subscript order, removing the triplet stride, or replacing the vector by a triplet fail.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.1.3;4.2;9.4.2C913/p2;9.5.3.1R919-R920/R922-R925/C927-C929;9.5.3.2;9.5.3.4.

### R922: A subscript triplet has optional first and second subscripts and optional stride around a required colon

**Source:** 9.5.3.1 R922, J3/24-007, 18 December 2023, physical PDF155. **Class:** Syntax.

**Definition:** The grammar for subscript-triplet is optional subscript, required colon, optional
subscript, and optional colon followed by stride. C930 restricts omission of the second
subscript in the last dimension of an assumed-size array; 9.5.3.4 owns sequence and
default-value semantics.

**Diagnostic obligation:** required.

**Facets:** `lower-and-upper-triplet`, `omitted-first-subscript`, `omitted-second-subscript`, `explicit-stride`, `missing-colon-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program covers
lower-and-upper triplet a(-1:1), omitted first subscript a(:1), omitted second subscript
a(1:), and explicit stride a(-2:2:2), all on an array with lower bound -2. Mutants omit
or supply a bound or remove the stride and thereby change shape or values.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.1.3;4.2;9.5.3.1R919/R924/C930;9.5.3.4.1-.2;8.5.8.5.

### R923: A multiple subscript triplet starts with at sign and has optional integer expressions around required colons

**Source:** 9.5.3.1 R923, J3/24-007, 18 December 2023, physical PDF155. **Class:** Syntax.

**Definition:** The grammar for multiple-subscript-triplet is @, optional int-expr, colon, optional
int-expr, and optional colon followed by int-expr. C928 requires at least one rank-one
array expression and conformability; C931 restricts omission of the second int-expr for
assumed-size last-section-subscript cases.

**Diagnostic obligation:** required.

**Facets:** `at-token-triplet`, `first-int-expr-present`, `first-int-expr-omitted`, `second-int-expr-present`, `second-int-expr-omitted`, `third-int-expr-present`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.1.3;4.2;9.5.3.1C928/C931;9.5.3.2;9.5.3.4.1-.2;8.5.8.5.

### C928: A multiple subscript triplet has a rank-one array integer expression and conformable integer expressions

**Source:** 9.5.3.1 C928, J3/24-007, 18 December 2023, physical PDF155. **Class:** Restriction.

**Definition:** A multiple-subscript-triplet shall have at least one int-expr that is a rank-one array.
The int-exprs in a multiple-subscript-triplet shall be conformable. Scalar int-exprs are
allowed only when at least one conformable rank-one array int-expr supplies the sequence
size.

**Diagnostic obligation:** required.

**Facets:** `at-least-one-rank-one-array`, `scalar-broadcast-with-array-control`, `nonconformable-int-exprs-rejected`, `no-array-int-expr-rejected`, `rank-two-array-int-expr-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.2;5.4.6 rank and conformance;7.4.1;10.1;9.5.3.1R923;9.5.3.2p2-p3.

### R924: A stride is a scalar integer expression

**Source:** 9.5.3.1 R924, J3/24-007, 18 December 2023, physical PDF155. **Class:** Syntax.

**Definition:** The grammar for stride is scalar-int-expr. The nonzero stride restriction and
positive/negative sequence semantics are owned by 9.5.3.4.2.

**Diagnostic obligation:** required.

**Facets:** `literal-scalar-int-stride`, `variable-scalar-int-stride`, `nonscalar-or-noninteger-stride-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program observes
both literal scalar stride a(-2:2:2) and variable scalar stride a(2:-2:step) with
step=-2. Removing either stride or changing the variable stride sign is conforming but
changes the selected sequence.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.1.3;4.2;7.4.1;10.1;9.5.3.1R922;9.5.3.4.2p1-p4.

### R925: A vector subscript is an integer expression

**Source:** 9.5.3.1 R925, J3/24-007, 18 December 2023, physical PDF155. **Class:** Syntax.

**Definition:** The grammar for vector-subscript is int-expr. C929 requires that expression to be an
integer array expression of rank one; definition, duplicate-element definability, and
finalization restrictions for vector-subscript sections are owned by 9.5.3.4.3.

**Diagnostic obligation:** required.

**Facets:** `int-expr-vector-subscript-form`, `vector-expression-order-source`, `non-int-expr-vector-subscript-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program uses the
integer expression idx as the vector subscript in a(idx), yielding values [66,11,44].
Reordering idx or replacing the vector subscript by a triplet is conforming and changes
the selected elements.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.1.3;4.2;7.4.1;10.1;9.5.3.1C929;9.5.3.4.1p1-p2;9.5.3.4.3p1-p3.

### C929: A vector subscript is a rank-one integer array expression

**Source:** 9.5.3.1 C929, J3/24-007, 18 December 2023, physical PDF155. **Class:** Restriction.

**Definition:** A vector-subscript shall be an integer array expression of rank one. This constraint
establishes type and rank only; it does not own bounds of the resulting values,
definedness of expression elements, duplicate-element definability, or finalization
restrictions.

**Diagnostic obligation:** required.

**Facets:** `integer-rank-one-array-expression`, `scalar-int-expr-rejected`, `rank-two-array-expression-rejected`, `noninteger-array-expression-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/positive-control/f2023 program uses the
rank-one integer array expression [3,-2,1] as a vector subscript and checks rank one
plus values [66,11,44]. Replacing it with a one-element constructor around a scalar
subscript changes size and value, while reordering the vector changes values.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 4.2;5.4.6;7.4.1;10.1;9.5.3.1R925;9.5.3.4.3.

### C930: The second subscript is required for a subscript triplet in the last dimension of an assumed-size array

**Source:** 9.5.3.1 C930, J3/24-007, 18 December 2023, physical PDF155. **Class:** Restriction.

**Definition:** For an R922 subscript-triplet in the last dimension of an assumed-size array, the second
subscript shall not be omitted. The assumed-size property and absence of a last upper
bound are owned by 8.5.8.5.

**Diagnostic obligation:** required.

**Facets:** `assumed-size-last-dimension-upper-present`, `nonlast-dimension-omission-control`, `ordinary-array-omission-control`, `assumed-size-last-dimension-upper-omitted-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.2;8.5.8.5p1/p4-p5;9.5.2S9.5.2-003;9.5.3.1R922;9.5.3.4.1-.2.

### C931: The second integer expression is required for a final multiple subscript triplet of an assumed-size array

**Source:** 9.5.3.1 C931, J3/24-007, 18 December 2023, physical PDF155. **Class:** Restriction.

**Definition:** If a multiple-subscript-triplet is the last section-subscript in the
section-subscript-list of an assumed-size array, the second int-expr shall appear. This
is the multiple-triplet counterpart of C930 and depends on 9.5.3.2 for expansion.

**Diagnostic obligation:** required.

**Facets:** `assumed-size-final-multiple-triplet-second-expr-present`, `nonfinal-multiple-triplet-omission-control`, `ordinary-array-multiple-triplet-omission-control`, `assumed-size-final-multiple-triplet-second-expr-omitted-rejected`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.2;8.5.8.5p1/p4-p5;9.5.3.1R923/C928;9.5.3.2;9.5.3.4.1-.2.

### S9.5.3.1-001: Array elements are scalar, array sections are arrays, and substring ranges apply elementwise

**Source:** 9.5.3.1 p1, J3/24-007, 18 December 2023, physical PDF155. **Class:** Effect.

**Definition:** An array element is scalar. An array section is an array. If a substring-range appears
in an array-section, each element of the result is the designated substring of the
corresponding element of the array section.

**Diagnostic obligation:** not-required.

**Facets:** `array-element-is-scalar`, `array-section-is-array`, `substring-range-applies-to-each-section-element`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

Array-section syntax fixture oracle: one run/effect/f2023 program observes words(:)(2:4)
as a rank-one character array whose element length is three and whose elements are the
substrings ['bcd','wxy','mno'] of the corresponding section elements. Removing the
substring range or dropping the lower section element changes length, size, or values.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

Array-section syntax fixture boundaries: these selected fixtures cover only ordinary
single-image default INTEGER arrays and default CHARACTER arrays with constant bounds
and defined values. Multiple-subscript @ forms, multiple-subscript-triplets,
assumed-size omission constraints, diagnostic negatives, zero stride, duplicate-vector
definability, finalization, coarrays, pointers, allocatables, complex part designators,
and nonfinal array-parent component sections remain pending or delegated to dependent
clauses. No I/O numeric formatting, rounding, optional plus sign, storage-layout,
address, undefined-value, or compiler-consensus oracle is used.

**Dependencies:** 5.4.6;7.4.4;9.4.1;9.4.2;9.4.4;9.5.3.1R917-R918/C924-C926;9.5.3.4.

### S9.5.3.1-002: An array element subscript value is within the bounds of its dimension

**Source:** 9.5.3.1 p2, J3/24-007, 18 December 2023, physical PDF155. **Class:** Restriction.

**Definition:** For an array element, the value of each subscript shall be within the bounds for its
dimension. This is a value restriction: constant out-of-bounds sources can be planned as
source-diagnostic candidates, while variable-valued subscripts require execution or
other context to establish the actual value. This p2 rule is for array elements;
array-section triplet and vector value rules are owned by 9.5.3.4.

**Diagnostic obligation:** context-dependent.

**Facets:** `constant-in-bounds-element`, `constant-out-of-bounds-element-source-candidate`, `runtime-valued-in-bounds-element`, `runtime-valued-out-of-bounds-element`.

**Oracle:** Every facet is a pending source plan. Positive-control plans use independently chosen
literal values, SHAPE/SIZE inquiries, or scalar results that first establish definedness
and reached execution; diagnostic plans pair an otherwise legal source with a minimal
repair and require the reported cause to match the stated grammar or semantic predicate,
not an unrelated parse, declaration, unsupported-feature, resource, or runtime failure.

**Oracle limitation:** This source-only catalogue creates no Fortran test program, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Numbered syntax rules and constraints carry the 4.2 detection and reporting duty, but
these plans do not require a fixed message, fatal rejection, nonzero status, or printed
rule number. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics; runtime-valued predicates remain runtime/source obligations unless a future
review separately establishes a static context.

**Dependencies:** 4.2;5.4.6;8.5.8.1-.6 bounds and
shape;9.4.2C913/p2-p3;9.5.3.1R917/C924/R919;9.5.3.3;9.5.3.4.

<!-- END GENERATED 9.5.3.1 -->
