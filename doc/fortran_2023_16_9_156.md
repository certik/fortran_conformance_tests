# Fortran 2023 16.9.156: NUM_IMAGES

The canonical catalogue is `doc/catalogues/num_images_16_9_156.json`. Effective review state is computed by the registry; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: body, p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.156 -->

### S16.9.156-001: NUM_IMAGES TEAM and TEAM_NUMBER argument restrictions

**Source:** S16.9.156, J3/24-007, 18 December 2023, physical PDF442. **Class:** Restriction.

**Definition:** TEAM shall be a scalar TEAM_TYPE identifying the current or an ancestor team;
TEAM_NUMBER shall be an integer scalar identifying the initial team or a sibling team of
the current team.

**Diagnostic obligation:** not-required.

**Facets:** `NUM_IMAGES-TEAM-scalar-current-or-ancestor-team`, `NUM_IMAGES-TEAM_NUMBER-initial-or-sibling-team`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets. Coarray, team, and multi-image observations
are marked OUT OF SCOPE for this suite rather than treated as no-oracle items.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155. NUM_IMAGES is image/team
dependent; multi-image and team facets are OUT OF SCOPE for this single-image suite by
user decision, while the bare single-image inquiry remains a concrete portable plan.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.156-002: NUM_IMAGES result is default integer scalar

**Source:** S16.9.156, J3/24-007, 18 December 2023, physical PDF442. **Class:** Effect.

**Definition:** The NUM_IMAGES result is a default integer scalar.

**Diagnostic obligation:** not-required.

**Facets:** `NUM_IMAGES-result-default-integer-scalar`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets. Coarray, team, and multi-image observations
are marked OUT OF SCOPE for this suite rather than treated as no-oracle items.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155. NUM_IMAGES is image/team
dependent; multi-image and team facets are OUT OF SCOPE for this single-image suite by
user decision, while the bare single-image inquiry remains a concrete portable plan.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

### S16.9.156-003: NUM_IMAGES returns number of images in current or specified team

**Source:** S16.9.156, J3/24-007, 18 December 2023, physical PDF442. **Class:** Effect.

**Definition:** NUM_IMAGES returns the number of images in the specified team, or in the current team
without a team argument.

**Diagnostic obligation:** not-required.

**Facets:** `NUM_IMAGES-current-team-single-image-count`, `NUM_IMAGES-specified-team-count`.

**Oracle:** Source registration only. Future fixtures initialize observed variables to non-default
sentinels before invoking the intrinsic and assert only source-derived properties fixed
by 16.9: exact logical, integer, kind, character, shape, association/allocation status,
or bit-context observations. Processor-dependent approximations are separated from exact
obligations. Unnumbered argument shall-restrictions remain source-control restrictions
and are not promoted to diagnostic facets. Coarray, team, and multi-image observations
are marked OUT OF SCOPE for this suite rather than treated as no-oracle items.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for NINT, NORM2, NOT, NUM_IMAGES, and
OUT_OF_RANGE found no existing binding for 16.9.152-16.9.154 or 16.9.156-16.9.157; NOT
appears widely only as the logical operator/spelling in unrelated fixtures. NULL appears
in existing R/C and 7/8/10/13 fixtures, including structure-constructor and
initialization cases, but no binding is made here to 16.9.155. NUM_IMAGES is image/team
dependent; multi-image and team facets are OUT OF SCOPE for this single-image suite by
user decision, while the bare single-image inquiry remains a concrete portable plan.

**Dependencies:** Registered dependencies: clauses 6 through 13 where already catalogued for source form,
intrinsic and derived types, declarations, expressions, pointer/allocatable status,
arrays, assignment, execution, and internal I/O helpers. Unregistered dependencies
retained: 16.3 integer bit model, 16.4 numeric model terminology, 16.10 ISO_FORTRAN_ENV
named constants and team types, clause 17 IEEE support inquiries for guarded
special-value plans, and clauses 14 through 19 where future fixtures need program,
procedure, interoperability, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.156 -->
