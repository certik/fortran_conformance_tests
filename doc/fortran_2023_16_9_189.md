# Fortran 2023 16.9.189: SIGN

The canonical catalogue is `doc/catalogues/sign_intrinsic_16_9_189.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.189 -->

### S16.9.189-001: SIGN argument restrictions

**Source:** S16.9.189, J3/24-007, 18 December 2023, physical PDF457-458. **Class:** Restriction.

**Definition:** A shall be integer or real, and B shall have the same type as A.

**Diagnostic obligation:** not-required.

**Facets:** `SIGN-A-integer-or-real`, `SIGN-B-same-type-as-A`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact integer, logical, kind, shape, or bit-pattern observations, plus exactly
representable real sign-copy results. Processor-dependent approximations,
processor-dependent real-zero distinctions, and processor-dependent numeric
interpretations of bit sequences are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

### S16.9.189-002: SIGN result characteristics match A

**Source:** S16.9.189, J3/24-007, 18 December 2023, physical PDF457-458. **Class:** Effect.

**Definition:** The SIGN result has the same type and kind as A.

**Diagnostic obligation:** not-required.

**Facets:** `SIGN-result-same-type-kind-as-A`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact integer, logical, kind, shape, or bit-pattern observations, plus exactly
representable real sign-copy results. Processor-dependent approximations,
processor-dependent real-zero distinctions, and processor-dependent numeric
interpretations of bit sequences are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

### S16.9.189-003: SIGN nonzero and integer-zero result values

**Source:** S16.9.189, J3/24-007, 18 December 2023, physical PDF457-458. **Class:** Effect.

**Definition:** For B greater than zero SIGN returns |A|; for B less than zero SIGN returns -|A|; for
integer B equal to zero SIGN returns |A|.

**Diagnostic obligation:** not-required.

**Facets:** `SIGN-positive-B-magnitude`, `SIGN-negative-B-negative-magnitude`, `SIGN-integer-zero-B-magnitude`, `SIGN-negative-A-magnitude-copy`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact integer, logical, kind, shape, or bit-pattern observations, plus exactly
representable real sign-copy results. Processor-dependent approximations,
processor-dependent real-zero distinctions, and processor-dependent numeric
interpretations of bit sequences are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

### S16.9.189-004: SIGN positive real zero result

**Source:** S16.9.189, J3/24-007, 18 December 2023, physical PDF457-458. **Class:** Effect.

**Definition:** For real B equal to positive zero, or for a processor that does not distinguish positive
and negative real zero, SIGN returns |A|.

**Diagnostic obligation:** not-required.

**Facets:** `SIGN-real-positive-zero-magnitude`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact integer, logical, kind, shape, or bit-pattern observations, plus exactly
representable real sign-copy results. Processor-dependent approximations,
processor-dependent real-zero distinctions, and processor-dependent numeric
interpretations of bit sequences are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

### S16.9.189-005: SIGN negative real zero follows processor zero distinction

**Source:** S16.9.189, J3/24-007, 18 December 2023, physical PDF457-458. **Class:** Effect.

**Definition:** For real B equal to negative zero, processors with distinct positive and negative real
zero return -|A|, while processors without that distinction return |A|.

**Diagnostic obligation:** not-required.

**Facets:** `SIGN-real-negative-zero-processor-distinction`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact integer, logical, kind, shape, or bit-pattern observations, plus exactly
representable real sign-copy results. Processor-dependent approximations,
processor-dependent real-zero distinctions, and processor-dependent numeric
interpretations of bit sequences are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for SHAPE, SHIFTA, SHIFTL, SHIFTR,
SIGN, SIN, and SIND found no fixture-json metadata binding for these 16.9.185-16.9.191
sections. SHAPE appears as helper coverage in unrelated clause 7, 8, 9, 10, and
contiguous-property fixture sources; SIGN appears only in unrelated Python variable
names or text, and no Fortran SIGN, SHIFTA, SHIFTL, SHIFTR, SIN, or SIND call match was
found. Existing uses remain unbound by this source packet.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, pointer, allocation, and I/O foundations from clauses 6
through 13 where already catalogued. Unregistered dependencies retained: 16.3 integer
bit-sequence model, 16.4 numeric model terminology, 17 IEEE support for future guarded
signed-zero probes, sibling 16.9 intrinsics used by pending plans such as ABS, BIT_SIZE,
BTEST, IBSET, ISHFT, KIND, LBOUND, POPCNT, SELECTED_INT_KIND, and SIZE, and clauses 14,
15, 18, and 19 where future fixtures need program, procedure, processor-dependency, or
scope terms.

<!-- END GENERATED 16.9.189 -->
