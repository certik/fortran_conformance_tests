# Fortran 2023: 11.7.8 EVENT WAIT statement

Source-only draft catalogue: see the corresponding `doc/catalogues/*.json` entry.
No Fortran test program, execution, oracle approval, fixture approval, or coverage claim is supplied.

<!-- BEGIN GENERATED 11.7.8 -->

### S11.7.8-001: EVENT WAIT waits until an event is posted

**Source:** 11.7.8 p1; J3/24-007 PDF p236. **Class:** Effect.

**Definition:** The EVENT WAIT statement waits until an event is posted.

**Diagnostic obligation:** not-required.

**Facets:** `event-wait-purpose-source-control`.

**Oracle:** This purpose sentence is not a portable timing or blocking oracle.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.7 EVENT POST in this packet;16 EVENT_TYPE NOT YET REGISTERED.

### R1176: EVENT WAIT statement syntax

**Source:** 11.7.8 R1176; J3/24-007 PDF p236. **Class:** Syntax.

**Definition:** An event-wait-stmt is EVENT WAIT with an event-variable and optional
event-wait-spec-list.

**Diagnostic obligation:** required.

**Facets:** `event-wait-minimal-form`, `event-wait-spec-list-form`.

**Oracle:** This grammar rule owns source form only; waiting, counts, and segment ordering are
p3/p5.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1177;R1178;C1184;C1185;16 EVENT_TYPE NOT YET REGISTERED.

### R1177: EVENT WAIT specifiers are UNTIL_COUNT= or sync-stat

**Source:** 11.7.8 R1177; J3/24-007 PDF p236. **Class:** Syntax.

**Definition:** An event-wait-spec is an until-spec or sync-stat.

**Diagnostic obligation:** required.

**Facets:** `event-wait-until-count-spec`, `event-wait-sync-stat-spec`.

**Oracle:** This grammar rule admits the specifier alternatives; C1185 owns duplicate specifiers.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1178;11.7.11 sync-stat in this packet.

### R1178: UNTIL_COUNT= syntax

**Source:** 11.7.8 R1178; J3/24-007 PDF p236. **Class:** Syntax.

**Definition:** An until-spec is UNTIL_COUNT = scalar-int-expr.

**Diagnostic obligation:** required.

**Facets:** `until-count-scalar-int-expr-form`.

**Oracle:** This grammar rule owns the specifier expression form; threshold interpretation is p3.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 10 expressions registered;7 integer/scalar concepts registered.

### C1184: EVENT WAIT event variables are not coindexed

**Source:** 11.7.8 C1184; J3/24-007 PDF p236. **Class:** Restriction.

**Definition:** The event-variable in an event-wait-stmt shall not be coindexed.

**Diagnostic obligation:** required.

**Facets:** `noncoindexed-event-wait-control`, `coindexed-event-wait-variable-rejected`.

**Oracle:** The negative/control plan changes only coindexing of the EVENT WAIT event-variable.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 9.6 image selectors registered;R1176 in this packet.

### C1185: EVENT WAIT specifiers are not repeated

**Source:** 11.7.8 C1185; J3/24-007 PDF p236. **Class:** Restriction.

**Definition:** No specifier shall appear more than once in an event-wait-spec-list.

**Diagnostic obligation:** required.

**Facets:** `unique-event-wait-spec-list-control`, `duplicate-until-count-rejected`, `duplicate-stat-rejected`, `duplicate-errmsg-rejected`.

**Oracle:** Each negative/control plan changes one duplicated specifier named by the constraint.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** R1177;R1178;11.7.11 sync-stat in this packet.

### S11.7.8-002: EVENT WAIT event-variable independence from status variables

**Source:** 11.7.8 p2; J3/24-007 PDF p236. **Class:** Restriction.

**Definition:** The event-variable shall not depend on the value of a stat-variable or errmsg-variable.

**Diagnostic obligation:** not-required.

**Facets:** `wait-event-variable-stat-errmsg-independence-source-control`, `wait-event-variable-stat-dependence-source-control`, `wait-event-variable-errmsg-dependence-source-control`.

**Oracle:** The two p2 dependency prohibitions are program obligations recorded as source-control
facets only. No conforming processor diagnostic is required, and no rejection facet is
claimed.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 sync-stat in this packet;19 association/dependence terminology NOT YET
REGISTERED.

### S11.7.8-003: EVENT WAIT threshold, wait, and decrement sequence

**Source:** 11.7.8 p3; J3/24-007 PDF p236. **Class:** Effect.

**Definition:** EVENT WAIT sets a threshold to one or max(UNTIL_COUNT, one), waits until the event count
reaches the threshold or an error occurs, and if no error occurs atomically decrements
the count by the threshold.

**Diagnostic obligation:** not-required.

**Facets:** `event-wait-threshold-source-control`, `event-wait-until-count-source-control`, `event-wait-decrement-source-control`.

**Oracle:** The p3 sequence is a dynamic event-count synchronization effect. It has no direct
single-image count inquiry in this suite packet.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.7 EVENT POST in this packet;11.7.11 STAT= in this packet;16 EVENT_TYPE NOT YET
REGISTERED.

### S11.7.8-004: EVENT WAIT error leaves count processor dependent

**Source:** 11.7.8 p4; J3/24-007 PDF p236. **Class:** Effect.

**Definition:** If an error condition occurs during EVENT WAIT, the event variable count value is
processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `event-wait-error-count-processor-dependent`.

**Oracle:** Processor-dependent count content is recorded as latitude, not as a required executable
result.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.11 p3 STAT= processor-dependent positive value in this packet.

### S11.7.8-005: EVENT WAIT satisfies posted events and orders following segments

**Source:** 11.7.8 p5; J3/24-007 PDF p236. **Class:** Effect.

**Definition:** An EVENT POST execution is initially unsatisfied; successful EVENT WAIT with threshold k
satisfies the first k unsatisfied posts for that event variable and causes the segment
after the wait to succeed the segments before those posts.

**Diagnostic obligation:** not-required.

**Facets:** `event-post-unsatisfied-source-control`, `event-wait-satisfies-first-k-source-control`, `event-wait-post-segment-order-source-control`.

**Oracle:** Satisfaction and segment precedence are dynamic synchronization relations. The suite has
no portable oracle for cross-image posts, waits, or ordering.

**Oracle limitation:** This source-only catalogue records pending plans only. It creates no Fortran test
program, invokes no processor, approves no fixture or oracle, and claims no coverage.
Future fixtures for image control statements must use independently derived source
relations or portable single-image observations only after their type, team, coarray,
and synchronization dependencies are registered; must not infer timing, scheduling,
interleaving, fairness, hangs, failed-image occurrence, stopped-image occurrence,
processor-dependent STAT values, ERRMSG text, or multi-image segment ordering from a
single-image run.

**Dependencies:** 11.7.2 segments UNREGISTERED (parallel);11.7.7 EVENT POST in this packet.

<!-- END GENERATED 11.7.8 -->
