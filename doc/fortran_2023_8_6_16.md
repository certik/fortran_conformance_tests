# Fortran 2023 8.6.16: VALUE statement

Original source review is recorded in batch077. Source and inventory-context
reviews are not fixture approvals or compiler observations. Current facet
states remain authoritative in the canonical record,
`doc/catalogues/value_statement_8_6_16.json`; effective status comes from
`Registry.catalogue_review_state("8.6.16")`.

The authority is J3/24-007, dated December 18, 2023, 688 physical pages,
SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The entire VALUE section is on physical PDF140. The actual next heading
is8.6.17 VOLATILE statement on141. Its section hash is
`7a00d620d231202e9f3df1eb8b01f513578e50b0fd1c79601df892e2406e82ab`.
The two original units are **R864 and p1**; there is no note.

R864 has an optional double colon and a mandatory nonempty dummy-name
list. The complete original502-head scan identifies **explicit R1534**
for `dummy-arg-name`, with C1571 requiring a real dummy argument.
R401 supplies list expansion; assumed R402 is overridden under4.1.3p2.
No array/coarray declarator, component selector or invocation is admitted
in this list.

All two original and seven fine units have explicit accounting, with
reciprocal requirement mappings. R864 was already numbered-accounted;
only p1 is newly accounted at base level. Fine units inherit their
original parent hash. No local S requirement is needed for the normative
attribute-route definition.

## Complete syntax controls and reporting

The ordinary controls are complete module procedures with real, previously
typed scalar INTEGER dummies, no duplicate VALUE attribute, no incompatible
attributes, and bodies without data use or calls. The array-name repair
uses an ordinary **non-BIND**, explicit-shape INTEGER dummy: it is not an
assumed-size/rank, coarray, pointer or allocatable context. A prior explicit
type is a useful isolation choice, not a universal type-before-attribute
rule.

Syntax admissions have compile/positive-control roles. Empty-list,
one-colon, separator and selected-name negatives have exact minimal
repairs with the same complete surrounding program unit. Numbered
reporting capability is not mandatory fatal rejection, nonzero status,
fixed English or a printed rule number. Any narrower reporting contract
needs separate qualification; unsupported facilities, duplicate attributes,
wrong roles, missing providers, source echoes, crashes or resource errors
do not corroborate another grammar predicate.

## Canonical eligibility and VALUE semantics

P1 is a normative statement-to-attribute definition, not an executable
copy operation. C867 requires an eligible **data** dummy; an ordinary
procedure or procedure pointer is not such a dummy. C867's assumed-size,
coarray and coarray-potential-subobject exclusions and C839's assumed-rank
VALUE exclusion remain active.

C868 applies to the dummy entity's attributes, not automatically to each
component or corresponding actual argument. It excludes ALLOCATABLE,
POINTER, VOLATILE, INTENT(OUT) and INTENT(INOUT), while INTENT(IN) and
no INTENT retain their actual permitted meanings. C869 forbids OPTIONAL
plus VALUE for a BIND procedure; it is not a general prohibition for
ordinary non-BIND procedures.

For a present VALUE dummy,15.5.2.4p4 defines association with a definable
anonymous object initialized from the actual value. Actual type, kind,
rank, parameters, definition, presence and interface conditions remain
canonical. Subsequent changes to the dummy do not imply changes to the
actual object's value, but this is not universal isolation of everything
reachable through the object. Under7.5.8, a pointer component's value is
its association: its target is not a subobject of the containing object
when dereferenced. An anonymous component can therefore retain association
to the same live target. Ordinary component changes, pointer association
changes and target-payload changes must be distinguished.

The finite source/operation plans name concrete scalar and simple-component
contexts and meaningful future observations, but remain **PENDING** at
their canonical15.5.2/7.5.8 owners. They do not create another S effect or
duplicate an existing parent. TARGET+VALUE pointer lifetime also retains
15.5.2.5p12: no escaped pointer to the anonymous dummy is queried after
return. The actual calling/storage mechanism is not a required stack copy,
address relation, allocator event or callback order.

VALUE alone establishes no universal C ABI or prototype.18.3.7 retains
the BIND/interface/prototype/reference conditions and all four pieces of
an actual interoperation. Any scalar C_INT context uses the original
guaranteed kind, not an arbitrary kind number; ordinary Fortran array or
derived VALUE admission cannot simply be relabelled as C interoperability.
Existing C interoperation, derived-value, BIND and other cases keep their
actual owners, roles, profiles and complete-parent requirements.

## Scope and remaining gates

C1107 forbids VALUE statements in BLOCK. Naming a USE/HOST entity or a
local variable does not manufacture a formal dummy. Real dummy/name
identity and C815 uniqueness precede any source-use or diagnostic claim.
A future relationship must bind the exact standalone-statement span,
resolved formal and VALUE attribute origin; keyword matching or a no-op
program is not that mechanism.

The exact author base is
`92465ae5931b989240e281ac7d530f442b14984f`, with2,056 cases and2,020 raw
reviews in2,018 groups. The preserved122 older catalogues,22 current
links/R402 and baseline bindings are recorded in the handoff.
The author's local two-entry index overlay was excluded from its commit.
Batch077 appends those entries to the then-current main index without
importing the older index snapshot. No earlier full or selected run is
relabelled as this source context.

<!-- BEGIN BATCH320 ATTRIBUTE STATEMENTS 8.6.16 -->
Batch320 VALUE statement fixtures retain scalar dummy-copy observations and syntax diagnostics; frozen LFortran lacks VALUE statement support and is reported as a known parent defect in mutation mode.
<!-- END BATCH320 ATTRIBUTE STATEMENTS 8.6.16 -->

<!-- BEGIN GENERATED 8.6.16 -->

### R864: A VALUE statement has an optional double colon and a required dummy-name list

**Source:** 8.6.16 R864, J3/24-007, 18 December 2023, physical PDF140 **Class:** Syntax.

**Definition:** VALUE is followed by an optional double colon and a nonempty comma-separated
dummy-arg-name list. R401 supplies the list; explicit R1534/C1571 supplies a name of a
real dummy argument, overriding assumed R402. C867 further restricts VALUE to eligible
dummy data objects, not dummy procedures or procedure pointers. Array/coarray
specifications, selected subobjects and invocations are not part of this name list.

**Diagnostic obligation:** required.

**Facets:** `double-colon-form`, `no-double-colon-form`, `multiple-data-dummies`, `required-name-list`, `single-colon-repair`, `list-separator-repairs`, `name-not-designator`, `dummy-name-and-eligibility-source`, `attribute-combinations-and-interoperability`, `anonymous-object-and-component-source`, `scope-order-and-existing-owner-source`.

**Oracle:** Complete ordinary program-unit compile admissions and source-minimal grammar
report/control pairs, with actual dummy identity, supported types and compatible
attributes already valid. Source/interface plans require bound statement origins, formal
declarations and canonical-owner evidence. Canonical invocation effects remain
separately pending; no case or relation is implemented here.

Batch320 R864 attribute-statement fixtures: VALUE statement controls cover a two-dummy
list and anonymous effective-object behavior. Calls mutate local VALUE dummies and then
assert caller actuals remain unchanged; feature mutants remove VALUE from individual
dummies. Three diagnostic pairs isolate missing list, single colon, and comma defects
with one-property repairs.

**Oracle limitation:** 4.2 requires a capability to detect and report numbered violations, not mandatory fatal
rejection, a nonzero status, a particular English message or printed R864. Generic
keyword/expected-token text, wrong type/role, duplicate attributes, missing providers,
unsupported features, source echoes, crashes and resource errors are not corroboration.
Syntax acceptance is not anonymous-object, component, ABI or runtime evidence. No
compiler run, local no-op S classifier, approval or automatic source-use/link credit is
supplied.

Batch320 R864 limits: only ordinary scalar data dummies in non-BIND module procedures
are covered. Optional-colon syntax forms, designator, array, interoperability,
conflicting-attribute, and canonical component/pointer source relations remain pending.
Frozen LFortran rejects VALUE statements as unsupported.

**Dependencies:** 4.1.3R401/p2;4.2;8.1/8.2/8.5.1C815;8.5.19C867-C869;8.5.8.7C839;11.1.4C1107;14.2.2/19.5.1.4;15.4.2.1-.2;15.6.2.2R1534/C1571;15.5.2.4p4/p6/note2;15.5.2.5;15.5.2.14;7.5.1/7.5.8;9.4.2p5;18.2.2p3/18.3.7.

<!-- END GENERATED 8.6.16 -->
