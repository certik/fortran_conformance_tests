# Fortran 2023 16.9.86: EXTENDS_TYPE_OF

The canonical catalogue is `doc/catalogues/extends_type_of_16_9_86.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, note1, note2, p6.

Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN INTRINSICS 16.9.I FIXTURES -->
## Intrinsics 16.9.I runtime observations

This batch adds source-derived runtime fixtures for EPSILON, ERF, ERFC, ERFC_SCALED, EXP, EXPONENT, and EXTENDS_TYPE_OF. Exact real-model assertions use RADIX, DIGITS, HUGE, and MAXEXPONENT of the same kind; approximate transcendental results are exercised only through argument, elemental, kind, and shape properties. Processor-dependent approximation and residual nonextensible EXTENDS_TYPE_OF facets remain pending.
<!-- END INTRINSICS 16.9.I FIXTURES -->

<!-- BEGIN GENERATED 16.9.86 -->

### S16.9.86-001: EXTENDS_TYPE_OF is the dynamic type extension inquiry

**Source:** S16.9.86, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** EXTENDS_TYPE_OF inquires whether one dynamic type extends another dynamic type.

**Diagnostic obligation:** not-required.

**Facets:** `extends-type-of-dynamic-type-extension-inquiry`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.86-001 intrinsics_16_9_i fixture: EXTENDS_TYPE_OF relation fixture observes
child-parent and inverse dynamic type extension results.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.86-001 intrinsics_16_9_i boundaries: This packet supplies the named run/effect or
positive-control fixture only; it does not approve unrelated facets, diagnostics, source
review, evidence links, baselines, coarray behavior, processor-dependent approximations,
or compiler consensus oracles.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.86-002: EXTENDS_TYPE_OF is an inquiry function

**Source:** S16.9.86, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** The intrinsic is classified as an inquiry function.

**Diagnostic obligation:** not-required.

**Facets:** `extends-type-of-inquiry-function-class`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.86-002 intrinsics_16_9_i fixture: EXTENDS_TYPE_OF inquiry fixture uses the
intrinsic in a scalar logical expression whose exact result is checked.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.86-002 intrinsics_16_9_i boundaries: This packet supplies the named run/effect or
positive-control fixture only; it does not approve unrelated facets, diagnostics, source
review, evidence links, baselines, coarray behavior, processor-dependent approximations,
or compiler consensus oracles.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.86-003: EXTENDS_TYPE_OF A argument has extensible or unlimited polymorphic declared type

**Source:** S16.9.86, J3/24-007, 18 December 2023, physical PDF409. **Class:** Restriction.

**Definition:** A shall be an object of extensible declared type or unlimited polymorphic; if it is a
polymorphic pointer, it shall not have undefined association status.

**Diagnostic obligation:** not-required.

**Facets:** `extends-type-of-a-extensible-or-unlimited`, `extends-type-of-a-pointer-defined-association`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.86-003 intrinsics_16_9_i fixture: EXTENDS_TYPE_OF A-argument fixture passes an
allocated unlimited-polymorphic A with an extensible dynamic type.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.86-003 intrinsics_16_9_i boundaries: This packet supplies the named run/effect or
positive-control fixture only; it does not approve unrelated facets, diagnostics, source
review, evidence links, baselines, coarray behavior, processor-dependent approximations,
or compiler consensus oracles.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.86-004: EXTENDS_TYPE_OF MOLD argument has extensible or unlimited polymorphic declared type

**Source:** S16.9.86, J3/24-007, 18 December 2023, physical PDF409. **Class:** Restriction.

**Definition:** MOLD shall be an object of extensible declared type or unlimited polymorphic; if it is a
polymorphic pointer, it shall not have undefined association status.

**Diagnostic obligation:** not-required.

**Facets:** `extends-type-of-mold-extensible-or-unlimited`, `extends-type-of-mold-pointer-defined-association`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.86-004 intrinsics_16_9_i fixture: EXTENDS_TYPE_OF MOLD-argument fixture passes
allocated unlimited-polymorphic A and MOLD objects with extensible dynamic types.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.86-004 intrinsics_16_9_i boundaries: This packet supplies the named run/effect or
positive-control fixture only; it does not approve unrelated facets, diagnostics, source
review, evidence links, baselines, coarray behavior, processor-dependent approximations,
or compiler consensus oracles.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.86-005: EXTENDS_TYPE_OF returns a default logical scalar

**Source:** S16.9.86, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** The result is a scalar of default logical type.

**Diagnostic obligation:** not-required.

**Facets:** `extends-type-of-default-logical-result`, `extends-type-of-scalar-result`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.86-005 intrinsics_16_9_i fixture: EXTENDS_TYPE_OF characteristics fixture inquires
KIND and RANK directly on EXTENDS_TYPE_OF expressions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.86-005 intrinsics_16_9_i boundaries: This packet supplies the named run/effect or
positive-control fixture only; it does not approve unrelated facets, diagnostics, source
review, evidence links, baselines, coarray behavior, processor-dependent approximations,
or compiler consensus oracles.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.86-006: EXTENDS_TYPE_OF has exact results for disassociated-or-unallocated unlimited arguments and extensible dynamic types

**Source:** S16.9.86, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** The result is true when MOLD is unlimited polymorphic and is either a disassociated
pointer or an unallocated allocatable variable; otherwise it is false when A is
unlimited polymorphic and is either a disassociated pointer or an unallocated
allocatable variable; otherwise it exactly reflects whether A's dynamic type extends
MOLD's dynamic type when either dynamic type is extensible.

**Diagnostic obligation:** not-required.

**Facets:** `extends-type-of-unlimited-mold-disassociated-or-unallocated-true`, `extends-type-of-unlimited-a-disassociated-or-unallocated-false`, `extends-type-of-extension-relation-true-iff`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

S16.9.86-006 intrinsics_16_9_i fixture: EXTENDS_TYPE_OF exact-value fixture checks true
and false directions for parent, child, grandchild, and sibling extensible dynamic
types.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

S16.9.86-006 intrinsics_16_9_i boundaries: This packet supplies the named run/effect or
positive-control fixture only; it does not approve unrelated facets, diagnostics, source
review, evidence links, baselines, coarray behavior, processor-dependent approximations,
or compiler consensus oracles.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

### S16.9.86-007: EXTENDS_TYPE_OF result is processor dependent for the nonextensible residual branch

**Source:** S16.9.86, J3/24-007, 18 December 2023, physical PDF409. **Class:** Effect.

**Definition:** When neither the disassociated-pointer or unallocated-allocatable unlimited polymorphic
cases nor an extensible dynamic type applies, the result is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `extends-type-of-nonextensible-result-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Exact intrinsic-result plans use discriminating
inputs fixed by the Result Value text, initialize every observed variable to a
non-default sentinel where assignment is involved, and mutate the intrinsic feature or
argument order so an oracle cannot pass vacuously. Argument shall-restrictions are
program restrictions with no diagnostic facet in this source packet. Processor-dependent
approximations, processor-dependent branches, image-failure observations, and IOMSG-like
messages are not asserted as exact values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative/diagnostic tests are not planned for unnumbered intrinsic argument
restrictions; future fixtures may add conforming positive controls only where a portable
single-image oracle is stated here.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 for source form, type
declaration and polymorphism, expressions, arrays, elemental/transformational calls,
execution, and existing formatting foundations. Unregistered dependencies explicitly
retained: the rest of clause 16 for related inquiry/model intrinsics, clause 14 and
coarray team/image semantics for image-dependent intrinsics, clause 17 IEEE support, and
clause 19 scope/lifetime details where future fixtures reach them.

<!-- END GENERATED 16.9.86 -->
