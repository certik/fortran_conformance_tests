# Fortran 2023: 10.1.2.5 Level-2 expressions

Catalogue: `doc/catalogues/level_2_expressions_10_1_2_5.json`. The original source-only registration supplied pending plans; the bounded batch295 fixtures below supply selected cases and mutation/diagnostic plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN EXPRESSIONS 10.1.2.5-10.1.2.7 FIXTURES -->
## Batch295 expression fixtures for 10.1.2.5

Generated batch295 fixtures cover selected facets for: S10.1.2.5-001, R1005, R1006, R1007, R1008, R1009, R1010. Runtime cases use exact integer, logical, and character LEN/value oracles; diagnostic cases are line-anchored one-property malformed forms with conforming repairs documented in generator metadata. Feature mutations, including concat operator replacements with defined .cat. controls, are permanent and checked on both the reference gfortran and frozen LFortran target. Facets not listed remain pending for the recorded reasons.
<!-- END EXPRESSIONS 10.1.2.5-10.1.2.7 FIXTURES -->

<!-- BEGIN GENERATED 10.1.2.5 -->

### S10.1.2.5-001: Level-2 expressions are the numeric-operator layer over level-1 expressions

**Source:** 10.1.2.5 p1; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** Level-2 expressions are built from level-1 expressions and optional numeric operators in
four distinct precedence steps: exponentiation, multiplication/division, unary
plus/minus at the first add-op position, and binary plus/minus in the left-recursive
level-2 chain. The grammar specifies right recursion for power and left recursion for
multiplication and binary addition/subtraction.

**Diagnostic obligation:** not-required.

**Facets:** `level-1-base`, `power-layer`, `multiply-divide-layer`, `add-subtract-layer`.

**Oracle:** Future precedence controls must use small integer literals with exact results and
parenthesized companion controls where needed; they must not infer floating-point or
overflow semantics.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: one run/positive-control/f2023 program
observes exact integer values for a level-1 base expression 2, right-recursive power
2**3**2=512, left-recursive multiplication/division 24/3*2=16, and add/subtract forms
including 10-3-2=5 and -2**2=-4. Each assertion has a conforming feature mutation that
changes the relevant operator, operand, or grouping and fails at run time.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 10.1.2.4R1003;10.1.3 Table 10.1;10.1.5.2;10.1.9;10.2.1.3.

### R1005: A mult-operand is a level-1 expression optionally exponentiated by a mult-operand

**Source:** 10.1.2.5 R1005; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The mult-operand syntax is a level-1-expr followed optionally by power-op and another
mult-operand, making unparenthesized exponentiation right-recursive within the grammar.

**Diagnostic obligation:** required.

**Facets:** `level-1-only`, `single-power`, `right-recursive-power`, `missing-right-mult-operand-rejected`.

**Oracle:** Power negatives must use the valid '**' token and valid left operand; malformed operator
spellings belong to R1008 or lexical rules.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: the level-2 numeric program observes a
bare level-1 literal 4, single power 2**3=8, and right-recursive power 2**3**2=512; a
diagnostic fixture isolates a missing mult-operand after a valid '**'.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 4.2;10.1.2.4R1003;10.1.2.5R1008;10.1.5.2;10.1.9.

### R1006: An add-operand is a left-recursive chain of mult-operands joined by mult-op

**Source:** 10.1.2.5 R1006; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The add-operand syntax is either a mult-operand or an add-operand followed by mult-op
and a mult-operand, giving multiplication and division the same level and left-recursive
grouping.

**Diagnostic obligation:** required.

**Facets:** `single-mult-operand`, `multiplication-chain`, `division-chain`, `left-recursive-mixed-chain`, `missing-right-mult-operand-rejected`.

**Oracle:** Plans must keep operands numeric and defined so future diagnostics are syntactic for
missing operands, not semantic type errors from 10.1.5.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: the numeric program observes
add-operands that are a single mult-operand 2**3, a multiplication chain 2*3*4=24, a
division chain 24/3/2=4, and mixed left recursion 24/3*2=16; a diagnostic fixture
isolates a missing mult-operand after '*'.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 4.2;10.1.2.5R1005/R1009;10.1.5.2;10.1.9.

### R1007: A level-2 expression is an add-operand with optional unary or binary add-op

**Source:** 10.1.2.5 R1007; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The level-2-expr syntax is an add-operand, optionally preceded by an add-op, or a
left-recursive level-2-expr followed by add-op and add-operand. This admits unary
plus/minus and binary plus/minus at the level-2 layer.

**Diagnostic obligation:** required.

**Facets:** `plain-add-operand`, `unary-plus`, `unary-minus`, `binary-plus`, `binary-minus`, `left-recursive-add-chain`, `missing-add-operand-rejected`.

**Oracle:** Unary and binary add-op plans must distinguish position. Missing-operand negatives must
not also introduce malformed operator tokens or undeclared names.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: the numeric program observes a plain
add-operand 2*3, unary +5, unary -5, binary 2+3 and 7-2, and left-recursive 10-3-2=5; a
diagnostic fixture and compiled one-property control isolate a missing add-operand after
'+'.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 4.2;10.1.2.5R1006/R1010;10.1.5.2;10.1.9.

### R1008: The power operator token is double asterisk

**Source:** 10.1.2.5 R1008; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The power-op token is '**'. It is the token used by R1005; this rule does not define
operand types or exponentiation interpretation.

**Diagnostic obligation:** required.

**Facets:** `double-asterisk-token`, `single-asterisk-not-power`.

**Oracle:** Do not attribute all uses of '*' to R1008; '*' is valid R1009. Any negative must place
the token where only power-op is syntactically possible.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: the numeric program observes the '**'
token in 2**3=8 and a boundary assertion that 2*3 is multiplication, not power. Feature
mutations swap between conforming '*' and '**' forms with distinct exact integer
results.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property.

**Dependencies:** 4.2;6.2.4R608;10.1.2.5R1005;10.1.5.2.

### R1009: Multiplication operators are asterisk and slash

**Source:** 10.1.2.5 R1009; J3/24-007 PDF p169. **Class:** Syntax.

**Definition:** The mult-op alternatives are '*' and '/'. This rule defines the tokens for R1006, not
numeric type admissibility or division semantics.

**Diagnostic obligation:** required.

**Facets:** `asterisk-token`, `slash-token`, `other-token-rejected`.

**Oracle:** Membership negatives must avoid tokens that are valid at another expression level in the
same source position unless parentheses make R1006's mult-op requirement unambiguous.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: the numeric program observes '*' in
2*4=8 and '/' in 8/2=4 with sibling operator substitutions that stay conforming and fail
the literal integer assertions.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property. The
other-token-rejected facet remains pending: no isolated one-property R1009-only
diagnostic was identified.

**Dependencies:** 4.2;6.2.4R608;10.1.2.5R1006;10.1.5.2.

### R1010: Addition operators are plus and minus

**Source:** 10.1.2.5 R1010; J3/24-007 PDF p170. **Class:** Syntax.

**Definition:** The add-op alternatives are '+' and '-'. The tokens can appear in unary or binary
positions through R1007; interpretation and operand validity are elsewhere.

**Diagnostic obligation:** required.

**Facets:** `plus-token`, `minus-token`, `unary-position`, `binary-position`, `other-token-rejected`.

**Oracle:** Negatives must not confuse an invalid add-op with a valid lower-precedence operator
parsed by another rule; the source shape must force the R1007 add-op slot.

Batch295 expressions 10.1.2.5-10.1.2.7 fixtures: the numeric program observes '+' and
'-' in unary and binary positions using exact integer assertions; sibling substitutions
or operand changes make each assertion load-bearing.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected fixtures and mutation/diagnostic plans without granting
unrelated approvals or claims. Numbered syntax rules and constraints carry the 4.2
diagnostic-capability duty, but future negative plans must isolate the cited rule or
constraint and must not rely on compiler agreement, address arithmetic, TRANSFER,
emitted code, undefined reads, or interpretation and evaluation rules owned by
10.1.3-10.1.12.

Batch295 expressions 10.1.2.5-10.1.2.7 boundaries: Only the listed selected facets are
represented by batch295 fixtures. Existing source review state, evidence approval,
rollout progress, xfail policy, and unrelated facets are not changed. Remaining pending
facets are left for the reasons recorded in pending. All runtime oracles use exact
integer, logical, character LEN, or literal character equality properties and no real
arithmetic, evaluation order, address, or compiler-consensus property. The
other-token-rejected facet remains pending: no isolated one-property R1010-only
diagnostic was identified.

**Dependencies:** 4.2;6.2.4R608;10.1.2.5R1007;10.1.5.2.

<!-- END GENERATED 10.1.2.5 -->
