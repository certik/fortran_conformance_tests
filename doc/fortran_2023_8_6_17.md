# Fortran 2023 8.6.17: VOLATILE statement

Original source review is recorded in batch077. The canonical
record is `doc/catalogues/volatile_statement_8_6_17.json`; effective status
comes from `Registry.catalogue_review_state("8.6.17")`. Source and
inventory-context reviews supply no fixture, compiler observation, profile,
source-use instance or canonical-link approval.

The authority is J3/24-007, dated December 18, 2023, 688 physical pages,
SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The entire section is on physical PDF141 and ends at the actual8.7
IMPLICIT statement heading on the same page. Its section hash is
`b0d2a4a9e7c2ab0e5a93861502b3cd11c8433249a0e729cb6d9335f11a793dc1`.
The original units are **R865 and p1**; there is no note and no8.6.18
scope in this packet.

R865 permits a double colon and requires a nonempty object-name list.
The complete original502-head scan identifies **explicit R804/C810**,
not assumed R402, for that data-object name. R401 supplies list expansion.
Subscripts, coarray suffixes, substrings, percent components and invocations
are not additional list declarators. C870 separately requires a variable
that is not an INTENT(IN) dummy, including an IN pointer dummy.

Two original and seven fine units are accounted with reciprocal maps.
R865 was already globally numbered-accounted; p1 is the one new base
unit. Fine units inherit their original parent's source binding. P1's
normative attribute route does not require a duplicate local S effect.

## Scope and identity

Ordinary complete syntax controls use distinct local INTEGER variables
without another VOLATILE specification, VALUE or INTENT(IN). Negative
list/separator/designator variants keep complete eligible surrounding
declarations and minimal conforming repairs. Required numbered reporting
is not a mandated fatal status, exact English or printed R865. A generic
VOLATILE word, unsupported feature, missing provider, wrong role, duplicate
attribute, recovery, crash or resource error is not the cause being tested.

USE and HOST association can preserve an object's identity while adding
VOLATILE in the local scope where permitted. Inherited VOLATILE and a
second local specification are different questions. C871 still prohibits
local specification for an associated coarray or a variable with coarray
potential subobjects. The noncoarray association permission does not waive
the coarray-potential-subobject actual/dummy iff in15.5.2.5p22.

VOLATILE alone inside BLOCK need not declare a new construct entity.
An enclosing eligible noncoarray variable can have the attribute only
inside that construct. A separately declared saved local coarray needs
its actual construct identity and all storage/type conditions. A USE
statement inside BLOCK creates construct entities under19.4p3, but this
does **not** override C871. C872's construct predicate is an additional
condition, not a blanket permission.

These scoped effects retain **S8.5.20-001**. Coarray actual/dummy and
pointer/target VOLATILE matching retain15.5.2.9 and10.2.2.3p7. No current
case, type-declaration attribute or source snippet is automatically a
standalone R865 witness or a new evidence link.

## Subobjects, states and normative verbs

S8.5.20-003 owns subobject propagation. Nonpointer component paths differ
from arbitrary pointer reachability. Under9.4.2p5, a sole rightmost pointer
is a subobject in association-status contexts; its dereferenced target is
not a subobject of the original base, and an earlier pointer hop breaks
that base-subobject relationship. Potential-subobject recursion follows
3.30.3/7.5.1p5.

The p1 external possibilities in8.5.20 do not mandate an external event.
Extra pointer association/type/parameter/bound changes and allocatable
allocation/type/parameter/bound changes have their original subjects;
declaring VOLATILE does not initialize those states or permit an otherwise
undefined status/value query.

| Canonical source | Preserved modality |
| --- | --- |
| 8.5.20p3 pointer advice | A pointer **should** be VOLATILE if its target is VOLATILE |
| 8.5.20p3 actual external target events | The conditional pointer-VOLATILE **shall** applies to actual external reference, definition or undefinition, not merely a target keyword |
| 8.5.20p3 EQUIVALENCE advice | Group members **should** have the attribute if any member does |
| 8.5.20p5 current definition | **Should** use the most recent definition when the value is required |
| 8.5.20p5 availability | **Should** make a Fortran definition available to non-Fortran parts as soon as possible |

S8.5.20-002 retains the conditional mandatory pointer rule. The adjacent
recommendations are not mandatory strategy, latency, atomicity,
synchronization or load/store requirements, and are not merely NOTE text.
No busy-loop, data race, optimizer trap, compulsory compiler strategy or
unsupported-feature success is proposed as an oracle.

## Pending evidence and preservation

Finite source/interface plans must bind actual statement spans, variable
identity, attribute scope and canonical owners, not arbitrary keyword
matches or declaration-only value prints. Scope, subobject and externally
caused state mechanisms remain pending at their existing owners. Current
supplementary-target link machinery does not automatically support numbered
statement wrappers; no duplicate program or fictional passing inquiry
fills that gap.

The original VALUE/VOLATILE source packet added two syntax requirements
and23 pending facets over four original and14 fine units. Only two new
base units were accounted; the two R units were already counted. There
were no new S IDs. Current facet states follow the catalogue.

The exact2056-case/2020-review/2018-group author context and
`c3b55716b758b2ac07f998bd9ee51853b91d8f557a606c61b4ec5eafdf6aad80`
baseline were preserved in the author packet. Its source overlay made the
effective inventory stale without renewing any raw review. Batch077 supplies
separate source and inventory-context reviews, not case or relation approvals.
The last actual full run remains batch059's2041-case context; selected072
concerned only four cases with122 catalogues. Neither is relabelled as an
execution of the two new source catalogues. Earlier DATA/DISR/OPSR
packets and coordinator todos remain untouched.

<!-- BEGIN GENERATED 8.6.17 -->

### R865: A VOLATILE statement has an optional double colon and a required object-name list

**Source:** 8.6.17 R865, J3/24-007, 18 December 2023, physical PDF141 **Class:** Syntax.

**Definition:** VOLATILE is followed by an optional double colon and a nonempty comma-separated
object-name list. R401 supplies the list; explicit R804/C810 supplies a data-object
name, overriding assumed R402. C870 further requires a variable that is not an
INTENT(IN) dummy. No array/coarray declarator, component selection, substring or
invocation is part of the list. Actual scope, inherited attributes and coarray-related
conditions remain canonical.

**Diagnostic obligation:** required.

**Facets:** `double-colon-form`, `no-double-colon-form`, `multiple-object-names`, `required-name-list`, `single-colon-repair`, `list-separator-repairs`, `name-not-designator`, `variable-eligibility-and-state-source`, `use-host-and-inherited-scope-source`, `block-scope-and-construct-identity`, `subobject-and-pointer-target-source`, `external-event-and-modality-source`.

**Oracle:** Complete eligible variable contexts and source-minimal local grammar repairs, with exact
source origins and one intended syntax predicate. Scoped attribute, propagation,
association and external-event mechanisms remain finite canonical source/processor plans
pending explicit adjudication. Compile admission alone cannot establish those effects or
any implementation strategy.

**Oracle limitation:** Required numbered reporting does not prescribe fatal rejection, nonzero exit, exact
English or printed R865. Generic variable/VOLATILE words, wrong entity role, duplicate
attributes, missing providers, unsupported facilities, recovery, crashes and resource
errors are not causes for an unrelated grammar defect. No externally visible value
change, timing, atomicity, barrier, mandatory optimization behavior or unconditional
target propagation is inferred. All facets remain pending.

**Dependencies:** 4.1.3R401/p2;4.2;8.1/8.2R804/C810;8.5.1C815;8.5.20C870-C872/p1-p5/S8.5.20-001 through
-003;3.30.3/7.5.1p5;9.4.2p5/note1;11.1.4/19.4p3;14.2.2p2/p9/p10;19.5.1.4p1/note1;8.5.6.1C826/8.5.16;15.5.2.5p21-p22/C1546-C1549;15.5.2.9;15.5.2.14;10.2.2.3p7.

<!-- END GENERATED 8.6.17 -->
