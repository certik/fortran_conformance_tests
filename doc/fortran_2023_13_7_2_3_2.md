# Fortran 2023 13.7.2.3.2: F editing

The canonical catalogue is `doc/catalogues/f_editing_13_7_2_3_2.json`. Effective review state comes from `Registry.catalogue_review_state("13.7.2.3.2")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Thirteen base units: all accounted. IEEE and NaN facets are guarded by processor support; payload/sign latitude is explicitly unassertable. Exact finite-output plans use representable values and name complete character fields.

<!-- BEGIN F EDITING FIXTURES -->
## Executable F editing fixtures

This packet adds complete internal-file programs for 25 selected 13.7.2.3.2 F-editing facets. The fixtures use only exactly representable finite values or IEEE exceptional values under the ieee-binary profile, initialize character buffers with '#' sentinels, guard-check distinguished input target sentinels before READ, check LEN before every character equality, use SS for all positive output fields, and include permanent descriptor- and sentinel-substitution matrices whose alternatives differ on the selected values. Review state and execution approval remain integrator-owned.
<!-- END F EDITING FIXTURES -->

<!-- BEGIN GENERATED 13.7.2.3.2 -->

### S13.7.2.3.2-001: Fw.d field width, fractional digits, and input-width restriction

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** F w.d fields occupy w positions except that w=0 selects output width; the fractional
part contains d digits; w shall not be zero on input.

**Diagnostic obligation:** not-required.

**Facets:** `F-field-width`, `F-fractional-digits`, `F-input-width-zero-prohibited`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.2-001 F-editing runtime fixtures: two complete internal-file programs
discharge selected field-width and fractional-digit effects. (SS,F5.1) with exact -3.0
writes ' -3.0' in five positions; (SS,F0.1) with exact 3.0 writes selected-width '3.0'
in a length-3 internal file; and (SS,F5.2) with exact 1.25 writes ' 1.25'. SS suppresses
optional plus signs. The generator permanently mutates F5.1->F6.1/F5.2, F0.1->F4.1, and
F5.2->F5.3/E12.4; every substitution changes the selected value's field and is required
to fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-002: Lowercase letters are equivalent in IEEE exceptional specifications and numeric exponents

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** Lowercase letters match uppercase in IEEE exceptional specifications and in numeric
input exponents.

**Diagnostic obligation:** not-required.

**Facets:** `lowercase-ieee-exceptional-input`, `lowercase-real-exponent-input`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.2-002 lowercase runtime fixtures: two complete programs internally read exact
lowercase fields. '1e1' with F3.0 expects exactly 10.0 because lowercase e is equivalent
to E in a numeric exponent; 'inf' with F3.0, under the ieee-binary profile, expects IEEE
positive infinity because lowercase letters are equivalent in IEEE exceptional
specifications. Descriptor substitutions F3.0->F3.1 and F3.0->F2.0 respectively change
the omitted-decimal interpretation or truncate the exceptional spelling and fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-003: F input standard forms and decimal-location interpretation

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** Standard F input is an IEEE exceptional specification, a hexadecimal-significand number,
or a mantissa optionally followed by an exponent; decimal symbols override d, omitted
decimal symbols use the rightmost d digits as fractional digits, and E/D exponent forms
are accepted.

**Diagnostic obligation:** not-required.

**Facets:** `F-input-ieee-form`, `F-input-hex-significand-form`, `F-input-mantissa-with-decimal-symbol`, `F-input-omitted-decimal-uses-d`, `F-input-E-exponent-form`, `F-input-D-exponent-form`, `F-input-extra-digits-permitted`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.2-003 F-input runtime fixtures: five complete programs read standard input
forms into default real values that are exactly representable: uppercase INF with F3.0
as IEEE positive infinity, '1.5' with F3.0 as 1.5, '15' with F2.1 as 1.5, '1E+1' with
F4.0 as 10.0, and '1D+1' with F4.0 as 10.0. Descriptor substitutions narrow the field or
change d so the value or validity changes; hexadecimal-significand and extra-digit
latitude facets remain pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-004: D exponent input is processed identically to E exponent input

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** An exponent containing D is processed identically to one containing E.

**Diagnostic obligation:** not-required.

**Facets:** `D-exponent-same-as-E-exponent`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.2-004 D/E exponent runtime fixture: one complete program reads '1E+1' and
'1D+1' with F4.0 and requires the two exact default-real results to be equal to 10.0.
Substituting F4.1 for either read changes the omitted-decimal interpretation to 1.0 and
fails.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-006: IEEE exceptional input field syntax for infinities and NaNs

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** An IEEE exceptional input field consists of optional blanks, an optional sign,
INF/INFINITY or NAN with optional alphanumeric parenthesized payload, and optional
trailing blanks.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-infinity-input-syntax`, `ieee-nan-input-syntax`, `ieee-exceptional-leading-trailing-blanks`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.2-006 IEEE exceptional input syntax runtime fixtures: two complete programs
read uppercase ' INF ' with F5.0 and uppercase 'NAN()' with F5.0. The infinity case
checks IEEE positive infinity and the leading/trailing blanks; the NaN case checks IEEE
NaN. F5.0->F3.0 and F5.0->F2.0 substitutions truncate the required spelling and fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-007: IEEE exceptional input support restrictions and NaN latitude

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** INF/INFINITY shall not be used without IEEE infinity support, NAN shall not be used
without IEEE NaN support, NAN or NAN() produces a quiet NaN, other NaN payload values
and NaN sign interpretation are processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `ieee-infinity-support-required`, `ieee-nan-support-required`, `nan-empty-payload-quiet`, `nan-payload-processor-dependent`, `nan-sign-processor-dependent`.

**Oracle:** Support restrictions are context-dependent. NaN payload and sign interpretation are
explicit processor latitude.

S13.7.2.3.2-007 NaN empty-payload runtime fixture: one complete program reads uppercase
'NAN()' with F5.0 under the ieee-binary profile after initializing the real variable to
finite -99.0. It asserts ieee_class(value) == ieee_quiet_nan, proving p6's required
quiet-NaN result without asserting any payload value or sign interpretation. F5.0->F2.0
truncates the spelling and fails.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-008: Hexadecimal-significand input form and value

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** A hexadecimal-significand input has optional sign, 0X, a hexadecimal significand with
optional decimal symbol, and P signed decimal exponent; embedded blanks are not
permitted, trailing blanks are ignored, and the value is significand times two to the
exponent, negated if signed minus.

**Diagnostic obligation:** not-required.

**Facets:** `hex-input-0X-form`, `hex-input-decimal-symbol-position`, `hex-input-P-exponent`, `hex-input-no-embedded-blanks`, `hex-input-trailing-blanks-ignored`, `hex-input-value-power-of-two`, `hex-input-minus-negates`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-009: Nonstandard unacceptable F input causes an error condition

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** If an F input field has none of the standard forms and is not acceptable to the
processor, an error condition occurs.

**Diagnostic obligation:** not-required.

**Facets:** `F-input-nonstandard-error-condition`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-010: Infinity output chooses Infinity, Inf, or asterisks by field width

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** For IEEE infinity output, the field is blanks plus sign and Inf/Infinity, right
justified; field-width thresholds choose Infinity, Inf, or all asterisks.

**Diagnostic obligation:** not-required.

**Facets:** `infinity-output-right-justified`, `infinity-output-infinity-form-when-wide`, `infinity-output-inf-form-when-narrow`, `infinity-output-asterisks-when-too-narrow`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.2-010 infinity output runtime fixtures: two complete programs write IEEE
positive infinity under the ieee-binary profile. (SS,F9.1) expects exactly ' Infinity':
width 9 is at least the minimum for the Infinity form with no sign, SS suppresses the
optional plus, and the one leading blank proves right justification. (SS,F4.1) expects
exactly ' Inf' because width 4 is less than the eight positions needed for 'Infinity'
but at least the three positions needed for 'Inf'; (SS,F2.1) expects exactly '**'
because width 2 is below the minimum for any infinity form. Descriptor substitutions
F9.1->F8.1/F4.1, F4.1->F3.1, and F2.1->F3.1 select different fields and fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-011: NaN output form and processor-dependent payload characters

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** For IEEE NaN output, the field is blanks, NaN, and optionally processor-dependent
alphanumeric payload characters in parentheses; widths 1-2 produce asterisks and w=0
produces NaN.

**Diagnostic obligation:** not-required.

**Facets:** `nan-output-NaN-right-justified`, `nan-output-payload-processor-dependent`, `nan-output-too-narrow-asterisks`, `nan-output-w0-NaN`.

**Oracle:** NaN payload spelling is explicit processor latitude; only width-forced asterisks or w=0
NaN is an exact portable output plan under IEEE support.

S13.7.2.3.2-011 NaN output runtime fixture: one complete program writes an IEEE quiet
NaN under the ieee-binary profile without asserting any payload spelling or sign
interpretation. (SS,F5.1) writes exactly '  NaN': width 5 admits the right-justified NaN
form but cannot contain any one-to-w-5 alphanumeric payload character. (SS,F0.1) writes
exactly 'NaN' to a length-3 internal file because w=0 selects the NaN output field;
(SS,F2.1) writes exactly '**' because positive widths less than 3 are filled with
asterisks. Descriptor substitutions F5.1->F4.1, F0.1->F4.1, and F2.1->F3.1 select
different fields and fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

### S13.7.2.3.2-012: Finite F output digits, decimal symbol, sign, rounding, and leading-zero rule

**Source:** S13.7.2.3.2, J3/24-007, 18 December 2023, physical PDF296. **Class:** Effect.

**Definition:** Finite non-NaN F output consists of blanks, required minus or optional plus, digits with
a decimal symbol representing magnitude after scale factor and rounding to d fractional
digits; leading zeros are forbidden except that a zero immediately left of the decimal
symbol is required if otherwise no digit would appear.

**Diagnostic obligation:** not-required.

**Facets:** `F-finite-output-sign`, `F-finite-output-decimal-symbol`, `F-finite-output-d-fractional-digits`, `F-finite-output-leading-zero-rule`, `F-finite-output-rounded`.

**Oracle:** Plans are pending and source-derived. Positive plans use independent literal expected
strings, integer sentinels, or scalar state checks. Negative/source-control plans change
one property from a conforming control and are not treated as diagnostic facets unless
this unit itself requires rejection.

S13.7.2.3.2-012 finite F-output runtime fixtures: three complete programs write exact
finite values to internal character variables. (SS,F5.1) with -3.0 expects ' -3.0' for
the required minus; (SS,F5.2) with 1.25 expects ' 1.25' for POINT decimal symbol and two
fractional digits; and (SS,RZ,F3.0) with exact 0.25 expects ' 0.'. RZ pins 13.7.2.3.8
rounding toward zero, so 0.25 with d=0 is not a tie and rounds to magnitude zero;
without the zero required by p11 there would be no digits in the field. This proves only
the mandatory leading-zero half, not any rounding facet. All positive finite outputs use
SS. Descriptor substitutions F5.1->F5.2/F6.1, F5.2->F5.3/E12.4, and F3.0->F2.0 change
the chosen value's field and fail.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Pending output plans use internal WRITE to a CHARACTER variable where possible and name
exact expected characters including blanks. Plans that rely on real approximation,
optional signs, D/E exponent-letter alternatives, EX binary-exponent selection, IEEE NaN
payloads, Unicode/non-Unicode files, list-directed input/output, stream positioning, or
unregistered 13.8 mode descriptors are marked deferred or processor-dependent rather
than asserted. Unnumbered prose restrictions are not promoted to numbered-constraint
diagnostics.

F-editing fixture boundaries: this packet discharges only the selected 25 finite,
exponent, IEEE-input, infinity-output, and forced-width NaN-output facets removed from
pending above. It deliberately leaves F0 input source-control, hexadecimal-significand
input, embedded-blank controls, nonstandard processor-acceptable input, NaN payload/sign
latitude, explicit rounded-output facets, scale factor, leading-zero mode, decimal comma
mode, external files, diagnostics, and processor-dependent latitude pending. Expected
finite strings are hand-derived from 13.7.2.3.2, 13.7.2.3.8, 13.8.4, and 13.8.5 using
exactly representable binary values and internal WRITE/READ only; the RZ descriptor in
the leading-zero fixture pins rounding solely to make p11's mandatory no-digits case
observable.

**Dependencies:** Registered dependencies: 13.1-13.4 for format specifications, edit descriptors, fields,
and format-control interaction; clauses 6, 8, 9, 10, and 11 for source form, labels,
names, character values, expressions, arrays, and executable constructs; registered
clause 7 catalogues where already present for signed-digit strings, significands, BOZ
literal digits, and type concepts. Unregistered dependencies explicitly retained: clause
12 I/O statements, file connection, internal/external files, list-directed I/O, and
ROUND=/DECIMAL= controls; sibling 13.7.3-13.9 for character/logical/G editing and
control/mode descriptors; sibling 13.10-13.11 for remaining format features; clause 15
for defined I/O; clause 16 for IEEE and inquiry intrinsics in future fixtures; clause 17
for IEEE rounding/exceptional values; and clauses 3, 5, 14, and 19 where future fixtures
need terms, constants, programs, or scope rules.

<!-- END GENERATED 13.7.2.3.2 -->
