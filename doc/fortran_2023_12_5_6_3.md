# Fortran 2023 12.5.6.3: ACCESS= specifier

The canonical catalogue is `doc/catalogues/access_specifier_12_5_6_3.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 12.5.6.3 -->

### S12.5.6.3-001: ACCESS= value is SEQUENTIAL, DIRECT, or STREAM

**Source:** S12.5.6.3, J3/24-007, 18 December 2023, physical PDF252. **Class:** Effect.

**Definition:** The ACCESS= scalar default character expression shall evaluate to SEQUENTIAL, DIRECT, or
STREAM.

**Diagnostic obligation:** not-required.

**Facets:** `access-value-set-sequential-direct-stream`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.3-002: ACCESS= selects the connection access method

**Source:** S12.5.6.3, J3/24-007, 18 December 2023, physical PDF252. **Class:** Effect.

**Definition:** ACCESS= specifies the access method for the file connection as sequential, direct, or
stream.

**Diagnostic obligation:** not-required.

**Facets:** `access-selects-connection-method`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.3-003: Omitted ACCESS= defaults to SEQUENTIAL

**Source:** S12.5.6.3, J3/24-007, 18 December 2023, physical PDF252. **Class:** Effect.

**Definition:** If ACCESS= is omitted, the default value is SEQUENTIAL.

**Diagnostic obligation:** not-required.

**Facets:** `access-default-sequential`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.3-004: Existing file ACCESS= method must be allowed for the file

**Source:** S12.5.6.3, J3/24-007, 18 December 2023, physical PDF252. **Class:** Restriction.

**Definition:** For an existing file, the specified access method shall be included in the set of
allowed access methods for the file.

**Diagnostic obligation:** not-required.

**Facets:** `access-existing-file-method-allowed`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

### S12.5.6.3-005: New files are created with the specified ACCESS= method allowed

**Source:** S12.5.6.3, J3/24-007, 18 December 2023, physical PDF252. **Class:** Effect.

**Definition:** For a new file, the processor creates the file with a set of allowed access methods that
includes the specified method.

**Diagnostic obligation:** not-required.

**Facets:** `access-new-file-method-included`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by creating or opening a known external file, writing known nonblank
records, initializing variables to nonzero/nonblank sentinels, or setting an opposite
inquiry value) and then observe only portable properties: successful IOSTAT zero,
nonzero IOSTAT for a specified required error condition, named intrinsic status values
where the standard names them, exact internal character records where formatting rules
fix them, or INQUIRE results for properties established by the program. IOMSG text,
processor-dependent filenames, unit numbers, case interpretation, encodings,
record-marker representations, file-storage-unit sizes, specific IOSTAT numbers, and
processor-dependent defaults are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself is a numbered constraint
or syntax rule that requires rejection and a future fixture can observe the rejection
portably. Program restrictions expressed only as prose shall-statements are not
diagnostic facets here. IOMSG wording and processor-dependent numeric status values are
never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: earlier clause 12 file, unit, connection,
mode, and access-method concepts; later clause 12 CLOSE, data-transfer, positioning,
INQUIRE, IOSTAT/ERR/IOMSG, and asynchronous rules; clause 13 formatting and mode
effects; clause 15 defined I/O; clause 16 ISO_FORTRAN_ENV named constants and inquiry
support; clauses 3, 5, 14, 17, 18, and 19 where terminology, normal termination, images,
IEEE support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.5.6.3 -->
