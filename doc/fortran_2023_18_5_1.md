# Fortran 2023 18.5.1: Summary of contents

The canonical catalogue is `doc/catalogues/iso_fortran_binding_summary_18_5_1.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 18.5.1 -->

### S18.5.1-001: ISO_Fortran_binding.h contains the specified structures, typedefs, macros, and prototypes

**Source:** S18.5.1 p1 required contents; J3/24-007, 18 December 2023, physical PDF531. **Class:** Effect.

**Definition:** ISO_Fortran_binding.h shall contain the C structure definitions, typedef declarations,
macro definitions, and function prototypes specified in 18.5.2 through 18.5.5.

**Diagnostic obligation:** not-required.

**Facets:** `iso-fortran-binding-required-structures`, `iso-fortran-binding-required-typedefs`, `iso-fortran-binding-required-macros`, `iso-fortran-binding-required-function-prototypes`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.1-002: ISO_Fortran_binding.h declarations let C functions interpret descriptors for non-interoperable dummies

**Source:** S18.5.1 p1 C descriptor use; J3/24-007, 18 December 2023, physical PDF531. **Class:** Effect.

**Definition:** The definitions and declarations in ISO_Fortran_binding.h can be used by a C function to
interpret and manipulate a C descriptor and to specify a C prototype interoperating with
a Fortran interface that has a non-interoperable dummy variable.

**Diagnostic obligation:** not-required.

**Facets:** `c-function-interprets-c-descriptor`, `c-prototype-for-noninteroperable-dummy`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.1-003: ISO_Fortran_binding.h inclusion is order-independent and idempotent apart from line numbers

**Source:** S18.5.1 p2; J3/24-007, 18 December 2023, physical PDF531. **Class:** Effect.

**Definition:** ISO_Fortran_binding.h may be included in any order relative to standard C headers and
may be included more than once in a scope with no effect different from a single
inclusion other than line numbers.

**Diagnostic obligation:** not-required.

**Facets:** `include-before-standard-c-headers`, `include-after-standard-c-headers`, `include-multiple-times-idempotent`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.1-004: C sources including ISO_Fortran_binding.h shall not use reserved CFI names or macro-redefine header structures

**Source:** S18.5.1 p3 restrictions; J3/24-007, 18 December 2023, physical PDF531. **Class:** Restriction.

**Definition:** A C source file that includes ISO_Fortran_binding.h shall not use CFI_-prefixed names
not defined in the header and shall not define the header-defined structure names as
macro names.

**Diagnostic obligation:** not-required.

**Facets:** `no-undefined-cfi-prefixed-user-names`, `no-macro-redefinition-of-header-structure-names`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

### S18.5.1-005: ISO_Fortran_binding.h exposes only CFI, underscore, member, or standard-header names

**Source:** S18.5.1 p3 header namespace; J3/24-007, 18 December 2023, physical PDF531. **Class:** Effect.

**Definition:** All names other than structure member names defined in ISO_Fortran_binding.h begin with
CFI_ or an underscore character, or are names defined by a standard C header that
ISO_Fortran_binding.h includes.

**Diagnostic obligation:** not-required.

**Facets:** `header-nonmember-name-namespace`.

**Oracle:** Source registration only. Future mixed C/Fortran fixtures compile a C translation unit
with the processor ISO_Fortran_binding.h supplied by the harness and assert only
portable properties: required type and member presence, required member ordering, macro
availability, integer-constant expression usability, sign and distinctness relations
fixed by the text, CFI_VERSION equality inside processor-created descriptors, descriptor
rank/extent/lower-bound/stride relations, and named error-code results. Numeric macro
values, descriptor storage addresses, unsupported C type availability, extra processor
extension codes, line-number effects, IOMSG text, and allocation-failure occurrence are
not hard-coded.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
The existing C-interoperability harness supports separate Fortran and C build steps,
processor-header staging via fortran_binding_header, and compiler-header provenance
checks; existing descriptor-related fixtures exercise that harness for other catalogues
but are not rebound here. Unnumbered program restrictions are recorded without
diagnostic facets.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, and 16 where already
catalogued for source form, declarations, intrinsic types and kinds, arrays, execution,
mixed-language harnesses, procedure interfaces, ISO_C_BINDING constants, and exact
inquiry helpers. Unregistered dependencies retained: earlier Clause 18 interoperability
rules, later 18.5.5 descriptor functions, clause 19 storage and association terms, and
the C standard library headers named or included by ISO_Fortran_binding.h.

<!-- END GENERATED 18.5.1 -->
