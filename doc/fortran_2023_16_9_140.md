# Fortran 2023 16.9.140: MERGE_BITS

The canonical catalogue is `doc/catalogues/merge_bits_intrinsic_16_9_140.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.140")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.140 -->

### S16.9.140-001: MERGE_BITS admits integer and bounded BOZ argument forms

**Source:** S16.9.140, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** I, J, and MASK may be integer arguments, and a BOZ literal constant is admitted for any
one side where the restrictions allow it.

**Diagnostic obligation:** not-required.

**Facets:** `MERGE_BITS-I-integer-or-BOZ-admitted`, `MERGE_BITS-J-integer-or-BOZ-admitted`, `MERGE_BITS-MASK-integer-or-BOZ-admitted`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MERGE_BITS occurrence and no
S16.9.140 binding. BOZ plans use actual BOZ literal arguments, and bit results are
observed only in bit context.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.140-002: MERGE_BITS argument restrictions

**Source:** S16.9.140, J3/24-007, 18 December 2023, physical PDF434. **Class:** Restriction.

**Definition:** I and J shall be integer or BOZ, integer I and J shall have the same kind, I and J shall
not both be BOZ literals, and integer MASK shall have the same kind as each other
integer argument.

**Diagnostic obligation:** not-required.

**Facets:** `MERGE_BITS-I-J-type-or-BOZ`, `MERGE_BITS-integer-I-J-same-kind`, `MERGE_BITS-I-J-not-both-BOZ`, `MERGE_BITS-MASK-type-or-BOZ`, `MERGE_BITS-integer-MASK-same-kind`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MERGE_BITS occurrence and no
S16.9.140 binding. BOZ plans use actual BOZ literal arguments, and bit results are
observed only in bit context.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.140-003: MERGE_BITS result kind follows the integer source argument

**Source:** S16.9.140, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** The MERGE_BITS result has the same type and kind as I for integer I, otherwise the same
type and kind as J.

**Diagnostic obligation:** not-required.

**Facets:** `MERGE_BITS-result-kind-from-I`, `MERGE_BITS-result-kind-from-J-with-BOZ-I`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MERGE_BITS occurrence and no
S16.9.140 binding. BOZ plans use actual BOZ literal arguments, and bit results are
observed only in bit context.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.140-004: MERGE_BITS converts BOZ operands and merges bit selections

**Source:** S16.9.140, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** BOZ literal arguments are converted as by INT to the result type and kind, and the
result value is IOR(IAND(I,MASK), IAND(J,NOT(MASK))).

**Diagnostic obligation:** not-required.

**Facets:** `MERGE_BITS-BOZ-converted-as-INT`, `MERGE_BITS-I-bits-selected-by-MASK`, `MERGE_BITS-J-bits-selected-by-NOT-MASK`, `MERGE_BITS-result-bit-expression`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json found no MERGE_BITS occurrence and no
S16.9.140 binding. BOZ plans use actual BOZ literal arguments, and bit results are
observed only in bit context.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.140 -->
