# Fortran 2023 16.9.139: MERGE

The canonical catalogue is `doc/catalogues/merge_intrinsic_16_9_139.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.139")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.139 -->

### S16.9.139-001: MERGE admits TSOURCE of any type

**Source:** S16.9.139, J3/24-007, 18 December 2023, physical PDF433. **Class:** Effect.

**Definition:** TSOURCE may be of any type.

**Diagnostic obligation:** not-required.

**Facets:** `MERGE-TSOURCE-any-type-admitted`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ found incidental MERGE use in profiles, generators, and unrelated
assertions; tests/fixtures/*/fixture.json has no MERGE binding and no S16.9.139 binding.
Reader extraction for p5 and p6 has the known census-hash-drift math-glyph issue; the
registry census hashes are retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.139-002: MERGE FSOURCE and MASK argument restrictions

**Source:** S16.9.139, J3/24-007, 18 December 2023, physical PDF433. **Class:** Restriction.

**Definition:** FSOURCE shall have the same type and type parameters as TSOURCE, and MASK shall be
logical.

**Diagnostic obligation:** not-required.

**Facets:** `MERGE-FSOURCE-same-type-and-parameters`, `MERGE-MASK-logical`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ found incidental MERGE use in profiles, generators, and unrelated
assertions; tests/fixtures/*/fixture.json has no MERGE binding and no S16.9.139 binding.
Reader extraction for p5 and p6 has the known census-hash-drift math-glyph issue; the
registry census hashes are retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.139-003: MERGE result type, parameters, and polymorphism follow the sources

**Source:** S16.9.139, J3/24-007, 18 December 2023, physical PDF433. **Class:** Effect.

**Definition:** The MERGE result has the same type and type parameters as TSOURCE, and is polymorphic
exactly for paired polymorphic TSOURCE and FSOURCE.

**Diagnostic obligation:** not-required.

**Facets:** `MERGE-result-same-type-and-parameters`, `MERGE-result-polymorphic-iff-both-sources-polymorphic`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ found incidental MERGE use in profiles, generators, and unrelated
assertions; tests/fixtures/*/fixture.json has no MERGE binding and no S16.9.139 binding.
Reader extraction for p5 and p6 has the known census-hash-drift math-glyph issue; the
registry census hashes are retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.139-004: MERGE selects TSOURCE for true MASK and FSOURCE otherwise

**Source:** S16.9.139, J3/24-007, 18 December 2023, physical PDF433. **Class:** Effect.

**Definition:** The MERGE result is TSOURCE for true MASK values and FSOURCE for false MASK values.

**Diagnostic obligation:** not-required.

**Facets:** `MERGE-true-mask-selects-TSOURCE`, `MERGE-false-mask-selects-FSOURCE`, `MERGE-array-mask-elementwise-selection`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details are separated from exact result obligations. Unnumbered
argument shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ found incidental MERGE use in profiles, generators, and unrelated
assertions; tests/fixtures/*/fixture.json has no MERGE binding and no S16.9.139 binding.
Reader extraction for p5 and p6 has the known census-hash-drift math-glyph issue; the
registry census hashes are retained after visual source comparison.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.139 -->
