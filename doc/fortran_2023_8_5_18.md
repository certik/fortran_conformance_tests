# Fortran 2023 8.5.18: TARGET attribute

Source review is recorded in batch064. Effective status comes from
`Registry.catalogue_review_state("8.5.18")`; current facet states remain in
the catalogue. Source classification does not approve fixtures or execution.
The canonical catalogue is `doc/catalogues/target_attribute_8_5_18.json`.

The seven original base units are on physical PDF page 133, printed page119,
of J3/24-007, December18,2023, 688 pages, SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.
The complete section hash is
`f3ee0100a4c4afc7ffbfbb5ddf18c5aac7641d8bbd5f8ace3b1133475c7c29fa`.
The actual next heading is 8.5.19 VALUE attribute on the same physical page;
no continuation, table or other section's body is attributed to TARGET.

TARGET is data-target eligibility, not POINTER, association, allocation,
defined payload or extended lifetime. An eligible target designator still
obeys10.2.2, including the distinct pointer-result and variable routes.
Neither arbitrary expressions nor vector-subscripted sections become valid
targets because a related object has TARGET. Copying a target's value to a
separate object does not transfer its TARGET attribute.

Nonpointer subobjects inherit TARGET. Pointer components remain pointers;
following one can leave the original aggregate, and its pointee is not
thereby a subobject. Allocation of a pointer creates a target object under
9.7.1.4, not a pointer entity with both incompatible attributes. The three
notes retain those explanatory roles and do not create new allocation or
declaration executions.

Argument association has its own premises. A TARGET dummy can receive an
actual not declared TARGET, but pointer persistence is not universal.
15.5.2.5p8-p12 distinguishes guaranteed, processor-dependent and undefined
post-return association, including TARGET+VALUE.15.5.2.13 governs presence;
15.5.2.14 supplies the separate aliasing restrictions. Undefined or escaped
pointer status is not queried with ASSOCIATED or treated as disassociated.
TARGET does not prevent deallocation or unsaved-local lifetime termination.

C865/C852 and C866/C854 overlaps are explicit future canonical-source gates,
not isolated negative evidence. C1028 retains target-designator diagnostic
ownership. Runtime propagation plans use real live subobjects and literal
effects; none is downgraded to compilation when a processor lacks support.

Only these source definitions and pending plans are authored. No fixture,
compiler observation, role retagging, profile, approval, link or SourceUse
is supplied. The additive local index overlay is excluded from the commit;
the handoff reports its exact source-context effect without renewing any
historical receipt or relabelling an execution.

<!-- BEGIN GENERATED 8.5.18 -->

### C865: A TARGET entity is a variable

**Source:** 8.5.18 C865, physical PDF133, printed119 **Class:** Restriction.

**Definition:** TARGET applies to a variable, not a named constant or procedure entity. It does not
require the variable to be a pointer, allocated, already defined, or already associated
with a pointer.

**Diagnostic obligation:** required.

**Facets:** `ordinary-variable-admission`, `named-constant-exclusion`, `procedure-entity-source-gate`, `specification-route-source`.

**Oracle:** Source-qualified variable admissions and minimally repaired nonvariable declarations.
Each negative needs a complete valid control, a causal role report and legitimate staged
subject origins. Jointly governing constraints remain explicit gates, not claimed
isolated evidence.

**Oracle limitation:** 4.2 requires reporting capability for numbered constraints, not rejection, a particular
status, printed code or English sentence. Broad role nouns, wrong-source reports,
echoes, quoted examples, unsupported features, Internal/ASR/verifier failures, crashes
and resource failures cannot establish this predicate. No compiler observations or
approvals are supplied.

**Dependencies:** 8.1;8.2 R801-R804/C810;8.5.13p1/C852;8.6.15 R862-R863;4.2p2.

### C866: TARGET and POINTER cannot be attributes of the same entity

**Source:** 8.5.18 C866, physical PDF133, printed119 **Class:** Restriction.

**Definition:** An entity with TARGET does not also have POINTER. A pointer may designate a target
without itself acquiring TARGET, and a TARGET object can contain pointer components
without giving those components both attributes.

**Diagnostic obligation:** required.

**Facets:** `combined-attribute-exclusion`, `separate-specification-exclusion`, `pointer-target-entity-boundary`.

**Oracle:** One precisely qualified same-entity attribute-conflict family, minimal
single-attribute/statement repairs and canonical pointer-versus-pointee source
boundaries. C854's defining ownership is preserved.

**Oracle limitation:** No fatal status or exact diagnostic vocabulary is mandated. A generic pointer, target or
attribute report alone is insufficient; the full conflict and staged origins are
required. Pointer declaration is not association, and an undefined pointer is not
queried just to make a compile control observable.

**Dependencies:** 8.5.14 C854/p1;8.5.18p2/note1/note3;8.6.15;9.7.1.4;10.2.2.2 C1028;4.2p2.

### S8.5.18-001: TARGET supplies qualified data-pointer target eligibility

**Source:** 8.5.18 p1, physical PDF133, printed119 **Class:** Restriction.

**Definition:** TARGET permits a data object to be a pointer target; an object without target
eligibility is not a data-pointer target. This attribute rule neither performs
association nor waives the canonical target-expression, type, parameter, rank, state,
image or lifetime conditions. In particular, an actual lacking a TARGET declaration can
correspond to a TARGET dummy under the separate argument rules; this is not a blanket
ban on that call or permanent attribute transfer to the actual.

**Diagnostic obligation:** not-required.

**Facets:** `explicit-live-target-admission`, `pointer-allocation-source`, `nontarget-exclusion-canonical-source`, `ordinary-target-dummy-source`, `processor-dependent-dummy-source`, `lifetime-and-presence-source`.

**Oracle:** Finite lawful target-admission positive controls and exact source relations to canonical
pointer/argument/lifetime owners. Pure prose does not acquire a universal
mandatory-rejection policy. Source-only and context-only dependencies supply no new
runtime or facet credit until separately reviewed.

**Oracle limitation:** TARGET does not imply POINTER, allocation, initialized data, static lifetime, aliasing
permission or a concrete passing ABI. Assigning a target's value to a separate object
does not transfer the TARGET attribute; that object needs its own eligibility.
ASSOCIATED requires defined pointer status; its two-argument zero-size/zero-storage
results differ from mere association. A processor-dependent or undefined status is never
read to force an answer. Required runtime mechanisms stay runtime plans even when a
processor lacks support.

**Dependencies:** 8.1p1;10.2.1.3p1;10.2.2.1-.3/C1016-C1029;9.7.1.4;15.4.2.2;15.5.2.4/.5/.8/.13/.14;16.9.20;19.5.2.1-.5;19.6.1/.2/.5/.6;C.11.4;4.2p2/p6.

### S8.5.18-002: Nonpointer subobjects inherit their object's TARGET attribute

**Source:** 8.5.18 p2, physical PDF133, printed119 **Class:** Effect.

**Definition:** Every nonpointer subobject of an object with TARGET also has TARGET. This includes
eligible nested and array/character subobjects, not the pointer attribute of pointer
components. A pointee reached through a pointer is not thereby a subobject of the
original aggregate, and propagation does not establish allocation, association, defined
payload or lifetime.

**Diagnostic obligation:** not-required.

**Facets:** `scalar-component-inheritance`, `nested-nonpointer-inheritance`, `array-element-and-section-inheritance`, `character-substring-inheritance`, `allocated-nonpointer-subobject-inheritance`, `pointer-component-boundary`.

**Oracle:** Actual subobjects in complete run/effect programs, compatible live pointers and
independent literal changes to the identified parent or pointee, with
visit/return/completion guards. Inherited target eligibility is observed without adding
TARGET to forbidden component grammar or using a copied object as a proxy.

**Oracle limitation:** No automatic declaration effect is credited by a no-op program. Pointer/allocatable
state and defined payload are established separately before observation. Zero-size
arrays and zero-length strings are always defined under19.6.2, but that is not pointer
association or an override of16.9.20's two-argument result. No negative twin, diagnostic
mandate, forced runtime trap or compile-only downgrade is invented for this propagation
effect.

**Dependencies:** 7.5.1p3-p6;9.4.2p1/p3-p6;8.5.18note3;10.2.2.2
C1028/10.2.2.3;16.9.20;19.5.2;19.6.1/.2;15.5.2.14.

<!-- END GENERATED 8.5.18 -->
