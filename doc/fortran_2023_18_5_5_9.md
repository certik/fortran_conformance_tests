# Fortran 2023 18.5.5.9: CFI_setpointer

The canonical catalogue is `doc/catalogues/cfi_setpointer_18_5_5_9.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted are listed in the catalogue. Fine census units are recorded there for mixed or compound source units.

<!-- BEGIN GENERATED 18.5.5.9 -->

### S18.5.5.9-001: CFI_setpointer has the ISO_Fortran_binding.h pointer-association interface

**Source:** S18.5.5.9, J3/24-007, 18 December 2023, physical PDF542. **Class:** Effect.

**Definition:** CFI_setpointer has the interface int CFI_setpointer(CFI_cdesc_t *result, CFI_cdesc_t
*source, const CFI_index_t lower_bounds[]) and updates a descriptor for a Fortran
pointer association or disassociation.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_setpointer-interface`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.9-002: CFI_setpointer actual arguments must satisfy pointer-result, source, rank/type/length, and lower-bound restrictions

**Source:** S18.5.5.9, J3/24-007, 18 December 2023, physical PDF542. **Class:** Restriction.

**Definition:** The result descriptor shall describe a Fortran pointer, source shall be null or describe
an allowed allocated, pointer, or nonallocatable nonpointer object that is not
assumed-size, corresponding rank and type shall match, element length shall match except
for deferred-length character pointer results, and nonzero-rank nonnull source calls
require adequate lower_bounds storage or a null lower_bounds argument.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_setpointer-result-pointer-descriptor`, `CFI_setpointer-source-allowed-or-null`, `CFI_setpointer-rank-type-and-elem-len-match`, `CFI_setpointer-lower-bounds-array`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.9-003: Successful CFI_setpointer disassociates or associates the pointer descriptor and applies lower bounds and deferred character length

**Source:** S18.5.5.9, J3/24-007, 18 December 2023, physical PDF542. **Class:** Effect.

**Definition:** Successful CFI_setpointer makes result describe a disassociated pointer for null or
disassociated source, or otherwise describe the source object with optional replacement
lower bounds; a deferred-length character pointer result receives source elem_len.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_setpointer-null-or-disassociated-source-disassociates`, `CFI_setpointer-associated-source-copies-object-and-lower-bounds`, `CFI_setpointer-deferred-character-elem-len-from-source`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.9-004: Detected CFI_setpointer errors preserve the result descriptor

**Source:** S18.5.5.9, J3/24-007, 18 December 2023, physical PDF542. **Class:** Effect.

**Definition:** After a detected CFI_setpointer error, the result descriptor is not modified.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_setpointer-detected-error-preserves-result`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

### S18.5.5.9-005: CFI_setpointer returns an error indicator

**Source:** S18.5.5.9, J3/24-007, 18 December 2023, physical PDF542. **Class:** Effect.

**Definition:** The result of CFI_setpointer is an error indicator.

**Diagnostic obligation:** not-required.

**Facets:** `CFI_setpointer-result-error-indicator`.

**Oracle:** Source registration only. Future mixed Fortran/C fixtures compile a C companion with the
processor ISO_Fortran_binding.h, link it with a Fortran driver through the existing C
interoperability harness, and assert only portable descriptor members, pointer equality
or address differences computed from descriptor storage strides, CFI_SUCCESS equal to
zero, named CFI error macros by identity rather than numeric value, and exact
integer/logical status results. Numeric values of error macros other than CFI_SUCCESS, C
object padding, alignment choices beyond successful creation, allocation addresses, and
selection among several detected errors remain processor latitude.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Caller shall-restrictions are source-control restrictions and are not promoted to
diagnostic facets. Named-error plans use only isolated conditions mapped by 18.5.4 Table
18.5; conditions without an isolated named code, or calls with multiple simultaneous
errors, remain no-oracle for the specific returned code.

**Dependencies:** Registered dependencies: existing source-form, type, kind, declaration, expression,
assignment, execution, allocation, pointer, array, derived-type, interoperability, and
Clause 16 intrinsic-module foundations where already catalogued. Unregistered
dependencies retained: sibling Clause 18 descriptor object, descriptor member,
ISO_Fortran_binding.h macro, type-code, attribute-code, and error-code sections, C
companion compilation support, and C standard alignment, offsetof, pointer, and
object-representation terminology reached by future fixtures.

<!-- END GENERATED 18.5.5.9 -->
