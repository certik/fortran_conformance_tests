# Fortran 2023 12.1: Input/output concepts

The canonical catalogue is `doc/catalogues/input_output_concepts_12_1.json`. This source packet records source accounting. Selected I/O records/files fixtures now supply executable standard-oracle observations and mutation plans; fixture approval remains separate.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN IO RECORDS FILES FIXTURES -->
## I/O records and files runtime observations

Five complete single-image fixtures discharge 31 selected facets in 12.1-12.3.2. They use internal character files, scratch sequential files, and local named files deleted by the program. All input targets and inquiry logicals start from distinctive sentinels with pre-transfer guards; character comparisons check LEN first; endfile observations compare only with ISO_FORTRAN_ENV IOSTAT_END. Permanent mutations remove or substitute the relevant WRITE/READ/REWIND/ENDFILE/INQUIRE/OPEN/CLOSE feature and alter sentinels/oracles; no fixture asserts byte sizes, record-marker layout, IOMSG text, numeric IOSTAT values, preconnected-file identity, or coarray behavior.
<!-- END IO RECORDS FILES FIXTURES -->

<!-- BEGIN GENERATED 12.1 -->

### S12.1-001: Input and output statements define reading, writing, and optional editing

**Source:** S12.1, J3/24-007, 18 December 2023, physical PDF242. **Class:** Effect.

**Definition:** Input statements transfer data from external media or internal files to internal storage
and are called reading; output statements transfer data from internal storage to
external media or internal files and are called writing; some input/output statements
specify editing.

**Diagnostic obligation:** not-required.

**Facets:** `input-reading-transfer`, `output-writing-transfer`, `editing-specified-by-some-io-statements`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.1-001 I/O records/files runtime fixtures: the internal formatted-transfer program
initializes a character internal file to '#', writes exact characters with
WRITE(record,'(I3,1X,A)'), then initializes integer and character input targets to
distinctive sentinels and READs the same record back with the same explicit format.
Exact record text, IOSTAT zero, and changed target values prove reading, writing, and
specified editing without external representation assumptions.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.1-001 I/O records/files fixture boundaries: These fixtures are single-image positive
runtime observations over internal CHARACTER files, scratch external files, and
conservative local named files created in the execution working directory and deleted by
the program. They assert only exact integers/logicals/characters, LEN before character
equality, IOSTAT zero, and equality with the named IOSTAT_END constant. They do not
assert byte sizes, record marker layout, IOMSG text, numeric IOSTAT values, ambient file
existence, path syntax beyond the chosen local names, preconnected file identity,
coarray image interactions, stream/record correspondence, or required diagnostics for
mismatched forms/nonexistent files. Remaining facets stay pending with their
source-accounting rationale.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.1-002: Auxiliary input/output statements manipulate or inquire about an external medium

**Source:** S12.1, J3/24-007, 18 December 2023, physical PDF242. **Class:** Effect.

**Definition:** Besides data-transfer statements, auxiliary input/output statements manipulate the
external medium or describe/inquire about properties of a connection to it.

**Diagnostic obligation:** not-required.

**Facets:** `auxiliary-manipulates-external-medium`, `auxiliary-inquires-connection-properties`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.1-002 I/O records/files runtime fixtures: the external formatted-record program
opens a scratch sequential formatted external file, INQUIREs OPENED=.true., writes
records, REWINDs, and reads them in order. The REWIND is load-bearing because deleting
it leaves the file position at terminal point; the INQUIRE logical is initialized
opposite to the expected value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.1-002 I/O records/files fixture boundaries: These fixtures are single-image positive
runtime observations over internal CHARACTER files, scratch external files, and
conservative local named files created in the execution working directory and deleted by
the program. They assert only exact integers/logicals/characters, LEN before character
equality, IOSTAT zero, and equality with the named IOSTAT_END constant. They do not
assert byte sizes, record marker layout, IOMSG text, numeric IOSTAT values, ambient file
existence, path syntax beyond the chosen local names, preconnected file identity,
coarray image interactions, stream/record correspondence, or required diagnostics for
mismatched forms/nonexistent files. Remaining facets stay pending with their
source-accounting rationale.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.1-003: The Fortran input/output statements are the listed statement forms

**Source:** S12.1, J3/24-007, 18 December 2023, physical PDF242. **Class:** Effect.

**Definition:** The input/output statement set is BACKSPACE, CLOSE, ENDFILE, FLUSH, INQUIRE, OPEN,
PRINT, READ, REWIND, WAIT, and WRITE.

**Diagnostic obligation:** not-required.

**Facets:** `listed-io-statement-set`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.1-004: Files are record files or stream files by composition

**Source:** S12.1, J3/24-007, 18 December 2023, physical PDF242. **Class:** Effect.

**Definition:** A file is composed of either file storage units or records; record files are composed of
records and stream files are composed of file storage units.

**Diagnostic obligation:** not-required.

**Facets:** `file-storage-unit-composition`, `record-file-composition`, `stream-file-composition`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.1-004 I/O records/files runtime fixtures: one scratch sequential formatted file is
observed as a record file by reading back two nonempty records around an empty formatted
record. No byte count, newline encoding, or stream/record correspondence is asserted;
stream and file-storage-unit latitude facets remain pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.1-004 I/O records/files fixture boundaries: These fixtures are single-image positive
runtime observations over internal CHARACTER files, scratch external files, and
conservative local named files created in the execution working directory and deleted by
the program. They assert only exact integers/logicals/characters, LEN before character
equality, IOSTAT zero, and equality with the named IOSTAT_END constant. They do not
assert byte sizes, record marker layout, IOMSG text, numeric IOSTAT values, ambient file
existence, path syntax beyond the chosen local names, preconnected file identity,
coarray image interactions, stream/record correspondence, or required diagnostics for
mismatched forms/nonexistent files. Remaining facets stay pending with their
source-accounting rationale.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.1-005: A processor may expose both record and stream views with processor-dependent correspondence

**Source:** S12.1, J3/24-007, 18 December 2023, physical PDF242. **Class:** Effect.

**Definition:** A processor may allow the same file to be viewed both as a record file and as a stream
file; if so, the relationship between stream file storage units and record-file records
is processor dependent.

**Diagnostic obligation:** not-required.

**Facets:** `dual-record-stream-view-permitted`, `record-stream-correspondence-processor-dependent`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

### S12.1-006: Files are classified as external or internal

**Source:** S12.1, J3/24-007, 18 December 2023, physical PDF242. **Class:** Effect.

**Definition:** A file is either an external file or an internal file.

**Diagnostic obligation:** not-required.

**Facets:** `external-or-internal-file-classification`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
(for example by writing known records, initializing variables to nonzero/nonblank
sentinels, or creating a known file) and then observe only portable properties: exact
internal records, successful IOSTAT zero, nonzero IOSTAT for a specified error
condition, or named intrinsic status values where the standard names them.
Processor-dependent unit numbers, file names, record lengths, storage-unit encodings,
buffering, timing, IOMSG text, and specific error numbers are not asserted.

S12.1-006 I/O records/files runtime fixtures: internal-file transfer is observed through
a CHARACTER variable and external-file transfer through scratch/named unit connections,
with exact data effects only.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Negative plans are source-control plans unless this unit itself requires rejection and a
future fixture can observe the rejection portably. IOMSG wording and processor-dependent
numeric status values are never an oracle.

S12.1-006 I/O records/files fixture boundaries: These fixtures are single-image positive
runtime observations over internal CHARACTER files, scratch external files, and
conservative local named files created in the execution working directory and deleted by
the program. They assert only exact integers/logicals/characters, LEN before character
equality, IOSTAT zero, and equality with the named IOSTAT_END constant. They do not
assert byte sizes, record marker layout, IOMSG text, numeric IOSTAT values, ambient file
existence, path syntax beyond the chosen local names, preconnected file identity,
coarray image interactions, stream/record correspondence, or required diagnostics for
mismatched forms/nonexistent files. Remaining facets stay pending with their
source-accounting rationale.

**Dependencies:** Registered dependencies: clauses 6, 8, 9, 10, and 11 for source form, names,
expressions, execution constructs, and existing declaration/execution foundations.
Unregistered dependencies explicitly retained: later clause 12 connection, specifier,
file-position, data-transfer, internal-file, and input/output-item rules; clause 13
formatting; clause 15 defined I/O; clause 16 inquiry intrinsics where future fixtures
need them; clauses 3, 5, 14, 17, 18, and 19 where terminology, constants, images, IEEE
support, deleted features, or scope/lifetime controls are reached.

<!-- END GENERATED 12.1 -->
