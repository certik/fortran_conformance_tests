# Fortran 2023 16.9.176: RRSPACING

The canonical catalogue is `doc/catalogues/rrspacing_intrinsic_16_9_176.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.176")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.176 -->

### S16.9.176-001: RRSPACING argument restriction

**Source:** S16.9.176, J3/24-007, 18 December 2023, physical PDF452. **Class:** Restriction.

**Definition:** X shall be of type real.

**Diagnostic obligation:** not-required.

**Facets:** `RRSPACING-X-real`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. RRSPACING model-value fixtures must assert portable relations among
FRACTION, RADIX, EPSILON, IEEE predicates, and the selected model number, not hard-coded
numeric model constants.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. RRSPACING exact numeric model values remain processor-model
latitude; the pending plans therefore compare the intrinsic to standard inquiry
relations and model predicates rather than to a fixed decimal value.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.176-002: RRSPACING result characteristics

**Source:** S16.9.176, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** The RRSPACING result has the same type and kind type parameter as X.

**Diagnostic obligation:** not-required.

**Facets:** `RRSPACING-result-same-type-and-kind-as-X`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. RRSPACING model-value fixtures must assert portable relations among
FRACTION, RADIX, EPSILON, IEEE predicates, and the selected model number, not hard-coded
numeric model constants.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. RRSPACING exact numeric model values remain processor-model
latitude; the pending plans therefore compare the intrinsic to standard inquiry
relations and model predicates rather than to a fixed decimal value.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.176-003: RRSPACING finite value follows the model formula

**Source:** S16.9.176, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** For finite X, RRSPACING has the value ABS(FRACTION(Y))*RADIX(X)/EPSILON(X), where Y is
the nearest model value of X with X's kind.

**Diagnostic obligation:** not-required.

**Facets:** `RRSPACING-finite-model-formula`, `RRSPACING-negative-argument-uses-ABS-fraction`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. RRSPACING model-value fixtures must assert portable relations among
FRACTION, RADIX, EPSILON, IEEE predicates, and the selected model number, not hard-coded
numeric model constants.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. RRSPACING exact numeric model values remain processor-model
latitude; the pending plans therefore compare the intrinsic to standard inquiry
relations and model predicates rather than to a fixed decimal value.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.176-004: RRSPACING nearest-model tie chooses greater absolute value

**Source:** S16.9.176, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** At an equal-distance tie between two nearest model values, RRSPACING uses the value of
greater absolute value for Y.

**Diagnostic obligation:** not-required.

**Facets:** `RRSPACING-nearest-model-tie-greater-absolute-value`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. RRSPACING model-value fixtures must assert portable relations among
FRACTION, RADIX, EPSILON, IEEE predicates, and the selected model number, not hard-coded
numeric model constants.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. RRSPACING exact numeric model values remain processor-model
latitude; the pending plans therefore compare the intrinsic to standard inquiry
relations and model predicates rather than to a fixed decimal value.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.176-005: RRSPACING IEEE infinity yields IEEE NaN

**Source:** S16.9.176, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** For IEEE infinity X, the RRSPACING result is an IEEE NaN.

**Diagnostic obligation:** not-required.

**Facets:** `RRSPACING-IEEE-infinity-result-NaN`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. RRSPACING model-value fixtures must assert portable relations among
FRACTION, RADIX, EPSILON, IEEE predicates, and the selected model number, not hard-coded
numeric model constants.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. RRSPACING exact numeric model values remain processor-model
latitude; the pending plans therefore compare the intrinsic to standard inquiry
relations and model predicates rather than to a fixed decimal value.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

### S16.9.176-006: RRSPACING IEEE NaN returns that NaN

**Source:** S16.9.176, J3/24-007, 18 December 2023, physical PDF452. **Class:** Effect.

**Definition:** For IEEE NaN X, the RRSPACING result is that NaN.

**Diagnostic obligation:** not-required.

**Facets:** `RRSPACING-IEEE-NaN-propagated`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, character, kind, shape, status, type-acceptance, or
bit-pattern observations. Processor-dependent approximations and processor-dependent
result values are separated from exact result obligations. Unnumbered Clause 16 argument
shall-restrictions are source-control restrictions only and are not promoted to
diagnostic facets. RRSPACING model-value fixtures must assert portable relations among
FRACTION, RADIX, EPSILON, IEEE predicates, and the selected model number, not hard-coded
numeric model constants.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ and tests/fixtures/*/fixture.json for REAL, REDUCE, REPEAT, RESHAPE,
RRSPACING, and SAME_TYPE_AS found no existing fixture-json binding to 16.9.172-16.9.177
in this worktree. Existing REDUCE and REPEAT hits are do-concurrent and I/O syntax
fixtures, REAL hits are type/literal text or output labels, and SAME_TYPE_AS appears
only in an unrelated Clause 7 diagnostic fixture; those uses remain existing coverage
and are not rebound. RRSPACING exact numeric model values remain processor-model
latitude; the pending plans therefore compare the intrinsic to standard inquiry
relations and model predicates rather than to a fixed decimal value.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, and 13 where already catalogued
for source form, intrinsic types and kinds, declarations, expressions, arrays, pointer
and allocation state, execution, internal I/O, and exact observation helpers.
Unregistered dependencies retained: clause 16.3 bit-sequence model, clause 16.4 numeric
model terminology, sibling 16.9 intrinsics named by pending plans such as ABS, FRACTION,
RADIX, EPSILON, PRODUCT, SHAPE, SIZE, STORAGE_SIZE, TRANSFER, and IEEE inquiry helpers,
and clauses 14 through 19 where future fixtures need program, procedure, IEEE,
deleted-feature, or scope terms.

<!-- END GENERATED 16.9.176 -->
