# Fortran 2023 18.2.2: Named constants and derived types

The canonical catalogue is `doc/catalogues/iso_c_binding_named_constants_derived_types_18_2_2.json`. This source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6, p7, table18.1, p8, p9, note-unnumbered. Fine census units are recorded in the catalogue where mixed source units are split.

<!-- BEGIN GENERATED 18.2.2 -->

### S18.2.2-001: Table 18.2 ISO_C_BINDING entities are default integer named constants

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** The Table 18.2 entities C_SIGNED_CHAR, C_SHORT, C_INT, C_LONG, C_LONG_LONG, C_SIZE_T,
C_INT8_T, C_INT16_T, C_INT32_T, C_INT64_T, C_INT_LEAST8_T, C_INT_LEAST16_T,
C_INT_LEAST32_T, C_INT_LEAST64_T, C_INT_FAST8_T, C_INT_FAST16_T, C_INT_FAST32_T,
C_INT_FAST64_T, C_INTMAX_T, C_INTPTR_T, C_PTRDIFF_T, C_FLOAT, C_DOUBLE, C_LONG_DOUBLE,
C_FLOAT_COMPLEX, C_DOUBLE_COMPLEX, C_LONG_DOUBLE_COMPLEX, C_BOOL, C_CHAR are default
integer named constants.

**Diagnostic obligation:** not-required.

**Facets:** `table-18-2-default-integer-named-constants`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-001 Table 18.2 default-integer constants fixture: direct KIND inquiries on every
Table 18.2 constant expression are compared with KIND(0), proving they are default
integer named constants without asserting their processor-dependent values.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-001 Table 18.2 default-integer constants boundaries: Only the listed exact
single-image integer, logical and character effects are claimed. Processor-dependent
numeric kind values, pointer bit patterns, storage alignment, warning text, extra
ISO_C_BINDING public entities, C_F_STRPOINTER/F_C_STRING availability on a reference
that lacks them, ISO_Fortran_binding.h descriptor semantics, interoperable enumeration
transfer, negative optional-kind sentinel branches not manifest on this host,
C_LONG_DOUBLE declarations when the c-long-double-positive profile exits 77, and later
18.2.3 procedures remain pending or belong to sibling catalogues. Every owned facet has
a distinct assertion and conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-002: Fortran kinds named by ISO_C_BINDING share representations with interoperating C types

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** A Fortran intrinsic type with a kind value from ISO_C_BINDING has the same
representation as the corresponding C type for every value, with C_BOOL true and false
matching (_Bool)1 and (_Bool)0.

**Diagnostic obligation:** not-required.

**Facets:** `c-compatible-kind-representation`, `c-bool-true-false-representation`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-002 C representation round-trip fixture: integer(C_INT), real(C_FLOAT),
real(C_DOUBLE), real(C_LONG_DOUBLE), logical(C_BOOL), and character(C_CHAR) values are
passed by value to C functions of the corresponding C types and exact representable
nondefault results are returned. The _Bool branch observes C true/false as 1/0. The
fixture requires the c-long-double-positive processor profile before declaring
real(C_LONG_DOUBLE).

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-002 C representation round-trip boundaries: Only the listed exact single-image
integer, logical and character effects are claimed. Processor-dependent numeric kind
values, pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING
public entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-003: C_INT is always a valid Fortran integer kind value

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** C_INT shall be a valid value for an integer kind parameter on the processor.

**Diagnostic obligation:** not-required.

**Facets:** `c-int-valid-integer-kind`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-003 C_INT valid-kind fixture: direct KIND(0_C_INT) equals C_INT and an
integer(C_INT) sentinel assignment changes from -7_C_INT to 19_C_INT, proving C_INT is
accepted as an integer kind parameter.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-003 C_INT valid-kind boundaries: Only the listed exact single-image integer,
logical and character effects are claimed. Processor-dependent numeric kind values,
pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING public
entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-004: Optional C integer kind constants use valid kinds or exact negative sentinels

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** The listed C integer kind constants shall be valid Fortran integer kind values, -1 for a
defined C type without an interoperating Fortran kind, or -2 for an absent corresponding
C type.

**Diagnostic obligation:** not-required.

**Facets:** `optional-integer-kind-valid-branch`, `optional-integer-kind-minus-one-branch`, `optional-integer-kind-minus-two-branch`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-004 optional integer valid-kind branch fixture: the host valid branch is
exercised for optional C integer kind constants by declaring integer(k) values for
C_SHORT, C_LONG, C_LONG_LONG, C_SIZE_T and stdint-family constants that are positive on
both target toolchains. Negative sentinel branches are not observed on this host and
remain pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-004 optional integer valid-kind branch boundaries: Only the listed exact
single-image integer, logical and character effects are claimed. Processor-dependent
numeric kind values, pointer bit patterns, storage alignment, warning text, extra
ISO_C_BINDING public entities, C_F_STRPOINTER/F_C_STRING availability on a reference
that lacks them, ISO_Fortran_binding.h descriptor semantics, interoperable enumeration
transfer, negative optional-kind sentinel branches not manifest on this host,
C_LONG_DOUBLE declarations when the c-long-double-positive profile exits 77, and later
18.2.3 procedures remain pending or belong to sibling catalogues. Every owned facet has
a distinct assertion and conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-005: Numeric values of C integer kind constants are processor dependent apart from fixed relations

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF516. **Class:** Effect.

**Definition:** The actual positive integer kind values used for C integer types are processor choices;
portable tests assert only validity, equality to KIND results, and the exact negative
sentinel meanings.

**Diagnostic obligation:** not-required.

**Facets:** `integer-kind-numeric-value-latitude`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-006: C real kind constants use valid kinds or exact negative sentinels

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** C_FLOAT, C_DOUBLE, and C_LONG_DOUBLE shall be valid real kind values or the exact
sentinels -1, -2, -3, or -4 for the specified precision, range, combined, or other
no-kind cases.

**Diagnostic obligation:** not-required.

**Facets:** `real-kind-valid-branch`, `real-kind-minus-one-precision-branch`, `real-kind-minus-two-range-branch`, `real-kind-minus-three-precision-range-branch`, `real-kind-minus-four-other-branch`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-006 real valid-kind branch fixture: C_FLOAT, C_DOUBLE, and C_LONG_DOUBLE are
positive on both target toolchains; real(k) values 1.5 are passed through C float,
double, and long double functions and return exactly 2.5 with direct KIND inquiries. The
fixture requires the c-long-double-positive processor profile; negative real sentinel
branches remain pending.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-006 real valid-kind branch boundaries: Only the listed exact single-image
integer, logical and character effects are claimed. Processor-dependent numeric kind
values, pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING
public entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-007: C complex kind constants equal their corresponding real kind constants

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** C_FLOAT_COMPLEX, C_DOUBLE_COMPLEX, and C_LONG_DOUBLE_COMPLEX have the same values as
C_FLOAT, C_DOUBLE, and C_LONG_DOUBLE respectively.

**Diagnostic obligation:** not-required.

**Facets:** `float-complex-kind-equals-float`, `double-complex-kind-equals-double`, `long-double-complex-kind-equals-long-double`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-007 complex/real kind equality fixture: three independent logical assertions
compare C_FLOAT_COMPLEX with C_FLOAT, C_DOUBLE_COMPLEX with C_DOUBLE, and
C_LONG_DOUBLE_COMPLEX with C_LONG_DOUBLE. The fixture requires the
c-long-double-positive processor profile before asserting the long-double pair on
processors where C_LONG_DOUBLE is a valid kind.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-007 complex/real kind equality boundaries: Only the listed exact single-image
integer, logical and character effects are claimed. Processor-dependent numeric kind
values, pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING
public entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-008: Numeric values of C real and complex kind constants are processor dependent apart from fixed relations

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** The actual positive real and complex kind values are processor choices; portable tests
assert only valid-kind status, negative sentinel meanings, and mandated equalities.

**Diagnostic obligation:** not-required.

**Facets:** `real-kind-numeric-value-latitude`, `complex-kind-numeric-value-latitude`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-009: C_BOOL is a valid logical kind or exactly -1

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** C_BOOL shall be a valid logical kind parameter value or -1.

**Diagnostic obligation:** not-required.

**Facets:** `c-bool-valid-logical-kind-branch`, `c-bool-minus-one-branch`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-009 C_BOOL valid-kind fixture: C_BOOL is positive on both target toolchains;
logical(C_BOOL) true and false are passed to a C _Bool checker and exact 1/0
observations plus KIND(.TRUE._C_BOOL)==C_BOOL are asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-009 C_BOOL valid-kind boundaries: Only the listed exact single-image integer,
logical and character effects are claimed. Processor-dependent numeric kind values,
pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING public
entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-010: Numeric value of C_BOOL is processor dependent apart from the -1 sentinel

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** A positive C_BOOL kind value is a processor choice; portable tests assert only valid
logical kind status or the exact -1 sentinel.

**Diagnostic obligation:** not-required.

**Facets:** `c-bool-numeric-value-latitude`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-011: C_CHAR is a valid character kind or exactly -1 with branch meaning

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** C_CHAR shall be a valid character kind value or -1; a nonnegative value specifies the C
character kind and a negative value means no C character kind.

**Diagnostic obligation:** not-required.

**Facets:** `c-char-valid-character-kind-branch`, `c-char-minus-one-no-c-character-kind`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-011 C_CHAR valid-kind fixture: C_CHAR is nonnegative on both target toolchains;
character(kind=C_CHAR,len=1) values including C_NULL_CHAR and C_NEW_LINE are passed to a
C char checker, and KIND(C_NULL_CHAR)==C_CHAR is inquired directly on the constant
expression.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-011 C_CHAR valid-kind boundaries: Only the listed exact single-image integer,
logical and character effects are claimed. Processor-dependent numeric kind values,
pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING public
entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-012: Numeric value of C_CHAR is processor dependent apart from the -1 sentinel

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** A nonnegative C_CHAR kind value is a processor choice; portable tests assert only valid
character kind status or the exact -1 sentinel.

**Diagnostic obligation:** not-required.

**Facets:** `c-char-numeric-value-latitude`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-013: C special-character constants have length one, branch-selected kind, and Table 18.1 values

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** C_NULL_CHAR, C_ALERT, C_BACKSPACE, C_FORM_FEED, C_NEW_LINE, C_CARRIAGE_RETURN,
C_HORIZONTAL_TAB, and C_VERTICAL_TAB are length-one character named constants with the
kind and values specified by C_CHAR and Table 18.1.

**Diagnostic obligation:** not-required.

**Facets:** `c-special-character-named-constants`, `c-special-character-length-one`, `c-special-character-kind-branch`, `c-special-character-table-values`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-013 C special-character constants fixture: all eight Table 18.1 character
constants are imported and observed as length one; the positive C_CHAR branch checks
KIND(C_NULL_CHAR)==C_CHAR and a C companion compares the values with C escapes \0, \a,
\b, \f, \n, \r, \t, and \v.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-013 C special-character constants boundaries: Only the listed exact single-image
integer, logical and character effects are claimed. Processor-dependent numeric kind
values, pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING
public entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

### S18.2.2-014: C_NULL_PTR and C_NULL_FUNPTR are named constants with C null pointer values

**Source:** S18.2.2, J3/24-007, 18 December 2023, physical PDF517. **Class:** Effect.

**Definition:** C_NULL_PTR is a named constant of type C_PTR with the C NULL value, and C_NULL_FUNPTR is
a named constant of type C_FUNPTR with a C null function pointer value.

**Diagnostic obligation:** not-required.

**Facets:** `c-null-ptr-type-and-value`, `c-null-funptr-type-and-value`.

**Oracle:** Plans are pending and source-derived. Positive plans establish a non-default state first
and assert only portable Clause 18 properties: ISO_C_BINDING accessibility, exact
named-constant relations, exact logical results, pointer association status, shape and
lower-bound results, values recovered through C_LOC/C_F_POINTER round trips, or mixed
Fortran/C calls through declared interoperable interfaces. Processor-dependent kind
values, pointer bit patterns, C address numeric values, companion-processor
availability, storage alignment, and C library details are not asserted.

S18.2.2-014 null pointer constants fixture: C_NULL_PTR and C_NULL_FUNPTR are assigned to
scalar C_PTR/C_FUNPTR variables that previously held nonnull C_LOC/C_FUNLOC values, and
C_ASSOCIATED then returns .false. for each null value.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Unnumbered argument restrictions do not create diagnostic obligations. Negative source
mutations are non-diagnostic unless a future numbered rule requires rejection and the
rejection is portably observable.

S18.2.2-014 null pointer constants boundaries: Only the listed exact single-image
integer, logical and character effects are claimed. Processor-dependent numeric kind
values, pointer bit patterns, storage alignment, warning text, extra ISO_C_BINDING
public entities, C_F_STRPOINTER/F_C_STRING availability on a reference that lacks them,
ISO_Fortran_binding.h descriptor semantics, interoperable enumeration transfer, negative
optional-kind sentinel branches not manifest on this host, C_LONG_DOUBLE declarations
when the c-long-double-positive profile exits 77, and later 18.2.3 procedures remain
pending or belong to sibling catalogues. Every owned facet has a distinct assertion and
conforming feature mutation in this generator.

**Dependencies:** Registered dependencies: clauses 6, 7, 8, 9, 10, 11, 12, 15, 16, and 17 for source form,
types, declarations, expressions, execution, modules, procedure interfaces,
C_LOC/C_SIZEOF and intrinsic procedure foundations, and IEEE/reference support.
Unregistered dependencies explicitly retained: later Clause 18 interoperability
conditions, ISO_C_BINDING procedures after 18.2.3.3, ISO_Fortran_binding.h descriptor
semantics, and companion C processor details.

<!-- END GENERATED 18.2.2 -->
