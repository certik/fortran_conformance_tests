# Fortran 2023 16.9.141: MIN

The canonical catalogue is `doc/catalogues/min_intrinsic_16_9_141.json`. Effective review state comes from `Registry.catalogue_review_state("16.9.141")`; this source packet records source accounting and pending plans only, not fixture approval.

Authority: original J3/24-007, 18 December 2023, 688 pages, SHA-256 `7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`.

Base units read and accounted: p1, p2, p3, p4, p5, p6. Fine census units are recorded in the catalogue where mixed or compound source units are split.

<!-- BEGIN GENERATED 16.9.141 -->

### S16.9.141-001: MIN argument restrictions

**Source:** S16.9.141, J3/24-007, 18 December 2023, physical PDF434. **Class:** Restriction.

**Definition:** All MIN arguments shall have the same type, that type shall be integer, real, or
character, and all arguments shall have the same kind type parameter.

**Diagnostic obligation:** not-required.

**Facets:** `MIN-arguments-same-type`, `MIN-arguments-integer-real-or-character`, `MIN-arguments-same-kind`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ found incidental MIN use in helper code, finalization fixtures,
array-section helpers, and DO CONCURRENT reduction controls;
tests/fixtures/*/fixture.json has no MIN binding and no S16.9.141 binding.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.141-002: MIN result type, kind, and character length

**Source:** S16.9.141, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** The MIN result has the same type and kind as the arguments, and for character arguments
its length is the length of the longest argument.

**Diagnostic obligation:** not-required.

**Facets:** `MIN-result-same-type-and-kind`, `MIN-character-result-longest-length`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ found incidental MIN use in helper code, finalization fixtures,
array-section helpers, and DO CONCURRENT reduction controls;
tests/fixtures/*/fixture.json has no MIN binding and no S16.9.141 binding.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

### S16.9.141-003: MIN returns the smallest argument and pads shorter character results

**Source:** S16.9.141, J3/24-007, 18 December 2023, physical PDF434. **Class:** Effect.

**Definition:** MIN returns the smallest argument, using intrinsic relational operators for character
arguments and right-blank extension to the longest argument length for a selected
shorter character argument.

**Diagnostic obligation:** not-required.

**Facets:** `MIN-integer-smallest-exact`, `MIN-real-smallest-exact`, `MIN-character-selected-by-relational-operators`, `MIN-character-shorter-selected-blank-extended`.

**Oracle:** Source registration only. Future fixtures initialize result variables to non-default or
opposite sentinels before invoking the intrinsic and assert only properties fixed by
16.9: exact logical, integer, kind, character length, rank, shape, selected argument, or
bit-pattern observations. Processor-dependent model values, approximations, and
character collating details outside the 7.4.4.4 guaranteed default-character order are
separated from exact result obligations. Unnumbered argument shall-restrictions are
source-control restrictions only and are not promoted to diagnostic facets.

**Oracle limitation:** Source accounting only. This packet creates no Fortran fixture, compiler invocation,
execution evidence, evidence link, fixture approval, oracle approval, or coverage claim.
Grep of tests/ found incidental MIN use in helper code, finalization fixtures,
array-section helpers, and DO CONCURRENT reduction controls;
tests/fixtures/*/fixture.json has no MIN binding and no S16.9.141 binding.

**Dependencies:** Registered dependencies: existing source-form, type, declaration, expression,
assignment, execution, array, character, and intrinsic-operation foundations from
clauses 6 through 13 where already catalogued. Unregistered dependencies retained: 7.7
BOZ literal context, 16.3 integer bit model, 16.4 numeric model terminology, sibling
16.9 intrinsics used by pending plans such as IAND, IOR, NOT, INT, BTEST, POPCNT, KIND,
LEN, PACK, HUGE, SHAPE, and relational operators, plus clauses 14 through 19 where
future fixtures need program, procedure, IEEE, deleted-feature, or scope terms.

<!-- END GENERATED 16.9.141 -->
