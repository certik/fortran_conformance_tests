# Fortran 2023: 10.1.5.4.1 Interpretation of logical intrinsic operations

Catalogue: `doc/catalogues/logical_intrinsic_operation_interpretation_10_1_5_4_1.json`. The original source-only registration supplied pending plans; the bounded runtime fixtures below supply selected cases and mutation plans, not oracle approval, fixture approval, source-review renewal, or universal coverage.

<!-- BEGIN LOGICAL INTRINSIC INTERPRETATION FIXTURES -->
## Logical intrinsic interpretation runtime observations

Six complete run/effect/f2023 programs cover 21 pending facets: the p1 logical-computation and result-type witnesses, both .NOT. rows, and the full TT/TF/FT/FF truth tables for .AND., .OR., .EQV. and .NEQV. The p1 result-type source passes the operation expressions directly to LOGICAL dummy arguments; no declared result variable launders the expression type. All truth-table guards use literal operands in direct expressions and hand-derived branch oracles.

The binary-operator programs intentionally assert every row. The generated discrimination table names a separating row for each operator and each alternative, including the load-bearing FF separation of .AND. from .EQV. and the load-bearing TT separation of .OR. from .NEQV. The permanent mutation campaign includes wrong-oracle mutations for every guard and completion literal, row-input mutations, whole-observation omissions, a nonzero reverse check-sentinel mutation, and all 12 binary operator substitutions.

Only operand-type-delegation remains pending because p1 delegates permitted operand types to 10.1.5.1. The fixtures make no short-circuit, side-effect, reassociation, array, nondefault-kind, defined-operator, diagnostic or evaluation-order claim. No evidence link, baseline update, SourceUse renewal, catalogue source-review renewal or approval is created.
<!-- END LOGICAL INTRINSIC INTERPRETATION FIXTURES -->

<!-- BEGIN GENERATED 10.1.5.4.1 -->

### S10.1.5.4.1-001: Logical intrinsic operations express logical computations and produce logical results

**Source:** 10.1.5.4.1 p1; J3/24-007 PDF p179. **Class:** Effect.

**Definition:** A logical intrinsic operation expresses a logical computation and, when evaluated,
produces a result of type logical. The permitted operand types are not redefined here;
they are the logical operand rules specified in 10.1.5.1.

**Diagnostic obligation:** not-required.

**Facets:** `logical-computation-context`, `logical-result-type`, `operand-type-delegation`.

**Oracle:** Use only literal logical values or standard logical inquiries. Truth-table controls must
not contain operand side effects, function-call counters, undefined branches, or any
assertion that .AND. or .OR. skipped an operand; Fortran does not require short-circuit
evaluation. Grouping of an associative intrinsic logical operation is not
value-observable, so any future grouping witness must be a valid non-associative defined
extension attributed to 10.1.6 and 15.4.3.4.2.

S10.1.5.4.1-001 logical intrinsic interpretation runtime fixtures: one complete
run/effect/f2023 program uses intrinsic logical operations directly in an IF context and
as actual arguments to LOGICAL dummy arguments. The context witness checks the
expression (.FALSE. .OR. .TRUE.) in the IF itself. The result-type witnesses pass
(.TRUE. .EQV. .FALSE.) and (.NOT. .FALSE.) directly to LOGICAL dummy arguments whose
bodies check the hand-derived truth values. No result is first assigned to a declared
LOGICAL variable whose declaration would launder the expression type.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal,
inquiry, or source-repair oracles and must not infer evaluation order, short-circuiting,
which operand was evaluated, or whether an operand was evaluated. They must not rely on
compiler agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or
a floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted
evaluation or mathematically equivalent rewriting.

S10.1.5.4.1-001 logical intrinsic interpretation fixture boundaries: only
logical-computation-context and logical-result-type are represented.
operand-type-delegation remains pending because 10.1.5.4.1 p1 delegates permitted
operand types to 10.1.5.1; this packet does not create nonlogical-operand
diagnostic/control claims. The result-type witnesses use default logical literal
operands and LOGICAL dummy arguments, not representation bits or mixed-kind selection.

**Dependencies:** 7.4.5;10.1.2.8R1015-R1022;10.1.3;10.1.4;10.1.5.1;10.1.9.3 p4;16.9.118 KIND.

### S10.1.5.4.1-002: .NOT. is logical negation

**Source:** 10.1.5.4.1 Table 10.5 .NOT. row and Table 10.6 .NOT. column; J3/24-007 PDF p179. **Class:** Effect.

**Definition:** The logical intrinsic operator .NOT. is a unary logical negation; .NOT. x2 is true if x2
is false and false if x2 is true.

**Diagnostic obligation:** not-required.

**Facets:** `not-true-is-false`, `not-false-is-true`, `not-table10-5-10-6-consistency`.

**Oracle:** Use only literal logical values or standard logical inquiries. Truth-table controls must
not contain operand side effects, function-call counters, undefined branches, or any
assertion that .AND. or .OR. skipped an operand; Fortran does not require short-circuit
evaluation. Grouping of an associative intrinsic logical operation is not
value-observable, so any future grouping witness must be a valid non-associative defined
extension attributed to 10.1.6 and 15.4.3.4.2.

S10.1.5.4.1-002 logical intrinsic interpretation runtime fixtures: one complete
run/effect/f2023 program asserts both rows of the unary .NOT. truth table directly:
.NOT. .TRUE. is false and .NOT. .FALSE. is true. The same source ties the Table 10.5
negation interpretation to both Table 10.6 rows.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal,
inquiry, or source-repair oracles and must not infer evaluation order, short-circuiting,
which operand was evaluated, or whether an operand was evaluated. They must not rely on
compiler agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or
a floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted
evaluation or mathematically equivalent rewriting.

S10.1.5.4.1-002 logical intrinsic interpretation fixture boundaries: only the two value
rows and their Table 10.5/Table 10.6 consistency are represented. The fixture does not
claim parsing ownership for R1019, nondefault kind coverage, or any evaluation-order
property.

**Dependencies:** 7.4.5;10.1.2.8R1019;10.1.3;10.1.5.1 S10.1.5.1-005;10.2.1.3.

### S10.1.5.4.1-003: .AND. is logical conjunction

**Source:** 10.1.5.4.1 Table 10.5 .AND. row and Table 10.6 .AND. column; J3/24-007 PDF p179. **Class:** Effect.

**Definition:** The logical intrinsic operator .AND. is true if and only if both x1 and x2 are true.

**Diagnostic obligation:** not-required.

**Facets:** `and-true-true`, `and-true-false`, `and-false-true`, `and-false-false`.

**Oracle:** Use only literal logical values or standard logical inquiries. Truth-table controls must
not contain operand side effects, function-call counters, undefined branches, or any
assertion that .AND. or .OR. skipped an operand; Fortran does not require short-circuit
evaluation. Grouping of an associative intrinsic logical operation is not
value-observable, so any future grouping witness must be a valid non-associative defined
extension attributed to 10.1.6 and 15.4.3.4.2.

S10.1.5.4.1-003 logical intrinsic interpretation runtime fixtures: one complete
run/effect/f2023 program asserts the full .AND. table TT/T, TF/F, FT/F and FF/F with
direct literal expressions. The FF row is present specifically to separate .AND. from
.EQV., and the generated operator-substitution campaign replaces all four .AND. operator
sites with each of .OR., .EQV. and .NEQV.; every replacement must fail.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal,
inquiry, or source-repair oracles and must not infer evaluation order, short-circuiting,
which operand was evaluated, or whether an operand was evaluated. They must not rely on
compiler agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or
a floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted
evaluation or mathematically equivalent rewriting.

S10.1.5.4.1-003 logical intrinsic interpretation fixture boundaries: only scalar
default-logical literal .AND. interpretation is represented. No short-circuit,
side-effect, array, reassociation, kind-selection, defined-operator, diagnostic or
evaluation-order claim is made.

**Dependencies:** 7.4.5;10.1.2.8R1020;10.1.3;10.1.4;10.1.5.1 S10.1.5.1-005;10.1.5.4.2;10.2.1.3.

### S10.1.5.4.1-004: .OR. is logical inclusive disjunction

**Source:** 10.1.5.4.1 Table 10.5 .OR. row and Table 10.6 .OR. column; J3/24-007 PDF p179. **Class:** Effect.

**Definition:** The logical intrinsic operator .OR. is inclusive disjunction; x1 .OR. x2 is true if x1
or x2 or both are true, and false only when both operands are false.

**Diagnostic obligation:** not-required.

**Facets:** `or-true-true`, `or-true-false`, `or-false-true`, `or-false-false`.

**Oracle:** Use only literal logical values or standard logical inquiries. Truth-table controls must
not contain operand side effects, function-call counters, undefined branches, or any
assertion that .AND. or .OR. skipped an operand; Fortran does not require short-circuit
evaluation. Grouping of an associative intrinsic logical operation is not
value-observable, so any future grouping witness must be a valid non-associative defined
extension attributed to 10.1.6 and 15.4.3.4.2.

S10.1.5.4.1-004 logical intrinsic interpretation runtime fixtures: one complete
run/effect/f2023 program asserts the full inclusive .OR. table TT/T, TF/T, FT/T and FF/F
with direct literal expressions. The TT row is present specifically to separate .OR.
from .NEQV., and the generated operator-substitution campaign replaces all four .OR.
operator sites with each of .AND., .EQV. and .NEQV.; every replacement must fail.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal,
inquiry, or source-repair oracles and must not infer evaluation order, short-circuiting,
which operand was evaluated, or whether an operand was evaluated. They must not rely on
compiler agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or
a floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted
evaluation or mathematically equivalent rewriting.

S10.1.5.4.1-004 logical intrinsic interpretation fixture boundaries: only scalar
default-logical literal .OR. interpretation is represented. No short-circuit,
side-effect, array, reassociation, kind-selection, defined-operator, diagnostic or
evaluation-order claim is made.

**Dependencies:** 7.4.5;10.1.2.8R1021;10.1.3;10.1.4;10.1.5.1 S10.1.5.1-005;10.1.5.4.2;10.2.1.3.

### S10.1.5.4.1-005: .EQV. is logical equivalence

**Source:** 10.1.5.4.1 Table 10.5 .EQV. row and Table 10.6 .EQV. column; J3/24-007 PDF p179. **Class:** Effect.

**Definition:** The logical intrinsic operator .EQV. is true when x1 and x2 have the same logical value,
both true or both false, and false otherwise.

**Diagnostic obligation:** not-required.

**Facets:** `eqv-true-true`, `eqv-true-false`, `eqv-false-true`, `eqv-false-false`.

**Oracle:** Use only literal logical values or standard logical inquiries. Truth-table controls must
not contain operand side effects, function-call counters, undefined branches, or any
assertion that .AND. or .OR. skipped an operand; Fortran does not require short-circuit
evaluation. Grouping of an associative intrinsic logical operation is not
value-observable, so any future grouping witness must be a valid non-associative defined
extension attributed to 10.1.6 and 15.4.3.4.2.

S10.1.5.4.1-005 logical intrinsic interpretation runtime fixtures: one complete
run/effect/f2023 program asserts the full .EQV. table TT/T, TF/F, FT/F and FF/T with
direct literal expressions. The FF row separates .EQV. from .AND. and the generated
operator-substitution campaign replaces all four .EQV. sites with .AND., .OR. and
.NEQV.; every replacement must fail.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal,
inquiry, or source-repair oracles and must not infer evaluation order, short-circuiting,
which operand was evaluated, or whether an operand was evaluated. They must not rely on
compiler agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or
a floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted
evaluation or mathematically equivalent rewriting.

S10.1.5.4.1-005 logical intrinsic interpretation fixture boundaries: only scalar
default-logical literal .EQV. interpretation is represented. No precedence,
kind-selection, defined-operator, array, diagnostic or evaluation-order claim is made.

**Dependencies:** 7.4.5;10.1.2.8R1022;10.1.3;10.1.5.1 S10.1.5.1-005;10.2.1.3.

### S10.1.5.4.1-006: .NEQV. is logical nonequivalence

**Source:** 10.1.5.4.1 Table 10.5 .NEQV. row and Table 10.6 .NEQV. column; J3/24-007 PDF p179. **Class:** Effect.

**Definition:** The logical intrinsic operator .NEQV. is true when exactly one of x1 and x2 is true, and
false when the operands have the same logical value.

**Diagnostic obligation:** not-required.

**Facets:** `neqv-true-true`, `neqv-true-false`, `neqv-false-true`, `neqv-false-false`.

**Oracle:** Use only literal logical values or standard logical inquiries. Truth-table controls must
not contain operand side effects, function-call counters, undefined branches, or any
assertion that .AND. or .OR. skipped an operand; Fortran does not require short-circuit
evaluation. Grouping of an associative intrinsic logical operation is not
value-observable, so any future grouping witness must be a valid non-associative defined
extension attributed to 10.1.6 and 15.4.3.4.2.

S10.1.5.4.1-006 logical intrinsic interpretation runtime fixtures: one complete
run/effect/f2023 program asserts the full .NEQV. table TT/F, TF/T, FT/T and FF/F with
direct literal expressions. The TT row separates .NEQV. from .OR. and .EQV., and the
generated operator-substitution campaign replaces all four .NEQV. sites with .AND., .OR.
and .EQV.; every replacement must fail.

**Oracle limitation:** The original source-only catalogue recorded pending plans only and created no Fortran
test program, compiler invocation, fixture approval, oracle approval, or coverage claim;
this generator supplies selected runtime fixtures and mutation plans without granting
those approvals or claims. Future fixtures must use independently derived literal,
inquiry, or source-repair oracles and must not infer evaluation order, short-circuiting,
which operand was evaluated, or whether an operand was evaluated. They must not rely on
compiler agreement, address arithmetic, TRANSFER, emitted assembly, undefined reads, or
a floating-point result that 10.1.7 or 10.1.8 permits to differ through omitted
evaluation or mathematically equivalent rewriting.

S10.1.5.4.1-006 logical intrinsic interpretation fixture boundaries: only scalar
default-logical literal .NEQV. interpretation is represented. No precedence,
kind-selection, defined-operator, array, diagnostic or evaluation-order claim is made.

**Dependencies:** 7.4.5;10.1.2.8R1022;10.1.3;10.1.5.1 S10.1.5.1-005;10.2.1.3.

<!-- END GENERATED 10.1.5.4.1 -->
