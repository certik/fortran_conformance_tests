{
  "schema_version": 1,
  "id": "C769_invalid__initialization_strided",
  "rule": "C769",
  "facets": [
    "noncontiguous-target"
  ],
  "standard": "f2023",
  "evidence": "effect",
  "files": [
    "source.f90"
  ],
  "build": [
    {
      "id": "source",
      "source": "source.f90",
      "language": "fortran",
      "form": "free",
      "output": "source.o"
    }
  ],
  "expect": {
    "phase": "compile",
    "step": "source",
    "outcome": "diagnose",
    "diagnostic": {
      "file": "source.f90",
      "line": 8,
      "contains_any": [
        "CONTIGUOUS pointer component 'alias' requires a contiguous initial target",
        "Assignment to contiguous pointer from non-contiguous target",
        "Contiguous pointer 'alias' cannot be associated with a noncontiguous target",
        "CONTIGUOUS pointer may not be associated with a discontiguous target"
      ],
      "excludes_any": [
        "not implemented",
        "not yet implemented",
        "unimplemented",
        "not supported",
        "unsupported",
        "internal compiler error"
      ]
    }
  }
}
