{
  "schema_version": 1,
  "id": "C701_invalid__nonconstant:dummy-expression",
  "rule": "C701",
  "facets": ["nonconstant-expression"],
  "evidence": "effect",
  "files": ["source.f90"],
  "build": [
    {"id": "source", "source": "source.f90", "language": "fortran", "form": "free", "output": "source.o"}
  ],
  "expect": {
    "phase": "compile",
    "step": "source",
    "outcome": "diagnose",
    "diagnostic": {
      "file": "source.f90", "line": 10,
      "contains_any": ["does not reduce to a constant expression", "must be constant", "cannot either be ASSUMED or DEFERRED"]
    }
  }
}
