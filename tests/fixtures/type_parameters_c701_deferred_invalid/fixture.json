{
  "schema_version": 1,
  "id": "C701_invalid__deferred:colon",
  "rule": "C701",
  "facets": ["deferred-kind"],
  "evidence": "effect",
  "files": ["source.f90"],
  "build": [
    {"id": "source", "source": "source.f90", "language": "fortran", "form": "free", "output": "source.o"}
  ],
  "expect": {
    "phase": "compile",
    "step": "source",
    "outcome": "diagnose",
    "diagnostic": {
      "file": "source.f90", "line": 9,
      "contains_any": ["does not reduce to a constant expression", "must be constant", "cannot either be ASSUMED or DEFERRED"]
    }
  }
}
